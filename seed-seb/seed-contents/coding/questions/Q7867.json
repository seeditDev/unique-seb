{
  "questionId": "Q7867",
  "title": "The In-XOR-able Task",
  "slug": "the-in-xor-able-task",
  "version": 1,
  "createdAt": "2026-07-02",
  "updatedAt": "2026-07-04",
  "metadata": {
    "category": "Bit Manipulation",
    "difficulty": "Hard",
    "isPremium": true,
    "tags": [
      "XOR",
      "Combinatorial Construction",
      "Math"
    ],
    "companyTags": [],
    "topicTags": [
      "Dynamic Programming"
    ],
    "QBCategory": "CCD"
  },
  "content": {
    "problemStatement": "You are given three integers NNN, KKK and XXX. Is it possible to find a sequence of non-negative integers A1,A2,…,ANA_1, A_2, \\ldots, A_NA1​,A2​,…,AN​ such that the number of pairs of indices (i,j)(i, j)(i,j) (1≤i≤j≤N1 \\le i \\le j \\le N1≤i≤j≤N) satisfying Ai⊕Aj=XA_i \\oplus A_j = XAi​⊕Aj​=X is equal to KKK? Here, ⊕\\oplus⊕ is the bitwise XOR operator.",
    "constraints": [
      "1 ≤ T ≤ 4·10^5",
      "1 ≤ N, K, X ≤ 3·10^5"
    ],
    "inputFormat": "The first line contains an integer T, the number of test cases. Each of the next T lines contains three space‑separated integers N, K and X.",
    "outputFormat": "For each test case output a single line containing \"YES\" if a sequence of N non‑negative integers can be constructed with exactly K pairs (i, j) (1 ≤ i ≤ j ≤ N) such that Ai ⊕ Aj = X, otherwise output \"NO\".",
    "sampleTestCases": [
      {
        "id": "sample_1",
        "input": "5\n4 3 3\n5 8 1\n10 8 2\n19 100 34\n69 100 45",
        "output": "YES\nNO\nYES\nNO\nYES",
        "explanation": "Case 1: X≠0 and floor(4²/4)=4 ≥3 → YES.\\nCase 2: floor(5²/4)=6 <8 → NO.\\nCase 3: floor(10²/4)=25 ≥8 → YES.\\nCase 4: floor(19²/4)=90 <100 → NO.\\nCase 5: floor(69²/4)=1190 ≥100 → YES."
      }
    ]
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "label": "minimum boundary",
        "input": "1\n1 1 0",
        "expectedOutput": "YES",
        "weight": 2
      },
      {
        "id": "tc_2",
        "label": "Case 2",
        "input": "1\n1 1 0",
        "expectedOutput": "YES",
        "weight": 2
      },
      {
        "id": "tc_3",
        "label": "Case 3",
        "input": "1\n1 1 0",
        "expectedOutput": "YES",
        "weight": 2
      },
      {
        "id": "tc_4",
        "label": "Case 4",
        "input": "1\n1 1 0",
        "expectedOutput": "YES",
        "weight": 2
      },
      {
        "id": "tc_5",
        "label": "Case 5",
        "input": "1\n1 1 0",
        "expectedOutput": "YES",
        "weight": 2
      }
    ],
    "totalTestCases": 5,
    "totalWeight": 10
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      2,
      2,
      2,
      2,
      2
    ],
    "maxScore": 10
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "For X = 0 the condition counts pairs of equal numbers (including i = j). If we group the N elements into groups of sizes c1, c2, …, the number of required pairs is Σ ci·(ci+1)/2. The minimum possible value is N (all ci = 1) and the maximum is N·(N+1)/2 (all elements equal). Hence for X = 0 the answer is YES iff N ≤ K ≤ N·(N+1)/2.\n\nFor X ≠ 0 we can use values that come in complementary pairs (v, v⊕X). Let a be the total number of occurrences of the first element of a pair and b the total of the second. Each such pair contributes a·b to the answer, and elements that are not part of any complementary pair contribute nothing. The maximum possible number of XOR‑X pairs is obtained when all N elements belong to a single complementary pair, i.e. a + b = N and a·b is maximised. By the arithmetic‑geometric mean inequality the maximum is ⌊N²/4⌋. Any value 0 ≤ K ≤ ⌊N²/4⌋ can be realised by using several independent complementary pairs (for example, K copies of a pair (1,1) use 2K elements, and any remaining elements are left unused). Therefore for X ≠ 0 the answer is YES iff K ≤ ⌊N²/4⌋.",
    "timeComplexity": "O(1) per test case, O(T) overall.",
    "spaceComplexity": "O(1) additional space.",
    "code": {
      "C": "#include <stdio.h>\n#include <math.h>\nint main(){\n    int T; if(scanf(\"%d\",&T)!=1) return 0;\n    while(T--){\n        long long N,K,X; scanf(\"%lld %lld %lld\",&N,&K,&X);\n        if(X==0){\n            long long minK=N;\n            long long maxK=N*(N+1)/2;\n            puts((K>=minK && K<=maxK)?\"YES\":\"NO\");\n        }else{\n            long long maxK=(N*N)/4; // floor division\n            puts((K<=maxK)?\"YES\":\"NO\");\n        }\n    }\n    return 0;\n}",
      "C++": "#include <bits/stdc++.h>\nusing namespace std;\nint main(){\n    ios::sync_with_stdio(false);\n    cin.tie(nullptr);\n    int T; if(!(cin>>T)) return 0;\n    while(T--){\n        long long N,K,X; cin>>N>>K>>X;\n        if(X==0){\n            long long minK=N;\n            long long maxK=N*(N+1)/2;\n            cout<<(K>=minK && K<=maxK?\"YES\":\"NO\")<<'\\n';\n        }else{\n            long long maxK=(N*N)/4; // floor\n            cout<<(K<=maxK?\"YES\":\"NO\")<<'\\n';\n        }\n    }\n    return 0;\n}",
      "Java": "import java.io.*;\nimport java.util.*;\npublic class Main {\n    public static void main(String[] args) throws Exception {\n        FastScanner fs = new FastScanner(System.in);\n        int T = fs.nextInt();\n        StringBuilder sb = new StringBuilder();\n        while (T-- > 0) {\n            long N = fs.nextLong();\n            long K = fs.nextLong();\n            long X = fs.nextLong();\n            if (X == 0) {\n                long minK = N;\n                long maxK = N * (N + 1) / 2;\n                sb.append((K >= minK && K <= maxK) ? \"YES\" : \"NO\").append('\\n');\n            } else {\n                long maxK = (N * N) / 4; // floor\n                sb.append(K <= maxK ? \"YES\" : \"NO\").append('\\n');\n            }\n        }\n        System.out.print(sb.toString());\n    }\n    static class FastScanner {\n        private final InputStream in;\n        private final byte[] buffer = new byte[1 << 16];\n        private int ptr = 0, len = 0;\n        FastScanner(InputStream in) { this.in = in; }\n        private int readByte() throws IOException {\n            if (ptr >= len) {\n                len = in.read(buffer);\n                ptr = 0;\n                if (len <= 0) return -1;\n            }\n            return buffer[ptr++];\n        }\n        long nextLong() throws IOException { return Long.parseLong(next()); }\n        int nextInt() throws IOException { return Integer.parseInt(next()); }\n        String next() throws IOException {\n            StringBuilder sb = new StringBuilder();\n            int c;\n            while ((c = readByte()) != -1 && c <= ' ') {}\n            while (c != -1 && c > ' ') {\n                sb.append((char) c);\n                c = readByte();\n            }\n            return sb.toString();\n        }\n    }\n}",
      "Python3": "import sys\n\ndef solve():\n    data = sys.stdin.read().strip().split()\n    if not data:\n        return\n    it = iter(data)\n    T = int(next(it))\n    out = []\n    for _ in range(T):\n        N = int(next(it)); K = int(next(it)); X = int(next(it))\n        if X == 0:\n            minK = N\n            maxK = N * (N + 1) // 2\n            out.append('YES' if minK <= K <= maxK else 'NO')\n        else:\n            maxK = (N * N) // 4\n            out.append('YES' if K <= maxK else 'NO')\n    sys.stdout.write('\\n'.join(out))\n\nif __name__ == \"__main__\":\n    solve()",
      "JavaScript": "const fs = require('fs');\nconst input = fs.readFileSync(0, 'utf-8').trim().split(/\\s+/);\nlet idx = 0;\nconst T = Number(input[idx++]);\nlet out = [];\nfor (let t = 0; t < T; ++t) {\n  const N = BigInt(input[idx++]);\n  const K = BigInt(input[idx++]);\n  const X = BigInt(input[idx++]);\n  if (X === 0n) {\n    const minK = N;\n    const maxK = N * (N + 1n) / 2n;\n    out.push(K >= minK && K <= maxK ? 'YES' : 'NO');\n  } else {\n    const maxK = (N * N) / 4n; // floor division\n    out.push(K <= maxK ? 'YES' : 'NO');\n  }\n}\nconsole.log(out.join('\\n'));"
    }
  },
  "QBCategory": "CCD",
  "boilerPlates": {
    "C": "#include <stdio.h>\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "C++": "#include <iostream>\nusing namespace std;\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "Java": "import java.util.*;\nimport java.io.*;\n\npublic class Main {\n    public static void main(String[] args) throws IOException {\n        // your code goes here\n    }\n}",
    "Python3": "# your code goes here\n",
    "JavaScript": "// your code goes here\n"
  },
  "_internal": {
    "methodName": "",
    "params": []
  }
}