{
  "questionId": "Q11900",
  "title": "Minimum number of bracket reversals to make an expression balanced",
  "slug": "minimum-add-to-make-parentheses-valid",
  "version": 1,
  "createdAt": "2026-07-05",
  "updatedAt": "2026-07-05",
  "metadata": {
    "category": "String Manipulation",
    "difficulty": "Hard",
    "isPremium": true,
    "tags": [
      "Balanced Brackets",
      "Reversals"
    ],
    "companyTags": [],
    "topicTags": [],
    "QBCategory": "CCD"
  },
  "content": {
    "problemStatement": "Given a string s consisting of only opening and closing brackets '(' and ')', find out the minimum number of reversals required to convert the string into a balanced expression.",
    "constraints": [
      "1 <= length of string s <= 10^5",
      "String s consists of only '(' and ')'"
    ],
    "inputFormat": "A string s consisting of only opening and closing brackets '(' and ')'",
    "outputFormat": "The minimum number of reversals required to convert the string into a balanced expression",
    "sampleTestCases": []
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "label": "Minimum boundary",
        "input": "()",
        "expectedOutput": "0",
        "weight": 2
      },
      {
        "id": "tc_2",
        "label": "Case 2",
        "input": "()",
        "expectedOutput": "0",
        "weight": 2
      },
      {
        "id": "tc_3",
        "label": "Case 3",
        "input": "()",
        "expectedOutput": "0",
        "weight": 2
      },
      {
        "id": "tc_4",
        "label": "Case 4",
        "input": "()",
        "expectedOutput": "0",
        "weight": 2
      },
      {
        "id": "tc_5",
        "label": "Case 5",
        "input": "()",
        "expectedOutput": "0",
        "weight": 2
      }
    ],
    "totalTestCases": 5,
    "totalWeight": 10
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      2,
      2,
      2,
      2,
      2
    ],
    "maxScore": 10
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "Counting unmatched brackets and calculating minimum reversals",
    "timeComplexity": "O(n)",
    "spaceComplexity": "O(1)",
    "code": {
      "C++": "#include <bits/stdc++.h>\nusing namespace std;\n\nclass Solution {\npublic:\n    // Function to calculate the minimum number of reversals to balance the expression\n    int minReversalsToBalance(string expression) {\n        // Count of unmatched '(' brackets\n        int openBrackets = 0;\n        \n        // Count of unmatched ')' brackets\n        int closeBrackets = 0;\n\n        // Traverse the string\n        for (char ch : expression) {\n            if (ch == '(') {\n                // Consider '(' as unmatched for now\n                openBrackets++;\n            } else {\n                if (openBrackets > 0) {\n                    // Match this ')' with a previous '('\n                    openBrackets--;\n                } else {\n                    // No matching '(' exists, so this ')' is unmatched\n                    closeBrackets++;\n                }\n            }\n        }\n\n        // If total number of unmatched brackets is odd, return -1\n        if ((openBrackets + closeBrackets) % 2 != 0) return -1;\n\n        // Return minimum reversals required\n        return (openBrackets + 1) / 2 + (closeBrackets + 1) / 2;\n    }\n};\n\nint main() {\n    // Input expression\n    string expression = \"(()))(\";\n\n    // Create object of Solution\n    Solution solver;\n\n    // Get result from function\n    int result = solver.minReversalsToBalance(expression);\n\n    // Print the result\n    cout << \"Minimum reversals required: \" << result << endl;\n\n    return 0;\n}",
      "Java": "public class Solution {\n    // Function to calculate the minimum number of reversals to balance the expression\n    public int minReversalsToBalance(String expression) {\n        // Count of unmatched '(' brackets\n        int openBrackets = 0;\n\n        // Count of unmatched ')' brackets\n        int closeBrackets = 0;\n\n        // Traverse the string\n        for (char ch : expression.toCharArray()) {\n            if (ch == '(') {\n                // Consider '(' as unmatched for now\n                openBrackets++;\n            } else {\n                if (openBrackets > 0) {\n                    // Match this ')' with a previous '('\n                    openBrackets--;\n                } else {\n                    // No matching '(' exists, so this ')' is unmatched\n                    closeBrackets++;\n                }\n            }\n        }\n\n        // If total number of unmatched brackets is odd, return -1\n        if ((openBrackets + closeBrackets) % 2 != 0) return -1;\n\n        // Return minimum reversals required\n        return (openBrackets + 1) / 2 + (closeBrackets + 1) / 2;\n    }\n}\n\npublic class Main {\n    public static void main(String[] args) {\n        // Input expression\n        String expression = \"(()))(\";\n\n        // Create object of Solution\n        Solution solver = new Solution();\n\n        // Get result from function\n        int result = solver.minReversalsToBalance(expression);\n\n        // Print the result\n        System.out.println(\"Minimum reversals required: \" + result);\n    }\n}",
      "Python3": "class Solution:\n    # Function to calculate the minimum number of reversals to balance the expression\n    def minReversalsToBalance(self, expression: str) -> int:\n        # Count of unmatched '(' brackets\n        open_brackets = 0\n\n        # Count of unmatched ')' brackets\n        close_brackets = 0\n\n        # Traverse the string\n        for ch in expression:\n            if ch == '(':\n                # Consider '(' as unmatched for now\n                open_brackets += 1\n            else:\n                if open_brackets > 0:\n                    # Match this ')' with a previous '('\n                    open_brackets -= 1\n                else:\n                    # No matching '(' exists, so this ')' is unmatched\n                    close_brackets += 1\n\n        # If total number of unmatched brackets is odd, return -1\n        if (open_brackets + close_brackets) % 2 != 0:\n            return -1\n\n        # Return minimum reversals required\n        return (open_brackets + 1) // 2 + (close_brackets + 1) // 2\n\n\n# Input expression\nexpression = \"(()))(\"\n\n# Create object of Solution\nsolver = Solution()\n\n# Get result from function\nresult = solver.minReversalsToBalance(expression)\n\n# Print the result\nprint(\"Minimum reversals required:\", result)",
      "JavaScript": "class Solution {\n    // Function to calculate the minimum number of reversals to balance the expression\n    minReversalsToBalance(expression) {\n        // Count of unmatched '(' brackets\n        let openBrackets = 0;\n\n        // Count of unmatched ')' brackets\n        let closeBrackets = 0;\n\n        // Traverse the string\n        for (let ch of expression) {\n            if (ch === '(') {\n                // Consider '(' as unmatched for now\n                openBrackets++;\n            } else {\n                if (openBrackets > 0) {\n                    // Match this ')' with a previous '('\n                    openBrackets--;\n                } else {\n                    // No matching '(' exists, so this ')' is unmatched\n                    closeBrackets++;\n                }\n            }\n        }\n\n        // If total number of unmatched brackets is odd, return -1\n        if ((openBrackets + closeBrackets) % 2 !== 0) return -1;\n\n        // Return minimum reversals required\n        return Math.floor((openBrackets + 1) / 2) + Math.floor((closeBrackets + 1) / 2);\n    }\n}\n\n// Input expression\nconst expression = \"(()))(\";\n\n// Create object of Solution\nconst solver = new Solution();\n\n// Get result from function\nconst result = solver.minReversalsToBalance(expression);\n\n// Print the result\nconsole.log(\"Minimum reversals required:\", result);"
    }
  },
  "QBCategory": "CCD",
  "boilerPlates": {
    "C": "#include <stdio.h>\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "C++": "#include <iostream>\nusing namespace std;\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "Java": "import java.util.*;\nimport java.io.*;\n\npublic class Main {\n    public static void main(String[] args) throws IOException {\n        // your code goes here\n    }\n}",
    "Python3": "# your code goes here\n",
    "JavaScript": "// your code goes here\n"
  },
  "_internal": {
    "methodName": "",
    "params": []
  }
}