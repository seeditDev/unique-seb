{
  "questionId": "Q1250",
  "title": "Binary Search",
  "slug": "binary-search",
  "version": 1,
  "createdAt": "2026-07-01",
  "updatedAt": "2026-07-05",
  "metadata": {
    "category": "Searching",
    "difficulty": "Easy",
    "isPremium": false,
    "tags": [
      "Searching",
      "Binary Search"
    ],
    "companyTags": [],
    "topicTags": [
      "Binary Search"
    ]
  },
  "content": {
    "problemStatement": "Binary search is an efficient algorithm for finding a specific target value within a sorted array or list. It basically uses the ordered nature of the array to its advantage to find an element in an array very fast.\n\nBinary search works as -\n\n\nInitialise the search space with two indices, leftleftleft and rightrightright. Initially, left is set to the beginning of the array and right is set to the end.\nTake the mid-point of the search space as mid=(left+right)/2mid = (left + right)/2mid=(left+right)/2.\nCompare the target element to the element at the midmidmid index.\n\nIf the target value matches the midpoint value, the search is successful.\nIf the target value is less than the midpoint value, update right=mid\u22121right = mid - 1right=mid\u22121 to search the left half.\nIf the target value is greater than the midpoint value, update left=mid+1left = mid + 1left=mid+1 to search the right half.\n\n\nRepeat till the search space is not empty, i.e, right&gt;=leftright &gt;= leftright&gt;=left.\nTaskThe binary search function searches for the index of the element, returns back the index (1-base) if the element is found, else returns -1.\nComplete the implementation of the binary search function to solve this problem.",
    "constraints": [
      "1 <= T <= 10",
      "1 <= N <= 1000",
      "1 <= X <= 10^9",
      "Array elements are integers and sorted in non\u2011decreasing order"
    ],
    "inputFormat": "First line contains an integer T, the number of test cases. Each test case consists of a line with two integers N and X, where N is the size of the sorted array and X is the target value. The next line contains N space\u2011separated integers in non\u2011decreasing order.",
    "outputFormat": "For each test case output a single line containing the 1\u2011based index of X in the array if it exists, otherwise -1.",
    "sampleTestCases": [
      {
        "id": "sample_1",
        "input": "2\n5 7\n2 4 7 8 10\n5 6\n2 4 7 8 10",
        "output": "3\n-1",
        "explanation": "In the first test case the target 7 is at position 3 (1\u2011based). In the second test case 6 does not appear, so -1 is printed."
      },
      {
        "id": "sample_2",
        "input": "1\n3 5\n1 3 5",
        "output": "3",
        "explanation": "Target 5 is the third element."
      },
      {
        "id": "sample_3",
        "input": "1\n4 2\n1 2 3 4",
        "output": "2",
        "explanation": "Target 2 is at index 2."
      }
    ]
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "label": "minimum boundary",
        "input": "1\n1 5\n5",
        "expectedOutput": "1",
        "weight": 2
      },
      {
        "id": "tc_2",
        "label": "maximum boundary (compact)",
        "input": "1\n15 1000000000\n999999986 999999987 999999988 999999989 999999990 999999991 999999992 999999993 999999994 999999995 999999996 999999997 999999998 999999999 1000000000",
        "expectedOutput": "15",
        "weight": 2
      },
      {
        "id": "tc_3",
        "label": "uniform array, target absent",
        "input": "1\n5 4\n3 3 3 3 3",
        "expectedOutput": "-1",
        "weight": 2
      },
      {
        "id": "tc_4",
        "label": "target greater than all elements",
        "input": "1\n3 40\n10 20 30",
        "expectedOutput": "-1",
        "weight": 2
      },
      {
        "id": "tc_5",
        "label": "stress/general multiple cases",
        "input": "3\n4 2\n1 2 3 4\n6 9\n1 3 5 7 9 11\n2 100\n50 100",
        "expectedOutput": "2\n5\n2",
        "weight": 2
      }
    ],
    "totalTestCases": 5,
    "totalWeight": 10
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      2,
      2,
      2,
      2,
      2
    ],
    "maxScore": 10
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "For each test case we perform a classic binary search on the sorted array. We keep two pointers, left (initially 0) and right (initially N\u20111). While left <= right we compute mid = (left + right) / 2 (integer division). If arr[mid] equals the target we return mid+1 (1\u2011based index). If arr[mid] is larger we move right to mid\u20111, otherwise we move left to mid+1. If the loop finishes without finding the target we return -1. The algorithm runs in O(log N) time per test case and uses O(1) extra space.",
    "timeComplexity": "O(log N) per test case, O(T log N) total.",
    "spaceComplexity": "O(1) auxiliary space besides the input array.",
    "code": {
      "C": "#include <stdio.h>\n\nint binary_search(int arr[], int size, int target) {\n    int left = 0, right = size - 1;\n    while (left <= right) {\n        int mid = left + (right - left) / 2; // avoid overflow\n        if (arr[mid] == target) return mid + 1; // 1\u2011based index\n        else if (arr[mid] > target) right = mid - 1;\n        else left = mid + 1;\n    }\n    return -1;\n}\n\nint main() {\n    int T;\n    if (scanf(\"%d\", &T) != 1) return 0;\n    while (T--) {\n        int N, X;\n        scanf(\"%d %d\", &N, &X);\n        int arr[1000];\n        for (int i = 0; i < N; ++i) scanf(\"%d\", &arr[i]);\n        printf(\"%d\\n\", binary_search(arr, N, X));\n    }\n    return 0;\n}",
      "C++": "#include <bits/stdc++.h>\nusing namespace std;\n\nint binary_search(const vector<int>& arr, int target) {\n    int left = 0, right = (int)arr.size() - 1;\n    while (left <= right) {\n        int mid = left + (right - left) / 2;\n        if (arr[mid] == target) return mid + 1; // 1\u2011based\n        else if (arr[mid] > target) right = mid - 1;\n        else left = mid + 1;\n    }\n    return -1;\n}\n\nint main() {\n    ios::sync_with_stdio(false);\n    cin.tie(nullptr);\n    int T;\n    if (!(cin >> T)) return 0;\n    while (T--) {\n        int N, X;\n        cin >> N >> X;\n        vector<int> arr(N);\n        for (int i = 0; i < N; ++i) cin >> arr[i];\n        cout << binary_search(arr, X) << '\\n';\n    }\n    return 0;\n}",
      "Java": "import java.io.*;\nimport java.util.*;\n\npublic class Main {\n    private static int binarySearch(int[] arr, int target) {\n        int left = 0, right = arr.length - 1;\n        while (left <= right) {\n            int mid = left + (right - left) / 2;\n            if (arr[mid] == target) return mid + 1; // 1\u2011based\n            else if (arr[mid] > target) right = mid - 1;\n            else left = mid + 1;\n        }\n        return -1;\n    }\n\n    public static void main(String[] args) throws Exception {\n        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));\n        StringTokenizer st = new StringTokenizer(br.readLine());\n        int T = Integer.parseInt(st.nextToken());\n        StringBuilder sb = new StringBuilder();\n        while (T-- > 0) {\n            st = new StringTokenizer(br.readLine());\n            int N = Integer.parseInt(st.nextToken());\n            int X = Integer.parseInt(st.nextToken());\n            int[] arr = new int[N];\n            st = new StringTokenizer(br.readLine());\n            for (int i = 0; i < N; ++i) {\n                arr[i] = Integer.parseInt(st.nextToken());\n            }\n            sb.append(binarySearch(arr, X)).append('\\n');\n        }\n        System.out.print(sb.toString());\n    }\n}",
      "Python3": "#!/usr/bin/env python3\nimport sys\n\ndef binary_search(arr, target):\n    left, right = 0, len(arr) - 1\n    while left <= right:\n        mid = (left + right) // 2\n        if arr[mid] == target:\n            return mid + 1  # 1\u2011based\n        elif arr[mid] > target:\n            right = mid - 1\n        else:\n            left = mid + 1\n    return -1\n\ndef main():\n    data = sys.stdin.buffer.read().split()\n    if not data:\n        return\n    it = iter(data)\n    t = int(next(it))\n    out_lines = []\n    for _ in range(t):\n        n = int(next(it))\n        x = int(next(it))\n        arr = [int(next(it)) for _ in range(n)]\n        out_lines.append(str(binary_search(arr, x)))\n    sys.stdout.write('\\n'.join(out_lines))\n\nif __name__ == \"__main__\":\n    main()",
      "JavaScript": "const fs = require('fs');\nconst input = fs.readFileSync(0, 'utf-8').trim().split(/\\s+/);\nlet idx = 0;\nconst t = Number(input[idx++]);\nlet outputs = [];\nfunction binarySearch(arr, target) {\n    let left = 0, right = arr.length - 1;\n    while (left <= right) {\n        const mid = Math.floor((left + right) / 2);\n        if (arr[mid] === target) return mid + 1; // 1\u2011based\n        else if (arr[mid] > target) right = mid - 1;\n        else left = mid + 1;\n    }\n    return -1;\n}\nfor (let caseNum = 0; caseNum < t; caseNum++) {\n    const n = Number(input[idx++]);\n    const x = Number(input[idx++]);\n    const arr = new Array(n);\n    for (let i = 0; i < n; i++) arr[i] = Number(input[idx++]);\n    outputs.push(String(binarySearch(arr, x)));\n}\nconsole.log(outputs.join('\\n'));\n"
    }
  }
}