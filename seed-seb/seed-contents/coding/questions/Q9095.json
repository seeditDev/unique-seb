{
  "questionId": "Q9095",
  "title": "KBG and GN-Theory",
  "slug": "kbg-and-gn-theory",
  "version": 1,
  "createdAt": "2026-07-02",
  "updatedAt": "2026-07-04",
  "metadata": {
    "category": "Graph | Number Theory",
    "difficulty": "Hard",
    "isPremium": true,
    "tags": [
      "Shortest Path",
      "Divisibility",
      "GCD",
      "LCM",
      "Math"
    ],
    "companyTags": [],
    "topicTags": [
      "Mathematics"
    ],
    "QBCategory": "CCD"
  },
  "content": {
    "problemStatement": "After solving some Graph Theory related problems KBG challenged Sameer to solve the following problem.\n\nThere is an undirected graph consisting of NNN vertices numbered from 111 to NNN.\n\nFor every pair of vertices uuu and vvv (u≠v)(u \\neq v)(u=v) in the graph, there is a undirected weighted edge between uuu and vvv if and only if uuu divides vvv, with weight vu\\frac{v}{u}uv​. Note that such a graph will not have self loops or multiple edges.\n\nYou must answer QQQ queries. In each query you are given two vertices uuu and vvv. You must find the minimum distance from vertex uuu to vertex vvv.\n\n\nInput Format\n\nThe first line contains a single integer TTT — the number of test cases. Then the test cases follow.\nThe first line of each test case contains two integers NNN and QQQ — the number of vertices and the number of queries.\nThe next QQQ lines contain two space-separated integers uuu and vvv — vertices corresponding to the query.\n\n\nOutput Format\nFor each test case, print QQQ lines — the answers to the QQQ queries.",
    "constraints": [
      "1 ≤ T ≤ 10^5",
      "1 ≤ N ≤ 10^5",
      "1 ≤ Q ≤ 10^5",
      "1 ≤ u, v ≤ N",
      "The sum of N over all test cases does not exceed 10^5",
      "The sum of Q over all test cases does not exceed 10^5"
    ],
    "inputFormat": "The first line contains an integer T, the number of test cases. For each test case: the first line contains two integers N and Q (the number of vertices and the number of queries). Then Q lines follow, each containing two integers u and v (1 ≤ u, v ≤ N).",
    "outputFormat": "For each query output a single line with the minimum possible distance between vertices u and v in the described graph.",
    "sampleTestCases": [
      {
        "id": "sample_1",
        "input": "1\n6 2\n3 6\n2 3\n",
        "output": "2\n5\n",
        "explanation": "Query 1: 3 divides 6, direct edge weight = 6/3 = 2.\nQuery 2: 2 and 3 are coprime, gcd = 1, answer = (2+3)/1 = 5."
      },
      {
        "id": "sample_2",
        "input": "1\n10 3\n4 8\n5 5\n6 9\n",
        "output": "2\n0\n5\n",
        "explanation": "4|8 → weight 8/4 = 2.\n5 = 5 → distance 0.\n6 and 9: gcd=3, not divisible, answer = (6+9)/3 = 5."
      }
    ]
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "label": "Case 1",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_2",
        "label": "Case 2",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_3",
        "label": "Case 3",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_4",
        "label": "Case 4",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_5",
        "label": "Case 5",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      }
    ],
    "totalTestCases": 5,
    "totalWeight": 10
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      2,
      2,
      2,
      2,
      2
    ],
    "maxScore": 10
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "For two vertices u and v the graph contains an edge only when one divides the other, with weight equal to the ratio of the larger to the smaller number. If u = v the distance is 0. If one number divides the other, the direct edge gives the optimal distance = max(u, v) / min(u, v). Otherwise there is no direct edge. Any path must go through a common divisor or a common multiple. The cheapest such path is to go from u down to gcd(u, v) and then up to v (or symmetrically via lcm). The total cost of that route is (u / g) + (v / g) = (u + v) / g where g = gcd(u, v). This value is optimal because every step adds a positive integer ratio and the sum of the two ratios needed to change the prime‑exponent vector from u to v cannot be smaller than the sum of the upward and downward ratios required to reach a common divisor. Hence the answer for a query is:\n- if u == v → 0\n- else if max(u, v) % min(u, v) == 0 → max(u, v) / min(u, v)\n- else → (u + v) / gcd(u, v)\nThe algorithm processes each query independently, computing gcd with the Euclidean algorithm (O(log min(u,v))) and performing a few arithmetic operations. Overall complexity is linear in the total number of queries.",
    "timeComplexity": "O(total Q * log N) – each query needs a gcd computation.",
    "spaceComplexity": "O(1) besides input storage.",
    "code": {
      "C": "#include <stdio.h>\n#include <stdlib.h>\n\nstatic long long gcd_ll(long long a, long long b) {\n    while (b) {\n        long long t = a % b;\n        a = b;\n        b = t;\n    }\n    return a;\n}\n\nint main() {\n    int T; if (scanf(\"%d\", &T)!=1) return 0;\n    while (T--) {\n        int N, Q; scanf(\"%d %d\", &N, &Q);\n        while (Q--) {\n            long long u, v; scanf(\"%lld %lld\", &u, &v);\n            if (u == v) {\n                printf(\"0\\n\");\n                continue;\n            }\n            long long a = u < v ? u : v;\n            long long b = u < v ? v : u;\n            if (b % a == 0) {\n                printf(\"%lld\\n\", b / a);\n            } else {\n                long long g = gcd_ll(u, v);\n                printf(\"%lld\\n\", (u + v) / g);\n            }\n        }\n    }\n    return 0;\n}",
      "C++": "#include <bits/stdc++.h>\nusing namespace std;\n\nlong long gcd_ll(long long a, long long b) {\n    while (b) {\n        long long t = a % b;\n        a = b;\n        b = t;\n    }\n    return a;\n}\n\nint main() {\n    ios::sync_with_stdio(false);\n    cin.tie(nullptr);\n    int T; if(!(cin>>T)) return 0;\n    while (T--) {\n        int N, Q; cin >> N >> Q;\n        while (Q--) {\n            long long u, v; cin >> u >> v;\n            if (u == v) {\n                cout << 0 << '\\n';\n                continue;\n            }\n            long long a = min(u, v), b = max(u, v);\n            if (b % a == 0) {\n                cout << b / a << '\\n';\n            } else {\n                long long g = gcd_ll(u, v);\n                cout << (u + v) / g << '\\n';\n            }\n        }\n    }\n    return 0;\n}",
      "Java": "import java.io.*;\nimport java.util.*;\n\npublic class Main {\n    private static long gcd(long a, long b) {\n        while (b != 0) {\n            long t = a % b;\n            a = b;\n            b = t;\n        }\n        return a;\n    }\n    public static void main(String[] args) throws Exception {\n        FastScanner fs = new FastScanner(System.in);\n        int T = fs.nextInt();\n        StringBuilder sb = new StringBuilder();\n        while (T-- > 0) {\n            int N = fs.nextInt();\n            int Q = fs.nextInt();\n            while (Q-- > 0) {\n                long u = fs.nextLong();\n                long v = fs.nextLong();\n                if (u == v) {\n                    sb.append(0).append('\\n');\n                    continue;\n                }\n                long a = Math.min(u, v);\n                long b = Math.max(u, v);\n                if (b % a == 0) {\n                    sb.append(b / a).append('\\n');\n                } else {\n                    long g = gcd(u, v);\n                    sb.append((u + v) / g).append('\\n');\n                }\n            }\n        }\n        System.out.print(sb.toString());\n    }\n    // Fast scanner for large input\n    static class FastScanner {\n        private final InputStream in;\n        private final byte[] buffer = new byte[1 << 16];\n        private int ptr = 0, len = 0;\n        FastScanner(InputStream in) { this.in = in; }\n        private int readByte() throws IOException {\n            if (ptr >= len) {\n                len = in.read(buffer);\n                ptr = 0;\n                if (len <= 0) return -1;\n            }\n            return buffer[ptr++];\n        }\n        long nextLong() throws IOException {\n            int c; do { c = readByte(); } while (c <= ' ');\n            boolean neg = false;\n            if (c == '-') { neg = true; c = readByte(); }\n            long val = 0;\n            while (c > ' ') {\n                val = val * 10 + (c - '0');\n                c = readByte();\n            }\n            return neg ? -val : val;\n        }\n        int nextInt() throws IOException { return (int) nextLong(); }\n    }\n}",
      "Python3": "import sys\n\ndef gcd(a, b):\n    while b:\n        a, b = b, a % b\n    return a\n\ndef solve():\n    data = sys.stdin.read().strip().split()\n    if not data:\n        return\n    it = iter(data)\n    T = int(next(it))\n    out_lines = []\n    for _ in range(T):\n        N = int(next(it))\n        Q = int(next(it))\n        for __ in range(Q):\n            u = int(next(it))\n            v = int(next(it))\n            if u == v:\n                out_lines.append('0')\n                continue\n            a, b = (u, v) if u < v else (v, u)\n            if b % a == 0:\n                out_lines.append(str(b // a))\n            else:\n                g = gcd(u, v)\n                out_lines.append(str((u + v) // g))\n    sys.stdout.write('\\n'.join(out_lines))\n\nif __name__ == \"__main__\":\n    solve()",
      "JavaScript": "const fs = require('fs');\nconst input = fs.readFileSync(0, 'utf-8').trim().split(/\\s+/);\nlet idx = 0;\nfunction nextInt() { return Number(input[idx++]); }\nfunction gcd(a, b) {\n  while (b !== 0) {\n    const t = a % b;\n    a = b;\n    b = t;\n  }\n  return a;\n}\nlet T = nextInt();\nlet out = [];\nfor (let tc = 0; tc < T; ++tc) {\n  const N = nextInt(); // not used directly\n  const Q = nextInt();\n  for (let i = 0; i < Q; ++i) {\n    const u = nextInt();\n    const v = nextInt();\n    if (u === v) {\n      out.push('0');\n      continue;\n    }\n    const a = Math.min(u, v);\n    const b = Math.max(u, v);\n    if (b % a === 0) {\n      out.push(String(b / a));\n    } else {\n      const g = gcd(u, v);\n      out.push(String((u + v) / g));\n    }\n  }\n}\nconsole.log(out.join('\\n'));"
    }
  },
  "QBCategory": "CCD",
  "boilerPlates": {
    "C": "#include <stdio.h>\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "C++": "#include <iostream>\nusing namespace std;\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "Java": "import java.util.*;\nimport java.io.*;\n\npublic class Main {\n    public static void main(String[] args) throws IOException {\n        // your code goes here\n    }\n}",
    "Python3": "# your code goes here\n",
    "JavaScript": "// your code goes here\n"
  },
  "_internal": {
    "methodName": "",
    "params": []
  }
}