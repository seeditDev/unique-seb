{
  "questionId": "Q8666",
  "title": "Divisors and Reciprocals",
  "slug": "divisors-and-reciprocals",
  "version": 1,
  "createdAt": "2026-07-02",
  "updatedAt": "2026-07-04",
  "metadata": {
    "category": "Number Theory",
    "difficulty": "Medium",
    "isPremium": true,
    "tags": [
      "Divisor Sum",
      "Prime Factorisation",
      "Greatest Common Divisor",
      "Integer Arithmetic"
    ],
    "companyTags": [],
    "topicTags": [
      "Mathematics"
    ],
    "QBCategory": "CCD"
  },
  "content": {
    "problemStatement": "Alice is teaching Bob maths via a game called NNN-guesser.\n\nAlice has a positive integer NNN which Bob needs to guess. She gives him two pieces of information with which to do this:\n\n\nA positive integer XXX, which denotes the sum of divisors of NNN.\nTwo positive integers AAA and BBB, which denote that the sum of reciprocals of divisors of NNN is A/BA/BA/B.\nBob either needs to guess NNN or tell that no such number exists.\n\nIt can be shown that if a valid NNN exists, it is unique.\n\n\nInput Format\n\nThe first line of input contains a single integer TTT, denoting the number of test cases. The description of TTT test cases follows.\nEach test case consists of a single line of input, containing three space-separated integers X,A,BX, A, BX,A,B.\n\n\nOutput Format\nFor each test case, output a new line containing the answer — Alice's number NNN, or −1-1−1 if no such number exists.",
    "constraints": [
      "1 ≤ T ≤ 1000",
      "1 ≤ X, A, B ≤ 10^9",
      "gcd(A, B) = 1"
    ],
    "inputFormat": "The first line contains an integer T, the number of test cases. Each of the next T lines contains three space‑separated integers X, A and B (1 ≤ X, A, B ≤ 10^9, gcd(A,B)=1). X is the sum of all positive divisors of the unknown integer N, and A/B is the sum of the reciprocals of those divisors expressed in lowest terms.",
    "outputFormat": "For each test case output a single line containing N if a positive integer satisfying both conditions exists, otherwise output -1.",
    "sampleTestCases": [
      {
        "id": "sample_1",
        "input": "2\n4 4 3\n4 1 1",
        "output": "3\n-1",
        "explanation": "Test case 1: X=4, A/B=4/3. N = X*B/A = 4*3/4 = 3. σ(3)=1+3=4 matches X, so answer is 3.\nTest case 2: X=4, A/B=1/1. N = 4*1/1 = 4. σ(4)=1+2+4=7 ≠ 4, therefore no such N, output -1."
      }
    ]
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "label": "minimum boundary",
        "input": "1\n1 1 1",
        "expectedOutput": "1",
        "weight": 2
      },
      {
        "id": "tc_2",
        "label": "prime number",
        "input": "1\n6 6 5",
        "expectedOutput": "5",
        "weight": 2
      },
      {
        "id": "tc_3",
        "label": "non‑integer candidate",
        "input": "1\n10 3 2",
        "expectedOutput": "-1",
        "weight": 2
      },
      {
        "id": "tc_4",
        "label": "perfect number 28",
        "input": "1\n56 56 28",
        "expectedOutput": "28",
        "weight": 2
      },
      {
        "id": "tc_5",
        "label": "large composite",
        "input": "1\n4032 4032 1260",
        "expectedOutput": "1260",
        "weight": 2
      }
    ],
    "totalTestCases": 5,
    "totalWeight": 10
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      2,
      2,
      2,
      2,
      2
    ],
    "maxScore": 10
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "For any positive integer N we have:\n    σ(N) = sum of divisors of N\n    Σ 1/d (d|N) = σ(N) / N.\nTherefore the two given values must satisfy X / N = A / B, i.e. N = X * B / A. The algorithm is:\n1. Check if X * B is divisible by A. If not, answer is -1.\n2. Compute candidate N = X * B / A using 64‑bit integer arithmetic.\n3. Compute σ(N) by prime factorising N. For a prime power p^e, its contribution to σ is (p^{e+1}-1)/(p-1). Multiply contributions for all distinct primes.\n4. If the computed σ(N) equals the given X, output N; otherwise output -1.\nThe factorisation is done by trial division up to sqrt(N) using a pre‑generated list of primes up to 10^6 (sufficient because sqrt(10^12) ≤ 10^6). This runs in O(√N) per test case, fast enough for the given limits.",
    "timeComplexity": "O(T * √N) in the worst case, where N is the candidate number (≤ 10^12 in practice).",
    "spaceComplexity": "O(√N) for the list of primes (≈ 78 k integers) plus O(1) extra per test case.",
    "code": {
      "C": "#include <stdio.h>\n#include <stdlib.h>\n#include <stdint.h>\n#include <math.h>\n\n// simple trial division without pre‑computed primes (fast enough for N ≤ 1e12)\nstatic uint64_t sigma(uint64_t n) {\n    uint64_t original = n;\n    uint64_t result = 1;\n    for (uint64_t p = 2; p * p <= n; ++p) {\n        if (n % p) continue;\n        uint64_t term = 1;\n        uint64_t power = 1;\n        while (n % p == 0) {\n            n /= p;\n            power *= p;\n            term += power;\n        }\n        result *= term;\n    }\n    if (n > 1) result *= (1 + n); // remaining prime factor\n    return result;\n}\n\nint main() {\n    int T; if (scanf(\"%d\", &T)!=1) return 0;\n    while (T--) {\n        uint64_t X, A, B; scanf(\"%llu %llu %llu\", &X, &A, &B);\n        __uint128_t prod = ( (__uint128_t) X) * B;\n        if (prod % A != 0) {\n            printf(\"-1\\n\");\n            continue;\n        }\n        uint64_t N = (uint64_t)(prod / A);\n        if (sigma(N) == X) printf(\"%llu\\n\", N);\n        else printf(\"-1\\n\");\n    }\n    return 0;\n}",
      "C++": "#include <bits/stdc++.h>\nusing namespace std;\nusing int64 = long long;\nusing i128 = __int128_t;\n\nint64 sigma(int64 n) {\n    int64 res = 1;\n    for (int64 p = 2; p * p <= n; ++p) {\n        if (n % p) continue;\n        int64 term = 1, power = 1;\n        while (n % p == 0) {\n            n /= p;\n            power *= p;\n            term += power;\n        }\n        res *= term;\n    }\n    if (n > 1) res *= (1 + n);\n    return res;\n}\n\nint main() {\n    ios::sync_with_stdio(false);\n    cin.tie(nullptr);\n    int T; if(!(cin>>T)) return 0;\n    while (T--) {\n        int64 X, A, B; cin >> X >> A >> B;\n        i128 prod = (i128)X * B;\n        if (prod % A != 0) {\n            cout << -1 << '\\n';\n            continue;\n        }\n        int64 N = (int64)(prod / A);\n        if (sigma(N) == X) cout << N << '\\n';\n        else cout << -1 << '\\n';\n    }\n    return 0;\n}",
      "Java": "import java.io.*;\nimport java.util.*;\n\npublic class Main {\n    private static long sigma(long n) {\n        long res = 1;\n        for (long p = 2; p * p <= n; ++p) {\n            if (n % p != 0) continue;\n            long term = 1, power = 1;\n            while (n % p == 0) {\n                n /= p;\n                power *= p;\n                term += power;\n            }\n            res *= term;\n        }\n        if (n > 1) res *= (1 + n);\n        return res;\n    }\n\n    public static void main(String[] args) throws Exception {\n        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));\n        StringTokenizer st = new StringTokenizer(br.readLine());\n        int T = Integer.parseInt(st.nextToken());\n        StringBuilder out = new StringBuilder();\n        for (int t = 0; t < T; ++t) {\n            st = new StringTokenizer(br.readLine());\n            long X = Long.parseLong(st.nextToken());\n            long A = Long.parseLong(st.nextToken());\n            long B = Long.parseLong(st.nextToken());\n            java.math.BigInteger prod = java.math.BigInteger.valueOf(X).multiply(java.math.BigInteger.valueOf(B));\n            if (!prod.mod(java.math.BigInteger.valueOf(A)).equals(java.math.BigInteger.ZERO)) {\n                out.append(-1).append('\\n');\n                continue;\n            }\n            long N = prod.divide(java.math.BigInteger.valueOf(A)).longValue();\n            if (sigma(N) == X) out.append(N).append('\\n');\n            else out.append(-1).append('\\n');\n        }\n        System.out.print(out.toString());\n    }\n}",
      "Python3": "#!/usr/bin/env python3\nimport sys, math\n\ndef sigma(n: int) -> int:\n    res = 1\n    p = 2\n    while p * p <= n:\n        if n % p:\n            p += 1\n            continue\n        term = 1\n        power = 1\n        while n % p == 0:\n            n //= p\n            power *= p\n            term += power\n        res *= term\n        p += 1\n    if n > 1:\n        res *= (1 + n)\n    return res\n\ndef solve():\n    data = sys.stdin.read().strip().split()\n    if not data:\n        return\n    it = iter(data)\n    T = int(next(it))\n    out = []\n    for _ in range(T):\n        X = int(next(it)); A = int(next(it)); B = int(next(it))\n        prod = X * B\n        if prod % A != 0:\n            out.append('-1')\n            continue\n        N = prod // A\n        if sigma(N) == X:\n            out.append(str(N))\n        else:\n            out.append('-1')\n    sys.stdout.write('\\n'.join(out))\n\nif __name__ == \"__main__\":\n    solve()\n",
      "JavaScript": "const fs = require('fs');\nconst input = fs.readFileSync(0, 'utf-8').trim().split(/\\s+/);\nlet idx = 0;\nconst T = Number(input[idx++]);\nlet out = [];\nfunction sigma(n) {\n  let res = 1n;\n  let nn = BigInt(n);\n  let p = 2n;\n  while (p * p <= nn) {\n    if (nn % p !== 0n) { p++; continue; }\n    let term = 1n;\n    let power = 1n;\n    while (nn % p === 0n) {\n      nn /= p;\n      power *= p;\n      term += power;\n    }\n    res *= term;\n    p++;\n  }\n  if (nn > 1n) res *= (1n + nn);\n  return Number(res);\n}\nfor (let t = 0; t < T; ++t) {\n  const X = BigInt(input[idx++]);\n  const A = BigInt(input[idx++]);\n  const B = BigInt(input[idx++]);\n  const prod = X * B;\n  if (prod % A !== 0n) { out.push('-1'); continue; }\n  const Nbig = prod / A;\n  const N = Number(Nbig); // N fits into Number for given limits\n  if (sigma(N) === Number(X)) out.push(String(N));\n  else out.push('-1');\n}\nconsole.log(out.join('\\n'));\n"
    }
  },
  "QBCategory": "CCD",
  "boilerPlates": {
    "C": "#include <stdio.h>\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "C++": "#include <iostream>\nusing namespace std;\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "Java": "import java.util.*;\nimport java.io.*;\n\npublic class Main {\n    public static void main(String[] args) throws IOException {\n        // your code goes here\n    }\n}",
    "Python3": "# your code goes here\n",
    "JavaScript": "// your code goes here\n"
  },
  "_internal": {
    "methodName": "",
    "params": []
  }
}