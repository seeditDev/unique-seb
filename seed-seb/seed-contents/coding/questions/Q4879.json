{
  "questionId": "Q4879",
  "title": "Simple Inventory Management System",
  "slug": "simple-inventory-management-system",
  "version": 1,
  "createdAt": "2026-07-01",
  "updatedAt": "2026-07-04",
  "metadata": {
    "category": "Data Structures",
    "difficulty": "Medium",
    "isPremium": true,
    "tags": [
      "OOP",
      "Simulation",
      "HashMap",
      "Array",
      "Classes"
    ],
    "companyTags": [],
    "topicTags": [
      "OOP Concepts"
    ],
    "QBCategory": "CCD"
  },
  "content": {
    "problemStatement": "A small store wants a simple inventory system to manage its products. Your task is to build this using **Object-Oriented Programming** principles.\n\nRequirements\n\nCreate a `Product` class with:\n\n\n\nFields: `id`, `name`, `quantity`\nA method `display()` to print product details\n\n\n\nIn the main program, support the following operations:\n\n\n\n`add id name quantity` → Add a new product\n`sell id quantity` → Reduce quantity of the product\n`restock id quantity` → Increase quantity\n`display` → Display all current products in the format:\n\n`ID: x, Name: y, Quantity: z`\n\n\n\nInput Format\n\nFirst line: An integer `N`, the number of operations\nNext `N` lines: Each line is one of the supported operations\n\n\nOutput Format\n\nFor every `display` command, show all products in the order they were added.",
    "constraints": [
      "1 <= T <= 10",
      "1 <= N <= 100",
      "1 <= id <= 1000",
      "0 <= quantity <= 1000",
      "name consists of alphabetic characters without spaces and length <= 20"
    ],
    "inputFormat": "The first line contains an integer T, the number of test cases. For each test case, the first line contains an integer N, the number of operations. The next N lines each contain one operation in one of the following forms:\\nadd id name quantity\\nsell id quantity\\nrestock id quantity\\ndisplay",
    "outputFormat": "For every display command of every test case, output the current list of products in the order they were added. Each product is printed on its own line in the format: ID: x, Name: y, Quantity: z. Separate the outputs of different test cases with a newline (i.e., after finishing a test case, print a blank line if there is another test case).",
    "sampleTestCases": [
      {
        "id": "sample_1",
        "input": "1\\n6\\nadd 1 Apple 10\\nadd 2 Banana 5\\nsell 1 3\\nrestock 2 10\\ndisplay\\nsell 1 20",
        "output": "ID: 1, Name: Apple, Quantity: 7\\nID: 2, Name: Banana, Quantity: 15",
        "explanation": "Two products are added. Apple quantity becomes 10-3=7 after selling 3. Banana is restocked to 5+10=15. The display command prints both products in insertion order. The final sell operation tries to sell 20 Apples but only 7 are available, so nothing changes."
      },
      {
        "id": "sample_2",
        "input": "1\\n4\\nadd 1 Orange 20\\nadd 2 Grape 15\\nrestock 1 10\\ndisplay",
        "output": "ID: 1, Name: Orange, Quantity: 30\\nID: 2, Name: Grape, Quantity: 15",
        "explanation": "Orange is added with 20, then restocked by 10 → 30. Grape stays at 15. Display prints both in the order they were added."
      },
      {
        "id": "sample_3",
        "input": "1\\n3\\nadd 1 Pear 5\\nsell 1 3\\ndisplay",
        "output": "ID: 1, Name: Pear, Quantity: 2",
        "explanation": "Pear starts with 5, 3 are sold, leaving 2. Display shows the single product."
      }
    ]
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "label": "minimum boundary",
        "input": "1\\n1\\nadd 1 X 0",
        "expectedOutput": "ID: 1, Name: X, Quantity: 0",
        "weight": 2
      },
      {
        "id": "tc_2",
        "label": "Case 2",
        "input": "1\\n1\\nadd 1 X 0",
        "expectedOutput": "ID: 1, Name: X, Quantity: 0",
        "weight": 2
      },
      {
        "id": "tc_3",
        "label": "Case 3",
        "input": "1\\n1\\nadd 1 X 0",
        "expectedOutput": "ID: 1, Name: X, Quantity: 0",
        "weight": 2
      },
      {
        "id": "tc_4",
        "label": "Case 4",
        "input": "1\\n1\\nadd 1 X 0",
        "expectedOutput": "ID: 1, Name: X, Quantity: 0",
        "weight": 2
      },
      {
        "id": "tc_5",
        "label": "Case 5",
        "input": "1\\n1\\nadd 1 X 0",
        "expectedOutput": "ID: 1, Name: X, Quantity: 0",
        "weight": 2
      }
    ],
    "totalTestCases": 5,
    "totalWeight": 10
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      2,
      2,
      2,
      2,
      2
    ],
    "maxScore": 10
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "For each test case we maintain two structures: (1) a vector/ArrayList/list that stores Product objects in the order they are added, and (2) a hash map (unordered_map, HashMap, dict) that maps a product id to its position in the vector.\\n\\nWhen an add operation arrives we create a new Product, push it to the vector and store its index in the map.\\nWhen sell or restock arrives we locate the product via the map (O(1) average) and adjust its quantity, ensuring that we never reduce below zero (the statement says to ignore the operation if not enough stock).\\nWhen display arrives we simply iterate over the vector and print each product using the required format.\\n\\nAll operations are processed sequentially, giving an overall O(N) time per test case and O(N) memory.",
    "timeComplexity": "O(N) per test case, where N is the number of operations. Each operation is O(1) except display which is O(current number of products).",
    "spaceComplexity": "O(P) per test case, where P is the number of distinct products added (P ≤ N).",
    "code": {
      "C": "#include <stdio.h>\n#include <stdlib.h>\n#include <string.h>\n\ntypedef struct {\n    int id;\n    char name[25];\n    int quantity;\n} Product;\n\ntypedef struct {\n    Product *arr;\n    int size;\n    int capacity;\n} ProductList;\n\nvoid initList(ProductList *list) {\n    list->capacity = 4;\n    list->size = 0;\n    list->arr = malloc(list->capacity * sizeof(Product));\n}\n\nvoid addProduct(ProductList *list, int id, const char *name, int qty, int *posMap) {\n    if (list->size == list->capacity) {\n        list->capacity *= 2;\n        list->arr = realloc(list->arr, list->capacity * sizeof(Product));\n    }\n    Product *p = &list->arr[list->size];\n    p->id = id;\n    strcpy(p->name, name);\n    p->quantity = qty;\n    posMap[id] = list->size; // store index (0‑based)\n    list->size++;\n}\n\nvoid sellProduct(ProductList *list, int id, int qty, int *posMap) {\n    int idx = posMap[id];\n    if (idx >= 0 && idx < list->size) {\n        Product *p = &list->arr[idx];\n        if (p->quantity >= qty) p->quantity -= qty;\n    }\n}\n\nvoid restockProduct(ProductList *list, int id, int qty, int *posMap) {\n    int idx = posMap[id];\n    if (idx >= 0 && idx < list->size) {\n        list->arr[idx].quantity += qty;\n    }\n}\n\nvoid displayProducts(ProductList *list) {\n    for (int i = 0; i < list->size; ++i) {\n        printf(\"ID: %d, Name: %s, Quantity: %d\\n\", list->arr[i].id, list->arr[i].name, list->arr[i].quantity);\n    }\n}\n\nint main() {\n    int T; scanf(\"%d\", &T);\n    while (T--) {\n        int N; scanf(\"%d\", &N);\n        ProductList list; initList(&list);\n        int posMap[1001];\n        for (int i = 0; i <= 1000; ++i) posMap[i] = -1;\n        for (int i = 0; i < N; ++i) {\n            char op[10]; scanf(\"%s\", op);\n            if (strcmp(op, \"add\") == 0) {\n                int id, qty; char name[25];\n                scanf(\"%d %s %d\", &id, name, &qty);\n                addProduct(&list, id, name, qty, posMap);\n            } else if (strcmp(op, \"sell\") == 0) {\n                int id, qty; scanf(\"%d %d\", &id, &qty);\n                sellProduct(&list, id, qty, posMap);\n            } else if (strcmp(op, \"restock\") == 0) {\n                int id, qty; scanf(\"%d %d\", &id, &qty);\n                restockProduct(&list, id, qty, posMap);\n            } else if (strcmp(op, \"display\") == 0) {\n                displayProducts(&list);\n            }\n        }\n        if (T) printf(\"\\n\"); // blank line between test cases\n        free(list.arr);\n    }\n    return 0;\n}",
      "C++": "#include <bits/stdc++.h>\nusing namespace std;\n\nclass Product {\npublic:\n    int id;\n    string name;\n    int quantity;\n    Product(int i, const string &n, int q) : id(i), name(n), quantity(q) {}\n};\n\nint main() {\n    ios::sync_with_stdio(false);\n    cin.tie(nullptr);\n    int T; if(!(cin>>T)) return 0;\n    while (T--) {\n        int N; cin>>N;\n        vector<Product> order; // preserves insertion order\n        unordered_map<int,int> idx; // id -> position in order\n        for (int i=0;i<N;++i) {\n            string op; cin>>op;\n            if (op==\"add\") {\n                int id, qty; string name; cin>>id>>name>>qty;\n                idx[id]=order.size();\n                order.emplace_back(id,name,qty);\n            } else if (op==\"sell\") {\n                int id, qty; cin>>id>>qty;\n                auto it=idx.find(id);\n                if(it!=idx.end()) {\n                    Product &p=order[it->second];\n                    if(p.quantity>=qty) p.quantity-=qty;\n                }\n            } else if (op==\"restock\") {\n                int id, qty; cin>>id>>qty;\n                auto it=idx.find(id);\n                if(it!=idx.end()) order[it->second].quantity+=qty;\n            } else if (op==\"display\") {\n                for(const auto &p:order) {\n                    cout<<\"ID: \"<<p.id<<\", Name: \"<<p.name<<\", Quantity: \"<<p.quantity<<\"\\n\";\n                }\n            }\n        }\n        if (T) cout<<\"\\n\"; // blank line between test cases\n    }\n    return 0;\n}",
      "Java": "import java.io.*;\nimport java.util.*;\n\nclass Product {\n    int id;\n    String name;\n    int quantity;\n    Product(int id, String name, int quantity) {\n        this.id = id;\n        this.name = name;\n        this.quantity = quantity;\n    }\n}\n\npublic class Main {\n    public static void main(String[] args) throws Exception {\n        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));\n        StringBuilder out = new StringBuilder();\n        int T = Integer.parseInt(br.readLine().trim());\n        for (int tc = 0; tc < T; ++tc) {\n            int N = Integer.parseInt(br.readLine().trim());\n            List<Product> order = new ArrayList<>();\n            Map<Integer, Integer> idx = new HashMap<>(); // id -> position in list\n            for (int i = 0; i < N; ++i) {\n                String line = br.readLine();\n                StringTokenizer st = new StringTokenizer(line);\n                String op = st.nextToken();\n                if (op.equals(\"add\")) {\n                    int id = Integer.parseInt(st.nextToken());\n                    String name = st.nextToken();\n                    int qty = Integer.parseInt(st.nextToken());\n                    idx.put(id, order.size());\n                    order.add(new Product(id, name, qty));\n                } else if (op.equals(\"sell\")) {\n                    int id = Integer.parseInt(st.nextToken());\n                    int qty = Integer.parseInt(st.nextToken());\n                    Integer pos = idx.get(id);\n                    if (pos != null) {\n                        Product p = order.get(pos);\n                        if (p.quantity >= qty) p.quantity -= qty;\n                    }\n                } else if (op.equals(\"restock\")) {\n                    int id = Integer.parseInt(st.nextToken());\n                    int qty = Integer.parseInt(st.nextToken());\n                    Integer pos = idx.get(id);\n                    if (pos != null) order.get(pos).quantity += qty;\n                } else if (op.equals(\"display\")) {\n                    for (Product p : order) {\n                        out.append(\"ID: \").append(p.id).append(\", Name: \").append(p.name).append(\", Quantity: \").append(p.quantity).append('\\n');\n                    }\n                }\n            }\n            if (tc != T-1) out.append('\\n'); // blank line between test cases\n        }\n        System.out.print(out.toString());\n    }\n}",
      "Python3": "import sys\n\ndef main():\n    data = sys.stdin.read().strip().split('\\n')\n    if not data:\n        return\n    t = int(data[0])\n    line_idx = 1\n    out_lines = []\n    for _ in range(t):\n        n = int(data[line_idx]); line_idx += 1\n        products = []  # list preserving order\n        pos = {}       # id -> index in products\n        for _ in range(n):\n            parts = data[line_idx].split(); line_idx += 1\n            op = parts[0]\n            if op == 'add':\n                pid = int(parts[1]); name = parts[2]; qty = int(parts[3])\n                pos[pid] = len(products)\n                products.append([pid, name, qty])\n            elif op == 'sell':\n                pid = int(parts[1]); qty = int(parts[2])\n                if pid in pos:\n                    p = products[pos[pid]]\n                    if p[2] >= qty:\n                        p[2] -= qty\n            elif op == 'restock':\n                pid = int(parts[1]); qty = int(parts[2])\n                if pid in pos:\n                    products[pos[pid]][2] += qty\n            elif op == 'display':\n                for p in products:\n                    out_lines.append(f\"ID: {p[0]}, Name: {p[1]}, Quantity: {p[2]}\")\n        if _ != t-1:\n            out_lines.append('')  # blank line between test cases\n    sys.stdout.write('\\n'.join(out_lines))\n\nif __name__ == \"__main__\":\n    main()",
      "JavaScript": "const fs = require('fs');\nconst input = fs.readFileSync(0, 'utf-8').trim().split('\\n');\nlet idx = 0;\nconst T = parseInt(input[idx++]);\nlet output = [];\nfor (let tc = 0; tc < T; ++tc) {\n  const N = parseInt(input[idx++]);\n  const products = [];\n  const pos = new Map(); // id -> index in products array\n  for (let i = 0; i < N; ++i) {\n    const parts = input[idx++].trim().split(' ');\n    const op = parts[0];\n    if (op === 'add') {\n      const id = parseInt(parts[1]);\n      const name = parts[2];\n      const qty = parseInt(parts[3]);\n      pos.set(id, products.length);\n      products.push({id, name, quantity: qty});\n    } else if (op === 'sell') {\n      const id = parseInt(parts[1]);\n      const qty = parseInt(parts[2]);\n      if (pos.has(id)) {\n        const p = products[pos.get(id)];\n        if (p.quantity >= qty) p.quantity -= qty;\n      }\n    } else if (op === 'restock') {\n      const id = parseInt(parts[1]);\n      const qty = parseInt(parts[2]);\n      if (pos.has(id)) {\n        products[pos.get(id)].quantity += qty;\n      }\n    } else if (op === 'display') {\n      for (const p of products) {\n        output.push(`ID: ${p.id}, Name: ${p.name}, Quantity: ${p.quantity}`);\n      }\n    }\n  }\n  if (tc !== T - 1) output.push(''); // blank line between test cases\n}\nconsole.log(output.join('\\n'));\n"
    }
  },
  "QBCategory": "CCD",
  "boilerPlates": {
    "C": "#include <stdio.h>\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "C++": "#include <iostream>\nusing namespace std;\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "Java": "import java.util.*;\nimport java.io.*;\n\npublic class Main {\n    public static void main(String[] args) throws IOException {\n        // your code goes here\n    }\n}",
    "Python3": "# your code goes here\n",
    "JavaScript": "// your code goes here\n"
  },
  "_internal": {
    "methodName": "",
    "params": []
  }
}