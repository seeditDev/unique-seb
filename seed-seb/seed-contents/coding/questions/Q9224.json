{
  "questionId": "Q9224",
  "title": "Nil Xor",
  "slug": "nil-xor",
  "version": 1,
  "createdAt": "2026-07-02",
  "updatedAt": "2026-07-04",
  "metadata": {
    "category": "Bit Manipulation",
    "difficulty": "Hard",
    "isPremium": true,
    "tags": [
      "Bitmask",
      "DP on bits",
      "Counting",
      "Prefix DP"
    ],
    "companyTags": [],
    "topicTags": [
      "Dynamic Programming"
    ],
    "QBCategory": "CCD"
  },
  "content": {
    "problemStatement": "Chef has two non negative integers NNN and XXX. He now wants to find the number of integers KKK such that 0≤K&lt;N0 \\le K \\lt N0≤K&lt;N, and (N⊕K)&amp;X=0(N \\oplus K) \\And X = 0(N⊕K)&amp;X=0.\n\nNote that ⊕\\oplus⊕ denotes the bitwise XOR operator and &amp;\\And&amp; denotes the bitwise AND operator.\n\nHelp Chef in finding the total required count.\n\n\nInput Format\n\nThe first line of input will contain a single integer TTT, denoting the number of test cases.\nEach test case consists of a single line of input containing NNN and XXX denoting the two integers as described in the statement.\n\n\nOutput Format\nFor each test case, output the total number of integers KKK which satisfy the requirements mentioned.",
    "constraints": [
      "1 ≤ T ≤ 10^5",
      "0 ≤ N, X ≤ 2^30‑1"
    ],
    "inputFormat": "The first line contains an integer T, the number of test cases. Each of the next T lines contains two space‑separated non‑negative integers N and X (0 ≤ N, X ≤ 2^30‑1).",
    "outputFormat": "For each test case output a single line containing the number of integers K such that 0 ≤ K < N and ((N xor K) & X) = 0.",
    "sampleTestCases": [
      {
        "id": "sample_1",
        "input": "3\n4 5\n8 4\n1 2",
        "output": "0\n4\n1",
        "explanation": "Explanation:\nTestcase 1: N=4,X=5N = 4, X = 5N=4,X=5. So we need to find the number of KKK, which satisfy 0≤K<40 \\le K \n\\lt 40≤K<4, and (4⊕K)&5=0(4 \\oplus K) \\And 5 = 0(4⊕K)&5=0.\n\n(1002⊕0002)&1012=1002&1012=1002≠0(100_2 \\oplus 000_2) \\And 101_2 = 100_2 \\And 101_2 = 100_2 \\neq 0(1002​⊕0002​)&1012​=1002​&1012​=1002​=0\n(1002⊕0012)&1012=1012&1012=1012≠0(100_2 \\oplus 001_2) \\And 101_2 = 101_2 \\And 101_2 = 101_2 \\neq 0(1002​⊕0012​)&1012​=1012​&1012​=1012​=0\n(1002⊕0102)&1012=1102&1012=1002≠0(100_2 \\oplus 010_2) \\And 101_2 = 110_2 \\And 101_2 = 100_2 \\neq 0(1002​⊕0102​)&1012​=1102​&1012​=1002​=0\n(1002⊕0112)&1012=1112&1012=1012≠0(100_2 \\oplus 011_2) \\And 101_2 = 111_2 \\And 101_2 = 101_2 \\neq 0(1002​⊕0112​)&1012​=1112​&1012​=1012​=0\n\nSo, we have looked at all values of KKK, and found no value which satisfies the requirements. So the answer is 000."
      }
    ]
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "label": "Case 1",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_2",
        "label": "Case 2",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_3",
        "label": "Case 3",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_4",
        "label": "Case 4",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_5",
        "label": "Case 5",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      }
    ],
    "totalTestCases": 5,
    "totalWeight": 10
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      2,
      2,
      2,
      2,
      2
    ],
    "maxScore": 10
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "For a bit i where X has 1, the condition forces K_i = N_i. For bits where X has 0, K_i can be 0 or 1. Let base = N & X (the forced part). Any valid K can be written as K = base + V where V uses only positions with X_i = 0 (i.e., V & X = 0). The inequality K < N becomes V < N - base. Hence the problem reduces to counting numbers V in [0, limit‑1] (limit = N - base) that do not set any bit where X has 1. This is a classic digit‑DP on binary representation: iterate bits from most significant to least, keep a flag whether the prefix of V is already smaller than the prefix of limit (tight flag). At each bit, if X_i = 1 we can only place 0, otherwise we may place 0 or 1 respecting the tight condition. The DP runs in O(number of bits) ≈ 31 per test case, easily fast for T = 10^5.",
    "timeComplexity": "O(T * B) where B = 31 (number of bits needed for values up to 2^30).",
    "spaceComplexity": "O(1) – only a few integer variables are used.",
    "code": {
      "C": "#include <stdio.h>\n#include <stdint.h>\n\nstatic uint64_t countValid(uint32_t limit, uint32_t mask) {\n    uint64_t dp_tight = 1, dp_loose = 0; // dp[tight]\n    for (int i = 30; i >= 0; --i) {\n        int bitL = (limit >> i) & 1;\n        int bitM = (mask >> i) & 1;\n        uint64_t ndp_tight = 0, ndp_loose = 0;\n        // transitions from tight state\n        if (dp_tight) {\n            // allowed bits for V at this position\n            for (int vb = 0; vb <= 1; ++vb) {\n                if (bitM && vb) continue; // mask forces 0\n                if (vb > bitL) continue; // would exceed limit\n                if (vb == bitL) ndp_tight += dp_tight; // stay tight\n                else ndp_loose += dp_tight; // become loose\n            }\n        }\n        // transitions from loose state (already smaller)\n        if (dp_loose) {\n            for (int vb = 0; vb <= 1; ++vb) {\n                if (bitM && vb) continue;\n                ndp_loose += dp_loose; // any allowed vb keeps loose\n            }\n        }\n        dp_tight = ndp_tight;\n        dp_loose = ndp_loose;\n    }\n    return dp_tight + dp_loose;\n}\n\nint main() {\n    int T;\n    if (scanf(\"%d\", &T) != 1) return 0;\n    while (T--) {\n        uint32_t N, X;\n        scanf(\"%u %u\", &N, &X);\n        uint32_t base = N & X;\n        if (base >= N) {\n            printf(\"0\\n\");\n            continue;\n        }\n        uint32_t limit = N - base;\n        uint64_t ans = countValid(limit, X);\n        printf(\"%llu\\n\", ans);\n    }\n    return 0;\n}",
      "C++": "#include <bits/stdc++.h>\nusing namespace std;\n\nstatic uint64_t countValid(uint32_t limit, uint32_t mask) {\n    uint64_t dp_tight = 1, dp_loose = 0;\n    for (int i = 30; i >= 0; --i) {\n        int bitL = (limit >> i) & 1;\n        int bitM = (mask >> i) & 1;\n        uint64_t ndp_tight = 0, ndp_loose = 0;\n        // from tight\n        if (dp_tight) {\n            for (int vb = 0; vb <= 1; ++vb) {\n                if (bitM && vb) continue; // forced 0\n                if (vb > bitL) continue;\n                if (vb == bitL) ndp_tight += dp_tight;\n                else ndp_loose += dp_tight;\n            }\n        }\n        // from loose\n        if (dp_loose) {\n            for (int vb = 0; vb <= 1; ++vb) {\n                if (bitM && vb) continue;\n                ndp_loose += dp_loose;\n            }\n        }\n        dp_tight = ndp_tight;\n        dp_loose = ndp_loose;\n    }\n    return dp_tight + dp_loose;\n}\n\nint main() {\n    ios::sync_with_stdio(false);\n    cin.tie(nullptr);\n    int T; cin >> T;\n    while (T--) {\n        uint32_t N, X; cin >> N >> X;\n        uint32_t base = N & X;\n        if (base >= N) {\n            cout << 0 << '\\n';\n            continue;\n        }\n        uint32_t limit = N - base;\n        uint64_t ans = countValid(limit, X);\n        cout << ans << '\\n';\n    }\n    return 0;\n}",
      "Java": "import java.io.*;\nimport java.util.*;\n\npublic class Main {\n    private static long countValid(int limit, int mask) {\n        long dpTight = 1, dpLoose = 0;\n        for (int i = 30; i >= 0; --i) {\n            int bitL = (limit >> i) & 1;\n            int bitM = (mask >> i) & 1;\n            long ndpTight = 0, ndpLoose = 0;\n            if (dpTight != 0) {\n                for (int vb = 0; vb <= 1; ++vb) {\n                    if (bitM == 1 && vb == 1) continue;\n                    if (vb > bitL) continue;\n                    if (vb == bitL) ndpTight += dpTight;\n                    else ndpLoose += dpTight;\n                }\n            }\n            if (dpLoose != 0) {\n                for (int vb = 0; vb <= 1; ++vb) {\n                    if (bitM == 1 && vb == 1) continue;\n                    ndpLoose += dpLoose;\n                }\n            }\n            dpTight = ndpTight;\n            dpLoose = ndpLoose;\n        }\n        return dpTight + dpLoose;\n    }\n\n    public static void main(String[] args) throws Exception {\n        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));\n        StringBuilder sb = new StringBuilder();\n        int T = Integer.parseInt(br.readLine().trim());\n        for (int t = 0; t < T; ++t) {\n            StringTokenizer st = new StringTokenizer(br.readLine());\n            int N = Integer.parseInt(st.nextToken());\n            int X = Integer.parseInt(st.nextToken());\n            int base = N & X;\n            if (base >= N) {\n                sb.append(0).append('\\n');\n                continue;\n            }\n            int limit = N - base;\n            long ans = countValid(limit, X);\n            sb.append(ans).append('\\n');\n        }\n        System.out.print(sb.toString());\n    }\n}",
      "Python3": "import sys\n\ndef count_valid(limit: int, mask: int) -> int:\n    dp_tight, dp_loose = 1, 0\n    for i in range(30, -1, -1):\n        bitL = (limit >> i) & 1\n        bitM = (mask >> i) & 1\n        ndp_tight = ndp_loose = 0\n        if dp_tight:\n            for vb in (0, 1):\n                if bitM and vb:\n                    continue\n                if vb > bitL:\n                    continue\n                if vb == bitL:\n                    ndp_tight += dp_tight\n                else:\n                    ndp_loose += dp_tight\n        if dp_loose:\n            for vb in (0, 1):\n                if bitM and vb:\n                    continue\n                ndp_loose += dp_loose\n        dp_tight, dp_loose = ndp_tight, ndp_loose\n    return dp_tight + dp_loose\n\ndef solve():\n    data = sys.stdin.read().strip().split()\n    if not data:\n        return\n    it = iter(data)\n    T = int(next(it))\n    out_lines = []\n    for _ in range(T):\n        N = int(next(it))\n        X = int(next(it))\n        base = N & X\n        if base >= N:\n            out_lines.append('0')\n            continue\n        limit = N - base\n        ans = count_valid(limit, X)\n        out_lines.append(str(ans))\n    sys.stdout.write('\\n'.join(out_lines))\n\nif __name__ == \"__main__\":\n    solve()\n",
      "JavaScript": "const fs = require('fs');\nconst input = fs.readFileSync(0, 'utf-8').trim().split(/\\s+/);\nlet idx = 0;\nconst T = Number(input[idx++]);\nlet output = [];\nfunction countValid(limit, mask) {\n  let dpTight = 1n, dpLoose = 0n;\n  for (let i = 30; i >= 0; --i) {\n    const bitL = (limit >> i) & 1;\n    const bitM = (mask >> i) & 1;\n    let ndpTight = 0n, ndpLoose = 0n;\n    if (dpTight) {\n      for (let vb = 0; vb <= 1; ++vb) {\n        if (bitM && vb) continue;\n        if (vb > bitL) continue;\n        if (vb === bitL) ndpTight += dpTight;\n        else ndpLoose += dpTight;\n      }\n    }\n    if (dpLoose) {\n      for (let vb = 0; vb <= 1; ++vb) {\n        if (bitM && vb) continue;\n        ndpLoose += dpLoose;\n      }\n    }\n    dpTight = ndpTight;\n    dpLoose = ndpLoose;\n  }\n  return dpTight + dpLoose;\n}\nfor (let t = 0; t < T; ++t) {\n  const N = Number(input[idx++]);\n  const X = Number(input[idx++]);\n  const base = N & X;\n  if (base >= N) {\n    output.push('0');\n    continue;\n  }\n  const limit = N - base;\n  const ans = countValid(limit, X);\n  output.push(ans.toString());\n}\nconsole.log(output.join('\\n'));\n"
    }
  },
  "QBCategory": "CCD",
  "boilerPlates": {
    "C": "#include <stdio.h>\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "C++": "#include <iostream>\nusing namespace std;\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "Java": "import java.util.*;\nimport java.io.*;\n\npublic class Main {\n    public static void main(String[] args) throws IOException {\n        // your code goes here\n    }\n}",
    "Python3": "# your code goes here\n",
    "JavaScript": "// your code goes here\n"
  },
  "_internal": {
    "methodName": "",
    "params": []
  }
}