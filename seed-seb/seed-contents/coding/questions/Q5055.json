{
  "questionId": "Q5055",
  "title": "String Character Analyzer",
  "slug": "string-character-analyzer",
  "version": 1,
  "createdAt": "2026-07-01",
  "updatedAt": "2026-07-04",
  "metadata": {
    "category": "Strings",
    "difficulty": "Easy",
    "isPremium": true,
    "tags": [
      "String",
      "Character Classification",
      "Counting",
      "Implementation"
    ],
    "companyTags": [],
    "topicTags": [
      "Strings in Python"
    ],
    "QBCategory": "CCD"
  },
  "content": {
    "problemStatement": "**Q3. B):** You are working on a text processing utility. Your task is to create a Python function named `analyze_string` that takes a single argument. This function should categorize and count the characters within the given string.\n\nThe function must count four types of characters:\n\n\n**Digits**: Any character from '0' to '9'.\n**Alphabets**: Any character from 'a' to 'z' or 'A' to 'Z' (case-sensitive).\n**Whitespaces**: Characters like space, tab (`\\t`), or newline (`\\n`).\n**Special Characters**: Any character that does not fall into the above three categories.\nThe function should return a dictionary containing the counts for these four categories. If the input provided to the function is not a string, it must handle this error gracefully by returning the exact string `\"Error: Input must be a string.\"`.\n\n\nInput Format\nThe function (input_data) will be called with a single argument `input_data`.\n\n\n\nOutput Format\n\nIf `input_data` is a string, return a dictionary of the format `{'digits': D, 'alphabets': A, 'whitespaces': W, 'special': S}`, where D, A, W, and S are the integer counts.\nIf `input_data` is not a string, return the string `\"Error: Input must be a string.\"`.",
    "constraints": [
      "1 ≤ T ≤ 100",
      "0 ≤ |S| ≤ 100",
      "All characters are standard ASCII"
    ],
    "inputFormat": "The first line contains an integer T (1 ≤ T ≤ 100), the number of test cases. Each of the next T lines contains a string S (may include spaces, tabs). The length of S does not exceed 100 characters.",
    "outputFormat": "For each test case output a single line containing a dictionary in the exact format {'digits': D, 'alphabets': A, 'whitespaces': W, 'special': S} where D, A, W and S are the counts of digit characters, alphabetic characters, whitespace characters and all other characters respectively. If the provided input is not a string (which cannot happen with the described I/O), output the exact string \"Error: Input must be a string.\".",
    "sampleTestCases": [
      {
        "id": "sample_1",
        "input": "1\nHello World! 123",
        "output": "{'digits': 3, 'alphabets': 10, 'whitespaces': 2, 'special': 1}",
        "explanation": "The string contains digits 1,2,3 (3), alphabets H e l l o W o r l d (10), two spaces (2) and one special character '!'."
      },
      {
        "id": "sample_2",
        "input": "1\n100",
        "output": "{'digits': 3, 'alphabets': 0, 'whitespaces': 0, 'special': 0}",
        "explanation": "All three characters are digits; there are no alphabets, whitespaces or special characters."
      },
      {
        "id": "sample_3",
        "input": "2\n\\tTab\n!@# $%^&*()",
        "output": "{'digits': 0, 'alphabets': 3, 'whitespaces': 1, 'special': 0}\n{'digits': 0, 'alphabets': 0, 'whitespaces': 1, 'special': 10}",
        "explanation": "First test case: a leading tab (whitespace) and the letters T a b (3 alphabets). No digits or specials. Second test case: one space (whitespace) and ten punctuation symbols, all counted as special."
      }
    ]
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "label": "minimum boundary",
        "input": "1\n",
        "expectedOutput": "{'digits': 0, 'alphabets': 0, 'whitespaces': 0, 'special': 0}",
        "weight": 2
      },
      {
        "id": "tc_2",
        "label": "maximum boundary",
        "input": "1\n1111111111111111111111111aaaaaaaaaaaaaaaaaaaaaaaaa                         !!!!!!!!!!!!!!!!!!!!!!!!!",
        "expectedOutput": "{'digits': 25, 'alphabets': 25, 'whitespaces': 25, 'special': 25}",
        "weight": 2
      },
      {
        "id": "tc_3",
        "label": "edge case",
        "input": "1\n\\t  \\t",
        "expectedOutput": "{'digits': 0, 'alphabets': 0, 'whitespaces': 4, 'special': 0}",
        "weight": 2
      },
      {
        "id": "tc_4",
        "label": "general case 1",
        "input": "1\nC0d3 #2021!",
        "expectedOutput": "{'digits': 6, 'alphabets': 2, 'whitespaces': 1, 'special': 2}",
        "weight": 2
      },
      {
        "id": "tc_5",
        "label": "general case 2",
        "input": "1\n   ",
        "expectedOutput": "{'digits': 0, 'alphabets': 0, 'whitespaces': 3, 'special': 0}",
        "weight": 2
      }
    ],
    "totalTestCases": 5,
    "totalWeight": 10
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      2,
      2,
      2,
      2,
      2
    ],
    "maxScore": 10
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "For each test case iterate over the characters of the given string. Use language‑specific predicates to decide the class of each character: digit, alphabetic, whitespace, or special. Increment the corresponding counter. After processing the whole line, output the counters in the required dictionary format. The algorithm is linear in the length of the string and uses O(1) extra memory.",
    "timeComplexity": "O(total length of all strings) – each character is examined once.",
    "spaceComplexity": "O(1) – only a few integer counters are stored.",
    "code": {
      "C": "#include <stdio.h>\n#include <string.h>\n#include <ctype.h>\n\nint main() {\n    int T;\n    if (scanf(\"%d\", &T) != 1) return 0;\n    getchar(); // consume newline after T\n    char buffer[105];\n    for (int tc = 0; tc < T; ++tc) {\n        if (!fgets(buffer, sizeof(buffer), stdin)) break;\n        size_t len = strlen(buffer);\n        if (len && buffer[len-1] == '\\n') buffer[--len] = '\\0';\n        int digits = 0, alphabets = 0, whitespaces = 0, special = 0;\n        for (size_t i = 0; i < len; ++i) {\n            unsigned char ch = buffer[i];\n            if (isdigit(ch)) digits++;\n            else if (isalpha(ch)) alphabets++;\n            else if (isspace(ch)) whitespaces++;\n            else special++;\n        }\n        printf(\"{'digits': %d, 'alphabets': %d, 'whitespaces': %d, 'special': %d}\\n\", digits, alphabets, whitespaces, special);\n    }\n    return 0;\n}",
      "C++": "#include <bits/stdc++.h>\nusing namespace std;\n\nint main() {\n    ios::sync_with_stdio(false);\n    cin.tie(nullptr);\n    int T;\n    if(!(cin >> T)) return 0;\n    string line;\n    getline(cin, line); // consume endline after T\n    for (int tc = 0; tc < T; ++tc) {\n        getline(cin, line);\n        int digits = 0, alphabets = 0, whitespaces = 0, special = 0;\n        for (char ch : line) {\n            unsigned char u = static_cast<unsigned char>(ch);\n            if (isdigit(u)) digits++;\n            else if (isalpha(u)) alphabets++;\n            else if (isspace(u)) whitespaces++;\n            else special++;\n        }\n        cout << \"{'digits': \" << digits << \", 'alphabets': \" << alphabets << \", 'whitespaces': \" << whitespaces << \", 'special': \" << special << \"}\\n\";\n    }\n    return 0;\n}",
      "Java": "import java.io.*;\nimport java.util.*;\n\npublic class Main {\n    public static void main(String[] args) throws Exception {\n        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));\n        String first = br.readLine();\n        if (first == null) return;\n        int T = Integer.parseInt(first.trim());\n        for (int tc = 0; tc < T; ++tc) {\n            String line = br.readLine();\n            if (line == null) line = \"\";\n            int digits = 0, alphabets = 0, whitespaces = 0, special = 0;\n            for (char ch : line.toCharArray()) {\n                if (Character.isDigit(ch)) digits++;\n                else if (Character.isAlphabetic(ch)) alphabets++;\n                else if (Character.isWhitespace(ch)) whitespaces++;\n                else special++;\n            }\n            System.out.println(\"{'digits': \" + digits + \", 'alphabets': \" + alphabets + \", 'whitespaces': \" + whitespaces + \", 'special': \" + special + \"}\");\n        }\n    }\n}",
      "Python3": "import sys\n\ndef analyze_string(s):\n    if not isinstance(s, str):\n        return \"Error: Input must be a string.\"\n    result = {'digits': 0, 'alphabets': 0, 'whitespaces': 0, 'special': 0}\n    for ch in s:\n        if ch.isdigit():\n            result['digits'] += 1\n        elif ch.isalpha():\n            result['alphabets'] += 1\n        elif ch.isspace():\n            result['whitespaces'] += 1\n        else:\n            result['special'] += 1\n    return result\n\ndata = sys.stdin.read().splitlines()\nif not data:\n    sys.exit()\nT = int(data[0].strip())\nfor i in range(1, T + 1):\n    line = data[i] if i < len(data) else ''\n    res = analyze_string(line)\n    if isinstance(res, dict):\n        print(f\"{{'digits': {res['digits']}, 'alphabets': {res['alphabets']}, 'whitespaces': {res['whitespaces']}, 'special': {res['special']}}}\")\n    else:\n        print(res)\n",
      "JavaScript": "const fs = require('fs');\nconst input = fs.readFileSync(0, 'utf-8').split(/\\n/);\nlet idx = 0;\nconst T = parseInt((input[idx++] || '').trim(), 10) || 0;\nfunction analyzeString(str) {\n  if (typeof str !== 'string') return \"Error: Input must be a string.\";\n  let digits = 0, alphabets = 0, whitespaces = 0, special = 0;\n  for (const ch of str) {\n    if (/[0-9]/.test(ch)) digits++;\n    else if (/[a-zA-Z]/.test(ch)) alphabets++;\n    else if (/\\s/.test(ch)) whitespaces++;\n    else special++;\n  }\n  return `{'digits': ${digits}, 'alphabets': ${alphabets}, 'whitespaces': ${whitespaces}, 'special': ${special}}`;\n}\nlet outputs = [];\nfor (let t = 0; t < T; ++t) {\n  const line = input[idx++] ?? '';\n  outputs.push(analyzeString(line));\n}\nconsole.log(outputs.join('\\n'));\n"
    }
  },
  "QBCategory": "CCD",
  "boilerPlates": {
    "C": "#include <stdio.h>\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "C++": "#include <iostream>\nusing namespace std;\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "Java": "import java.util.*;\nimport java.io.*;\n\npublic class Main {\n    public static void main(String[] args) throws IOException {\n        // your code goes here\n    }\n}",
    "Python3": "# your code goes here\n",
    "JavaScript": "// your code goes here\n"
  },
  "_internal": {
    "methodName": "",
    "params": []
  }
}