{
  "questionId": "Q1370",
  "title": "Find Intersection of two linked lists",
  "slug": "find-intersection-of-two-linked-lists",
  "version": 1,
  "createdAt": "2026-07-01",
  "updatedAt": "2026-07-04",
  "metadata": {
    "category": "Data Structures",
    "difficulty": "Easy",
    "isPremium": false,
    "tags": [
      "Data Structures",
      "Intersection Point",
      "Linked Lists"
    ],
    "companyTags": [],
    "topicTags": [
      "Hashing"
    ]
  },
  "content": {
    "problemStatement": "Given two linked lists , find the point of intersection of both the lists from where the values are common in both the lists. The length of linkedlist1 will be NNN and of linkedlist2 will be MMM.\n\nThe task is to complete the function intersectPoint() which takes the pointer to the head of linklist1(head1) and linklist2(head2) as input parameters and returns the data value of a node where two linked lists intersect. If the linked list does not merge at any point, then it should return \u22121-1\u22121.\n\n\nInput Format\n\nFirst line contains three numbers, xxx (number of nodes before merge point in 1st list), yyy (number of nodes before merge point in 2nd list) and zzz (number of nodes after merge point).\nNext three lines contain xxx, yyy and zzz numbers respectively.\nNNN will be equal to the sum of xxx and zzz.\nMMM will be equal to the sum of yyy and zzz.\n\n\nOutput Format\n\nPrint the value of a node that intersect and if it does not intersect print \u22121-1\u22121.",
    "constraints": [
      "1 <= x, y, z <= 100000",
      "1 <= T <= 100",
      "1 <= N, M <= 1000001"
    ],
    "inputFormat": "First line contains three integers, x, y, and z, representing the number of nodes before the merge point in the first list, the number of nodes before the merge point in the second list, and the number of nodes after the merge point, respectively. The next three lines contain x, y, and z integers, respectively, representing the values of the nodes in the first list before the merge point, the values of the nodes in the second list before the merge point, and the values of the nodes after the merge point, respectively.",
    "outputFormat": "The output should be the value of the node where the two linked lists intersect. If the linked lists do not intersect, the output should be -1.",
    "sampleTestCases": [
      {
        "id": "sample_1",
        "input": "3 1 2\n3 6 9\n10\n15 30",
        "output": "15",
        "explanation": "Explanation:\nThe first linked list is 3->6->9->15->30\nThe second linked list is 10->15->30\nIn both the linked lists the values from node 15 start matching so the common part that is intersecting is 15->30."
      },
      {
        "id": "sample_2",
        "input": "3 1 0\n1 2 3\n4",
        "output": "-1",
        "explanation": "Explanation:\nThe first linked list is 1->2->3\nSecond one is 4\nNo elements are common elements so -1."
      }
    ]
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "label": "Case 1",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_2",
        "label": "Case 2",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_3",
        "label": "Case 3",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_4",
        "label": "Case 4",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_5",
        "label": "Case 5",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      }
    ],
    "totalTestCases": 5,
    "totalWeight": 10
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      2,
      2,
      2,
      2,
      2
    ],
    "maxScore": 10
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "To find the intersection point of two linked lists, we can use a set to store the nodes of the first list and then traverse the second list to check for intersection. The key insight is that if the two lists intersect, the intersection point will be a node that is common to both lists. We can use a set to keep track of the nodes in the first list and then check each node in the second list to see if it is in the set. If we find a node that is in the set, we have found the intersection point.",
    "timeComplexity": "O(N + M) per test case, where N and M are the lengths of the two linked lists. The total time complexity is O(T * (N + M))",
    "spaceComplexity": "O(N) for storing the nodes of the first list in a set.",
    "code": {
      "C": "#include <stdio.h>\n#include <stdlib.h>\n\n// Link list Node\nstruct Node {\n    int data;\n    struct Node* next;\n};\n\n// Function to find intersection point in Y shaped Linked Lists\nint intersectPoint(struct Node* head1, struct Node* head2) {\n    // Create a set to store the nodes of the first list\n    struct Node* nodes[100000]; // Assuming a reasonable size for the array\n    int index = 0;\n    \n    // Traverse the first list and store each node in the array\n    struct Node* temp1 = head1;\n    while (temp1 != NULL) {\n        nodes[index++] = temp1;\n        temp1 = temp1->next;\n    }\n    \n    // Traverse the second list and check for intersection\n    struct Node* temp2 = head2;\n    while (temp2 != NULL) {\n        for (int i = 0; i < index; i++) {\n            if (temp2 == nodes[i]) {\n                return temp2->data;\n            }\n        }\n        temp2 = temp2->next;\n    }\n    \n    // If no intersection is found\n    return -1;\n}\n\n// Function to take input and create a linked list\nstruct Node* inputList(int size, int v[]) {\n    if (size == 0) return NULL;\n    \n    struct Node* head = (struct Node*)malloc(sizeof(struct Node));\n    head->data = v[0];\n    head->next = NULL;\n    struct Node* tail = head;\n    \n    for (int i = 1; i < size; i++) {\n        struct Node* newNode = (struct Node*)malloc(sizeof(struct Node));\n        newNode->data = v[i];\n        newNode->next = NULL;\n        tail->next = newNode;\n        tail = newNode;\n    }\n    \n    return head;\n}\n\nint main() {\n    int T = 1; // Number of test cases\n    \n    while (T--) {\n        int n1, n2, n3;\n        scanf(\"%d %d %d\", &n1, &n2, &n3);\n        \n        int v1[n1], v2[n2], v3[n3];\n        \n        for (int i = 0; i < n1; i++) scanf(\"%d\", &v1[i]);\n        for (int i = 0; i < n2; i++) scanf(\"%d\", &v2[i]);\n        for (int i = 0; i < n3; i++) scanf(\"%d\", &v3[i]);\n        \n        struct Node* head1 = NULL;\n        struct Node* head2 = NULL;\n        struct Node* common = NULL;\n        \n        head1 = inputList(n1, v1);\n        head2 = inputList(n2, v2);\n        common = inputList(n3, v3);\n        \n        // Connect common part to both lists\n        struct Node* temp = head1;\n        while (temp && temp->next) temp = temp->next;\n        if (temp) temp->next = common;\n        \n        temp = head2;\n        while (temp && temp->next) temp = temp->next;\n        if (temp) temp->next = common;\n        \n        printf(\"%d\\n\", intersectPoint(head1, head2));\n    }\n    \n    return 0;\n}",
      "C++": "#include <bits/stdc++.h>\nusing namespace std;\n\n// Link list Node\nstruct Node {\n    int data;\n    struct Node* next;\n    Node(int x) {\n        data = x;\n        next = NULL;\n    }\n};\n\n// Function to find intersection point in Y shaped Linked Lists\nint intersectPoint(struct Node* head1, struct Node* head2) {\n    // Create a set to store the nodes of the first list\n    unordered_set<Node*> nodes;\n    \n    // Traverse the first list and store each node in the set\n    Node* temp1 = head1;\n    while (temp1 != nullptr) {\n        nodes.insert(temp1);\n        temp1 = temp1->next;\n    }\n    \n    // Traverse the second list and check for intersection\n    Node* temp2 = head2;\n    while (temp2 != nullptr) {\n        if (nodes.find(temp2) != nodes.end()) {\n            return temp2->data;\n        }\n        temp2 = temp2->next;\n    }\n    \n    // If no intersection is found\n    return -1;\n}\n\n// Function to take input and create a linked list\nNode* inputList(int size, vector<int> v) {\n    if (size == 0) return NULL;\n    \n    int val = v[0];\n    \n    Node* head = new Node(val);\n    Node* tail = head;\n    \n    for (int i = 0; i < size - 1; i++) {\n        val = v[i + 1];\n        tail->next = new Node(val);\n        tail = tail->next;\n    }\n    \n    return head;\n}\n\nint main() {\n    int T = 1; // Number of test cases\n    \n    while (T--) {\n        int n1, n2, n3;\n        cin >> n1 >> n2 >> n3;\n        \n        vector<int> v1(n1), v2(n2), v3(n3);\n        \n        for (int i = 0; i < n1; i++) cin >> v1[i];\n        for (int i = 0; i < n2; i++) cin >> v2[i];\n        for (int i = 0; i < n3; i++) cin >> v3[i];\n        \n        Node* head1 = NULL;\n        Node* head2 = NULL;\n        Node* common = NULL;\n        \n        head1 = inputList(n1, v1);\n        head2 = inputList(n2, v2);\n        common = inputList(n3, v3);\n        \n        Node* temp = head1;\n        while (temp != NULL && temp->next != NULL) temp = temp->next;\n        if (temp) temp->next = common;\n        \n        temp = head2;\n        while (temp != NULL && temp->next != NULL) temp = temp->next;\n        if (temp) temp->next = common;\n        \n        cout << intersectPoint(head1, head2) << endl;\n    }\n    \n    return 0;\n}",
      "Java": "import java.util.HashSet;\nimport java.util.Scanner;\nimport java.util.Set;\n\n// Link list Node\nclass Node {\n    int data;\n    Node next;\n    \n    Node(int x) {\n        data = x;\n        next = null;\n    }\n}\n\npublic class Main {\n\n    // Function to find intersection point in Y shaped Linked Lists\n    public static int intersectPoint(Node head1, Node head2) {\n        // Create a set to store the nodes of the first list\n        Set<Node> nodes = new HashSet<>();\n        \n        // Traverse the first list and store each node in the set\n        Node temp1 = head1;\n        while (temp1 != null) {\n            nodes.add(temp1);\n            temp1 = temp1.next;\n        }\n        \n        // Traverse the second list and check for intersection\n        Node temp2 = head2;\n        while (temp2 != null) {\n            if (nodes.contains(temp2)) {\n                return temp2.data;\n            }\n            temp2 = temp2.next;\n        }\n        \n        // If no intersection is found\n        return -1;\n    }\n\n    // Function to take input and create a linked list\n    public static Node inputList(int size, int[] v) {\n        if (size == 0) return null;\n        \n        Node head = new Node(v[0]);\n        Node tail = head;\n        \n        for (int i = 1; i < size; i++) {\n            tail.next = new Node(v[i]);\n            tail = tail.next;\n        }\n        \n        return head;\n    }\n\n    public static void main(String[] args) {\n        Scanner scanner = new Scanner(System.in);\n        int T = 1; // Number of test cases\n        \n        while (T-- > 0) {\n            int n1 = scanner.nextInt(); // Size of the first list\n            int n2 = scanner.nextInt(); // Size of the second list\n            int n3 = scanner.nextInt(); // Size of the common list\n            \n            int[] v1 = new int[n1];\n            int[] v2 = new int[n2];\n            int[] v3 = new int[n3];\n            \n            for (int i = 0; i < n1; i++) v1[i] = scanner.nextInt();\n            for (int i = 0; i < n2; i++) v2[i] = scanner.nextInt();\n            for (int i = 0; i < n3; i++) v3[i] = scanner.nextInt();\n            \n            Node head1 = null;\n            Node head2 = null;\n            Node common = null;\n            \n            head1 = inputList(n1, v1);\n            head2 = inputList(n2, v2);\n            common = inputList(n3, v3);\n            \n            Node temp = head1;\n            while (temp != null && temp.next != null)\n                temp = temp.next;\n            if (temp != null) temp.next = common;\n            \n            temp = head2;\n            while (temp != null && temp.next != null)\n                temp = temp.next;\n            if (temp != null) temp.next = common;\n            \n            System.out.println(intersectPoint(head1, head2));\n        }\n        \n        scanner.close();\n    }\n}",
      "Python3": "# Definition for singly-linked list node\nclass Node:\n    def __init__(self, x):\n        self.data = x\n        self.next = None\n\n# Function to find intersection point in Y shaped Linked Lists\ndef intersectPoint(head1, head2):\n    # Create a set to store the nodes of the first list\n    nodes = set()\n    \n    # Traverse the first list and store each node in the set\n    temp1 = head1\n    while temp1:\n        nodes.add(temp1)\n        temp1 = temp1.next\n    \n    # Traverse the second list and check for intersection\n    temp2 = head2\n    while temp2:\n        if temp2 in nodes:\n            return temp2.data\n        temp2 = temp2.next\n    \n    # If no intersection is found\n    return -1\n\n# Function to take input and create a linked list\ndef inputList(size, v):\n    if size == 0:\n        return None\n    \n    head = Node(v[0])\n    tail = head\n    \n    for i in range(1, size):\n        tail.next = Node(v[i])\n        tail = tail.next\n    \n    return head\n\nif __name__ == \"__main__\":\n    import sys\n    \n    T = 1  # Number of test cases\n    \n    while T > 0:\n        T -= 1\n        \n        n1, n2, n3 = list(map(int, input().split()))  # Size of the first list\n        # n2 = int(input())  # Size of the second list\n        # n3 = int(input())  # Size of the common list\n        \n        v1, v2, v3 = [], [], []\n        if n1 != 0:\n            v1 = list(map(int, input().split()))\n        if n2 != 0:\n            v2 = list(map(int, input().split()))\n        if n3 != 0:\n            v3 = list(map(int, input().split()))\n        \n        head1 = None\n        head2 = None\n        common = None\n        \n        head1 = inputList(n1, v1)\n        head2 = inputList(n2, v2)\n        common = inputList(n3, v3)\n        \n        temp = head1\n        while temp and temp.next:\n            temp = temp.next\n        if temp:\n            temp.next = common\n        \n        temp = head2\n        while temp and temp.next:\n            temp = temp.next\n        if temp:\n            temp.next = common\n        \n        print(intersectPoint(head1, head2))",
      "JavaScript": "const fs = require('fs');\nconst lines = fs.readFileSync(0, 'utf-8').trim().split('\\n');\nlet idx = 0;\n\n// Link list Node\nfunction Node(x) {\n    this.data = x;\n    this.next = null;\n}\n\n// Function to find intersection point in Y shaped Linked Lists\nfunction intersectPoint(head1, head2) {\n    // Create a set to store the nodes of the first list\n    let nodes = new Set();\n    \n    // Traverse the first list and store each node in the set\n    let temp1 = head1;\n    while (temp1 !== null) {\n        nodes.add(temp1);\n        temp1 = temp1.next;\n    }\n    \n    // Traverse the second list and check for intersection\n    let temp2 = head2;\n    while (temp2 !== null) {\n        if (nodes.has(temp2)) {\n            return temp2.data;\n        }\n        temp2 = temp2.next;\n    }\n    \n    // If no intersection is found\n    return -1;\n}\n\n// Function to take input and create a linked list\nfunction inputList(size, v) {\n    if (size === 0) return null;\n    \n    let head = new Node(v[0]);\n    let tail = head;\n    \n    for (let i = 1; i < size; i++) {\n        tail.next = new Node(v[i]);\n        tail = tail.next;\n    }\n    \n    return head;\n}\n\nlet T = +lines[idx++];\n\nwhile (T-- > 0) {\n    let [n1, n2, n3] = lines[idx++].trim().split(' ').map(Number);\n    \n    let v1 = lines[idx++].trim().split(' ').map(Number);\n    let v2 = lines[idx++].trim().split(' ').map(Number);\n    let v3 = lines[idx++].trim().split(' ').map(Number);\n    \n    let head1 = null;\n    let head2 = null;\n    let common = null;\n    \n    head1 = inputList(n1, v1);\n    head2 = inputList(n2, v2);\n    common = inputList(n3, v3);\n    \n    let temp = head1;\n    while (temp !== null && temp.next !== null) temp = temp.next;\n    if (temp !== null) temp.next = common;\n    \n    temp = head2;\n    while (temp !== null && temp.next !== null) temp = temp.next;\n    if (temp !== null) temp.next = common;\n    \n    console.log(intersectPoint(head1, head2));\n}"
    }
  }
}