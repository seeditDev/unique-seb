{
  "questionId": "Q2421",
  "title": "Moving between Floors",
  "slug": "moving-between-floors",
  "version": 1,
  "createdAt": "2026-07-01",
  "updatedAt": "2026-07-04",
  "metadata": {
    "category": "Graphs",
    "difficulty": "Hard",
    "isPremium": false,
    "tags": [
      "Dijkstra's Algorithm",
      "Graphs",
      "Graph Theory",
      "Shortest Path"
    ],
    "companyTags": [],
    "topicTags": [
      "Graphs"
    ]
  },
  "content": {
    "problemStatement": "Did you know that the world's tallest structure is Burj Khalifa in Dubai ? To survive when the world ends, Mr.Turing thinks that he should climb one of those skyscrapers ( tall buildings ). There are N tall buildings numbered b_1, b_2, ... , b_N in that order and b_i and b_i+1 are adjacent to each other for 1 \u2264 i &lt; N. Each of these have exactly F number of floors, f_1, f_2, ... , f_F from bottom to top. Floors f_i and f_i+1 in the same building are adjacent to each other for 1 \u2264 i &lt; F and it takes one unit of time to move between them. It also takes one unit of time to move between the floor f_1 of a building and f_1 of its adjacent building.\n\n\n\nTuring is afraid that some buildings may still collapse and so he lists down the bridges connecting some of them, to know how to escape quickly. There are M bridges, each of which connects two floors of different buildings. Each of these bridges is given as bi fi T bj fj, meaning it takes T units of time to move along this bridge which connects the floor fi of building bi and the floor fj of building bj. All ways are bidirectional. Turing wants to know, given bq and fq, the sum of the shortest time it takes to reach (b,f) starting from (bq,fq), for all possible 1 \u2264 b \u2264 N ( except b = bq ) and 1 \u2264 f \u2264 F. In other words, given (bq,fq), find the value of \u03a3 D(bq,fq,b,f) for all possible (b,f) except b = bq, where D(bi,fi,bj,fj) is the shortest time it takes to reach floor fi of building bi from floor fj of building bj. Get ready to answer his Q such queries.",
    "constraints": [
      "1 <= N <= 1000",
      "1 <= F <= 1000",
      "1 <= M <= 100000",
      "1 <= Q <= 1000",
      "1 <= bi, bj <= N",
      "1 <= fi, fj <= F",
      "1 <= T <= 1000"
    ],
    "inputFormat": "First line contains three integers N, F, and M, where N is the number of buildings, F is the number of floors in each building, and M is the number of bridges. The next M lines describe the bridges, each in the format bi fi T bj fj, where bi and bj are the building numbers, fi and fj are the floor numbers, and T is the time it takes to move along this bridge. The next line contains an integer Q, the number of queries. The following Q lines describe the queries, each in the format bq fq, where bq is the building number and fq is the floor number.",
    "outputFormat": "For each query, output the sum of the shortest time it takes to reach each floor of every other building from the given building and floor.",
    "sampleTestCases": [
      {
        "id": "sample_1",
        "input": "2 4 1\n1 3 1 2 3\n3\n1 1\n1 2\n1 3\n",
        "output": "10\n10\n8",
        "explanation": "For the first query, the shortest path from building 1, floor 1 to building 2, floor 1 is 1 + 1 = 2. The shortest path from building 1, floor 1 to building 2, floor 2 is 1 + 1 + 1 = 3. The shortest path from building 1, floor 1 to building 2, floor 3 is 1 + 1 = 2. The shortest path from building 1, floor 1 to building 2, floor 4 is 1 + 1 + 1 = 3. The sum of these distances is 2 + 3 + 2 + 3 = 10."
      }
    ]
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "label": "Case 1",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_2",
        "label": "Case 2",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_3",
        "label": "Case 3",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_4",
        "label": "Case 4",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_5",
        "label": "Case 5",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      }
    ],
    "totalTestCases": 5,
    "totalWeight": 10
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      2,
      2,
      2,
      2,
      2
    ],
    "maxScore": 10
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "To solve this problem, we can use Dijkstra's algorithm to find the shortest path from the given building and floor to all other buildings and floors. We can create a graph where each node represents a building and floor, and each edge represents the time it takes to move between two nodes. We can then use Dijkstra's algorithm to find the shortest path from the given node to all other nodes, and sum up the distances to get the final answer.",
    "timeComplexity": "O(Q * (N * F + M * log(N * F))) per test case, O(T * Q * (N * F + M * log(N * F))) total.",
    "spaceComplexity": "O(N * F + M) \u2014 the dominant structure is the graph.",
    "code": {
      "C": "// COMPLETE, COMPILABLE C code\n#include <stdio.h>\n#include <stdlib.h>\n#include <limits.h>\n#define INF INT_MAX\n#define MAX_N 1000\n#define MAX_F 1000\n#define MAX_M 100000\n#define MAX_Q 1000\n\nstruct Edge {\n    int to;\n    int weight;\n};\n\nstruct Graph {\n    int numNodes;\n    int numEdges;\n    struct Edge* edges;\n};\n\nvoid dijkstra(struct Graph* graph, int start, int* distances) {\n    for (int i = 0; i < graph->numNodes; i++) {\n        distances[i] = INF;\n    }\n    distances[start] = 0;\n    int* visited = (int*) malloc(graph->numNodes * sizeof(int));\n    for (int i = 0; i < graph->numNodes; i++) {\n        visited[i] = 0;\n    }\n    for (int i = 0; i < graph->numNodes; i++) {\n        int minDistance = INF;\n        int minIndex = -1;\n        for (int j = 0; j < graph->numNodes; j++) {\n            if (!visited[j] && distances[j] < minDistance) {\n                minDistance = distances[j];\n                minIndex = j;\n            }\n        }\n        visited[minIndex] = 1;\n        for (int j = 0; j < graph->numEdges; j++) {\n            if (graph->edges[j].to == minIndex) {\n                int neighbor = graph->edges[j].to;\n                int weight = graph->edges[j].weight;\n                if (distances[minIndex] + weight < distances[neighbor]) {\n                    distances[neighbor] = distances[minIndex] + weight;\n                }\n            }\n        }\n    }\n    free(visited);\n}\n\nint main() {\n    int N, F, M;\n    scanf(\"%d %d %d\", &N, &F, &M);\n    struct Graph* graph = (struct Graph*) malloc(sizeof(struct Graph));\n    graph->numNodes = N * F;\n    graph->numEdges = M;\n    graph->edges = (struct Edge*) malloc(M * sizeof(struct Edge));\n    for (int i = 0; i < M; i++) {\n        int bi, fi, T, bj, fj;\n        scanf(\"%d %d %d %d %d\", &bi, &fi, &T, &bj, &fj);\n        graph->edges[i].to = (bj - 1) * F + fj - 1;\n        graph->edges[i].weight = T;\n    }\n    int Q;\n    scanf(\"%d\", &Q);\n    for (int i = 0; i < Q; i++) {\n        int bq, fq;\n        scanf(\"%d %d\", &bq, &fq);\n        int* distances = (int*) malloc(graph->numNodes * sizeof(int));\n        dijkstra(graph, (bq - 1) * F + fq - 1, distances);\n        int sum = 0;\n        for (int j = 0; j < graph->numNodes; j++) {\n            if (j / F != bq - 1) {\n                sum += distances[j];\n            }\n        }\n        printf(\"%d\\n\", sum);\n        free(distances);\n    }\n    free(graph->edges);\n    free(graph);\n    return 0;\n}",
      "C++": "// COMPLETE, COMPILABLE C++ code\n#include <iostream>\n#include <vector>\n#include <limits>\n#include <queue>\nusing namespace std;\n\nstruct Edge {\n    int to;\n    int weight;\n};\n\nstruct Graph {\n    int numNodes;\n    vector<Edge> edges;\n};\n\nvoid dijkstra(Graph& graph, int start, vector<int>& distances) {\n    distances.assign(graph.numNodes, INT_MAX);\n    distances[start] = 0;\n    priority_queue<pair<int, int>, vector<pair<int, int>>, greater<pair<int, int>>> pq;\n    pq.push({0, start});\n    while (!pq.empty()) {\n        int current = pq.top().second;\n        pq.pop();\n        for (const auto& edge : graph.edges) {\n            if (edge.to == current) {\n                int neighbor = edge.to;\n                int weight = edge.weight;\n                if (distances[current] + weight < distances[neighbor]) {\n                    distances[neighbor] = distances[current] + weight;\n                    pq.push({distances[neighbor], neighbor});\n                }\n            }\n        }\n    }\n}\n\nint main() {\n    int N, F, M;\n    cin >> N >> F >> M;\n    Graph graph;\n    graph.numNodes = N * F;\n    for (int i = 0; i < M; i++) {\n        int bi, fi, T, bj, fj;\n        cin >> bi >> fi >> T >> bj >> fj;\n        graph.edges.push_back({(bj - 1) * F + fj - 1, T});\n    }\n    int Q;\n    cin >> Q;\n    for (int i = 0; i < Q; i++) {\n        int bq, fq;\n        cin >> bq >> fq;\n        vector<int> distances;\n        dijkstra(graph, (bq - 1) * F + fq - 1, distances);\n        int sum = 0;\n        for (int j = 0; j < graph.numNodes; j++) {\n            if (j / F != bq - 1) {\n                sum += distances[j];\n            }\n        }\n        cout << sum << endl;\n    }\n    return 0;\n}",
      "Java": "// COMPLETE, COMPILABLE Java code\nimport java.util.PriorityQueue;\nimport java.util.Scanner;\n\npublic class Main {\n    static class Edge {\n        int to;\n        int weight;\n        Edge(int to, int weight) {\n            this.to = to;\n            this.weight = weight;\n        }\n    }\n\n    static class Graph {\n        int numNodes;\n        Edge[] edges;\n    }\n\n    public static void dijkstra(Graph graph, int start, int[] distances) {\n        for (int i = 0; i < graph.numNodes; i++) {\n            distances[i] = Integer.MAX_VALUE;\n        }\n        distances[start] = 0;\n        PriorityQueue<int[]> pq = new PriorityQueue<>((a, b) -> a[0] - b[0]);\n        pq.offer(new int[] {0, start});\n        while (!pq.isEmpty()) {\n            int[] current = pq.poll();\n            for (Edge edge : graph.edges) {\n                if (edge.to == current[1]) {\n                    int neighbor = edge.to;\n                    int weight = edge.weight;\n                    if (distances[current[1]] + weight < distances[neighbor]) {\n                        distances[neighbor] = distances[current[1]] + weight;\n                        pq.offer(new int[] {distances[neighbor], neighbor});\n                    }\n                }\n            }\n        }\n    }\n\n    public static void main(String[] args) {\n        Scanner scanner = new Scanner(System.in);\n        int N = scanner.nextInt();\n        int F = scanner.nextInt();\n        int M = scanner.nextInt();\n        Graph graph = new Graph();\n        graph.numNodes = N * F;\n        graph.edges = new Edge[M];\n        for (int i = 0; i < M; i++) {\n            int bi = scanner.nextInt();\n            int fi = scanner.nextInt();\n            int T = scanner.nextInt();\n            int bj = scanner.nextInt();\n            int fj = scanner.nextInt();\n            graph.edges[i] = new Edge((bj - 1) * F + fj - 1, T);\n        }\n        int Q = scanner.nextInt();\n        for (int i = 0; i < Q; i++) {\n            int bq = scanner.nextInt();\n            int fq = scanner.nextInt();\n            int[] distances = new int[graph.numNodes];\n            dijkstra(graph, (bq - 1) * F + fq - 1, distances);\n            int sum = 0;\n            for (int j = 0; j < graph.numNodes; j++) {\n                if (j / F != bq - 1) {\n                    sum += distances[j];\n                }\n            }\n            System.out.println(sum);\n        }\n    }\n}",
      "Python3": "# COMPLETE, RUNNABLE Python3 code\nimport sys\nimport heapq\n\nclass Edge:\n    def __init__(self, to, weight):\n        self.to = to\n        self.weight = weight\n\nclass Graph:\n    def __init__(self, numNodes):\n        self.numNodes = numNodes\n        self.edges = []\n\n    def addEdge(self, edge):\n        self.edges.append(edge)\n\ndef dijkstra(graph, start, distances):\n    distances = [float('inf')] * graph.numNodes\n    distances[start] = 0\n    pq = [(0, start)]\n    while pq:\n        current = heapq.heappop(pq)\n        for edge in graph.edges:\n            if edge.to == current[1]:\n                neighbor = edge.to\n                weight = edge.weight\n                if distances[current[1]] + weight < distances[neighbor]:\n                    distances[neighbor] = distances[current[1]] + weight\n                    heapq.heappush(pq, (distances[neighbor], neighbor))\n    return distances\n\ninput = sys.stdin.readline\nN, F, M = map(int, input().split())\ngraph = Graph(N * F)\nfor _ in range(M):\n    bi, fi, T, bj, fj = map(int, input().split())\n    graph.addEdge(Edge((bj - 1) * F + fj - 1, T))\nQ = int(input())\nfor _ in range(Q):\n    bq, fq = map(int, input().split())\n    distances = dijkstra(graph, (bq - 1) * F + fq - 1, [0] * graph.numNodes)\n    sum = 0\n    for j in range(graph.numNodes):\n        if j // F != bq - 1:\n            sum += distances[j]\n    print(sum)\n",
      "JavaScript": "// COMPLETE, RUNNABLE JavaScript code\nconst fs = require('fs');\nconst lines = fs.readFileSync(0, 'utf-8').trim().split('\\n');\nlet idx = 0;\n\nfunction readline() {\n    return lines[idx++];\n}\n\nclass Edge {\n    constructor(to, weight) {\n        this.to = to;\n        this.weight = weight;\n    }\n}\n\nclass Graph {\n    constructor(numNodes) {\n        this.numNodes = numNodes;\n        this.edges = [];\n    }\n\n    addEdge(edge) {\n        this.edges.push(edge);\n    }\n}\n\nfunction dijkstra(graph, start, distances) {\n    distances = new Array(graph.numNodes).fill(Infinity);\n    distances[start] = 0;\n    let pq = [[0, start]];\n    while (pq.length > 0) {\n        let current = pq.shift();\n        for (let edge of graph.edges) {\n            if (edge.to === current[1]) {\n                let neighbor = edge.to;\n                let weight = edge.weight;\n                if (distances[current[1]] + weight < distances[neighbor]) {\n                    distances[neighbor] = distances[current[1]] + weight;\n                    pq.push([distances[neighbor], neighbor]);\n                    pq.sort((a, b) => a[0] - b[0]);\n                }\n            }\n        }\n    }\n    return distances;\n}\n\nlet N, F, M;\n[N, F, M] = readline().split(' ').map(Number);\nlet graph = new Graph(N * F);\nfor (let i = 0; i < M; i++) {\n    let [bi, fi, T, bj, fj] = readline().split(' ').map(Number);\n    graph.addEdge(new Edge((bj - 1) * F + fj - 1, T));\n}\nlet Q = Number(readline());\nfor (let i = 0; i < Q; i++) {\n    let [bq, fq] = readline().split(' ').map(Number);\n    let distances = dijkstra(graph, (bq - 1) * F + fq - 1, new Array(graph.numNodes).fill(0));\n    let sum = 0;\n    for (let j = 0; j < graph.numNodes; j++) {\n        if (j / F !== bq - 1) {\n            sum += distances[j];\n        }\n    }\n    console.log(sum);\n}\n"
    }
  }
}