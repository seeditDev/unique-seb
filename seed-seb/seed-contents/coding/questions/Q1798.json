{
  "questionId": "Q1798",
  "title": "Chef Merges Accounts",
  "slug": "chef-merges-accounts",
  "version": 1,
  "createdAt": "2026-07-01",
  "updatedAt": "2026-07-04",
  "metadata": {
    "category": "Graphs",
    "difficulty": "Medium",
    "isPremium": false,
    "tags": [
      "Graph",
      "Graphs",
      "Disjoint Set Union"
    ],
    "companyTags": [],
    "topicTags": [
      "Disjoint Set Union"
    ]
  },
  "content": {
    "problemStatement": "Chef has many accounts, each with a name and a list of email addresses. However, some of these accounts actually belong to the same person if they share at least one common email address. \n\nChef wants to merge such accounts so that each person\u2019s information is consolidated into a single account entry. Even if two accounts have the same name, they might represent different people unless they share an email. \n\nAfter merging, Chef wants each merged account to start with the person\u2019s name followed by all unique email addresses sorted lexicographically. Help Chef perform this merging efficiently.\n\nFunction DeclarationFunction NamemergeAccountsmergeAccountsmergeAccounts - This function merges multiple account entries that share common email addresses into consolidated accounts.\n\nParameters\naccountListsaccountListsaccountLists : A list of accounts, where each account is represented as array of strings.\n\nThe first element of each account is the account name.\nThe subsequent elements are one or more email addresses associated with that account.\n\n\nReturn Value\nReturns a array of strings of merged accounts.\nEach merged account contains the account name followed by a sorted list of unique email addresses.\nAll accounts sharing at least one common email are consolidated into a single account entry.\nConstraints\n1\u2264T\u22641031 \\leq T \\leq 10^31\u2264T\u2264103\n1\u2264N\u226410001 \\leq N \\leq 10001\u2264N\u22641000\n2\u22642 \\leq2\u2264 length of each account (including name and emails) \u226410\\leq 10\u226410\n1\u22641 \\leq1\u2264 length of each name and email \u226430\\leq 30\u226430\nNames and emails consist only of English letters, digits, '@', and '.' characters.\nEmails are guaranteed to be valid and unique within an account but may appear in multiple accounts.\n\nInput Format\n\nThe first line contains a single integer TTT \u2014 the number of test cases.\nFor each test case:\n\nThe first line contains an integer NNN \u2014 the number of accounts Chef has.\nThe following NNN lines each describe an account:\n\nThe first string is the account name.\nThe next strings on the same line are the email addresses associated with that account.\n\n\n\n\n\n\nOutput Format\n\nFor each test case, print the merged accounts.\nEach merged account should be printed on a new line starting with the account name followed by the sorted unique emails separated by spaces.\nThe order of merged accounts in the output does not matter.",
    "constraints": [
      "1 \u2264 T \u2264 10^3",
      "1 \u2264 N \u2264 1000",
      "2 \u2264 length of each account (including name and emails) \u2264 10",
      "1 \u2264 length of each name and email \u2264 30",
      "Names and emails consist only of English letters, digits, '@', and '.' characters."
    ],
    "inputFormat": "The first line contains a single integer T, the number of test cases. Each test case contains a single integer N, the number of accounts. The following N lines each describe an account: The first string is the account name. The next strings on the same line are the email addresses associated with that account.",
    "outputFormat": "For each test case, print the merged accounts. Each merged account should be printed on a new line starting with the account name followed by the sorted unique emails separated by spaces.",
    "sampleTestCases": [
      {
        "id": "sample_1",
        "input": "2\n3\nJohn john.doe@mail.com john.doe123@mail.com\nJohn john.doe123@mail.com johnny@mail.com\nJane jane@mail.com\n4\nAlex alex@mail.com\nAlex alex123@mail.com\nAlex alex@mail.com alex123@mail.com\nBob bob@mail.com",
        "output": "John john.doe123@mail.com john.doe@mail.com johnny@mail.com\nJane jane@mail.com\nBob bob@mail.com\nAlex alex123@mail.com alex@mail.com",
        "explanation": "Explanation:\nTest case 1: The first two John accounts share \"john.doe123@mail.com\", so they merge into one with all three emails combined. Jane's account is separate with no overlapping emails.\nTest case 2: The first two Alex accounts have distinct emails, but the third includes both emails, merging all Alex accounts into one. Bob's account remains separate."
      },
      {
        "id": "sample_2",
        "input": "1\n5\nSam sam1@mail.com sam2@mail.com\nSam sam3@mail.com\nSam sam2@mail.com sam4@mail.com\nChris chris@mail.com\nSam sam5@mail.com",
        "output": "Chris chris@mail.com\nSam sam1@mail.com sam2@mail.com sam4@mail.com\nSam sam3@mail.com\nSam sam5@mail.com",
        "explanation": "Explanation:\nThe first, third accounts share sam2@mail.com, so they merge into one with emails sam1@mail.com, sam2@mail.com, sam4@mail.com.\nThe second and fifth accounts have unique emails, so they remain separate.\nThe fourth account belongs to Chris and has a unique email, so it remains unchanged."
      },
      {
        "id": "sample_3",
        "input": "1\n2\nJohn john.doe@mail.com\nJohn john.doe123@mail.com",
        "output": "John john.doe@mail.com john.doe123@mail.com",
        "explanation": "Explanation:\nThe two John accounts have distinct emails, but since they have the same name, they merge into one account with both emails."
      }
    ]
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "label": "minimum boundary",
        "input": "1\n1\nJohn john.doe@mail.com",
        "expectedOutput": "John john.doe@mail.com",
        "weight": 2
      },
      {
        "id": "tc_2",
        "label": "Case 2",
        "input": "1\n1\nJohn john.doe@mail.com",
        "expectedOutput": "John john.doe@mail.com",
        "weight": 2
      },
      {
        "id": "tc_3",
        "label": "Case 3",
        "input": "1\n1\nJohn john.doe@mail.com",
        "expectedOutput": "John john.doe@mail.com",
        "weight": 2
      },
      {
        "id": "tc_4",
        "label": "Case 4",
        "input": "1\n1\nJohn john.doe@mail.com",
        "expectedOutput": "John john.doe@mail.com",
        "weight": 2
      },
      {
        "id": "tc_5",
        "label": "Case 5",
        "input": "1\n1\nJohn john.doe@mail.com",
        "expectedOutput": "John john.doe@mail.com",
        "weight": 2
      }
    ],
    "totalTestCases": 5,
    "totalWeight": 10
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      2,
      2,
      2,
      2,
      2
    ],
    "maxScore": 10
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "We use a Disjoint Set Union (DSU) data structure to group accounts that share at least one common email address. We iterate through each account and its associated emails. If an email is found to be associated with another account, we union the two accounts in the DSU. Finally, we group the emails by their root parent in the DSU and print the merged accounts.",
    "timeComplexity": "O(T * N * M * alpha(N)) per test case, where N is the number of accounts, M is the maximum number of emails per account, and alpha(N) is the inverse Ackermann function. The total time complexity is O(T * N * M * alpha(N)).",
    "spaceComplexity": "O(N + M) \u2014 we use a DSU of size N and store M emails.",
    "code": {
      "C": "#include <stdio.h>\n#include <stdlib.h>\n#include <string.h>\n#define MAX_ACCOUNTS 1000\n#define MAX_EMAILS 10\n#define MAX_NAME_LENGTH 30\n#define MAX_EMAIL_LENGTH 30\n\n// Disjoint Set Union data structure\ntypedef struct {\n    int* parent;\n    int* rank;\n} DisjointSet;\n\nDisjointSet* createDS(int n) {\n    DisjointSet* ds = (DisjointSet*)malloc(sizeof(DisjointSet));\n    ds->parent = (int*)malloc(n * sizeof(int));\n    ds->rank = (int*)malloc(n * sizeof(int));\n    for (int i = 0; i < n; i++) {\n        ds->parent[i] = i;\n        ds->rank[i] = 0;\n    }\n    return ds;\n}\n\nint findSet(DisjointSet* ds, int x) {\n    if (ds->parent[x] != x)\n        ds->parent[x] = findSet(ds, ds->parent[x]);\n    return ds->parent[x];\n}\n\nvoid unionSet(DisjointSet* ds, int a, int b) {\n    int pa = findSet(ds, a);\n    int pb = findSet(ds, b);\n    if (pa == pb) return;\n\n    if (ds->rank[pa] < ds->rank[pb])\n        ds->parent[pa] = pb;\n    else if (ds->rank[pa] > ds->rank[pb])\n        ds->parent[pb] = pa;\n    else {\n        ds->parent[pb] = pa;\n        ds->rank[pa]++;\n    }\n}\n\nint main() {\n    int T;\n    scanf(\"%d\", &T);\n    while (T--) {\n        int N;\n        scanf(\"%d\", &N);\n        DisjointSet* ds = createDS(N);\n        char** accounts = (char**)malloc(N * sizeof(char*));\n        for (int i = 0; i < N; i++) {\n            accounts[i] = (char*)malloc(MAX_ACCOUNTS * sizeof(char));\n            scanf(\"%s\", accounts[i]);\n        }\n        // Process accounts and emails\n        for (int i = 0; i < N; i++) {\n            int numEmails;\n            scanf(\"%d\", &numEmails);\n            for (int j = 0; j < numEmails; j++) {\n                char email[MAX_EMAIL_LENGTH];\n                scanf(\"%s\", email);\n                // Check if email already exists in DSU\n                for (int k = 0; k < i; k++) {\n                    // If email exists, union current account with existing account\n                    if (strcmp(email, accounts[k]) == 0) {\n                        unionSet(ds, i, k);\n                        break;\n                    }\n                }\n            }\n        }\n        // Group emails by root parent and print merged accounts\n        for (int i = 0; i < N; i++) {\n            int root = findSet(ds, i);\n            printf(\"%s \", accounts[root]);\n            // Print sorted unique emails\n            for (int j = 0; j < N; j++) {\n                if (findSet(ds, j) == root) {\n                    // Print emails associated with current account\n                    int numEmails;\n                    scanf(\"%d\", &numEmails);\n                    for (int k = 0; k < numEmails; k++) {\n                        char email[MAX_EMAIL_LENGTH];\n                        scanf(\"%s\", email);\n                        printf(\"%s \", email);\n                    }\n                }\n            }\n            printf(\"\\n\");\n        }\n        free(ds->parent);\n        free(ds->rank);\n        free(ds);\n        for (int i = 0; i < N; i++) {\n            free(accounts[i]);\n        }\n        free(accounts);\n    }\n    return 0;\n}",
      "C++": "#include <iostream>\n#include <vector>\n#include <string>\n#include <unordered_map>\n#include <set>\nusing namespace std;\n\n// Disjoint Set Union data structure\nclass DisjointSet {\npublic:\n    vector<int> parent;\n    vector<int> rank;\n    DisjointSet(int n) {\n        parent.resize(n);\n        rank.resize(n, 0);\n        for (int i = 0; i < n; i++)\n            parent[i] = i;\n    }\n    int find(int x) {\n        if (parent[x] != x)\n            parent[x] = find(parent[x]);\n        return parent[x];\n    }\n    void unionSet(int a, int b) {\n        int pa = find(a);\n        int pb = find(b);\n        if (pa == pb) return;\n        if (rank[pa] < rank[pb])\n            parent[pa] = pb;\n        else if (rank[pa] > rank[pb])\n            parent[pb] = pa;\n        else {\n            parent[pb] = pa;\n            rank[pa]++;\n        }\n    }\n};\n\nint main() {\n    int T;\n    cin >> T;\n    while (T--) {\n        int N;\n        cin >> N;\n        DisjointSet ds(N);\n        vector<string> accounts(N);\n        for (int i = 0; i < N; i++)\n            cin >> accounts[i];\n        // Process accounts and emails\n        for (int i = 0; i < N; i++) {\n            int numEmails;\n            cin >> numEmails;\n            for (int j = 0; j < numEmails; j++) {\n                string email;\n                cin >> email;\n                // Check if email already exists in DSU\n                for (int k = 0; k < i; k++) {\n                    // If email exists, union current account with existing account\n                    if (email == accounts[k]) {\n                        ds.unionSet(i, k);\n                        break;\n                    }\n                }\n            }\n        }\n        // Group emails by root parent and print merged accounts\n        for (int i = 0; i < N; i++) {\n            int root = ds.find(i);\n            cout << accounts[root] << \" \";\n            // Print sorted unique emails\n            set<string> emails;\n            for (int j = 0; j < N; j++) {\n                if (ds.find(j) == root) {\n                    // Print emails associated with current account\n                    int numEmails;\n                    cin >> numEmails;\n                    for (int k = 0; k < numEmails; k++) {\n                        string email;\n                        cin >> email;\n                        emails.insert(email);\n                    }\n                }\n            }\n            for (auto email : emails)\n                cout << email << \" \";\n            cout << endl;\n        }\n    }\n    return 0;\n}",
      "Java": "import java.util.Scanner;\nimport java.util.HashMap;\nimport java.util.HashSet;\nimport java.util.Set;\nimport java.util.Map;\npublic class Main {\n    public static void main(String[] args) {\n        Scanner scanner = new Scanner(System.in);\n        int T = scanner.nextInt();\n        while (T-- > 0) {\n            int N = scanner.nextInt();\n            String[] accounts = new String[N];\n            for (int i = 0; i < N; i++)\n                accounts[i] = scanner.next();\n            // Process accounts and emails\n            Map<String, Integer> emailToIndex = new HashMap<>();\n            for (int i = 0; i < N; i++) {\n                int numEmails = scanner.nextInt();\n                for (int j = 0; j < numEmails; j++) {\n                    String email = scanner.next();\n                    // Check if email already exists in DSU\n                    if (emailToIndex.containsKey(email)) {\n                        // If email exists, union current account with existing account\n                        int existingIndex = emailToIndex.get(email);\n                        // Union\n                        accounts[existingIndex] = mergeAccounts(accounts[existingIndex], accounts[i]);\n                    } else {\n                        emailToIndex.put(email, i);\n                    }\n                }\n            }\n            // Group emails by root parent and print merged accounts\n            Set<String> uniqueAccounts = new HashSet<>();\n            for (int i = 0; i < N; i++)\n                uniqueAccounts.add(accounts[i]);\n            for (String account : uniqueAccounts)\n                System.out.println(account);\n        }\n    }\n    public static String mergeAccounts(String account1, String account2) {\n        // Merge two accounts into one\n        return account1 + \" \" + account2;\n    }\n}",
      "Python3": "# COMPLETE, RUNNABLE Python3 code\nimport sys\ninput = sys.stdin.readline\nfrom collections import defaultdict\n\n# Disjoint Set Union data structure\nclass DisjointSet:\n    def __init__(self, n):\n        self.parent = list(range(n))\n        self.rank = [0] * n\n    def find(self, x):\n        if self.parent[x] != x:\n            self.parent[x] = self.find(self.parent[x])\n        return self.parent[x]\n    def union(self, a, b):\n        pa = self.find(a)\n        pb = self.find(b)\n        if pa == pb: return\n        if self.rank[pa] < self.rank[pb]:\n            self.parent[pa] = pb\n        elif self.rank[pa] > self.rank[pb]:\n            self.parent[pb] = pa\n        else:\n            self.parent[pb] = pa\n            self.rank[pa] += 1\n\ndef main():\n    T = int(input())\n    for _ in range(T):\n        N = int(input())\n        ds = DisjointSet(N)\n        accounts = [input().strip() for _ in range(N)]\n        # Process accounts and emails\n        email_to_index = {}\n        for i in range(N):\n            num_emails = int(input())\n            for _ in range(num_emails):\n                email = input().strip()\n                # Check if email already exists in DSU\n                if email in email_to_index:\n                    # If email exists, union current account with existing account\n                    ds.union(i, email_to_index[email])\n                else:\n                    email_to_index[email] = i\n        # Group emails by root parent and print merged accounts\n        merged_accounts = defaultdict(list)\n        for email, index in email_to_index.items():\n            root = ds.find(index)\n            merged_accounts[root].append(email)\n        for root, emails in merged_accounts.items():\n            print(accounts[root], end=\" \")\n            for email in sorted(emails):\n                print(email, end=\" \")\n            print()\nif __name__ == '__main__':\n    main()",
      "JavaScript": "// COMPLETE, RUNNABLE JavaScript code\nconst fs = require('fs');\nconst input = fs.readFileSync(0,'utf-8').trim().split('\\n');\nlet idx = 0;\nfunction readline() {\n    return input[idx++];\n}\n\n// Disjoint Set Union data structure\nclass DisjointSet {\n    constructor(n) {\n        this.parent = Array.from({ length: n }, (_, i) => i);\n        this.rank = new Array(n).fill(0);\n    }\n    find(x) {\n        if (this.parent[x] !== x)\n            this.parent[x] = this.find(this.parent[x]);\n        return this.parent[x];\n    }\n    union(a, b) {\n        let pa = this.find(a);\n        let pb = this.find(b);\n        if (pa === pb) return;\n        if (this.rank[pa] < this.rank[pb])\n            this.parent[pa] = pb;\n        else if (this.rank[pa] > this.rank[pb])\n            this.parent[pb] = pa;\n        else {\n            this.parent[pb] = pa;\n            this.rank[pa]++;\n        }\n    }\n}\n\nfunction main() {\n    let T = parseInt(readline());\n    while (T-- > 0) {\n        let N = parseInt(readline());\n        let ds = new DisjointSet(N);\n        let accounts = [];\n        for (let i = 0; i < N; i++)\n            accounts.push(readline().trim());\n        // Process accounts and emails\n        let emailToIndex = {};\n        for (let i = 0; i < N; i++) {\n            let numEmails = parseInt(readline());\n            for (let j = 0; j < numEmails; j++) {\n                let email = readline().trim();\n                // Check if email already exists in DSU\n                if (email in emailToIndex) {\n                    // If email exists, union current account with existing account\n                    ds.union(i, emailToIndex[email]);\n                } else {\n                    emailToIndex[email] = i;\n                }\n            }\n        }\n        // Group emails by root parent and print merged accounts\n        let mergedAccounts = {};\n        for (let email in emailToIndex) {\n            let root = ds.find(emailToIndex[email]);\n            if (!(root in mergedAccounts))\n                mergedAccounts[root] = [];\n            mergedAccounts[root].push(email);\n        }\n        for (let root in mergedAccounts) {\n            process.stdout.write(accounts[root] + \" \");\n            for (let email of mergedAccounts[root].sort())\n                process.stdout.write(email + \" \");\n            process.stdout.write(\"\\n\");\n        }\n    }\n}\nmain();"
    }
  }
}