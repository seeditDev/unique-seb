{
  "questionId": "Q5578",
  "title": "Triangular Queries",
  "slug": "triangular-queries",
  "version": 1,
  "createdAt": "2026-07-01",
  "updatedAt": "2026-07-04",
  "metadata": {
    "category": "Geometry",
    "difficulty": "Hard",
    "isPremium": true,
    "tags": [
      "2D BIT",
      "Offline queries",
      "Coordinate compression",
      "Fenwick Tree",
      "Sorting"
    ],
    "companyTags": [],
    "topicTags": [
      "Geometry"
    ],
    "QBCategory": "CCD"
  },
  "content": {
    "problemStatement": "You are given **N** lattice points **(Xi, Yi), i = 1, 2, ..., N** on a 2D Coordinate System.\nYou have to process **Q** queries, each query will be given as the form **\"x y d\"**.\nLet **ABC** be the triangle having vertices at **A(x+d, y)**, **B(x, y)** and **C(x, y+d)**.\nFor each query, you have to find how many of the given lattice points lie inside or on the boundary of the triangle **ABC**.",
    "constraints": [
      "1 ≤ T ≤ 5",
      "1 ≤ N ≤ 300000",
      "1 ≤ Q ≤ 200000",
      "1 ≤ Xi, Yi, x, y, d ≤ 300000",
      "Sum of N over all test cases ≤ 300000",
      "Sum of Q over all test cases ≤ 200000"
    ],
    "inputFormat": "The first line contains an integer T, the number of test cases. For each test case:\n- The first line contains two integers N and Q.\n- The next N lines each contain two integers Xi Yi, the coordinates of the i‑th lattice point.\n- The next Q lines each contain three integers x y d describing a query.\nAll coordinates satisfy 1 ≤ Xi, Yi, x, y, d ≤ 300000.",
    "outputFormat": "For each query, output a single line containing the number of given lattice points that lie inside or on the boundary of the triangle ABC defined by A(x+d, y), B(x, y) and C(x, y+d). The answers for the queries of all test cases are printed in the order they appear.",
    "sampleTestCases": [
      {
        "id": "sample_1",
        "input": "5 3\n1 3\n1 5\n3 6\n4 4\n2 6\n1 5 3\n1 5 4\n1 1 1",
        "output": "3\n3\n0",
        "explanation": "For the first query, the points (1, 3), (1, 5), and (2, 6) lie inside or on the boundary of the triangle. For the second query, the points (1, 3), (1, 5), and (2, 6) lie inside or on the boundary of the triangle. For the third query, no points lie inside or on the boundary of the triangle."
      },
      {
        "id": "sample_2",
        "input": "2 2\n1 5\n3 7\n2 5 6\n2 3 4",
        "output": "1\n0",
        "explanation": "For the first query, the point (1, 5) lies inside or on the boundary of the triangle. For the second query, no points lie inside or on the boundary of the triangle."
      }
    ]
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "label": "Case 1",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_2",
        "label": "Case 2",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_3",
        "label": "Case 3",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_4",
        "label": "Case 4",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_5",
        "label": "Case 5",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      }
    ],
    "totalTestCases": 5,
    "totalWeight": 10
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      2,
      2,
      2,
      2,
      2
    ],
    "maxScore": 10
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "For a point P(px,py) to be inside the right‑isosceles triangle with right angle at B(x,y) we need:\n    x ≤ px ≤ x+d,\n    y ≤ py ≤ y+d,\n    (px‑x) + (py‑y) ≤ d   (i.e. px+py ≤ x+y+d).\nDefine s = px+py. The three conditions become a rectangle query on (px,py) together with a bound on s.\nWe process all points sorted by s and all queries sorted by limit L = x+y+d.\nWhile scanning increasing s we insert points whose s ≤ current L into a 2‑dimensional Fenwick tree that supports \"add a point\" and \"count points inside a rectangle\". When a query is reached, the tree already contains exactly the points satisfying the sum condition, so a rectangle query on [x, x+d] × [y, y+d] yields the answer.\nThe 2‑D Fenwick tree is built offline: for each distinct x‑coordinate we store a sorted list of y‑coordinates that ever appear in that column (both from points and from query rectangle borders). After compression we have a BIT of size ≤ N+Q for each x‑node. Both insertion and rectangle query run in O(log² MAX) time.\nOverall complexity: O((N+Q) log² MAX) time and O((N+Q) log MAX) memory, which easily fits the limits.\nEdge cases such as d = 0 (degenerate triangle) are naturally handled because the rectangle becomes a single point and the sum condition forces px+py = x+y.",
    "timeComplexity": "O((N + Q) · log² 300000)",
    "spaceComplexity": "O((N + Q) · log 300000)",
    "code": {
      "C": "// --------------- C solution (offline 2‑D Fenwick) ---------------\n#include <stdio.h>\n#include <stdlib.h>\n\ntypedef struct {int x, y, s;} Point;\ntypedef struct {int x, y, d, id; long long limit; int ans;} Query;\n\n// ---------- simple dynamic array ----------\ntypedef struct {int *a; int sz, cap;} IntVec;\nvoid iv_init(IntVec *v){v->a=NULL;v->sz=0;v->cap=0;}\nvoid iv_push(IntVec *v,int val){if(v->sz==v->cap){v->cap=v->cap?v->cap*2:4;v->a=realloc(v->a,v->cap*sizeof(int));}v->a[v->sz++]=val;}\nint cmp_int(const void *a,const void *b){return *(int*)a-*(int*)b;}\n\n// ---------- Fenwick tree of vectors ----------\nint MAXX = 300000 + 5;\nIntVec ft[MAXX];\nint Nglobal;\n\nvoid ft_add_point(int x,int y){for(int i=x;i<=Nglobal;i+=i&-i){iv_push(&ft[i], y);} }\n\nint ft_query(int x,int y){int res=0;for(int i=x;i>0;i-=i&-i){int *arr=ft[i].a;int lo=0,hi=ft[i].sz;while(lo<hi){int mid=(lo+hi)/2; if(arr[mid]<=y) lo=mid+1; else hi=mid;}res+=lo;}return res;}\n\nint rect_query(int x1,int y1,int x2,int y2){ // inclusive\n    if(x1> x2 || y1> y2) return 0;\n    return ft_query(x2,y2)-ft_query(x1-1,y2)-ft_query(x2,y1-1)+ft_query(x1-1,y1-1);\n}\n\nint cmp_point(const void *a,const void *b){return ((Point*)a)->s-((Point*)b)->s;}\nint cmp_query(const void *a,const void *b){long long da=((Query*)a)->limit, db=((Query*)b)->limit; return da<db?-1:da>db?1:0;}\n\nint main(){\n    int T; if(scanf(\"%d\",&T)!=1) return 0;\n    while(T--){\n        int N,Q; scanf(\"%d %d\",&N,&Q);\n        Point *pts = (Point*)malloc(N*sizeof(Point));\n        for(int i=0;i<N;i++){\n            int xi,yi; scanf(\"%d %d\",&xi,&yi);\n            pts[i].x=xi; pts[i].y=yi; pts[i].s=xi+yi;\n        }\n        Query *qs = (Query*)malloc(Q*sizeof(Query));\n        for(int i=0;i<Q;i++){\n            int x,y,d; scanf(\"%d %d %d\",&x,&y,&d);\n            qs[i].x=x; qs[i].y=y; qs[i].d=d; qs[i].id=i; qs[i].limit = (long long)x + y + d;\n        }\n        // coordinate compression for x (Fenwick size)\n        int maxX=0; for(int i=0;i<N;i++) if(pts[i].x>maxX) maxX=pts[i].x; for(int i=0;i<Q;i++) if(qs[i].x+qs[i].d>maxX) maxX=qs[i].x+qs[i].d; Nglobal = maxX+2;\n        // clear ft vectors\n        for(int i=0;i<=Nglobal;i++){free(ft[i].a); ft[i].a=NULL; ft[i].sz=ft[i].cap=0;}\n        // collect y values for each ft node (offline building)\n        // first pass: add all points that will ever be inserted (all points)\n        for(int i=0;i<N;i++) ft_add_point(pts[i].x, pts[i].y);\n        // second pass: also add rectangle borders from queries (x2 and y2) to guarantee they exist in vectors\n        for(int i=0;i<Q;i++){\n            int x2 = qs[i].x + qs[i].d;\n            int y2 = qs[i].y + qs[i].d;\n            ft_add_point(x2, y2);\n        }\n        // sort and deduplicate each vector, then replace with BIT array (initially zero)\n        for(int i=1;i<=Nglobal;i++) if(ft[i].sz){ qsort(ft[i].a, ft[i].sz, sizeof(int), cmp_int); int w=0; for(int j=0;j<ft[i].sz;j++) if(j==0||ft[i].a[j]!=ft[i].a[j-1]) ft[i].a[w++]=ft[i].a[j]; ft[i].sz=w; ft[i].a = realloc(ft[i].a, (w+2)*sizeof(int)); }\n        // sort points and queries by sum limit\n        qsort(pts,N,sizeof(Point),cmp_point);\n        qsort(qs,Q,sizeof(Query),cmp_query);\n        // BIT for counts (same structure as ft but stores int counts)\n        // we reuse ft vectors as index maps; we need a parallel array of counts per node\n        int **bit = (int**)malloc((Nglobal+1)*sizeof(int*));\n        for(int i=1;i<=Nglobal;i++) if(ft[i].sz) bit[i]=(int*)calloc(ft[i].sz+1,sizeof(int)); else bit[i]=NULL;\n        // helper to update a point in 2‑D BIT\n        void update2D(int x,int y,int delta){ for(int i=x;i<=Nglobal;i+=i&-i){ int *arr=ft[i].a; int lo=0,hi=ft[i].sz; while(lo<hi){int mid=(lo+hi)/2; if(arr[mid]<=y) lo=mid+1; else hi=mid;} int pos=lo; for(int j=pos;j<=ft[i].sz;j+=j&-j) bit[i][j]+=delta; } };\n        // helper to query prefix\n        int query2D(int x,int y){ int res=0; for(int i=x;i>0;i-=i&-i){ int *arr=ft[i].a; int lo=0,hi=ft[i].sz; while(lo<hi){int mid=(lo+hi)/2; if(arr[mid]<=y) lo=mid+1; else hi=mid;} int pos=lo; for(int j=pos;j>0;j-=j&-j) res+=bit[i][j]; } return res; };\n        // process\n        int pIdx=0;\n        for(int qi=0; qi<Q; ++qi){\n            while(pIdx<N && pts[pIdx].s <= qs[qi].limit){\n                update2D(pts[pIdx].x, pts[pIdx].y, 1);\n                ++pIdx;\n            }\n            int x1 = qs[qi].x;\n            int y1 = qs[qi].y;\n            int x2 = qs[qi].x + qs[qi].d;\n            int y2 = qs[qi].y + qs[qi].d;\n            int ans = query2D(x2, y2) - query2D(x1-1, y2) - query2D(x2, y1-1) + query2D(x1-1, y1-1);\n            qs[qi].ans = ans;\n        }\n        // restore original order and output\n        int *out = (int*)malloc(Q*sizeof(int));\n        for(int i=0;i<Q;i++) out[qs[i].id]=qs[i].ans;\n        for(int i=0;i<Q;i++) printf(\"%d\\n\", out[i]);\n        // free\n        free(pts); free(qs); free(out);\n        for(int i=1;i<=Nglobal;i++) free(bit[i]); free(bit);\n    }\n    return 0;\n}\n",
      "C++": ""
    }
  },
  "QBCategory": "CCD",
  "boilerPlates": {
    "C": "#include <stdio.h>\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "C++": "#include <iostream>\nusing namespace std;\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "Java": "import java.util.*;\nimport java.io.*;\n\npublic class Main {\n    public static void main(String[] args) throws IOException {\n        // your code goes here\n    }\n}",
    "Python3": "# your code goes here\n",
    "JavaScript": "// your code goes here\n"
  },
  "_internal": {
    "methodName": "",
    "params": []
  }
}