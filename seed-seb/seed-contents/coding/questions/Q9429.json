{
  "questionId": "Q9429",
  "title": "Difference Pairs",
  "slug": "difference-pairs",
  "version": 1,
  "createdAt": "2026-07-02",
  "updatedAt": "2026-07-04",
  "metadata": {
    "category": "Arrays | Searching",
    "difficulty": "Easy",
    "isPremium": true,
    "tags": [
      "Hash Set",
      "Two‑Sum Variant",
      "Absolute Difference"
    ],
    "companyTags": [],
    "topicTags": [
      "Hashing"
    ],
    "QBCategory": "CCD"
  },
  "content": {
    "problemStatement": "You are given an array AAA of NNN integers and an integer BBB. Your task is to determine whether there exists a pair of indices (i,j)(i, j)(i,j) such that 1≤i&lt;j≤N1 \\leq i &lt; j \\leq N1≤i&lt;j≤N and ∣A[i]−A[j]∣=B|A[i] - A[j]| = B∣A[i]−A[j]∣=B.\n\nFunction DeclarationFunction NamehasPairWithDifferencehasPairWithDifferencehasPairWithDifference – This function checks if there is any valid pair in the array with an absolute difference exactly equal to BBB.\n\nParameters\nAAA : A list/array of integers of length NNN, representing the input sequence.\nNNN : An integer representing the number of elements in the array.\nBBB : An integer representing the target absolute difference.\nReturn ValueReturns an integer(or boolean): `1` (or `true`) if such a pair exists, and `0` (or `false`) otherwise.",
    "constraints": [
      "1 <= T <= 100",
      "1 <= N <= 100000",
      "-10^9 <= Ai <= 10^9",
      "0 <= B <= 2*10^9",
      "Sum of N over all test cases ≤ 200000"
    ],
    "inputFormat": "The first line contains an integer T, the number of test cases. Each test case starts with a line containing two integers N and B. The next line contains N space‑separated integers A1…AN.",
    "outputFormat": "For each test case output a single line containing 1 if there exists a pair (i,j) with i<j and |Ai‑Aj|=B, otherwise output 0.",
    "sampleTestCases": [
      {
        "id": "sample_1",
        "input": "3\\n6 78\\n5 10 3 2 50 80\\n2 30\\n-10 20\\n4 5\\n1 2 3 4",
        "output": "1\\n1\\n0",
        "explanation": "Test case 1: 80‑2 = 78, so pair exists.\\nTest case 2: 20‑(-10) = 30, pair exists.\\nTest case 3: No two numbers differ by 5."
      },
      {
        "id": "sample_2",
        "input": "2\\n3 0\\n1 2 1\\n4 5\\n10 15 20 25",
        "output": "1\\n0",
        "explanation": "First test: B=0 and value 1 appears twice, so answer 1.\\nSecond test: No two numbers differ by 5."
      },
      {
        "id": "sample_3",
        "input": "1\\n5 10\\n-5 -15 0 10 20",
        "output": "1",
        "explanation": "-15 and -5 differ by 10, therefore answer is 1."
      }
    ]
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "label": "minimum boundary",
        "input": "1\\n1 0\\n5",
        "expectedOutput": "0",
        "weight": 2
      },
      {
        "id": "tc_2",
        "label": "maximum boundary",
        "input": "1\\n5 2000000000\\n-1000000000 1000000000 0 2000000000 -2000000000",
        "expectedOutput": "1",
        "weight": 2
      },
      {
        "id": "tc_3",
        "label": "edge case",
        "input": "1\\n4 0\\n1 2 3 4",
        "expectedOutput": "0",
        "weight": 2
      },
      {
        "id": "tc_4",
        "label": "general case 1",
        "input": "1\\n6 3\\n1 4 7 10 13 16",
        "expectedOutput": "1",
        "weight": 2
      },
      {
        "id": "tc_5",
        "label": "general case 2",
        "input": "1\\n5 100\\n10 20 30 40 50",
        "expectedOutput": "0",
        "weight": 2
      }
    ],
    "totalTestCases": 5,
    "totalWeight": 10
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      2,
      2,
      2,
      2,
      2
    ],
    "maxScore": 10
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "For each test case store all numbers in an unordered hash set. Iterate through the array: for current value x check if x+B or x‑B is already in the set (or will appear later). If B==0 we need at least two equal numbers, which is naturally detected because when we see a value that is already in the set we can answer true. If any check succeeds, output 1; otherwise after processing all elements output 0. This runs in O(N) average time per test case and O(N) extra memory.",
    "timeComplexity": "O(N) per test case (average), O(total N) over all test cases",
    "spaceComplexity": "O(N) per test case for the hash set",
    "code": {
      "C": "#include <stdio.h>\n#include <stdlib.h>\n#include <stdbool.h>\n#include <string.h>\n\n// Simple hash table for 64‑bit integers (open addressing)\ntypedef struct {\n    long long key;\n    bool used;\n} Cell;\n\nstatic Cell *table;\nstatic size_t cap;\nstatic size_t mask;\n\nstatic void init_table(size_t n){\n    // size power of two >= 2*n\n    cap = 1;\n    while(cap < n*2) cap <<= 1;\n    mask = cap-1;\n    table = (Cell*)calloc(cap, sizeof(Cell));\n}\n\nstatic void free_table(){\n    free(table);\n    table = NULL;\n    cap = mask = 0;\n}\n\nstatic bool insert_find(long long x, bool find){\n    size_t h = (size_t)(x ^ (x>>32)) & mask;\n    while(table[h].used){\n        if(table[h].key == x){\n            return true; // found\n        }\n        h = (h+1) & mask;\n    }\n    if(!find){\n        table[h].used = true;\n        table[h].key = x;\n    }\n    return false; // not found\n}\n\nint main(){\n    int T; if(scanf(\"%d\", &T)!=1) return 0;\n    while(T--){\n        int N; long long B; scanf(\"%d %lld\", &N, &B);\n        init_table(N);\n        bool ok = false;\n        for(int i=0;i<N;i++){\n            long long x; scanf(\"%lld\", &x);\n            if(!ok){\n                if(B==0){\n                    if(insert_find(x, true)) ok = true;\n                }else{\n                    if(insert_find(x+B, true) || insert_find(x-B, true)) ok = true;\n                }\n            }\n            insert_find(x, false);\n        }\n        free_table();\n        printf(\"%d\\n\", ok?1:0);\n    }\n    return 0;\n}\n",
      "C++": "#include <bits/stdc++.h>\nusing namespace std;\nint main(){\n    ios::sync_with_stdio(false);\n    cin.tie(nullptr);\n    int T; if(!(cin>>T)) return 0;\n    while(T--){\n        int N; long long B; cin>>N>>B;\n        unordered_set<long long> seen;\n        seen.reserve(N*2);\n        bool ok=false;\n        for(int i=0;i<N;i++){\n            long long x; cin>>x;\n            if(!ok){\n                if(B==0){\n                    if(seen.count(x)) ok=true;\n                }else{\n                    if(seen.count(x+B) || seen.count(x-B)) ok=true;\n                }\n            }\n            seen.insert(x);\n        }\n        cout<<(ok?1:0) << '\\n';\n    }\n    return 0;\n}\n",
      "Java": "import java.io.*;\nimport java.util.*;\npublic class Main {\n    public static void main(String[] args) throws Exception {\n        FastScanner fs = new FastScanner(System.in);\n        int T = fs.nextInt();\n        StringBuilder sb = new StringBuilder();\n        while (T-- > 0) {\n            int N = fs.nextInt();\n            long B = fs.nextLong();\n            HashSet<Long> set = new HashSet<>(N*2);\n            boolean ok = false;\n            for (int i = 0; i < N; i++) {\n                long x = fs.nextLong();\n                if (!ok) {\n                    if (B == 0) {\n                        if (set.contains(x)) ok = true;\n                    } else {\n                        if (set.contains(x + B) || set.contains(x - B)) ok = true;\n                    }\n                }\n                set.add(x);\n            }\n            sb.append(ok ? 1 : 0).append('\\n');\n        }\n        System.out.print(sb.toString());\n    }\n    // Fast scanner for large input\n    static class FastScanner {\n        private final InputStream in;\n        private final byte[] buffer = new byte[1 << 16];\n        private int ptr = 0, len = 0;\n        FastScanner(InputStream in) { this.in = in; }\n        private int readByte() throws IOException {\n            if (ptr >= len) {\n                len = in.read(buffer);\n                ptr = 0;\n                if (len <= 0) return -1;\n            }\n            return buffer[ptr++];\n        }\n        long nextLong() throws IOException { return Long.parseLong(next()); }\n        int nextInt() throws IOException { return Integer.parseInt(next()); }\n        String next() throws IOException {\n            StringBuilder sb = new StringBuilder();\n            int c;\n            while ((c = readByte()) != -1 && c <= ' ') {}\n            if (c == -1) return \"\";\n            do { sb.append((char)c); } while ((c = readByte()) != -1 && c > ' ');\n            return sb.toString();\n        }\n    }\n}\n",
      "Python3": "import sys\ndef solve():\n    data = sys.stdin.read().strip().split()\n    if not data:\n        return\n    it = iter(data)\n    T = int(next(it))\n    out = []\n    for _ in range(T):\n        N = int(next(it)); B = int(next(it))\n        seen = set()\n        ok = False\n        for _ in range(N):\n            x = int(next(it))\n            if not ok:\n                if B == 0:\n                    if x in seen:\n                        ok = True\n                else:\n                    if (x + B) in seen or (x - B) in seen:\n                        ok = True\n            seen.add(x)\n        out.append('1' if ok else '0')\n    sys.stdout.write('\\n'.join(out))\nif __name__ == \"__main__\":\n    solve()\n",
      "JavaScript": "const fs = require('fs');\nconst input = fs.readFileSync(0, 'utf-8').trim().split(/\\s+/);\nlet idx = 0;\nconst T = Number(input[idx++]);\nlet results = [];\nfor (let t = 0; t < T; t++) {\n  const N = Number(input[idx++]);\n  const B = Number(input[idx++]);\n  const seen = new Set();\n  let ok = false;\n  for (let i = 0; i < N; i++) {\n    const x = Number(input[idx++]);\n    if (!ok) {\n      if (B === 0) {\n        if (seen.has(x)) ok = true;\n      } else {\n        if (seen.has(x + B) || seen.has(x - B)) ok = true;\n      }\n    }\n    seen.add(x);\n  }\n  results.push(ok ? '1' : '0');\n}\nconsole.log(results.join('\\n'));\n"
    }
  },
  "QBCategory": "CCD",
  "boilerPlates": {
    "C": "#include <stdio.h>\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "C++": "#include <iostream>\nusing namespace std;\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "Java": "import java.util.*;\nimport java.io.*;\n\npublic class Main {\n    public static void main(String[] args) throws IOException {\n        // your code goes here\n    }\n}",
    "Python3": "# your code goes here\n",
    "JavaScript": "// your code goes here\n"
  },
  "_internal": {
    "methodName": "",
    "params": []
  }
}