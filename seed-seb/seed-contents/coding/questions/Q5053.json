{
  "questionId": "Q5053",
  "title": "Vowel Counter",
  "slug": "vowel-counter",
  "version": 1,
  "createdAt": "2026-07-01",
  "updatedAt": "2026-07-04",
  "metadata": {
    "category": "Strings",
    "difficulty": "Easy",
    "isPremium": true,
    "tags": [
      "String Parsing",
      "Hash Map",
      "Ordered Dictionary"
    ],
    "companyTags": [],
    "topicTags": [
      "Hashing & Frequency Counting"
    ],
    "QBCategory": "CCD"
  },
  "content": {
    "problemStatement": "**Q2. B):** A linguistics student, Alex, is analyzing text and needs a tool to quickly count the vowels in each word of a sentence. Your task is to Write a Python Code that takes a sentence as input and returns a dictionary where each word is a key and its value is the number of vowels in that word.\n\n**Rules:**\n\n\nWords are sequences of characters separated by spaces.\nVowels are 'a', 'e', 'i', 'o', 'u'. The count must be case-insensitive.\nPunctuation attached to a word is considered part of the word (e.g., in \"Hello!\", the word is \"Hello!\").\nIf a word appears more than once, its entry in the dictionary will be based on its last occurrence, effectively overwriting any previous entry for that same word.\n\nInput Format\n\nA single line of text representing the sentence.\n\n\nOutput Format\n\nPrint the resulting dictionary.",
    "constraints": [
      "1 <= T <= 10",
      "Each sentence length <= 1000 characters",
      "Words are separated by a single space"
    ],
    "inputFormat": "The first line contains an integer T (1 ≤ T ≤ 10), the number of test cases. Each of the next T lines contains a sentence (a string of up to 1000 characters). Words are separated by a single space.",
    "outputFormat": "For each test case output a single line containing a Python‑style dictionary. Each distinct word from the sentence is a key (preserving original case and punctuation) and its value is the number of vowels (a, e, i, o, u, case‑insensitive) in that word. If a word appears multiple times, the entry from its last occurrence overwrites earlier ones. Keys are printed in the order of their last appearance.",
    "sampleTestCases": [
      {
        "id": "sample_1",
        "input": "1\\nPython is awesome",
        "output": "{'Python': 1, 'is': 1, 'awesome': 4}",
        "explanation": "The sentence has three words. 'Python' contains 1 vowel (o). 'is' contains 1 vowel (i). 'awesome' contains 4 vowels (a, e, o, e)."
      },
      {
        "id": "sample_2",
        "input": "1\\nhello world hello",
        "output": "{'world': 1, 'hello': 2}",
        "explanation": "Words appear as hello, world, hello. The last occurrence of 'hello' overwrites the first. Order of last appearance is world (position 2) then hello (position 3). Vowel counts: world -> 1 (o), hello -> 2 (e, o)."
      },
      {
        "id": "sample_3",
        "input": "1\\nHello! This is a test.",
        "output": "{'Hello!': 2, 'This': 1, 'is': 1, 'a': 1, 'test.': 1}",
        "explanation": "Punctuation is part of the word. Vowel counts: Hello! (e,o)=2, This (i)=1, is (i)=1, a (a)=1, test. (e)=1."
      }
    ]
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "label": "minimum boundary",
        "input": "1\\n",
        "expectedOutput": "{}",
        "weight": 2
      },
      {
        "id": "tc_2",
        "label": "Case 2",
        "input": "1\\n",
        "expectedOutput": "{}",
        "weight": 2
      },
      {
        "id": "tc_3",
        "label": "Case 3",
        "input": "1\\n",
        "expectedOutput": "{}",
        "weight": 2
      },
      {
        "id": "tc_4",
        "label": "Case 4",
        "input": "1\\n",
        "expectedOutput": "{}",
        "weight": 2
      },
      {
        "id": "tc_5",
        "label": "Case 5",
        "input": "1\\n",
        "expectedOutput": "{}",
        "weight": 2
      }
    ],
    "totalTestCases": 5,
    "totalWeight": 10
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      2,
      2,
      2,
      2,
      2
    ],
    "maxScore": 10
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "For each test case split the line on spaces to obtain the words. For each word count vowels by scanning its characters and checking membership in the set {a,e,i,o,u} (case‑insensitive). To respect the \"last appearance\" order we maintain an ordered container (e.g., a list of keys) and a map from word to count. When a word is seen again we remove its previous position from the list (if present) and append it to the end, then update the count in the map. Finally iterate over the list and print the dictionary in the required format.",
    "timeComplexity": "O(L) per test case, where L is the total number of characters in the sentence (each character is examined once).",
    "spaceComplexity": "O(W) per test case, where W is the number of distinct words (storage for map and order list).",
    "code": {
      "C": "#include <stdio.h>\n#include <string.h>\n#include <ctype.h>\n#include <stdlib.h>\n\nint isVowel(char c){\n    c = tolower((unsigned char)c);\n    return c=='a'||c=='e'||c=='i'||c=='o'||c=='u';\n}\n\ntypedef struct Node{char *word; int cnt; struct Node *next;} Node;\n\nint main(){\n    int T; if(scanf(\"%d\",&T)!=1) return 0; getchar(); // consume newline\n    char line[2005];\n    for(int tc=0; tc<T; ++tc){\n        if(!fgets(line,sizeof(line),stdin)) line[0]='\\0';\n        size_t len=strlen(line);\n        if(len && line[len-1]=='\\n') line[len-1]='\\0';\n        // ordered list of keys\n        Node *head=NULL, *tail=NULL;\n        // map for fast lookup (simple linear search because constraints are tiny)\n        Node *search=NULL;\n        char *token = strtok(line, \" \");\n        while(token){\n            int cnt=0; for(char *p=token; *p; ++p) if(isVowel(*p)) ++cnt;\n            // check if already exists\n            Node *prev=NULL, *cur=head;\n            while(cur && strcmp(cur->word, token)!=0){ prev=cur; cur=cur->next; }\n            if(cur){ // exists: update count and move to tail\n                cur->cnt = cnt;\n                if(cur!=tail){\n                    // unlink cur\n                    if(prev) prev->next = cur->next; else head = cur->next;\n                    // append at tail\n                    tail->next = cur; cur->next = NULL; tail = cur;\n                }\n            } else {\n                Node *node = malloc(sizeof(Node));\n                node->word = strdup(token);\n                node->cnt = cnt;\n                node->next = NULL;\n                if(!head) head = tail = node; else { tail->next = node; tail = node; }\n            }\n            token = strtok(NULL, \" \");\n        }\n        // print dictionary\n        printf(\"{\");\n        for(Node *p=head; p; p=p->next){\n            printf(\"'%s': %d\", p->word, p->cnt);\n            if(p->next) printf(\", \");\n        }\n        printf(\"}\\n\");\n        // free\n        for(Node *p=head; p;){ Node *n=p->next; free(p->word); free(p); p=n; }\n    }\n    return 0;\n}",
      "C++": "#include <bits/stdc++.h>\nusing namespace std;\nbool isVowel(char c){ c = tolower((unsigned char)c); return c=='a'||c=='e'||c=='i'||c=='o'||c=='u'; }\nint main(){ ios::sync_with_stdio(false); cin.tie(nullptr);\n    int T; if(!(cin>>T)) return 0; string dummy; getline(cin,dummy); // consume endline\n    for(int tc=0; tc<T; ++tc){\n        string line; getline(cin,line);\n        unordered_map<string,int> mp; vector<string> order;\n        stringstream ss(line);\n        string word;\n        while(ss>>word){\n            int cnt=0; for(char ch:word) if(isVowel(ch)) ++cnt;\n            // if word already seen, erase its previous position\n            auto it = mp.find(word);\n            if(it!=mp.end()){\n                // remove old occurrence from order vector\n                for(auto oit = order.begin(); oit!=order.end(); ++oit){ if(*oit==word){ order.erase(oit); break; } }\n            }\n            order.push_back(word);\n            mp[word]=cnt;\n        }\n        cout<<\"{\";\n        for(size_t i=0;i<order.size();++i){\n            const string &w=order[i];\n            cout<<\"'\"<<w<<\"': \"<<mp[w];\n            if(i+1<order.size()) cout<<\", \";\n        }\n        cout<<\"}\"<<\"\\n\";\n    }\n    return 0;\n}",
      "Java": "import java.io.*;\nimport java.util.*;\npublic class Main {\n    private static boolean isVowel(char c){\n        c = Character.toLowerCase(c);\n        return c=='a'||c=='e'||c=='i'||c=='o'||c=='u';\n    }\n    public static void main(String[] args) throws Exception{\n        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));\n        String line = br.readLine();\n        if(line==null) return;\n        int T = Integer.parseInt(line.trim());\n        for(int tc=0; tc<T; ++tc){\n            String sentence = br.readLine();\n            if(sentence==null) sentence = \"\";\n            String[] parts = sentence.split(\" \");\n            LinkedHashMap<String,Integer> map = new LinkedHashMap<>();\n            List<String> order = new ArrayList<>();\n            for(String w: parts){\n                if(w.isEmpty()) continue;\n                int cnt=0;\n                for(char ch: w.toCharArray()) if(isVowel(ch)) ++cnt;\n                if(map.containsKey(w)){\n                    // remove previous occurrence from order list\n                    order.remove(w);\n                }\n                order.add(w);\n                map.put(w,cnt);\n            }\n            StringBuilder sb = new StringBuilder();\n            sb.append('{');\n            for(int i=0;i<order.size();++i){\n                String w = order.get(i);\n                sb.append('\\'').append(w).append('\\'').append(\": \").append(map.get(w));\n                if(i+1<order.size()) sb.append(\", \");\n            }\n            sb.append('}');\n            System.out.println(sb.toString());\n        }\n    }\n}",
      "Python3": "import sys\n\ndef vowel_count(word):\n    return sum(1 for ch in word if ch.lower() in 'aeiou')\n\ndef process(sentence):\n    order = []\n    mp = {}\n    for w in sentence.split():\n        cnt = vowel_count(w)\n        if w in mp:\n            order.remove(w)\n        order.append(w)\n        mp[w] = cnt\n    return '{' + ', '.join(f\"'{k}': {mp[k]}\" for k in order) + '}'\n\ndef main():\n    data = sys.stdin.read().splitlines()\n    if not data:\n        return\n    T = int(data[0].strip())\n    for i in range(1, T+1):\n        sentence = data[i] if i < len(data) else ''\n        print(process(sentence))\n\nif __name__ == '__main__':\n    main()",
      "JavaScript": "const fs = require('fs');\nconst input = fs.readFileSync(0,'utf-8').split(/\\n/);\nif (input.length===0) process.exit(0);\nconst T = parseInt(input[0].trim(),10)||0;\nfunction isVowel(ch){\n  const c = ch.toLowerCase();\n  return c==='a'||c==='e'||c==='i'||c==='o'||c==='u';\n}\nlet out = [];\nfor(let i=1;i<=T;i++){\n  const line = input[i]!==undefined?input[i]:'';\n  const words = line.split(' ').filter(w=>w.length>0);\n  const map = new Map();\n  const order = [];\n  for(const w of words){\n    let cnt=0; for(const ch of w){ if(isVowel(ch)) cnt++; }\n    if(map.has(w)){\n      const idx = order.indexOf(w);\n      if(idx!==-1) order.splice(idx,1);\n    }\n    order.push(w);\n    map.set(w,cnt);\n  }\n  const dict = '{' + order.map(w=>`'${w}': ${map.get(w)}`).join(', ') + '}';\n  out.push(dict);\n}\nconsole.log(out.join('\\n'));\n"
    }
  },
  "QBCategory": "CCD",
  "boilerPlates": {
    "C": "#include <stdio.h>\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "C++": "#include <iostream>\nusing namespace std;\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "Java": "import java.util.*;\nimport java.io.*;\n\npublic class Main {\n    public static void main(String[] args) throws IOException {\n        // your code goes here\n    }\n}",
    "Python3": "# your code goes here\n",
    "JavaScript": "// your code goes here\n"
  },
  "_internal": {
    "methodName": "",
    "params": []
  }
}