{
  "questionId": "Q11780",
  "title": "Minimum cost to cut the stick",
  "slug": "minimum-cost-to-cut-a-stick",
  "version": 1,
  "createdAt": "2026-07-05",
  "updatedAt": "2026-07-05",
  "metadata": {
    "category": "Dynamic Programming",
    "difficulty": "Hard",
    "isPremium": true,
    "tags": [
      "stick cutting",
      "minimum cost",
      "dynamic programming"
    ],
    "companyTags": [],
    "topicTags": [],
    "QBCategory": "CCD"
  },
  "content": {
    "problemStatement": "**Problem Description: **We are given a stick of length N and a cuts array of size C. The stick has markings as shown, and the cuts array depicts the marking at which the stick needs to be cut (shown in red).\n\n\n\n\n\n\n\n\n\n**Note: **It is necessary to make cuts at all the red markings.\n\n\n\n\n\nWhenever we make a cut, we incur a cost. This cost is equal to the length of the stick on which we are making the cut.\n\n\n\n\n<!--",
    "constraints": [
      "1 <= N <= 10^5",
      "1 <= C <= 100",
      "0 < cuts[i] < N"
    ],
    "inputFormat": "The first line contains an integer N, the length of the stick. The second line contains an integer C, the number of cuts. The third line contains C space-separated integers representing the cuts.",
    "outputFormat": "The minimum cost incurred to cut the stick.",
    "sampleTestCases": []
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "label": "min boundary",
        "input": "5 1\n1",
        "expectedOutput": "5",
        "weight": 2
      },
      {
        "id": "tc_2",
        "label": "max boundary",
        "input": "100000 9\n10000 20000 30000 40000 50000 60000 70000 80000 90000",
        "expectedOutput": "100000",
        "weight": 2
      },
      {
        "id": "tc_3",
        "label": "uniform/edge cases",
        "input": "10 5\n1 2 3 4 5",
        "expectedOutput": "15",
        "weight": 2
      },
      {
        "id": "tc_4",
        "label": "tricky case / empty-like / negative if allowed",
        "input": "10 0",
        "expectedOutput": "0",
        "weight": 2
      },
      {
        "id": "tc_5",
        "label": "general mixed case",
        "input": "20 4\n3 7 12 18",
        "expectedOutput": "38",
        "weight": 2
      }
    ],
    "totalTestCases": 5,
    "totalWeight": 10
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      2,
      2,
      2,
      2,
      2
    ],
    "maxScore": 10
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "Use dynamic programming to find the minimum cost to cut the stick by trying all possible cuts and taking the minimum cost.",
    "timeComplexity": "O(C^2 * N)",
    "spaceComplexity": "O(C)",
    "code": {
      "C": "#include <stdio.h>\n#include <stdlib.h>\n\nint minCost(int* cuts, int n, int c) {\n    int** dp = (int**)malloc((c + 2) * sizeof(int*));\n    for (int i = 0; i < c + 2; i++) {\n        dp[i] = (int*)malloc((c + 2) * sizeof(int));\n    }\n\n    for (int i = 1; i <= c + 1; i++) {\n        for (int j = i; j <= c + 1; j++) {\n            if (i == j) {\n                dp[i][j] = 0;\n            } else {\n                dp[i][j] = INT_MAX;\n                for (int k = i + 1; k < j; k++) {\n                    dp[i][j] = (dp[i][j] < dp[i][k] + dp[k][j] + cuts[j] - cuts[i - 1]) ? dp[i][j] : dp[i][k] + dp[k][j] + cuts[j] - cuts[i - 1];\n                }\n            }\n        }\n    }\n\n    int result = dp[1][c + 1];\n    for (int i = 0; i < c + 2; i++) {\n        free(dp[i]);\n    }\n    free(dp);\n    return result;\n}\n\nint main() {\n    int n, c;\n    scanf(\"%d %d\", &n, &c);\n    int* cuts = (int*)malloc((c + 2) * sizeof(int));\n    cuts[0] = 0;\n    cuts[c + 1] = n;\n    for (int i = 1; i <= c; i++) {\n        scanf(\"%d\", &cuts[i]);\n    }\n\n    printf(\"%d\\n\", minCost(cuts, n, c));\n\n    free(cuts);\n    return 0;\n}",
      "C++": "#include <bits/stdc++.h>\nusing namespace std;\n\n// Solution class to compute minimum cost to cut a stick\nclass Solution {\npublic:\n    // Recursive function to find the minimum cost to cut between indices i and j in cuts\n    int findMinimumCost(int i, int j, vector<int> &cuts) {\n        // Base case: no cuts to consider\n        if (i > j) {\n            return 0;\n        }\n\n        int mini = INT_MAX;\n\n        // Try cutting at every position between i and j\n        for (int ind = i; ind <= j; ind++) {\n            // Cost of making the current cut plus\n            // cost of cutting left and right sub-segments recursively\n            int ans = cuts[j + 1] - cuts[i - 1] +\n                      findMinimumCost(i, ind - 1, cuts) +\n                      findMinimumCost(ind + 1, j, cuts);\n\n            // Update minimum cost\n            mini = min(mini, ans);\n        }\n\n        return mini;\n    }\n\n    // Function to calculate minimum total cost to cut the stick\n    int minimumCost(int n, int c, vector<int> &cuts) {\n        // Add the two ends of the stick to the cuts array\n        cuts.push_back(n);\n        cuts.insert(cuts.begin(), 0);\n\n        // Sort the cuts array\n        sort(cuts.begin(), cuts.end());\n\n        // Compute the minimum cost to make all cuts between indices 1 and c\n        return findMinimumCost(1, c, cuts);\n    }\n};\n\nint main() {\n    vector<int> cuts = {3, 5, 1, 4};\n    int c = cuts.size();\n    int n = 7;\n\n    Solution sol;\n    cout << \"The minimum cost incurred is: \" << sol.minimumCost(n, c, cuts) << endl;\n\n    return 0;\n}",
      "Java": "import java.util.*;\n\n// Solution class to compute minimum cost to cut a stick\nclass Solution {\n    // Recursive function to find the minimum cost to cut between indices i and j in cuts\n    public int findMinimumCost(int i, int j, List<Integer> cuts) {\n        // Base case: no cuts to consider\n        if (i > j) {\n            return 0;\n        }\n\n        int mini = Integer.MAX_VALUE;\n\n        // Try cutting at every position between i and j\n        for (int ind = i; ind <= j; ind++) {\n            // Cost of making the current cut plus\n            // cost of cutting left and right sub-segments recursively\n            int ans = cuts.get(j + 1) - cuts.get(i - 1) +\n                      findMinimumCost(i, ind - 1, cuts) +\n                      findMinimumCost(ind + 1, j, cuts);\n\n            // Update minimum cost\n            mini = Math.min(mini, ans);\n        }\n\n        return mini;\n    }\n\n    // Function to calculate minimum total cost to cut the stick\n    public int minimumCost(int n, List<Integer> cuts) {\n        // Add the two ends of the stick to the cuts list\n        cuts.add(n);\n        cuts.add(0);\n        Collections.sort(cuts);\n\n        // Compute the minimum cost to make all cuts between indices 1 and cuts.size()-2\n        return findMinimumCost(1, cuts.size() - 2, cuts);\n    }\n}\n\npublic class Main {\n    public static void main(String[] args) {\n        List<Integer> cuts = new ArrayList<>(Arrays.asList(3, 5, 1, 4));\n        int n = 7;\n\n        Solution sol = new Solution();\n        System.out.println(\"The minimum cost incurred is: \" + sol.minimumCost(n, cuts));\n    }\n}",
      "Python3": "# Solution class to compute minimum cost to cut a stick\nclass Solution:\n    # Recursive function to find the minimum cost to cut between indices i and j in cuts\n    def findMinimumCost(self, i, j, cuts):\n        # Base case: no cuts to consider\n        if i > j:\n            return 0\n\n        mini = float('inf')\n\n        # Try cutting at every position between i and j\n        for ind in range(i, j + 1):\n            # Cost of making the current cut plus\n            # cost of cutting left and right sub-segments recursively\n            ans = cuts[j + 1] - cuts[i - 1] + \\\n                  self.findMinimumCost(i, ind - 1, cuts) + \\\n                  self.findMinimumCost(ind + 1, j, cuts)\n\n            # Update minimum cost\n            mini = min(mini, ans)\n\n        return mini\n\n    # Function to calculate minimum total cost to cut the stick\n    def minimumCost(self, n, cuts):\n        # Add the two ends of the stick to the cuts list\n        cuts.append(n)\n        cuts.insert(0, 0)\n        cuts.sort()\n\n        # Compute the minimum cost to make all cuts between indices 1 and len(cuts)-2\n        return self.findMinimumCost(1, len(cuts) - 2, cuts)\n\n\nif __name__ == \"__main__\":\n    cuts = [3, 5, 1, 4]\n    n = 7\n\n    sol = Solution()\n    print(\"The minimum cost incurred is:\", sol.minimumCost(n, cuts))",
      "JavaScript": "// Solution class to compute minimum cost to cut a stick\nclass Solution {\n    // Recursive function to find the minimum cost to cut between indices i and j in cuts\n    findMinimumCost(i, j, cuts) {\n        // Base case: no cuts to consider\n        if (i > j) {\n            return 0;\n        }\n\n        let mini = Number.MAX_SAFE_INTEGER;\n\n        // Try cutting at every position between i and j\n        for (let ind = i; ind <= j; ind++) {\n            // Cost of making the current cut plus\n            // cost of cutting left and right sub-segments recursively\n            let ans = cuts[j + 1] - cuts[i - 1] +\n                      this.findMinimumCost(i, ind - 1, cuts) +\n                      this.findMinimumCost(ind + 1, j, cuts);\n\n            // Update minimum cost\n            mini = Math.min(mini, ans);\n        }\n\n        return mini;\n    }\n\n    // Function to calculate minimum total cost to cut the stick\n    minimumCost(n, cuts) {\n        // Add the two ends of the stick to the cuts array\n        cuts.push(n);\n        cuts.unshift(0);\n        cuts.sort((a, b) => a - b);\n\n        // Compute the minimum cost to make all cuts between indices 1 and cuts.length-2\n        return this.findMinimumCost(1, cuts.length - 2, cuts);\n    }\n}\n\n// Main execution\nconst cuts = [3, 5, 1, 4];\nconst n = 7;\nconst sol = new Solution();\n\nconsole.log(\"The minimum cost incurred is:\", sol.minimumCost(n, cuts));"
    }
  },
  "QBCategory": "CCD",
  "boilerPlates": {
    "C": "#include <stdio.h>\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "C++": "#include <iostream>\nusing namespace std;\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "Java": "import java.util.*;\nimport java.io.*;\n\npublic class Main {\n    public static void main(String[] args) throws IOException {\n        // your code goes here\n    }\n}",
    "Python3": "# your code goes here\n",
    "JavaScript": "// your code goes here\n"
  },
  "_internal": {
    "methodName": "",
    "params": []
  }
}