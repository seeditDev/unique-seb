{
  "questionId": "Q5426",
  "title": "Home Delivery",
  "slug": "home-delivery",
  "version": 1,
  "createdAt": "2026-07-01",
  "updatedAt": "2026-07-04",
  "metadata": {
    "category": "Graph",
    "difficulty": "Medium",
    "isPremium": true,
    "tags": [
      "DFS",
      "Connected Components"
    ],
    "companyTags": [],
    "topicTags": [
      "Graphs"
    ],
    "QBCategory": "CCD"
  },
  "content": {
    "problemStatement": "Chef has decided to start home delivery from his restaurant. He hopes that he will get a lot of orders for delivery, however there is a concern. He doesn't have enough work forces for all the deliveries.  For this he has came up with an idea - he will group together all those orders which have to be delivered in nearby areas. \nIn particular, he has identified certain bidirectional roads which he calls as fast roads. They are short and usually traffic free, so the fast travel is possible along these roads. His plan is that he will send orders to locations **A** and **B** together if and only if it is possible to travel between **A** and **B** using only fast roads. Your task is, given the configuration of fast roads, identify which orders are to be sent together.",
    "constraints": [
      "1 ≤ T ≤ 100",
      "1 ≤ N ≤ 100",
      "1 ≤ M ≤ 1000",
      "0 ≤ A, B, X, Y ≤ N-1",
      "1 ≤ Q ≤ 3000"
    ],
    "inputFormat": "The first line contains an integer T, the number of test cases. Then T test cases follow. The first line of each test case contains two space-separated integers N and M, denoting the number of locations and the number of fast roads. Then M lines follow, each containing two space-separated integers A and B, denoting that there is a fast road between locations A and B. The next line contains an integer Q, denoting the number of queries. Each of the next Q lines contains two integers X and Y.",
    "outputFormat": "For each query, output 'YO' if the orders are to be delivered together and 'NO' otherwise.",
    "sampleTestCases": [
      {
        "id": "sample_1",
        "input": "1\n4 2\n0 1\n1 2\n3\n0 2\n0 3\n2 1",
        "output": "YO\nNO\nYO",
        "explanation": "The graph has two connected components: {0, 1, 2} and {3}. The queries are: (0, 2) - YES, (0, 3) - NO, (2, 1) - YES."
      },
      {
        "id": "sample_2",
        "input": "1\n5 4\n0 1\n1 2\n2 3\n3 4\n2\n0 4\n1 3",
        "output": "YO\nYO",
        "explanation": "The graph has one connected component: {0, 1, 2, 3, 4}. The queries are: (0, 4) - YES, (1, 3) - YES."
      },
      {
        "id": "sample_3",
        "input": "1\n3 0\n1\n0 1",
        "output": "NO",
        "explanation": "The graph has three connected components: {0}, {1}, {2}. The query is: (0, 1) - NO."
      }
    ]
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "label": "minimum boundary",
        "input": "1\n1 0\n1\n0 0",
        "expectedOutput": "NO",
        "weight": 2
      },
      {
        "id": "tc_2",
        "label": "maximum boundary",
        "input": "1\n100 1000\n",
        "expectedOutput": "...",
        "weight": 2
      },
      {
        "id": "tc_3",
        "label": "uniform/edge case",
        "input": "1\n10 0\n10\n0 0",
        "expectedOutput": "NO",
        "weight": 2
      },
      {
        "id": "tc_4",
        "label": "tricky case",
        "input": "1\n5 4\n0 1\n1 2\n2 3\n3 4\n2\n0 4\n1 3",
        "expectedOutput": "YO\nYO",
        "weight": 2
      },
      {
        "id": "tc_5",
        "label": "stress/general",
        "input": "10\n10 10\n",
        "expectedOutput": "...",
        "weight": 2
      }
    ],
    "totalTestCases": 5,
    "totalWeight": 10
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      2,
      2,
      2,
      2,
      2
    ],
    "maxScore": 10
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "The problem can be solved by using Depth-First Search (DFS) to find the connected components in the graph. We start by initializing the color of each node to 0. Then, we iterate over all nodes and for each unvisited node, we perform a DFS and assign the same color to all nodes in the same connected component. Finally, we iterate over all queries and for each query, we check if the colors of the two nodes are the same. If they are the same, we output 'YO', otherwise we output 'NO'.",
    "timeComplexity": "O(T * (N + M + Q))",
    "spaceComplexity": "O(N + M)",
    "code": {
      "C": "#include <stdio.h>\n#include <stdlib.h>\n\n#define N 101\n\n// Structure for adjacency list node\nstruct Node {\n    int vertex;\n    struct Node* next;\n};\n\n// Array of adjacency lists\nstruct Node* adj[N];\n\n// Array to keep track of visited nodes\nint vis[N];\n\n// Array to keep track of node colors\nint color[N];\n\n// Function to create a new adjacency list node\nstruct Node* createNode(int v) {\n    struct Node* newNode = (struct Node*)malloc(sizeof(struct Node));\n    newNode->vertex = v;\n    newNode->next = NULL;\n    return newNode;\n}\n\n// Function to add an edge to an undirected graph\nvoid addEdge(int src, int dest) {\n    struct Node* newNode = createNode(dest);\n    newNode->next = adj[src];\n    adj[src] = newNode;\n\n    newNode = createNode(src);\n    newNode->next = adj[dest];\n    adj[dest] = newNode;\n}\n\n// Depth First Search function\nvoid dfs(int v, int col) {\n    if (vis[v]) {\n        return; // If the node is already visited, do not process it\n    }\n    vis[v] = 1; // Mark the node as visited\n    color[v] = col; // Color the node\n\n    struct Node* temp = adj[v];\n    while (temp) {\n        int u = temp->vertex;\n        dfs(u, col);\n        temp = temp->next;\n    }\n}\n\nint main() {\n    int t;\n    scanf(\"%d\", &t);\n\n    while (t--) {\n        int n, m;\n        scanf(\"%d %d\", &n, &m);\n\n        // Resetting the adjacency list, color, and visited array\n        for (int i = 0; i <= n; i++) {\n            struct Node* temp = adj[i];\n            while (temp) {\n                struct Node* next = temp->next;\n                free(temp);\n                temp = next;\n            }\n            adj[i] = NULL;\n            color[i] = 0;\n            vis[i] = 0;\n        }\n\n        for (int i = 1; i <= m; i++) {\n            int x, y;\n            scanf(\"%d %d\", &x, &y);\n            addEdge(x, y);\n        }\n\n        int current = 1;\n        for (int i = 0; i < n; i++) {\n            if (!vis[i]) {\n                // Color all the nodes connected with i\n                // with the same color\n                dfs(i, current);\n                current++;\n            }\n        }\n\n        // Two connected places will now have the same color\n\n        int q;\n        scanf(\"%d\", &q);\n        while (q--) {\n            int x, y;\n            scanf(\"%d %d\", &x, &y);\n\n            // Output \"YO\" if both nodes have the same color\n            if (color[x] == color[y]) {\n                printf(\"YO\\n\");\n            } else {\n                printf(\"NO\\n\");\n            }\n        }\n    }\n    return 0;\n}",
      "C++": "#include <bits/stdc++.h>\nusing namespace std;\n\nconst int N = 101;\nvector<int> adj[N];\nint vis[N];\n\nint color[N];\n\nvoid dfs(int v, int col) {\n    if(vis[v]) \n        return;\n\n    vis[v] = 1;\n    color[v] = col;\n\n    for(auto x: adj[v]) {\n        dfs(x, col);\n    }\n}\n\n\nint main() {\n    int t; \n    cin >> t;\n\n    while(t--) {\n        int n, m; \n        cin >> n >> m;\n\n        // Resetting the adjacency list, color and visited array\n        for(int i = 0; i <= n; i++) {\n            adj[i].clear();\n            color[i] = 0;\n            vis[i] = 0;\n        }   \n\n        for(int i = 1; i <= m; i++) {\n            int x, y; \n            cin >> x >> y;\n            adj[x].push_back(y);\n            adj[y].push_back(x);\n        }   \n\n        int current = 1;\n        for(int i = 0; i < n; i++) {\n            if(!vis[i]) {\n\n                // Color all the nodes connected with i \n                // with the same color\n                dfs(i, current);\n\n                current++;\n            }\n        }\n\n        /*  \n        Two connected place will now have same color\n        */  \n\n        int q; cin >> q;\n        while(q--) {\n            int x, y; \n            cin >> x >> y;\n\n            // Output YES if both nodes have the same color\n            if(color[x] == color[y]) {\n                cout << \"YO\\n\";\n            }\n            else {\n                cout << \"NO\\n\";\n            }\n        }\n    }\n}",
      "Java": "import java.util.*;\n\nclass Main {\n    static int N = 101;\n    static List<Integer>[] adj = new ArrayList[N];\n    static int[] vis = new int[N];\n    static int[] color = new int[N];\n\n    public static void dfs(int v, int col) {\n        if (vis[v] == 1)\n            return;\n\n        vis[v] = 1;\n        color[v] = col;\n\n        for (int x : adj[v]) {\n            dfs(x, col);\n        }\n    }\n\n    public static void main(String[] args) {\n        Scanner scanner = new Scanner(System.in);\n        int t = scanner.nextInt(); \n\n        while (t-- > 0) {\n            int n = scanner.nextInt(); \n            int m = scanner.nextInt(); \n\n            for (int i = 0; i <= n; i++) {\n                adj[i] = new ArrayList<>();\n                color[i] = 0;\n                vis[i] = 0;\n            }\n\n            // Creating the graph\n            for (int i = 1; i <= m; i++) {\n                int x = scanner.nextInt();\n                int y = scanner.nextInt();\n                adj[x].add(y);\n                adj[y].add(x);\n            }\n\n            int current = 1;\n            for (int i = 0; i < n; i++) {\n                if (vis[i] == 0) {\n                    // Color all the connected nodes with the same color\n                    dfs(i, current);\n                    current++;\n                }\n            }\n\n            int q = scanner.nextInt(); \n\n            // Checking if nodes have the same color\n            while (q-- > 0) {\n                int x = scanner.nextInt();\n                int y = scanner.nextInt();\n\n                if (color[x] == color[y]) {\n                    System.out.println(\"YO\");\n                } else {\n                    System.out.println(\"NO\");\n                }\n            }\n        }\n    }\n}",
      "Python3": "N = 101\nadj = [[] for _ in range(N)]\nvis = [0] * N\ncolor = [0] * N\n\ndef dfs(v, col):\n    if vis[v]:\n        return\n    \n    vis[v] = 1\n    color[v] = col\n\n    for x in adj[v]:\n        dfs(x, col)\n\nif __name__ == \"__main__\":\n    t = int(input())\n\n    for _ in range(t):\n        n, m = map(int, input().split())\n\n        # Resetting the adjacency list, color, and visited array\n        adj = [[] for _ in range(n + 1)]\n        color = [0] * (n + 1)\n        vis = [0] * (n + 1)\n\n        for _ in range(m):\n            x, y = map(int, input().split())\n            adj[x].append(y)\n            adj[y].append(x)\n\n        current = 1\n        for i in range(n):\n            if not vis[i]:\n                # Color all the nodes connected with i \n                # with the same color\n                dfs(i, current)\n                current += 1\n\n        q = int(input())\n        for _ in range(q):\n            x, y = map(int, input().split())\n\n            # Output YES if both nodes have the same color\n            if color[x] == color[y]:\n                print(\"YO\")\n            else:\n                print(\"NO\")\n",
      "JavaScript": "const fs = require('fs');\nconst lines = fs.readFileSync(0, 'utf-8').trim().split('\\n');\nlet idx = 0;\n\nfunction readline() {\n    return lines[idx++];\n}\n\nfunction dfs(v, col, adj, vis, color) {\n    if (vis[v]) {\n        return;\n    }\n    vis[v] = 1;\n    color[v] = col;\n\n    for (let x of adj[v]) {\n        dfs(x, col, adj, vis, color);\n    }\n}\n\nlet t = parseInt(readline());\n\nwhile (t--) {\n    let [n, m] = readline().split(' ').map(Number);\n\n    let adj = Array.from({ length: n + 1 }, () => []);\n    let vis = new Array(n + 1).fill(0);\n    let color = new Array(n + 1).fill(0);\n\n    for (let i = 1; i <= m; i++) {\n        let [x, y] = readline().split(' ').map(Number);\n        adj[x].push(y);\n        adj[y].push(x);\n    }\n\n    let current = 1;\n    for (let i = 0; i < n; i++) {\n        if (!vis[i]) {\n            dfs(i, current, adj, vis, color);\n            current++;\n        }\n    }\n\n    let q = parseInt(readline());\n    while (q--) {\n        let [x, y] = readline().split(' ').map(Number);\n\n        if (color[x] === color[y]) {\n            console.log('YO');\n        } else {\n            console.log('NO');\n        }\n    }\n}"
    }
  },
  "QBCategory": "CCD",
  "boilerPlates": {
    "C": "#include <stdio.h>\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "C++": "#include <iostream>\nusing namespace std;\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "Java": "import java.util.*;\nimport java.io.*;\n\npublic class Main {\n    public static void main(String[] args) throws IOException {\n        // your code goes here\n    }\n}",
    "Python3": "# your code goes here\n",
    "JavaScript": "// your code goes here\n"
  },
  "_internal": {
    "methodName": "",
    "params": []
  }
}