{
  "questionId": "Q11951",
  "title": "Reverse LL in group of given size K",
  "slug": "reverse-nodes-in-k-group",
  "version": 1,
  "createdAt": "2026-07-05",
  "updatedAt": "2026-07-05",
  "metadata": {
    "category": "Linked List",
    "difficulty": "Hard",
    "isPremium": true,
    "tags": [
      "Reversal",
      "Grouping"
    ],
    "companyTags": [],
    "topicTags": [],
    "QBCategory": "CCD"
  },
  "content": {
    "problemStatement": "Given the head of a singly linked list containing integers, reverse the nodes of the list in groups of k and return the head of the modified list. If the number of nodes is not a multiple of k, then the remaining nodes at the end should be kept as is and not reversed.\n\n\n\n\nDo not change the values of the nodes, only change the links between nodes.",
    "constraints": [
      "1 <= k <= 10^5",
      "0 <= number of nodes in the linked list <= 10^5",
      "1 <= node value <= 10^5"
    ],
    "inputFormat": "The input will contain the head of a singly linked list and an integer k. The head will be represented as a list of integers, and k will be a single integer.",
    "outputFormat": "The output will be the head of the modified linked list, represented as a list of integers.",
    "sampleTestCases": []
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "label": "min boundary",
        "input": {
          "head": [
            1
          ],
          "k": 1
        },
        "expectedOutput": [
          1
        ],
        "weight": 2
      },
      {
        "id": "tc_2",
        "label": "max boundary",
        "input": {
          "head": [
            1,
            2,
            3,
            4,
            5,
            6,
            7,
            8,
            9
          ],
          "k": 9
        },
        "expectedOutput": [
          9,
          8,
          7,
          6,
          5,
          4,
          3,
          2,
          1
        ],
        "weight": 2
      },
      {
        "id": "tc_3",
        "label": "uniform case",
        "input": {
          "head": [
            1,
            2,
            3,
            4,
            5,
            6
          ],
          "k": 2
        },
        "expectedOutput": [
          2,
          1,
          4,
          3,
          6,
          5
        ],
        "weight": 2
      },
      {
        "id": "tc_4",
        "label": "tricky case",
        "input": {
          "head": [
            1,
            2,
            3,
            4,
            5
          ],
          "k": 3
        },
        "expectedOutput": [
          3,
          2,
          1,
          4,
          5
        ],
        "weight": 2
      },
      {
        "id": "tc_5",
        "label": "general mixed case",
        "input": {
          "head": [
            1,
            2,
            3,
            4,
            5,
            6,
            7,
            8
          ],
          "k": 3
        },
        "expectedOutput": [
          3,
          2,
          1,
          6,
          5,
          4,
          7,
          8
        ],
        "weight": 2
      }
    ],
    "totalTestCases": 5,
    "totalWeight": 10
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      2,
      2,
      2,
      2,
      2
    ],
    "maxScore": 10
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "To solve this problem, we can use a two-pointer approach. We will maintain two pointers, one at the start of the current group and one at the end of the current group. We will then reverse the nodes in the current group and move the pointers to the next group.",
    "timeComplexity": "O(n)",
    "spaceComplexity": "O(1)",
    "code": {
      "C": "#include <stdio.h>\n#include <stdlib.h>\n\n// Definition for singly-linked list.\ntypedef struct ListNode {\n    int val;\n    struct ListNode *next;\n} ListNode;\n\n// Function to reverse nodes in groups of k\nListNode* reverseKGroup(ListNode* head, int k) {\n    // Create a dummy node to handle edge cases easily\n    ListNode* dummy = (ListNode*)malloc(sizeof(ListNode));\n    dummy->next = head;\n    ListNode* groupPrev = dummy;\n\n    while (1) {\n        // Finding the k-th node from the groupPrev\n        ListNode* kth = getKthNode(groupPrev, k);\n        if (!kth) break;\n\n        // Store the next group's head\n        ListNode* groupNext = kth->next;\n\n        // Break the chain to reverse current k-group cleanly\n        ListNode* prev = groupNext;\n        ListNode* curr = groupPrev->next;\n\n        // Reversing k nodes\n        for (int i = 0; i < k; i++) {\n            ListNode* temp = curr->next;\n            curr->next = prev;\n            prev = curr;\n            curr = temp;\n        }\n\n        // Connecting previous group to the reversed group\n        ListNode* temp = groupPrev->next;\n        groupPrev->next = kth;\n        groupPrev = temp;\n    }\n\n    // Returning the new head\n    return dummy->next;\n}\n\n// Helper function to find the k-th node from the current node\nListNode* getKthNode(ListNode* curr, int k) {\n    while (curr && k > 0) {\n        curr = curr->next;\n        k--;\n    }\n    return curr;\n}",
      "C++": "#include <bits/stdc++.h>\nusing namespace std;\n\n// Definition of singly-linked list node\nstruct ListNode {\n    int val;\n    ListNode* next;\n    ListNode(int x) {\n        val = x;\n        next = NULL;\n    }\n};\n\nclass Solution {\npublic:\n    // Function to reverse nodes in groups of k\n    ListNode* reverseKGroup(ListNode* head, int k) {\n        // Creating a dummy node to handle edge cases easily\n        ListNode* dummy = new ListNode(0);\n        dummy->next = head;\n\n        // Pointer to keep track of the previous group's tail\n        ListNode* groupPrev = dummy;\n\n        while (true) {\n            // Finding the k-th node from the groupPrev\n            ListNode* kth = getKthNode(groupPrev, k);\n            if (!kth) break;\n\n            // Store the next group's head\n            ListNode* groupNext = kth->next;\n\n            // Break the chain to reverse current k-group cleanly\n            ListNode* prev = groupNext;\n            ListNode* curr = groupPrev->next;\n\n            // Reversing k nodes\n            for (int i = 0; i < k; i++) {\n                ListNode* temp = curr->next;\n                curr->next = prev;\n                prev = curr;\n                curr = temp;\n            }\n\n            // Connecting previous group to the reversed group\n            ListNode* temp = groupPrev->next;\n            groupPrev->next = kth;\n            groupPrev = temp;\n        }\n\n        // Returning the new head\n        return dummy->next;\n    }\n\n    // Helper function to find the k-th node from the current node\n    ListNode* getKthNode(ListNode* curr, int k) {\n        while (curr && k > 0) {\n            curr = curr->next;\n            k--;\n        }\n        return curr;\n    }\n};",
      "Java": "import java.util.*;\n// Definition for singly-linked list node\npublic class ListNode {\n    int val;\n    ListNode next;\n    ListNode(int x) {\n        val = x;\n        next = null;\n    }\n}\n\nclass Solution {\n    // Function to reverse nodes in groups of k\n    public ListNode reverseKGroup(ListNode head, int k) {\n        // Create a dummy node to handle edge cases easily\n        ListNode dummy = new ListNode(0);\n        dummy.next = head;\n\n        // Pointer to keep track of the previous group's tail\n        ListNode groupPrev = dummy;\n\n        while (true) {\n            // Finding the k-th node from the groupPrev\n            ListNode kth = getKthNode(groupPrev, k);\n            if (kth == null) break;\n\n            // Store the next group's head\n            ListNode groupNext = kth.next;\n\n            // Break the chain to reverse current k-group cleanly\n            ListNode prev = groupNext;\n            ListNode curr = groupPrev.next;\n\n            // Reversing k nodes\n            for (int i = 0; i < k; i++) {\n                ListNode temp = curr.next;\n                curr.next = prev;\n                prev = curr;\n                curr = temp;\n            }\n\n            // Connecting previous group to the reversed group\n            ListNode temp = groupPrev.next;\n            groupPrev.next = kth;\n            groupPrev = temp;\n        }\n\n        // Returning the new head\n        return dummy.next;\n    }\n\n    // Helper function to find the k-th node from the current node\n    private ListNode getKthNode(ListNode curr, int k) {\n        while (curr != null && k > 0) {\n            curr = curr.next;\n            k--;\n        }\n        return curr;\n    }\n}",
      "Python3": "# Definition for singly-linked list node\nclass ListNode:\n    def __init__(self, val):\n        self.val = val\n        self.next = None\n\nclass Solution:\n    # Function to reverse nodes in groups of k\n    def reverseKGroup(self, head, k):\n        # Creating a dummy node to handle edge cases easily\n        dummy = ListNode(0)\n        dummy.next = head;\n\n        # Pointer to keep track of the previous group's tail\n        groupPrev = dummy;\n\n        while True:\n            # Finding the k-th node from the groupPrev\n            kth = self.getKthNode(groupPrev, k);\n            if not kth: break;\n\n            # Store the next group's head\n            groupNext = kth.next;\n\n            # Break the chain to reverse current k-group cleanly\n            prev = groupNext;\n            curr = groupPrev.next;\n\n            # Reversing k nodes\n            for _ in range(k):\n                temp = curr.next;\n                curr.next = prev;\n                prev = curr;\n                curr = temp;\n\n            # Connecting previous group to the reversed group\n            temp = groupPrev.next;\n            groupPrev.next = kth;\n            groupPrev = temp;\n\n        # Returning the new head\n        return dummy.next;\n\n    # Helper function to find the k-th node from the current node\n    def getKthNode(self, curr, k):\n        while curr and k > 0:\n            curr = curr.next;\n            k -= 1;\n        return curr;",
      "JavaScript": "// Definition for singly-linked list node\nclass ListNode {\n    constructor(val) {\n        this.val = val;\n        this.next = null;\n    }\n}\n\nclass Solution {\n    // Function to reverse nodes in groups of k\n    reverseKGroup(head, k) {\n        // Create a dummy node to handle edge cases easily\n        let dummy = new ListNode(0);\n        dummy.next = head;\n\n        // Pointer to keep track of the previous group's tail\n        let groupPrev = dummy;\n\n        while (true) {\n            // Finding the k-th node from the groupPrev\n            let kth = this.getKthNode(groupPrev, k);\n            if (!kth) break;\n\n            // Store the next group's head\n            let groupNext = kth.next;\n\n            // Break the chain to reverse current k-group cleanly\n            let prev = groupNext;\n            let curr = groupPrev.next;\n\n            // Reversing k nodes\n            for (let i = 0; i < k; i++) {\n                let temp = curr.next;\n                curr.next = prev;\n                prev = curr;\n                curr = temp;\n            }\n\n            // Connecting previous group to the reversed group\n            let temp = groupPrev.next;\n            groupPrev.next = kth;\n            groupPrev = temp;\n        }\n\n        // Returning the new head\n        return dummy.next;\n    }\n\n    // Helper function to find the k-th node from the current node\n    getKthNode(curr, k) {\n        while (curr && k > 0) {\n            curr = curr.next;\n            k--;\n        }\n        return curr;\n    }\n}"
    }
  },
  "QBCategory": "CCD",
  "boilerPlates": {
    "C": "#include <stdio.h>\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "C++": "#include <iostream>\nusing namespace std;\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "Java": "import java.util.*;\nimport java.io.*;\n\npublic class Main {\n    public static void main(String[] args) throws IOException {\n        // your code goes here\n    }\n}",
    "Python3": "# your code goes here\n",
    "JavaScript": "// your code goes here\n"
  },
  "_internal": {
    "methodName": "",
    "params": []
  }
}