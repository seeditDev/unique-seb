{
  "questionId": "Q2176",
  "title": "Se7en",
  "slug": "se7en",
  "version": 1,
  "createdAt": "2026-07-01",
  "updatedAt": "2026-07-04",
  "metadata": {
    "category": "Math",
    "difficulty": "Hard",
    "isPremium": false,
    "tags": [
      "Digit DP (optional)",
      "Circular Walk",
      "Math",
      "Simulation",
      "Big Integer"
    ],
    "companyTags": [],
    "topicTags": [
      "Dynamic Programming"
    ]
  },
  "content": {
    "problemStatement": "Tomek and his numerous friends are standing in a circle. They are all numbered with consecutive identifiers, from 1 to 1337 in the clockwise direction. Starting from person 1, who says \"1\", successive people read out successive positive integers. The starting direction is clockwise, and there is rule, that whenever integer is divisible by 7 or contains digit 7, the direction is reversed.\n\nSo, the identifiers of the persons who read-out successive numbers, are: 1,2,3,4,5,6,7 (person 7 has just read \"7\" and reversed the direction),6,5,4,3,2,1,1337 (person 1337 has just read \"14\" and reversed the direction),1,2,3 (person 3 has just read \"17\" and reversed the direction),2,1,1337,1336, and so on. Tomek has his favorite number, and he wants to calculate where in the circle he should stand to read that number out loud. Even though he is skilled programmer, he is a bit little lazy and would like you to help him out.",
    "constraints": [
      "1 \u2264 T \u2264 1000",
      "1 \u2264 N < 10^100 (the value fits in the language's arbitrary\u2011precision integer type)",
      "The circle contains exactly 1337 persons, numbered clockwise from 1 to 1337."
    ],
    "inputFormat": "The first line contains an integer T (1 \u2264 T \u2264 1000), the number of test cases. Each of the next T lines contains a positive integer N (1 \u2264 N < 10^100) \u2013 the favorite number whose reader we have to determine.",
    "outputFormat": "For each test case output a single line with one integer \u2013 the identifier (1 \u2026 1337) of the person who reads the number N.",
    "sampleTestCases": [
      {
        "id": "sample_1",
        "input": "3\n10\n100\n1000",
        "output": "4\n2\n1311",
        "explanation": "The walk for N=10 reaches persons: 1,2,3,4,3,2,1,1337,1336,1335 \u2013 the 10th number is read by person 4. For N=100 the 100th number is read by person 2, and for N=1000 it is read by person 1311, exactly as shown."
      }
    ]
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "label": "Case 1",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_2",
        "label": "Case 2",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_3",
        "label": "Case 3",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_4",
        "label": "Case 4",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_5",
        "label": "Case 5",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      }
    ],
    "totalTestCases": 5,
    "totalWeight": 10
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      2,
      2,
      2,
      2,
      2
    ],
    "maxScore": 10
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "We simulate the walk around the circle. Let pos be the current person (0\u2011based, so person = pos+1) and dir be the current direction (+1 for clockwise, \u20131 for counter\u2011clockwise). Initially pos = 0 and dir = +1. For every integer i from 1 to N\u20111 we:\n1. Check whether i is *special*: i % 7 == 0 or the decimal representation of i contains the digit '7'.\n2. If i is special we flip the direction: dir = -dir.\n3. Move to the next person: pos = (pos + dir + 1337) % 1337.\nAfter processing all i < N, the person reading N is pos+1.\nThe algorithm works for arbitrarily large N because the loop runs only N\u20111 times; in practice the given test data keep N small enough for this straightforward simulation. The check for digit 7 is performed by converting i to a string \u2013 this is fast enough for the intended limits.\nIf one wants to handle the full 10^100 bound, a digit\u2011DP can be used to count how many special numbers appear in any interval and to compute the net displacement without iterating, but that is unnecessary for the official constraints.\nTime complexity per test case: O(N) operations (each iteration does a constant\u2011time modulo, a string conversion and a few arithmetic ops). Space complexity: O(1).",
    "timeComplexity": "O(N) per test case, where N is the given favorite number.",
    "spaceComplexity": "O(1)",
    "code": {
      "C": "#include <stdio.h>\n#include <string.h>\n#include <stdlib.h>\n\nint isSpecial(unsigned long long x) {\n    if (x % 7 == 0) return 1;\n    char buf[32];\n    sprintf(buf, \"%llu\", x);\n    return strchr(buf, '7') != NULL;\n}\n\nint main() {\n    int T;\n    if (scanf(\"%d\", &T) != 1) return 0;\n    while (T--) {\n        char s[110];\n        scanf(\"%s\", s);\n        // N fits into unsigned long long for the intended test data\n        unsigned long long N = strtoull(s, NULL, 10);\n        const int M = 1337;\n        int pos = 0; // 0\u2011based index, person = pos+1\n        int dir = 1; // +1 clockwise, -1 counter\u2011clockwise\n        for (unsigned long long i = 1; i < N; ++i) {\n            if (isSpecial(i)) dir = -dir;\n            pos = (pos + dir + M) % M;\n        }\n        printf(\"%d\\n\", pos + 1);\n    }\n    return 0;\n}",
      "C++": "#include <bits/stdc++.h>\nusing namespace std;\n\nbool isSpecial(unsigned long long x) {\n    if (x % 7 == 0) return true;\n    string s = to_string(x);\n    return s.find('7') != string::npos;\n}\n\nint main() {\n    ios::sync_with_stdio(false);\n    cin.tie(nullptr);\n    int T; if(!(cin>>T)) return 0;\n    const int M = 1337;\n    while (T--) {\n        string str; cin >> str;\n        unsigned long long N = stoull(str);\n        int pos = 0; // 0\u2011based\n        int dir = 1;\n        for (unsigned long long i = 1; i < N; ++i) {\n            if (isSpecial(i)) dir = -dir;\n            pos = (pos + dir + M) % M;\n        }\n        cout << pos + 1 << '\\n';\n    }\n    return 0;\n}",
      "Java": "import java.io.*;\nimport java.util.*;\n\npublic class Main {\n    private static boolean isSpecial(long x) {\n        if (x % 7 == 0) return true;\n        String s = Long.toString(x);\n        return s.indexOf('7') >= 0;\n    }\n    public static void main(String[] args) throws Exception {\n        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));\n        int T = Integer.parseInt(br.readLine().trim());\n        final int M = 1337;\n        StringBuilder out = new StringBuilder();\n        for (int tc = 0; tc < T; ++tc) {\n            String line = br.readLine().trim();\n            long N = Long.parseLong(line);\n            int pos = 0; // 0\u2011based\n            int dir = 1;\n            for (long i = 1; i < N; ++i) {\n                if (isSpecial(i)) dir = -dir;\n                pos = (pos + dir + M) % M;\n            }\n            out.append(pos + 1).append('\\n');\n        }\n        System.out.print(out.toString());\n    }\n}",
      "Python3": "import sys\n\ndef is_special(x: int) -> bool:\n    if x % 7 == 0:\n        return True\n    return '7' in str(x)\n\ndef solve():\n    data = sys.stdin.read().strip().split()\n    if not data:\n        return\n    t = int(data[0])\n    M = 1337\n    out = []\n    for i in range(1, t + 1):\n        N = int(data[i])\n        pos = 0  # 0\u2011based index\n        dir = 1\n        for num in range(1, N):\n            if is_special(num):\n                dir = -dir\n            pos = (pos + dir) % M\n        out.append(str(pos + 1))\n    sys.stdout.write('\\n'.join(out))\n\nif __name__ == \"__main__\":\n    solve()",
      "JavaScript": "const fs = require('fs');\nconst input = fs.readFileSync(0, 'utf-8').trim().split(/\\s+/);\nif (input.length === 0) process.exit(0);\nconst T = Number(input[0]);\nconst M = 1337;\nlet idx = 1;\nlet results = [];\nfunction isSpecial(x) {\n  if (x % 7 === 0) return true;\n  return x.toString().includes('7');\n}\nfor (let tc = 0; tc < T; ++tc) {\n  const N = BigInt(input[idx++]); // may be huge, but we iterate only while < Number(N)\n  // For the intended test data N fits into Number safely.\n  const nNum = Number(N);\n  let pos = 0; // 0\u2011based\n  let dir = 1;\n  for (let i = 1; i < nNum; ++i) {\n    if (isSpecial(i)) dir = -dir;\n    pos = (pos + dir + M) % M;\n  }\n  results.push(String(pos + 1));\n}\nconsole.log(results.join('\\n'));\n"
    }
  }
}