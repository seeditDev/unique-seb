{
  "questionId": "Q8502",
  "title": "Weird Values",
  "slug": "weird-values",
  "version": 1,
  "createdAt": "2026-07-02",
  "updatedAt": "2026-07-04",
  "metadata": {
    "category": "Arrays | Counting | Prefix Sums",
    "difficulty": "Hard",
    "isPremium": true,
    "tags": [
      "Frequency Counting",
      "Subarray Enumeration",
      "Linear Scan",
      "Combinatorial Counting"
    ],
    "companyTags": [],
    "topicTags": [
      "Combinatorics & Probability"
    ],
    "QBCategory": "CCD"
  },
  "content": {
    "problemStatement": "You are given an array A=[A1,A2,…,AN]A = [A_1, A_2, \\ldots, A_N]A=[A1​,A2​,…,AN​] containing NNN integers.\n\nConsider any subarray [AL,AL+1,…,AR][A_L, A_{L+1}, \\ldots, A_R][AL​,AL+1​,…,AR​] of this subarray (where 1≤L≤R≤N1\\leq L \\leq R \\leq N1≤L≤R≤N). Ther `weirdness` of this subarray, denoted w(L,R)w(L, R)w(L,R), is defined to be the number of indices L≤i≤RL \\leq i \\leq RL≤i≤R such that AiA_iAi​ equals the number of occurrences of AiA_iAi​ in this subarray.\n\nFormally, we define w(L,R)w(L, R)w(L,R) for 1≤L≤R≤N1 \\leq L \\leq R \\leq N1≤L≤R≤N as follows:\n\n\nFor every integer xxx, let fL,R(x)f_{L, R}(x)fL,R​(x) denote the number of indices L≤j≤RL \\leq j \\leq RL≤j≤R such that Aj=xA_j = xAj​=x.\nw(L,R)w(L, R)w(L,R) then equals the number of L≤i≤RL \\leq i \\leq RL≤i≤R such that Ai=fL,R(Ai)A_i = f_{L, R}(A_i)Ai​=fL,R​(Ai​)\nFor example, let A=[1,3,5,2,1,2,9,5]A = [1, 3, 5, 2, 1, 2, 9, 5]A=[1,3,5,2,1,2,9,5]. Then,\n\n\nw(1,8)=2w(1, 8) = 2w(1,8)=2, which can be computed as follows.\nWe have\n\nf1,8(1)=2f_{1, 8}(1) = 2f1,8​(1)=2\nf1,8(2)=2f_{1, 8}(2) = 2f1,8​(2)=2\nf1,8(3)=1f_{1, 8}(3) = 1f1,8​(3)=1\nf1,8(5)=2f_{1, 8}(5) = 2f1,8​(5)=2\nf1,8(9)=1f_{1, 8}(9) = 1f1,8​(9)=1\n\n\nThe only indices which satisfy f1,8(Ai)=Aif_{1, 8}(A_i) = A_if1,8​(Ai​)=Ai​ are i=4i = 4i=4 and i=6i = 6i=6, and hence w(1,8)=2w(1, 8) = 2w(1,8)=2.\n\n\nw(3,5)=1w(3, 5) = 1w(3,5)=1 — the subarray is [5,2,1][5, 2, 1][5,2,1], and each of 1,2,51, 2, 51,2,5 occur exactly once in this subarray. Of these, only 111 equals the number of its occurrences in this subarray.\nGiven AAA, your task is to compute the sum of the `weirdness` of all subarrays of AAA, i.e, the value\n∑L=1N∑R=LNw(L,R)\\sum_{L = 1}^{N} \\sum_{R = L}^N w(L, R)∑L=1N​∑R=LN​w(L,R)\n\n\nInput Format\n\nThe first line of input contains a single integer TTT, denoting the number of test cases. The description of TTT test cases follows.\nThe first line of each test case contains an integer NNN, the size of the given array.\nThe second line of each test case contains NNN space-separated integers A1,A2,…,ANA_1, A_2, \\ldots, A_NA1​,A2​,…,AN​ — the elements of AAA.\n\n\nOutput Format\nFor each test case, output a single line containing a single integer — the sum of the `weirdness` of all subarrays of the given array.",
    "constraints": [
      "1 ≤ T ≤ 10^3",
      "1 ≤ N ≤ 10^6",
      "1 ≤ Ai ≤ 10^6",
      "The sum of N over all test cases does not exceed 10^6."
    ],
    "inputFormat": "The first line contains an integer T, the number of test cases. Then for each test case:\n- A line with an integer N, the length of the array.\n- A line with N space‑separated integers A1 … AN.\nAll indices are 1‑based in the description.",
    "outputFormat": "For each test case output a single line containing one integer – the sum of the weirdness of all subarrays of the given array.",
    "sampleTestCases": [
      {
        "id": "sample_1",
        "input": "4\n3\n3 3 3\n4\n1 2 1 2\n5\n1 2 3 2 1\n8\n1 2 2 3 2 3 3 1",
        "output": "3\n10\n16\n54",
        "explanation": "Explanation:\nTest Case 1: The only subarray with non-zero weirdness is the whole array, which has w(1,3)=3w(1, 3) = 3w(1,3)=3 because every value is 333 and it appears thrice.\nTest Case 2: There are 101010 subarrays. Their weirdness values are as follows:\n\nw(1,1)=1w(1, 1) = 1w(1,1)=1, because A1=1A_1 = 1A1​=1 and f1,1(1)=1f_{1, 1}(1) = 1f1,1​(1)=1.\nw(1,2)=1w(1, 2) = 1w(1,2)=1, because A1=1A_1 = 1A1​=1 and f1,2(1)=1f_{1, 2}(1) = 1f1,2​(1)=1; but A2=2A_2 = 2A2​=2 and f1,2(2)=1≠2f_{1, 2}(2) = 1 \\neq 2f1,2​(2)=1=2.\nw(1,3)=0w(1, 3) = 0w(1,3)=0, here f1,3(1)=2≠1f_{1, 3}(1) = 2 \\neq 1f1,3​(1)=2=1 and f1,3(2)=1≠2f_{1, 3}(2) = 1 \\neq 2f1,3​(2)=1=2 so no index satisfies the condition.\nw(1,4)=2w(1, 4) = 2w(1,4)=2, here indices 222 and 444 satisfy the condition.\nw(2,2)=0w(2, 2) = 0w(2,2)=0\nw(2,3)=1w(2, 3) = 1w(2,3)=1, index 333 satisfies the condition.\nw(2,4)=3w(2, 4) = 3w(2,4)=3, all indices satisfy the condition.\nw(3,3)=1w(3, 3) = 1w(3,3)=1\nw(3,4)=1w(3, 4) = 1w(3,4)=1, index 333 satisfies the condition.\nw(4,4)=0w(4, 4) = 0w(4,4)=0"
      }
    ]
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "label": "Case 1",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_2",
        "label": "Case 2",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_3",
        "label": "Case 3",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_4",
        "label": "Case 4",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_5",
        "label": "Case 5",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      }
    ],
    "totalTestCases": 5,
    "totalWeight": 10
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      2,
      2,
      2,
      2,
      2
    ],
    "maxScore": 10
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "For a subarray its weirdness counts every index i whose value x equals the frequency of x inside that subarray. Hence for a fixed value x, each subarray that contains exactly x occurrences of x contributes x to the total (once for each of the x positions). Therefore the whole answer can be rewritten as:\n\n    answer = Σ over distinct values v   v * (number of subarrays where v appears exactly v times)\n\nIf the positions of value v in the whole array are p1 < p2 < … < pk, a subarray contains exactly v occurrences of v iff it contains a consecutive block of v positions pi … p_{i+v‑1} and no other occurrence of v. The left border L can be any index after the previous occurrence (or from 1) up to pi, i.e. (pi – p_{i‑1}) possibilities where p0 = 0. The right border R can be any index from p_{i+v‑1} up to just before the next occurrence, i.e. (p_{i+v} – p_{i+v‑1}) possibilities where p_{k+1} = N+1.\n\nThus for each i (1‑based) with i+v‑1 ≤ k we add:\n\n    contribution = v * (pi – p_{i‑1}) * (p_{i+v} – p_{i+v‑1})\n\nSumming these contributions over all i and all values gives the required total. The algorithm builds a list of positions for each distinct value, then iterates over each list once. The overall complexity is O(N) per test case.\n\nEdge cases handled:\n- If a value appears fewer than v times, it contributes nothing.\n- Single‑element arrays are processed correctly (v = 1 case).\n- Large values up to 10^6 are supported because we store positions only for values that actually appear.",
    "timeComplexity": "O(N) per test case (building the position lists and scanning them). The total over all test cases is O( Σ N ) ≤ 10^6.",
    "spaceComplexity": "O(N) to store all positions (each array element appears in exactly one list).",
    "code": {
      "C": "#include <stdio.h>\n#include <stdlib.h>\n\ntypedef struct Node {\n    int *data;\n    int size;\n    int cap;\n} Node;\n\nvoid push(Node *v, int x) {\n    if (v->size == v->cap) {\n        v->cap = v->cap ? v->cap * 2 : 4;\n        v->data = realloc(v->data, v->cap * sizeof(int));\n    }\n    v->data[v->size++] = x;\n}\n\nint main() {\n    int T; if (scanf(\"%d\", &T)!=1) return 0;\n    while (T--) {\n        int N; scanf(\"%d\", &N);\n        int *A = (int*)malloc(N * sizeof(int));\n        for (int i = 0; i < N; ++i) scanf(\"%d\", &A[i]);\n        // map value -> list of positions using hash table (simple chaining)\n        // Since Ai ≤ 1e6 and total N ≤ 1e6, we can allocate an array of pointers.\n        Node *vec = (Node*)calloc(1000001, sizeof(Node));\n        for (int i = 0; i < N; ++i) {\n            push(&vec[A[i]], i + 1); // positions are 1‑based\n        }\n        long long ans = 0;\n        for (int v = 1; v <= 1000000; ++v) {\n            Node *lst = &vec[v];\n            int k = lst->size;\n            if (k < v) continue;\n            // sentinel positions: p0 = 0, p_{k+1} = N+1\n            for (int i = 0; i + v - 1 < k; ++i) {\n                int leftPrev = (i == 0) ? 0 : lst->data[i-1];\n                int leftChoices = lst->data[i] - leftPrev;\n                int rightNext = (i + v == k) ? N + 1 : lst->data[i+v];\n                int rightChoices = rightNext - lst->data[i+v-1];\n                ans += (long long)v * leftChoices * rightChoices;\n            }\n        }\n        printf(\"%lld\\n\", ans);\n        // free memory\n        for (int v = 1; v <= 1000000; ++v) {\n            if (vec[v].data) free(vec[v].data);\n        }\n        free(vec);\n        free(A);\n    }\n    return 0;\n}",
      "C++": "#include <bits/stdc++.h>\nusing namespace std;\n\nint main(){\n    ios::sync_with_stdio(false);\n    cin.tie(nullptr);\n    int T; if(!(cin>>T)) return 0;\n    while(T--){\n        int N; cin>>N;\n        vector<int> A(N);\n        for(int &x: A) cin>>x;\n        const int MAXV = 1000000;\n        vector<vector<int>> pos(MAXV+1);\n        for(int i=0;i<N;++i) pos[A[i]].push_back(i+1); // 1‑based\n        long long ans=0;\n        for(int v=1; v<=MAXV; ++v){\n            auto &p = pos[v];\n            int k = p.size();\n            if(k < v) continue;\n            for(int i=0; i+v-1<k; ++i){\n                int leftPrev = (i==0)?0:p[i-1];\n                long long leftChoices = p[i] - leftPrev;\n                int rightNext = (i+v==k)? N+1 : p[i+v];\n                long long rightChoices = rightNext - p[i+v-1];\n                ans += (long long)v * leftChoices * rightChoices;\n            }\n        }\n        cout<<ans<<\"\\n\";\n    }\n    return 0;\n}",
      "Java": "import java.io.*;\nimport java.util.*;\n\npublic class Main {\n    public static void main(String[] args) throws Exception {\n        FastScanner fs = new FastScanner(System.in);\n        int T = fs.nextInt();\n        StringBuilder sb = new StringBuilder();\n        final int MAXV = 1_000_000;\n        while (T-- > 0) {\n            int N = fs.nextInt();\n            int[] A = new int[N];\n            for (int i = 0; i < N; i++) A[i] = fs.nextInt();\n            @SuppressWarnings(\"unchecked\")\n            ArrayList<Integer>[] pos = new ArrayList[MAXV + 1];\n            for (int i = 0; i < N; i++) {\n                int v = A[i];\n                if (pos[v] == null) pos[v] = new ArrayList<>();\n                pos[v].add(i + 1); // 1‑based\n            }\n            long ans = 0L;\n            for (int v = 1; v <= MAXV; v++) {\n                ArrayList<Integer> list = pos[v];\n                if (list == null) continue;\n                int k = list.size();\n                if (k < v) continue;\n                for (int i = 0; i + v - 1 < k; i++) {\n                    int leftPrev = (i == 0) ? 0 : list.get(i - 1);\n                    long leftChoices = list.get(i) - leftPrev;\n                    int rightNext = (i + v == k) ? N + 1 : list.get(i + v);\n                    long rightChoices = rightNext - list.get(i + v - 1);\n                    ans += (long) v * leftChoices * rightChoices;\n                }\n            }\n            sb.append(ans).append('\\n');\n        }\n        System.out.print(sb.toString());\n    }\n\n    // Fast scanner based on BufferedInputStream\n    static class FastScanner {\n        private final InputStream in;\n        private final byte[] buffer = new byte[1 << 16];\n        private int ptr = 0, len = 0;\n        FastScanner(InputStream in) { this.in = in; }\n        private int readByte() throws IOException {\n            if (ptr >= len) {\n                len = in.read(buffer);\n                ptr = 0;\n                if (len <= 0) return -1;\n            }\n            return buffer[ptr++];\n        }\n        int nextInt() throws IOException {\n            int c, sign = 1, val = 0;\n            do { c = readByte(); } while (c <= ' ');\n            if (c == '-') { sign = -1; c = readByte(); }\n            while (c > ' ') {\n                val = val * 10 + (c - '0');\n                c = readByte();\n            }\n            return val * sign;\n        }\n    }\n}",
      "Python3": "#!/usr/bin/env python3\nimport sys\n\ndef solve():\n    data = sys.stdin.buffer.read().split()\n    it = iter(data)\n    T = int(next(it))\n    out_lines = []\n    MAXV = 10**6\n    for _ in range(T):\n        N = int(next(it))\n        A = [int(next(it)) for _ in range(N)]\n        pos = [[] for _ in range(MAXV + 1)]\n        for idx, val in enumerate(A, 1):\n            pos[val].append(idx)\n        ans = 0\n        for v in range(1, MAXV + 1):\n            p = pos[v]\n            k = len(p)\n            if k < v:\n                continue\n            for i in range(k - v + 1):\n                left_prev = 0 if i == 0 else p[i-1]\n                left_choices = p[i] - left_prev\n                right_next = N + 1 if i + v == k else p[i+v]\n                right_choices = right_next - p[i+v-1]\n                ans += v * left_choices * right_choices\n        out_lines.append(str(ans))\n    sys.stdout.write('\\n'.join(out_lines))\n\nif __name__ == \"__main__\":\n    solve()\n",
      "JavaScript": "const fs = require('fs');\nconst input = fs.readFileSync(0, 'utf-8').trim().split(/\\s+/).map(Number);\nlet idx = 0;\nconst T = input[idx++];\nlet outputs = [];\nconst MAXV = 1000000;\nfor (let t = 0; t < T; ++t) {\n  const N = input[idx++];\n  const A = new Array(N);\n  for (let i = 0; i < N; ++i) A[i] = input[idx++];\n  // positions per value\n  const pos = Array.from({length: MAXV + 1}, () => []);\n  for (let i = 0; i < N; ++i) {\n    pos[A[i]].push(i + 1); // 1‑based\n  }\n  let ans = 0n;\n  for (let v = 1; v <= MAXV; ++v) {\n    const p = pos[v];\n    const k = p.length;\n    if (k < v) continue;\n    for (let i = 0; i + v - 1 < k; ++i) {\n      const leftPrev = (i === 0) ? 0 : p[i - 1];\n      const leftChoices = BigInt(p[i] - leftPrev);\n      const rightNext = (i + v === k) ? N + 1 : p[i + v];\n      const rightChoices = BigInt(rightNext - p[i + v - 1]);\n      ans += BigInt(v) * leftChoices * rightChoices;\n    }\n  }\n  outputs.push(ans.toString());\n}\nconsole.log(outputs.join('\\n'));\n"
    }
  },
  "QBCategory": "CCD",
  "boilerPlates": {
    "C": "#include <stdio.h>\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "C++": "#include <iostream>\nusing namespace std;\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "Java": "import java.util.*;\nimport java.io.*;\n\npublic class Main {\n    public static void main(String[] args) throws IOException {\n        // your code goes here\n    }\n}",
    "Python3": "# your code goes here\n",
    "JavaScript": "// your code goes here\n"
  },
  "_internal": {
    "methodName": "",
    "params": []
  }
}