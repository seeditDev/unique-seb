{
  "questionId": "Q8931",
  "title": "Palindrome Pain",
  "slug": "palindrome-pain",
  "version": 1,
  "createdAt": "2026-07-02",
  "updatedAt": "2026-07-04",
  "metadata": {
    "category": "Strings",
    "difficulty": "Medium",
    "isPremium": true,
    "tags": [
      "Palindromes",
      "Constructive Algorithms",
      "Greedy"
    ],
    "companyTags": [],
    "topicTags": [
      "Constructive Algorithms"
    ],
    "QBCategory": "CCD"
  },
  "content": {
    "problemStatement": "You are given two integers XXX and YYY. You need to construct two different strings S1S_1S1​ and S2S_2S2​ consisting of only the characters ‘a’‘a’‘a’ and ‘b’‘b’‘b’, such that the following conditions are satisfied:\n\n\nBoth S1S_1S1​ and S2S_2S2​ are palindromes.\nBoth S1S_1S1​ and S2S_2S2​ should contain exactly XXX occurrences of a\\texttt{a}a and YYY occurrences of b\\texttt{b}b.\nIf there are multiple possible answers, you may print **any** of them. If it is not possible to construct two distinct strings satisfying the conditions, print −1-1−1 instead.\n\n\nInput Format\n\nThe first line of input will contain a single integer TTT, denoting the number of test cases. Then the test cases follow.\nEach test case consists of a single line of input containing two space-separated integers X,YX, YX,Y.\n\n\nOutput Format\nFor each test case:\n\n\n\nIf it is not possible to construct two distinct strings satisfying the conditions, print −1-1−1\nOtherwise, print two lines — the first should contain the string S1S_1S1​ and the second should contain S2S_2S2​\nIf there are multiple possible answers, you may print any of them.",
    "constraints": [
      "1 ≤ T ≤ 1000",
      "1 ≤ X, Y ≤ 1000"
    ],
    "inputFormat": "The first line contains an integer T, the number of test cases. Each of the next T lines contains two space‑separated integers X and Y.",
    "outputFormat": "For each test case, if it is impossible to output two distinct palindromes with exactly X letters 'a' and Y letters 'b', print a single line containing -1. Otherwise print two lines: the first line is any valid palindrome S1, the second line is another valid palindrome S2 (S1 ≠ S2).",
    "sampleTestCases": [
      {
        "id": "sample_1",
        "input": "2\n2 4\n3 3",
        "output": "abbbba\\nbbaabb\\n-1",
        "explanation": "Test case 1: X=2, Y=4 are both even, so a palindrome exists. aHalf=1, bHalf=2. First half \"abb\" gives palindrome \"abbbba\". Swapping halves gives \"bbaabb\". Both satisfy the counts and are different. Test case 2: X=3, Y=3 are both odd, therefore no palindrome can be formed, so output is -1."
      }
    ]
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "label": "Case 1",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_2",
        "label": "Case 2",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_3",
        "label": "Case 3",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_4",
        "label": "Case 4",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_5",
        "label": "Case 5",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      }
    ],
    "totalTestCases": 5,
    "totalWeight": 10
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      2,
      2,
      2,
      2,
      2
    ],
    "maxScore": 10
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "A palindrome can be formed iff at most one of the character counts is odd. Therefore if both X and Y are odd the answer is -1. Even when a palindrome exists we need at least two different ones. This is impossible when one character does not appear at all (X=0 or Y=0) or when the only odd count equals 1 (X=1 or Y=1) because the centre is forced and the remaining half is forced. In all other feasible cases we can build two distinct palindromes by arranging the first half differently. Let aHalf = X/2, bHalf = Y/2 (integer division). If X is odd the centre character is 'a', else if Y is odd the centre is 'b', otherwise there is no centre. First palindrome: first half = aHalf times 'a' followed by bHalf times 'b'. Second palindrome: first half = bHalf times 'b' followed by aHalf times 'a'. Append the centre (if any) and the reverse of the first half to obtain the full strings. Because both aHalf and bHalf are positive in the feasible region, the two palindromes are guaranteed to be different.",
    "timeComplexity": "O(X+Y) per test case – we only construct strings of length X+Y.",
    "spaceComplexity": "O(X+Y) for the output strings.",
    "code": {
      "C": "#include <stdio.h>\n#include <stdlib.h>\n#include <string.h>\n\nvoid solveCase(int X, int Y) {\n    if ((X & 1) && (Y & 1)) { printf(\"-1\\n\"); return; }\n    if (X == 0 || Y == 0) { printf(\"-1\\n\"); return; }\n    if ((X & 1) && X == 1) { printf(\"-1\\n\"); return; }\n    if ((Y & 1) && Y == 1) { printf(\"-1\\n\"); return; }\n    int aHalf = X / 2;\n    int bHalf = Y / 2;\n    char centre = 0;\n    if (X & 1) centre = 'a';\n    else if (Y & 1) centre = 'b';\n    int len = X + Y;\n    char *first = (char*)malloc(aHalf + bHalf + 1);\n    char *second = (char*)malloc(aHalf + bHalf + 1);\n    int pos = 0;\n    for (int i = 0; i < aHalf; ++i) first[pos++] = 'a';\n    for (int i = 0; i < bHalf; ++i) first[pos++] = 'b';\n    first[pos] = '\\0';\n    pos = 0;\n    for (int i = 0; i < bHalf; ++i) second[pos++] = 'b';\n    for (int i = 0; i < aHalf; ++i) second[pos++] = 'a';\n    second[pos] = '\\0';\n    // build and print first palindrome\n    for (int i = 0; i < aHalf + bHalf; ++i) putchar(first[i]);\n    if (centre) putchar(centre);\n    for (int i = aHalf + bHalf - 1; i >= 0; --i) putchar(first[i]);\n    putchar('\\n');\n    // second palindrome\n    for (int i = 0; i < aHalf + bHalf; ++i) putchar(second[i]);\n    if (centre) putchar(centre);\n    for (int i = aHalf + bHalf - 1; i >= 0; --i) putchar(second[i]);\n    putchar('\\n');\n    free(first);\n    free(second);\n}\n\nint main() {\n    int T; if (scanf(\"%d\", &T)!=1) return 0;\n    while (T--) {\n        int X, Y; scanf(\"%d %d\", &X, &Y);\n        solveCase(X, Y);\n    }\n    return 0;\n}",
      "C++": "#include <bits/stdc++.h>\nusing namespace std;\n\nvoid solve(int X, int Y) {\n    if ((X & 1) && (Y & 1)) { cout << -1 << '\\n'; return; }\n    if (X == 0 || Y == 0) { cout << -1 << '\\n'; return; }\n    if ((X & 1) && X == 1) { cout << -1 << '\\n'; return; }\n    if ((Y & 1) && Y == 1) { cout << -1 << '\\n'; return; }\n    int aHalf = X / 2, bHalf = Y / 2;\n    char centre = 0;\n    if (X & 1) centre = 'a';\n    else if (Y & 1) centre = 'b';\n    string firstHalf(aHalf, 'a');\n    firstHalf.append(bHalf, 'b');\n    string secondHalf(bHalf, 'b');\n    secondHalf.append(aHalf, 'a');\n    auto build = [&](const string &fh) {\n        string res = fh;\n        if (centre) res.push_back(centre);\n        res.append(string(fh.rbegin(), fh.rend()));\n        return res;\n    };\n    cout << build(firstHalf) << '\\n';\n    cout << build(secondHalf) << '\\n';\n}\n\nint main() {\n    ios::sync_with_stdio(false);\n    cin.tie(nullptr);\n    int T; if(!(cin>>T)) return 0;\n    while(T--) {\n        int X,Y; cin>>X>>Y;\n        solve(X,Y);\n    }\n    return 0;\n}",
      "Java": "import java.io.*;\nimport java.util.*;\n\npublic class Main {\n    private static String build(String half, char centre) {\n        StringBuilder sb = new StringBuilder();\n        sb.append(half);\n        if (centre != 0) sb.append(centre);\n        sb.append(new StringBuilder(half).reverse());\n        return sb.toString();\n    }\n    private static void solve(int X, int Y, StringBuilder out) {\n        if ((X & 1) == 1 && (Y & 1) == 1) { out.append(\"-1\\n\"); return; }\n        if (X == 0 || Y == 0) { out.append(\"-1\\n\"); return; }\n        if ((X & 1) == 1 && X == 1) { out.append(\"-1\\n\"); return; }\n        if ((Y & 1) == 1 && Y == 1) { out.append(\"-1\\n\"); return; }\n        int aHalf = X / 2, bHalf = Y / 2;\n        char centre = 0;\n        if ((X & 1) == 1) centre = 'a';\n        else if ((Y & 1) == 1) centre = 'b';\n        StringBuilder firstHalf = new StringBuilder();\n        for (int i = 0; i < aHalf; ++i) firstHalf.append('a');\n        for (int i = 0; i < bHalf; ++i) firstHalf.append('b');\n        StringBuilder secondHalf = new StringBuilder();\n        for (int i = 0; i < bHalf; ++i) secondHalf.append('b');\n        for (int i = 0; i < aHalf; ++i) secondHalf.append('a');\n        out.append(build(firstHalf.toString(), centre)).append('\\n');\n        out.append(build(secondHalf.toString(), centre)).append('\\n');\n    }\n    public static void main(String[] args) throws Exception {\n        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));\n        StringBuilder out = new StringBuilder();\n        int T = Integer.parseInt(br.readLine().trim());\n        for (int t = 0; t < T; ++t) {\n            StringTokenizer st = new StringTokenizer(br.readLine());\n            int X = Integer.parseInt(st.nextToken());\n            int Y = Integer.parseInt(st.nextToken());\n            solve(X, Y, out);\n        }\n        System.out.print(out.toString());\n    }\n}",
      "Python3": "#!/usr/bin/env python3\nimport sys\n\ndef solve_case(X, Y):\n    if (X & 1) and (Y & 1):\n        return [\"-1\"]\n    if X == 0 or Y == 0:\n        return [\"-1\"]\n    if (X & 1) and X == 1:\n        return [\"-1\"]\n    if (Y & 1) and Y == 1:\n        return [\"-1\"]\n    a_half = X // 2\n    b_half = Y // 2\n    centre = ''\n    if X & 1:\n        centre = 'a'\n    elif Y & 1:\n        centre = 'b'\n    first_half = 'a' * a_half + 'b' * b_half\n    second_half = 'b' * b_half + 'a' * a_half\n    def build(half):\n        return half + centre + half[::-1]\n    return [build(first_half), build(second_half)]\n\ndef main():\n    data = sys.stdin.read().strip().split()\n    if not data:\n        return\n    it = iter(data)\n    T = int(next(it))\n    out_lines = []\n    for _ in range(T):\n        X = int(next(it)); Y = int(next(it))\n        res = solve_case(X, Y)\n        out_lines.extend(res)\n    sys.stdout.write('\\n'.join(out_lines))\n\nif __name__ == \"__main__\":\n    main()",
      "JavaScript": "const fs = require('fs');\nconst input = fs.readFileSync(0, 'utf-8').trim().split(/\\s+/);\nlet idx = 0;\nconst T = Number(input[idx++]);\nlet out = [];\nfunction solveCase(X, Y) {\n  if ((X & 1) && (Y & 1)) { out.push('-1'); return; }\n  if (X === 0 || Y === 0) { out.push('-1'); return; }\n  if ((X & 1) && X === 1) { out.push('-1'); return; }\n  if ((Y & 1) && Y === 1) { out.push('-1'); return; }\n  const aHalf = Math.floor(X / 2);\n  const bHalf = Math.floor(Y / 2);\n  let centre = '';\n  if (X & 1) centre = 'a';\n  else if (Y & 1) centre = 'b';\n  const firstHalf = 'a'.repeat(aHalf) + 'b'.repeat(bHalf);\n  const secondHalf = 'b'.repeat(bHalf) + 'a'.repeat(aHalf);\n  const build = (half) => half + centre + half.split('').reverse().join('');\n  out.push(build(firstHalf));\n  out.push(build(secondHalf));\n}\nfor (let t = 0; t < T; ++t) {\n  const X = Number(input[idx++]);\n  const Y = Number(input[idx++]);\n  solveCase(X, Y);\n}\nconsole.log(out.join('\\n'));\n"
    }
  },
  "QBCategory": "CCD",
  "boilerPlates": {
    "C": "#include <stdio.h>\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "C++": "#include <iostream>\nusing namespace std;\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "Java": "import java.util.*;\nimport java.io.*;\n\npublic class Main {\n    public static void main(String[] args) throws IOException {\n        // your code goes here\n    }\n}",
    "Python3": "# your code goes here\n",
    "JavaScript": "// your code goes here\n"
  },
  "_internal": {
    "methodName": "",
    "params": []
  }
}