{
  "questionId": "Q5319",
  "title": "Internet Media Types",
  "slug": "internet-media-types",
  "version": 1,
  "createdAt": "2026-07-01",
  "updatedAt": "2026-07-04",
  "metadata": {
    "category": "Strings",
    "difficulty": "Easy",
    "isPremium": true,
    "tags": [
      "Hash Table",
      "String Manipulation"
    ],
    "companyTags": [],
    "topicTags": [
      "Data Structures"
    ],
    "QBCategory": "CCD"
  },
  "content": {
    "problemStatement": "Many internet protocols these days include the option of associating a\nmedia type with the content being sent.\nThe type is usually inferred from the file extension.\nYou are to write a program that facilitates the lookup of media types for\na number of files.\n\nYou will be given a table of media type associations that associate a certain\nfile extension with a certain media type.\nYou will then be given a number of file names, and tasked to determine the correct\nmedia type for each file.\nA file extension is defined as the part of the file name after the final period.\nIf a file name has no periods, then it has no extension and the media type cannot\nbe determined.\nIf the file extension is not present in the table, then the media type cannot be\ndetermined.\nIn such cases you will print \"unknown\" as the media type.\nIf the file extension does appear in the table (case matters), then print the associated\nmedia type.",
    "constraints": [
      "1 <= T <= 10",
      "0 <= N, Q <= 100 per test case",
      "File extensions consist only of letters and digits, length <= 10, and are distinct within a test case.",
      "Media types consist of printable characters without spaces, length <= 50.",
      "File names consist of letters, digits and periods, length <= 50."
    ],
    "inputFormat": "First line contains T, the number of test cases. For each test case: a line with two integers N and Q, where N is the number of known file-extension to media-type associations and Q is the number of file names to query. N following lines each contain a file extension and its media type, separated by a single space. Q following lines each contain a file name.",
    "outputFormat": "For each of the Q file names of each test case, output on its own line the associated media type. If the file has no extension, the extension is not present in the table, or the lookup is case-sensitive and fails, output 'unknown'.",
    "sampleTestCases": [
      {
        "id": "sample_1",
        "input": "1\n5 6\nhtml text/html\nhtm text/html\npng image/png\nsvg image/svg+xml\ntxt text/plain\nindex.html\nthis.file.has.lots.of.dots.txt\nnodotsatall\nvirus.exe\ndont.let.the.png.fool.you\ncase.matters.TXT",
        "output": "text/html\ntext/plain\nunknown\nunknown\nunknown\nunknown",
        "explanation": "The table maps 'html' and 'txt' to their types. Extensions are extracted after the last period. Case matters, so 'TXT' does not match 'txt'."
      },
      {
        "id": "sample_2",
        "input": "1\n2 2\njpg image/jpeg\npng image/png\nimage.jpg\nimage.png",
        "output": "image/jpeg\nimage/png",
        "explanation": "The table maps 'jpg' and 'png' to their types. Extensions are extracted after the last period."
      },
      {
        "id": "sample_3",
        "input": "1\n0 1\nnoextension",
        "output": "unknown",
        "explanation": "The file has no extension, so the media type cannot be determined."
      }
    ]
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "label": "minimum boundary",
        "input": "1\n0 0",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_2",
        "label": "maximum boundary",
        "input": "1\n100 100\nhtml text/html\nhtm text/html\npng image/png\nsvg image/svg+xml\ntxt text/plain\nindex.html\nthis.file.has.lots.of.dots.txt\nnodotsatall\nvirus.exe\ndont.let.the.png.fool.you\ncase.matters.TXT",
        "expectedOutput": "text/html\ntext/plain\nunknown\nunknown\nunknown\nunknown",
        "weight": 2
      },
      {
        "id": "tc_3",
        "label": "uniform/edge case",
        "input": "1\n1 1\njpg image/jpeg\nimage.jpg",
        "expectedOutput": "image/jpeg",
        "weight": 2
      },
      {
        "id": "tc_4",
        "label": "tricky case",
        "input": "1\n1 1\njpg image/jpeg\nimage.JPG",
        "expectedOutput": "unknown",
        "weight": 2
      },
      {
        "id": "tc_5",
        "label": "stress/general",
        "input": "2\n2 2\njpg image/jpeg\npng image/png\nimage.jpg\nimage.png\n2 2\nhtml text/html\nhtm text/html\nindex.html\nthis.file.has.lots.of.dots.htm",
        "expectedOutput": "image/jpeg\nimage/png\ntext/html\nunknown",
        "weight": 2
      }
    ],
    "totalTestCases": 5,
    "totalWeight": 10
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      2,
      2,
      2,
      2,
      2
    ],
    "maxScore": 10
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "The problem can be solved by using a hash table to store the file extensions and their corresponding media types. For each file name, we extract the file extension and look it up in the hash table. If the extension is found, we print the corresponding media type; otherwise, we print 'unknown'.",
    "timeComplexity": "O(T * (N + Q)) per test case, O(T * (N + Q)) total. The hash table operations (insertion and lookup) take constant time on average.",
    "spaceComplexity": "O(N) — the space used by the hash table to store the file extensions and their corresponding media types.",
    "code": {
      "C": "#include <stdio.h>\n#include <string.h>\n#include <stdlib.h>\n\n#define MAX_EXT 11\n#define MAX_TYPE 51\n#define MAX_NAME 51\n\ntypedef struct Node { char ext[MAX_EXT]; char type[MAX_TYPE]; struct Node* next; } Node;\n\nunsigned long hash(const char *s) {\n    unsigned long h = 5381;\n    for (; *s; ++s) h = ((h << 5) + h) + (unsigned char)*s;\n    return h;\n}\n\nint main() {\n    int T; if (scanf(\"%d\", &T)!=1) return 0;\n    while (T--) {\n        int N, Q; scanf(\"%d %d\", &N, &Q);\n        // simple hash table with chaining, size enough for N<=100\n        const int SZ = 211;\n        Node* table[SZ]; memset(table, 0, sizeof(table));\n        for (int i=0;i<N;++i) {\n            char ext[MAX_EXT]; char type[MAX_TYPE];\n            scanf(\"%s %s\", ext, type);\n            unsigned long h = hash(ext) % SZ;\n            Node* n = (Node*)malloc(sizeof(Node));\n            strcpy(n->ext, ext); strcpy(n->type, type); n->next = table[h];\n            table[h] = n;\n        }\n        char name[MAX_NAME];\n        for (int i=0;i<Q;++i) {\n            scanf(\"%s\", name);\n            char *p = strrchr(name, '.');\n            if (!p || *(p+1)=='\\0') {\n                printf(\"unknown\\n\");\n                continue;\n            }\n            char *ext = p+1;\n            unsigned long h = hash(ext) % SZ;\n            Node* cur = table[h];\n            while (cur && strcmp(cur->ext, ext)!=0) cur = cur->next;\n            if (cur) printf(\"%s\\n\", cur->type);\n            else printf(\"unknown\\n\");\n        }\n        // free memory\n        for (int i=0;i<SZ;++i) {\n            Node* cur = table[i];\n            while (cur) { Node* tmp = cur; cur = cur->next; free(tmp); }\n        }\n    }\n    return 0;\n}",
      "C++": "#include <bits/stdc++.h>\nusing namespace std;\nint main(){\n    ios::sync_with_stdio(false);\n    cin.tie(nullptr);\n    int T; if(!(cin>>T)) return 0;\n    while(T--){\n        int N,Q; cin>>N>>Q;\n        unordered_map<string,string> mp;\n        mp.reserve(N*2);\n        for(int i=0;i<N;++i){\n            string ext, typ; cin>>ext>>typ; mp[ext]=typ;\n        }\n        string name;\n        for(int i=0;i<Q;++i){\n            cin>>name;\n            size_t pos = name.rfind('.');\n            if(pos==string::npos || pos+1==name.size()){\n                cout<<\"unknown\\n\";\n                continue;\n            }\n            string ext = name.substr(pos+1);\n            auto it = mp.find(ext);\n            if(it!=mp.end()) cout<<it->second<<\"\\n\";\n            else cout<<\"unknown\\n\";\n        }\n    }\n    return 0;\n}",
      "Java": "import java.io.*;\nimport java.util.*;\npublic class Main {\n    public static void main(String[] args) throws Exception {\n        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));\n        StringTokenizer st = new StringTokenizer(br.readLine());\n        int T = Integer.parseInt(st.nextToken());\n        StringBuilder out = new StringBuilder();\n        while (T-- > 0) {\n            st = new StringTokenizer(br.readLine());\n            int N = Integer.parseInt(st.nextToken());\n            int Q = Integer.parseInt(st.nextToken());\n            HashMap<String,String> map = new HashMap<>(N*2);\n            for (int i = 0; i < N; i++) {\n                st = new StringTokenizer(br.readLine());\n                String ext = st.nextToken();\n                String typ = st.nextToken();\n                map.put(ext, typ);\n            }\n            for (int i = 0; i < Q; i++) {\n                String name = br.readLine();\n                int idx = name.lastIndexOf('.');\n                if (idx == -1 || idx == name.length() - 1) {\n                    out.append(\"unknown\\n\");\n                } else {\n                    String ext = name.substring(idx + 1);\n                    String typ = map.get(ext);\n                    out.append(typ != null ? typ : \"unknown\").append('\\n');\n                }\n            }\n        }\n        System.out.print(out.toString());\n    }\n}",
      "Python3": "#!/usr/bin/env python3\nimport sys\ndef solve():\n    data = sys.stdin.read().splitlines()\n    if not data:\n        return\n    it = iter(data)\n    T = int(next(it))\n    out_lines = []\n    for _ in range(T):\n        N_Q = next(it).split()\n        while len(N_Q) < 2:  # handle possible empty lines\n            N_Q = next(it).split()\n        N, Q = map(int, N_Q)\n        mp = {}\n        for _ in range(N):\n            ext, typ = next(it).split()\n            mp[ext] = typ\n        for _ in range(Q):\n            name = next(it)\n            pos = name.rfind('.')\n            if pos == -1 or pos == len(name) - 1:\n                out_lines.append('unknown')\n            else:\n                ext = name[pos+1:]\n                out_lines.append(mp.get(ext, 'unknown'))\n    sys.stdout.write('\\n'.join(out_lines))\nif __name__ == \"__main__\":\n    solve()\n",
      "JavaScript": "const lines = require('fs').readFileSync(0, 'utf-8').trim().split('\\n');\nlet idx = 0;\nconst T = parseInt(lines[idx++], 10);\nlet output = [];\nfor (let t = 0; t < T; t++) {\n  const [Nstr, Qstr] = lines[idx++].trim().split(/\\s+/);\n  const N = parseInt(Nstr, 10);\n  const Q = parseInt(Qstr, 10);\n  const map = new Map();\n  for (let i = 0; i < N; i++) {\n    const [ext, typ] = lines[idx++].trim().split(/\\s+/);\n    map.set(ext, typ);\n  }\n  for (let i = 0; i < Q; i++) {\n    const name = lines[idx++].trim();\n    const pos = name.lastIndexOf('.');\n    if (pos === -1 || pos === name.length - 1) {\n      output.push('unknown');\n    } else {\n      const ext = name.substring(pos + 1);\n      output.push(map.has(ext) ? map.get(ext) : 'unknown');\n    }\n  }\n}\nconsole.log(output.join('\\n'));"
    }
  },
  "QBCategory": "CCD",
  "boilerPlates": {
    "C": "#include <stdio.h>\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "C++": "#include <iostream>\nusing namespace std;\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "Java": "import java.util.*;\nimport java.io.*;\n\npublic class Main {\n    public static void main(String[] args) throws IOException {\n        // your code goes here\n    }\n}",
    "Python3": "# your code goes here\n",
    "JavaScript": "// your code goes here\n"
  },
  "_internal": {
    "methodName": "",
    "params": []
  }
}