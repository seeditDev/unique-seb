{
  "questionId": "Q4316",
  "title": "LCA in the BST",
  "slug": "lca-in-the-bst",
  "version": 1,
  "createdAt": "2026-07-01",
  "updatedAt": "2026-07-04",
  "metadata": {
    "category": "Graph",
    "difficulty": "Medium",
    "isPremium": true,
    "tags": [
      "Lowest Common Ancestor",
      "Binary Search Tree"
    ],
    "companyTags": [],
    "topicTags": [
      "Trees"
    ],
    "QBCategory": "CCD"
  },
  "content": {
    "problemStatement": "Given a binary search tree, and two nodes values XXX and YYY, find the LCA (lowest common ancestor) of these two nodes.\n\nFor eg., in the following BST, the LCA of nodes with values 3 and 5 is the root node 4:\n\n\n\n\nInput Format\n\nThe first line of the input contains three space separated integers NNN, XXX and YYY — the number of nodes in the binary search tree, and the two given nodes.\nNext N−1N - 1N−1 lines contain three space separated characters pi,ci,Rip_i, c_i, R_ipi​,ci​,Ri​, describing the tree with edge informations - pip_ipi​ is an integer denoting the parent node of the iiith edge, cic_ici​ is child node, and the letter RiR_iRi​ denotes whether the child is left child or right child, it's value is `L` if cic_ici​ is left child of pip_ipi​, else `R`.\n\n\nOutput Format\nOutput on the single line, the LCA of node XXX and YYY.",
    "constraints": [
      "1 ≤ N ≤ 10000",
      "1 ≤ pi, ci ≤ 100000",
      "Ri = 'L' or 'R'",
      "All pi's and ci's are distinct (same pairs are not repeated).",
      "1 ≤ X, Y ≤ 100000"
    ],
    "inputFormat": "The first line contains three space-separated integers N, X, and Y — the number of nodes in the binary search tree, and the two given nodes. Next N-1 lines contain three space-separated characters pi, ci, Ri, describing the tree with edge informations - pi is an integer denoting the parent node of the iith edge, ci is child node, and the letter Ri denotes whether the child is left child or right child, it's value is 'L' if ci is left child of pi, else 'R'.",
    "outputFormat": "Output on the single line, the LCA of node X and Y.",
    "sampleTestCases": [
      {
        "id": "sample_1",
        "input": "17 22 3\n3 1 L\n6 3 L\n6 21 R\n22 6 L\n22 31 R\n31 23 L\n31 32 R\n51 22 L\n51 94 R\n66 63 L\n73 66 L\n73 93 R\n94 73 L\n94 97 R\n97 95 L\n97 99 R",
        "output": "6",
        "explanation": "The LCA of nodes 22 and 3 is node 6."
      },
      {
        "id": "sample_2",
        "input": "5 2 4\n1 2 L\n1 3 R\n2 4 L\n3 5 R",
        "output": "1",
        "explanation": "The LCA of nodes 2 and 4 is node 1."
      },
      {
        "id": "sample_3",
        "input": "3 1 2\n1 2 L\n2 3 R",
        "output": "1",
        "explanation": "The LCA of nodes 1 and 2 is node 1."
      }
    ]
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "label": "minimum boundary",
        "input": "1 1 1\n",
        "expectedOutput": "1",
        "weight": 2
      },
      {
        "id": "tc_2",
        "label": "maximum boundary",
        "input": "10000 10000 9999\n",
        "expectedOutput": "9999",
        "weight": 2
      },
      {
        "id": "tc_3",
        "label": "uniform/edge case",
        "input": "5 5 5\n1 2 L\n1 3 R\n2 4 L\n3 5 R",
        "expectedOutput": "5",
        "weight": 2
      },
      {
        "id": "tc_4",
        "label": "tricky case",
        "input": "5 1 5\n1 2 L\n1 3 R\n2 4 L\n3 5 R",
        "expectedOutput": "1",
        "weight": 2
      },
      {
        "id": "tc_5",
        "label": "stress/general",
        "input": "10 5 8\n1 2 L\n1 3 R\n2 4 L\n3 5 R\n4 6 L\n5 7 L\n6 8 R\n7 9 R\n8 10 R",
        "expectedOutput": "5",
        "weight": 2
      }
    ],
    "totalTestCases": 5,
    "totalWeight": 10
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      2,
      2,
      2,
      2,
      2
    ],
    "maxScore": 10
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "To find the LCA of two nodes in a binary search tree, we can use a recursive approach. We start at the root of the tree and compare the values of the two nodes with the current node. If both nodes have values less than the current node, we move to the left subtree. If both nodes have values greater than the current node, we move to the right subtree. If one node has a value less than the current node and the other has a value greater, the current node is the LCA.",
    "timeComplexity": "O(N) per test case, where N is the number of nodes in the tree. The space complexity is O(H), where H is the height of the tree, due to the recursive call stack.",
    "spaceComplexity": "O(H) — the dominant structure is the recursive call stack.",
    "code": {
      "C": "#include <stdio.h>\n#include <stdlib.h>\n#include <string.h>\n#define MAX 10000\n\n// Define the structure for a tree node\ntypedef struct Node {\n    int val;\n    struct Node* left;\n    struct Node* right;\n} Node;\n\n// Function to create a new node\nNode* createNode(int val) {\n    Node* newNode = (Node*)malloc(sizeof(Node));\n    newNode->val = val;\n    newNode->left = newNode->right = NULL;\n    return newNode;\n}\n\n// Function to find the LCA of two nodes\nNode* findLCA(Node* root, int X, int Y) {\n    if (root == NULL) return NULL;\n    if (root->val > X && root->val > Y) return findLCA(root->left, X, Y);\n    if (root->val < X && root->val < Y) return findLCA(root->right, X, Y);\n    return root;\n}\n\nint main() {\n    int N, X, Y;\n    scanf(\"%d %d %d\", &N, &X, &Y);\n    Node* root = NULL;\n    Node* nodes[MAX];\n    for (int i = 0; i < N; i++) {\n        nodes[i] = createNode(i+1);\n    }\n    for (int i = 0; i < N - 1; i++) {\n        int pi, ci;\n        char Ri;\n        scanf(\"%d %d %c\", &pi, &ci, &Ri);\n        if (Ri == 'L') {\n            nodes[pi-1]->left = nodes[ci-1];\n        } else {\n            nodes[pi-1]->right = nodes[ci-1];\n        }\n    }\n    root = nodes[0];\n    Node* lca = findLCA(root, X, Y);\n    printf(\"%d\\n\", lca->val);\n    return 0;\n}",
      "C++": "#include <iostream>\nusing namespace std;\n\n// Define the structure for a tree node\nstruct Node {\n    int val;\n    Node* left;\n    Node* right;\n    Node(int node_value) {\n        val = node_value;\n        left = right = NULL;\n    }\n};\n\n// Function to find the LCA of two nodes\nNode* findLCA(Node* root, int X, int Y) {\n    if (root == NULL) return NULL;\n    if (root->val > X && root->val > Y) return findLCA(root->left, X, Y);\n    if (root->val < X && root->val < Y) return findLCA(root->right, X, Y);\n    return root;\n}\n\nint main() {\n    int N, X, Y;\n    cin >> N >> X >> Y;\n    Node* root = NULL;\n    Node* nodes[N];\n    for (int i = 0; i < N; i++) {\n        nodes[i] = new Node(i+1);\n    }\n    for (int i = 0; i < N - 1; i++) {\n        int pi, ci;\n        char Ri;\n        cin >> pi >> ci >> Ri;\n        if (Ri == 'L') {\n            nodes[pi-1]->left = nodes[ci-1];\n        } else {\n            nodes[pi-1]->right = nodes[ci-1];\n        }\n    }\n    root = nodes[0];\n    Node* lca = findLCA(root, X, Y);\n    cout << lca->val << endl;\n    return 0;\n}",
      "Java": "import java.util.Scanner;\npublic class Main {\n    static class Node {\n        int val;\n        Node left;\n        Node right;\n        Node(int node_value) {\n            val = node_value;\n            left = right = null;\n        }\n    }\n    public static Node findLCA(Node root, int X, int Y) {\n        if (root == null) return null;\n        if (root.val > X && root.val > Y) return findLCA(root.left, X, Y);\n        if (root.val < X && root.val < Y) return findLCA(root.right, X, Y);\n        return root;\n    }\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int N = sc.nextInt();\n        int X = sc.nextInt();\n        int Y = sc.nextInt();\n        Node root = null;\n        Node[] nodes = new Node[N];\n        for (int i = 0; i < N; i++) {\n            nodes[i] = new Node(i+1);\n        }\n        for (int i = 0; i < N - 1; i++) {\n            int pi = sc.nextInt();\n            int ci = sc.nextInt();\n            char Ri = sc.next().charAt(0);\n            if (Ri == 'L') {\n                nodes[pi-1].left = nodes[ci-1];\n            } else {\n                nodes[pi-1].right = nodes[ci-1];\n            }\n        }\n        root = nodes[0];\n        Node lca = findLCA(root, X, Y);\n        System.out.println(lca.val);\n    }\n}",
      "Python3": "class Node:\n    def __init__(self, val):\n        self.val = val\n        self.left = None\n        self.right = None\n\ndef findLCA(root, X, Y):\n    if root is None: return None\n    if root.val > X and root.val > Y: return findLCA(root.left, X, Y)\n    if root.val < X and root.val < Y: return findLCA(root.right, X, Y)\n    return root\n\ndef main():\n    N, X, Y = map(int, input().split())\n    nodes = [Node(i+1) for i in range(N)]\n    for _ in range(N - 1):\n        pi, ci, Ri = input().split()\n        pi, ci = int(pi), int(ci)\n        if Ri == 'L':\n            nodes[pi-1].left = nodes[ci-1]\n        else:\n            nodes[pi-1].right = nodes[ci-1]\n    root = nodes[0]\n    lca = findLCA(root, X, Y)\n    print(lca.val)\nif __name__ == '__main__':\n    main()",
      "JavaScript": "const fs = require('fs');\nconst lines = fs.readFileSync(0, 'utf-8').trim().split('\\n');\nlet i = 0;\nfunction readline() {\n    return lines[i++];\n}\n\nclass Node {\n    constructor(val) {\n        this.val = val;\n        this.left = null;\n        this.right = null;\n    }\n}\n\nfunction findLCA(root, X, Y) {\n    if (root === null) return null;\n    if (root.val > X && root.val > Y) return findLCA(root.left, X, Y);\n    if (root.val < X && root.val < Y) return findLCA(root.right, X, Y);\n    return root;\n}\n\nfunction main() {\n    let [N, X, Y] = readline().split(' ').map(Number);\n    let nodes = Array(N).fill(0).map((_, i) => new Node(i+1));\n    for (let i = 0; i < N - 1; i++) {\n        let [pi, ci, Ri] = readline().split(' ');\n        pi = parseInt(pi);\n        ci = parseInt(ci);\n        if (Ri === 'L') {\n            nodes[pi-1].left = nodes[ci-1];\n        } else {\n            nodes[pi-1].right = nodes[ci-1];\n        }\n    }\n    let root = nodes[0];\n    let lca = findLCA(root, X, Y);\n    console.log(lca.val);\n}\nmain();"
    }
  },
  "QBCategory": "CCD",
  "boilerPlates": {
    "C": "#include <stdio.h>\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "C++": "#include <iostream>\nusing namespace std;\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "Java": "import java.util.*;\nimport java.io.*;\n\npublic class Main {\n    public static void main(String[] args) throws IOException {\n        // your code goes here\n    }\n}",
    "Python3": "# your code goes here\n",
    "JavaScript": "// your code goes here\n"
  },
  "_internal": {
    "methodName": "",
    "params": []
  }
}