{
  "questionId": "Q2783",
  "title": "Chef and Strange Graph",
  "slug": "chef-and-strange-graph",
  "version": 1,
  "createdAt": "2026-07-01",
  "updatedAt": "2026-07-04",
  "metadata": {
    "category": "Graphs",
    "difficulty": "Hard",
    "isPremium": false,
    "tags": [
      "Graph Reconstruction",
      "Graphs",
      "Union-Find"
    ],
    "companyTags": [],
    "topicTags": [
      "Constructive Algorithms"
    ]
  },
  "content": {
    "problemStatement": "Read problems statements in Russian hereProblem StatementChef has a connected undirected graph **G** without multiedges and self-loops. A graph is called connected if there exists a path between any two vertices of the graph. This graph, **G** has an awesome property: it contains at least one vertex with degree at least 4. \n One day Chef was playing with his graph. He constructed a graph **L(G)** based on the graph **G**.\n\nThe vertices of graph **L(G)** are the edges of graph **G**. Two vertices in the graph **L(G)** are connected with an undirected edge if and only if the\n\ncorresponding edges in graph **G** share a common vertex.\n\n\nToday Chef has lost his favorite graph **G**. But he has the graph **L(G)**. Please help Chef, restore the graph **G** from the graph **L(G)**.",
    "constraints": [
      "1 \u2264 T \u2264 5000",
      "1 \u2264 N \u2264 5000",
      "1 \u2264 M \u2264 5000",
      "Given graph L(G) is connected and doesn't contain multiedges and self-loops.",
      "It's guaranteed that sum of all M values for all test cases doesn't exceed 5000."
    ],
    "inputFormat": "The first line contains an integer T, the number of test cases. Each test case contains two integers N and M, the number of vertices in L(G) and the number of edges in it. The next M lines contain description of the edges. Each line contains two integers Ai, Bi - indices of vertices, connecting by the current edge.",
    "outputFormat": "For each test case, print the number of vertices in the graph G. The next K lines should contain the description of the edges of the graph G (the i-th edge of G should correspond to the i-th vertex of L(G)).",
    "sampleTestCases": [
      {
        "id": "sample_1",
        "input": "2\n4 6\n1 2\n2 3\n3 4\n4 1\n1 3\n2 4\n5 8\n1 2\n2 3\n3 4\n4 1\n1 3\n2 4\n1 5\n2 5",
        "output": "5\n2 1\n1 3\n1 4\n1 5\n5\n1 2\n1 3\n1 4\n1 5\n2 3",
        "explanation": "The first test case has 4 vertices in L(G) and 6 edges. The reconstructed graph G has 5 vertices. The second test case has 5 vertices in L(G) and 8 edges. The reconstructed graph G has 5 vertices."
      },
      {
        "id": "sample_2",
        "input": "1\n3 3\n1 2\n2 3\n3 1",
        "output": "3\n1 2\n2 3\n3 1",
        "explanation": "The test case has 3 vertices in L(G) and 3 edges. The reconstructed graph G has 3 vertices."
      },
      {
        "id": "sample_3",
        "input": "1\n5 7\n1 2\n2 3\n3 4\n4 1\n1 3\n2 4\n1 5",
        "output": "5\n1 2\n2 3\n3 4\n4 1\n1 5",
        "explanation": "The test case has 5 vertices in L(G) and 7 edges. The reconstructed graph G has 5 vertices."
      }
    ]
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "label": "minimum boundary",
        "input": "1\n1 0",
        "expectedOutput": "0",
        "weight": 2
      },
      {
        "id": "tc_2",
        "label": "maximum boundary",
        "input": "1\n5000 5000",
        "expectedOutput": "5000",
        "weight": 2
      },
      {
        "id": "tc_3",
        "label": "uniform/edge case",
        "input": "1\n3 3\n1 2\n2 3\n3 1",
        "expectedOutput": "3\n1 2\n2 3\n3 1",
        "weight": 2
      },
      {
        "id": "tc_4",
        "label": "Case 4",
        "input": "1\n1 0",
        "expectedOutput": "0",
        "weight": 2
      },
      {
        "id": "tc_5",
        "label": "Case 5",
        "input": "1\n1 0",
        "expectedOutput": "0",
        "weight": 2
      }
    ],
    "totalTestCases": 5,
    "totalWeight": 10
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      2,
      2,
      2,
      2,
      2
    ],
    "maxScore": 10
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "The problem can be solved by using a union-find data structure to keep track of the connected components in the graph L(G). We iterate over the edges of L(G) and for each edge, we check if the two vertices are in the same connected component. If they are not, we add an edge to the graph G and union the two components. The number of vertices in the graph G is equal to the number of connected components in the graph L(G).",
    "timeComplexity": "O(T * (N + M * alpha(N))) per test case, where alpha(N) is the inverse Ackermann function, which grows very slowly. The total time complexity is O(T * (N + M * alpha(N))).",
    "spaceComplexity": "O(N) \u2014 for the union-find data structure and the graph G.",
    "code": {
      "C": "// COMPLETE, COMPILABLE C code\n// Uses correct I/O\n// Handles all T test cases\n#include <stdio.h>\n#include <stdlib.h>\n#define MAXN 5000\n#define MAXM 5000\n\n// Union-find data structure\nint parent[MAXN];\nint rank[MAXN];\n\n// Function to find the parent of a vertex\nint find(int x) {\n    if (parent[x] != x) {\n        parent[x] = find(parent[x]);\n    }\n    return parent[x];\n}\n\n// Function to union two vertices\nvoid unionSet(int x, int y) {\n    int rootX = find(x);\n    int rootY = find(y);\n    if (rootX != rootY) {\n        if (rank[rootX] > rank[rootY]) {\n            parent[rootY] = rootX;\n        } else if (rank[rootX] < rank[rootY]) {\n            parent[rootX] = rootY;\n        } else {\n            parent[rootY] = rootX;\n            rank[rootX]++;\n        }\n    }\n}\n\nint main() {\n    int T;\n    scanf(\"%d\", &T);\n    while (T--) {\n        int N, M;\n        scanf(\"%d %d\", &N, &M);\n        // Initialize the union-find data structure\n        for (int i = 0; i < N; i++) {\n            parent[i] = i;\n            rank[i] = 0;\n        }\n        // Read the edges of L(G)\n        int edges[M][2];\n        for (int i = 0; i < M; i++) {\n            scanf(\"%d %d\", &edges[i][0], &edges[i][1]);\n            edges[i][0]--;\n            edges[i][1]--;\n        }\n        // Reconstruct the graph G\n        int K = 0;\n        int graph[N][2];\n        for (int i = 0; i < N; i++) {\n            int x = edges[i][0];\n            int y = edges[i][1];\n            int rootX = find(x);\n            int rootY = find(y);\n            if (rootX != rootY) {\n                graph[K][0] = rootX;\n                graph[K][1] = rootY;\n                K++;\n                unionSet(x, y);\n            }\n        }\n        printf(\"%d\\n\", K);\n        for (int i = 0; i < K; i++) {\n            printf(\"%d %d\\n\", graph[i][0] + 1, graph[i][1] + 1);\n        }\n    }\n    return 0;\n}",
      "C++": "// COMPLETE, COMPILABLE C++ code\n// Uses correct I/O\n// Handles all T test cases\n#include <iostream>\n#include <vector>\nusing namespace std;\n\nconst int MAXN = 5000;\nconst int MAXM = 5000;\n\n// Union-find data structure\nint parent[MAXN];\nint rank[MAXN];\n\n// Function to find the parent of a vertex\nint find(int x) {\n    if (parent[x] != x) {\n        parent[x] = find(parent[x]);\n    }\n    return parent[x];\n}\n\n// Function to union two vertices\nvoid unionSet(int x, int y) {\n    int rootX = find(x);\n    int rootY = find(y);\n    if (rootX != rootY) {\n        if (rank[rootX] > rank[rootY]) {\n            parent[rootY] = rootX;\n        } else if (rank[rootX] < rank[rootY]) {\n            parent[rootX] = rootY;\n        } else {\n            parent[rootY] = rootX;\n            rank[rootX]++;\n        }\n    }\n}\n\nint main() {\n    int T;\n    cin >> T;\n    while (T--) {\n        int N, M;\n        cin >> N >> M;\n        // Initialize the union-find data structure\n        for (int i = 0; i < N; i++) {\n            parent[i] = i;\n            rank[i] = 0;\n        }\n        // Read the edges of L(G)\n        vector<pair<int, int>> edges(M);\n        for (int i = 0; i < M; i++) {\n            cin >> edges[i].first >> edges[i].second;\n            edges[i].first--;\n            edges[i].second--;\n        }\n        // Reconstruct the graph G\n        int K = 0;\n        vector<pair<int, int>> graph(N);\n        for (int i = 0; i < N; i++) {\n            int x = edges[i].first;\n            int y = edges[i].second;\n            int rootX = find(x);\n            int rootY = find(y);\n            if (rootX != rootY) {\n                graph[K].first = rootX;\n                graph[K].second = rootY;\n                K++;\n                unionSet(x, y);\n            }\n        }\n        cout << K << endl;\n        for (int i = 0; i < K; i++) {\n            cout << graph[i].first + 1 << \" \" << graph[i].second + 1 << endl;\n        }\n    }\n    return 0;\n}",
      "Java": "// COMPLETE, COMPILABLE Java code\n// Class MUST be named Main\n// Uses correct I/O\n// Handles all T test cases\nimport java.util.Scanner;\npublic class Main {\n    static final int MAXN = 5000;\n    static final int MAXM = 5000;\n    // Union-find data structure\n    static int[] parent = new int[MAXN];\n    static int[] rank = new int[MAXN];\n\n    // Function to find the parent of a vertex\n    static int find(int x) {\n        if (parent[x] != x) {\n            parent[x] = find(parent[x]);\n        }\n        return parent[x];\n    }\n\n    // Function to union two vertices\n    static void unionSet(int x, int y) {\n        int rootX = find(x);\n        int rootY = find(y);\n        if (rootX != rootY) {\n            if (rank[rootX] > rank[rootY]) {\n                parent[rootY] = rootX;\n            } else if (rank[rootX] < rank[rootY]) {\n                parent[rootX] = rootY;\n            } else {\n                parent[rootY] = rootX;\n                rank[rootX]++;\n            }\n        }\n    }\n\n    public static void main(String[] args) {\n        Scanner scanner = new Scanner(System.in);\n        int T = scanner.nextInt();\n        while (T-- > 0) {\n            int N = scanner.nextInt();\n            int M = scanner.nextInt();\n            // Initialize the union-find data structure\n            for (int i = 0; i < N; i++) {\n                parent[i] = i;\n                rank[i] = 0;\n            }\n            // Read the edges of L(G)\n            int[][] edges = new int[M][2];\n            for (int i = 0; i < M; i++) {\n                edges[i][0] = scanner.nextInt() - 1;\n                edges[i][1] = scanner.nextInt() - 1;\n            }\n            // Reconstruct the graph G\n            int K = 0;\n            int[][] graph = new int[N][2];\n            for (int i = 0; i < N; i++) {\n                int x = edges[i][0];\n                int y = edges[i][1];\n                int rootX = find(x);\n                int rootY = find(y);\n                if (rootX != rootY) {\n                    graph[K][0] = rootX;\n                    graph[K][1] = rootY;\n                    K++;\n                    unionSet(x, y);\n                }\n            }\n            System.out.println(K);\n            for (int i = 0; i < K; i++) {\n                System.out.println((graph[i][0] + 1) + \" \" + (graph[i][1] + 1));\n            }\n        }\n    }\n}",
      "Python3": "# COMPLETE, RUNNABLE Python3 code\n# Uses sys.stdin.readline() for speed\n# Handles all T test cases\nimport sys\ninput = sys.stdin.readline\n\nMAXN = 5000\nMAXM = 5000\n\n# Union-find data structure\nparent = [0] * MAXN\nrank = [0] * MAXN\n\n# Function to find the parent of a vertex\ndef find(x):\n    if parent[x] != x:\n        parent[x] = find(parent[x])\n    return parent[x]\n\n# Function to union two vertices\ndef unionSet(x, y):\n    rootX = find(x)\n    rootY = find(y)\n    if rootX != rootY:\n        if rank[rootX] > rank[rootY]:\n            parent[rootY] = rootX\n        elif rank[rootX] < rank[rootY]:\n            parent[rootX] = rootY\n        else:\n            parent[rootY] = rootX\n            rank[rootX] += 1\n\nT = int(input())\nwhile T > 0:\n    N, M = map(int, input().split())\n    # Initialize the union-find data structure\n    for i in range(N):\n        parent[i] = i\n        rank[i] = 0\n    # Read the edges of L(G)\n    edges = [list(map(int, input().split())) for _ in range(M)]\n    for i in range(M):\n        edges[i][0] -= 1\n        edges[i][1] -= 1\n    # Reconstruct the graph G\n    K = 0\n    graph = [[0, 0] for _ in range(N)]\n    for i in range(N):\n        x = edges[i][0]\n        y = edges[i][1]\n        rootX = find(x)\n        rootY = find(y)\n        if rootX != rootY:\n            graph[K][0] = rootX\n            graph[K][1] = rootY\n            K += 1\n            unionSet(x, y)\n    print(K)\n    for i in range(K):\n        print(graph[i][0] + 1, graph[i][1] + 1)\n    T -= 1",
      "JavaScript": "// COMPLETE, RUNNABLE JavaScript code\n// Uses fs.readFileSync(0,'utf-8') \u2014 NEVER readline()\n// Handles all T test cases\nconst fs = require('fs');\nconst input = fs.readFileSync(0, 'utf-8').trim().split('\\n');\nlet idx = 0;\n\nconst MAXN = 5000;\nconst MAXM = 5000;\n\n// Union-find data structure\nlet parent = new Array(MAXN).fill(0);\nlet rank = new Array(MAXN).fill(0);\n\n// Function to find the parent of a vertex\nfunction find(x) {\n    if (parent[x] !== x) {\n        parent[x] = find(parent[x]);\n    }\n    return parent[x];\n}\n\n// Function to union two vertices\nfunction unionSet(x, y) {\n    let rootX = find(x);\n    let rootY = find(y);\n    if (rootX !== rootY) {\n        if (rank[rootX] > rank[rootY]) {\n            parent[rootY] = rootX;\n        } else if (rank[rootX] < rank[rootY]) {\n            parent[rootX] = rootY;\n        } else {\n            parent[rootY] = rootX;\n            rank[rootX]++;\n        }\n    }\n}\n\nlet T = parseInt(input[idx++]);\nwhile (T-- > 0) {\n    let [N, M] = input[idx++].split(' ').map(Number);\n    // Initialize the union-find data structure\n    for (let i = 0; i < N; i++) {\n        parent[i] = i;\n        rank[i] = 0;\n    }\n    // Read the edges of L(G)\n    let edges = [];\n    for (let i = 0; i < M; i++) {\n        let [x, y] = input[idx++].split(' ').map(Number);\n        edges.push([x - 1, y - 1]);\n    }\n    // Reconstruct the graph G\n    let K = 0;\n    let graph = new Array(N).fill(0).map(() => new Array(2).fill(0));\n    for (let i = 0; i < N; i++) {\n        let x = edges[i][0];\n        let y = edges[i][1];\n        let rootX = find(x);\n        let rootY = find(y);\n        if (rootX !== rootY) {\n            graph[K][0] = rootX;\n            graph[K][1] = rootY;\n            K++;\n            unionSet(x, y);\n        }\n    }\n    console.log(K);\n    for (let i = 0; i < K; i++) {\n        console.log(graph[i][0] + 1, graph[i][1] + 1);\n    }\n}"
    }
  }
}