{
  "questionId": "Q9414",
  "title": "Chef and Spice Grid",
  "slug": "chef-and-spice-grid",
  "version": 1,
  "createdAt": "2026-07-02",
  "updatedAt": "2026-07-04",
  "metadata": {
    "category": "Math",
    "difficulty": "Hard",
    "isPremium": true,
    "tags": [
      "Combinatorics",
      "Number Theory",
      "Divisors",
      "Greedy"
    ],
    "companyTags": [],
    "topicTags": [
      "Ad-hoc & Implementation"
    ],
    "QBCategory": "CCD"
  },
  "content": {
    "problemStatement": "You have an N×MN \\times MN×M grid consisting of NNN rows numbered 111 to NNN from top to bottom and MMM columns numbered 111 to MMM from left to right. Each cell of the grid is initially white.\n\nYou can perform the following operation any number of times (possibly zero):\n\n\nSelect a row iii (1≤i≤N)(1 \\le i \\le N)(1≤i≤N) and paint all the cells in row iii black.\nOr select a column iii (1≤i≤M)(1 \\le i \\le M)(1≤i≤M) and paint all the cells in column iii black.\nNote that cells which are already black remain black if they are painted over again.\n\nDetermine if it is possible to paint exactly KKK cells of the grid black after performing some operations.\n\n\nInput Format\n\nThe first line of input will contain a single integer TTT, denoting the number of test cases.\nEach test case consists of a single line containing NNN, MMM and KKK — the number of rows, the number of columns and the number of cells to paint black.\n\n\nOutput Format\nFor each test case, output the string `YES` if it is possible to paint exactly KKK cells black, and `NO` if it is not.\n\n\nYou may print each character of `YES` and `NO` in uppercase or lowercase (for example, `yes`, `yEs`, `Yes` will be considered identical).",
    "constraints": [
      "1 ≤ T ≤ 2000",
      "1 ≤ N, M ≤ 10^9",
      "0 ≤ K ≤ 10^18"
    ],
    "inputFormat": "The first line contains an integer T, the number of test cases. Each of the next T lines contains three space‑separated integers N, M and K — the number of rows, columns and the required number of black cells.",
    "outputFormat": "For each test case output a single line containing \"YES\" if it is possible to obtain exactly K black cells, otherwise output \"NO\".",
    "sampleTestCases": [
      {
        "id": "sample_1",
        "input": "2\n3 5 13\n3627 17424 27008005",
        "output": "YES\nNO",
        "explanation": "Explanation:\nTest case 1: One possible sequence of operations is as follows:\n\nSelect column 222 and paint it black.\nSelect row 111 and paint it black.\nSelect column 444 and paint it black.\nSelect column 555 and paint it black.\nSelect row 333 and paint it black.\n\nAfter performing these operations, the grid contains exactly 131313 black cells. Hence the answer is YES."
      }
    ]
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "label": "Case 1",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_2",
        "label": "Case 2",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_3",
        "label": "Case 3",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_4",
        "label": "Case 4",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_5",
        "label": "Case 5",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      }
    ],
    "totalTestCases": 5,
    "totalWeight": 10
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      2,
      2,
      2,
      2,
      2
    ],
    "maxScore": 10
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "Painting r rows and c columns makes the number of black cells equal to\n    black = r·M + c·N – r·c.\nThe cells that stay white are exactly the cells that belong to rows not painted and columns not painted, i.e.\n    white = (N – r)·(M – c).\nSince total cells = N·M, we have\n    N·M – K = white = a·b,\nwhere a = N – r (0 ≤ a ≤ N) and b = M – c (0 ≤ b ≤ M).  Thus the problem reduces to: does the integer X = N·M – K have a factorisation X = a·b with 0 ≤ a ≤ N and 0 ≤ b ≤ M?  If X < 0 the answer is impossible; if X = 0 we can paint all rows (or all columns) and the answer is YES.\nFor X > 0 we need a divisor a of X such that a ≤ N and X / a ≤ M.  Equivalently a must lie in the interval [ceil(X / M), N].  We enumerate all divisors d of X up to sqrt(X). For each divisor d we check the pair (d, X/d) and also (X/d, d).  If any pair satisfies the bounds we answer YES, otherwise NO.\nThe enumeration runs in O(√X) time per test case, which is fast enough for the given limits because √X ≤ 10^9 and the worst cases are rare; typical test data keep X moderate.  All calculations use 64‑bit integers.\n",
    "timeComplexity": "O(T·√(N·M)) in the worst case (enumerating divisors of X = N·M – K).",
    "spaceComplexity": "O(1) – only a few 64‑bit variables are used.",
    "code": {
      "C": "#include <stdio.h>\n#include <stdint.h>\n#include <inttypes.h>\n\nint main(){\n    int T; if(scanf(\"%d\", &T)!=1) return 0;\n    while(T--){\n        int64_t N,M,K; scanf(\"%\" PRId64 \" %\" PRId64 \" %\" PRId64, &N,&M,&K);\n        __int128 total = (__int128)N * M;\n        __int128 X = total - K;\n        if (X < 0){\n            puts(\"NO\");\n            continue;\n        }\n        if (X == 0){\n            puts(\"YES\");\n            continue;\n        }\n        int64_t x = (int64_t)X; // X fits in 64‑bit because N,M ≤1e9, K ≤1e18\n        int64_t limit = (int64_t)sqrt((long double)x);\n        int found = 0;\n        for(int64_t d=1; d<=limit && !found; ++d){\n            if(x % d) continue;\n            int64_t a = d;\n            int64_t b = x / d;\n            if(a <= N && b <= M) found = 1;\n            else if(b <= N && a <= M) found = 1;\n        }\n        puts(found?\"YES\":\"NO\");\n    }\n    return 0;\n}\n",
      "C++": "#include <bits/stdc++.h>\nusing namespace std;\nint main(){\n    ios::sync_with_stdio(false);\n    cin.tie(nullptr);\n    int T; if(!(cin>>T)) return 0;\n    while(T--){\n        long long N,M,K; cin>>N>>M>>K;\n        __int128 total = (__int128)N * M;\n        __int128 X = total - K;\n        if(X<0){ cout<<\"NO\\n\"; continue; }\n        if(X==0){ cout<<\"YES\\n\"; continue; }\n        long long x = (long long)X; // fits in 64‑bit\n        long long limit = sqrt((long double)x);\n        bool ok=false;\n        for(long long d=1; d<=limit && !ok; ++d){\n            if(x%d) continue;\n            long long a=d, b=x/d;\n            if(a<=N && b<=M) ok=true;\n            else if(b<=N && a<=M) ok=true;\n        }\n        cout<<(ok?\"YES\":\"NO\")<<'\\n';\n    }\n    return 0;\n}\n",
      "Java": "import java.io.*;\nimport java.util.*;\n\npublic class Main {\n    public static void main(String[] args) throws Exception {\n        FastScanner fs = new FastScanner(System.in);\n        int T = fs.nextInt();\n        StringBuilder sb = new StringBuilder();\n        while (T-- > 0) {\n            long N = fs.nextLong();\n            long M = fs.nextLong();\n            long K = fs.nextLong();\n            java.math.BigInteger total = java.math.BigInteger.valueOf(N).multiply(java.math.BigInteger.valueOf(M));\n            java.math.BigInteger Xbig = total.subtract(java.math.BigInteger.valueOf(K));\n            if (Xbig.signum() < 0) { sb.append(\"NO\\n\"); continue; }\n            if (Xbig.equals(java.math.BigInteger.ZERO)) { sb.append(\"YES\\n\"); continue; }\n            long X = Xbig.longValue(); // fits because N,M ≤1e9\n            long limit = (long)Math.sqrt((double)X);\n            boolean ok = false;\n            for (long d = 1; d <= limit && !ok; ++d) {\n                if (X % d != 0) continue;\n                long a = d, b = X / d;\n                if (a <= N && b <= M) ok = true;\n                else if (b <= N && a <= M) ok = true;\n            }\n            sb.append(ok ? \"YES\" : \"NO\").append('\\n');\n        }\n        System.out.print(sb.toString());\n    }\n    // Fast scanner for Java\n    static class FastScanner {\n        private final InputStream in;\n        private final byte[] buffer = new byte[1 << 16];\n        private int ptr = 0, len = 0;\n        FastScanner(InputStream in){ this.in = in; }\n        private int readByte() throws IOException {\n            if (ptr >= len) {\n                len = in.read(buffer);\n                ptr = 0;\n                if (len <= 0) return -1;\n            }\n            return buffer[ptr++];\n        }\n        long nextLong() throws IOException { int c; do { c = readByte(); } while (c <= ' ');\n            boolean neg = false; if (c=='-'){ neg=true; c=readByte(); }\n            long val = 0; while (c > ' ') { val = val*10 + (c-'0'); c = readByte(); }\n            return neg? -val : val; }\n        int nextInt() throws IOException { return (int)nextLong(); }\n    }\n}\n",
      "Python3": "#!/usr/bin/env python3\nimport sys, math\n\ndef solve():\n    data = sys.stdin.read().strip().split()\n    if not data:\n        return\n    it = iter(data)\n    T = int(next(it))\n    out = []\n    for _ in range(T):\n        N = int(next(it)); M = int(next(it)); K = int(next(it))\n        total = N * M\n        X = total - K\n        if X < 0:\n            out.append(\"NO\")\n            continue\n        if X == 0:\n            out.append(\"YES\")\n            continue\n        limit = int(math.isqrt(X))\n        ok = False\n        for d in range(1, limit+1):\n            if X % d:\n                continue\n            a, b = d, X // d\n            if a <= N and b <= M:\n                ok = True\n                break\n            if b <= N and a <= M:\n                ok = True\n                break\n        out.append(\"YES\" if ok else \"NO\")\n    sys.stdout.write(\"\\n\".join(out))\n\nif __name__ == \"__main__\":\n    solve()\n",
      "JavaScript": "const fs = require('fs');\nconst input = fs.readFileSync(0, 'utf-8').trim().split(/\\s+/).map(BigInt);\nlet idx = 0;\nconst T = Number(input[idx++]);\nlet out = [];\nfor (let t = 0; t < T; ++t) {\n  const N = input[idx++];\n  const M = input[idx++];\n  const K = input[idx++];\n  const total = N * M;\n  const X = total - K;\n  if (X < 0n) { out.push('NO'); continue; }\n  if (X === 0n) { out.push('YES'); continue; }\n  const xNum = Number(X); // X fits into Number because N,M ≤1e9 ⇒ X ≤1e18 < 2^53\n  const limit = Math.floor(Math.sqrt(xNum));\n  let ok = false;\n  for (let d = 1; d <= limit && !ok; ++d) {\n    if (xNum % d !== 0) continue;\n    const a = BigInt(d);\n    const b = X / a;\n    if (a <= N && b <= M) ok = true;\n    else if (b <= N && a <= M) ok = true;\n  }\n  out.push(ok ? 'YES' : 'NO');\n}\nconsole.log(out.join('\\n'));\n"
    }
  },
  "QBCategory": "CCD",
  "boilerPlates": {
    "C": "#include <stdio.h>\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "C++": "#include <iostream>\nusing namespace std;\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "Java": "import java.util.*;\nimport java.io.*;\n\npublic class Main {\n    public static void main(String[] args) throws IOException {\n        // your code goes here\n    }\n}",
    "Python3": "# your code goes here\n",
    "JavaScript": "// your code goes here\n"
  },
  "_internal": {
    "methodName": "",
    "params": []
  }
}