{
  "questionId": "Q11800",
  "title": "Word ladder II",
  "slug": "word-ladder-ii",
  "version": 1,
  "createdAt": "2026-07-05",
  "updatedAt": "2026-07-05",
  "metadata": {
    "category": "Graph",
    "difficulty": "Hard",
    "isPremium": true,
    "tags": [
      "BFS",
      "Word Ladder"
    ],
    "companyTags": [],
    "topicTags": [],
    "QBCategory": "CCD"
  },
  "content": {
    "problemStatement": "Given two distinct words startWord and targetWord, and a list denoting wordList of unique words of equal lengths. Find all shortest transformation sequence(s) from startWord to targetWord. You can return them in any order possible.",
    "constraints": [
      "1 <= wordList.length <= 5 * 10^3",
      "1 <= startWord.length, targetWord.length, wordList[i].length <= 10",
      "startWord, targetWord, and all words in wordList consist of lowercase English letters.",
      "No two words in wordList are the same.",
      "startWord is not equal to targetWord."
    ],
    "inputFormat": "The input will contain three parameters: startWord (string), targetWord (string), and wordList (array of strings).",
    "outputFormat": "The output will be an array of arrays, where each inner array represents a transformation sequence from startWord to targetWord.",
    "sampleTestCases": []
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "label": "min boundary",
        "input": {
          "startWord": "hit",
          "targetWord": "cog",
          "wordList": [
            "hot",
            "dot",
            "dog",
            "lot",
            "log",
            "cog"
          ]
        },
        "expectedOutput": [
          [
            "hit",
            "hot",
            "dot",
            "dog",
            "cog"
          ],
          [
            "hit",
            "hot",
            "lot",
            "log",
            "cog"
          ]
        ],
        "weight": 2
      },
      {
        "id": "tc_2",
        "label": "max boundary",
        "input": {
          "startWord": "aaaa",
          "targetWord": "zzzz",
          "wordList": [
            "aaaa",
            "baaa",
            "caaa",
            "daaa",
            "eaaa",
            "faaa",
            "gaaa",
            "haaa",
            "iaaa",
            "jaaa",
            "kaaa",
            "laaa",
            "maaa",
            "naaa",
            "oaaa",
            "paaa",
            "qaaa",
            "raaa",
            "saaa",
            "taaa",
            "uaaa",
            "vaaa",
            "waaa",
            "xaaa",
            "yaaa",
            "zaaa",
            "abaa",
            "acaa",
            "adaa",
            "aeaa",
            "afaa",
            "agaa",
            "ahaa",
            "aiaa",
            "ajaa",
            "akaa",
            "alaa",
            "amaa",
            "anaa",
            "aoaa",
            "apaa",
            "aqaa",
            "araa",
            "asaa",
            "ataa",
            "auaa",
            "avaa",
            "awaa",
            "axaa",
            "ayaa",
            "azaa",
            "bbaa",
            "bcaa",
            "bdaa",
            "beaa",
            "bfaa",
            "bgaa",
            "bhaa",
            "biaa",
            "bjaa",
            "bkaa",
            "blaa",
            "bmaa",
            "bnaa",
            "boaa",
            "bpaa",
            "bqaa",
            "braa",
            "bsaa",
            "btaa",
            "buaa",
            "bva",
            "bwaa",
            "bxaa",
            "byaa",
            "bzaa",
            "zzzz"
          ]
        },
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_3",
        "label": "Case 3",
        "input": {
          "startWord": "hit",
          "targetWord": "cog",
          "wordList": [
            "hot",
            "dot",
            "dog",
            "lot",
            "log",
            "cog"
          ]
        },
        "expectedOutput": [
          [
            "hit",
            "hot",
            "dot",
            "dog",
            "cog"
          ],
          [
            "hit",
            "hot",
            "lot",
            "log",
            "cog"
          ]
        ],
        "weight": 2
      },
      {
        "id": "tc_4",
        "label": "Case 4",
        "input": {
          "startWord": "hit",
          "targetWord": "cog",
          "wordList": [
            "hot",
            "dot",
            "dog",
            "lot",
            "log",
            "cog"
          ]
        },
        "expectedOutput": [
          [
            "hit",
            "hot",
            "dot",
            "dog",
            "cog"
          ],
          [
            "hit",
            "hot",
            "lot",
            "log",
            "cog"
          ]
        ],
        "weight": 2
      },
      {
        "id": "tc_5",
        "label": "Case 5",
        "input": {
          "startWord": "hit",
          "targetWord": "cog",
          "wordList": [
            "hot",
            "dot",
            "dog",
            "lot",
            "log",
            "cog"
          ]
        },
        "expectedOutput": [
          [
            "hit",
            "hot",
            "dot",
            "dog",
            "cog"
          ],
          [
            "hit",
            "hot",
            "lot",
            "log",
            "cog"
          ]
        ],
        "weight": 2
      }
    ],
    "totalTestCases": 5,
    "totalWeight": 10
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      2,
      2,
      2,
      2,
      2
    ],
    "maxScore": 10
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "Use a breadth-first search (BFS) algorithm to explore all possible transformation sequences from startWord to targetWord.",
    "timeComplexity": "O(N * M * 26^L)",
    "spaceComplexity": "O(N * M * L)",
    "code": {
      "C++": "#include <bits/stdc++.h>\nusing namespace std;\n\nclass Solution\n{\npublic:\n    vector<vector<string>> findSequences(string beginWord, string endWord,\n                                         vector<string> &wordList)\n    {\n        // Push all values of wordList into a set\n        // to make deletion from it easier and in less time complexity.\n        unordered_set<string> st(wordList.begin(), wordList.end());\n        \n        // Creating a queue ds which stores the words in a sequence which is\n        // required to reach the targetWord after successive transformations.\n        queue<vector<string>> q;\n\n        // BFS traversal with pushing the new formed sequence in queue \n        // when after a transformation, a word is found in wordList.\n\n        q.push({beginWord});\n\n        // A vector defined to store the words being currently used\n        // on a level during BFS.\n        vector<string> usedOnLevel;\n        usedOnLevel.push_back(beginWord);\n        int level = 0;\n       \n        // A vector to store the resultant transformation sequence.\n        vector<vector<string>> ans;\n        while (!q.empty())\n        {\n            vector<string> vec = q.front();\n            q.pop();\n\n            // Now, erase all words that have been\n            // used in the previous levels to transform\n            if (vec.size() > level)\n            {\n                level++;\n                for (auto it : usedOnLevel)\n                {\n                    st.erase(it);\n                }\n            }\n\n            string word = vec.back();\n\n            // store the answers if the end word matches with targetWord.\n            if (word == endWord)\n            {\n                // the first sequence where we reached end\n                if (ans.size() == 0)\n                {\n                    ans.push_back(vec);\n                }\n                else if (ans[0].size() == vec.size())\n                {\n                    ans.push_back(vec);\n                }\n            }\n            for (int i = 0; i < word.size(); i++)\n            {   \n                // Now, replace each character of ‘word’ with char\n                // from a-z then check if ‘word’ exists in wordList.\n                char original = word[i];\n                for (char c = 'a'; c <= 'z'; c++)\n                {\n                    word[i] = c;\n                    if (st.count(word) > 0)\n                    { \n                        // Check if the word is present in the wordList and\n                        // push the word along with the new sequence in the queue.\n                        vec.push_back(word);\n                        q.push(vec);\n                        // mark as visited on the level\n                        usedOnLevel.push_back(word);\n                        vec.pop_back();\n                    }\n                }\n                word[i] = original;\n            }\n        }\n        return ans;\n    }\n};\n\n// A comparator function to sort the answer.\nbool comp(vector<string> a, vector<string> b)\n{\n    string x = \"\", y = \"\";\n    for (string i : a)\n        x += i;\n    for (string i : b)\n        y += i;\n\n    return x < y;\n}\n\nint main()\n{\n\n    vector<string> wordList = {\"des\", \"der\", \"dfr\", \"dgt\", \"dfs\"};\n    string startWord = \"der\", targetWord = \"dfs\";\n    Solution obj;\n    vector<vector<string>> ans = obj.findSequences(startWord, targetWord, wordList);\n    \n    // If no transformation sequence is possible.\n    if (ans.size() == 0)\n        cout << -1 << endl;\n    else\n    {\n        sort(ans.begin(), ans.end(), comp);\n        for (int i = 0; i < ans.size(); i++)\n        {\n            for (int j = 0; j < ans[i].size(); j++)\n            {\n                cout << ans[i][j] << \" \";\n            }\n            cout << endl;\n        }\n    }\n\n    return 0;\n}",
      "Java": "import java.util.*;\n\nclass Solution {\n    public List<List<String>> findSequences(String beginWord, String endWord, List<String> wordList) {\n        // Use a HashSet for fast lookup and removal\n        Set<String> st = new HashSet<>(wordList);\n\n        // Queue stores paths (each path is a list of words)\n        Queue<List<String>> q = new LinkedList<>();\n        q.offer(new ArrayList<>(Arrays.asList(beginWord)));\n\n        // Track words used at the current BFS level\n        List<String> usedOnLevel = new ArrayList<>();\n        usedOnLevel.add(beginWord);\n        int level = 0;\n\n        // Resultant list of sequences\n        List<List<String>> ans = new ArrayList<>();\n\n        while (!q.isEmpty()) {\n            List<String> vec = q.poll();\n\n            // Remove used words when moving to a deeper BFS level\n            if (vec.size() > level) {\n                level++;\n                for (String used : usedOnLevel) {\n                    st.remove(used);\n                }\n            }\n\n            String word = vec.get(vec.size() - 1);\n\n            // If we found a sequence that reaches the endWord\n            if (word.equals(endWord)) {\n                if (ans.isEmpty()) {\n                    ans.add(new ArrayList<>(vec));\n                } else if (ans.get(0).size() == vec.size()) {\n                    ans.add(new ArrayList<>(vec));\n                }\n            }\n\n            // Try changing each character of the current word\n            char[] wordArr = word.toCharArray();\n            for (int i = 0; i < wordArr.length; i++) {\n                char original = wordArr[i];\n                for (char c = 'a'; c <= 'z'; c++) {\n                    wordArr[i] = c;\n                    String newWord = new String(wordArr);\n\n                    // If this transformed word exists in the set\n                    if (st.contains(newWord)) {\n                        vec.add(newWord);\n                        q.offer(new ArrayList<>(vec));\n                        usedOnLevel.add(newWord);\n                        vec.remove(vec.size() - 1);\n                    }\n                }\n                wordArr[i] = original;\n            }\n        }\n\n        return ans;\n    }\n\n    public static void main(String[] args) {\n        List<String> wordList = Arrays.asList(\"des\", \"der\", \"dfr\", \"dgt\", \"dfs\");\n        String startWord = \"der\", targetWord = \"dfs\";\n\n        Solution obj = new Solution();\n        List<List<String>> ans = obj.findSequences(startWord, targetWord, new ArrayList<>(wordList));\n\n        if (ans.isEmpty()) {\n            System.out.println(-1);\n        } else {\n            ans.sort((a, b) -> String.join(\"\", a).compareTo(String.join(\"\", b)));\n            for (List<String> sequence : ans) {\n                System.out.println(String.join(\" \", sequence));\n            }\n        }\n    }\n}",
      "Python3": "from collections import deque\n\nclass Solution:\n    def findSequences(self, beginWord, endWord, wordList):\n        \"\"\"\n        Finds all the shortest transformation sequences from beginWord to endWord.\n        Each transformation changes one letter and must exist in wordList.\n        \"\"\"\n        word_set = set(wordList)  # Fast lookup and deletion\n        queue = deque([[beginWord]])  # Each element is a path (list of words)\n        used_on_level = [beginWord]\n        level = 0\n        ans = []\n\n        while queue:\n            path = queue.popleft()\n\n            # Remove used words when moving to the next BFS level\n            if len(path) > level:\n                level += 1\n                for word in used_on_level:\n                    word_set.discard(word)\n\n            word = path[-1]\n\n            # If we found the target word\n            if word == endWord:\n                if not ans:\n                    ans.append(path[:])\n                elif len(ans[0]) == len(path):\n                    ans.append(path[:])\n\n            # Try changing each character of the current word\n            for i in range(len(word)):\n                original = word[i]\n                for c in 'abcdefghijklmnopqrstuvwxyz':\n                    new_word = word[:i] + c + word[i+1:]\n                    if new_word in word_set:\n                        path.append(new_word)\n                        queue.append(path[:])\n                        used_on_level.append(new_word)\n                        path.pop()\n        \n        return ans\n\n\n# Driver code\nif __name__ == \"__main__\":\n    wordList = [\"des\", \"der\", \"dfr\", \"dgt\", \"dfs\"]\n    startWord, targetWord = \"der\", \"dfs\"\n    obj = Solution()\n    ans = obj.findSequences(startWord, targetWord, wordList)\n\n    if not ans:\n        print(-1)\n    else:\n        ans.sort(key=lambda x: \"\".join(x))\n        for sequence in ans:\n            print(\" \".join(sequence))",
      "JavaScript": "class Solution {\n    findSequences(beginWord, endWord, wordList) {\n        /**\n         * Finds all the shortest transformation sequences from beginWord to endWord.\n         */\n        const wordSet = new Set(wordList); // Fast lookup and removal\n        const queue = [[beginWord]]; // Each element is a path (array of words)\n        const usedOnLevel = [beginWord];\n        let level = 0;\n        const ans = [];\n\n        while (queue.length > 0) {\n            const path = queue.shift();\n\n            // Remove used words when moving to the next level\n            if (path.length > level) {\n                level++;\n                for (let word of usedOnLevel) {\n                    wordSet.delete(word);\n                }\n            }\n\n            const word = path[path.length - 1];\n\n            // If we found the target word\n            if (word === endWord) {\n                if (ans.length === 0) {\n                    ans.push([...path]);\n                } else if (ans[0].length === path.length) {\n                    ans.push([...path]);\n                }\n            }\n\n            // Try changing each character of the current word\n            for (let i = 0; i < word.length; i++) {\n                for (let c = 97; c <= 122; c++) { // 'a' to 'z'\n                    const newWord = word.slice(0, i) + String.fromCharCode(c) + word.slice(i + 1);\n\n                    if (wordSet.has(newWord)) {\n                        path.push(newWord);\n                        queue.push([...path]);\n                        usedOnLevel.push(newWord);\n                        path.pop();\n                    }\n                }\n            }\n        }\n\n        return ans;\n    }\n}\n\n// Driver code\nconst wordList = [\"des\", \"der\", \"dfr\", \"dgt\", \"dfs\"];\nconst startWord = \"der\", targetWord = \"dfs\";\nconst obj = new Solution();\nconst ans = obj.findSequences(startWord, targetWord, wordList);\n\nif (ans.length === 0) {\n    console.log(-1);\n} else {\n    ans.sort((a, b) => a.join(\"\").localeCompare(b.join(\"\")));\n    ans.forEach(seq => console.log(seq.join(\" \")));\n}"
    }
  },
  "QBCategory": "CCD",
  "boilerPlates": {
    "C": "#include <stdio.h>\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "C++": "#include <iostream>\nusing namespace std;\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "Java": "import java.util.*;\nimport java.io.*;\n\npublic class Main {\n    public static void main(String[] args) throws IOException {\n        // your code goes here\n    }\n}",
    "Python3": "# your code goes here\n",
    "JavaScript": "// your code goes here\n"
  },
  "_internal": {
    "methodName": "",
    "params": []
  }
}