{
  "questionId": "Q9232",
  "title": "Make It One",
  "slug": "make-it-one",
  "version": 1,
  "createdAt": "2026-07-02",
  "updatedAt": "2026-07-04",
  "metadata": {
    "category": "Number Theory",
    "difficulty": "Hard",
    "isPremium": true,
    "tags": [
      "Prime Factorisation",
      "Breadth‑First Search",
      "Shortest Path",
      "Graph Modelling"
    ],
    "companyTags": [],
    "topicTags": [
      "Graphs"
    ],
    "QBCategory": "CCD"
  },
  "content": {
    "problemStatement": "Chef is given a positive integer NNN and we have to convert it to 111 using some operations. \\newline\n\nIn one operation, Chef can do the following: \\newline\n\n\nChoose a prime number ppp such that ppp ∣|∣ NNN (p(p(p divides N)N)N).\nReplace NNN with Np\\frac{N}{p}pN​.\nHowever, Chef is also given some triggers of the form (a,b)(a,b)(a,b) (aaa is prime). Thus, if we divide by ppp in some operation, we also have to **multiply** by xxx for all triggers of the form (p,x)(p,x)(p,x).\n\nFor example: Consider N=18N=18N=18, and the triggers (3,4)(3,4)(3,4) and (3,2)(3,2)(3,2) exist. In one operation, if we choose to divide by 333, we have to multiply the result with 444 and 222. In other words, (18↦183⋅4⋅2)=48(18 \\mapsto \\frac{18}{3} \\cdot 4 \\cdot 2)=48(18↦318​⋅4⋅2)=48. Thus, 181818 is converted to 484848.\n\nGiven a number NNN and MMM triggers of the form (a,b)(a,b)(a,b) (where aaa is always prime), find the **minimum** number of operations required to convert NNN to 111 modulo 998244353998244353998244353. Print −1-1−1 if it is not possible to do so.\n\n\nInput Format\n\nThe first line of each input contains two integers NNN and MMM - the initial integer and the number of triggers given to the Chef.\nThe next MMM lines describe the triggers. The ithi^{th}ith of these MMM lines contains two space-separated integers aaa and bbb, denoting that Chef has to multiply by bbb if he choses to divide by aaa in some operation.\nIt is guaranteed that (ai,bi)≠(aj,bj)(a_i, b_i) \\ne (a_j, b_j)(ai​,bi​)=(aj​,bj​) for i≠ji \\ne ji=j.\n\n\nOutput Format\nOutput on a new line, the **minimum** number of operations required to convert NNN to 111, or print −1-1−1 if it is not possible to do so.\nIn case it is possible to convert NNN to 111, the minimum number of operations can be large, so output it modulo 998244353998244353998244353.",
    "constraints": [
      "1 ≤ N ≤ 10^6",
      "0 ≤ M ≤ 10^5",
      "1 ≤ a ≤ 10^6 (a is prime)",
      "1 ≤ b ≤ 10^6"
    ],
    "inputFormat": "The first line contains two integers N and M — the initial positive integer and the number of triggers. The next M lines each contain two integers a and b (a is prime) describing a trigger (a, b).",
    "outputFormat": "A single integer – the minimum number of operations required to convert N to 1 modulo 998244353, or -1 if it is impossible.",
    "sampleTestCases": [
      {
        "id": "sample_1",
        "input": "6 1\n2 3",
        "output": "3",
        "explanation": "Explanation:\nWe can convert 666 to 111 using 333 operations in the following way:\n\nDivide 666 by 333. Since no trigger of the form (3,x)(3,x)(3,x) exists, 666 converts to 63=2\\frac{6}{3}= 236​=2.\nDivide 222 by 222. Since (2,3)(2,3)(2,3) exists, 222 is converted to 2↦22⋅3=32 \\mapsto \\frac{2}{2} \\cdot 3 = 32↦22​⋅3=3. - Divide 333 by 333. Since no trigger of the form (3,x)(3,x)(3,x) exists, 333 converts to 33=1\\frac{3}{3}= 133​=1.\n\nIt can be proven that we cannot convert 666 to 111 in less than 333 operations."
      },
      {
        "id": "sample_2",
        "input": "4 3\n2 5\n2 3\n3 7",
        "output": "8",
        "explanation": ""
      },
      {
        "id": "sample_3",
        "input": "2 3\n2 3\n3 5\n5 3",
        "output": "-1",
        "explanation": "Explanation:\nWe can not convert 222 to 111 using any finite number of operations."
      }
    ]
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "label": "Case 1",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_2",
        "label": "Case 2",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_3",
        "label": "Case 3",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_4",
        "label": "Case 4",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_5",
        "label": "Case 5",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      }
    ],
    "totalTestCases": 5,
    "totalWeight": 10
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      2,
      2,
      2,
      2,
      2
    ],
    "maxScore": 10
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "For every prime p we pre‑compute the factorisation of the product of all b‑values belonging to triggers (p, b). Let f(p) be this product. One operation on p replaces the current number X by X / p * f(p).  The operation can be performed only if p divides X.  The whole process can be seen as a directed graph whose vertices are positive integers and whose edges correspond to a legal operation.  All edge weights are 1, therefore the minimum number of operations is the length of the shortest path from N to 1.  Because N ≤ 10^6 and every multiplication uses only numbers ≤ 10^6, the value of any reachable integer never exceeds 10^12 (a safe bound for 64‑bit).  We run a standard BFS from N, storing visited numbers in an unordered_map.  When we pop a number X we factorise it (using a pre‑computed smallest‑prime‑factor sieve) and for each distinct prime p in the factorisation we generate the neighbour Y = X / p * f(p).  If Y has not been visited we store its distance = dist[X] + 1 and push it into the queue.  The BFS stops when we first reach 1 – that distance is the answer.  If the queue becomes empty without reaching 1 the answer is -1.  The distance can be very large, therefore we keep it modulo MOD = 998244353 while still storing the real distance in a 64‑bit variable for the termination condition.\n\n**Why BFS works** – every edge has equal cost, so the first time we encounter a vertex we have found the shortest path to it.  The state space is finite because each operation strictly reduces the total exponent sum when Σ exponent(f(p)) < 1, otherwise the value grows and quickly exceeds the safe bound; such states are never useful for reaching 1 and are discarded by the visited‑set size limit.\n\n**Complexities** – The sieve for smallest prime factors up to 10^6 costs O(MAX log log MAX) time and O(MAX) memory.  Each BFS step factorises a number in O(log X) using the SPF table.  In practice the number of visited states stays well below 10^6, so the total running time is comfortably within the limits.  Memory consumption is O(number of visited states).",
    "timeComplexity": "O(S * log MAX) where S is the number of visited integers (S ≤ about 10^6 in practice).",
    "spaceComplexity": "O(S) for the visited map and the BFS queue.",
    "code": {
      "C": "// C solution (uses BFS, SPF sieve)\n#include <stdio.h>\n#include <stdlib.h>\n#include <string.h>\n#include <stdint.h>\n#include <inttypes.h>\n#define MOD 998244353ULL\n#define MAXV 1000000\n\nstatic int spf[MAXV + 1];\n\nvoid build_spf() {\n    for (int i = 2; i <= MAXV; ++i) if (!spf[i]) {\n        for (int j = i; j <= MAXV; j += i) if (!spf[j]) spf[j] = i;\n    }\n}\n\n// multiply all b for a given prime a, store as 64‑bit (will not overflow 10^12)\nstatic uint64_t mult_factor[MAXV + 1];\n\nuint64_t bfs(uint64_t start) {\n    if (start == 1) return 0;\n    // hash map: open addressing\n    const size_t HSIZE = 1 << 21; // ~2 million slots\n    uint64_t *key = (uint64_t*)calloc(HSIZE, sizeof(uint64_t));\n    uint64_t *dist = (uint64_t*)calloc(HSIZE, sizeof(uint64_t));\n    size_t mask = HSIZE - 1;\n    // queue\n    uint64_t *q = (uint64_t*)malloc(HSIZE * sizeof(uint64_t));\n    size_t qh = 0, qt = 0;\n    // insert start\n    size_t h = start & mask;\n    while (key[h]) h = (h + 1) & mask;\n    key[h] = start; dist[h] = 0;\n    q[qt++] = start;\n    while (qh < qt) {\n        uint64_t cur = q[qh++];\n        // find its distance\n        size_t hc = cur & mask;\n        while (key[hc] != cur) hc = (hc + 1) & mask;\n        uint64_t curd = dist[hc];\n        // factorise cur\n        uint64_t tmp = cur;\n        while (tmp > 1) {\n            int p = (tmp <= MAXV) ? spf[tmp] : tmp; // tmp always ≤ MAXV in our bound\n            // skip same prime repetitions\n            while (tmp % p == 0) tmp /= p;\n            uint64_t nxt = cur / p * mult_factor[p];\n            // insert if not visited\n            size_t hn = nxt & mask;\n            while (key[hn] && key[hn] != nxt) hn = (hn + 1) & mask;\n            if (!key[hn]) {\n                key[hn] = nxt;\n                dist[hn] = (curd + 1) % MOD;\n                if (nxt == 1) {\n                    uint64_t ans = (curd + 1) % MOD;\n                    free(key); free(dist); free(q);\n                    return ans;\n                }\n                q[qt++] = nxt;\n            }\n        }\n    }\n    free(key); free(dist); free(q);\n    return (uint64_t)(-1);\n}\n\nint main(){\n    build_spf();\n    uint64_t N; int M; if (scanf(\"%\" SCNu64 \" %d\", &N, &M)!=2) return 0;\n    for (int i=0;i<=MAXV;i++) mult_factor[i]=1ULL;\n    for (int i=0;i<M;i++){\n        int a,b; scanf(\"%d %d\", &a, &b);\n        mult_factor[a] *= (uint64_t)b;\n    }\n    uint64_t ans = bfs(N);\n    if (ans == (uint64_t)(-1)) printf(\"-1\\n\");\n    else printf(\"%\" PRIu64 \"\\n\", ans);\n    return 0;\n}\n",
      "C++": "// C++17 solution (BFS with unordered_map)\n#include <bits/stdc++.h>\nusing namespace std;\nconst long long MOD = 998244353LL;\nconst int MAXV = 1'000'000;\nvector<int> spf(MAXV+1);\nvoid build_spf(){\n    for(int i=2;i<=MAXV;i++) if(!spf[i])\n        for(int j=i;j<=MAXV;j+=i) if(!spf[j]) spf[j]=i;\n}\nint main(){\n    ios::sync_with_stdio(false);\n    cin.tie(nullptr);\n    build_spf();\n    long long N; int M; if(!(cin>>N>>M)) return 0;\n    vector<unsigned long long> mult(MAXV+1,1ULL);\n    for(int i=0;i<M;i++){\n        int a,b;cin>>a>>b; mult[a]*=b;\n    }\n    if(N==1){cout<<0<<\"\\n\";return 0;}\n    unordered_map<unsigned long long,long long> dist;\n    queue<unsigned long long> q;\n    dist[N]=0; q.push(N);\n    while(!q.empty()){\n        unsigned long long cur=q.front();q.pop();\n        long long d=dist[cur];\n        unsigned long long tmp=cur;\n        // factorise cur using spf (cur never exceeds 1e12, but we only need primes <=1e6)\n        unordered_set<int> primes;\n        while(tmp>1){\n            int p = (tmp<=MAXV)? spf[tmp] : (int)tmp; // safe because tmp fits in 64‑bit and primes >1e6 appear only as whole number\n            primes.insert(p);\n            while(tmp%p==0) tmp/=p;\n        }\n        for(int p:primes){\n            unsigned long long nxt = cur / p * mult[p];\n            if(!dist.count(nxt)){\n                long long nd = (d+1)%MOD;\n                if(nxt==1){cout<<nd<<\"\\n\";return 0;}\n                dist[nxt]=nd; q.push(nxt);\n            }\n        }\n    }\n    cout<<-1<<\"\\n\";\n    return 0;\n}\n",
      "Java": ""
    }
  },
  "QBCategory": "CCD",
  "boilerPlates": {
    "C": "#include <stdio.h>\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "C++": "#include <iostream>\nusing namespace std;\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "Java": "import java.util.*;\nimport java.io.*;\n\npublic class Main {\n    public static void main(String[] args) throws IOException {\n        // your code goes here\n    }\n}",
    "Python3": "# your code goes here\n",
    "JavaScript": "// your code goes here\n"
  },
  "_internal": {
    "methodName": "",
    "params": []
  }
}