{
  "questionId": "Q8225",
  "title": "Beautiful Subsequence",
  "slug": "beautiful-subsequence",
  "version": 1,
  "createdAt": "2026-07-02",
  "updatedAt": "2026-07-04",
  "metadata": {
    "category": "Arrays | Dynamic Programming",
    "difficulty": "Hard",
    "isPremium": true,
    "tags": [
      "DP on runs",
      "Sliding maximum over values",
      "O(N·K) solution"
    ],
    "companyTags": [],
    "topicTags": [
      "Dynamic Programming"
    ],
    "QBCategory": "CCD"
  },
  "content": {
    "problemStatement": "An array AAA consisting of NNN integers is called KKK-beautiful if it holds the following property:\nThe number of adjacent indices with different values is at most KKK. More formally, there are at most KKK indices iii (1≤i&lt;N1\\le i\\lt N1≤i&lt;N) such that Ai≠Ai+1A_i\\ne A_{i+1}Ai​=Ai+1​. Note that according to this definition, if an array AAA is KKK-beautiful, then AAA is also (K+1)(K+1)(K+1)-beautiful.\n\nYou are given an array AAA consisting of NNN integers and an integer KKK. Find the length of the longest KKK-beautiful subsequence of AAA, where a subsequence is defined below.\n\nFor example, consider the array A=[1,1,2,3,2,4]A = [1 , 1, 2, 3, 2 , 4]A=[1,1,2,3,2,4]. The subsequences [1,1][1, 1][1,1] and [1,1,2][1, 1, 2][1,1,2] are 111-beautiful, and the subsequences [1,2,3][1, 2, 3][1,2,3] and [1,2,2,4][1, 2, 2, 4][1,2,2,4] are 222-beautiful.\n\n**Note:** A sequence aaa is a subsequence of a sequence bbb if aaa can be obtained from bbb by deletion of several (possibly, zero) elements. For example, [3,1][3,1][3,1] is a subsequence of [3,2,1][3,2,1][3,2,1] and [4,3,1][4,3,1][4,3,1], but not a subsequence of [1,3,3,7][1,3,3,7][1,3,3,7] and [3,10,4][3,10,4][3,10,4].\n\n\nInput Format\nThe first line contains an integer TTT - the number of test cases. Then TTT test cases follow.\n\n\nThe first line of each test case contains two integers NNN, KKK - the length of array AAA and the parameter for a beautiful sequence.\n\n\nThe second line of each testcase contains NNN integers A1,…,ANA_1,\\ldots, A_NA1​,…,AN​.\n\n\n\nOutput Format\nFor each test case, output the maximum possible length of a KKK-beautiful subsequence of AAA.",
    "constraints": [
      "1 ≤ T ≤ 100",
      "1 ≤ N ≤ 1000",
      "1 ≤ K ≤ 1000",
      "1 ≤ Ai ≤ 1000"
    ],
    "inputFormat": "The first line contains an integer T, the number of test cases. Each test case starts with a line containing two integers N and K. The next line contains N integers A1 … AN, the elements of the array.",
    "outputFormat": "For each test case output a single line with the maximum possible length of a K‑beautiful subsequence of the given array.",
    "sampleTestCases": [
      {
        "id": "sample_1",
        "input": "4\n4 1\n1 1 2 3\n9 2\n1 2 3 2 4 5 6 7 5\n5 5\n1 1 1 1 1\n10 1\n1 2 1 2 1 2 1 2 1 2",
        "output": "3\n5\n5\n6",
        "explanation": "Explanation:\nTest Case 1 : You can either choose [1,1,2][1, 1, 2][1,1,2] or [1,1,3][1, 1, 3][1,1,3] as a subsequence. Both of the subsequences are 111-beautiful. So the maximum length of a 111-beautiful subsequence is 333.\n\n\nTest Case 2 : It is optimal to choose the subsequence [1,2,2,5,5][1, 2, 2, 5, 5][1,2,2,5,5]. So the maximum length of a 222-beautiful subsequence is 555.\n\n\nTest Case 3 : You can choose the entire array as a subsequence because the given array is KKK-beautiful for any K≥0K\\ge 0K≥0, which includes K=5K=5K=5."
      }
    ]
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "label": "Case 1",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_2",
        "label": "Case 2",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_3",
        "label": "Case 3",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_4",
        "label": "Case 4",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_5",
        "label": "Case 5",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      }
    ],
    "totalTestCases": 5,
    "totalWeight": 10
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      2,
      2,
      2,
      2,
      2
    ],
    "maxScore": 10
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "Let dp[t][v] be the maximum length of a subsequence that ends with value v and uses exactly t runs (t‑1 changes). While scanning the array from left to right we can extend a subsequence in two ways:\n1. Take the current element a and continue a subsequence that already ends with a (same run). This adds 1 to dp[t][a].\n2. Start a new run with a after any subsequence that used t‑1 runs and ends with a different value. The best length among all values for t‑1 runs is stored in best[t‑1]. Adding the current a gives best[t‑1] + 1.\nFor each element we update dp[t][a] for t = K+1 … 1 (descending to avoid using the just‑updated values). After each update we also refresh best[t] = max(best[t], dp[t][a]). The answer for a test case is max_{1≤t≤K+1} best[t].\nThe algorithm runs in O(N·K) time and O(K·V) memory, where V = 1000 is the maximum possible value of Ai.",
    "timeComplexity": "O(N·K) per test case (N ≤ 1000, K ≤ 1000).",
    "spaceComplexity": "O(K·V) where V = 1001 (≈ 1e6 integers, well within limits).",
    "code": {
      "C": "#include <stdio.h>\n#include <string.h>\n\n#define MAXV 1001\n\nint main(){\n    int T; if(scanf(\"%d\", &T)!=1) return 0;\n    while(T--){\n        int N, K; scanf(\"%d %d\", &N, &K);\n        int A[1005];\n        for(int i=0;i<N;i++) scanf(\"%d\", &A[i]);\n        int runs = K+1;\n        static int dp[1002][MAXV]; // dp[t][v]\n        static int best[1002];\n        memset(dp, 0, sizeof(dp));\n        memset(best, 0, sizeof(best));\n        for(int idx=0; idx<N; ++idx){\n            int a = A[idx];\n            for(int t=runs; t>=1; --t){\n                int old = dp[t][a];\n                int cand = old + 1; // continue same run\n                if(t>1) cand = cand > best[t-1]+1 ? cand : best[t-1]+1;\n                else cand = cand > 1 ? cand : 1; // start new subsequence\n                if(cand > dp[t][a]) dp[t][a] = cand;\n                if(dp[t][a] > best[t]) best[t] = dp[t][a];\n            }\n        }\n        int ans = 0;\n        for(int t=1; t<=runs; ++t) if(best[t] > ans) ans = best[t];\n        printf(\"%d\\n\", ans);\n    }\n    return 0;\n}\n",
      "C++": "#include <bits/stdc++.h>\nusing namespace std;\n\nint main(){\n    ios::sync_with_stdio(false);\n    cin.tie(nullptr);\n    int T; if(!(cin>>T)) return 0;\n    const int MAXV = 1001;\n    while(T--){\n        int N,K; cin>>N>>K;\n        vector<int> A(N);\n        for(int &x: A) cin>>x;\n        int runs = K+1;\n        vector<vector<int>> dp(runs+1, vector<int>(MAXV,0));\n        vector<int> best(runs+1,0);\n        for(int a: A){\n            for(int t=runs; t>=1; --t){\n                int old = dp[t][a];\n                int cand = old + 1; // continue same value\n                if(t>1) cand = max(cand, best[t-1] + 1);\n                else cand = max(cand, 1);\n                dp[t][a] = max(dp[t][a], cand);\n                best[t] = max(best[t], dp[t][a]);\n            }\n        }\n        int ans = 0;\n        for(int t=1; t<=runs; ++t) ans = max(ans, best[t]);\n        cout<<ans<<\"\\n\";\n    }\n    return 0;\n}\n",
      "Java": "import java.io.*;\nimport java.util.*;\n\npublic class Main {\n    static final int MAXV = 1001;\n    public static void main(String[] args) throws Exception {\n        FastScanner fs = new FastScanner(System.in);\n        int T = fs.nextInt();\n        StringBuilder sb = new StringBuilder();\n        while (T-- > 0) {\n            int N = fs.nextInt();\n            int K = fs.nextInt();\n            int[] A = new int[N];\n            for (int i = 0; i < N; i++) A[i] = fs.nextInt();\n            int runs = K + 1;\n            int[][] dp = new int[runs + 1][MAXV];\n            int[] best = new int[runs + 1];\n            for (int a : A) {\n                for (int t = runs; t >= 1; t--) {\n                    int old = dp[t][a];\n                    int cand = old + 1; // continue same run\n                    if (t > 1) cand = Math.max(cand, best[t - 1] + 1);\n                    else cand = Math.max(cand, 1);\n                    dp[t][a] = Math.max(dp[t][a], cand);\n                    best[t] = Math.max(best[t], dp[t][a]);\n                }\n            }\n            int ans = 0;\n            for (int t = 1; t <= runs; t++) ans = Math.max(ans, best[t]);\n            sb.append(ans).append('\\n');\n        }\n        System.out.print(sb.toString());\n    }\n\n    // Fast scanner for Java\n    static class FastScanner {\n        private final InputStream in;\n        private final byte[] buffer = new byte[1 << 16];\n        private int ptr = 0, len = 0;\n        FastScanner(InputStream in) { this.in = in; }\n        private int readByte() throws IOException {\n            if (ptr >= len) {\n                len = in.read(buffer);\n                ptr = 0;\n                if (len <= 0) return -1;\n            }\n            return buffer[ptr++];\n        }\n        int nextInt() throws IOException {\n            int c, sign = 1, val = 0;\n            do { c = readByte(); } while (c <= ' ');\n            if (c == '-') { sign = -1; c = readByte(); }\n            while (c > ' ') {\n                val = val * 10 + (c - '0');\n                c = readByte();\n            }\n            return val * sign;\n        }\n    }\n}\n",
      "Python3": "import sys\n\ndef solve():\n    data = sys.stdin.read().strip().split()\n    if not data:\n        return\n    it = iter(data)\n    T = int(next(it))\n    out_lines = []\n    MAXV = 1001\n    for _ in range(T):\n        N = int(next(it)); K = int(next(it))\n        A = [int(next(it)) for _ in range(N)]\n        runs = K + 1\n        dp = [[0]*MAXV for _ in range(runs+1)]\n        best = [0]*(runs+1)\n        for a in A:\n            for t in range(runs, 0, -1):\n                old = dp[t][a]\n                cand = old + 1  # continue same run\n                if t > 1:\n                    cand = max(cand, best[t-1] + 1)\n                else:\n                    cand = max(cand, 1)\n                if cand > dp[t][a]:\n                    dp[t][a] = cand\n                if dp[t][a] > best[t]:\n                    best[t] = dp[t][a]\n        ans = max(best[1:])\n        out_lines.append(str(ans))\n    sys.stdout.write('\\n'.join(out_lines))\n\nif __name__ == \"__main__\":\n    solve()\n",
      "JavaScript": "const fs = require('fs');\nconst input = fs.readFileSync(0, 'utf-8').trim().split(/\\s+/).map(Number);\nlet idx = 0;\nconst T = input[idx++];\nlet outputs = [];\nconst MAXV = 1001;\nfor (let tc = 0; tc < T; ++tc) {\n  const N = input[idx++];\n  const K = input[idx++];\n  const A = input.slice(idx, idx + N);\n  idx += N;\n  const runs = K + 1;\n  const dp = Array.from({ length: runs + 1 }, () => new Int32Array(MAXV));\n  const best = new Int32Array(runs + 1);\n  for (const a of A) {\n    for (let t = runs; t >= 1; --t) {\n      const old = dp[t][a];\n      let cand = old + 1; // continue same run\n      if (t > 1) cand = Math.max(cand, best[t - 1] + 1);\n      else cand = Math.max(cand, 1);\n      if (cand > dp[t][a]) dp[t][a] = cand;\n      if (dp[t][a] > best[t]) best[t] = dp[t][a];\n    }\n  }\n  let ans = 0;\n  for (let t = 1; t <= runs; ++t) if (best[t] > ans) ans = best[t];\n  outputs.push(String(ans));\n}\nconsole.log(outputs.join('\\n'));\n"
    }
  },
  "QBCategory": "CCD",
  "boilerPlates": {
    "C": "#include <stdio.h>\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "C++": "#include <iostream>\nusing namespace std;\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "Java": "import java.util.*;\nimport java.io.*;\n\npublic class Main {\n    public static void main(String[] args) throws IOException {\n        // your code goes here\n    }\n}",
    "Python3": "# your code goes here\n",
    "JavaScript": "// your code goes here\n"
  },
  "_internal": {
    "methodName": "",
    "params": []
  }
}