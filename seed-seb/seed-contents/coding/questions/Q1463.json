{
  "questionId": "Q1463",
  "title": "Calculate MST weight",
  "slug": "calculate-mst-weight",
  "version": 1,
  "createdAt": "2026-07-01",
  "updatedAt": "2026-07-04",
  "metadata": {
    "category": "Graphs",
    "difficulty": "Easy",
    "isPremium": false,
    "tags": [
      "Prim's Algorithm",
      "Graphs",
      "Minimum Spanning Tree"
    ],
    "companyTags": [],
    "topicTags": [
      "Graphs"
    ]
  },
  "content": {
    "problemStatement": "You are given an undirected weighted graph it has NNN vertices and MMM edges. \n\nYou are tasked to find the weight of the Minimum Spanning Tree of the graph.\n\nIt is guaranteed that all the vertices in the graph are connected\n\n\nInput Format\n\nThe first line of input will contain a two integers NNN and MMM, denoting the number of vertices and edges.\nNext MMM lines describes the edges. i\u2212thi-thi\u2212th of these MMM lines contains three space-separated integers uiu_iui\u200b, viv_ivi\u200b and wiw_iwi\u200b denoting there exists an edge between uiu_iui\u200b and viv_ivi\u200b with weight wiw_iwi\u200b.\n\n\nOutput Format\nFor each test case, output on a new line the weight of the Minimum Spanning Tree of the graph.",
    "constraints": [
      "1 \u2264 N \u2264 2 * 10^5",
      "2 \u2264 M \u2264 min(2 * 10^5, N * (N - 1) / 2)",
      "1 \u2264 u, v \u2264 N",
      "1 \u2264 w \u2264 10^9",
      "It is guaranteed that the input contains a simple graph with no self-edges."
    ],
    "inputFormat": "The first line contains two integers N and M, denoting the number of vertices and edges. Next M lines describe the edges. The i-th of these M lines contains three space-separated integers u, v, and w, denoting that there exists an edge between u and v with weight w.",
    "outputFormat": "For each test case, output on a new line the weight of the Minimum Spanning Tree of the graph.",
    "sampleTestCases": [
      {
        "id": "sample_1",
        "input": "6 8\n1 2 4\n1 3 4\n2 3 2\n3 4 3 \n3 5 2\n3 6 4\n4 6 3 \n5 6 3",
        "output": "14",
        "explanation": "The minimum spanning tree of the given graph has a total weight of 14."
      },
      {
        "id": "sample_2",
        "input": "4 5\n1 2 1\n1 3 2\n2 3 3\n2 4 4\n3 4 5",
        "output": "8",
        "explanation": "The minimum spanning tree of the given graph has a total weight of 8."
      },
      {
        "id": "sample_3",
        "input": "5 7\n1 2 1\n1 3 2\n2 3 3\n2 4 4\n3 4 5\n3 5 6\n4 5 7",
        "output": "12",
        "explanation": "The minimum spanning tree of the given graph has a total weight of 12."
      }
    ]
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "label": "minimum boundary",
        "input": "2 1\n1 2 1",
        "expectedOutput": "1",
        "weight": 2
      },
      {
        "id": "tc_2",
        "label": "maximum boundary",
        "input": "200000 199999\n1 2 1\n1 3 2\n2 3 3\n2 4 4\n3 4 5\n3 5 6\n4 5 7",
        "expectedOutput": "999999950000",
        "weight": 2
      },
      {
        "id": "tc_3",
        "label": "uniform/edge case",
        "input": "5 10\n1 2 1\n1 3 1\n2 3 1\n2 4 1\n3 4 1\n3 5 1\n4 5 1\n1 4 1\n1 5 1\n2 5 1",
        "expectedOutput": "4",
        "weight": 2
      },
      {
        "id": "tc_4",
        "label": "tricky case",
        "input": "4 6\n1 2 1\n1 3 2\n2 3 3\n2 4 4\n3 4 5\n1 4 6",
        "expectedOutput": "6",
        "weight": 2
      },
      {
        "id": "tc_5",
        "label": "stress/general",
        "input": "100 4950\n1 2 1\n1 3 2\n2 3 3\n2 4 4\n3 4 5\n3 5 6\n4 5 7\n1 4 8\n1 5 9\n2 5 10",
        "expectedOutput": "4949",
        "weight": 2
      }
    ],
    "totalTestCases": 5,
    "totalWeight": 10
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      2,
      2,
      2,
      2,
      2
    ],
    "maxScore": 10
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "The problem can be solved using Prim's algorithm, which is a greedy algorithm that finds a minimum spanning tree for a connected weighted undirected graph. The key insight is to start with an arbitrary vertex and then iteratively add the minimum-weight edge that connects a vertex in the tree to a vertex not yet in the tree.",
    "timeComplexity": "O(M log N) per test case, where M is the number of edges and N is the number of vertices. This is because we use a priority queue to select the minimum-weight edge at each step, and the priority queue operations take O(log N) time.",
    "spaceComplexity": "O(N + M), where N is the number of vertices and M is the number of edges. This is because we need to store the adjacency list representation of the graph, as well as the minimum spanning tree.",
    "code": {
      "C": "#include <stdio.h>\n#include <stdlib.h>\n#include <stdbool.h>\n#include <limits.h>\n\n#define N 200010\n\ntypedef struct {\n    int to, weight;\n} Edge;\n\ntypedef struct {\n    Edge *edges;\n    int size, capacity;\n} AdjList;\n\nbool *vis;\nAdjList *adj;\n\n// Priority queue structure for edges\ntypedef struct {\n    int weight, vertex;\n} PQNode;\n\ntypedef struct {\n    PQNode *nodes;\n    int size, capacity;\n} PriorityQueue;\n\nvoid pq_init(PriorityQueue *pq, int capacity) {\n    pq->nodes = (PQNode *)malloc(capacity * sizeof(PQNode));\n    pq->size = 0;\n    pq->capacity = capacity;\n}\n\nvoid pq_push(PriorityQueue *pq, int weight, int vertex) {\n    if (pq->size == pq->capacity) {\n        pq->capacity *= 2;\n        pq->nodes = (PQNode *)realloc(pq->nodes, pq->capacity * sizeof(PQNode));\n    }\n    int i = pq->size++;\n    while (i > 0) {\n        int p = (i - 1) / 2;\n        if (pq->nodes[p].weight <= weight) break;\n        pq->nodes[i] = pq->nodes[p];\n        i = p;\n    }\n    pq->nodes[i].weight = weight;\n    pq->nodes[i].vertex = vertex;\n}\n\nPQNode pq_pop(PriorityQueue *pq) {\n    PQNode ret = pq->nodes[0];\n    PQNode tmp = pq->nodes[--pq->size];\n    int i = 0;\n    while (i * 2 + 1 < pq->size) {\n        int left = i * 2 + 1, right = i * 2 + 2;\n        int j = left;\n        if (right < pq->size && pq->nodes[right].weight < pq->nodes[left].weight) {\n            j = right;\n        }\n        if (pq->nodes[j].weight >= tmp.weight) break;\n        pq->nodes[i] = pq->nodes[j];\n        i = j;\n    }\n    pq->nodes[i] = tmp;\n    return ret;\n}\n\nvoid adjlist_init(AdjList *list) {\n    list->edges = (Edge *)malloc(4 * sizeof(Edge));\n    list->size = 0;\n    list->capacity = 4;\n}\n\nvoid adjlist_add(AdjList *list, int to, int weight) {\n    if (list->size == list->capacity) {\n        list->capacity *= 2;\n        list->edges = (Edge *)realloc(list->edges, list->capacity * sizeof(Edge));\n    }\n    list->edges[list->size].to = to;\n    list->edges[list->size].weight = weight;\n    list->size++;\n}\n\nvoid prims(int source, int n) { // Source is the starting vertex\n    long long ans = 0;  // Total weight of the MST\n    PriorityQueue pq;\n    pq_init(&pq, n);\n\n    pq_push(&pq, 0, source);\n\n    while (pq.size > 0) {\n        PQNode node = pq_pop(&pq);\n        int v = node.vertex;\n\n        if (vis[v]) {\n            continue;\n        }\n\n        ans += node.weight;  // Add the edge weight to answer\n        vis[v] = true;  // Mark vertex as processed\n\n        for (int i = 0; i < adj[v].size; i++) {\n            Edge edge = adj[v].edges[i];\n            if (!vis[edge.to]) {\n                pq_push(&pq, edge.weight, edge.to);\n            }\n        }\n    }\n\n    printf(\"%lld\\n\", ans);\n\n    free(pq.nodes);\n}\n\nint main() {\n    int t;\n    scanf(\"%d\", &t);\n    while (t--) {\n        int n, m;\n        scanf(\"%d %d\", &n, &m);\n\n        // Allocate memory for vis array and adjacency list\n        vis = (bool *)malloc((n + 1) * sizeof(bool));\n        adj = (AdjList *)malloc((n + 1) * sizeof(AdjList));\n\n        // Initialize vis array and adjacency list\n        for (int i = 1; i <= n; i++) {\n            vis[i] = false;\n            adjlist_init(&adj[i]);\n        }\n\n        // Read edges\n        for (int i = 0; i < m; i++) {\n            int x, y, w;\n            scanf(\"%d %d %d\", &x, &y, &w);\n            adjlist_add(&adj[x], y, w);\n            adjlist_add(&adj[y], x, w);\n        }\n\n        prims(1, n);\n\n        // Free allocated memory\n        free(vis);\n        for (int i = 1; i <= n; i++) {\n            free(adj[i].edges);\n        }\n        free(adj);\n    }\n\n    return 0;\n}",
      "C++": "#include <bits/stdc++.h>\nusing namespace std;\n\nconst int N = 2e5 + 10;\nvector<bool> vis(N, false);\nvector<pair<int,int>> adj[N];\n\nvoid prims(int source, int n) { \n    long long ans = 0; // Total weight of the MST\n\n    priority_queue<vector<int>, vector<vector<int>>, greater<vector<int>>> pq;\n\n    pq.push({0, source}); \n    \n    while(pq.size() != 0) {\n        vector<int> node = pq.top();\n        pq.pop();\n\n        int v = node[1];\n\n        if(vis[v]){\n            continue;\n        }\n\n        ans += node[0]; // Add the edge weight to answer\n\n        vis[v] = 1; // Mark vertex as processed\n\n        for(auto edge: adj[v]) {\n            if(!vis[edge.first]){\n                pq.push({edge.second, edge.first});   \n            }\n        }\n    }      \n\n    cout << ans << \"\\n\"; \n}\n\n\nint main() {\n    ios::sync_with_stdio(false);\n    cin.tie(NULL);\n    int t;\n    cin >> t;\n    while (t--) {\n        int n, m;\n        cin >> n >> m;\n\n        for (int i = 0; i < m; i++) {\n            int x, y, w;\n            cin >> x >> y >> w;\n            adj[x].push_back({y, w});\n            adj[y].push_back({x, w});\n        }\n\n        prims(1, n);\n\n        for (int i = 1; i <= n; i++) {\n            adj[i].clear();\n        }\n    }\n\n    return 0;\n}",
      "Java": "import java.util.*;\n\nclass Main {\n\n    static final int N = 200010;\n    static ArrayList<Boolean> vis = new ArrayList<>(N);\n    static ArrayList<ArrayList<Pair>> adj = new ArrayList<>(N);\n\n    static class Pair {\n        int first, second;\n\n        Pair(int first, int second) {\n            this.first = first;\n            this.second = second;\n        }\n    }\n\n    static void prims(int source, int n) { // Source is the starting vertex\n        // Stores weight, newly added vertex and parent vertex\n        PriorityQueue<int[]> pq = new PriorityQueue<>((a, b) -> Integer.compare(a[0], b[0]));\n\n        long ans = 0;\n\n        pq.add(new int[]{0, source}); // push the starting vertex to the pq\n\n        while (!pq.isEmpty()) {\n            int[] node = pq.poll();\n\n            int v = node[1];\n\n            // If vertex is already processed, continue\n            if (vis.get(v)) {\n                continue;\n            }\n\n            ans = ans + node[0];\n\n            vis.set(v, true); // Mark vertex as processed\n\n            for (Pair edge : adj.get(v)) {\n                if (!vis.get(edge.first)) {\n                    pq.add(new int[]{edge.second, edge.first});\n                }\n            }\n        }\n\n        System.out.println(ans);\n\n    }\n\n    public static void main(String[] args) {\n        Scanner scanner = new Scanner(System.in);\n        int t = scanner.nextInt();\n        while (t-- > 0) {\n            for (int i = 0; i < N; i++) {\n                vis.add(false);\n                adj.add(new ArrayList<>());\n            }\n            int n = scanner.nextInt();\n            int m = scanner.nextInt();\n\n            for (int i = 0; i < m; i++) {\n                int x = scanner.nextInt();\n                int y = scanner.nextInt();\n                int w = scanner.nextInt();\n                adj.get(x).add(new Pair(y, w));\n                adj.get(y).add(new Pair(x, w));\n            }\n\n            prims(1, n);\n        }\n    }\n}",
      "Python3": "from heapq import heappush, heappop\n\ndef minimum_spanning_tree_weight(n, edges):\n    graph = {i: [] for i in range(1, n+1)}\n\n    for u, v, w in edges:\n        graph[u].append((v, w))\n        graph[v].append((u, w))\n\n    visited = set()\n    min_heap = [(0, 1)]  # (weight, vertex)\n\n    min_mst_weight = 0\n\n    while min_heap:\n        weight, current_vertex = heappop(min_heap)\n\n        if current_vertex not in visited:\n            visited.add(current_vertex)\n            min_mst_weight += weight\n\n            for neighbor, edge_weight in graph[current_vertex]:\n                if neighbor not in visited:\n                    heappush(min_heap, (edge_weight, neighbor))\n\n    return min_mst_weight\n\n# Read input\nimport sys\ninput = sys.stdin.readline\n\nt = int(input())\nwhile t > 0:\n    n, m = map(int, input().split())\n    edges = [tuple(map(int, input().split())) for _ in range(m)]\n\n    # Calculate and print the minimum MST weight\n    result = minimum_spanning_tree_weight(n, edges)\n    print(result)\n    t -= 1",
      "JavaScript": "const fs = require('fs');\nconst lines = fs.readFileSync(0, 'utf-8').trim().split('\\n');\nlet idx = 0;\n\nfunction readline() {\n    return lines[idx++];\n}\n\nfunction prims(n, m, edges) {\n    let ans = 0;\n    let vis = new Array(n + 1).fill(false);\n    let adj = new Array(n + 1).fill(0).map(() => []);\n\n    for (let i = 0; i < m; i++) {\n        let [u, v, w] = edges[i];\n        adj[u].push([v, w]);\n        adj[v].push([u, w]);\n    }\n\n    let pq = [[0, 1]];\n\n    while (pq.length > 0) {\n        pq.sort((a, b) => a[0] - b[0]);\n        let [weight, v] = pq.shift();\n\n        if (vis[v]) {\n            continue;\n        }\n\n        ans += weight;\n        vis[v] = true;\n\n        for (let [u, w] of adj[v]) {\n            if (!vis[u]) {\n                pq.push([w, u]);\n            }\n        }\n    }\n\n    return ans;\n}\n\nlet t = parseInt(readline());\nwhile (t-- > 0) {\n    let [n, m] = readline().split(' ').map(Number);\n    let edges = [];\n    for (let i = 0; i < m; i++) {\n        edges.push(readline().split(' ').map(Number));\n    }\n\n    console.log(prims(n, m, edges));\n}"
    }
  }
}