{
  "questionId": "Q9516",
  "title": "First To Last",
  "slug": "first-to-last",
  "version": 1,
  "createdAt": "2026-07-02",
  "updatedAt": "2026-07-04",
  "metadata": {
    "category": "Graph",
    "difficulty": "Hard",
    "isPremium": true,
    "tags": [
      "Shortest Path",
      "LIS",
      "Greedy",
      "Binary Search",
      "Sorting"
    ],
    "companyTags": [],
    "topicTags": [
      "Graphs"
    ],
    "QBCategory": "CCD"
  },
  "content": {
    "problemStatement": "Chef has a grid of size N×MN \\times MN×M.\n\nIn one move, Chef can move from his current cell to an adjacent cell. If the Chef is currently at (i,j)(i, j)(i,j) then, in one move, Chef can move to (i+1,j),(i−1,j),(i,j+1)(i + 1, j), (i - 1, j), (i, j + 1)(i+1,j),(i−1,j),(i,j+1) or (i,j−1)(i, j - 1)(i,j−1).\n\nThere are also KKK special cells in the grid. These special cells support a corner move as well. If the Chef is currently at a special cell (i,j)(i, j)(i,j) then, in one move, Chef can move to (i+1,j+1),(i−1,j−1),(i+1,j−1)(i + 1, j + 1), (i - 1, j - 1), (i + 1, j - 1)(i+1,j+1),(i−1,j−1),(i+1,j−1) or (i−1,j+1)(i - 1, j + 1)(i−1,j+1) in addition to the adjacent cells ((i+1,j),(i−1,j),(i,j+1)(i + 1, j), (i - 1, j), (i, j + 1)(i+1,j),(i−1,j),(i,j+1) or (i,j−1)(i, j - 1)(i,j−1)).\n\nChef is not allowed to leave the grid at any moment. Determine the **minimum** number of moves Chef needs to go from (1,1)(1, 1)(1,1) to (N,M)(N, M)(N,M).\n\n\nInput Format\n\nThe first line contains a single integer TTT — the number of test cases. Then the test cases follow.\nThe first line of each test case contains three space-separated integers NNN, MMM and KKK — the number of rows in the grid, the number of columns in the grid and the number of special cells.\nKKK lines follow. The iii-th line of these KKK lines contains two space-separated integers XiX_iXi​ and YiY_iYi​ such that (Xi,Yi)(X_i, Y_i)(Xi​,Yi​) is the iii-th special cell.\n\n\nOutput Format\nFor each test case, output the minimum number of moves required by the Chef to go to (N,M)(N, M)(N,M) from (1,1)(1, 1)(1,1).",
    "constraints": [
      "1 ≤ T ≤ 10^4",
      "1 ≤ N, M ≤ 10^9",
      "0 ≤ K ≤ 2·10^5",
      "1 ≤ Xi ≤ N",
      "1 ≤ Yi ≤ M",
      "All special cells are distinct.",
      "Sum of K over all test cases ≤ 2·10^5"
    ],
    "inputFormat": "The first line contains an integer T, the number of test cases. Then T test cases follow. Each test case starts with three integers N, M and K – the number of rows, columns and special cells. The next K lines each contain two integers Xi Yi describing a special cell (Xi, Yi).",
    "outputFormat": "For each test case output a single line containing the minimum number of moves required for Chef to travel from (1,1) to (N,M).",
    "sampleTestCases": [
      {
        "id": "sample_1",
        "input": "2\n3 3 0\n3 3 2\n2 2\n2 3",
        "output": "4\n3",
        "explanation": "Explanation:\nTest Case 1: There is no special cell in this case. One of the optimal ways is: (1,1)→(1,2)→(1,3)→(2,3)→(3,3)(1,1) \\rightarrow (1,2) \\rightarrow (1,3) \\rightarrow (2,3) \\rightarrow (3,3)(1,1)→(1,2)→(1,3)→(2,3)→(3,3). Therefore a total of 444 moves is required.\nTest Case 2: There are 222 special cells in this case. One of the optimal ways is: (1,1)→(2,1)→(2,2)→(3,3)(1,1) \\rightarrow (2,1) \\rightarrow (2,2) \\rightarrow (3,3)(1,1)→(2,1)→(2,2)→(3,3). Therefore a total of 333 moves is required."
      }
    ]
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "label": "Case 1",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_2",
        "label": "Case 2",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_3",
        "label": "Case 3",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_4",
        "label": "Case 4",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_5",
        "label": "Case 5",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      }
    ],
    "totalTestCases": 5,
    "totalWeight": 10
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      2,
      2,
      2,
      2,
      2
    ],
    "maxScore": 10
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "Moving orthogonally (up, down, left, right) always costs one move. A diagonal move (down‑right) replaces two orthogonal moves, therefore each diagonal saves exactly one step. Diagonal moves are allowed only when Chef starts them from a special cell. Consequently the problem becomes: maximise the number of down‑right diagonal moves that can be performed on a monotone path from (1,1) to (N,M). After a diagonal from (x,y) Chef lands at (x+1,y+1). From there he can walk orthogonally to any cell with coordinates ≥ (x+1, y+1). Hence we only need a sequence of special cells (x1,y1), (x2,y2), … such that 1 ≤ x1 < x2 < … < N and 1 ≤ y1 < y2 < … < M. The longest possible number of diagonals equals the length of the longest strictly increasing subsequence (LIS) in two dimensions among the special cells that lie strictly inside the grid (x < N and y < M). The answer is (N‑1)+(M‑1) – LIS_length.\n\nAlgorithm per test case:\n1. Read N, M, K.\n2. Keep only specials with x < N and y < M.\n3. Sort the remaining specials by x ascending, and for equal x by y ascending.\n4. Run a classic O(K log K) LIS on the y‑coordinates using a vector `dp` where `dp[i]` is the smallest possible tail y of an increasing subsequence of length i+1. For each y, find the lower_bound position in `dp` and replace it (lower_bound gives the first element ≥ y, guaranteeing strict increase).\n5. Let L = dp.size() (the LIS length). Output (N+M‑2) – L.\n\nCorrectness follows from the observation that any feasible path can be transformed into a monotone path that uses only down‑right diagonals, and the set of starting cells of those diagonals must be a strictly increasing chain. Conversely, any such chain can be realised by walking orthogonally between consecutive specials and taking a diagonal at each of them.\n\nComplexities: Sorting O(K log K) and LIS O(K log K). Memory O(K).",
    "timeComplexity": "O(K log K) per test case (K is the number of special cells in that test).",
    "spaceComplexity": "O(K) for storing the filtered specials and the LIS helper array.",
    "code": {
      "C": "#include <stdio.h>\n#include <stdlib.h>\n\ntypedef struct {int x; int y;} Cell;\n\nint cmp(const void *a, const void *b) {\n    Cell *c1 = (Cell *)a, *c2 = (Cell *)b;\n    if (c1->x != c2->x) return c1->x - c2->x;\n    return c1->y - c2->y;\n}\n\nint lower_bound(int *arr, int size, int val) {\n    int l = 0, r = size; // [l, r)\n    while (l < r) {\n        int m = (l + r) >> 1;\n        if (arr[m] >= val) r = m; else l = m + 1;\n    }\n    return l;\n}\n\nint main() {\n    int T; if (scanf(\"%d\", &T)!=1) return 0;\n    while (T--) {\n        long long N, M; int K; scanf(\"%lld %lld %d\", &N, &M, &K);\n        Cell *tmp = (Cell *)malloc(sizeof(Cell) * K);\n        int cnt = 0;\n        for (int i = 0; i < K; ++i) {\n            int x, y; scanf(\"%d %d\", &x, &y);\n            if ((long long)x < N && (long long)y < M) {\n                tmp[cnt].x = x; tmp[cnt].y = y; ++cnt;\n            }\n        }\n        qsort(tmp, cnt, sizeof(Cell), cmp);\n        int *dp = (int *)malloc(sizeof(int) * cnt);\n        int len = 0;\n        for (int i = 0; i < cnt; ++i) {\n            int pos = lower_bound(dp, len, tmp[i].y);\n            if (pos == len) dp[len++] = tmp[i].y; else dp[pos] = tmp[i].y;\n        }\n        long long ans = (N - 1) + (M - 1) - len;\n        printf(\"%lld\\n\", ans);\n        free(tmp);\n        free(dp);\n    }\n    return 0;\n}",
      "C++": "#include <bits/stdc++.h>\nusing namespace std;\n\nint main(){\n    ios::sync_with_stdio(false);\n    cin.tie(nullptr);\n    int T; if(!(cin>>T)) return 0;\n    while(T--){\n        long long N,M; int K; cin>>N>>M>>K;\n        vector<pair<int,int>> v; v.reserve(K);\n        for(int i=0;i<K;++i){\n            int x,y;cin>>x>>y;\n            if((long long)x<N && (long long)y<M) v.emplace_back(x,y);\n        }\n        sort(v.begin(), v.end(), [](auto &a, auto &b){\n            if(a.first!=b.first) return a.first<b.first;\n            return a.second<b.second;\n        });\n        vector<int> dp;\n        for(auto &p: v){\n            int y=p.second;\n            auto it=lower_bound(dp.begin(), dp.end(), y);\n            if(it==dp.end()) dp.push_back(y);\n            else *it=y;\n        }\n        long long ans = (N-1)+(M-1)- (long long)dp.size();\n        cout<<ans<<\"\\n\";\n    }\n    return 0;\n}",
      "Java": "import java.io.*;\nimport java.util.*;\n\npublic class Main {\n    static class FastScanner {\n        private final InputStream in;\n        private final byte[] buffer = new byte[1 << 16];\n        private int ptr = 0, len = 0;\n        FastScanner(InputStream in) { this.in = in; }\n        private int readByte() throws IOException {\n            if (ptr >= len) {\n                len = in.read(buffer);\n                ptr = 0;\n                if (len <= 0) return -1;\n            }\n            return buffer[ptr++];\n        }\n        long nextLong() throws IOException {\n            int c; do { c = readByte(); } while (c <= ' ');\n            boolean neg = false;\n            if (c == '-') { neg = true; c = readByte(); }\n            long val = 0;\n            while (c > ' ') {\n                val = val * 10 + (c - '0');\n                c = readByte();\n            }\n            return neg ? -val : val;\n        }\n        int nextInt() throws IOException { return (int) nextLong(); }\n    }\n    public static void main(String[] args) throws Exception {\n        FastScanner fs = new FastScanner(System.in);\n        StringBuilder sb = new StringBuilder();\n        int T = fs.nextInt();\n        while (T-- > 0) {\n            long N = fs.nextLong();\n            long M = fs.nextLong();\n            int K = fs.nextInt();\n            List<int[]> list = new ArrayList<>(K);\n            for (int i = 0; i < K; ++i) {\n                int x = fs.nextInt();\n                int y = fs.nextInt();\n                if ((long) x < N && (long) y < M) list.add(new int[]{x, y});\n            }\n            list.sort((a, b) -> {\n                if (a[0] != b[0]) return Integer.compare(a[0], b[0]);\n                return Integer.compare(a[1], b[1]);\n            });\n            int[] dp = new int[list.size()];\n            int len = 0;\n            for (int[] p : list) {\n                int y = p[1];\n                int pos = Arrays.binarySearch(dp, 0, len, y);\n                if (pos < 0) pos = -pos - 1;\n                dp[pos] = y;\n                if (pos == len) len++;\n            }\n            long ans = (N - 1) + (M - 1) - len;\n            sb.append(ans).append('\\n');\n        }\n        System.out.print(sb.toString());\n    }\n}",
      "Python3": "import sys\n\ndef solve():\n    data = sys.stdin.buffer.read().split()\n    it = iter(data)\n    t = int(next(it))\n    out_lines = []\n    for _ in range(t):\n        N = int(next(it)); M = int(next(it)); K = int(next(it))\n        specials = []\n        for __ in range(K):\n            x = int(next(it)); y = int(next(it))\n            if x < N and y < M:\n                specials.append((x, y))\n        specials.sort(key=lambda p: (p[0], p[1]))\n        dp = []\n        import bisect\n        for _, y in specials:\n            pos = bisect.bisect_left(dp, y)\n            if pos == len(dp):\n                dp.append(y)\n            else:\n                dp[pos] = y\n        ans = (N - 1) + (M - 1) - len(dp)\n        out_lines.append(str(ans))\n    sys.stdout.write('\\n'.join(out_lines))\n\nif __name__ == \"__main__\":\n    solve()\n",
      "JavaScript": "const fs = require('fs');\nconst input = fs.readFileSync(0, 'utf-8').trim().split(/\\s+/).map(Number);\nlet idx = 0;\nconst T = input[idx++];\nlet out = [];\nfor (let tc = 0; tc < T; ++tc) {\n  const N = input[idx++];\n  const M = input[idx++];\n  const K = input[idx++];\n  let specials = [];\n  for (let i = 0; i < K; ++i) {\n    const x = input[idx++];\n    const y = input[idx++];\n    if (x < N && y < M) specials.push([x, y]);\n  }\n  specials.sort((a, b) => a[0] === b[0] ? a[1] - b[1] : a[0] - b[0]);\n  const dp = [];\n  for (const [, y] of specials) {\n    // binary search lower_bound\n    let l = 0, r = dp.length;\n    while (l < r) {\n      const m = (l + r) >> 1;\n      if (dp[m] >= y) r = m; else l = m + 1;\n    }\n    if (l === dp.length) dp.push(y); else dp[l] = y;\n  }\n  const ans = (N - 1) + (M - 1) - dp.length;\n  out.push(String(ans));\n}\nconsole.log(out.join('\\n'));\n"
    }
  },
  "QBCategory": "CCD",
  "boilerPlates": {
    "C": "#include <stdio.h>\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "C++": "#include <iostream>\nusing namespace std;\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "Java": "import java.util.*;\nimport java.io.*;\n\npublic class Main {\n    public static void main(String[] args) throws IOException {\n        // your code goes here\n    }\n}",
    "Python3": "# your code goes here\n",
    "JavaScript": "// your code goes here\n"
  },
  "_internal": {
    "methodName": "",
    "params": []
  }
}