{
  "questionId": "Q3996",
  "title": "Masha and Minions",
  "slug": "masha-and-minions",
  "version": 1,
  "createdAt": "2026-07-02",
  "updatedAt": "2026-07-04",
  "metadata": {
    "category": "Greedy",
    "difficulty": "Hard",
    "isPremium": false,
    "tags": [
      "Minion Selection",
      "Sorting",
      "Greedy"
    ],
    "companyTags": [],
    "topicTags": [
      "Binary Search"
    ]
  },
  "content": {
    "problemStatement": "Masha has NNN minions. Each minion has two characteristics: power aaa and endurance bbb. Masha thinks that a non-empty set of minions {m1,m2,\u2026,mk}\\{m_1, m_2, \\dots, m_k\\}{m1\u200b,m2\u200b,\u2026,mk\u200b} with characteristics (am1,bm1),(am2,bm2),\u2026,(amk,bmk)(a_{m_1},b_{m_1}), (a_{m_2},b_{m_2}), \\dots, (a_{m_k},b_{m_k})(am1\u200b\u200b,bm1\u200b\u200b),(am2\u200b\u200b,bm2\u200b\u200b),\u2026,(amk\u200b\u200b,bmk\u200b\u200b) is good if the mean endurance of this set doesn't exceed the minimum power in it, i.e. if min(am1,am2,\u2026,amk)\u2265(bm1+bm2+\u22ef+bmk)/kmin(a_{m_1}, a_{m_2}, \\dots, a_{m_k}) \\ge (b_{m_1}+b_{m_2}+\\dots+b_{m_k}) / kmin(am1\u200b\u200b,am2\u200b\u200b,\u2026,amk\u200b\u200b)\u2265(bm1\u200b\u200b+bm2\u200b\u200b+\u22ef+bmk\u200b\u200b)/k.\n\nMasha would like to choose a good subset of her minions and give these minions to Mark. Your task is to calculate the maximum number of minions Masha can give to Mark.",
    "constraints": [
      "1 <= T <= 1000",
      "1 <= N <= 4*10^5",
      "1 <= a, b <= 10^9",
      "The sum of N for all test cases does not exceed 4*10^5"
    ],
    "inputFormat": "The first line of the input contains a single integer T denoting the number of test cases. The description of T test cases follows. The first line of each test case contains a single integer N. The following N lines describe minions. Each of these lines contains two space-separated integers a and b denoting the power and endurance of one minion.",
    "outputFormat": "For each test case, print a single line containing one number \u2014 the size of the largest good set of minions, or 0 if no such set exists.",
    "sampleTestCases": [
      {
        "id": "sample_1",
        "input": "2\n3\n1 4\n3 3\n2 1\n2\n3 5\n1 4",
        "output": "2\n0",
        "explanation": "For the first test case, the set of minions {2,3} is good, since min(3,2) >= (3+1)/2. For the second test case, there is no non-empty good subset of minions."
      }
    ]
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "label": "minimum boundary",
        "input": "1\n1\n1 1",
        "expectedOutput": "1",
        "weight": 2
      },
      {
        "id": "tc_2",
        "label": "maximum boundary",
        "input": "1\n4\n10 1\n10 1\n10 1\n10 1",
        "expectedOutput": "4",
        "weight": 2
      },
      {
        "id": "tc_3",
        "label": "edge case",
        "input": "1\n2\n1 1\n1 2",
        "expectedOutput": "1",
        "weight": 2
      },
      {
        "id": "tc_4",
        "label": "general case 1",
        "input": "1\n3\n2 1\n3 2\n1 3",
        "expectedOutput": "2",
        "weight": 2
      },
      {
        "id": "tc_5",
        "label": "general case 2",
        "input": "1\n4\n4 1\n3 2\n2 3\n1 4",
        "expectedOutput": "2",
        "weight": 2
      }
    ],
    "totalTestCases": 5,
    "totalWeight": 10
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      2,
      2,
      2,
      2,
      2
    ],
    "maxScore": 10
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "To solve this problem, we first sort the minions based on their power in descending order. Then, we iterate over the sorted list and for each minion, we check if it can be added to the current set of selected minions without violating the condition. If it can, we add it to the set and update the total endurance of the set. We keep track of the maximum size of the set that can be achieved.",
    "timeComplexity": "O(T*N*log(N)) \u2014 due to sorting the minions for each test case",
    "spaceComplexity": "O(N) \u2014 for storing the minions",
    "code": {
      "C": "#include <stdio.h>\n#include <stdlib.h>\n\n// Structure to represent a minion\ntypedef struct {\n    int power;\n    int endurance;\n} Minion;\n\n// Comparison function for sorting minions based on power\nint compareMinions(const void *a, const void *b) {\n    Minion *m1 = (Minion *)a;\n    Minion *m2 = (Minion *)b;\n    return m2->power - m1->power;\n}\n\nint main() {\n    int T;\n    scanf(\"%d\", &T);\n    while (T--) {\n        int N;\n        scanf(\"%d\", &N);\n        Minion minions[N];\n        for (int i = 0; i < N; i++) {\n            scanf(\"%d %d\", &minions[i].power, &minions[i].endurance);\n        }\n        qsort(minions, N, sizeof(Minion), compareMinions);\n        int maxSize = 0;\n        for (int i = 0; i < N; i++) {\n            int totalEndurance = 0;\n            int setSize = 0;\n            for (int j = i; j < N; j++) {\n                if (minions[j].power >= (totalEndurance + minions[j].endurance) / (setSize + 1.0)) {\n                    totalEndurance += minions[j].endurance;\n                    setSize++;\n                }\n            }\n            maxSize = (maxSize > setSize) ? maxSize : setSize;\n        }\n        printf(\"%d\\n\", maxSize);\n    }\n    return 0;\n}",
      "C++": "#include <iostream>\n#include <algorithm>\nusing namespace std;\n\nstruct Minion {\n    int power;\n    int endurance;\n};\n\nbool compareMinions(const Minion &m1, const Minion &m2) {\n    return m2.power > m1.power;\n}\n\nint main() {\n    int T;\n    cin >> T;\n    while (T--) {\n        int N;\n        cin >> N;\n        Minion minions[N];\n        for (int i = 0; i < N; i++) {\n            cin >> minions[i].power >> minions[i].endurance;\n        }\n        sort(minions, minions + N, compareMinions);\n        int maxSize = 0;\n        for (int i = 0; i < N; i++) {\n            int totalEndurance = 0;\n            int setSize = 0;\n            for (int j = i; j < N; j++) {\n                if (minions[j].power >= (totalEndurance + minions[j].endurance) / (double)(setSize + 1)) {\n                    totalEndurance += minions[j].endurance;\n                    setSize++;\n                }\n            }\n            maxSize = max(maxSize, setSize);\n        }\n        cout << maxSize << endl;\n    }\n    return 0;\n}",
      "Java": "import java.util.Arrays;\nimport java.util.Scanner;\n\npublic class Main {\n    static class Minion implements Comparable<Minion> {\n        int power;\n        int endurance;\n        Minion(int power, int endurance) {\n            this.power = power;\n            this.endurance = endurance;\n        }\n        @Override\n        public int compareTo(Minion other) {\n            return Integer.compare(other.power, this.power);\n        }\n    }\n    public static void main(String[] args) {\n        Scanner scanner = new Scanner(System.in);\n        int T = scanner.nextInt();\n        while (T-- > 0) {\n            int N = scanner.nextInt();\n            Minion[] minions = new Minion[N];\n            for (int i = 0; i < N; i++) {\n                minions[i] = new Minion(scanner.nextInt(), scanner.nextInt());\n            }\n            Arrays.sort(minions);\n            int maxSize = 0;\n            for (int i = 0; i < N; i++) {\n                int totalEndurance = 0;\n                int setSize = 0;\n                for (int j = i; j < N; j++) {\n                    if (minions[j].power >= (double)(totalEndurance + minions[j].endurance) / (setSize + 1)) {\n                        totalEndurance += minions[j].endurance;\n                        setSize++;\n                    }\n                }\n                maxSize = Math.max(maxSize, setSize);\n            }\n            System.out.println(maxSize);\n        }\n    }\n}",
      "Python3": "import sys\n\nclass Minion:\n    def __init__(self, power, endurance):\n        self.power = power\n        self.endurance = endurance\n\n    def __lt__(self, other):\n        return self.power < other.power\n\nT = int(input())\nfor _ in range(T):\n    N = int(input())\n    minions = []\n    for _ in range(N):\n        power, endurance = map(int, input().split())\n        minions.append(Minion(power, endurance))\n    minions.sort(reverse=True)\n    max_size = 0\n    for i in range(N):\n        total_endurance = 0\n        set_size = 0\n        for j in range(i, N):\n            if minions[j].power >= (total_endurance + minions[j].endurance) / (set_size + 1):\n                total_endurance += minions[j].endurance\n                set_size += 1\n        max_size = max(max_size, set_size)\n    print(max_size)",
      "JavaScript": "const fs = require('fs');\nconst lines = fs.readFileSync(0, 'utf-8').trim().split('\\n');\nlet idx = 0;\n\nfunction nextInt() {\n    return parseInt(lines[idx++]);\n}\n\nfunction nextLine() {\n    return lines[idx++];\n}\n\nclass Minion {\n    constructor(power, endurance) {\n        this.power = power;\n        this.endurance = endurance;\n    }\n}\n\nlet T = nextInt();\nwhile (T-- > 0) {\n    let N = nextInt();\n    let minions = [];\n    for (let i = 0; i < N; i++) {\n        let [power, endurance] = nextLine().split(' ').map(Number);\n        minions.push(new Minion(power, endurance));\n    }\n    minions.sort((a, b) => b.power - a.power);\n    let maxSize = 0;\n    for (let i = 0; i < N; i++) {\n        let totalEndurance = 0;\n        let setSize = 0;\n        for (let j = i; j < N; j++) {\n            if (minions[j].power >= (totalEndurance + minions[j].endurance) / (setSize + 1)) {\n                totalEndurance += minions[j].endurance;\n                setSize++;\n            }\n        }\n        maxSize = Math.max(maxSize, setSize);\n    }\n    console.log(maxSize);\n}"
    }
  }
}