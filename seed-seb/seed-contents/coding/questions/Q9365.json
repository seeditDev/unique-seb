{
  "questionId": "Q9365",
  "title": "Largest Rectangle in Histogram",
  "slug": "largest-rectangle-in-histogram",
  "version": 1,
  "createdAt": "2026-07-02",
  "updatedAt": "2026-07-04",
  "metadata": {
    "category": "Data Structures",
    "difficulty": "Medium",
    "isPremium": true,
    "tags": [
      "Stack",
      "Monotonic Stack",
      "Histogram",
      "Largest Rectangle"
    ],
    "companyTags": [],
    "topicTags": [
      "Stacks & Queues"
    ],
    "QBCategory": "CCD"
  },
  "content": {
    "problemStatement": "You are given an array A1,A2,…,ANA_1, A_2, \\dots, A_NA1​,A2​,…,AN​ of length NNN. AAA represents a histogram which means the height of the ithi^{th}ith bar will be AiA_iAi​ units and the width of each bar is 111 unit.\n\nFind the area of the largest rectangle in the histogram.\n\nFunction DeclarationFunction NamelargestRectangleArealargestRectangleArealargestRectangleArea – This function calculates the maximum rectangular area possible within the given histogram.\n\nParametersNNN : An integer representing the number of bars in the histogram.\n\nAAA : An array of integers representing the heights of the histogram bars.\n\nReturn ValueReturns an integer: the area of the largest rectangle that can be formed in the histogram.",
    "constraints": [
      "1 <= T <= 10",
      "1 <= N <= 100000",
      "0 <= Ai <= 1000000000"
    ],
    "inputFormat": "The first line contains an integer T, the number of test cases. For each test case, the first line contains an integer N, the number of histogram bars. The second line contains N space‑separated integers A1 … AN, the heights of the bars.",
    "outputFormat": "For each test case output a single line containing the maximum rectangular area that can be formed in the given histogram.",
    "sampleTestCases": [
      {
        "id": "sample_1",
        "input": "3\n4\n2 6 8 4\n4\n5 10 5 2\n2\n5 5",
        "output": "12\n15\n10",
        "explanation": "Test case 1: bars 2 and 3 give min height 6, width 2 → area 12.\\nTest case 2: bars 1‑3 give min height 5, width 3 → area 15.\\nTest case 3: both bars have height 5, width 2 → area 10."
      },
      {
        "id": "sample_2",
        "input": "1\n1\n5",
        "output": "5",
        "explanation": "Only one bar of height 5, width 1 → area 5."
      },
      {
        "id": "sample_3",
        "input": "1\n3\n2 1 2",
        "output": "3",
        "explanation": "The whole histogram (height 1, width 3) gives the maximum area 3."
      }
    ]
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "label": "minimum boundary",
        "input": "1\n1\n0",
        "expectedOutput": "0",
        "weight": 2
      },
      {
        "id": "tc_2",
        "label": "maximum boundary",
        "input": "1\n15\n1000000000 1000000000 1000000000 1000000000 1000000000 1000000000 1000000000 1000000000 1000000000 1000000000 1000000000 1000000000 1000000000 1000000000 1000000000",
        "expectedOutput": "15000000000",
        "weight": 2
      },
      {
        "id": "tc_3",
        "label": "edge case",
        "input": "1\n5\n2 0 2 2 2",
        "expectedOutput": "6",
        "weight": 2
      },
      {
        "id": "tc_4",
        "label": "general case 1",
        "input": "1\n7\n6 2 5 4 5 1 6",
        "expectedOutput": "12",
        "weight": 2
      },
      {
        "id": "tc_5",
        "label": "general case 2",
        "input": "1\n8\n1 2 3 4 5 6 7 8",
        "expectedOutput": "20",
        "weight": 2
      }
    ],
    "totalTestCases": 5,
    "totalWeight": 10
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      2,
      2,
      2,
      2,
      2
    ],
    "maxScore": 10
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "For each test case we scan the heights from left to right while maintaining a monotonic increasing stack of indices. When the current height is lower than the height at the stack top, we pop the stack, compute the area with the popped height as the smallest bar, and update the answer. The width of that rectangle is (current index) - (index of new stack top) - 1, or current index if the stack becomes empty. After processing all bars we pop remaining indices using N as the right boundary. This yields the maximal rectangle in O(N) time.",
    "timeComplexity": "O(N) per test case, where N is the number of bars.",
    "spaceComplexity": "O(N) for the stack (worst case when heights are strictly increasing).",
    "code": {
      "C": "#include <stdio.h>\n#include <stdlib.h>\n\nlong long maxRectangle(int *h, int n) {\n    int *stack = (int*)malloc(n * sizeof(int));\n    int top = -1;\n    long long maxArea = 0;\n    for (int i = 0; i < n; ++i) {\n        while (top >= 0 && h[stack[top]] > h[i]) {\n            int height = h[stack[top--]];\n            int width = (top == -1) ? i : i - stack[top] - 1;\n            long long area = (long long)height * width;\n            if (area > maxArea) maxArea = area;\n        }\n        stack[++top] = i;\n    }\n    while (top >= 0) {\n        int height = h[stack[top--]];\n        int width = (top == -1) ? n : n - stack[top] - 1;\n        long long area = (long long)height * width;\n        if (area > maxArea) maxArea = area;\n    }\n    free(stack);\n    return maxArea;\n}\n\nint main() {\n    int T; if (scanf(\"%d\", &T) != 1) return 0;\n    while (T--) {\n        int N; scanf(\"%d\", &N);\n        int *arr = (int*)malloc(N * sizeof(int));\n        for (int i = 0; i < N; ++i) scanf(\"%d\", &arr[i]);\n        printf(\"%lld\\n\", maxRectangle(arr, N));\n        free(arr);\n    }\n    return 0;\n}",
      "C++": "#include <bits/stdc++.h>\nusing namespace std;\n\nlong long largestRectangle(const vector<long long>& h) {\n    int n = h.size();\n    vector<int> st;\n    long long ans = 0;\n    for (int i = 0; i < n; ++i) {\n        while (!st.empty() && h[st.back()] > h[i]) {\n            long long height = h[st.back()];\n            st.pop_back();\n            int left = st.empty() ? -1 : st.back();\n            long long width = i - left - 1;\n            ans = max(ans, height * width);\n        }\n        st.push_back(i);\n    }\n    while (!st.empty()) {\n        long long height = h[st.back()];\n        st.pop_back();\n        int left = st.empty() ? -1 : st.back();\n        long long width = n - left - 1;\n        ans = max(ans, height * width);\n    }\n    return ans;\n}\n\nint main() {\n    ios::sync_with_stdio(false);\n    cin.tie(nullptr);\n    int T; if(!(cin>>T)) return 0;\n    while (T--) {\n        int N; cin>>N;\n        vector<long long> a(N);\n        for (int i=0;i<N;++i) cin>>a[i];\n        cout<<largestRectangle(a)<<\"\\n\";\n    }\n    return 0;\n}",
      "Java": "import java.io.*;\nimport java.util.*;\n\npublic class Main {\n    private static long largestRectangle(long[] h) {\n        int n = h.length;\n        Deque<Integer> stack = new ArrayDeque<>();\n        long ans = 0;\n        for (int i = 0; i < n; i++) {\n            while (!stack.isEmpty() && h[stack.peek()] > h[i]) {\n                long height = h[stack.pop()];\n                int left = stack.isEmpty() ? -1 : stack.peek();\n                long width = i - left - 1;\n                ans = Math.max(ans, height * width);\n            }\n            stack.push(i);\n        }\n        while (!stack.isEmpty()) {\n            long height = h[stack.pop()];\n            int left = stack.isEmpty() ? -1 : stack.peek();\n            long width = n - left - 1;\n            ans = Math.max(ans, height * width);\n        }\n        return ans;\n    }\n\n    public static void main(String[] args) throws Exception {\n        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));\n        StringTokenizer st;\n        int T = Integer.parseInt(br.readLine().trim());\n        StringBuilder sb = new StringBuilder();\n        while (T-- > 0) {\n            int N = Integer.parseInt(br.readLine().trim());\n            long[] arr = new long[N];\n            st = new StringTokenizer(br.readLine());\n            for (int i = 0; i < N; i++) arr[i] = Long.parseLong(st.nextToken());\n            sb.append(largestRectangle(arr)).append('\\n');\n        }\n        System.out.print(sb.toString());\n    }\n}",
      "Python3": "import sys\n\ndef largest_rectangle(heights):\n    stack = []\n    max_area = 0\n    for i, h in enumerate(heights):\n        while stack and heights[stack[-1]] > h:\n            height = heights[stack.pop()]\n            left = stack[-1] if stack else -1\n            width = i - left - 1\n            max_area = max(max_area, height * width)\n        stack.append(i)\n    n = len(heights)\n    while stack:\n        height = heights[stack.pop()]\n        left = stack[-1] if stack else -1\n        width = n - left - 1\n        max_area = max(max_area, height * width)\n    return max_area\n\ndef main():\n    data = sys.stdin.read().strip().split()\n    if not data:\n        return\n    it = iter(data)\n    T = int(next(it))\n    out = []\n    for _ in range(T):\n        N = int(next(it))\n        arr = [int(next(it)) for _ in range(N)]\n        out.append(str(largest_rectangle(arr)))\n    sys.stdout.write('\\n'.join(out))\n\nif __name__ == \"__main__\":\n    main()",
      "JavaScript": "const fs = require('fs');\nconst input = fs.readFileSync(0, 'utf-8').trim().split(/\\s+/);\nlet idx = 0;\nconst T = Number(input[idx++]);\nlet results = [];\nfunction largestRectangle(heights) {\n  const stack = [];\n  let maxArea = 0;\n  for (let i = 0; i < heights.length; i++) {\n    while (stack.length && heights[stack[stack.length - 1]] > heights[i]) {\n      const height = heights[stack.pop()];\n      const left = stack.length ? stack[stack.length - 1] : -1;\n      const width = i - left - 1;\n      const area = height * width;\n      if (area > maxArea) maxArea = area;\n    }\n    stack.push(i);\n  }\n  const n = heights.length;\n  while (stack.length) {\n    const height = heights[stack.pop()];\n    const left = stack.length ? stack[stack.length - 1] : -1;\n    const width = n - left - 1;\n    const area = height * width;\n    if (area > maxArea) maxArea = area;\n  }\n  return maxArea;\n}\nfor (let t = 0; t < T; t++) {\n  const N = Number(input[idx++]);\n  const arr = [];\n  for (let i = 0; i < N; i++) arr.push(Number(input[idx++]));\n  results.push(String(largestRectangle(arr)));\n}\nconsole.log(results.join('\\n'));"
    }
  },
  "QBCategory": "CCD",
  "boilerPlates": {
    "C": "#include <stdio.h>\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "C++": "#include <iostream>\nusing namespace std;\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "Java": "import java.util.*;\nimport java.io.*;\n\npublic class Main {\n    public static void main(String[] args) throws IOException {\n        // your code goes here\n    }\n}",
    "Python3": "# your code goes here\n",
    "JavaScript": "// your code goes here\n"
  },
  "_internal": {
    "methodName": "",
    "params": []
  }
}