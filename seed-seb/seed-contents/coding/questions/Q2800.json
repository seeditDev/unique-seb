{
  "questionId": "Q2800",
  "title": "Chef and the Cake II",
  "slug": "chef-and-the-cake-ii",
  "version": 1,
  "createdAt": "2026-07-01",
  "updatedAt": "2026-07-04",
  "metadata": {
    "category": "Math",
    "difficulty": "Hard",
    "isPremium": false,
    "tags": [
      "Rectilinear Polygon",
      "Maximal Area",
      "Non-Overlapping Pieces",
      "Math"
    ],
    "companyTags": [],
    "topicTags": [
      "Flow & Matching"
    ]
  },
  "content": {
    "problemStatement": "Your friend Chef has made a cake for you on your birthday. Before having the cake you two have made some cuts on the cake. If you consider the cake as a rectangular grid with co-ordinates from (**0**,**0**) to (**n**,**n**), then each of the cut can be considered as a rectilinear polygon (a polygon with sides parallel to the axis and possibly self-intersecting). These cuts turn the cake into several pieces. Now you two want to distribute the pieces among yourself. Since it is your birthday, your friend has given you a choice of choosing some pieces for yourself. You are hungry and you want to have the whole cake. But you don't want to make your friend suspicious. So instead of having all the pieces you want to select the set of pieces such that no pair of pieces have a common side. You want to maximize the total amount of cakes you can have. This amount can be measured as the sum of the areas of the top surface for each piece (or the amount of cake in each piece is the area of the polygon that define that piece).",
    "constraints": [
      "1 <= T <= 50",
      "1 <= n <= 10000",
      "1 <= m <= 20"
    ],
    "inputFormat": "The first line contains an integer T, the number of test cases. Each test case starts with two integers n and m, where n is the size of the cake and m is the number of cuts. Then, for each cut, there are 4 integers x1, y1, x2, y2, representing the coordinates of the two points that define the cut. The input is terminated by a newline.",
    "outputFormat": "For each test case, output the maximum total area of the cake pieces that can be chosen such that no pair of pieces have a common side.",
    "sampleTestCases": [
      {
        "id": "sample_1",
        "input": "",
        "output": "Input:\n2\n5 1\n4 1 1 1 4 4 4 4 1\n5 2\n4 1 1 1 3 3 3 3 1\n4 2 2 2 4 4 4 4 2\nOutput:\r\n16\r\n19",
        "explanation": ""
      }
    ]
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "label": "Case 1",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_2",
        "label": "Case 2",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_3",
        "label": "Case 3",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_4",
        "label": "Case 4",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_5",
        "label": "Case 5",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      }
    ],
    "totalTestCases": 5,
    "totalWeight": 10
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      2,
      2,
      2,
      2,
      2
    ],
    "maxScore": 10
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "To solve this problem, we can use a greedy approach. First, we need to find all the pieces of the cake after the cuts. Then, we sort the pieces by their areas in descending order. Finally, we iterate over the sorted pieces and choose the pieces that do not have a common side with any previously chosen piece.",
    "timeComplexity": "O(T * m * n) \u2014 the time complexity is dominated by the iteration over all the pieces of the cake for each test case.",
    "spaceComplexity": "O(T * m * n) \u2014 the space complexity is dominated by the storage of all the pieces of the cake for each test case.",
    "code": {
      "C": "// COMPLETE, COMPILABLE C code\n#include <stdio.h>\n#include <stdlib.h>\n#include <string.h>\n#define MAX_N 10000\n#define MAX_M 20\n#define MAX_P 80\n\n// Structure to represent a piece of the cake\ntypedef struct {\n    int x1, y1, x2, y2;\n    int area;\n} Piece;\n\n// Function to calculate the area of a piece\nint calculateArea(Piece piece) {\n    return abs(piece.x2 - piece.x1) * abs(piece.y2 - piece.y1);\n}\n\n// Function to check if two pieces have a common side\nint haveCommonSide(Piece piece1, Piece piece2) {\n    // Check if the pieces have a common side\n    if (piece1.x1 == piece2.x1 && piece1.x2 == piece2.x2 &&\n        (piece1.y1 <= piece2.y1 && piece2.y1 <= piece1.y2 ||\n         piece2.y1 <= piece1.y1 && piece1.y1 <= piece2.y2)) {\n        return 1;\n    }\n    if (piece1.y1 == piece2.y1 && piece1.y2 == piece2.y2 &&\n        (piece1.x1 <= piece2.x1 && piece2.x1 <= piece1.x2 ||\n         piece2.x1 <= piece1.x1 && piece1.x1 <= piece2.x2)) {\n        return 1;\n    }\n    return 0;\n}\n\n// Function to solve the problem\nvoid solve() {\n    int T, n, m, i, j, k;\n    scanf(\"%d\", &T);\n    while (T--) {\n        scanf(\"%d %d\", &n, &m);\n        Piece pieces[MAX_P];\n        int numPieces = 0;\n        // Generate all possible pieces\n        for (i = 0; i < n; i++) {\n            for (j = 0; j < n; j++) {\n                // Check if the current piece is not cut by any line\n                int isValid = 1;\n                for (k = 0; k < m; k++) {\n                    int x1, y1, x2, y2;\n                    scanf(\"%d %d %d %d\", &x1, &y1, &x2, &y2);\n                    if (x1 == i && y1 == j && x2 == i && y2 == j) {\n                        isValid = 0;\n                        break;\n                    }\n                }\n                if (isValid) {\n                    Piece piece;\n                    piece.x1 = i;\n                    piece.y1 = j;\n                    piece.x2 = i;\n                    piece.y2 = j;\n                    piece.area = calculateArea(piece);\n                    pieces[numPieces++] = piece;\n                }\n            }\n        }\n        // Sort the pieces by their areas in descending order\n        for (i = 0; i < numPieces; i++) {\n            for (j = i + 1; j < numPieces; j++) {\n                if (pieces[i].area < pieces[j].area) {\n                    Piece temp = pieces[i];\n                    pieces[i] = pieces[j];\n                    pieces[j] = temp;\n                }\n            }\n        }\n        int maxArea = 0;\n        // Choose the pieces that do not have a common side with any previously chosen piece\n        for (i = 0; i < numPieces; i++) {\n            int isValid = 1;\n            for (j = 0; j < i; j++) {\n                if (haveCommonSide(pieces[i], pieces[j])) {\n                    isValid = 0;\n                    break;\n                }\n            }\n            if (isValid) {\n                maxArea += pieces[i].area;\n            }\n        }\n        printf(\"%d\\n\", maxArea);\n    }\n}",
      "C++": "// COMPLETE, COMPILABLE C++ code\n#include <iostream>\n#include <vector>\n#include <algorithm>\nusing namespace std;\n#define MAX_N 10000\n#define MAX_M 20\n#define MAX_P 80\n\n// Structure to represent a piece of the cake\nstruct Piece {\n    int x1, y1, x2, y2;\n    int area;\n};\n\n// Function to calculate the area of a piece\nint calculateArea(Piece piece) {\n    return abs(piece.x2 - piece.x1) * abs(piece.y2 - piece.y1);\n}\n\n// Function to check if two pieces have a common side\nint haveCommonSide(Piece piece1, Piece piece2) {\n    // Check if the pieces have a common side\n    if (piece1.x1 == piece2.x1 && piece1.x2 == piece2.x2 &&\n        (piece1.y1 <= piece2.y1 && piece2.y1 <= piece1.y2 ||\n         piece2.y1 <= piece1.y1 && piece1.y1 <= piece2.y2)) {\n        return 1;\n    }\n    if (piece1.y1 == piece2.y1 && piece1.y2 == piece2.y2 &&\n        (piece1.x1 <= piece2.x1 && piece2.x1 <= piece1.x2 ||\n         piece2.x1 <= piece1.x1 && piece1.x1 <= piece2.x2)) {\n        return 1;\n    }\n    return 0;\n}\n\n// Function to solve the problem\nvoid solve() {\n    int T, n, m, i, j, k;\n    cin >> T;\n    while (T--) {\n        cin >> n >> m;\n        vector<Piece> pieces;\n        // Generate all possible pieces\n        for (i = 0; i < n; i++) {\n            for (j = 0; j < n; j++) {\n                // Check if the current piece is not cut by any line\n                int isValid = 1;\n                for (k = 0; k < m; k++) {\n                    int x1, y1, x2, y2;\n                    cin >> x1 >> y1 >> x2 >> y2;\n                    if (x1 == i && y1 == j && x2 == i && y2 == j) {\n                        isValid = 0;\n                        break;\n                    }\n                }\n                if (isValid) {\n                    Piece piece;\n                    piece.x1 = i;\n                    piece.y1 = j;\n                    piece.x2 = i;\n                    piece.y2 = j;\n                    piece.area = calculateArea(piece);\n                    pieces.push_back(piece);\n                }\n            }\n        }\n        // Sort the pieces by their areas in descending order\n        sort(pieces.begin(), pieces.end(), [](Piece a, Piece b) {\n            return a.area > b.area;\n        });\n        int maxArea = 0;\n        // Choose the pieces that do not have a common side with any previously chosen piece\n        for (i = 0; i < pieces.size(); i++) {\n            int isValid = 1;\n            for (j = 0; j < i; j++) {\n                if (haveCommonSide(pieces[i], pieces[j])) {\n                    isValid = 0;\n                    break;\n                }\n            }\n            if (isValid) {\n                maxArea += pieces[i].area;\n            }\n        }\n        cout << maxArea << endl;\n    }\n}",
      "Java": "// COMPLETE, COMPILABLE Java code\nimport java.util.Scanner;\nimport java.util.ArrayList;\nimport java.util.Collections;\npublic class Main {\n    public static void main(String[] args) {\n        Scanner scanner = new Scanner(System.in);\n        int T = scanner.nextInt();\n        while (T-- > 0) {\n            int n = scanner.nextInt();\n            int m = scanner.nextInt();\n            ArrayList<Piece> pieces = new ArrayList<>();\n            // Generate all possible pieces\n            for (int i = 0; i < n; i++) {\n                for (int j = 0; j < n; j++) {\n                    // Check if the current piece is not cut by any line\n                    boolean isValid = true;\n                    for (int k = 0; k < m; k++) {\n                        int x1 = scanner.nextInt();\n                        int y1 = scanner.nextInt();\n                        int x2 = scanner.nextInt();\n                        int y2 = scanner.nextInt();\n                        if (x1 == i && y1 == j && x2 == i && y2 == j) {\n                            isValid = false;\n                            break;\n                        }\n                    }\n                    if (isValid) {\n                        Piece piece = new Piece(i, j, i, j);\n                        pieces.add(piece);\n                    }\n                }\n            }\n            // Sort the pieces by their areas in descending order\n            Collections.sort(pieces, (a, b) -> Integer.compare(b.getArea(), a.getArea()));\n            int maxArea = 0;\n            // Choose the pieces that do not have a common side with any previously chosen piece\n            for (int i = 0; i < pieces.size(); i++) {\n                boolean isValid = true;\n                for (int j = 0; j < i; j++) {\n                    if (haveCommonSide(pieces.get(i), pieces.get(j))) {\n                        isValid = false;\n                        break;\n                    }\n                }\n                if (isValid) {\n                    maxArea += pieces.get(i).getArea();\n                }\n            }\n            System.out.println(maxArea);\n        }\n    }\n    // Function to check if two pieces have a common side\n    public static boolean haveCommonSide(Piece piece1, Piece piece2) {\n        // Check if the pieces have a common side\n        if (piece1.getX1() == piece2.getX1() && piece1.getX2() == piece2.getX2() &&\n            (piece1.getY1() <= piece2.getY1() && piece2.getY1() <= piece1.getY2() ||\n             piece2.getY1() <= piece1.getY1() && piece1.getY1() <= piece2.getY2())) {\n            return true;\n        }\n        if (piece1.getY1() == piece2.getY1() && piece1.getY2() == piece2.getY2() &&\n            (piece1.getX1() <= piece2.getX1() && piece2.getX1() <= piece1.getX2() ||\n             piece2.getX1() <= piece1.getX1() && piece1.getX1() <= piece2.getX2())) {\n            return true;\n        }\n        return false;\n    }\n    // Class to represent a piece of the cake\n    public static class Piece {\n        private int x1, y1, x2, y2;\n        public Piece(int x1, int y1, int x2, int y2) {\n            this.x1 = x1;\n            this.y1 = y1;\n            this.x2 = x2;\n            this.y2 = y2;\n        }\n        public int getArea() {\n            return Math.abs(x2 - x1) * Math.abs(y2 - y1);\n        }\n        public int getX1() {\n            return x1;\n        }\n        public int getX2() {\n            return x2;\n        }\n        public int getY1() {\n            return y1;\n        }\n        public int getY2() {\n            return y2;\n        }\n    }\n}",
      "Python3": "# COMPLETE, RUNNABLE Python3 code\nimport sys\ninput = sys.stdin.readline\n\n# Function to calculate the area of a piece\ndef calculateArea(piece):\n    return abs(piece[2] - piece[0]) * abs(piece[3] - piece[1])\n\n# Function to check if two pieces have a common side\ndef haveCommonSide(piece1, piece2):\n    # Check if the pieces have a common side\n    if piece1[0] == piece2[0] and piece1[2] == piece2[2] and (\n        piece1[1] <= piece2[1] and piece2[1] <= piece1[3] or\n        piece2[1] <= piece1[1] and piece1[1] <= piece2[3]\n    ):\n        return True\n    if piece1[1] == piece2[1] and piece1[3] == piece2[3] and (\n        piece1[0] <= piece2[0] and piece2[0] <= piece1[2] or\n        piece2[0] <= piece1[0] and piece1[0] <= piece2[2]\n    ):\n        return True\n    return False\n\n# Function to solve the problem\ndef solve():\n    T = int(input())\n    while T > 0:\n        n, m = map(int, input().split())\n        pieces = []\n        # Generate all possible pieces\n        for i in range(n):\n            for j in range(n):\n                # Check if the current piece is not cut by any line\n                isValid = True\n                for _ in range(m):\n                    x1, y1, x2, y2 = map(int, input().split())\n                    if x1 == i and y1 == j and x2 == i and y2 == j:\n                        isValid = False\n                        break\n                if isValid:\n                    piece = [i, j, i, j]\n                    pieces.append(piece)\n        # Sort the pieces by their areas in descending order\n        pieces.sort(key=calculateArea, reverse=True)\n        maxArea = 0\n        # Choose the pieces that do not have a common side with any previously chosen piece\n        for i in range(len(pieces)):\n            isValid = True\n            for j in range(i):\n                if haveCommonSide(pieces[i], pieces[j]):\n                    isValid = False\n                    break\n            if isValid:\n                maxArea += calculateArea(pieces[i])\n        print(maxArea)\n        T -= 1\n\nsolve()",
      "JavaScript": ""
    }
  }
}