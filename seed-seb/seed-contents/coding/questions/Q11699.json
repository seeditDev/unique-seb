{
  "questionId": "Q11699",
  "title": "Left Rotate Array by K Places",
  "slug": "rotate-array",
  "version": 1,
  "createdAt": "2026-07-05",
  "updatedAt": "2026-07-05",
  "metadata": {
    "category": "Array",
    "difficulty": "Easy",
    "isPremium": true,
    "tags": [
      "Array Rotation",
      "Left Rotation",
      "Right Rotation"
    ],
    "companyTags": [],
    "topicTags": [],
    "QBCategory": "CCD"
  },
  "content": {
    "problemStatement": "Given an array of integers, rotating array of elements by k elements either left or right.",
    "constraints": [
      "1 <= n <= 10^5",
      "1 <= k <= 10^5",
      "-10^9 <= arr[i] <= 10^9"
    ],
    "inputFormat": "The input will contain an array of integers and an integer k. The first line will contain the number of elements in the array (n), the second line will contain the array elements, and the third line will contain the value of k.",
    "outputFormat": "The output will be the array after rotating it to the left or right by k positions.",
    "sampleTestCases": []
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "label": "min boundary",
        "input": "5\n1 2 3 4 5\n1",
        "expectedOutput": "2 3 4 5 1",
        "weight": 2
      },
      {
        "id": "tc_2",
        "label": "max boundary",
        "input": "5\n1 2 3 4 5\n5",
        "expectedOutput": "1 2 3 4 5",
        "weight": 2
      },
      {
        "id": "tc_3",
        "label": "uniform/edge cases",
        "input": "5\n1 1 1 1 1\n2",
        "expectedOutput": "1 1 1 1 1",
        "weight": 2
      },
      {
        "id": "tc_4",
        "label": "tricky case / empty-like / negative if allowed",
        "input": "0\n\n0",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_5",
        "label": "general mixed case",
        "input": "7\n1 2 3 4 5 6 7\n3",
        "expectedOutput": "4 5 6 7 1 2 3",
        "weight": 2
      }
    ],
    "totalTestCases": 5,
    "totalWeight": 10
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      2,
      2,
      2,
      2,
      2
    ],
    "maxScore": 10
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "To solve this problem, we can use a temporary array to store the elements that need to be rotated. Then, we can shift the remaining elements and copy the stored elements to their new positions.",
    "timeComplexity": "O(n)",
    "spaceComplexity": "O(k)",
    "code": {
      "C": "#include <stdio.h>\n\nvoid rotateLeft(int arr[], int n, int k) {\n    int temp[k];\n    for (int i = 0; i < k; i++) {\n        temp[i] = arr[i];\n    }\n    for (int i = k; i < n; i++) {\n        arr[i - k] = arr[i];\n    }\n    for (int i = 0; i < k; i++) {\n        arr[n - k + i] = temp[i];\n    }\n}\n\nvoid rotateRight(int arr[], int n, int k) {\n    int temp[k];\n    for (int i = n - k; i < n; i++) {\n        temp[i - n + k] = arr[i];\n    }\n    for (int i = n - k - 1; i >= 0; i--) {\n        arr[i + k] = arr[i];\n    }\n    for (int i = 0; i < k; i++) {\n        arr[i] = temp[i];\n    }\n}\n\nint main() {\n    int n, k;\n    scanf(\"%d\", &n);\n    int arr[n];\n    for (int i = 0; i < n; i++) {\n        scanf(\"%d\", &arr[i]);\n    }\n    scanf(\"%d\", &k);\n    rotateLeft(arr, n, k);\n    for (int i = 0; i < n; i++) {\n        printf(\"%d \", arr[i]);\n    }\n    return 0;\n}",
      "C++": "#include <bits/stdc++.h>\nusing namespace std;\n\nclass Solution {\npublic:\n    // Function to rotate the array to the right by k positions\n    void rotateRight(int arr[], int n, int k) {\n        if (n == 0) return;\n\n        // Normalize k if greater than n\n        k = k % n;\n\n        // Store last k elements in a temporary array\n        int temp[k];\n        for (int i = n - k; i < n; i++) {\n            temp[i - n + k] = arr[i];\n        }\n\n        // Shift the first n-k elements to the right by k steps\n        for (int i = n - k - 1; i >= 0; i--) {\n            arr[i + k] = arr[i];\n        }\n\n        // Copy back the k elements to the start\n        for (int i = 0; i < k; i++) {\n            arr[i] = temp[i];\n        }\n    }\n\n    // Function to rotate the array to the left by k positions\n    void rotateLeft(int arr[], int n, int k) {\n        if (n == 0) return;\n\n        // Normalize k if greater than n\n        k = k % n;\n\n        // Store first k elements in temporary array\n        int temp[k];\n        for (int i = 0; i < k; i++) {\n            temp[i] = arr[i];\n        }\n\n        // Shift remaining elements to the left by k positions\n        for (int i = k; i < n; i++) {\n            arr[i - k] = arr[i];\n        }\n\n        // Copy back the stored elements to the end\n        for (int i = 0; i < k; i++) {\n            arr[n - k + i] = temp[i];\n        }\n    }\n};\n\n// Driver code\nint main() {\n    Solution sol;\n\n    int arr[] = {1, 2, 3, 4, 5, 6, 7};\n    int n = sizeof(arr) / sizeof(arr[0]);\n    int k = 2;\n\n    sol.rotateRight(arr, n, k);\n    cout << \"Array after right rotation by \" << k << \" steps:\\n\";\n    for (int i = 0; i < n; i++) cout << arr[i] << \" \";\n\n    cout << \"\\n\";\n\n    // Reset array for left rotation demo\n    int arr2[] = {1, 2, 3, 4, 5, 6, 7};\n    sol.rotateLeft(arr2, n, k);\n    cout << \"Array after left rotation by \" << k << \" steps:\\n\";\n    for (int i = 0; i < n; i++) cout << arr2[i] << \" \";\n\n    return 0;\n}",
      "Java": "import java.util.Arrays;\n\nclass Solution {\n    // Rotate the array to the right by k positions\n    public void rotateRight(int[] arr, int k) {\n        int n = arr.length;\n        if (n == 0) return;\n\n        k = k % n;\n\n        // Store last k elements\n        int[] temp = Arrays.copyOfRange(arr, n - k, n);\n\n        // Shift the remaining elements to the right\n        for (int i = n - k - 1; i >= 0; i--) {\n            arr[i + k] = arr[i];\n        }\n\n        // Copy the stored elements to the front\n        for (int i = 0; i < k; i++) {\n            arr[i] = temp[i];\n        }\n    }\n\n    // Rotate the array to the left by k positions\n    public void rotateLeft(int[] arr, int k) {\n        int n = arr.length;\n        if (n == 0) return;\n\n        k = k % n;\n\n        // Store first k elements\n        int[] temp = Arrays.copyOfRange(arr, 0, k);\n\n        // Shift remaining elements to the left\n        for (int i = k; i < n; i++) {\n            arr[i - k] = arr[i];\n        }\n\n        // Copy stored elements to the end\n        for (int i = 0; i < k; i++) {\n            arr[n - k + i] = temp[i];\n        }\n    }\n\n    public static void main(String[] args) {\n        Solution sol = new Solution();\n\n        int[] arr = {1, 2, 3, 4, 5, 6, 7};\n        int k = 2;\n\n        sol.rotateRight(arr, k);\n        System.out.println(\"Array after right rotation: \" + Arrays.toString(arr));\n\n        int[] arr2 = {1, 2, 3, 4, 5, 6, 7};\n        sol.rotateLeft(arr2, k);\n        System.out.println(\"Array after left rotation: \" + Arrays.toString(arr2));\n    }\n}",
      "Python3": "class Solution:\n    # Rotate the array to the right by k positions\n    def rotateRight(self, arr, k):\n        n = len(arr)\n        if n == 0:\n            return\n\n        k %= n\n\n        # Store last k elements\n        temp = arr[-k:]\n\n        # Shift the remaining elements\n        for i in range(n - k - 1, -1, -1):\n            arr[i + k] = arr[i]\n\n        # Copy stored elements to the front\n        for i in range(k):\n            arr[i] = temp[i]\n\n    # Rotate the array to the left by k positions\n    def rotateLeft(self, arr, k):\n        n = len(arr)\n        if n == 0:\n            return\n\n        k %= n\n\n        # Store first k elements\n        temp = arr[:k]\n\n        # Shift remaining elements\n        for i in range(k, n):\n            arr[i - k] = arr[i]\n\n        # Copy stored elements to the end\n        for i in range(k):\n            arr[n - k + i] = temp[i]\n\n\n# Driver code\nsol = Solution()\n\narr = [1, 2, 3, 4, 5, 6, 7]\nk = 2\nsol.rotateRight(arr, k)\nprint(\"Array after right rotation:\", arr)\n\narr2 = [1, 2, 3, 4, 5, 6, 7]\nsol.rotateLeft(arr2, k)\nprint(\"Array after left rotation:\", arr2)",
      "JavaScript": "class Solution {\n    // Rotate the array to the right by k positions\n    rotateRight(arr, k) {\n        const n = arr.length;\n        if (n === 0) return;\n\n        k = k % n;\n\n        // Store last k elements\n        const temp = arr.slice(n - k);\n\n        // Shift remaining elements\n        for (let i = n - k - 1; i >= 0; i--) {\n            arr[i + k] = arr[i];\n        }\n\n        // Copy stored elements to the front\n        for (let i = 0; i < k; i++) {\n            arr[i] = temp[i];\n        }\n    }\n\n    // Rotate the array to the left by k positions\n    rotateLeft(arr, k) {\n        const n = arr.length;\n        if (n === 0) return;\n\n        k = k % n;\n\n        // Store first k elements\n        const temp = arr.slice(0, k);\n\n        // Shift remaining elements\n        for (let i = k; i < n; i++) {\n            arr[i - k] = arr[i];\n        }\n\n        // Copy stored elements to the end\n        for (let i = 0; i < k; i++) {\n            arr[n - k + i] = temp[i];\n        }\n    }\n}\n\n// Driver code\nconst sol = new Solution();\n\nlet arr = [1, 2, 3, 4, 5, 6, 7];\nlet k = 2;\n\nsol.rotateRight(arr, k);\nconsole.log(\"Array after right rotation:\", arr);\n\nlet arr2 = [1, 2, 3, 4, 5, 6, 7];\nsol.rotateLeft(arr2, k);\nconsole.log(\"Array after left rotation:\", arr2);"
    }
  },
  "QBCategory": "CCD",
  "boilerPlates": {
    "C": "#include <stdio.h>\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "C++": "#include <iostream>\nusing namespace std;\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "Java": "import java.util.*;\nimport java.io.*;\n\npublic class Main {\n    public static void main(String[] args) throws IOException {\n        // your code goes here\n    }\n}",
    "Python3": "# your code goes here\n",
    "JavaScript": "// your code goes here\n"
  },
  "_internal": {
    "methodName": "",
    "params": []
  }
}