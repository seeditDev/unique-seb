{
  "questionId": "Q9180",
  "title": "High Frequency",
  "slug": "high-frequency",
  "version": 1,
  "createdAt": "2026-07-02",
  "updatedAt": "2026-07-04",
  "metadata": {
    "category": "Arrays | Greedy",
    "difficulty": "Medium",
    "isPremium": true,
    "tags": [
      "Frequency",
      "Greedy",
      "Hash Map",
      "Counting"
    ],
    "companyTags": [],
    "topicTags": [
      "Data Structures"
    ],
    "QBCategory": "CCD"
  },
  "content": {
    "problemStatement": "Chef has an array AAA of length NNN.\n\nLet F(A)F(A)F(A) denote the maximum frequency of any element present in the array.\n\nFor example:\n\n\nIf A=[1,2,3,2,2,1]A = [1, 2, 3, 2, 2, 1]A=[1,2,3,2,2,1], then F(A)=3F(A) = 3F(A)=3 since element 222 has the highest frequency =3 = 3=3.\nIf A=[1,2,3,3,2,1]A = [1, 2, 3, 3, 2, 1]A=[1,2,3,3,2,1], then F(A)=2F(A) = 2F(A)=2 since highest frequency of any element is 222.\nChef can perform the following operation **at most once**:\n\n\nChoose any subsequence SSS of the array such that every element of SSS is the same, say xxx. Then, choose an integer yyy and replace every element in this subsequence with yyy.\nFor example, let A=[1,2,2,1,2,2]A = [1, 2, 2, 1, 2, 2]A=[1,2,2,1,2,2]. A few examples of the operation that Chef can perform are:\n\n\n[1,2,2,1,2,2]→[1,5,5,1,2,2][1, \\textcolor{red}{2, 2}, 1, 2, 2] \\to [1, \\textcolor{blue}{5, 5}, 1, 2, 2][1,2,2,1,2,2]→[1,5,5,1,2,2]\n[1,2,2,1,2,2]→[1,19,2,1,19,19][1, \\textcolor{red}{2}, 2, 1, \\textcolor{red}{2, 2}] \\to [1, \\textcolor{blue}{19}, 2, 1, \\textcolor{blue}{19, 19}][1,2,2,1,2,2]→[1,19,2,1,19,19]\n[1,2,2,1,2,2]→[2,2,2,1,2,2][\\textcolor{red}{1}, 2, 2, 1, 2, 2] \\to [\\textcolor{blue}{2}, 2, 2, 1, 2, 2][1,2,2,1,2,2]→[2,2,2,1,2,2]\nDetermine the **minimum** possible value of F(A)F(A)F(A) Chef can get by performing the given operation at most once.\n\n\nInput Format\n\nThe first line of input will contain a single integer TTT, denoting the number of test cases.\nEach test case consists of two lines of input.\n\nThe first line of each test case contains a single integer NNN denoting the length of array AAA.\nThe second line contains NNN space-separated integers denoting the array AAA.\n\n\n\n\nOutput Format\nFor each test case, output the minimum value of F(A)F(A)F(A) Chef can get if he performs the operation optimally.",
    "constraints": [
      "1 ≤ T ≤ 5000",
      "2 ≤ N ≤ 10^5",
      "1 ≤ Ai ≤ N",
      "Sum of N over all test cases ≤ 2·10^5"
    ],
    "inputFormat": "The first line contains an integer T, the number of test cases. Then for each test case:\n- A line with integer N, the length of the array.\n- A line with N space‑separated integers A1 … AN.\nAll Ai satisfy 1 ≤ Ai ≤ N.",
    "outputFormat": "For each test case output a single integer – the minimum possible value of F(A) after performing at most one allowed operation.",
    "sampleTestCases": [
      {
        "id": "sample_1",
        "input": "3\n4\n1 2 1 2\n5\n1 1 1 1 1\n6\n1 2 2 1 2 2",
        "output": "2\n3\n2",
        "explanation": "Test case 1: frequencies are {1:2,2:2}. max_f=2, max2=2, answer=max(2,1)=2.\\nTest case 2: only value 1 with frequency 5. max_f=5, max2=0, answer=max(0,3)=3.\\nTest case 3: frequencies {1:2,2:4}. max_f=4, max2=2, answer=max(2,2)=2."
      }
    ]
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "label": "minimum boundary",
        "input": "1\n2\n1 2",
        "expectedOutput": "1",
        "weight": 2
      },
      {
        "id": "tc_2",
        "label": "maximum boundary",
        "input": "1\n5\n1 1 2 2 3",
        "expectedOutput": "2",
        "weight": 2
      },
      {
        "id": "tc_3",
        "label": "edge case – all same with even count",
        "input": "1\n6\n7 7 7 7 7 7",
        "expectedOutput": "3",
        "weight": 2
      },
      {
        "id": "tc_4",
        "label": "general case with two dominant values",
        "input": "1\n8\n4 4 4 4 5 5 5 6",
        "expectedOutput": "4",
        "weight": 2
      },
      {
        "id": "tc_5",
        "label": "general case with many distinct values",
        "input": "1\n9\n1 2 3 4 5 6 7 8 9",
        "expectedOutput": "1",
        "weight": 2
      }
    ],
    "totalTestCases": 5,
    "totalWeight": 10
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      2,
      2,
      2,
      2,
      2
    ],
    "maxScore": 10
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "For each test case count the frequency of every distinct value. Let max_f be the largest frequency and max2 the second largest (0 if only one distinct value exists).\\nIf we choose the value with frequency max_f and change a part of its occurrences to a brand new value, the two resulting frequencies are k and max_f‑k. The best split is k = ⌈max_f/2⌉, giving a maximum frequency of ⌈max_f/2⌉ for this value. All other values keep their original frequencies, the largest among them being max2. Therefore after the optimal operation the answer is max(max2, ⌈max_f/2⌉). If we do not perform any operation the answer would be max_f, which is never smaller than the expression above, so the formula already gives the minimal possible value.\\nAlgorithm per test case:\\n1. Count frequencies using a hash map.\\n2. Find the largest (max_f) and second largest (max2) frequencies.\\n3. Answer = max(max2, (max_f + 1) / 2).\\nComplexities: O(N) time and O(distinct values) memory per test case.",
    "timeComplexity": "O(N) per test case, where N is the length of the array. Overall O(ΣN) ≤ 2·10^5.",
    "spaceComplexity": "O(D) where D is the number of distinct values (≤ N).",
    "code": {
      "C": "#include <stdio.h>\n#include <stdlib.h>\n\ntypedef struct Node {\n    int key;\n    int cnt;\n    struct Node* next;\n} Node;\n\n#define MOD 100003\n\nNode* table[MOD];\n\nunsigned int hash_int(int x) {\n    return ((unsigned int)x * 2654435761u) % MOD;\n}\n\nNode* find(int x) {\n    unsigned int h = hash_int(x);\n    for (Node* p = table[h]; p; p = p->next) if (p->key == x) return p;\n    return NULL;\n}\n\nvoid insert(int x) {\n    unsigned int h = hash_int(x);\n    Node* p = (Node*)malloc(sizeof(Node));\n    p->key = x; p->cnt = 1; p->next = table[h];\n    table[h] = p;\n}\n\nint main(){\n    int T; if(scanf(\"%d\", &T)!=1) return 0;\n    while(T--){\n        int N; scanf(\"%d\", &N);\n        // clear hash table\n        for(int i=0;i<MOD;i++){\n            Node* p = table[i];\n            while(p){ Node* nxt = p->next; free(p); p = nxt; }\n            table[i]=NULL;\n        }\n        int max_f=0, max2=0;\n        for(int i=0;i<N;i++){\n            int x; scanf(\"%d\", &x);\n            Node* nd = find(x);\n            if(nd){ nd->cnt++; }\n            else { insert(x); nd = table[hash_int(x)]; }\n            int c = nd->cnt;\n            if(c > max_f){ max2 = max_f; max_f = c; }\n            else if(c > max2 && c != max_f){ max2 = c; }\n        }\n        int ans = max2;\n        int half = (max_f + 1) / 2;\n        if(half > ans) ans = half;\n        printf(\"%d\\n\", ans);\n    }\n    return 0;\n}\n",
      "C++": "#include <bits/stdc++.h>\nusing namespace std;\nint main(){\n    ios::sync_with_stdio(false);\n    cin.tie(nullptr);\n    int T; if(!(cin>>T)) return 0;\n    while(T--){\n        int N; cin>>N;\n        unordered_map<int,int> freq;\n        freq.reserve(N*2);\n        int max_f=0, max2=0;\n        for(int i=0;i<N;i++){\n            int x; cin>>x;\n            int c = ++freq[x];\n            if(c>max_f){ max2=max_f; max_f=c; }\n            else if(c>max2 && c!=max_f){ max2=c; }\n        }\n        int ans = max(max2, (max_f+1)/2);\n        cout<<ans<<\"\\n\";\n    }\n    return 0;\n}\n",
      "Java": "import java.io.*;\nimport java.util.*;\npublic class Main {\n    public static void main(String[] args) throws Exception {\n        FastScanner fs = new FastScanner(System.in);\n        int T = fs.nextInt();\n        StringBuilder sb = new StringBuilder();\n        while (T-- > 0) {\n            int N = fs.nextInt();\n            HashMap<Integer,Integer> map = new HashMap<>(N*2);\n            int maxF = 0, max2 = 0;\n            for (int i = 0; i < N; i++) {\n                int x = fs.nextInt();\n                int c = map.getOrDefault(x, 0) + 1;\n                map.put(x, c);\n                if (c > maxF) {\n                    max2 = maxF;\n                    maxF = c;\n                } else if (c > max2 && c != maxF) {\n                    max2 = c;\n                }\n            }\n            int ans = Math.max(max2, (maxF + 1) / 2);\n            sb.append(ans).append('\\n');\n        }\n        System.out.print(sb.toString());\n    }\n    // Fast scanner for large input\n    static class FastScanner {\n        private final InputStream in;\n        private final byte[] buffer = new byte[1 << 16];\n        private int ptr = 0, len = 0;\n        FastScanner(InputStream in) { this.in = in; }\n        private int readByte() throws IOException {\n            if (ptr >= len) {\n                len = in.read(buffer);\n                ptr = 0;\n                if (len <= 0) return -1;\n            }\n            return buffer[ptr++];\n        }\n        int nextInt() throws IOException {\n            int c, sign = 1, val = 0;\n            do { c = readByte(); } while (c <= ' ');\n            if (c == '-') { sign = -1; c = readByte(); }\n            while (c > ' ') {\n                val = val * 10 + (c - '0');\n                c = readByte();\n            }\n            return val * sign;\n        }\n    }\n}\n",
      "Python3": "#!/usr/bin/env python3\nimport sys\n\ndef solve():\n    data = sys.stdin.read().split()\n    it = iter(data)\n    t = int(next(it))\n    out_lines = []\n    for _ in range(t):\n        n = int(next(it))\n        freq = {}\n        max_f = 0\n        max2 = 0\n        for _ in range(n):\n            x = int(next(it))\n            c = freq.get(x, 0) + 1\n            freq[x] = c\n            if c > max_f:\n                max2 = max_f\n                max_f = c\n            elif c > max2 and c != max_f:\n                max2 = c\n        ans = max(max2, (max_f + 1) // 2)\n        out_lines.append(str(ans))\n    sys.stdout.write('\\n'.join(out_lines))\n\nif __name__ == \"__main__\":\n    solve()\n",
      "JavaScript": "const fs = require('fs');\nconst input = fs.readFileSync(0, 'utf-8').trim().split(/\\s+/).map(Number);\nlet idx = 0;\nconst T = input[idx++];\nlet outputs = [];\nfor (let tc = 0; tc < T; tc++) {\n  const N = input[idx++];\n  const freq = new Map();\n  let maxF = 0, max2 = 0;\n  for (let i = 0; i < N; i++) {\n    const x = input[idx++];\n    const c = (freq.get(x) || 0) + 1;\n    freq.set(x, c);\n    if (c > maxF) {\n      max2 = maxF;\n      maxF = c;\n    } else if (c > max2 && c !== maxF) {\n      max2 = c;\n    }\n  }\n  const ans = Math.max(max2, Math.floor((maxF + 1) / 2));\n  outputs.push(String(ans));\n}\nconsole.log(outputs.join('\\n'));\n"
    }
  },
  "QBCategory": "CCD",
  "boilerPlates": {
    "C": "#include <stdio.h>\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "C++": "#include <iostream>\nusing namespace std;\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "Java": "import java.util.*;\nimport java.io.*;\n\npublic class Main {\n    public static void main(String[] args) throws IOException {\n        // your code goes here\n    }\n}",
    "Python3": "# your code goes here\n",
    "JavaScript": "// your code goes here\n"
  },
  "_internal": {
    "methodName": "",
    "params": []
  }
}