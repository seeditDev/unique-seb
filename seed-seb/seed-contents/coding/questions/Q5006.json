{
  "questionId": "Q5006",
  "title": "Class Creation using OOPs",
  "slug": "class-creation-using-oops",
  "version": 1,
  "createdAt": "2026-07-01",
  "updatedAt": "2026-07-04",
  "metadata": {
    "category": "Implementation",
    "difficulty": "Medium",
    "isPremium": true,
    "tags": [
      "OOP",
      "Classes",
      "Constructor",
      "String Representation",
      "Method"
    ],
    "companyTags": [],
    "topicTags": [
      "OOP in Python"
    ],
    "QBCategory": "CCD"
  },
  "content": {
    "problemStatement": "Using Python OOPs, create two classes:\n\n\n`Employee`: Represents an employee with attributes like `name` and `salary`.\n`Student`: Represents a student with attributes like `name` and `marks`.\nFor both classes, implement the following:\n\n\nA **constructor** (`__init__`)\nA **method** to display information\nA `__str__()` method for user-friendly string representation\nA `__repr__()` method for formal string representation\n\nInput Format\n\nFirst line of input contains Employee name\nSecond line contains Employee salary (integer)\nThird line contains Student name\nFourth line contains Student marks (integer)\n\n\nOutput Format\n\nPrint `str()` and `repr()` output for both `Employee` and `Student` objects\nCall and print the result of the custom display method for both classes",
    "constraints": [
      "1 ≤ T ≤ 10",
      "Employee salary and Student marks are non‑negative integers that fit in 32‑bit signed int",
      "Names contain only printable ASCII characters and have length at most 100"
    ],
    "inputFormat": "The first line contains an integer T, the number of test cases. For each test case, four lines follow:\n1. Employee name (string, may contain spaces, length ≤ 100)\n2. Employee salary (integer)\n3. Student name (string, may contain spaces, length ≤ 100)\n4. Student marks (integer)",
    "outputFormat": "For each test case output six lines in the exact order:\n1. \"str(Employee): \" followed by str() of the Employee object\n2. \"repr(Employee): \" followed by repr() of the Employee object\n3. The string returned by the Employee custom display method\n4. \"str(Student): \" followed by str() of the Student object\n5. \"repr(Student): \" followed by repr() of the Student object\n6. The string returned by the Student custom display method\nEach line ends with a newline character. No extra blank lines between test cases.",
    "sampleTestCases": [
      {
        "id": "sample_1",
        "input": "1\nAlice\n50000\nBob\n85",
        "output": "str(Employee): Employee Name: Alice, Salary: 50000\nrepr(Employee): Employee('Alice', 50000)\nEmployee Info: Name: Alice | Salary: 50000\nstr(Student): Student Name: Bob, Marks: 85\nrepr(Student): Student('Bob', 85)\nStudent Info: Name: Bob | Marks: 85",
        "explanation": "One test case. Employee object is created with name Alice and salary 50000; its __str__, __repr__ and show_info produce the three lines. Student object is created with name Bob and marks 85; its three representations are printed next."
      }
    ]
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "label": "Case 1",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_2",
        "label": "Case 2",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_3",
        "label": "Case 3",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_4",
        "label": "Case 4",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_5",
        "label": "Case 5",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      }
    ],
    "totalTestCases": 5,
    "totalWeight": 10
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      2,
      2,
      2,
      2,
      2
    ],
    "maxScore": 10
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "Read the integer T. Loop T times reading the four lines for each test case. Create an Employee object with the read name and salary and a Student object with the read name and marks. The classes implement:\n- __init__ (constructor) to store the attributes\n- show_info (custom display method) returning a formatted string\n- __str__ returning a user‑friendly representation\n- __repr__ returning a formal representation suitable for debugging\nAfter creating the objects, print the required six lines using str(), repr() and the custom method. The algorithm is O(T) time and O(1) extra space.",
    "timeComplexity": "O(T) – each test case is processed in constant time.",
    "spaceComplexity": "O(1) – only a few variables are stored regardless of input size.",
    "code": {
      "C": "#include <stdio.h>\n#include <string.h>\n#define MAXLEN 105\ntypedef struct {char name[MAXLEN];int salary;} Employee;\ntypedef struct {char name[MAXLEN];int marks;} Student;\nvoid employee_show_info(const Employee *e,char *out){sprintf(out,\"Employee Info: Name: %s | Salary: %d\",e->name,e->salary);} \nvoid employee_str(const Employee *e,char *out){sprintf(out,\"Employee Name: %s, Salary: %d\",e->name,e->salary);} \nvoid employee_repr(const Employee *e,char *out){sprintf(out,\"Employee('%s', %d)\",e->name,e->salary);} \nvoid student_show_info(const Student *s,char *out){sprintf(out,\"Student Info: Name: %s | Marks: %d\",s->name,s->marks);} \nvoid student_str(const Student *s,char *out){sprintf(out,\"Student Name: %s, Marks: %d\",s->name,s->marks);} \nvoid student_repr(const Student *s,char *out){sprintf(out,\"Student('%s', %d)\",s->name,s->marks);} \nint main(){int T; if(scanf(\"%d\",&T)!=1) return 0; getchar(); for(int tc=0;tc<T;++tc){Employee e;Student s;char buf[MAXLEN];\n    fgets(e.name,MAXLEN,stdin); e.name[strcspn(e.name,\"\\n\")]='\\0';\n    scanf(\"%d\",&e.salary); getchar();\n    fgets(s.name,MAXLEN,stdin); s.name[strcspn(s.name,\"\\n\")]='\\0';\n    scanf(\"%d\",&s.marks); getchar();\n    char out1[256],out2[256],out3[256];\n    employee_str(&e,out1); employee_repr(&e,out2); employee_show_info(&e,out3);\n    printf(\"str(Employee): %s\\n\",out1);\n    printf(\"repr(Employee): %s\\n\",out2);\n    printf(\"%s\\n\",out3);\n    student_str(&s,out1); student_repr(&s,out2); student_show_info(&s,out3);\n    printf(\"str(Student): %s\\n\",out1);\n    printf(\"repr(Student): %s\\n\",out2);\n    printf(\"%s\",out3);\n    if(tc!=T-1) printf(\"\\n\");\n}\nreturn 0;}\n",
      "C++": "#include <bits/stdc++.h>\nusing namespace std;\nclass Employee{public:string name;int salary;Employee(const string& n,int s):name(n),salary(s){};string showInfo() const{return \"Employee Info: Name: \" + name + \" | Salary: \" + to_string(salary);}string toStr() const{return \"Employee Name: \" + name + \", Salary: \" + to_string(salary);}string repr() const{return \"Employee('\" + name + \"', \" + to_string(salary) + \")\";}};\nclass Student{public:string name;int marks;Student(const string& n,int m):name(n),marks(m){};string showInfo() const{return \"Student Info: Name: \" + name + \" | Marks: \" + to_string(marks);}string toStr() const{return \"Student Name: \" + name + \", Marks: \" + to_string(marks);}string repr() const{return \"Student('\" + name + \"', \" + to_string(marks) + \")\";}};\nint main(){ios::sync_with_stdio(false);cin.tie(nullptr);int T; if(!(cin>>T)) return 0; string dummy; getline(cin,dummy);\nfor(int tc=0;tc<T;++tc){string empName,studName;int empSal,studMarks;getline(cin,empName);cin>>empSal;getline(cin,dummy);getline(cin,studName);cin>>studMarks;getline(cin,dummy);\n        Employee e(empName,empSal);Student s(studName,studMarks);\n        cout<<\"str(Employee): \"<<e.toStr()<<'\\n';\n        cout<<\"repr(Employee): \"<<e.repr()<<'\\n';\n        cout<<e.showInfo()<<'\\n';\n        cout<<\"str(Student): \"<<s.toStr()<<'\\n';\n        cout<<\"repr(Student): \"<<s.repr()<<'\\n';\n        cout<<s.showInfo();\n        if(tc!=T-1) cout<<'\\n';\n}\nreturn 0;}\n",
      "Java": "import java.io.*;\npublic class Main{\n    static class Employee{String name;int salary;Employee(String n,int s){name=n;salary=s;}String showInfo(){return \"Employee Info: Name: \" + name + \" | Salary: \" + salary;}@Override public String toString(){return \"Employee Name: \" + name + \", Salary: \" + salary;}String repr(){return \"Employee('\" + name + \"', \" + salary + \")\";}}\n    static class Student{String name;int marks;Student(String n,int m){name=n;marks=m;}String showInfo(){return \"Student Info: Name: \" + name + \" | Marks: \" + marks;}@Override public String toString(){return \"Student Name: \" + name + \", Marks: \" + marks;}String repr(){return \"Student('\" + name + \"', \" + marks + \")\";}}\n    public static void main(String[] args) throws Exception{BufferedReader br=new BufferedReader(new InputStreamReader(System.in));int T=Integer.parseInt(br.readLine().trim());StringBuilder sb=new StringBuilder();for(int tc=0;tc<T;++tc){String empName=br.readLine();int empSal=Integer.parseInt(br.readLine().trim());String studName=br.readLine();int studMarks=Integer.parseInt(br.readLine().trim());Employee e=new Employee(empName,empSal);Student s=new Student(studName,studMarks);sb.append(\"str(Employee): \").append(e.toString()).append('\\n');sb.append(\"repr(Employee): \").append(e.repr()).append('\\n');sb.append(e.showInfo()).append('\\n');sb.append(\"str(Student): \").append(s.toString()).append('\\n');sb.append(\"repr(Student): \").append(s.repr()).append('\\n');sb.append(s.showInfo());if(tc!=T-1) sb.append('\\n');}System.out.print(sb.toString());}}\n",
      "Python3": "# COMPLETE, RUNNABLE Python3 code\nimport sys\nclass Employee:\n    def __init__(self, name, salary):\n        self.name = name\n        self.salary = salary\n    def show_info(self):\n        return f\"Employee Info: Name: {self.name} | Salary: {self.salary}\"\n    def __str__(self):\n        return f\"Employee Name: {self.name}, Salary: {self.salary}\"\n    def __repr__(self):\n        return f\"Employee('{self.name}', {self.salary})\"\nclass Student:\n    def __init__(self, name, marks):\n        self.name = name\n        self.marks = marks\n    def show_info(self):\n        return f\"Student Info: Name: {self.name} | Marks: {self.marks}\"\n    def __str__(self):\n        return f\"Student Name: {self.name}, Marks: {self.marks}\"\n    def __repr__(self):\n        return f\"Student('{self.name}', {self.marks})\"\ndef main():\n    data = sys.stdin.read().splitlines()\n    if not data:\n        return\n    it = iter(data)\n    T = int(next(it).strip())\n    out = []\n    for _ in range(T):\n        emp_name = next(it).rstrip('\\n')\n        emp_salary = int(next(it).strip())\n        stud_name = next(it).rstrip('\\n')\n        stud_marks = int(next(it).strip())\n        emp = Employee(emp_name, emp_salary)\n        stud = Student(stud_name, stud_marks)\n        out.append(f\"str(Employee): {str(emp)}\")\n        out.append(f\"repr(Employee): {repr(emp)}\")\n        out.append(emp.show_info())\n        out.append(f\"str(Student): {str(stud)}\")\n        out.append(f\"repr(Student): {repr(stud)}\")\n        out.append(stud.show_info())\n    sys.stdout.write(\"\\n\".join(out))\nif __name__ == \"__main__\":\n    main()\n",
      "JavaScript": "// COMPLETE, RUNNABLE JavaScript code\nconst fs = require('fs');\nconst lines = fs.readFileSync(0, 'utf-8').trim().split(/\\n/);\nlet idx = 0;\nconst T = parseInt(lines[idx++].trim(), 10);\nlet output = [];\nfor (let tc = 0; tc < T; ++tc) {\n  const empName = lines[idx++];\n  const empSalary = parseInt(lines[idx++].trim(), 10);\n  const studName = lines[idx++];\n  const studMarks = parseInt(lines[idx++].trim(), 10);\n  class Employee {\n    constructor(name, salary) { this.name = name; this.salary = salary; }\n    showInfo() { return `Employee Info: Name: ${this.name} | Salary: ${this.salary}`; }\n    toString() { return `Employee Name: ${this.name}, Salary: ${this.salary}`; }\n    repr() { return `Employee('${this.name}', ${this.salary})`; }\n  }\n  class Student {\n    constructor(name, marks) { this.name = name; this.marks = marks; }\n    showInfo() { return `Student Info: Name: ${this.name} | Marks: ${this.marks}`; }\n    toString() { return `Student Name: ${this.name}, Marks: ${this.marks}`; }\n    repr() { return `Student('${this.name}', ${this.marks})`; }\n  }\n  const emp = new Employee(empName, empSalary);\n  const stud = new Student(studName, studMarks);\n  output.push(`str(Employee): ${emp.toString()}`);\n  output.push(`repr(Employee): ${emp.repr()}`);\n  output.push(emp.showInfo());\n  output.push(`str(Student): ${stud.toString()}`);\n  output.push(`repr(Student): ${stud.repr()}`);\n  output.push(stud.showInfo());\n}\nconsole.log(output.join('\\n'));\n"
    }
  },
  "QBCategory": "CCD",
  "boilerPlates": {
    "C": "#include <stdio.h>\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "C++": "#include <iostream>\nusing namespace std;\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "Java": "import java.util.*;\nimport java.io.*;\n\npublic class Main {\n    public static void main(String[] args) throws IOException {\n        // your code goes here\n    }\n}",
    "Python3": "# your code goes here\n",
    "JavaScript": "// your code goes here\n"
  },
  "_internal": {
    "methodName": "",
    "params": []
  }
}