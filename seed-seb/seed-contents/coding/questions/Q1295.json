{
  "questionId": "Q1295",
  "title": "PreOrder Traversal",
  "slug": "preorder-traversal",
  "version": 1,
  "createdAt": "2026-07-01",
  "updatedAt": "2026-07-04",
  "metadata": {
    "category": "Data Structures",
    "difficulty": "Easy",
    "isPremium": false,
    "tags": [
      "Tree",
      "DFS",
      "Preorder Traversal",
      "Data Structures",
      "Iterative Stack"
    ],
    "companyTags": [],
    "topicTags": [
      "Trees"
    ]
  },
  "content": {
    "problemStatement": "Traversing a binary tree is involves visiting all the nodes in the tree in a specific order. The purpose of tree traversal is to access each node exactly once so that the data within the nodes can be processed. There are several methods to traverse a binary tree.\n\nLet's learn about Pre Order traversal.\n\nPre-order traversal involves visiting the root node first, then recursively visiting the left subtree, and finally the right subtree.\n\nPseudo code:\n\n`void preOrder(node root) {\n    if (root == null) {\n        return\n    }\n    print(root-&gt;value)\n    preOrder(root-&gt;left)\n    preOrder(root-&gt;right)\n}\n`Video Explanation\nFor example, check the preorder traversal of the following binary tree\n\n\nTaskGiven a binary tree, complete the function preOrderTraversal to print the Preorder traversal of the tree.\n\n\nInput Format\n\nThe first line of the input contains a single integer NNN \u2014 the number of nodes in the binary tree.\nNext N\u22121N - 1N\u22121 lines contain three space separated characters pi,ci,Rip_i, c_i, R_ipi\u200b,ci\u200b,Ri\u200b, describing the tree with edge informations - pip_ipi\u200b is an integer denoting the parent node of the iiith edge, cic_ici\u200b is child node, and the letter RiR_iRi\u200b denotes whether the child is left child or right child, it's value is `L` if cic_ici\u200b is left child of pip_ipi\u200b, else `R`.\n\n\nOutput Format\nOutput on the single line, NNN space separated integers - the preorder traversal of the given tree.",
    "constraints": [
      "1 <= T <= 10",
      "1 <= N <= 100000",
      "1 <= pi, ci <= 100000",
      "Ri \u2208 {'L','R'}",
      "All node values are distinct"
    ],
    "inputFormat": "The first line contains an integer T, the number of test cases. For each test case:\n- The first line contains an integer N, the number of nodes in the binary tree.\n- The next N\u20111 lines each contain three space\u2011separated values: pi ci Ri, where pi is the parent node, ci is the child node, and Ri is either 'L' (ci is the left child of pi) or 'R' (ci is the right child of pi). All node values are distinct integers.",
    "outputFormat": "For each test case output a single line containing N space\u2011separated integers \u2013 the preorder traversal of the given tree.",
    "sampleTestCases": [
      {
        "id": "sample_1",
        "input": "5\n1 2 L\n1 3 R\n3 4 L\n3 5 R",
        "output": "1 2 3 4 5",
        "explanation": ""
      }
    ]
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "label": "Case 1",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_2",
        "label": "Case 2",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_3",
        "label": "Case 3",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_4",
        "label": "Case 4",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_5",
        "label": "Case 5",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      }
    ],
    "totalTestCases": 5,
    "totalWeight": 10
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      2,
      2,
      2,
      2,
      2
    ],
    "maxScore": 10
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "For each test case we first reconstruct the tree using two arrays left[] and right[] where left[x] stores the left child of node x (0 if none) and right[x] stores the right child. While reading the edges we also mark every child node. The root is the only node that never appears as a child. After the root is identified we perform an iterative preorder traversal using an explicit stack (push right child first, then left child) so that the left subtree is processed before the right one. The visited nodes are printed in order.",
    "timeComplexity": "O(N) per test case \u2013 each edge is processed once and each node is visited once during the traversal.",
    "spaceComplexity": "O(N) per test case for the left, right and isChild arrays plus the stack (at most O(N) in the worst case of a skewed tree).",
    "code": {
      "C": "#include <stdio.h>\n#include <stdlib.h>\n#include <string.h>\n\nint main() {\n    int T; if (scanf(\"%d\", &T)!=1) return 0;\n    const int MAXV = 100000 + 5;\n    int *left = (int*)calloc(MAXV, sizeof(int));\n    int *right = (int*)calloc(MAXV, sizeof(int));\n    int *isChild = (int*)calloc(MAXV, sizeof(int));\n    while (T--) {\n        int N; scanf(\"%d\", &N);\n        memset(left, 0, MAXV * sizeof(int));\n        memset(right, 0, MAXV * sizeof(int));\n        memset(isChild, 0, MAXV * sizeof(int));\n        int maxNode = 0;\n        for (int i = 0; i < N-1; ++i) {\n            int p, c; char dir;\n            scanf(\"%d %d %c\", &p, &c, &dir);\n            if (dir == 'L') left[p] = c; else right[p] = c;\n            isChild[c] = 1;\n            if (p > maxNode) maxNode = p;\n            if (c > maxNode) maxNode = c;\n        }\n        int root = -1;\n        for (int i = 1; i <= maxNode; ++i) {\n            if ((left[i] || right[i] || isChild[i]) && !isChild[i]) { root = i; break; }\n        }\n        int *stack = (int*)malloc((N+5) * sizeof(int));\n        int top = -1;\n        stack[++top] = root;\n        int first = 1;\n        while (top >= 0) {\n            int node = stack[top--];\n            if (first) { printf(\"%d\", node); first = 0; }\n            else printf(\" %d\", node);\n            if (right[node]) stack[++top] = right[node];\n            if (left[node])  stack[++top] = left[node];\n        }\n        printf(\"\\n\");\n        free(stack);\n    }\n    free(left); free(right); free(isChild);\n    return 0;\n}",
      "C++": "#include <bits/stdc++.h>\nusing namespace std;\n\nint main() {\n    ios::sync_with_stdio(false);\n    cin.tie(nullptr);\n    int T; if(!(cin>>T)) return 0;\n    const int MAXV = 100000 + 5;\n    vector<int> left(MAXV), right(MAXV), isChild(MAXV);\n    while(T--) {\n        int N; cin>>N;\n        fill(left.begin(), left.end(), 0);\n        fill(right.begin(), right.end(), 0);\n        fill(isChild.begin(), isChild.end(), 0);\n        int maxNode = 0;\n        for(int i=0;i<N-1;++i){\n            int p,c; char dir; cin>>p>>c>>dir;\n            if(dir=='L') left[p]=c; else right[p]=c;\n            isChild[c]=1;\n            maxNode = max({maxNode,p,c});\n        }\n        int root=-1;\n        for(int i=1;i<=maxNode;++i){\n            if((left[i]||right[i]||isChild[i]) && !isChild[i]){root=i;break;}\n        }\n        vector<int> st; st.reserve(N);\n        st.push_back(root);\n        bool first=true;\n        while(!st.empty()){\n            int node=st.back(); st.pop_back();\n            if(first){cout<<node;first=false;} else cout<<' '<<node;\n            if(right[node]) st.push_back(right[node]);\n            if(left[node])  st.push_back(left[node]);\n        }\n        cout<<'\\n';\n    }\n    return 0;\n}",
      "Java": "import java.io.*;\nimport java.util.*;\n\npublic class Main {\n    public static void main(String[] args) throws Exception {\n        FastScanner fs = new FastScanner(System.in);\n        int T = fs.nextInt();\n        final int MAXV = 100000 + 5;\n        int[] left = new int[MAXV];\n        int[] right = new int[MAXV];\n        int[] isChild = new int[MAXV];\n        StringBuilder out = new StringBuilder();\n        while (T-- > 0) {\n            int N = fs.nextInt();\n            Arrays.fill(left, 0);\n            Arrays.fill(right, 0);\n            Arrays.fill(isChild, 0);\n            int maxNode = 0;\n            for (int i = 0; i < N - 1; i++) {\n                int p = fs.nextInt();\n                int c = fs.nextInt();\n                char dir = fs.next().charAt(0);\n                if (dir == 'L') left[p] = c; else right[p] = c;\n                isChild[c] = 1;\n                maxNode = Math.max(maxNode, Math.max(p, c));\n            }\n            int root = -1;\n            for (int i = 1; i <= maxNode; i++) {\n                if ((left[i] != 0 || right[i] != 0 || isChild[i] != 0) && isChild[i] == 0) { root = i; break; }\n            }\n            Deque<Integer> stack = new ArrayDeque<>();\n            stack.push(root);\n            boolean first = true;\n            while (!stack.isEmpty()) {\n                int node = stack.pop();\n                if (first) { out.append(node); first = false; }\n                else out.append(' ').append(node);\n                if (right[node] != 0) stack.push(right[node]);\n                if (left[node] != 0)  stack.push(left[node]);\n            }\n            out.append('\\n');\n        }\n        System.out.print(out.toString());\n    }\n\n    // Fast scanner for competitive programming\n    static class FastScanner {\n        private final BufferedInputStream in;\n        private final byte[] buffer = new byte[1 << 16];\n        private int ptr = 0, len = 0;\n        FastScanner(InputStream is) { in = new BufferedInputStream(is); }\n        private int readByte() throws IOException {\n            if (ptr >= len) {\n                len = in.read(buffer);\n                ptr = 0;\n                if (len <= 0) return -1;\n            }\n            return buffer[ptr++];\n        }\n        String next() throws IOException {\n            StringBuilder sb = new StringBuilder();\n            int c;\n            while ((c = readByte()) != -1 && c <= ' ') {}\n            while (c != -1 && c > ' ') {\n                sb.append((char) c);\n                c = readByte();\n            }\n            return sb.toString();\n        }\n        int nextInt() throws IOException { return Integer.parseInt(next()); }\n    }\n}",
      "Python3": "#!/usr/bin/env python3\nimport sys\n\ndef solve():\n    data = sys.stdin.read().strip().split()\n    if not data:\n        return\n    it = iter(data)\n    T = int(next(it))\n    out_lines = []\n    MAXV = 100000 + 5\n    for _ in range(T):\n        N = int(next(it))\n        left = [0] * MAXV\n        right = [0] * MAXV\n        is_child = [0] * MAXV\n        max_node = 0\n        for __ in range(N - 1):\n            p = int(next(it))\n            c = int(next(it))\n            dir = next(it)\n            if dir == 'L':\n                left[p] = c\n            else:\n                right[p] = c\n            is_child[c] = 1\n            if p > max_node: max_node = p\n            if c > max_node: max_node = c\n        root = -1\n        for i in range(1, max_node + 1):\n            if (left[i] or right[i] or is_child[i]) and not is_child[i]:\n                root = i\n                break\n        stack = [root]\n        res = []\n        while stack:\n            node = stack.pop()\n            res.append(str(node))\n            if right[node]:\n                stack.append(right[node])\n            if left[node]:\n                stack.append(left[node])\n        out_lines.append(' '.join(res))\n    sys.stdout.write('\\n'.join(out_lines))\n\nif __name__ == \"__main__\":\n    solve()\n",
      "JavaScript": "const fs = require('fs');\nconst input = fs.readFileSync(0, 'utf-8').trim().split(/\\s+/);\nlet idx = 0;\nif (input.length === 0) process.exit(0);\nconst T = parseInt(input[idx++], 10);\nconst MAXV = 100000 + 5;\nlet outputs = [];\nfor (let t = 0; t < T; t++) {\n  const N = parseInt(input[idx++], 10);\n  const left = new Int32Array(MAXV);\n  const right = new Int32Array(MAXV);\n  const isChild = new Uint8Array(MAXV);\n  let maxNode = 0;\n  for (let i = 0; i < N - 1; i++) {\n    const p = parseInt(input[idx++], 10);\n    const c = parseInt(input[idx++], 10);\n    const dir = input[idx++];\n    if (dir === 'L') left[p] = c; else right[p] = c;\n    isChild[c] = 1;\n    if (p > maxNode) maxNode = p;\n    if (c > maxNode) maxNode = c;\n  }\n  let root = -1;\n  for (let i = 1; i <= maxNode; i++) {\n    if ((left[i] || right[i] || isChild[i]) && !isChild[i]) { root = i; break; }\n  }\n  const stack = [];\n  stack.push(root);\n  const res = [];\n  while (stack.length) {\n    const node = stack.pop();\n    res.push(node);\n    if (right[node]) stack.push(right[node]);\n    if (left[node]) stack.push(left[node]);\n  }\n  outputs.push(res.join(' '));\n}\nconsole.log(outputs.join('\\n'));\n"
    }
  }
}