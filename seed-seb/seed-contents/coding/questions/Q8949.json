{
  "questionId": "Q8949",
  "title": "Crazy Substrings",
  "slug": "crazy-substrings",
  "version": 1,
  "createdAt": "2026-07-02",
  "updatedAt": "2026-07-04",
  "metadata": {
    "category": "Strings | Greedy | Sorting",
    "difficulty": "Hard",
    "isPremium": true,
    "tags": [
      "segment lengths",
      "reversal operation",
      "prefix sums",
      "sorting",
      "greedy merging"
    ],
    "companyTags": [],
    "topicTags": [
      "Constructive Algorithms"
    ],
    "QBCategory": "CCD"
  },
  "content": {
    "problemStatement": "Gotham City is the home of Batman, and Batman likes to keep his people together.\n\nThere are NNN houses in the city. The resident of the ithi^{th}ith house is of type AiA_iAi​. It is known that people of the same type are friends and people of different types are not friends.\n\nTo maintain peace, Batman can do the following type of operation:\n\n\nChoose indices iii and jjj (1≤i≤j≤N)(1\\leq i \\le j \\le N)(1≤i≤j≤N) and reverse the substring A[i,j]A[i,j]A[i,j].\nDetermine the length of the **longest** range of friends after applying **at most** kkk operations on the string for all 0≤k≤N0 \\le k \\le N0≤k≤N.\n\n\nInput Format\n\nFirst line will contain TTT, the number of test cases. Then the test cases follow.\nThe first line of each test case contains a single integer NNN, the number of houses in Gotham city.\nThe next line contains a string AAA with NNN characters. The ithi^{th}ith character denotes the type of the ithi^{th}ith person.\n\n\nOutput Format\nFor each test case, output a single line, (N+1)(N+1)(N+1) space-separated integers where the ithi^{th}ith integer signifies the length of the longest range of friends after applying  **at most** iii operations.",
    "constraints": [
      "1 ≤ T ≤ 1000",
      "1 ≤ N ≤ 2·10^5",
      "Sum of N over all test cases ≤ 2·10^5",
      "S contains only lowercase English letters"
    ],
    "inputFormat": "The first line contains an integer T, the number of test cases. For each test case: the first line contains an integer N – the length of the string. The second line contains a string S of length N consisting of lowercase English letters.",
    "outputFormat": "For each test case output a single line with N+1 space‑separated integers. The i‑th integer (0‑based) is the maximum possible length of a contiguous substring consisting of a single character after performing at most i operations (reversals).",
    "sampleTestCases": [
      {
        "id": "sample_1",
        "input": "3\n4\naaaa\n4\nabca\n7\nbabbaab",
        "output": "4 4 4 4 4\n1 2 2 2 2\n2 3 4 4 4 4 4 4",
        "explanation": "Explanation:\nTest case 111: All 444 people are friends. Thus, the answer is 444 for all values of kkk.\nTest case 222:\n\nFor k=0k=0k=0, the length of the longest range of friends is 111.\nFor k=1k=1k=1, we can choose i=1i=1i=1 and j=3j=3j=3. On reversing the substring [1,3][1,3][1,3], we get cbaa. Thus, length of longest range of friends is 222.\nFor k=2,3,k=2,3,k=2,3, and 444, the length of the longest range of friends is 222 as no more people of the same type can be grouped together."
      }
    ]
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "label": "Case 1",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_2",
        "label": "Case 2",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_3",
        "label": "Case 3",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_4",
        "label": "Case 4",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_5",
        "label": "Case 5",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      }
    ],
    "totalTestCases": 5,
    "totalWeight": 10
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      2,
      2,
      2,
      2,
      2
    ],
    "maxScore": 10
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "For each character we look at the lengths of its consecutive blocks (segments) in the original string. If a character appears in s segments, we need s‑1 reversals to merge all its segments into one block. With at most k reversals we can merge at most k+1 segments, therefore the best we can obtain for this character is the sum of its largest (k+1) segment lengths. For every character we sort its segment lengths in descending order and build a prefix sum array pref[t] = sum of the t largest segments. For each possible number of reversals k (0 ≤ k ≤ N) we consider t = min(k+1, number_of_segments) and keep the maximum pref[t] over all characters. After processing all characters we make the answer array non‑decreasing because allowing more operations cannot worsen the result. The algorithm runs in O(N) to collect segments plus O(total_segments log total_segments) for sorting, which is O(N log N) in the worst case, well within limits.",
    "timeComplexity": "O(N log N) per test case (dominated by sorting segment lengths).",
    "spaceComplexity": "O(N) for storing segment lists and prefix sums.",
    "code": {
      "C": "#include <stdio.h>\n#include <stdlib.h>\n#include <string.h>\n\nint cmp_desc(const void *a, const void *b) {\n    int x = *(int*)b - *(int*)a;\n    return x;\n}\n\nint main(){\n    int T; if(scanf(\"%d\", &T)!=1) return 0;\n    while(T--){\n        int N; scanf(\"%d\", &N);\n        char *s = (char*)malloc(N+5);\n        scanf(\"%s\", s);\n        // vectors for 26 letters\n        int *seg[26];\n        int segCnt[26];\n        for(int i=0;i<26;i++){ seg[i]=NULL; segCnt[i]=0; }\n        int i=0;\n        while(i<N){\n            char c = s[i];\n            int j=i;\n            while(j<N && s[j]==c) j++;\n            int len = j-i;\n            int idx = c-'a';\n            seg[idx] = (int*)realloc(seg[idx], (segCnt[idx]+1)*sizeof(int));\n            seg[idx][segCnt[idx]++] = len;\n            i=j;\n        }\n        // answer array\n        int *ans = (int*)calloc(N+1, sizeof(int));\n        for(int ch=0; ch<26; ++ch){\n            if(segCnt[ch]==0) continue;\n            qsort(seg[ch], segCnt[ch], sizeof(int), cmp_desc);\n            int pref = 0;\n            for(int t=1; t<=segCnt[ch]; ++t){\n                pref += seg[ch][t-1];\n                int k = t-1; // need t-1 reversals\n                if(pref > ans[k]) ans[k] = pref;\n            }\n        }\n        // make non‑decreasing\n        for(int k=1;k<=N;k++) if(ans[k] < ans[k-1]) ans[k] = ans[k-1];\n        // output\n        for(int k=0;k<=N;k++){\n            if(k) printf(\" \");\n            printf(\"%d\", ans[k]);\n        }\n        printf(\"\\n\");\n        // free\n        for(int ch=0; ch<26; ++ch) free(seg[ch]);\n        free(ans); free(s);\n    }\n    return 0;\n}\n",
      "C++": "#include <bits/stdc++.h>\nusing namespace std;\nint main(){\n    ios::sync_with_stdio(false);\n    cin.tie(nullptr);\n    int T; if(!(cin>>T)) return 0;\n    while(T--){\n        int N; cin>>N; string s; cin>>s;\n        vector<vector<int>> seg(26);\n        for(int i=0;i<N;){\n            char c=s[i];\n            int j=i;\n            while(j<N && s[j]==c) ++j;\n            seg[c-'a'].push_back(j-i);\n            i=j;\n        }\n        vector<int> ans(N+1,0);\n        for(int ch=0; ch<26; ++ch){\n            auto &v=seg[ch];\n            if(v.empty()) continue;\n            sort(v.begin(), v.end(), greater<int>());\n            int pref=0;\n            for(int t=1; t<= (int)v.size(); ++t){\n                pref+=v[t-1];\n                int k=t-1;\n                ans[k]=max(ans[k], pref);\n            }\n        }\n        for(int k=1;k<=N;++k) ans[k]=max(ans[k], ans[k-1]);\n        for(int k=0;k<=N;++k){\n            if(k) cout<<' ';\n            cout<<ans[k];\n        }\n        cout<<'\\n';\n    }\n    return 0;\n}\n",
      "Java": "import java.io.*;\nimport java.util.*;\n\npublic class Main {\n    public static void main(String[] args) throws Exception {\n        FastScanner fs = new FastScanner(System.in);\n        int T = fs.nextInt();\n        StringBuilder out = new StringBuilder();\n        while (T-- > 0) {\n            int N = fs.nextInt();\n            char[] s = fs.next().toCharArray();\n            @SuppressWarnings(\"unchecked\")\n            ArrayList<Integer>[] seg = new ArrayList[26];\n            for (int i = 0; i < 26; i++) seg[i] = new ArrayList<>();\n            for (int i = 0; i < N; ) {\n                char c = s[i];\n                int j = i;\n                while (j < N && s[j] == c) j++;\n                seg[c - 'a'].add(j - i);\n                i = j;\n            }\n            int[] ans = new int[N + 1];\n            for (int ch = 0; ch < 26; ch++) {\n                List<Integer> list = seg[ch];\n                if (list.isEmpty()) continue;\n                list.sort(Collections.reverseOrder());\n                int pref = 0;\n                for (int t = 1; t <= list.size(); t++) {\n                    pref += list.get(t - 1);\n                    int k = t - 1;\n                    if (pref > ans[k]) ans[k] = pref;\n                }\n            }\n            for (int k = 1; k <= N; k++) ans[k] = Math.max(ans[k], ans[k - 1]);\n            for (int k = 0; k <= N; k++) {\n                if (k > 0) out.append(' ');\n                out.append(ans[k]);\n            }\n            out.append('\\n');\n        }\n        System.out.print(out.toString());\n    }\n\n    // Fast scanner for Java\n    static class FastScanner {\n        private final BufferedInputStream in;\n        private final byte[] buffer = new byte[1 << 16];\n        private int ptr = 0, len = 0;\n        FastScanner(InputStream is) { in = new BufferedInputStream(is); }\n        private int readByte() throws IOException {\n            if (ptr >= len) {\n                len = in.read(buffer);\n                ptr = 0;\n                if (len <= 0) return -1;\n            }\n            return buffer[ptr++];\n        }\n        String next() throws IOException {\n            StringBuilder sb = new StringBuilder();\n            int c;\n            while ((c = readByte()) != -1 && c <= ' ') {}\n            while (c != -1 && c > ' ') {\n                sb.append((char) c);\n                c = readByte();\n            }\n            return sb.toString();\n        }\n        int nextInt() throws IOException { return Integer.parseInt(next()); }\n    }\n}\n",
      "Python3": "import sys\n\ndef solve():\n    data = sys.stdin.read().split()\n    it = iter(data)\n    T = int(next(it))\n    out_lines = []\n    for _ in range(T):\n        N = int(next(it))\n        s = next(it).strip()\n        seg = [[] for _ in range(26)]\n        i = 0\n        while i < N:\n            c = s[i]\n            j = i\n            while j < N and s[j] == c:\n                j += 1\n            seg[ord(c) - 97].append(j - i)\n            i = j\n        ans = [0] * (N + 1)\n        for lst in seg:\n            if not lst:\n                continue\n            lst.sort(reverse=True)\n            pref = 0\n            for t, val in enumerate(lst, start=1):\n                pref += val\n                k = t - 1\n                if pref > ans[k]:\n                    ans[k] = pref\n        for k in range(1, N + 1):\n            if ans[k] < ans[k - 1]:\n                ans[k] = ans[k - 1]\n        out_lines.append(' '.join(map(str, ans)))\n    sys.stdout.write('\\n'.join(out_lines))\n\nif __name__ == \"__main__\":\n    solve()\n",
      "JavaScript": "const fs = require('fs');\nconst input = fs.readFileSync(0, 'utf-8').trim().split(/\\s+/);\nlet idx = 0;\nconst T = Number(input[idx++]);\nlet outputs = [];\nfor (let tc = 0; tc < T; ++tc) {\n  const N = Number(input[idx++]);\n  const s = input[idx++];\n  const seg = Array.from({ length: 26 }, () => []);\n  let i = 0;\n  while (i < N) {\n    const ch = s.charCodeAt(i) - 97;\n    let j = i;\n    while (j < N && s.charCodeAt(j) - 97 === ch) ++j;\n    seg[ch].push(j - i);\n    i = j;\n  }\n  const ans = new Array(N + 1).fill(0);\n  for (let ch = 0; ch < 26; ++ch) {\n    const arr = seg[ch];\n    if (arr.length === 0) continue;\n    arr.sort((a, b) => b - a);\n    let pref = 0;\n    for (let t = 1; t <= arr.length; ++t) {\n      pref += arr[t - 1];\n      const k = t - 1;\n      if (pref > ans[k]) ans[k] = pref;\n    }\n  }\n  for (let k = 1; k <= N; ++k) if (ans[k] < ans[k - 1]) ans[k] = ans[k - 1];\n  outputs.push(ans.join(' '));\n}\nconsole.log(outputs.join('\\n'));\n"
    }
  },
  "QBCategory": "CCD",
  "boilerPlates": {
    "C": "#include <stdio.h>\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "C++": "#include <iostream>\nusing namespace std;\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "Java": "import java.util.*;\nimport java.io.*;\n\npublic class Main {\n    public static void main(String[] args) throws IOException {\n        // your code goes here\n    }\n}",
    "Python3": "# your code goes here\n",
    "JavaScript": "// your code goes here\n"
  },
  "_internal": {
    "methodName": "",
    "params": []
  }
}