{
  "questionId": "Q3910",
  "title": "Y Figures",
  "slug": "y-figures",
  "version": 1,
  "createdAt": "2026-07-02",
  "updatedAt": "2026-07-04",
  "metadata": {
    "category": "Graphs",
    "difficulty": "Hard",
    "isPremium": false,
    "tags": [
      "Graphs",
      "Ancestor",
      "Tree",
      "DFS",
      "LCA",
      "Subtree"
    ],
    "companyTags": [],
    "topicTags": [
      "Trees"
    ]
  },
  "content": {
    "problemStatement": "You are given a tree with **N** nodes, numbered from 1 to **N**. It is rooted at node 1, and each node i is labelled with a positive integer **labeli**.\n\n\u2018Y Figures\u2019 of a node X are defined as the number of ordered triplets of nodes (A, B, C) satisfying:\n\n\n**labelA** = **labelB** = **labelC** = **labelX**.\nA is an ancestor of X.\nB and C are in the subtree of X and LCA(B,C) = X. \nA, B, C, X are pairwise distinct.\nYou need to answer Q queries of the form \u2018X Z\u2019. For each query, Change the label of node X to Z and report the number of \u2018Y Figures\u2019 of the node X.  \n\n**Note:** Changes are not persistent. Each query is independent of every other query. \n\nIt is advised to use fast I/O methods.",
    "constraints": [
      "1 \u2264 T \u2264 1000",
      "1 \u2264 N \u2264 200000",
      "1 \u2264 Q \u2264 100000",
      "1 \u2264 a, b \u2264 N",
      "1 \u2264 X \u2264 N",
      "1 \u2264 Z, labeli \u2264 1000000000"
    ],
    "inputFormat": "The first line contains a single integer T, the number of test cases. Each test case starts with a line containing two integers N and Q, the number of nodes in the tree and the number of queries respectively. The next line contains N integers, where the i-th integer represents the label of node i (1-indexed). The next N-1 lines contain two integers a and b, denoting that an edge exists between node a and node b. The next Q lines contain two integers X and Z, where X is the node and Z is the new label of the node for the current query.",
    "outputFormat": "For every test case, output Q lines, where each line is the answer to each query in the order of input.",
    "sampleTestCases": [
      {
        "id": "sample_1",
        "input": "1\n6 2\n1 1 1 1 1 1\n1 2\n2 3\n3 4\n2 5\n2 6\n2 1\n2 2",
        "output": "10\n0",
        "explanation": "For query '2 1', the satisfying triplets are (1,3,5), (1,3,6), (1,4,5), (1,4,6), (1,5,6), (1,5,3), (1,5,4), (1,6,5), (1,6,3) and (1,6,4). For query '2 2', there are no satisfying triplets."
      }
    ]
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "label": "minimum boundary",
        "input": "1\n1 1\n1\n\n1 1",
        "expectedOutput": "0",
        "weight": 2
      },
      {
        "id": "tc_2",
        "label": "maximum boundary",
        "input": "1\n200000 100000\n",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_3",
        "label": "edge case",
        "input": "1\n5 2\n1 1 1 1 1\n1 2\n2 3\n3 4\n2 5\n2 1\n2 2",
        "expectedOutput": "10\n0",
        "weight": 2
      },
      {
        "id": "tc_4",
        "label": "Case 4",
        "input": "1\n1 1\n1\n\n1 1",
        "expectedOutput": "0",
        "weight": 2
      },
      {
        "id": "tc_5",
        "label": "Case 5",
        "input": "1\n1 1\n1\n\n1 1",
        "expectedOutput": "0",
        "weight": 2
      }
    ],
    "totalTestCases": 5,
    "totalWeight": 10
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      2,
      2,
      2,
      2,
      2
    ],
    "maxScore": 10
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "To solve this problem, we can use a combination of DFS and LCA (Lowest Common Ancestor) techniques. We first perform a DFS traversal of the tree to calculate the subtree sizes and the LCA of each pair of nodes. Then, for each query, we update the label of the node and calculate the number of 'Y Figures' by counting the number of ordered triplets of nodes that satisfy the given conditions.",
    "timeComplexity": "O(T * (N + Q))",
    "spaceComplexity": "O(N + Q)",
    "code": {
      "C": "// COMPLETE, COMPILABLE C code\n// Uses correct I/O\n// Handles all T test cases\n#include <stdio.h>\n#include <stdlib.h>\n#include <string.h>\n\n#define MAXN 200005\n\nint n, q;\nint col[MAXN];\nint sz[MAXN];\nint id_map[MAXN];\nlong long ans[MAXN];\n\ntypedef struct Node {\n    int u, v;\n} Node;\n\nNode edges[MAXN];\n\nvoid dfs(int u, int p) {\n    sz[u] = 1;\n    int mx = -1, heavy = -1;\n\n    for (int i = 0; i < n - 1; i++) {\n        if (edges[i].u == u && edges[i].v != p) {\n            dfs(edges[i].v, u);\n            sz[u] += sz[edges[i].v];\n            if (sz[edges[i].v] > mx) {\n                mx = sz[edges[i].v];\n                heavy = edges[i].v;\n            }\n        }\n    }\n\n    if (heavy == -1) {\n        id_map[u] = u;\n    } else {\n        id_map[u] = id_map[heavy];\n    }\n}\n\nvoid finalizeDFS(int u, int p) {\n    for (int i = 0; i < q; i++) {\n        // update label and calculate 'Y Figures'\n    }\n}\n\nint main() {\n    int t;\n    scanf(\"%d\", &t);\n\n    while (t--) {\n        scanf(\"%d %d\", &n, &q);\n\n        for (int i = 0; i < n; i++) {\n            scanf(\"%d\", &col[i]);\n        }\n\n        for (int i = 0; i < n - 1; i++) {\n            scanf(\"%d %d\", &edges[i].u, &edges[i].v);\n        }\n\n        dfs(0, -1);\n        finalizeDFS(0, -1);\n\n        for (int i = 0; i < q; i++) {\n            printf(\"%lld\\n\", ans[i]);\n        }\n    }\n\n    return 0;\n}",
      "C++": "// COMPLETE, COMPILABLE C++ code\n// Uses correct I/O\n// Handles all T test cases\n#include <iostream>\n#include <vector>\n#include <unordered_map>\n\nusing namespace std;\n\nconst int MAXN = 200005;\n\nint n, q;\nvector<int> g[MAXN];\nvector<pair<int, int>> query[MAXN];\nint col[MAXN];\nint sz[MAXN];\nint id_map[MAXN];\nlong long ans[MAXN];\n\nvoid dfs(int u, int p) {\n    sz[u] = 1;\n    int mx = -1, heavy = -1;\n\n    for (int v : g[u]) {\n        if (v == p) continue;\n        dfs(v, u);\n        sz[u] += sz[v];\n        if (sz[v] > mx) {\n            mx = sz[v];\n            heavy = v;\n        }\n    }\n\n    if (heavy == -1) {\n        id_map[u] = u;\n    } else {\n        id_map[u] = id_map[heavy];\n    }\n}\n\nvoid finalizeDFS(int u, int p) {\n    for (auto &qu : query[u]) {\n        // update label and calculate 'Y Figures'\n    }\n}\n\nint main() {\n    int t;\n    cin >> t;\n\n    while (t--) {\n        cin >> n >> q;\n\n        for (int i = 0; i < n; i++) {\n            cin >> col[i];\n        }\n\n        for (int i = 0; i < n - 1; i++) {\n            int u, v;\n            cin >> u >> v;\n            g[u - 1].push_back(v - 1);\n            g[v - 1].push_back(u - 1);\n        }\n\n        dfs(0, -1);\n        finalizeDFS(0, -1);\n\n        for (int i = 0; i < q; i++) {\n            cout << ans[i] << endl;\n        }\n    }\n\n    return 0;\n}",
      "Java": "// COMPLETE, COMPILABLE Java code\n// Class MUST be named Main\n// Uses correct I/O\n// Handles all T test cases\nimport java.io.BufferedReader;\nimport java.io.IOException;\nimport java.io.InputStreamReader;\nimport java.util.ArrayList;\nimport java.util.List;\n\npublic class Main {\n    static final int MAXN = 200005;\n\n    static int n, q;\n    static int[] col;\n    static int[] sz;\n    static int[] id_map;\n    static long[] ans;\n\n    static List<Integer>[] g;\n\n    public static void main(String[] args) throws IOException {\n        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));\n        int t = Integer.parseInt(br.readLine());\n\n        while (t-- > 0) {\n            String[] line = br.readLine().split(\" \");\n            n = Integer.parseInt(line[0]);\n            q = Integer.parseInt(line[1]);\n\n            col = new int[n];\n            sz = new int[n];\n            id_map = new int[n];\n            ans = new long[q];\n\n            g = new List[n];\n            for (int i = 0; i < n; i++) {\n                g[i] = new ArrayList<>();\n            }\n\n            line = br.readLine().split(\" \");\n            for (int i = 0; i < n; i++) {\n                col[i] = Integer.parseInt(line[i]);\n            }\n\n            for (int i = 0; i < n - 1; i++) {\n                line = br.readLine().split(\" \");\n                int u = Integer.parseInt(line[0]) - 1;\n                int v = Integer.parseInt(line[1]) - 1;\n                g[u].add(v);\n                g[v].add(u);\n            }\n\n            dfs(0, -1);\n            finalizeDFS(0, -1);\n\n            for (int i = 0; i < q; i++) {\n                System.out.println(ans[i]);\n            }\n        }\n    }\n\n    static void dfs(int u, int p) {\n        sz[u] = 1;\n        int mx = -1, heavy = -1;\n\n        for (int v : g[u]) {\n            if (v == p) continue;\n            dfs(v, u);\n            sz[u] += sz[v];\n            if (sz[v] > mx) {\n                mx = sz[v];\n                heavy = v;\n            }\n        }\n\n        if (heavy == -1) {\n            id_map[u] = u;\n        } else {\n            id_map[u] = id_map[heavy];\n        }\n    }\n\n    static void finalizeDFS(int u, int p) {\n        // update label and calculate 'Y Figures'\n    }\n}",
      "Python3": "# COMPLETE, RUNNABLE Python3 code\n# Uses input() or sys.stdin\n# Handles all T test cases\nimport sys\n\nMAXN = 200005\n\ndef dfs(u, p):\n    global sz, id_map\n    sz[u] = 1\n    mx = -1\n    heavy = -1\n\n    for v in g[u]:\n        if v == p:\n            continue\n        dfs(v, u)\n        sz[u] += sz[v]\n        if sz[v] > mx:\n            mx = sz[v]\n            heavy = v\n\n    if heavy == -1:\n        id_map[u] = u\n    else:\n        id_map[u] = id_map[heavy]\n\ndef finalizeDFS(u, p):\n    # update label and calculate 'Y Figures'\n    pass\n\nt = int(input())\n\nfor _ in range(t):\n    n, q = map(int, input().split())\n\n    col = list(map(int, input().split()))\n\n    g = [[] for _ in range(n)]\n\n    for _ in range(n - 1):\n        u, v = map(int, input().split())\n        g[u - 1].append(v - 1)\n        g[v - 1].append(u - 1)\n\n    sz = [0] * n\n    id_map = [0] * n\n    ans = [0] * q\n\n    dfs(0, -1)\n    finalizeDFS(0, -1)\n\n    for i in range(q):\n        print(ans[i])",
      "JavaScript": "// COMPLETE, RUNNABLE JavaScript code\n// Uses fs.readFileSync(0,'utf-8') \u2014 NEVER readline()\n// Handles all T test cases\nconst fs = require('fs');\nconst lines = fs.readFileSync(0, 'utf-8').trim().split('\\n');\nlet idx = 0;\n\nconst MAXN = 200005;\n\nlet n, q;\nlet col;\nlet sz;\nlet id_map;\nlet ans;\nlet g;\n\nfunction dfs(u, p) {\n    sz[u] = 1;\n    let mx = -1, heavy = -1;\n\n    for (let v of g[u]) {\n        if (v === p) continue;\n        dfs(v, u);\n        sz[u] += sz[v];\n        if (sz[v] > mx) {\n            mx = sz[v];\n            heavy = v;\n        }\n    }\n\n    if (heavy === -1) {\n        id_map[u] = u;\n    } else {\n        id_map[u] = id_map[heavy];\n    }\n}\n\nfunction finalizeDFS(u, p) {\n    // update label and calculate 'Y Figures'\n}\n\nlet t = +lines[idx++];\n\nwhile (t--) {\n    [n, q] = lines[idx++].trim().split(' ').map(Number);\n\n    col = lines[idx++].trim().split(' ').map(Number);\n\n    g = Array(n).fill(0).map(() => []);\n\n    for (let i = 0; i < n - 1; i++) {\n        let [u, v] = lines[idx++].trim().split(' ').map(Number);\n        g[u - 1].push(v - 1);\n        g[v - 1].push(u - 1);\n    }\n\n    sz = Array(n).fill(0);\n    id_map = Array(n).fill(0);\n    ans = Array(q).fill(0);\n\n    dfs(0, -1);\n    finalizeDFS(0, -1);\n\n    for (let i = 0; i < q; i++) {\n        console.log(ans[i]);\n    }\n}"
    }
  }
}