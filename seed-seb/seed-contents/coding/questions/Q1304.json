{
  "questionId": "Q1304",
  "title": "Top view of the Binary Tree",
  "slug": "top-view-of-the-binary-tree",
  "version": 1,
  "createdAt": "2026-07-01",
  "updatedAt": "2026-07-04",
  "metadata": {
    "category": "Trees",
    "difficulty": "Medium",
    "isPremium": false,
    "tags": [
      "Tree Traversal",
      "Top View",
      "BFS",
      "Trees"
    ],
    "companyTags": [],
    "topicTags": [
      "Trees"
    ]
  },
  "content": {
    "problemStatement": "Given a binary tree, print the top view of the tree. The top view of a binary tree is the set of nodes visible when the tree is viewed from the top. It is defined as the nodes visible from the top along the vertical line passing through the leftmost node to rightmost node of the tree.\n\nExamples:\n\n\n\n\nInput Format\n\nThe first line of the input contains a single integer NNN \u2014 the number of nodes in the binary tree.\nNext N\u22121N - 1N\u22121 lines contain three space separated characters pi,ci,Rip_i, c_i, R_ipi\u200b,ci\u200b,Ri\u200b, describing the tree with edge informations - pip_ipi\u200b is an integer denoting the parent node of the iiith edge, cic_ici\u200b is child node, and the letter RiR_iRi\u200b denotes whether the child is left child or right child, it's value is `L` if cic_ici\u200b is left child of pip_ipi\u200b, else `R`.\n\n\nOutput Format\nPrint on the single line, KKK (if there are KKK nodes visible from the top) space separated node values - the top view nodes of the given binary tree from left to right.",
    "constraints": [
      "1 <= N <= 10000",
      "1 <= pi, ci <= 100000",
      "Ri = 'L' or 'R'",
      "All pi's and ci's are distinct"
    ],
    "inputFormat": "The first line contains a single integer N, the number of nodes in the binary tree. Each of the next N-1 lines contains three space-separated values: pi, ci, and Ri, where pi is the parent node, ci is the child node, and Ri is either 'L' or 'R', indicating whether the child is the left or right child of the parent.",
    "outputFormat": "Print the top view of the binary tree, which is the set of nodes visible when the tree is viewed from the top, on a single line, separated by spaces.",
    "sampleTestCases": [
      {
        "id": "sample_1",
        "input": "8\n1 2 L\n1 3 R\n3 4 L\n3 5 R\n2 6 L\n2 7 R\n7 8 R",
        "output": "6 2 1 3 5",
        "explanation": "The top view of the given binary tree is 6 2 1 3 5."
      }
    ]
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "label": "Case 1",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_2",
        "label": "Case 2",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_3",
        "label": "Case 3",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_4",
        "label": "Case 4",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_5",
        "label": "Case 5",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      }
    ],
    "totalTestCases": 5,
    "totalWeight": 10
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      2,
      2,
      2,
      2,
      2
    ],
    "maxScore": 10
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "To find the top view of a binary tree, we can use a breadth-first search (BFS) traversal and keep track of the nodes at each horizontal distance from the root. We start by initializing a queue with the root node and its horizontal distance. Then, we perform BFS traversal, and for each node, we check if we have encountered a node at the same horizontal distance before. If not, we add the node's value to the top view. We continue this process until we have traversed all nodes in the tree.",
    "timeComplexity": "O(N), where N is the number of nodes in the tree, since we visit each node once during the BFS traversal.",
    "spaceComplexity": "O(N), since in the worst case, we might need to store all nodes in the queue.",
    "code": {
      "C": "#include <stdio.h>\n#include <stdlib.h>\n#include <string.h>\n#define MAX_NODES 10000\n#define MAX_CHILDREN 2\n\n// Define the structure for a tree node\ntypedef struct Node {\n    int val;\n    struct Node* left;\n    struct Node* right;\n} Node;\n\n// Function to create a new node\nNode* createNode(int val) {\n    Node* newNode = (Node*) malloc(sizeof(Node));\n    if (!newNode) {\n        printf(\"Memory error\\n\");\n        return NULL;\n    }\n    newNode->val = val;\n    newNode->left = newNode->right = NULL;\n    return newNode;\n}\n\n// Function to perform BFS traversal and find the top view\nvoid topView(Node* root) {\n    if (!root) return;\n\n    // Create a queue for BFS traversal\n    Node** queue = (Node**) malloc(sizeof(Node*) * MAX_NODES);\n    int* hd = (int*) malloc(sizeof(int) * MAX_NODES);\n    int front = 0, rear = 0;\n\n    // Enqueue the root node\n    queue[rear] = root;\n    hd[rear] = 0;\n    rear++;\n\n    // Create a map to store the top view nodes\n    int map[MAX_NODES] = {0};\n\n    while (front < rear) {\n        Node* node = queue[front];\n        int horizontalDistance = hd[front];\n        front++;\n\n        // If we haven't encountered a node at the current horizontal distance before\n        if (map[horizontalDistance + MAX_NODES / 2] == 0) {\n            map[horizontalDistance + MAX_NODES / 2] = node->val;\n        }\n\n        // Enqueue the children of the current node\n        if (node->left) {\n            queue[rear] = node->left;\n            hd[rear] = horizontalDistance - 1;\n            rear++;\n        }\n        if (node->right) {\n            queue[rear] = node->right;\n            hd[rear] = horizontalDistance + 1;\n            rear++;\n        }\n    }\n\n    // Print the top view nodes\n    for (int i = 0; i < MAX_NODES; i++) {\n        if (map[i] != 0) {\n            printf(\"%d \", map[i]);\n        }\n    }\n    printf(\"\\n\");\n}\n\nint main() {\n    int N;\n    scanf(\"%d\", &N);\n\n    Node* root = NULL;\n    for (int i = 0; i < N - 1; i++) {\n        int pi, ci;\n        char Ri;\n        scanf(\"%d %d %c\", &pi, &ci, &Ri);\n\n        // Create a new node for the child\n        Node* child = createNode(ci);\n\n        // Find the parent node\n        Node* parent = root;\n        if (parent == NULL) {\n            parent = createNode(pi);\n            root = parent;\n        } else {\n            Node* temp = parent;\n            while (temp->left != NULL && temp->right != NULL) {\n                if (temp->left->val == pi) {\n                    temp = temp->left;\n                } else if (temp->right->val == pi) {\n                    temp = temp->right;\n                } else {\n                    // If the parent node is not found, create a new node for the parent\n                    Node* newParent = createNode(pi);\n                    if (temp->left == NULL) {\n                        temp->left = newParent;\n                    } else {\n                        temp->right = newParent;\n                    }\n                    temp = newParent;\n                }\n            }\n            if (temp->left == NULL) {\n                temp->left = child;\n            } else {\n                temp->right = child;\n            }\n        }\n    }\n\n    topView(root);\n\n    return 0;\n}",
      "C++": "#include <iostream>\n#include <queue>\n#include <map>\nusing namespace std;\n\n// Define the structure for a tree node\nstruct Node {\n    int val;\n    Node* left;\n    Node* right;\n    Node(int node_value) {\n        val = node_value;\n        left = right = NULL;\n    }\n};\n\n// Function to perform BFS traversal and find the top view\nvoid topView(Node* root) {\n    if (!root) return;\n\n    // Create a queue for BFS traversal\n    queue<pair<Node*, int>> q;\n    q.push({root, 0});\n\n    // Create a map to store the top view nodes\n    map<int, int> v;\n\n    while (!q.empty()) {\n        Node* node = q.front().first;\n        int horizontalDistance = q.front().second;\n        q.pop();\n\n        // If we haven't encountered a node at the current horizontal distance before\n        if (v.find(horizontalDistance) == v.end()) {\n            v[horizontalDistance] = node->val;\n        }\n\n        // Enqueue the children of the current node\n        if (node->left) {\n            q.push({node->left, horizontalDistance - 1});\n        }\n        if (node->right) {\n            q.push({node->right, horizontalDistance + 1});\n        }\n    }\n\n    // Print the top view nodes\n    for (auto& x : v) {\n        cout << x.second << \" \";\n    }\n    cout << endl;\n}\n\nint main() {\n    int N;\n    cin >> N;\n\n    Node* root = NULL;\n    for (int i = 0; i < N - 1; i++) {\n        int pi, ci;\n        char Ri;\n        cin >> pi >> ci >> Ri;\n\n        // Create a new node for the child\n        Node* child = new Node(ci);\n\n        // Find the parent node\n        Node* parent = root;\n        if (parent == NULL) {\n            parent = new Node(pi);\n            root = parent;\n        } else {\n            Node* temp = parent;\n            while (temp->left != NULL && temp->right != NULL) {\n                if (temp->left->val == pi) {\n                    temp = temp->left;\n                } else if (temp->right->val == pi) {\n                    temp = temp->right;\n                } else {\n                    // If the parent node is not found, create a new node for the parent\n                    Node* newParent = new Node(pi);\n                    if (temp->left == NULL) {\n                        temp->left = newParent;\n                    } else {\n                        temp->right = newParent;\n                    }\n                    temp = newParent;\n                }\n            }\n            if (temp->left == NULL) {\n                temp->left = child;\n            } else {\n                temp->right = child;\n            }\n        }\n    }\n\n    topView(root);\n\n    return 0;\n}",
      "Java": "import java.util.Queue;\nimport java.util.LinkedList;\nimport java.util.Map;\nimport java.util.HashMap;\nimport java.util.Scanner;\npublic class Main {\n    // Define the structure for a tree node\n    static class Node {\n        int val;\n        Node left;\n        Node right;\n        Node(int node_value) {\n            val = node_value;\n            left = right = null;\n        }\n    }\n\n    // Function to perform BFS traversal and find the top view\n    public static void topView(Node root) {\n        if (root == null) return;\n\n        // Create a queue for BFS traversal\n        Queue<Node> queue = new LinkedList<>();\n        queue.add(root);\n        Queue<Integer> hd = new LinkedList<>();\n        hd.add(0);\n\n        // Create a map to store the top view nodes\n        Map<Integer, Integer> v = new HashMap<>();\n\n        while (!queue.isEmpty()) {\n            Node node = queue.poll();\n            int horizontalDistance = hd.poll();\n\n            // If we haven't encountered a node at the current horizontal distance before\n            if (!v.containsKey(horizontalDistance)) {\n                v.put(horizontalDistance, node.val);\n            }\n\n            // Enqueue the children of the current node\n            if (node.left != null) {\n                queue.add(node.left);\n                hd.add(horizontalDistance - 1);\n            }\n            if (node.right != null) {\n                queue.add(node.right);\n                hd.add(horizontalDistance + 1);\n            }\n        }\n\n        // Print the top view nodes\n        for (int value : v.values()) {\n            System.out.print(value + \" \");\n        }\n        System.out.println();\n    }\n\n    public static void main(String[] args) {\n        Scanner scanner = new Scanner(System.in);\n        int N = scanner.nextInt();\n        scanner.nextLine();\n\n        Node root = null;\n        for (int i = 0; i < N - 1; i++) {\n            int pi = scanner.nextInt();\n            int ci = scanner.nextInt();\n            char Ri = scanner.next().charAt(0);\n            scanner.nextLine();\n\n            // Create a new node for the child\n            Node child = new Node(ci);\n\n            // Find the parent node\n            Node parent = root;\n            if (parent == null) {\n                parent = new Node(pi);\n                root = parent;\n            } else {\n                Node temp = parent;\n                while (temp.left != null && temp.right != null) {\n                    if (temp.left.val == pi) {\n                        temp = temp.left;\n                    } else if (temp.right.val == pi) {\n                        temp = temp.right;\n                    } else {\n                        // If the parent node is not found, create a new node for the parent\n                        Node newParent = new Node(pi);\n                        if (temp.left == null) {\n                            temp.left = newParent;\n                        } else {\n                            temp.right = newParent;\n                        }\n                        temp = newParent;\n                    }\n                }\n                if (temp.left == null) {\n                    temp.left = child;\n                } else {\n                    temp.right = child;\n                }\n            }\n        }\n\n        topView(root);\n\n    }\n}",
      "Python3": "from collections import deque\nfrom sys import stdin\n\n# Define the structure for a tree node\nclass Node:\n    def __init__(self, node_value):\n        self.val = node_value\n        self.left = None\n        self.right = None\n\n# Function to perform BFS traversal and find the top view\ndef topView(root):\n    if not root:\n        return\n\n    # Create a queue for BFS traversal\n    queue = deque([(root, 0)])\n\n    # Create a map to store the top view nodes\n    v = {}\n\n    while queue:\n        node, hd = queue.popleft()\n\n        # If we haven't encountered a node at the current horizontal distance before\n        if hd not in v:\n            v[hd] = node.val\n\n        # Enqueue the children of the current node\n        if node.left:\n            queue.append((node.left, hd - 1))\n        if node.right:\n            queue.append((node.right, hd + 1))\n\n    # Print the top view nodes\n    for key in sorted(v.keys()):\n        print(v[key], end=\" \")\n    print()\n\n# Read input\nN = int(stdin.readline())\n\nroot = None\nfor i in range(N - 1):\n    pi, ci, Ri = stdin.readline().split()\n    pi, ci = int(pi), int(ci)\n\n    # Create a new node for the child\n    child = Node(ci)\n\n    # Find the parent node\n    parent = root\n    if parent is None:\n        parent = Node(pi)\n        root = parent\n    else:\n        temp = parent\n        while temp.left is not None and temp.right is not None:\n            if temp.left.val == pi:\n                temp = temp.left\n            elif temp.right.val == pi:\n                temp = temp.right\n            else:\n                # If the parent node is not found, create a new node for the parent\n                newParent = Node(pi)\n                if temp.left is None:\n                    temp.left = newParent\n                else:\n                    temp.right = newParent\n                temp = newParent\n        if temp.left is None:\n            temp.left = child\n        else:\n            temp.right = child\n\n# Call the function\nif root is not None:\n    topView(root)\n",
      "JavaScript": "const fs = require('fs');\nconst lines = fs.readFileSync(0, 'utf-8').trim().split('\\n');\nlet idx = 0;\n\n// Define the structure for a tree node\nclass Node {\n    constructor(node_value) {\n        this.val = node_value;\n        this.left = null;\n        this.right = null;\n    }\n}\n\n// Function to perform BFS traversal and find the top view\nfunction topView(root) {\n    if (!root) return;\n\n    // Create a queue for BFS traversal\n    let queue = [[root, 0]];\n\n    // Create a map to store the top view nodes\n    let v = {};\n\n    while (queue.length > 0) {\n        let [node, hd] = queue.shift();\n\n        // If we haven't encountered a node at the current horizontal distance before\n        if (!(hd in v)) {\n            v[hd] = node.val;\n        }\n\n        // Enqueue the children of the current node\n        if (node.left) {\n            queue.push([node.left, hd - 1]);\n        }\n        if (node.right) {\n            queue.push([node.right, hd + 1]);\n        }\n    }\n\n    // Print the top view nodes\n    for (let key in v) {\n        process.stdout.write(v[key] + ' ');\n    }\n    process.stdout.write('\\n');\n}\n\n// Read input\nlet N = parseInt(lines[idx++]);\n\nlet root = null;\nfor (let i = 0; i < N - 1; i++) {\n    let [pi, ci, Ri] = lines[idx++].split(' ');\n    pi = parseInt(pi);\n    ci = parseInt(ci);\n\n    // Create a new node for the child\n    let child = new Node(ci);\n\n    // Find the parent node\n    let parent = root;\n    if (parent === null) {\n        parent = new Node(pi);\n        root = parent;\n    } else {\n        let temp = parent;\n        while (temp.left !== null && temp.right !== null) {\n            if (temp.left.val === pi) {\n                temp = temp.left;\n            } else if (temp.right.val === pi) {\n                temp = temp.right;\n            } else {\n                // If the parent node is not found, create a new node for the parent\n                let newParent = new Node(pi);\n                if (temp.left === null) {\n                    temp.left = newParent;\n                } else {\n                    temp.right = newParent;\n                }\n                temp = newParent;\n            }\n        }\n        if (temp.left === null) {\n            temp.left = child;\n        } else {\n            temp.right = child;\n        }\n    }\n}\n\n// Call the function\nif (root !== null) {\n    topView(root);\n}\n"
    }
  }
}