{
  "questionId": "Q1931",
  "title": "Exception Handling Demo",
  "slug": "exception-handling-demo",
  "version": 1,
  "createdAt": "2026-07-01",
  "updatedAt": "2026-07-04",
  "metadata": {
    "category": "Math",
    "difficulty": "Medium",
    "isPremium": false,
    "tags": [
      "Integer Division",
      "Exception Handling",
      "Parsing",
      "Math"
    ],
    "companyTags": [],
    "topicTags": [
      "Exception Handling"
    ]
  },
  "content": {
    "problemStatement": "Chef is learning how to manage errors using exception handling in Java. Help Chef write a program that:\n\n\nTakes two string as input: a `numerator` and a `denominator`.\nConvert them to integers.\nIf the denominator is 0, manually throw an `ArithmeticException` using `throw`\nUse multiple catch blocks to handle:\n\n`NumberFormatException` \u2014 if input is not a valid integer\n`ArithmeticException` \u2014 if denominator is zero\n`Exception` \u2014 any other unexpected error\n\n\nUse a `finally` block to print \"Execution Completed\" after handling exceptions.\n\nInput Format\n\nTwo space-separated strings representing numerator and denominator.\n\n\nOutput Format\n\nIf inputs are valid and denominator \u2260 0, print the division result:\n\n`Result: &lt;value&gt;`\nIf the denominator is zero, print: `Cannot divide by zero`\nIf either the numerator or denominator is not a valid integer, print: `Invalid number format`\nIf any other unexpected error occurs, print: `Some other exception occurred`\nIn all cases, print:\n\n`Execution Completed`",
    "constraints": [
      "1 \u2264 T \u2264 100",
      "Each input string length \u2264 20",
      "Parsed integers fit in 32\u2011bit signed range"
    ],
    "inputFormat": "The first line contains an integer T, the number of test cases. Each of the next T lines contains two space\u2011separated strings representing the numerator and the denominator.",
    "outputFormat": "For each test case, print one line with one of the following messages:\n- \"Result: <value>\" if both inputs are valid integers and the denominator is non\u2011zero.\n- \"Cannot divide by zero\" if the denominator is zero.\n- \"Invalid number format\" if either input cannot be parsed as an integer.\n- \"Some other exception occurred\" for any other unexpected error.\nImmediately after that line, always print \"Execution Completed\" on the next line.",
    "sampleTestCases": [
      {
        "id": "sample_1",
        "input": "2\n10 2\n10 0",
        "output": "Result: 5\nExecution Completed\nCannot divide by zero\nExecution Completed",
        "explanation": "First test case: 10/2 = 5, printed as Result: 5. Second test case: denominator is zero, so the program throws and catches an ArithmeticException and prints the message. In both cases the finally block prints Execution Completed."
      },
      {
        "id": "sample_2",
        "input": "2\nabc 5\n5 xyz",
        "output": "Invalid number format\nExecution Completed\nInvalid number format\nExecution Completed",
        "explanation": "Both test cases contain a non\u2011numeric token, causing NumberFormatException (or ValueError) and resulting in the message Invalid number format, followed by the finally message."
      },
      {
        "id": "sample_3",
        "input": "1\n2147483647 -1",
        "output": "Result: -2147483647\nExecution Completed",
        "explanation": "Both strings are valid integers within 32\u2011bit range; division yields -2147483647, which is printed, then the finally block runs."
      }
    ]
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "label": "minimum boundary",
        "input": "1\n0 1",
        "expectedOutput": "Result: 0\nExecution Completed",
        "weight": 2
      },
      {
        "id": "tc_2",
        "label": "maximum boundary",
        "input": "1\n2147483647 -1",
        "expectedOutput": "Result: -2147483647\nExecution Completed",
        "weight": 2
      },
      {
        "id": "tc_3",
        "label": "edge case",
        "input": "1\n-5 0",
        "expectedOutput": "Cannot divide by zero\nExecution Completed",
        "weight": 2
      },
      {
        "id": "tc_4",
        "label": "general case 1",
        "input": "1\n100 3",
        "expectedOutput": "Result: 33\nExecution Completed",
        "weight": 2
      },
      {
        "id": "tc_5",
        "label": "general case 2",
        "input": "1\n12a 5",
        "expectedOutput": "Invalid number format\nExecution Completed",
        "weight": 2
      }
    ],
    "totalTestCases": 5,
    "totalWeight": 10
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      2,
      2,
      2,
      2,
      2
    ],
    "maxScore": 10
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "For each test case we read the two strings. We try to convert them to integers. If conversion fails a NumberFormatException (or its language equivalent) is caught and we output the corresponding message. After successful conversion we check whether the denominator is zero; if it is we manually throw an ArithmeticException (or raise a ZeroDivisionError) and catch it. If no exception occurs we perform integer division (truncating toward zero) and output the result. A generic catch block handles any other unexpected exception. A finally block (or equivalent code placed after the try\u2011catch) prints \"Execution Completed\" for every test case. The algorithm is O(T) time and O(1) extra space.",
    "timeComplexity": "O(T) \u2013 each test case is processed in constant time.",
    "spaceComplexity": "O(1) \u2013 only a few variables are used per test case.",
    "code": {
      "C": "#include <stdio.h>\n#include <stdlib.h>\n#include <string.h>\n\nint main() {\n    int T;\n    if (scanf(\"%d\", &T) != 1) return 0;\n    for (int tc = 0; tc < T; ++tc) {\n        char numStr[32], denStr[32];\n        if (scanf(\"%31s %31s\", numStr, denStr) != 2) break;\n        char *endptr;\n        long num = strtol(numStr, &endptr, 10);\n        int numOk = (*endptr == '\\0');\n        long den = strtol(denStr, &endptr, 10);\n        int denOk = (*endptr == '\\0');\n        if (!numOk || !denOk) {\n            printf(\"Invalid number format\\n\");\n        } else if (den == 0) {\n            printf(\"Cannot divide by zero\\n\");\n        } else {\n            printf(\"Result: %ld\\n\", num / den);\n        }\n        printf(\"Execution Completed\\n\");\n    }\n    return 0;\n}",
      "C++": "#include <bits/stdc++.h>\nusing namespace std;\nint main() {\n    ios::sync_with_stdio(false);\n    cin.tie(nullptr);\n    int T;\n    if (!(cin >> T)) return 0;\n    while (T--) {\n        string numStr, denStr;\n        cin >> numStr >> denStr;\n        try {\n            int num = stoi(numStr);\n            int den = stoi(denStr);\n            if (den == 0) throw runtime_error(\"Cannot divide by zero\");\n            cout << \"Result: \" << (num / den) << '\\n';\n        } catch (invalid_argument const&) {\n            cout << \"Invalid number format\" << '\\n';\n        } catch (runtime_error const& e) {\n            cout << e.what() << '\\n';\n        } catch (...) {\n            cout << \"Some other exception occurred\" << '\\n';\n        }\n        cout << \"Execution Completed\" << '\\n';\n    }\n    return 0;\n}",
      "Java": "import java.io.*;\nimport java.util.*;\n\npublic class Main {\n    public static void main(String[] args) throws Exception {\n        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));\n        String line = br.readLine();\n        if (line == null) return;\n        int T = Integer.parseInt(line.trim());\n        for (int i = 0; i < T; i++) {\n            String[] parts = br.readLine().trim().split(\" \");\n            String numStr = parts[0];\n            String denStr = parts[1];\n            try {\n                int num = Integer.parseInt(numStr);\n                int den = Integer.parseInt(denStr);\n                if (den == 0) {\n                    throw new ArithmeticException(\"Cannot divide by zero\");\n                }\n                System.out.println(\"Result: \" + (num / den));\n            } catch (NumberFormatException e) {\n                System.out.println(\"Invalid number format\");\n            } catch (ArithmeticException e) {\n                System.out.println(e.getMessage());\n            } catch (Exception e) {\n                System.out.println(\"Some other exception occurred\");\n            } finally {\n                System.out.println(\"Execution Completed\");\n            }\n        }\n    }\n}",
      "Python3": "#!/usr/bin/env python3\nimport sys\n\ndef main():\n    data = sys.stdin.read().strip().split()\n    if not data:\n        return\n    t = int(data[0])\n    idx = 1\n    out = []\n    for _ in range(t):\n        if idx + 1 >= len(data):\n            break\n        num_str = data[idx]\n        den_str = data[idx + 1]\n        idx += 2\n        try:\n            num = int(num_str)\n            den = int(den_str)\n            if den == 0:\n                raise ZeroDivisionError(\"Cannot divide by zero\")\n            out.append(f\"Result: {num // den}\")\n        except ValueError:\n            out.append(\"Invalid number format\")\n        except ZeroDivisionError as e:\n            out.append(str(e))\n        except Exception:\n            out.append(\"Some other exception occurred\")\n        finally:\n            out.append(\"Execution Completed\")\n    sys.stdout.write(\"\\n\".join(out))\n\nif __name__ == \"__main__\":\n    main()",
      "JavaScript": "const fs = require('fs');\nconst input = fs.readFileSync(0, 'utf-8').trim().split(/\\s+/);\nlet idx = 0;\nconst T = parseInt(input[idx++], 10);\nlet out = [];\nfor (let tc = 0; tc < T; tc++) {\n  const numStr = input[idx++];\n  const denStr = input[idx++];\n  try {\n    const num = parseInt(numStr, 10);\n    const den = parseInt(denStr, 10);\n    if (isNaN(num) || isNaN(den)) throw new Error('Invalid number format');\n    if (den === 0) throw new Error('Cannot divide by zero');\n    out.push('Result: ' + Math.trunc(num / den));\n  } catch (e) {\n    if (e.message === 'Invalid number format') {\n      out.push('Invalid number format');\n    } else if (e.message === 'Cannot divide by zero') {\n      out.push('Cannot divide by zero');\n    } else {\n      out.push('Some other exception occurred');\n    }\n  } finally {\n    out.push('Execution Completed');\n  }\n}\nconsole.log(out.join('\\n'));"
    }
  }
}