{
  "questionId": "Q2365",
  "title": "Repeated String",
  "slug": "repeated-string",
  "version": 1,
  "createdAt": "2026-07-01",
  "updatedAt": "2026-07-04",
  "metadata": {
    "category": "Strings",
    "difficulty": "Medium",
    "isPremium": false,
    "tags": [
      "Substring",
      "Frequency",
      "Strings"
    ],
    "companyTags": [],
    "topicTags": [
      "Strings"
    ]
  },
  "content": {
    "problemStatement": "Given a string S (containing at most 105 lowercase English letters). You are requested to find out from continuous substrings a string having length from L to H, which appears the most times; if there are more than one answer, find the most length.",
    "constraints": [
      "1 <= L <= H <= 100",
      "1 <= length of S <= 10^5",
      "S contains only lowercase English letters"
    ],
    "inputFormat": "First line contains two positive integers L and H, representing the minimum and maximum length of the substring. The second line contains a string S, consisting of at most 10^5 lowercase English letters.",
    "outputFormat": "For each test case, output two integers on a line, representing the number of times the most frequent substring appears and the length of the substring.",
    "sampleTestCases": [
      {
        "id": "sample_1",
        "input": "3 5\naabcbcbca",
        "output": "2 4",
        "explanation": "The most frequent substring of length between 3 and 5 is 'bcbc' which appears twice."
      },
      {
        "id": "sample_2",
        "input": "3 5\nbaaaababababbababbab",
        "output": "6 3",
        "explanation": "The most frequent substring of length between 3 and 5 is 'bab' which appears six times."
      },
      {
        "id": "sample_3",
        "input": "1 4\nabcd",
        "output": "1 4",
        "explanation": "The most frequent substring of length between 1 and 4 is 'abcd' which appears once."
      }
    ]
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "label": "minimum boundary",
        "input": "1 1\na",
        "expectedOutput": "1 1",
        "weight": 2
      },
      {
        "id": "tc_2",
        "label": "maximum boundary",
        "input": "100 100\nabcdefghijklmnopqrstuvwxyz",
        "expectedOutput": "1 100",
        "weight": 2
      },
      {
        "id": "tc_3",
        "label": "uniform/edge case",
        "input": "3 5\naaaaa",
        "expectedOutput": "1 5",
        "weight": 2
      },
      {
        "id": "tc_4",
        "label": "tricky case",
        "input": "3 5\nabcabcabc",
        "expectedOutput": "3 3",
        "weight": 2
      },
      {
        "id": "tc_5",
        "label": "stress/general",
        "input": "3 5\nabcabcabcabcabc",
        "expectedOutput": "5 3",
        "weight": 2
      }
    ],
    "totalTestCases": 5,
    "totalWeight": 10
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      2,
      2,
      2,
      2,
      2
    ],
    "maxScore": 10
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "The problem can be solved by iterating over all possible substrings of the given string and counting their frequencies. We keep track of the maximum frequency and the corresponding length of the substring. If there are multiple substrings with the same maximum frequency, we choose the one with the maximum length.",
    "timeComplexity": "O(T * (H - L + 1) * N^2) per test case, where N is the length of the string S. This is because we are iterating over all possible substrings of S and counting their frequencies.",
    "spaceComplexity": "O(N) for storing the string S and its substrings.",
    "code": {
      "C": "#include <stdio.h>\n#include <string.h>\nint main() {\n    int T, L, H;\n    char S[100001];\n    scanf(\"%d\", &T);\n    while (T--) {\n        scanf(\"%d %d\", &L, &H);\n        scanf(\"%s\", S);\n        int max_freq = 0, max_len = 0;\n        for (int len = L; len <= H; len++) {\n            for (int i = 0; i <= strlen(S) - len; i++) {\n                char substr[len + 1];\n                strncpy(substr, S + i, len);\n                substr[len] = '\\0';\n                int freq = 0;\n                for (int j = 0; j <= strlen(S) - len; j++) {\n                    if (strncmp(S + j, substr, len) == 0) freq++;\n                }\n                if (freq > max_freq || (freq == max_freq && len > max_len)) {\n                    max_freq = freq;\n                    max_len = len;\n                }\n            }\n        }\n        printf(\"%d %d\\n\", max_freq, max_len);\n    }\n    return 0;\n}",
      "C++": "#include <iostream>\n#include <string>\nusing namespace std;\nint main() {\n    int T, L, H;\n    string S;\n    cin >> T;\n    while (T--) {\n        cin >> L >> H;\n        cin >> S;\n        int max_freq = 0, max_len = 0;\n        for (int len = L; len <= H; len++) {\n            for (int i = 0; i <= S.length() - len; i++) {\n                string substr = S.substr(i, len);\n                int freq = 0;\n                for (int j = 0; j <= S.length() - len; j++) {\n                    if (S.substr(j, len) == substr) freq++;\n                }\n                if (freq > max_freq || (freq == max_freq && len > max_len)) {\n                    max_freq = freq;\n                    max_len = len;\n                }\n            }\n        }\n        cout << max_freq << \" \" << max_len << endl;\n    }\n    return 0;\n}",
      "Java": "import java.util.Scanner;\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int T = sc.nextInt();\n        while (T-- > 0) {\n            int L = sc.nextInt(), H = sc.nextInt();\n            String S = sc.next();\n            int max_freq = 0, max_len = 0;\n            for (int len = L; len <= H; len++) {\n                for (int i = 0; i <= S.length() - len; i++) {\n                    String substr = S.substring(i, i + len);\n                    int freq = 0;\n                    for (int j = 0; j <= S.length() - len; j++) {\n                        if (S.substring(j, j + len).equals(substr)) freq++;\n                    }\n                    if (freq > max_freq || (freq == max_freq && len > max_len)) {\n                        max_freq = freq;\n                        max_len = len;\n                    }\n                }\n            }\n            System.out.println(max_freq + \" \" + max_len);\n        }\n    }\n}",
      "Python3": "import sys\nT = int(input())\nfor _ in range(T):\n    L, H = map(int, input().split())\n    S = input()\n    max_freq = 0\n    max_len = 0\n    for len_ in range(L, H + 1):\n        for i in range(len(S) - len_ + 1):\n            substr = S[i:i + len_]\n            freq = 0\n            for j in range(len(S) - len_ + 1):\n                if S[j:j + len_] == substr: freq += 1\n            if freq > max_freq or (freq == max_freq and len_ > max_len):\n                max_freq = freq\n                max_len = len_\n    print(max_freq, max_len)",
      "JavaScript": "const lines = require('fs').readFileSync(0, 'utf-8').trim().split('\\n');\nlet i = 0;\nconst readline = () => lines[i++];\nconst T = parseInt(readline());\nfor (let i = 0; i < T; i++) {\n    const [L, H] = readline().split(' ').map(Number);\n    const S = readline();\n    let max_freq = 0;\n    let max_len = 0;\n    for (let len = L; len <= H; len++) {\n        for (let j = 0; j <= S.length - len; j++) {\n            const substr = S.substring(j, j + len);\n            let freq = 0;\n            for (let k = 0; k <= S.length - len; k++) {\n                if (S.substring(k, k + len) === substr) freq++;\n            }\n            if (freq > max_freq || (freq === max_freq && len > max_len)) {\n                max_freq = freq;\n                max_len = len;\n            }\n        }\n    }\n    console.log(max_freq + ' ' + max_len);\n}"
    }
  }
}