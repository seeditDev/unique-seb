{
  "questionId": "Q6778",
  "title": "Vertex Cover",
  "slug": "vertex-cover",
  "version": 1,
  "createdAt": "2026-07-02",
  "updatedAt": "2026-07-04",
  "metadata": {
    "category": "Graph",
    "difficulty": "Hard",
    "isPremium": true,
    "tags": [
      "Vertex Cover",
      "Graph Partitioning"
    ],
    "companyTags": [],
    "topicTags": [
      "Graphs"
    ],
    "QBCategory": "CCD"
  },
  "content": {
    "problemStatement": "You are given an undirected graph G=(V,E)G = (V, E)G=(V,E) containing NNN nodes and MMM edges. The nodes are numbered from 1 to NNN. A subset CCC of VVV is a vertex cover if for every edge (u,v)∈E(u, v) \\in E(u,v)∈E, at least one of uuu and vvv belong to CCC. Note that C=VC = VC=V is always a vertex cover.\n\nConsider a partition of VVV into two sets AAA and BBB. It is said to be a valid partition if the following two conditions are satisfied: AAA should be a vertex cover. And for each iii such that 1≤i≤n/21 \\leq i \\leq n/21≤i≤n/2, nodes 2i2i2i and 2i−12i - 12i−1 don't belong to the same set (i.e., one belongs to set AAA and the other to set BBB).\n\nDetermine if a valid partition exists. If it exists, provide an example of one valid partition.\n\n\nInput Format\n\nThe first line of the input contains a single integer TTT denoting the number of test cases. The description of TTT test cases follows.\nThe first line of each test case contains two space-separated integers NNN and MMM denoting the number of nodes and number of edges in the graph respectively.\nEach of the following MMM lines contains two space-separated integers uuu and vvv denoting an edge between nodes uuu and vvv.\n\n\nOutput Format\n\nFor each test case, print a line containing the string `\"possible\"` (without quotes) if a solution exists or `\"impossible\"` otherwise.\nIf a solution exists, print a second line containing a binary string. The iii-th character of this string should be `'0'` if vertex iii is in set BBB or `'1'` if it is in set AAA.",
    "constraints": [
      "1 <= T <= 10^5",
      "1 <= N <= 2*10^5",
      "0 <= M <= 2*10^5",
      "1 <= u, v <= N",
      "Sum of N over all test cases <= 10^6",
      "Sum of M over all test cases <= 10^6"
    ],
    "inputFormat": "The first line contains a single integer T denoting the number of test cases. Each test case starts with a line containing two space-separated integers N and M, denoting the number of nodes and the number of edges in the graph, respectively. Following this, there are M lines, each containing two space-separated integers u and v, denoting an edge between nodes u and v.",
    "outputFormat": "For each test case, print a line containing the string 'possible' if a valid partition exists, or 'impossible' otherwise. If a solution exists, print a second line containing a binary string where the i-th character is '0' if vertex i is in set B or '1' if it is in set A.",
    "sampleTestCases": [
      {
        "id": "sample_1",
        "input": "2\n3 2\n1 2\n2 3\n4 5\n1 3\n2 4\n1 4 \n1 2\n2 3",
        "output": "possible\\n011\\nimpossible",
        "explanation": "Explanation:\nExample case 1: We can put nodes numbered 2 and 3 in set A and node 1 in set B. Note that this is a valid partition because set A is a vertex cover; also, nodes numbered 1 and 2 belong to different sets.\nExample case 2: There exists no partition which satisfies the conditions."
      }
    ]
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "label": "minimum boundary",
        "input": "1\n2 1\n1 2",
        "expectedOutput": "possible\n10",
        "weight": 2
      },
      {
        "id": "tc_2",
        "label": "Case 2",
        "input": "1\n2 1\n1 2",
        "expectedOutput": "possible\n10",
        "weight": 2
      },
      {
        "id": "tc_3",
        "label": "Case 3",
        "input": "1\n2 1\n1 2",
        "expectedOutput": "possible\n10",
        "weight": 2
      },
      {
        "id": "tc_4",
        "label": "Case 4",
        "input": "1\n2 1\n1 2",
        "expectedOutput": "possible\n10",
        "weight": 2
      },
      {
        "id": "tc_5",
        "label": "Case 5",
        "input": "1\n2 1\n1 2",
        "expectedOutput": "possible\n10",
        "weight": 2
      }
    ],
    "totalTestCases": 5,
    "totalWeight": 10
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      2,
      2,
      2,
      2,
      2
    ],
    "maxScore": 10
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "To solve this problem, we can use a graph traversal approach. We start by initializing two sets, A and B, to empty sets. Then, we iterate over each edge in the graph. For each edge (u, v), we check if u is in set A and v is in set B, or vice versa. If neither condition is met, we add u to set A and v to set B. We also keep track of the nodes that have been assigned to set A or set B. After iterating over all edges, we check if the partition is valid by verifying that each edge has at least one node in set A and that no two adjacent nodes are in the same set.",
    "timeComplexity": "O(T * (N + M)) — where T is the number of test cases, N is the number of nodes, and M is the number of edges. The reason is that we iterate over each edge in the graph for each test case.",
    "spaceComplexity": "O(N + M) — where N is the number of nodes and M is the number of edges. The reason is that we need to store the nodes and edges in the graph.",
    "code": {
      "C": "#include <stdio.h>\n#include <stdlib.h>\n\n// Structure to represent a graph\ntypedef struct {\n    int numNodes;\n    int numEdges;\n    int** edges;\n} Graph;\n\n// Function to create a new graph\nGraph* createGraph(int numNodes, int numEdges) {\n    Graph* graph = (Graph*) malloc(sizeof(Graph));\n    graph->numNodes = numNodes;\n    graph->numEdges = numEdges;\n    graph->edges = (int**) malloc(numEdges * sizeof(int*));\n    for (int i = 0; i < numEdges; i++) {\n        graph->edges[i] = (int*) malloc(2 * sizeof(int));\n    }\n    return graph;\n}\n\n// Function to add an edge to the graph\nvoid addEdge(Graph* graph, int edgeIndex, int node1, int node2) {\n    graph->edges[edgeIndex][0] = node1;\n    graph->edges[edgeIndex][1] = node2;\n}\n\n// Function to check if a valid partition exists\nvoid checkPartition(Graph* graph) {\n    int* setA = (int*) malloc(graph->numNodes * sizeof(int));\n    int* setB = (int*) malloc(graph->numNodes * sizeof(int));\n    for (int i = 0; i < graph->numNodes; i++) {\n        setA[i] = 0;\n        setB[i] = 0;\n    }\n    for (int i = 0; i < graph->numEdges; i++) {\n        int node1 = graph->edges[i][0] - 1;\n        int node2 = graph->edges[i][1] - 1;\n        if (!setA[node1] && !setB[node1]) {\n            setA[node1] = 1;\n        }\n        if (!setA[node2] && !setB[node2]) {\n            setB[node2] = 1;\n        }\n    }\n    int validPartition = 1;\n    for (int i = 0; i < graph->numEdges; i++) {\n        int node1 = graph->edges[i][0] - 1;\n        int node2 = graph->edges[i][1] - 1;\n        if (!setA[node1] && !setA[node2]) {\n            validPartition = 0;\n            break;\n        }\n    }\n    if (validPartition) {\n        printf(\"possible\\n\");\n        for (int i = 0; i < graph->numNodes; i++) {\n            if (setA[i]) {\n                printf(\"1\");\n            } else {\n                printf(\"0\");\n            }\n        }\n        printf(\"\\n\");\n    } else {\n        printf(\"impossible\\n\");\n    }\n    free(setA);\n    free(setB);\n}\n\nint main() {\n    int T;\n    scanf(\"%d\", &T);\n    for (int t = 0; t < T; t++) {\n        int N, M;\n        scanf(\"%d %d\", &N, &M);\n        Graph* graph = createGraph(N, M);\n        for (int i = 0; i < M; i++) {\n            int node1, node2;\n            scanf(\"%d %d\", &node1, &node2);\n            addEdge(graph, i, node1, node2);\n        }\n        checkPartition(graph);\n        for (int i = 0; i < M; i++) {\n            free(graph->edges[i]);\n        }\n        free(graph->edges);\n        free(graph);\n    }\n    return 0;\n}",
      "C++": "#include <iostream>\n#include <vector>\nusing namespace std;\n\n// Structure to represent a graph\nstruct Graph {\n    int numNodes;\n    int numEdges;\n    vector<pair<int, int>> edges;\n};\n\n// Function to create a new graph\nGraph createGraph(int numNodes, int numEdges) {\n    Graph graph;\n    graph.numNodes = numNodes;\n    graph.numEdges = numEdges;\n    return graph;\n}\n\n// Function to add an edge to the graph\nvoid addEdge(Graph& graph, pair<int, int> edge) {\n    graph.edges.push_back(edge);\n}\n\n// Function to check if a valid partition exists\nvoid checkPartition(Graph& graph) {\n    vector<int> setA(graph.numNodes, 0);\n    vector<int> setB(graph.numNodes, 0);\n    for (int i = 0; i < graph.numEdges; i++) {\n        int node1 = graph.edges[i].first - 1;\n        int node2 = graph.edges[i].second - 1;\n        if (!setA[node1] && !setB[node1]) {\n            setA[node1] = 1;\n        }\n        if (!setA[node2] && !setB[node2]) {\n            setB[node2] = 1;\n        }\n    }\n    int validPartition = 1;\n    for (int i = 0; i < graph.numEdges; i++) {\n        int node1 = graph.edges[i].first - 1;\n        int node2 = graph.edges[i].second - 1;\n        if (!setA[node1] && !setA[node2]) {\n            validPartition = 0;\n            break;\n        }\n    }\n    if (validPartition) {\n        cout << \"possible\" << endl;\n        for (int i = 0; i < graph.numNodes; i++) {\n            if (setA[i]) {\n                cout << \"1\";\n            } else {\n                cout << \"0\";\n            }\n        }\n        cout << endl;\n    } else {\n        cout << \"impossible\" << endl;\n    }\n}\n\nint main() {\n    int T;\n    cin >> T;\n    for (int t = 0; t < T; t++) {\n        int N, M;\n        cin >> N >> M;\n        Graph graph = createGraph(N, M);\n        for (int i = 0; i < M; i++) {\n            int node1, node2;\n            cin >> node1 >> node2;\n            addEdge(graph, make_pair(node1, node2));\n        }\n        checkPartition(graph);\n    }\n    return 0;\n}",
      "Java": "import java.util.Scanner;\npublic class Main {\n    // Structure to represent a graph\n    static class Graph {\n        int numNodes;\n        int numEdges;\n        int[][] edges;\n        public Graph(int numNodes, int numEdges) {\n            this.numNodes = numNodes;\n            this.numEdges = numEdges;\n            this.edges = new int[numEdges][2];\n        }\n    }\n    // Function to add an edge to the graph\n    public static void addEdge(Graph graph, int edgeIndex, int node1, int node2) {\n        graph.edges[edgeIndex][0] = node1;\n        graph.edges[edgeIndex][1] = node2;\n    }\n    // Function to check if a valid partition exists\n    public static void checkPartition(Graph graph) {\n        int[] setA = new int[graph.numNodes];\n        int[] setB = new int[graph.numNodes];\n        for (int i = 0; i < graph.numEdges; i++) {\n            int node1 = graph.edges[i][0] - 1;\n            int node2 = graph.edges[i][1] - 1;\n            if (setA[node1] == 0 && setB[node1] == 0) {\n                setA[node1] = 1;\n            }\n            if (setA[node2] == 0 && setB[node2] == 0) {\n                setB[node2] = 1;\n            }\n        }\n        int validPartition = 1;\n        for (int i = 0; i < graph.numEdges; i++) {\n            int node1 = graph.edges[i][0] - 1;\n            int node2 = graph.edges[i][1] - 1;\n            if (setA[node1] == 0 && setA[node2] == 0) {\n                validPartition = 0;\n                break;\n            }\n        }\n        if (validPartition == 1) {\n            System.out.println(\"possible\");\n            for (int i = 0; i < graph.numNodes; i++) {\n                if (setA[i] == 1) {\n                    System.out.print(\"1\");\n                } else {\n                    System.out.print(\"0\");\n                }\n            }\n            System.out.println();\n        } else {\n            System.out.println(\"impossible\");\n        }\n    }\n    public static void main(String[] args) {\n        Scanner scanner = new Scanner(System.in);\n        int T = scanner.nextInt();\n        for (int t = 0; t < T; t++) {\n            int N = scanner.nextInt();\n            int M = scanner.nextInt();\n            Graph graph = new Graph(N, M);\n            for (int i = 0; i < M; i++) {\n                int node1 = scanner.nextInt();\n                int node2 = scanner.nextInt();\n                addEdge(graph, i, node1, node2);\n            }\n            checkPartition(graph);\n        }\n    }\n}",
      "Python3": "class Graph:\n    def __init__(self, numNodes, numEdges):\n        self.numNodes = numNodes\n        self.numEdges = numEdges\n        self.edges = {}\n    def addEdge(self, node1, node2):\n        if node1 not in self.edges:\n            self.edges[node1] = []\n        if node2 not in self.edges:\n            self.edges[node2] = []\n        self.edges[node1].append(node2)\n        self.edges[node2].append(node1)\n    def checkPartition(self):\n        setA = [0] * self.numNodes\n        setB = [0] * self.numNodes\n        for node1 in self.edges:\n            for node2 in self.edges[node1]:\n                if setA[node1 - 1] == 0 and setB[node1 - 1] == 0:\n                    setA[node1 - 1] = 1\n                if setA[node2 - 1] == 0 and setB[node2 - 1] == 0:\n                    setB[node2 - 1] = 1\n        validPartition = 1\n        for node1 in self.edges:\n            for node2 in self.edges[node1]:\n                if setA[node1 - 1] == 0 and setA[node2 - 1] == 0:\n                    validPartition = 0\n                    break\n        if validPartition == 1:\n            print(\"possible\")\n            for i in range(self.numNodes):\n                if setA[i] == 1:\n                    print(\"1\", end=\"\")\n                else:\n                    print(\"0\", end=\"\")\n            print()\n        else:\n            print(\"impossible\")\nT = int(input())\nfor t in range(T):\n    N, M = map(int, input().split())\n    graph = Graph(N, M)\n    for i in range(M):\n        node1, node2 = map(int, input().split())\n        graph.addEdge(node1, node2)\n    graph.checkPartition()",
      "JavaScript": "const fs = require('fs');\nconst input = fs.readFileSync(0, 'utf-8').trim().split('\\n');\nlet idx = 0;\nfunction readLine() {\n    return input[idx++];\n}\nfunction readInt() {\n    return parseInt(readLine());\n}\nfunction readInts() {\n    return readLine().split(' ').map(x => parseInt(x));\n}\nfunction Graph(numNodes, numEdges) {\n    this.numNodes = numNodes;\n    this.numEdges = numEdges;\n    this.edges = [];\n}\nGraph.prototype.addEdge = function(node1, node2) {\n    this.edges.push([node1, node2]);\n};\nGraph.prototype.checkPartition = function() {\n    let setA = new Array(this.numNodes).fill(0);\n    let setB = new Array(this.numNodes).fill(0);\n    for (let i = 0; i < this.numEdges; i++) {\n        let node1 = this.edges[i][0] - 1;\n        let node2 = this.edges[i][1] - 1;\n        if (setA[node1] == 0 && setB[node1] == 0) {\n            setA[node1] = 1;\n        }\n        if (setA[node2] == 0 && setB[node2] == 0) {\n            setB[node2] = 1;\n        }\n    }\n    let validPartition = 1;\n    for (let i = 0; i < this.numEdges; i++) {\n        let node1 = this.edges[i][0] - 1;\n        let node2 = this.edges[i][1] - 1;\n        if (setA[node1] == 0 && setA[node2] == 0) {\n            validPartition = 0;\n            break;\n        }\n    }\n    if (validPartition == 1) {\n        console.log('possible');\n        for (let i = 0; i < this.numNodes; i++) {\n            if (setA[i] == 1) {\n                process.stdout.write('1');\n            } else {\n                process.stdout.write('0');\n            }\n        }\n        console.log();\n    } else {\n        console.log('impossible');\n    }\n};\nlet T = readInt();\nfor (let t = 0; t < T; t++) {\n    let [N, M] = readInts();\n    let graph = new Graph(N, M);\n    for (let i = 0; i < M; i++) {\n        let [node1, node2] = readInts();\n        graph.addEdge(node1, node2);\n    }\n    graph.checkPartition();\n}"
    }
  },
  "QBCategory": "CCD",
  "boilerPlates": {
    "C": "#include <stdio.h>\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "C++": "#include <iostream>\nusing namespace std;\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "Java": "import java.util.*;\nimport java.io.*;\n\npublic class Main {\n    public static void main(String[] args) throws IOException {\n        // your code goes here\n    }\n}",
    "Python3": "# your code goes here\n",
    "JavaScript": "// your code goes here\n"
  },
  "_internal": {
    "methodName": "",
    "params": []
  }
}