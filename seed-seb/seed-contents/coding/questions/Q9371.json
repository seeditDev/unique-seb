{
  "questionId": "Q9371",
  "title": "One-XOR Deletions",
  "slug": "one-xor-deletions",
  "version": 1,
  "createdAt": "2026-07-02",
  "updatedAt": "2026-07-04",
  "metadata": {
    "category": "Bit Manipulation | Greedy",
    "difficulty": "Medium",
    "isPremium": true,
    "tags": [
      "XOR",
      "Frequency Counting",
      "Greedy",
      "Array"
    ],
    "companyTags": [],
    "topicTags": [
      "Bit Manipulation"
    ],
    "QBCategory": "CCD"
  },
  "content": {
    "problemStatement": "Chef has with him an array AAA of length NNN. In one move, he can delete any element from AAA.\n\nFind the minimum number of deletions Chef must make so that the following condition holds:\n\n\nLet BBB denote the resulting array, and MMM be the length of BBB.\nThen, Bi⊕Bj≤1B_i \\oplus B_j \\leq 1Bi​⊕Bj​≤1 for every 1≤i,j≤M1 \\leq i, j \\leq M1≤i,j≤M.\nHere, ⊕\\oplus⊕ denotes the bitwise XOR operation.\n\nFor example, [3,3,3][3, 3, 3][3,3,3] and [6,7,6,7][6, 7, 6, 7][6,7,6,7] are valid final arrays, while [1,2][1, 2][1,2] and [6,7,8][6, 7, 8][6,7,8] are not (because 1⊕2=31 \\oplus 2 = 31⊕2=3 and 7⊕8=157\\oplus 8 = 157⊕8=15, respectively).\n\n\nInput Format\n\nThe first line of input will contain a single integer TTT, denoting the number of test cases.\nEach test case consists of two lines of input.\n\nThe first line of each test case contains a single integer NNN, denoting the length of array AAA.\nThe second line contains NNN space-separated integers A1,A2,…,ANA_1, A_2, \\ldots, A_NA1​,A2​,…,AN​ — the elements of array AAA.\n\n\n\n\nOutput Format\nFor each test case, output on a new line the answer: the minimum number of deletions required so that the given condition is satisfied.",
    "constraints": [
      "1 ≤ T ≤ 10^5",
      "1 ≤ N ≤ 3·10^5",
      "0 ≤ Ai ≤ N",
      "∑ N over all test cases ≤ 3·10^5"
    ],
    "inputFormat": "The first line contains an integer T, the number of test cases. Each test case consists of two lines: the first line contains an integer N, the length of the array. The second line contains N space‑separated integers A1, A2, …, AN.",
    "outputFormat": "For each test case output a single integer on its own line – the minimum number of deletions required so that for every pair of remaining elements Bi, Bj we have (Bi XOR Bj) ≤ 1.",
    "sampleTestCases": [
      {
        "id": "sample_1",
        "input": "4\n4\n2 2 2 2\n5\n3 4 3 4 4\n5\n1 2 3 4 0\n6\n5 5 5 6 6 6",
        "output": "0\n2\n3\n3",
        "explanation": "Explanation:\nTest case 111: The given array already satisfies the condition, no deletions need to be done.\nTest case 222: Chef can delete both the 333's to make the array [4,4,4][4, 4, 4][4,4,4], which satisfies the condition.\nTest case 333: Chef can use three moves as follows:\n\nDelete the 111, the array is now [2,3,4,0][2, 3, 4, 0][2,3,4,0].\nDelete the 444, the array is now [2,3,0][2, 3, 0][2,3,0].\nDelete the 000, the array is now [2,3][2, 3][2,3] which satisfies the condition.\n\nIt can be verified that using two or less deletions cannot give an array that satisfies the condition.\nTest case 444: Chef must either delete all the 555's or all the 666's."
      }
    ]
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "label": "Case 1",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_2",
        "label": "Case 2",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_3",
        "label": "Case 3",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_4",
        "label": "Case 4",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_5",
        "label": "Case 5",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      }
    ],
    "totalTestCases": 5,
    "totalWeight": 10
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      2,
      2,
      2,
      2,
      2
    ],
    "maxScore": 10
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "For the final array B the condition (Bi XOR Bj) ≤ 1 must hold for every pair. The only possible values that satisfy this are:\n1. All elements are equal (XOR = 0).\n2. The array contains exactly two distinct numbers that differ by 1 and the lower one is even, i.e., numbers x (even) and x+1 (odd). In this case x XOR (x+1) = 1 and any pair of equal numbers still gives 0.\nTherefore the optimal strategy is to keep as many elements as possible that belong to one of the above valid sets and delete the rest.\n\nAlgorithm per test case:\n- Build a frequency map freq[value] for all values in the array (values are ≤ N, so an array of size N+2 is enough).\n- Let maxSingle be the maximum frequency of any single value.\n- Let maxPair be the maximum of freq[e] + freq[e+1] taken over all even e (0 ≤ e ≤ N). Only even e can form a valid pair because only even‑odd consecutive numbers have XOR = 1.\n- The largest keepable subset size is best = max(maxSingle, maxPair).\n- Minimum deletions = N - best.\nThe algorithm runs in O(N) time per test case and O(N) additional memory for the frequency array.\n\nCorrectness Sketch:\n*If the kept set contains more than two distinct values, pick any three of them. At least two of those three differ by more than 1 in binary, giving XOR ≥ 2, contradicting the condition. Hence any valid final array can have at most two distinct values.\n*If it has two distinct values, they must be consecutive and the smaller one must be even; otherwise their XOR would have a set bit higher than the least significant bit, making it >1. Thus any valid final array is either a single value or an even‑odd consecutive pair.\n*Our algorithm enumerates exactly these possibilities and chooses the one that keeps the most elements, which clearly yields the minimum number of deletions.\n",
    "timeComplexity": "O(N) per test case (building frequencies and scanning even indices). The total over all test cases is O(∑N) ≤ 3·10^5.",
    "spaceComplexity": "O(N) for the frequency array (size N+2).",
    "code": {
      "C": "#include <stdio.h>\n#include <stdlib.h>\n\nint main(){\n    int T; if(scanf(\"%d\", &T)!=1) return 0;\n    while(T--){\n        int N; scanf(\"%d\", &N);\n        int maxVal = N; // Ai <= N\n        int *freq = (int*)calloc(maxVal+2, sizeof(int));\n        for(int i=0;i<N;i++){\n            int x; scanf(\"%d\", &x);\n            freq[x]++;\n        }\n        int maxSingle = 0, maxPair = 0;\n        for(int v=0; v<=maxVal; ++v){\n            if(freq[v] > maxSingle) maxSingle = freq[v];\n        }\n        for(int e=0; e<=maxVal; e+=2){\n            int cur = freq[e] + freq[e+1];\n            if(cur > maxPair) maxPair = cur;\n        }\n        int best = maxSingle > maxPair ? maxSingle : maxPair;\n        printf(\"%d\\n\", N - best);\n        free(freq);\n    }\n    return 0;\n}\n",
      "C++": "#include <bits/stdc++.h>\nusing namespace std;\nint main(){\n    ios::sync_with_stdio(false);\n    cin.tie(nullptr);\n    int T; if(!(cin>>T)) return 0;\n    while(T--){\n        int N; cin>>N;\n        vector<int> freq(N+2,0);\n        for(int i=0;i<N;++i){\n            int x; cin>>x; ++freq[x];\n        }\n        int maxSingle=0, maxPair=0;\n        for(int v=0; v<=N; ++v) maxSingle = max(maxSingle, freq[v]);\n        for(int e=0; e<=N; e+=2) maxPair = max(maxPair, freq[e]+freq[e+1]);\n        int best = max(maxSingle, maxPair);\n        cout << N - best << '\\n';\n    }\n    return 0;\n}\n",
      "Java": "import java.io.*;\nimport java.util.*;\n\npublic class Main {\n    public static void main(String[] args) throws Exception {\n        FastScanner fs = new FastScanner(System.in);\n        int T = fs.nextInt();\n        StringBuilder sb = new StringBuilder();\n        while (T-- > 0) {\n            int N = fs.nextInt();\n            int[] freq = new int[N + 2];\n            for (int i = 0; i < N; i++) {\n                int x = fs.nextInt();\n                freq[x]++;\n            }\n            int maxSingle = 0, maxPair = 0;\n            for (int v = 0; v <= N; v++) {\n                if (freq[v] > maxSingle) maxSingle = freq[v];\n            }\n            for (int e = 0; e <= N; e += 2) {\n                int cur = freq[e] + freq[e + 1];\n                if (cur > maxPair) maxPair = cur;\n            }\n            int best = Math.max(maxSingle, maxPair);\n            sb.append(N - best).append('\\n');\n        }\n        System.out.print(sb.toString());\n    }\n\n    // Fast scanner for large input\n    static class FastScanner {\n        private final InputStream in;\n        private final byte[] buffer = new byte[1 << 16];\n        private int ptr = 0, len = 0;\n        FastScanner(InputStream in) { this.in = in; }\n        private int readByte() throws IOException {\n            if (ptr >= len) {\n                len = in.read(buffer);\n                ptr = 0;\n                if (len <= 0) return -1;\n            }\n            return buffer[ptr++];\n        }\n        int nextInt() throws IOException {\n            int c, sign = 1, val = 0;\n            do { c = readByte(); } while (c <= ' ');\n            if (c == '-') { sign = -1; c = readByte(); }\n            while (c > ' ') {\n                val = val * 10 + (c - '0');\n                c = readByte();\n            }\n            return val * sign;\n        }\n    }\n}\n",
      "Python3": "import sys\n\ndef solve():\n    data = sys.stdin.read().strip().split()\n    if not data:\n        return\n    it = iter(data)\n    T = int(next(it))\n    out_lines = []\n    for _ in range(T):\n        N = int(next(it))\n        freq = [0] * (N + 2)\n        for _ in range(N):\n            x = int(next(it))\n            freq[x] += 1\n        max_single = max(freq)\n        max_pair = 0\n        for e in range(0, N + 1, 2):\n            cur = freq[e] + freq[e + 1]\n            if cur > max_pair:\n                max_pair = cur\n        best = max(max_single, max_pair)\n        out_lines.append(str(N - best))\n    sys.stdout.write('\\n'.join(out_lines))\n\nif __name__ == \"__main__\":\n    solve()\n",
      "JavaScript": "const fs = require('fs');\nconst input = fs.readFileSync(0, 'utf-8').trim().split(/\\s+/).map(Number);\nlet idx = 0;\nconst T = input[idx++];\nlet outputs = [];\nfor (let t = 0; t < T; ++t) {\n  const N = input[idx++];\n  const freq = new Array(N + 2).fill(0);\n  for (let i = 0; i < N; ++i) {\n    const x = input[idx++];\n    freq[x]++;\n  }\n  let maxSingle = 0, maxPair = 0;\n  for (let v = 0; v <= N; ++v) if (freq[v] > maxSingle) maxSingle = freq[v];\n  for (let e = 0; e <= N; e += 2) {\n    const cur = freq[e] + freq[e + 1];\n    if (cur > maxPair) maxPair = cur;\n  }\n  const best = Math.max(maxSingle, maxPair);\n  outputs.push(String(N - best));\n}\nconsole.log(outputs.join('\\n'));\n"
    }
  },
  "QBCategory": "CCD",
  "boilerPlates": {
    "C": "#include <stdio.h>\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "C++": "#include <iostream>\nusing namespace std;\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "Java": "import java.util.*;\nimport java.io.*;\n\npublic class Main {\n    public static void main(String[] args) throws IOException {\n        // your code goes here\n    }\n}",
    "Python3": "# your code goes here\n",
    "JavaScript": "// your code goes here\n"
  },
  "_internal": {
    "methodName": "",
    "params": []
  }
}