{
  "questionId": "Q9317",
  "title": "Red Green Grids",
  "slug": "red-green-grids",
  "version": 1,
  "createdAt": "2026-07-02",
  "updatedAt": "2026-07-04",
  "metadata": {
    "category": "Math | Combinatorics | Dynamic Programming",
    "difficulty": "Hard",
    "isPremium": true,
    "tags": [
      "Binomial Coefficients",
      "Modular Arithmetic",
      "Exponentiation",
      "Paths in Grid"
    ],
    "companyTags": [],
    "topicTags": [
      "Combinatorics & Probability"
    ],
    "QBCategory": "CCD"
  },
  "content": {
    "problemStatement": "There is an empty grid (all cells are colored white) of NNN rows and MMM columns.\n\nChef can fill each cell with either `RED` or `GREEN` color.\n\nChef defines a valid path as a path of cells starting from (1,1)(1,1)(1,1) and ending at (N,M)(N,M)(N,M), where, Chef moves either `right` or `down` by 111 cell each time.\n\nFor a particular grid, Chef defines the score of the grid as the number of valid paths containing **equal** number of `RED` and `GREEN` cells.\n\nFind the **sum** of scores of all possible colored grids containing NNN rows and MMM columns.\n\nSince the answer can be large, output it modulo 998244353998244353998244353.\n\n\nInput Format\n\nThe first line of input will contain a single integer TTT, denoting the number of test cases.\nEach test case consists of two integers NNN and MMM - denoting the dimensions of the grid.\n\n\nOutput Format\nFor each test case, output the **sum** of scores of all possible colored grids containing NNN rows and MMM columns, modulo 998244353998244353998244353.",
    "constraints": [
      "1 <= T <= 1000",
      "1 <= N, M <= 1000"
    ],
    "inputFormat": "The first line contains an integer T, the number of test cases. Each of the next T lines contains two integers N and M denoting the number of rows and columns of the grid.",
    "outputFormat": "For each test case output a single line containing the sum of scores of all possible colourings of an N×M grid, modulo 998244353.",
    "sampleTestCases": [
      {
        "id": "sample_1",
        "input": "3\n1 1\n1 2\n2 3",
        "output": "0\n2\n72",
        "explanation": "Explanation:\nTest case 111: There are 222 possible coloured grids of size 1×11 \\times 11×1. In the first grid, the one and only cell is colored RED. In the second grid, the one and only cell is colored GREEN. Neither of the grids have a valid path with equal number of red and green cells. Thus, score of each grid is 000.\nTest case 222: There are 444 possible coloured grids of the given dimension:\n\nFor grids 111 and 444, the scores are 000 as there are no valid paths with equal red and green cells. Grids 222 and 333 have score 111 each. The valid path with equal red and green cells is (1,1)→(1,2)(1,1) \\rightarrow (1,2)(1,1)→(1,2) in both the grids.\nThus, the sum of scores of all possible coloured grids is 222."
      }
    ]
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "label": "Case 1",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_2",
        "label": "Case 2",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_3",
        "label": "Case 3",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_4",
        "label": "Case 4",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_5",
        "label": "Case 5",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      }
    ],
    "totalTestCases": 5,
    "totalWeight": 10
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      2,
      2,
      2,
      2,
      2
    ],
    "maxScore": 10
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "For a fixed path of length L = N+M-1 cells, the number of colourings that give equal numbers of RED and GREEN on that path is zero if L is odd, otherwise C(L, L/2). The remaining cells (N*M-L of them) can be coloured arbitrarily, giving a factor 2^{N*M-L}. All paths have the same length, and the number of paths from (1,1) to (N,M) moving only right or down is C(N+M-2, N-1). Hence the required sum is:\n\nIf (N+M-1) is odd: answer = 0.\nOtherwise: answer = C(N+M-2, N-1) * C(N+M-1, (N+M-1)/2) * 2^{N*M-(N+M-1)} (mod MOD).\n\nWe pre‑compute factorials and inverse factorials up to 2000 (the maximum possible N+M) to obtain binomial coefficients in O(1). Power of two is computed with fast exponentiation. The overall complexity per test case is O(log MOD).",
    "timeComplexity": "O(T * log MOD) – each test case needs a modular exponentiation.",
    "spaceComplexity": "O(MAX) where MAX ≈ 2000 for factorial tables.",
    "code": {
      "C": "#include <stdio.h>\n#include <stdint.h>\nconst int MOD = 998244353;\nlong long modpow(long long a, long long e){\n    long long r=1;\n    while(e){\n        if(e&1) r=r*a%MOD;\n        a=a*a%MOD;\n        e>>=1;\n    }\n    return r;\n}\nint main(){\n    int T; if(scanf(\"%d\",&T)!=1) return 0;\n    const int MAX = 2005;\n    static long long fact[MAX], invfact[MAX];\n    fact[0]=1;\n    for(int i=1;i<MAX;i++) fact[i]=fact[i-1]*i%MOD;\n    invfact[MAX-1]=modpow(fact[MAX-1], MOD-2);\n    for(int i=MAX-2;i>=0;i--) invfact[i]=invfact[i+1]*(i+1)%MOD;\n    while(T--){\n        long long N,M; scanf(\"%lld %lld\",&N,&M);\n        long long L = N+M-1;\n        if(L%2){\n            printf(\"0\\n\");\n            continue;\n        }\n        long long paths = fact[N+M-2]*invfact[N-1]%MOD*invfact[M-1]%MOD;\n        long long waysOnPath = fact[L]*invfact[L/2]%MOD*invfact[L/2]%MOD;\n        long long rest = modpow(2, N*M - L);\n        long long ans = paths * waysOnPath % MOD * rest % MOD;\n        printf(\"%lld\\n\", ans);\n    }\n    return 0;\n}",
      "C++": "#include <bits/stdc++.h>\nusing namespace std;\nconst long long MOD = 998244353LL;\nlong long modpow(long long a, long long e){\n    long long r=1;\n    while(e){\n        if(e&1) r=r*a%MOD;\n        a=a*a%MOD;\n        e>>=1;\n    }\n    return r;\n}\nint main(){\n    ios::sync_with_stdio(false);\n    cin.tie(nullptr);\n    int T; if(!(cin>>T)) return 0;\n    const int MAX = 2005;\n    vector<long long> fact(MAX), invfact(MAX);\n    fact[0]=1;\n    for(int i=1;i<MAX;i++) fact[i]=fact[i-1]*i%MOD;\n    invfact[MAX-1]=modpow(fact[MAX-1], MOD-2);\n    for(int i=MAX-2;i>=0;i--) invfact[i]=invfact[i+1]*(i+1)%MOD;\n    while(T--){\n        long long N,M; cin>>N>>M;\n        long long L = N+M-1;\n        if(L%2){\n            cout<<0<<\"\\n\";\n            continue;\n        }\n        long long paths = fact[N+M-2]*invfact[N-1]%MOD*invfact[M-1]%MOD;\n        long long waysOnPath = fact[L]*invfact[L/2]%MOD*invfact[L/2]%MOD;\n        long long rest = modpow(2, N*M - L);\n        long long ans = paths * waysOnPath % MOD * rest % MOD;\n        cout<<ans<<\"\\n\";\n    }\n    return 0;\n}",
      "Java": "import java.io.*;\nimport java.util.*;\n\npublic class Main {\n    static final long MOD = 998244353L;\n    static long modPow(long a, long e){\n        long r = 1;\n        while(e>0){\n            if((e & 1) == 1) r = r * a % MOD;\n            a = a * a % MOD;\n            e >>= 1;\n        }\n        return r;\n    }\n    public static void main(String[] args) throws Exception {\n        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));\n        StringTokenizer st = new StringTokenizer(br.readLine());\n        int T = Integer.parseInt(st.nextToken());\n        final int MAX = 2005;\n        long[] fact = new long[MAX];\n        long[] invFact = new long[MAX];\n        fact[0] = 1;\n        for(int i=1;i<MAX;i++) fact[i] = fact[i-1] * i % MOD;\n        invFact[MAX-1] = modPow(fact[MAX-1], MOD-2);\n        for(int i=MAX-2;i>=0;i--) invFact[i] = invFact[i+1] * (i+1) % MOD;\n        StringBuilder sb = new StringBuilder();\n        for(int tc=0; tc<T; tc++){\n            st = new StringTokenizer(br.readLine());\n            long N = Long.parseLong(st.nextToken());\n            long M = Long.parseLong(st.nextToken());\n            long L = N + M - 1;\n            if((L & 1) == 1){\n                sb.append(0).append('\\n');\n                continue;\n            }\n            long paths = fact[(int)(N+M-2)] * invFact[(int)(N-1)] % MOD * invFact[(int)(M-1)] % MOD;\n            long waysOnPath = fact[(int)L] * invFact[(int)(L/2)] % MOD * invFact[(int)(L/2)] % MOD;\n            long rest = modPow(2L, N*M - L);\n            long ans = paths * waysOnPath % MOD * rest % MOD;\n            sb.append(ans).append('\\n');\n        }\n        System.out.print(sb.toString());\n    }\n}",
      "Python3": "import sys\nMOD = 998244353\n\ndef mod_pow(a, e):\n    r = 1\n    while e:\n        if e & 1:\n            r = r * a % MOD\n        a = a * a % MOD\n        e >>= 1\n    return r\n\nMAX = 2005\nfact = [1] * MAX\nfor i in range(1, MAX):\n    fact[i] = fact[i-1] * i % MOD\ninv_fact = [1] * MAX\ninv_fact[-1] = mod_pow(fact[-1], MOD-2)\nfor i in range(MAX-2, -1, -1):\n    inv_fact[i] = inv_fact[i+1] * (i+1) % MOD\n\ndef nCr(n, r):\n    if r < 0 or r > n:\n        return 0\n    return fact[n] * inv_fact[r] % MOD * inv_fact[n-r] % MOD\n\ninput_data = sys.stdin.read().strip().split()\nif not input_data:\n    sys.exit()\nit = iter(input_data)\nT = int(next(it))\nout = []\nfor _ in range(T):\n    N = int(next(it)); M = int(next(it))\n    L = N + M - 1\n    if L % 2 == 1:\n        out.append('0')\n        continue\n    paths = nCr(N+M-2, N-1)\n    ways_on_path = nCr(L, L//2)\n    rest = mod_pow(2, N*M - L)\n    ans = paths * ways_on_path % MOD * rest % MOD\n    out.append(str(ans))\nprint('\\n'.join(out))",
      "JavaScript": "const fs = require('fs');\nconst MOD = 998244353n;\nfunction modPow(a, e){\n  let r = 1n;\n  while(e > 0n){\n    if(e & 1n) r = r * a % MOD;\n    a = a * a % MOD;\n    e >>= 1n;\n  }\n  return r;\n}\nconst input = fs.readFileSync(0, 'utf-8').trim().split(/\\s+/);\nlet idx = 0;\nif(input.length===0){process.exit(0);} \nconst T = Number(input[idx++]);\nconst MAX = 2005;\nconst fact = new Array(MAX).fill(0n);\nconst invFact = new Array(MAX).fill(0n);\nfact[0] = 1n;\nfor(let i=1;i<MAX;i++) fact[i] = fact[i-1] * BigInt(i) % MOD;\ninvFact[MAX-1] = modPow(fact[MAX-1], MOD-2n);\nfor(let i=MAX-2;i>=0;i--) invFact[i] = invFact[i+1] * BigInt(i+1) % MOD;\nfunction nCr(n, r){\n  if(r<0 || r>n) return 0n;\n  return fact[n] * invFact[r] % MOD * invFact[n-r] % MOD;\n}\nlet out = [];\nfor(let t=0;t<T;t++){\n  const N = Number(input[idx++]);\n  const M = Number(input[idx++]);\n  const L = N + M - 1;\n  if(L % 2 === 1){\n    out.push('0');\n    continue;\n  }\n  const paths = nCr(N+M-2, N-1);\n  const waysOnPath = nCr(L, Math.floor(L/2));\n  const rest = modPow(2n, BigInt(N*M - L));\n  const ans = paths * waysOnPath % MOD * rest % MOD;\n  out.push(ans.toString());\n}\nconsole.log(out.join('\\n'));\n"
    }
  },
  "QBCategory": "CCD",
  "boilerPlates": {
    "C": "#include <stdio.h>\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "C++": "#include <iostream>\nusing namespace std;\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "Java": "import java.util.*;\nimport java.io.*;\n\npublic class Main {\n    public static void main(String[] args) throws IOException {\n        // your code goes here\n    }\n}",
    "Python3": "# your code goes here\n",
    "JavaScript": "// your code goes here\n"
  },
  "_internal": {
    "methodName": "",
    "params": []
  }
}