{
  "questionId": "Q10325",
  "title": "Largest K",
  "slug": "largest-k",
  "version": 1,
  "createdAt": "2026-07-02",
  "updatedAt": "2026-07-04",
  "metadata": {
    "category": "Greedy | Sorting",
    "difficulty": "Medium",
    "isPremium": true,
    "tags": [
      "Frequency Counting",
      "Sorting",
      "Prefix Sum",
      "Greedy Selection"
    ],
    "companyTags": [],
    "topicTags": [
      "Greedy Algorithms"
    ],
    "QBCategory": "CCD"
  },
  "content": {
    "problemStatement": "You are given an array AAA of size NNN.\n\nFind the **largest** integer KKK such that there exists a subsequence SSS of length KKK where KKK is divisible by the number of **distinct** elements in SSS.\n\n\nInput Format\n\nThe first line contains a single integer TTT, denoting the number of test cases.\nThe first line of each test case contains a positive integer NNN, the length of array AAA.\nThe second line contains NNN space-separated integers, A1,A2,…,ANA_1, A_2, \\ldots, A_NA1​,A2​,…,AN​ −-− denoting the array AAA.\n\n\nOutput Format\nFor each test case, output the largest valid KKK.",
    "constraints": [
      "1 ≤ T ≤ 10^4",
      "1 ≤ N ≤ 2·10^5",
      "1 ≤ Ai ≤ N",
      "Sum of N over all test cases ≤ 2·10^5"
    ],
    "inputFormat": "The first line contains an integer T, the number of test cases. For each test case, the first line contains an integer N, the length of the array. The second line contains N space‑separated integers A1 … AN.",
    "outputFormat": "For each test case output a single line containing the largest integer K such that there exists a subsequence of length K whose length is divisible by the number of distinct elements in that subsequence.",
    "sampleTestCases": [
      {
        "id": "sample_1",
        "input": "3\n2\n2 1\n4\n1 2 1 3\n5\n1 5 3 2 4",
        "output": "2\n3\n5",
        "explanation": "Explanation:\nTest case 111: Consider the subsequence [2,1][2, 1][2,1] consisting of 222 distinct elements. The length of subsequence is 222 which is divisible by the number of distinct elements.\nTest case 222: Consider the subsequence [2,1,3][2, 1, 3][2,1,3] consisting of 333 distinct elements. The length of subsequence is 333 which is divisible by the number of distinct elements."
      }
    ]
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "label": "Case 1",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_2",
        "label": "Case 2",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_3",
        "label": "Case 3",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_4",
        "label": "Case 4",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_5",
        "label": "Case 5",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      }
    ],
    "totalTestCases": 5,
    "totalWeight": 10
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      2,
      2,
      2,
      2,
      2
    ],
    "maxScore": 10
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "For a fixed set D of distinct values (|D| = d) the longest subsequence we can build using only values from D has length equal to the total number of occurrences of those values in the whole array, i.e. total = Σ_{x∈D} freq[x]. Any length up to total is achievable by discarding some occurrences while preserving order. The required K must be a multiple of d, therefore the best K for this D is the largest multiple of d not exceeding total: K_D = floor(total / d) * d = total - (total mod d). To maximise K we should, for each possible d, pick the d values with the highest frequencies because that maximises total. Hence: 1. Count frequencies of all distinct numbers. 2. Sort the frequencies in non‑increasing order: f1 ≥ f2 ≥ … ≥ fm (m = number of distinct values). 3. Compute prefix sums pref[i] = f1 + … + fi. 4. For each i (i = 1 … m) compute candidate = pref[i] - (pref[i] % i). 5. Answer is the maximum candidate over all i. The algorithm runs in O(N log N) per test case due to sorting, and O(N) memory.",
    "timeComplexity": "O(N log N) per test case (dominated by sorting the frequency list). The total over all test cases is O((ΣN) log (ΣN)).",
    "spaceComplexity": "O(N) for storing frequencies and the sorted list (the sum of distinct values across a test case).",
    "code": {
      "C": "#include <stdio.h>\n#include <stdlib.h>\n\nint cmp_desc(const void *a, const void *b) {\n    int x = *(int *)b - *(int *)a; // descending\n    return x;\n}\n\nint main() {\n    int T; if (scanf(\"%d\", &T)!=1) return 0;\n    const int MAXN = 200000 + 5;\n    static int freq[MAXN];\n    static int distinct[MAXN];\n    while (T--) {\n        int N; scanf(\"%d\", &N);\n        int distinctCnt = 0;\n        for (int i = 0; i < N; ++i) {\n            int x; scanf(\"%d\", &x);\n            if (freq[x] == 0) distinct[distinctCnt++] = x;\n            freq[x]++;\n        }\n        int *f = (int *)malloc(distinctCnt * sizeof(int));\n        for (int i = 0; i < distinctCnt; ++i) f[i] = freq[distinct[i]];\n        qsort(f, distinctCnt, sizeof(int), cmp_desc);\n        long long pref = 0, ans = 0;\n        for (int i = 0; i < distinctCnt; ++i) {\n            pref += f[i];\n            long long d = i + 1;\n            long long cand = pref - (pref % d);\n            if (cand > ans) ans = cand;\n        }\n        printf(\"%lld\\n\", ans);\n        // reset frequencies\n        for (int i = 0; i < distinctCnt; ++i) freq[distinct[i]] = 0;\n        free(f);\n    }\n    return 0;\n}",
      "C++": "#include <bits/stdc++.h>\nusing namespace std;\nint main(){\n    ios::sync_with_stdio(false);\n    cin.tie(nullptr);\n    int T; if(!(cin>>T)) return 0;\n    const int MAXN = 200000 + 5;\n    static int freq[MAXN];\n    vector<int> distinct;\n    while(T--){\n        int N; cin>>N;\n        distinct.clear();\n        for(int i=0;i<N;++i){\n            int x; cin>>x;\n            if(freq[x]==0) distinct.push_back(x);\n            ++freq[x];\n        }\n        vector<int> f; f.reserve(distinct.size());\n        for(int v: distinct) f.push_back(freq[v]);\n        sort(f.begin(), f.end(), greater<int>());\n        long long pref=0, ans=0;\n        for(size_t i=0;i<f.size();++i){\n            pref+=f[i];\n            long long d=i+1;\n            long long cand = pref - (pref % d);\n            ans = max(ans, cand);\n        }\n        cout<<ans<<\"\\n\";\n        for(int v: distinct) freq[v]=0;\n    }\n    return 0;\n}",
      "Java": "import java.io.*;\nimport java.util.*;\n\npublic class Main {\n    static final int MAXN = 200000 + 5;\n    static int[] freq = new int[MAXN];\n    public static void main(String[] args) throws Exception {\n        FastScanner fs = new FastScanner(System.in);\n        int T = fs.nextInt();\n        StringBuilder sb = new StringBuilder();\n        int[] distinct = new int[MAXN];\n        while (T-- > 0) {\n            int N = fs.nextInt();\n            int distinctCnt = 0;\n            for (int i = 0; i < N; i++) {\n                int x = fs.nextInt();\n                if (freq[x] == 0) distinct[distinctCnt++] = x;\n                freq[x]++;\n            }\n            int[] f = new int[distinctCnt];\n            for (int i = 0; i < distinctCnt; i++) f[i] = freq[distinct[i]];\n            Arrays.sort(f);\n            // reverse to descending\n            long ans = 0, pref = 0;\n            for (int i = distinctCnt - 1, d = 1; i >= 0; i--, d++) {\n                pref += f[i];\n                long cand = pref - (pref % d);\n                if (cand > ans) ans = cand;\n            }\n            sb.append(ans).append('\\n');\n            // reset\n            for (int i = 0; i < distinctCnt; i++) freq[distinct[i]] = 0;\n        }\n        System.out.print(sb.toString());\n    }\n\n    // Fast scanner for Java\n    static class FastScanner {\n        private final InputStream in;\n        private final byte[] buffer = new byte[1 << 16];\n        private int ptr = 0, len = 0;\n        FastScanner(InputStream in) { this.in = in; }\n        private int readByte() throws IOException {\n            if (ptr >= len) {\n                len = in.read(buffer);\n                ptr = 0;\n                if (len <= 0) return -1;\n            }\n            return buffer[ptr++];\n        }\n        int nextInt() throws IOException {\n            int c, sign = 1, val = 0;\n            do { c = readByte(); } while (c <= ' ');\n            if (c == '-') { sign = -1; c = readByte(); }\n            while (c > ' ') {\n                val = val * 10 + (c - '0');\n                c = readByte();\n            }\n            return val * sign;\n        }\n    }\n}",
      "Python3": "import sys\n\ndef solve():\n    data = sys.stdin.read().strip().split()\n    if not data:\n        return\n    it = iter(data)\n    T = int(next(it))\n    out_lines = []\n    for _ in range(T):\n        N = int(next(it))\n        freq = {}\n        for _ in range(N):\n            x = int(next(it))\n            freq[x] = freq.get(x, 0) + 1\n        f = sorted(freq.values(), reverse=True)\n        pref = 0\n        ans = 0\n        for i, val in enumerate(f, 1):\n            pref += val\n            cand = pref - (pref % i)\n            if cand > ans:\n                ans = cand\n        out_lines.append(str(ans))\n    sys.stdout.write('\\n'.join(out_lines))\n\nif __name__ == \"__main__\":\n    solve()",
      "JavaScript": "const fs = require('fs');\nconst input = fs.readFileSync(0, 'utf-8').trim().split(/\\s+/).map(Number);\nlet idx = 0;\nconst T = input[idx++];\nlet outputs = [];\nfor (let t = 0; t < T; ++t) {\n  const N = input[idx++];\n  const freq = new Map();\n  for (let i = 0; i < N; ++i) {\n    const x = input[idx++];\n    freq.set(x, (freq.get(x) || 0) + 1);\n  }\n  const f = Array.from(freq.values()).sort((a, b) => b - a);\n  let pref = 0;\n  let ans = 0;\n  for (let i = 0; i < f.length; ++i) {\n    pref += f[i];\n    const d = i + 1;\n    const cand = pref - (pref % d);\n    if (cand > ans) ans = cand;\n  }\n  outputs.push(String(ans));\n}\nconsole.log(outputs.join('\\n'));\n"
    }
  },
  "QBCategory": "CCD",
  "boilerPlates": {
    "C": "#include <stdio.h>\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "C++": "#include <iostream>\nusing namespace std;\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "Java": "import java.util.*;\nimport java.io.*;\n\npublic class Main {\n    public static void main(String[] args) throws IOException {\n        // your code goes here\n    }\n}",
    "Python3": "# your code goes here\n",
    "JavaScript": "// your code goes here\n"
  },
  "_internal": {
    "methodName": "",
    "params": []
  }
}