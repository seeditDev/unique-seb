{
  "questionId": "Q11983",
  "title": "Two sum in BST",
  "slug": "two-sum-iv-input-is-a-bst",
  "version": 1,
  "createdAt": "2026-07-05",
  "updatedAt": "2026-07-05",
  "metadata": {
    "category": "Binary Search Tree",
    "difficulty": "Hard",
    "isPremium": true,
    "tags": [
      "Binary Search Tree",
      "Two Sum"
    ],
    "companyTags": [],
    "topicTags": [],
    "QBCategory": "CCD"
  },
  "content": {
    "problemStatement": "Given the root of a Binary Search Tree and an integer k, return true if there exist two elements in the BST such that their sum is equal to k, or false otherwise.",
    "constraints": [
      "1 <= Node.val <= 10^5",
      "1 <= k <= 2 * 10^5",
      "The number of nodes in the tree is in the range [1, 10^4]"
    ],
    "inputFormat": "The input will contain the root of a Binary Search Tree and an integer k.",
    "outputFormat": "The output will be true if there exist two elements in the BST such that their sum is equal to k, or false otherwise.",
    "sampleTestCases": [
      {
        "id": "sample_1",
        "input": "Binary Search Tree: 5 3 6 2 4 -1 7, K = 9",
        "output": "True",
        "explanation": ""
      },
      {
        "id": "sample_2",
        "input": "Binary Search Tree: 7 3 10 2 6 9 11 1 5 8 -1 -1 -1 -1 -1 4 -1 -1 -1",
        "output": "True\n**Explanation: **",
        "explanation": ""
      }
    ]
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "label": "min boundary",
        "input": "Binary Search Tree: 1, K = 2",
        "expectedOutput": "False",
        "weight": 2
      },
      {
        "id": "tc_2",
        "label": "max boundary",
        "input": "Binary Search Tree: 100000, K = 200000",
        "expectedOutput": "False",
        "weight": 2
      },
      {
        "id": "tc_3",
        "label": "uniform case",
        "input": "Binary Search Tree: 5 5 5, K = 10",
        "expectedOutput": "True",
        "weight": 2
      },
      {
        "id": "tc_4",
        "label": "tricky case",
        "input": "Binary Search Tree: 0, K = 0",
        "expectedOutput": "False",
        "weight": 2
      },
      {
        "id": "tc_5",
        "label": "mixed case",
        "input": "Binary Search Tree: 5 3 6 2 4 -1 7, K = 11",
        "expectedOutput": "True",
        "weight": 2
      }
    ],
    "totalTestCases": 5,
    "totalWeight": 10
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      2,
      2,
      2,
      2,
      2
    ],
    "maxScore": 10
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "Use a set to store the values of the nodes in the tree and check if k - node.val exists in the set.",
    "timeComplexity": "O(n)",
    "spaceComplexity": "O(n)",
    "code": {
      "C": "#include <stdio.h>\n#include <stdlib.h>\n\n// Define the structure for a tree node\ntypedef struct TreeNode {\n    int val;\n    struct TreeNode *left;\n    struct TreeNode *right;\n} TreeNode;\n\n// Function to create a new tree node\nTreeNode* createTreeNode(int val) {\n    TreeNode* newNode = (TreeNode*) malloc(sizeof(TreeNode));\n    if (!newNode) {\n        printf(\"Memory error\n\");\n        return NULL;\n    }\n    newNode->val = val;\n    newNode->left = newNode->right = NULL;\n    return newNode;\n}\n\n// Function to check if there exist two elements in the BST such that their sum is equal to k\nint findTarget(TreeNode* root, int k) {\n    // Create a set to store the values of the nodes in the tree\n    int* set = (int*) malloc(100000 * sizeof(int));\n    int size = 0;\n\n    // Function to perform inorder traversal and store the values in the set\n    void inorderTraversal(TreeNode* root) {\n        if (root) {\n            inorderTraversal(root->left);\n            set[size++] = root->val;\n            inorderTraversal(root->right);\n        }\n    }\n\n    inorderTraversal(root);\n\n    // Check if k - node.val exists in the set\n    for (int i = 0; i < size; i++) {\n        for (int j = i + 1; j < size; j++) {\n            if (set[i] + set[j] == k) {\n                free(set);\n                return 1;\n            }\n        }\n    }\n\n    free(set);\n    return 0;\n}\n\nint main() {\n    // Create the binary search tree\n    TreeNode* root = createTreeNode(5);\n    root->left = createTreeNode(3);\n    root->right = createTreeNode(6);\n    root->left->left = createTreeNode(2);\n    root->left->right = createTreeNode(4);\n    root->right->right = createTreeNode(7);\n\n    int k = 11;\n    int result = findTarget(root, k);\n\n    if (result) {\n        printf(\"True\\n\");\n    } else {\n        printf(\"False\\n\");\n    }\n\n    return 0;\n}",
      "C++": "#include <bits/stdc++.h>\nusing namespace std;\n\n// This structure defines a node in a Binary Search Tree\nstruct TreeNode {\n    // This stores the value of the node\n    int val;\n    // Pointer to the left child\n    TreeNode* left;\n    // Pointer to the right child\n    TreeNode* right;\n\n    // Constructor initializes node with value\n    TreeNode(int x) : val(x), left(nullptr), right(nullptr) {}\n};\n\n// This class contains the brute force solution\nclass Solution {\npublic:\n    // Function to check if there exists a pair with given sum in BST\n    bool findTarget(TreeNode* root, int k) {\n        // Vector stores inorder traversal of BST\n        vector<int> inorder;\n\n        // Call helper to fill vector\n        inorderTraversal(root, inorder);\n\n        // Initialize two pointers for searching in sorted inorder array\n        int left = 0;\n        int right = inorder.size() - 1;\n\n        // Loop until pointers meet\n        while (left < right) {\n            // Calculate sum of current pair\n            int sum = inorder[left] + inorder[right];\n\n            // If sum equals k, we found a pair\n            if (sum == k) {\n                return true;\n            }\n            // If sum smaller than k, move left pointer forward\n            else if (sum < k) {\n                left++;\n            }\n            // If sum larger than k, move right pointer backward\n            else {\n                right--;\n            }\n        }\n\n        // If no such pair found, return false\n        return false;\n    }\n\nprivate:\n    // Helper function to perform inorder traversal\n    void inorderTraversal(TreeNode* root, vector<int>& inorder) {\n        // If root is null, stop recursion\n        if (!root) return;\n\n        // Traverse left subtree\n        inorderTraversal(root->left, inorder);\n        // Add current node value to vector\n        inorder.push_back(root->val);\n        // Traverse right subtree\n        inorderTraversal(root->right, inorder);\n    }\n};\n\n// Driver code\nint main() {\n    // Create sample BST\n    TreeNode* root = new TreeNode(5);\n    root->left = new TreeNode(3);\n    root->right = new TreeNode(6);\n    root->left->left = new TreeNode(2);\n    root->left->right = new TreeNode(4);\n    root->right->right = new TreeNode(7);\n\n    // Create solution object\n    Solution solution;\n\n    // Define target sum\n    int target = 11;\n\n    // Call function to check for pair\n    bool exists = solution.findTarget(root, target);\n\n    // Print result\n    if (exists) cout << \"True\" << endl;\n    else cout << \"False\" << endl;\n\n    return 0;\n}",
      "Java": "import java.util.*;\n// This class defines a node in a Binary Search Tree\nclass TreeNode {\n    // Value of node\n    int val;\n    // Left child\n    TreeNode left;\n    // Right child\n    TreeNode right;\n\n    // Constructor initializes node with value\n    TreeNode(int x) {\n        val = x;\n        left = null;\n        right = null;\n    }\n}\n\n// This class contains the brute force solution\nclass Solution {\n    // Function to check if there exists a pair with sum k\n    public boolean findTarget(TreeNode root, int k) {\n        // List stores inorder traversal values\n        java.util.List<Integer> inorder = new java.util.ArrayList<>();\n\n        // Fill list using helper function\n        inorderTraversal(root, inorder);\n\n        // Two pointers for searching in sorted list\n        int left = 0;\n        int right = inorder.size() - 1;\n\n        // Loop until pointers meet\n        while (left < right) {\n            // Calculate sum of current pair\n            int sum = inorder.get(left) + inorder.get(right);\n\n            // If sum equals k, return true\n            if (sum == k) {\n                return true;\n            }\n            // If sum smaller, move left pointer\n            else if (sum < k) {\n                left++;\n            }\n            // If sum larger, move right pointer\n            else {\n                right--;\n            }\n        }\n\n        // If no pair found, return false\n        return false;\n    }\n\n    // Helper function to perform inorder traversal\n    private void inorderTraversal(TreeNode root, java.util.List<Integer> inorder) {\n        // If node is null, stop recursion\n        if (root == null) return;\n        // Visit left subtree\n        inorderTraversal(root.left, inorder);\n        // Add current node value\n        inorder.add(root.val);\n        // Visit right subtree\n        inorderTraversal(root.right, inorder);\n    }\n}\n\n// Driver class\npublic class Main {\n    // Function to print inorder traversal\n    public static void printInOrder(TreeNode root) {\n        if (root == null) return;\n        printInOrder(root.left);\n        System.out.print(root.val + \" \");\n        printInOrder(root.right);\n    }\n\n    // Main function\n    public static void main(String[] args) {\n        // Create sample BST\n        TreeNode root = new TreeNode(5);\n        root.left = new TreeNode(3);\n        root.right = new TreeNode(6);\n        root.left.left = new TreeNode(2);\n        root.left.right = new TreeNode(4);\n        root.right.right = new TreeNode(7);\n\n        // Print inorder traversal\n        System.out.print(\"Tree Initialized: \");\n        printInOrder(root);\n        System.out.println();\n\n        // Create solution object\n        Solution solution = new Solution();\n\n        // Define target sum\n        int target = 11;\n\n        // Check if pair exists\n        boolean exists = solution.findTarget(root, target);\n\n        // Print result\n        if (exists) System.out.println(\"True\");\n        else System.out.println(\"False\");\n    }\n}",
      "Python3": "# This class defines a node in a Binary Search Tree\nclass TreeNode:\n    # Constructor initializes value and children\n    def __init__(self, x):\n        self.val = x\n        self.left = None\n        self.right = None\n\n# This class contains the brute force solution\nclass Solution:\n    # Function checks if pair with target sum exists\n    def findTarget(self, root, k):\n        # List stores inorder traversal values\n        inorder = []\n        # Fill list using helper\n        self.inorderTraversal(root, inorder)\n\n        # Two pointers for searching\n        left = 0\n        right = len(inorder) - 1\n\n        # Loop until pointers meet\n        while left < right:\n            # Calculate sum of current pair\n            total = inorder[left] + inorder[right]\n\n            # If sum equals k, return True\n            if total == k:\n                return True\n            # If sum smaller, move left pointer\n            elif total < k:\n                left += 1\n            # If sum larger, move right pointer\n            else:\n                right -= 1\n\n        # If no pair found, return False\n        return False\n\n    # Helper function to do inorder traversal\n    def inorderTraversal(self, root, inorder):\n        # If node is None, stop recursion\n        if not root:\n            return\n        # Visit left subtree\n        self.inorderTraversal(root.left, inorder)\n        # Add current node value\n        inorder.append(root.val)\n        # Visit right subtree\n        self.inorderTraversal(root.right, inorder)\n\n# Function prints inorder traversal\ndef printInOrder(root):\n    if not root:\n        return\n    printInOrder(root.left)\n    print(root.val, end=\" \")\n    printInOrder(root.right)\n\n# Driver code\nif __name__ == \"__main__\":\n    # Create sample BST\n    root = TreeNode(5)\n    root.left = TreeNode(3)\n    root.right = TreeNode(6)\n    root.left.left = TreeNode(2)\n    root.left.right = TreeNode(4)\n    root.right.right = TreeNode(7)\n\n    # Print inorder traversal\n    print(\"Tree Initialized: \", end=\"\")\n    printInOrder(root)\n    print()\n\n    # Create solution object\n    solution = Solution()\n\n    # Define target sum\n    target = 11\n\n    # Check if pair exists\n    exists = solution.findTarget(root, target)\n\n    # Print result\n    if exists:\n        print(\"True\")\n    else:\n        print(\"False\")",
      "JavaScript": "// This class defines a node in a Binary Search Tree\nclass TreeNode {\n    // Constructor initializes value and children\n    constructor(x) {\n        this.val = x;\n        this.left = null;\n        this.right = null;\n    }\n}\n\n// This class contains the brute force solution\nclass Solution {\n    // Function checks if pair with target sum exists\n    findTarget(root, k) {\n        // Array stores inorder traversal values\n        let inorder = [];\n        // Fill array using helper\n        this.inorderTraversal(root, inorder);\n\n        // Two pointers for searching\n        let left = 0;\n        let right = inorder.length - 1;\n\n        // Loop until pointers meet\n        while (left < right) {\n            // Calculate sum of current pair\n            let sum = inorder[left] + inorder[right];\n\n            // If sum equals k, return true\n            if (sum === k) {\n                return true;\n            }\n            // If sum smaller, move left pointer\n            else if (sum < k) {\n                left++;\n            }\n            // If sum larger, move right pointer\n            else {\n                right--;\n            }\n        }\n\n        // If no pair found, return false\n        return false;\n    }\n\n    // Helper function does inorder traversal\n    inorderTraversal(root, inorder) {\n        // If node is null, stop recursion\n        if (!root) return;\n        // Visit left subtree\n        this.inorderTraversal(root.left, inorder);\n        // Add current node value\n        inorder.push(root.val);\n        // Visit right subtree\n        this.inorderTraversal(root.right, inorder);\n    }\n}\n\n// Function prints inorder traversal\nfunction printInOrder(root) {\n    if (!root) return;\n    printInOrder(root.left);\n    process.stdout.write(root.val + \" \");\n    printInOrder(root.right);\n}\n\n// Driver function\nfunction main() {\n    // Create sample BST\n    let root = new TreeNode(5);\n    root.left = new TreeNode(3);\n    root.right = new TreeNode(6);\n    root.left.left = new TreeNode(2);\n    root.left.right = new TreeNode(4);\n    root.right.right = new TreeNode(7);\n\n    // Print inorder traversal\n    process.stdout.write(\"Tree Initialized: \");\n    printInOrder(root);\n    console.log();\n\n    // Create solution object\n    let solution = new Solution();\n\n    // Define target sum\n    let target = 11;\n\n    // Check if pair exists\n    let exists = solution.findTarget(root, target);\n\n    // Print result\n    if (exists) {\n        console.log(\"True\");\n    } else {\n        console.log(\"False\");\n    }\n}\n\n// Run driver\nmain();"
    }
  },
  "QBCategory": "CCD",
  "boilerPlates": {
    "C": "#include <stdio.h>\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "C++": "#include <iostream>\nusing namespace std;\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "Java": "import java.util.*;\nimport java.io.*;\n\npublic class Main {\n    public static void main(String[] args) throws IOException {\n        // your code goes here\n    }\n}",
    "Python3": "# your code goes here\n",
    "JavaScript": "// your code goes here\n"
  },
  "_internal": {
    "methodName": "",
    "params": []
  }
}