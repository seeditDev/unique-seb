{
  "questionId": "Q8953",
  "title": "All Roads Lead To Rome",
  "slug": "all-roads-lead-to-rome",
  "version": 1,
  "createdAt": "2026-07-02",
  "updatedAt": "2026-07-04",
  "metadata": {
    "category": "Graph",
    "difficulty": "Hard",
    "isPremium": true,
    "tags": [
      "Topological Sort",
      "Directed Acyclic Graph",
      "Sink Nodes",
      "Cycle Detection"
    ],
    "companyTags": [],
    "topicTags": [
      "Graphs"
    ],
    "QBCategory": "CCD"
  },
  "content": {
    "problemStatement": "The country of Chefland is a group of NNN cities having MMM directed roads between its cities. It takes 111 second to travel on any road.\n\nChef has decided to travel through this country. His travel plan goes as follows -\n\n\nHe will randomly choose a starting city.\nWhile there are outgoing roads in his current city, he will randomly choose one of them and travel it. He can choose roads he has already traveled through and visit cities multiple times.\nHe will stop if there are no outgoing roads in the current city or NNN seconds have passed.\nChef will be disappointed if after traveling he does not end up in the capital city.\n\nThe ministers have not yet decided upon the capital city. They would like to choose it in such a way that Chef is not disappointed by his trip. To make their decision a bit easier, they can build some new roads between cities.\n\nFind the **minimum** number of roads that need to be built such that the ministers can choose a capital city that doesn't disappoint Chef no matter where his trip starts or how it progresses.\n\nIt is possible that the current road system is such that no matter which roads are added, a capital that doesn't disappoint Chef cannot be chosen. In this case, print −1-1−1 instead.\n\n\nInput Format\n\nThe first line will contain TTT, denoting the number of test cases. The description of TTT test cases follows.\nThe first line of each test case contains a single integer NNN, denoting the number of cities.\nThe second line of each test case contains a single integer MMM, denoting the number of roads in the city initially.\nThen MMM lines follow each containing two integers AAA and BBB, denoting a road from city AAA to city BBB.\n\n\nOutput Format\nFor each test case, output a single line denoting the minimum number of roads you have to build, or −1-1−1 if it is not possible to satisfy the given condition.",
    "constraints": [
      "1 ≤ T ≤ 5·10^4",
      "1 ≤ N ≤ 2·10^5",
      "0 ≤ M ≤ min(N·(N‑1), 4·10^5)",
      "Sum of N + M over all test cases ≤ 10^6",
      "No self‑loops or duplicate edges"
    ],
    "inputFormat": "The first line contains an integer T, the number of test cases. For each test case: the first line contains an integer N, the number of cities. The second line contains an integer M, the number of directed roads. Then M lines follow, each containing two integers A and B (1‑based), denoting a road from city A to city B.",
    "outputFormat": "For each test case output a single line containing the minimum number of roads that must be added so that a capital city can be chosen without disappointing Chef, or -1 if it is impossible.",
    "sampleTestCases": [
      {
        "id": "sample_1",
        "input": "2\n3\n2\n1 2\n2 3\n2\n2\n1 2\n2 1",
        "output": "0\n-1",
        "explanation": "Explanation:\nTest case 111: No matter where Chef starts, he will end up in city 333, so it can be made the capital city and we do not need to construct any roads.\nTest case 222: No new roads can be constructed. Chef will always end up in whichever city he starts from, so the ministers cannot decide upon a capital — choosing city 111 as the capital would disappoint Chef if his trip started from city 222 and vice versa."
      }
    ]
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "label": "Case 1",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_2",
        "label": "Case 2",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_3",
        "label": "Case 3",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_4",
        "label": "Case 4",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_5",
        "label": "Case 5",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      }
    ],
    "totalTestCases": 5,
    "totalWeight": 10
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      2,
      2,
      2,
      2,
      2
    ],
    "maxScore": 10
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "For each test case we need to know whether the existing directed graph contains a cycle. If a cycle exists, no set of added edges can make Chef always end in a single capital, because a cycle would allow Chef to wander indefinitely and stop after N seconds at a non‑capital city. Hence answer is -1.\nIf the graph is acyclic, every weakly connected component has at least one sink (a vertex with out‑degree 0). Chef will stop at any sink, so to guarantee a unique capital we must eliminate all sinks except one. The cheapest way is to pick any sink as the capital and add a single outgoing edge from each of the other sinks directly to this capital (or to any vertex that can already reach the capital). Adding such edges never creates a cycle because the capital has out‑degree 0. Therefore the minimum number of new roads required is (number_of_sinks – 1).\nAlgorithm per test case:\n1. Read N, M and build adjacency list; also store out‑degree of each vertex.\n2. Perform Kahn's topological sort (queue of vertices with indegree 0). Count how many vertices are removed. If the count < N, a directed cycle exists → output -1.\n3. If acyclic, count vertices with out‑degree 0 (sinks). Answer = sinks‑1 (if sinks≥1, which is always true for a DAG).\nComplexities: O(N+M) time and O(N+M) memory per test case, fitting the constraints.",
    "timeComplexity": "O(N + M) per test case",
    "spaceComplexity": "O(N + M) per test case",
    "code": {
      "C": "#include <stdio.h>\n#include <stdlib.h>\n\ntypedef struct Node {int v; struct Node* next;} Node;\n\nint main(){\n    int T; if(scanf(\"%d\", &T)!=1) return 0;\n    while(T--){\n        int N,M; scanf(\"%d %d\", &N, &M);\n        int *indeg = calloc(N, sizeof(int));\n        int *outdeg = calloc(N, sizeof(int));\n        Node **adj = calloc(N, sizeof(Node*));\n        for(int i=0;i<M;i++){\n            int a,b; scanf(\"%d %d\", &a, &b); a--; b--;\n            Node* nd = malloc(sizeof(Node)); nd->v=b; nd->next=adj[a]; adj[a]=nd;\n            indeg[b]++; outdeg[a]++;\n        }\n        // Kahn's algorithm\n        int *queue = malloc(N*sizeof(int)); int qs=0, qe=0;\n        for(int i=0;i<N;i++) if(indeg[i]==0) queue[qe++]=i;\n        int visited=0;\n        while(qs<qe){\n            int u=queue[qs++]; visited++;\n            for(Node* p=adj[u]; p; p=p->next){\n                int v=p->v; indeg[v]--; if(indeg[v]==0) queue[qe++]=v;\n            }\n        }\n        if(visited!=N){\n            printf(\"-1\\n\");\n        }else{\n            int sinks=0; for(int i=0;i<N;i++) if(outdeg[i]==0) sinks++;\n            printf(\"%d\\n\", sinks-1);\n        }\n        // free memory\n        for(int i=0;i<N;i++) while(adj[i]){Node* tmp=adj[i]; adj[i]=tmp->next; free(tmp);}        \n        free(adj); free(indeg); free(outdeg); free(queue);\n    }\n    return 0;\n}\n",
      "C++": "#include <bits/stdc++.h>\nusing namespace std;\nint main(){\n    ios::sync_with_stdio(false);\n    cin.tie(nullptr);\n    int T; if(!(cin>>T)) return 0;\n    while(T--){\n        int N,M; cin>>N>>M;\n        vector<vector<int>> adj(N);\n        vector<int> indeg(N,0), outdeg(N,0);\n        for(int i=0;i<M;i++){\n            int a,b;cin>>a>>b;--a;--b;\n            adj[a].push_back(b);\n            indeg[b]++; outdeg[a]++;\n        }\n        queue<int> q;\n        for(int i=0;i<N;i++) if(indeg[i]==0) q.push(i);\n        int visited=0;\n        while(!q.empty()){\n            int u=q.front();q.pop(); visited++;\n            for(int v:adj[u]) if(--indeg[v]==0) q.push(v);\n        }\n        if(visited!=N){\n            cout<<-1<<\"\\n\";\n        }else{\n            int sinks=0; for(int i=0;i<N;i++) if(outdeg[i]==0) sinks++;\n            cout<<sinks-1<<\"\\n\";\n        }\n    }\n    return 0;\n}\n",
      "Java": "import java.io.*;\nimport java.util.*;\npublic class Main {\n    public static void main(String[] args) throws Exception {\n        FastScanner fs = new FastScanner(System.in);\n        int T = fs.nextInt();\n        StringBuilder sb = new StringBuilder();\n        while (T-- > 0) {\n            int N = fs.nextInt();\n            int M = fs.nextInt();\n            List<Integer>[] adj = new ArrayList[N];\n            for (int i = 0; i < N; i++) adj[i] = new ArrayList<>();\n            int[] indeg = new int[N];\n            int[] outdeg = new int[N];\n            for (int i = 0; i < M; i++) {\n                int a = fs.nextInt() - 1;\n                int b = fs.nextInt() - 1;\n                adj[a].add(b);\n                indeg[b]++;\n                outdeg[a]++;\n            }\n            ArrayDeque<Integer> q = new ArrayDeque<>();\n            for (int i = 0; i < N; i++) if (indeg[i] == 0) q.add(i);\n            int visited = 0;\n            while (!q.isEmpty()) {\n                int u = q.poll();\n                visited++;\n                for (int v : adj[u]) {\n                    if (--indeg[v] == 0) q.add(v);\n                }\n            }\n            if (visited != N) {\n                sb.append(-1).append('\\n');\n            } else {\n                int sinks = 0;\n                for (int d : outdeg) if (d == 0) sinks++;\n                sb.append(sinks - 1).append('\\n');\n            }\n        }\n        System.out.print(sb.toString());\n    }\n    // Fast scanner for large input\n    static class FastScanner {\n        private final InputStream in;\n        private final byte[] buffer = new byte[1 << 16];\n        private int ptr = 0, len = 0;\n        FastScanner(InputStream in) { this.in = in; }\n        private int readByte() throws IOException {\n            if (ptr >= len) {\n                len = in.read(buffer);\n                ptr = 0;\n                if (len <= 0) return -1;\n            }\n            return buffer[ptr++];\n        }\n        int nextInt() throws IOException {\n            int c, sign = 1, val = 0;\n            do { c = readByte(); } while (c <= ' ');\n            if (c == '-') { sign = -1; c = readByte(); }\n            while (c > ' ') {\n                val = val * 10 + (c - '0');\n                c = readByte();\n            }\n            return val * sign;\n        }\n    }\n}\n",
      "Python3": "import sys\nfrom collections import deque\n\ndef solve():\n    data = sys.stdin.read().strip().split()\n    if not data:\n        return\n    it = iter(data)\n    T = int(next(it))\n    out_lines = []\n    for _ in range(T):\n        N = int(next(it)); M = int(next(it))\n        adj = [[] for _ in range(N)]\n        indeg = [0]*N\n        outdeg = [0]*N\n        for _ in range(M):\n            a = int(next(it))-1\n            b = int(next(it))-1\n            adj[a].append(b)\n            indeg[b] += 1\n            outdeg[a] += 1\n        q = deque([i for i in range(N) if indeg[i]==0])\n        visited = 0\n        while q:\n            u = q.popleft()\n            visited += 1\n            for v in adj[u]:\n                indeg[v] -= 1\n                if indeg[v]==0:\n                    q.append(v)\n        if visited != N:\n            out_lines.append('-1')\n        else:\n            sinks = sum(1 for d in outdeg if d==0)\n            out_lines.append(str(sinks-1))\n    sys.stdout.write('\\n'.join(out_lines))\n\nif __name__ == \"__main__\":\n    solve()\n",
      "JavaScript": "const fs = require('fs');\nconst input = fs.readFileSync(0, 'utf-8').trim().split(/\\s+/).map(Number);\nlet idx = 0;\nconst T = input[idx++];\nlet results = [];\nfor (let t = 0; t < T; t++) {\n  const N = input[idx++];\n  const M = input[idx++];\n  const adj = Array.from({ length: N }, () => []);\n  const indeg = new Array(N).fill(0);\n  const outdeg = new Array(N).fill(0);\n  for (let i = 0; i < M; i++) {\n    const a = input[idx++] - 1;\n    const b = input[idx++] - 1;\n    adj[a].push(b);\n    indeg[b]++;\n    outdeg[a]++;\n  }\n  const queue = [];\n  for (let i = 0; i < N; i++) if (indeg[i] === 0) queue.push(i);\n  let head = 0;\n  let visited = 0;\n  while (head < queue.length) {\n    const u = queue[head++];\n    visited++;\n    for (const v of adj[u]) {\n      indeg[v]--;\n      if (indeg[v] === 0) queue.push(v);\n    }\n  }\n  if (visited !== N) {\n    results.push('-1');\n  } else {\n    let sinks = 0;\n    for (let d of outdeg) if (d === 0) sinks++;\n    results.push(String(sinks - 1));\n  }\n}\nconsole.log(results.join('\\n'));\n"
    }
  },
  "QBCategory": "CCD",
  "boilerPlates": {
    "C": "#include <stdio.h>\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "C++": "#include <iostream>\nusing namespace std;\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "Java": "import java.util.*;\nimport java.io.*;\n\npublic class Main {\n    public static void main(String[] args) throws IOException {\n        // your code goes here\n    }\n}",
    "Python3": "# your code goes here\n",
    "JavaScript": "// your code goes here\n"
  },
  "_internal": {
    "methodName": "",
    "params": []
  }
}