{
  "questionId": "Q1689",
  "title": "PreOrder Traversal",
  "slug": "preorder-traversal",
  "version": 1,
  "createdAt": "2026-07-01",
  "updatedAt": "2026-07-04",
  "metadata": {
    "category": "Trees",
    "difficulty": "Easy",
    "isPremium": false,
    "tags": [
      "Binary Tree",
      "Trees",
      "Preorder Traversal",
      "Tree Traversal"
    ],
    "companyTags": [],
    "topicTags": [
      "Trees"
    ]
  },
  "content": {
    "problemStatement": "Given a binary tree, complete the function preOrderTraversal to print the Preorder traversal of the tree.\n\n\nInput Format\n\nThe first line of the input contains a single integer NNN \u2014 the number of nodes in the binary tree.\nNext N\u22121N - 1N\u22121 lines contain three space separated characters pi,ci,Rip_i, c_i, R_ipi\u200b,ci\u200b,Ri\u200b, describing the tree with edge informations - pip_ipi\u200b is an integer denoting the parent node of the iiith edge, cic_ici\u200b is child node, and the letter RiR_iRi\u200b denotes whether the child is left child or right child, it's value is `L` if cic_ici\u200b is left child of pip_ipi\u200b, else `R`.\n\n\nOutput Format\nOutput on the single line, NNN space separated integers - the preorder traversal of the given tree.",
    "constraints": [
      "1 <= N <= 100000",
      "1 <= pi, ci <= 100000",
      "Ri is either 'L' or 'R'",
      "All node labels are distinct within a test case"
    ],
    "inputFormat": "The first line contains a single integer N, the number of nodes in the binary tree. Next N-1 lines contain three space-separated characters pi, ci, Ri, describing the tree with edge information - pi is an integer denoting the parent node of the ith edge, ci is the child node, and the letter Ri denotes whether the child is a left child or right child, it's value is 'L' if ci is the left child of pi, else 'R'.",
    "outputFormat": "Output on a single line, N space-separated integers - the preorder traversal of the given tree.",
    "sampleTestCases": [
      {
        "id": "sample_1",
        "input": "1\n5\n1 2 L\n1 3 R\n3 4 L\n3 5 R",
        "output": "1 2 3 4 5",
        "explanation": "Root is 1. Preorder visits root (1), then left subtree (2), then right subtree (3) whose left child is 4 and right child is 5."
      },
      {
        "id": "sample_2",
        "input": "1\n1",
        "output": "1",
        "explanation": "A single node tree; preorder is just that node."
      },
      {
        "id": "sample_3",
        "input": "1\n4\n1 2 L\n2 3 L\n3 4 L",
        "output": "1 2 3 4",
        "explanation": "The tree is a left-skewed chain 1\u21922\u21923\u21924, so preorder follows the chain."
      }
    ]
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "label": "minimum boundary",
        "input": "1\n1",
        "expectedOutput": "1",
        "weight": 2
      },
      {
        "id": "tc_2",
        "label": "maximum boundary",
        "input": "1\n100000\n1 2 L\n1 3 R\n3 4 L\n3 5 R",
        "expectedOutput": "1 2 3 4 5",
        "weight": 2
      },
      {
        "id": "tc_3",
        "label": "uniform/edge case",
        "input": "1\n5\n1 2 L\n1 3 R\n3 4 L\n3 5 R",
        "expectedOutput": "1 2 3 4 5",
        "weight": 2
      },
      {
        "id": "tc_4",
        "label": "tricky case",
        "input": "1\n4\n1 2 L\n2 3 L\n3 4 L",
        "expectedOutput": "1 2 3 4",
        "weight": 2
      },
      {
        "id": "tc_5",
        "label": "stress/general",
        "input": "2\n5\n1 2 L\n1 3 R\n3 4 L\n3 5 R\n4\n1 2 L\n2 3 L\n3 4 L",
        "expectedOutput": "1 2 3 4 5\n1 2 3 4",
        "weight": 2
      }
    ],
    "totalTestCases": 5,
    "totalWeight": 10
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      2,
      2,
      2,
      2,
      2
    ],
    "maxScore": 10
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "To solve this problem, we can use a recursive or iterative approach to perform the preorder traversal of the binary tree. We start by finding the root of the tree, which is the node that is not a child of any other node. Then, we traverse the tree in preorder, visiting the root node, then the left subtree, and finally the right subtree.",
    "timeComplexity": "O(N) per test case, where N is the number of nodes in the binary tree, since we visit each node once during the preorder traversal.",
    "spaceComplexity": "O(N) for the recursive call stack in the worst case, when the tree is skewed to one side.",
    "code": {
      "C": "#include <stdio.h>\n#include <stdlib.h>\n\n#define MAXN 100005\n\nint leftChild[MAXN];\nint rightChild[MAXN];\nint isChild[MAXN];\n\nvoid preorder(int node) {\n    if (node == 0) return;\n    printf(\"%d \", node);\n    preorder(leftChild[node]);\n    preorder(rightChild[node]);\n}\n\nint main() {\n    int T; if (scanf(\"%d\", &T)!=1) return 0;\n    while (T--) {\n        int N; scanf(\"%d\", &N);\n        for (int i = 1; i <= N; ++i) {\n            leftChild[i] = rightChild[i] = isChild[i] = 0;\n        }\n        for (int i = 0; i < N-1; ++i) {\n            int p, c; char dir;\n            scanf(\"%d %d %c\", &p, &c, &dir);\n            if (dir == 'L') leftChild[p] = c; else rightChild[p] = c;\n            isChild[c] = 1;\n        }\n        int root = 1;\n        for (int i = 1; i <= N; ++i) if (!isChild[i]) { root = i; break; }\n        preorder(root);\n        printf(\"\\n\");\n    }\n    return 0;\n}\n",
      "C++": "#include <bits/stdc++.h>\nusing namespace std;\n\nint main() {\n    ios::sync_with_stdio(false);\n    cin.tie(nullptr);\n    int T; if(!(cin>>T)) return 0;\n    while(T--) {\n        int N; cin>>N;\n        vector<int> left(N+1,0), right(N+1,0), isChild(N+1,0);\n        for(int i=0;i<N-1;++i){\n            int p,c; char dir; cin>>p>>c>>dir;\n            if(dir=='L') left[p]=c; else right[p]=c;\n            isChild[c]=1;\n        }\n        int root=1; for(int i=1;i<=N;++i) if(!isChild[i]){root=i;break;}\n        // iterative preorder to avoid recursion depth issues\n        vector<int> st; st.push_back(root);\n        bool first=true;\n        while(!st.empty()){\n            int node=st.back(); st.pop_back();\n            if(first){cout<<node;first=false;} else cout<<' '<<node;\n            if(right[node]) st.push_back(right[node]);\n            if(left[node]) st.push_back(left[node]);\n        }\n        cout<<\"\\n\";\n    }\n    return 0;\n}\n",
      "Java": "import java.io.*;\nimport java.util.*;\n\npublic class Main {\n    public static void main(String[] args) throws Exception {\n        FastScanner fs = new FastScanner(System.in);\n        int T = fs.nextInt();\n        StringBuilder out = new StringBuilder();\n        while (T-- > 0) {\n            int N = fs.nextInt();\n            int[] left = new int[N + 1];\n            int[] right = new int[N + 1];\n            boolean[] isChild = new boolean[N + 1];\n            for (int i = 0; i < N - 1; i++) {\n                int p = fs.nextInt();\n                int c = fs.nextInt();\n                char dir = fs.next().charAt(0);\n                if (dir == 'L') left[p] = c; else right[p] = c;\n                isChild[c] = true;\n            }\n            int root = 1;\n            for (int i = 1; i <= N; i++) if (!isChild[i]) { root = i; break; }\n            Deque<Integer> stack = new ArrayDeque<>();\n            stack.push(root);\n            boolean first = true;\n            while (!stack.isEmpty()) {\n                int node = stack.pop();\n                if (first) { out.append(node); first = false; } else out.append(' ').append(node);\n                if (right[node] != 0) stack.push(right[node]);\n                if (left[node] != 0) stack.push(left[node]);\n            }\n            out.append('\\n');\n        }\n        System.out.print(out.toString());\n    }\n\n    // Fast scanner for Java\n    static class FastScanner {\n        private final BufferedInputStream in;\n        private final byte[] buffer = new byte[1 << 16];\n        private int ptr = 0, len = 0;\n        FastScanner(InputStream is) { in = new BufferedInputStream(is); }\n        private int readByte() throws IOException {\n            if (ptr >= len) {\n                len = in.read(buffer);\n                ptr = 0;\n                if (len <= 0) return -1;\n            }\n            return buffer[ptr++];\n        }\n        String next() throws IOException {\n            StringBuilder sb = new StringBuilder();\n            int c;\n            while ((c = readByte()) != -1 && c <= ' ') {}\n            while (c != -1 && c > ' ') {\n                sb.append((char) c);\n                c = readByte();\n            }\n            return sb.toString();\n        }\n        int nextInt() throws IOException { return Integer.parseInt(next()); }\n    }\n}\n",
      "Python3": "#!/usr/bin/env python3\nimport sys\nsys.setrecursionlimit(1_000_000)\n\ndef solve():\n    data = sys.stdin.read().strip().split()\n    if not data:\n        return\n    it = iter(data)\n    T = int(next(it))\n    out_lines = []\n    for _ in range(T):\n        N = int(next(it))\n        left = [0] * (N + 1)\n        right = [0] * (N + 1)\n        is_child = [False] * (N + 1)\n        for __ in range(N - 1):\n            p = int(next(it))\n            c = int(next(it))\n            dir = next(it)\n            if dir == 'L':\n                left[p] = c\n            else:\n                right[p] = c\n            is_child[c] = True\n        root = 1\n        for i in range(1, N + 1):\n            if not is_child[i]:\n                root = i\n                break\n        res = []\n        def dfs(u):\n            if u == 0:\n                return\n            res.append(str(u))\n            dfs(left[u])\n            dfs(right[u])\n        dfs(root)\n        out_lines.append(' '.join(res))\n    sys.stdout.write('\\n'.join(out_lines))\n\nif __name__ == \"__main__\":\n    solve()\n",
      "JavaScript": "const fs = require('fs');\nconst input = fs.readFileSync(0, 'utf-8').trim().split(/\\s+/);\nlet idx = 0;\nif (input.length === 0) process.exit(0);\nconst T = parseInt(input[idx++], 10);\nlet outputs = [];\nfor (let t = 0; t < T; t++) {\n  const N = parseInt(input[idx++], 10);\n  const left = new Array(N + 1).fill(0);\n  const right = new Array(N + 1).fill(0);\n  const isChild = new Array(N + 1).fill(false);\n  for (let i = 0; i < N - 1; i++) {\n    const p = parseInt(input[idx++], 10);\n    const c = parseInt(input[idx++], 10);\n    const dir = input[idx++];\n    if (dir === 'L') left[p] = c; else right[p] = c;\n    isChild[c] = true;\n  }\n  let root = 1;\n  for (let i = 1; i <= N; i++) if (!isChild[i]) { root = i; break; }\n  const stack = [root];\n  const order = [];\n  while (stack.length) {\n    const node = stack.pop();\n    order.push(node);\n    if (right[node]) stack.push(right[node]);\n    if (left[node]) stack.push(left[node]);\n  }\n  outputs.push(order.join(' '));\n}\nconsole.log(outputs.join('\\n'));\n"
    }
  }
}