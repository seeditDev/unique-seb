{
  "questionId": "Q4562",
  "title": "Right side view of Binary tree",
  "slug": "right-side-view-of-binary-tree",
  "version": 1,
  "createdAt": "2026-07-01",
  "updatedAt": "2026-07-04",
  "metadata": {
    "category": "Trees",
    "difficulty": "Medium",
    "isPremium": true,
    "tags": [
      "BFS",
      "Level Order Traversal"
    ],
    "companyTags": [],
    "topicTags": [
      "Graphs"
    ],
    "QBCategory": "CCD"
  },
  "content": {
    "problemStatement": "Given the rootrootroot of a binary tree, imagine standing on the **right side** of the tree. Return an array of the values of the nodes you can see ordered from top to bottom.\n\n**Notes:**\n\n\nReturn the rightmost node value at each depth level (top → bottom).\nExpected solutions commonly use BFS level-order traversal or DFS with depth tracking.\nFunction Declaration\n\nFunction Name\n\n\n\nrightSideViewrightSideViewrightSideView — This function returns the list of node values visible when viewing a binary tree from the right side.\n\n\n\nParameters\n\n\n\nrootrootroot: A pointer to the root node of the binary tree.\nThe binary tree may contain 000 to 100100100 nodes, and each node contains an integer value or may be null (`-1` in level-order input).\n\n\n\nReturn Value\n\n\n\nReturns an array containing the values of the rightmost nodes at each depth level of the binary tree.\nThe values are ordered from **top to bottom**, representing the nodes visible from the right side.",
    "constraints": [
      "1 <= T <= 1000",
      "1 <= N <= 1000",
      "-100 <= Node.val <= 100"
    ],
    "inputFormat": "First line contains T, the number of test cases. Each test case contains two lines of input: The first line contains a single integer N - size of the array that contains nodes and empty leaf nodes. The second line contains N space-separated integers representing the level-order traversal of the tree, where -1 denotes a null child.",
    "outputFormat": "For each test case, output on a new line the right side view of the binary tree — a sequence of space-separated integers representing the values of the nodes visible from the right side.",
    "sampleTestCases": [
      {
        "id": "sample_1",
        "input": "3\n7\n10 5 15 3 7 -1 20\n7\n1 2 3 4 5 6 7\n5\n1 -1 2 -1 3",
        "output": "10 15 20\n1 3 7\n1 2 3",
        "explanation": "Explanation:\nFor the first test case:\n\n    10\n   /  \\\n  5    15\n / \\     \\\n3   7     20\n\nFrom the right side you see 10, then 15, then 20."
      },
      {
        "id": "sample_2",
        "input": "1\n3\n1 2 3",
        "output": "1 3",
        "explanation": "Explanation:\nFor the test case:\n\n    1\n   / \\\n  2   3\n\nFrom the right side you see 1, then 3."
      },
      {
        "id": "sample_3",
        "input": "1\n5\n1 -1 2 -1 3",
        "output": "1 2 3",
        "explanation": "Explanation:\nFor the test case:\n\n    1\n     \\\n      2\n       \\\n        3\n\nFrom the right side you see 1, then 2, then 3."
      }
    ]
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "label": "minimum boundary",
        "input": "1\n1\n10",
        "expectedOutput": "10",
        "weight": 2
      },
      {
        "id": "tc_2",
        "label": "maximum boundary",
        "input": "1\n15\n10 5 15 3 7 -1 20 1 2 3 4 5 6 7",
        "expectedOutput": "10 15 20 7",
        "weight": 2
      },
      {
        "id": "tc_3",
        "label": "uniform/edge case",
        "input": "1\n7\n1 1 1 1 1 1 1",
        "expectedOutput": "1 1 1",
        "weight": 2
      },
      {
        "id": "tc_4",
        "label": "tricky case",
        "input": "1\n7\n10 5 15 3 7 -1 20",
        "expectedOutput": "10 15 20",
        "weight": 2
      },
      {
        "id": "tc_5",
        "label": "stress/general",
        "input": "2\n7\n10 5 15 3 7 -1 20\n7\n1 2 3 4 5 6 7",
        "expectedOutput": "10 15 20\n1 3 7",
        "weight": 2
      }
    ],
    "totalTestCases": 5,
    "totalWeight": 10
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      2,
      2,
      2,
      2,
      2
    ],
    "maxScore": 10
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "To solve this problem, we use a level order traversal (BFS) approach. We start by creating a queue and adding the root node to it. Then, we enter a loop where we keep removing nodes from the queue and adding their children until the queue is empty. We keep track of the number of nodes at each level and add the last node at each level to the result.",
    "timeComplexity": "O(T * N) per test case, O(T * N) total. The precomputation is done once for each test case.",
    "spaceComplexity": "O(N) — the dominant structure is the queue used for BFS.",
    "code": {
      "C": "#include <stdio.h>\n#include <stdlib.h>\n#include <queue>\n\n// Define the structure for a tree node\ntypedef struct Node {\n    int val;\n    struct Node* left;\n    struct Node* right;\n} Node;\n\n// Function to create a new node\nNode* createNode(int val) {\n    Node* newNode = (Node*) malloc(sizeof(Node));\n    if (!newNode) {\n        printf(\"Memory error\\n\");\n        return NULL;\n    }\n    newNode->val = val;\n    newNode->left = newNode->right = NULL;\n    return newNode;\n}\n\n// Function to get the right side view of the binary tree\nvoid rightSideView(Node* root, int* result, int* index, int level) {\n    if (root == NULL) return;\n    if (*index == level) {\n        result[level] = root->val;\n        (*index)++;\n    }\n    rightSideView(root->right, result, index, level + 1);\n    rightSideView(root->left, result, index, level + 1);\n}\n\nint main() {\n    int T;\n    scanf(\"%d\", &T);\n    while (T--) {\n        int N;\n        scanf(\"%d\", &N);\n        int* nodes = (int*) malloc(N * sizeof(int));\n        for (int i = 0; i < N; i++) {\n            scanf(\"%d\", &nodes[i]);\n        }\n        Node* root = NULL;\n        // Create the binary tree from the level order traversal\n        if (nodes[0] != -1) {\n            root = createNode(nodes[0]);\n        }\n        int queueSize = 1;\n        Node** queue = (Node**) malloc(queueSize * sizeof(Node*));\n        queue[0] = root;\n        int nodeIndex = 1;\n        for (int i = 1; i < N; i++) {\n            if (nodes[i] != -1) {\n                Node* newNode = createNode(nodes[i]);\n                if (i % 2 == 1) {\n                    queue[(i - 1) / 2]->left = newNode;\n                } else {\n                    queue[(i - 2) / 2]->right = newNode;\n                }\n                queue = (Node**) realloc(queue, (queueSize + 1) * sizeof(Node*));\n                queue[queueSize] = newNode;\n                queueSize++;\n            }\n        }\n        int* result = (int*) malloc(queueSize * sizeof(int));\n        int index = 0;\n        rightSideView(root, result, &index, 0);\n        for (int i = 0; i < index; i++) {\n            printf(\"%d \", result[i]);\n        }\n        printf(\"\\n\");\n        free(nodes);\n        free(queue);\n        free(result);\n    }\n    return 0;\n}",
      "C++": "#include <iostream>\n#include <queue>\nusing namespace std;\n\n// Define the structure for a tree node\nstruct Node {\n    int val;\n    Node* left;\n    Node* right;\n};\n\n// Function to create a new node\nNode* createNode(int val) {\n    Node* newNode = new Node();\n    newNode->val = val;\n    newNode->left = newNode->right = NULL;\n    return newNode;\n}\n\n// Function to get the right side view of the binary tree\nvoid rightSideView(Node* root, vector<int>& result) {\n    if (root == NULL) return;\n    queue<Node*> q;\n    q.push(root);\n    while (!q.empty()) {\n        int size = q.size();\n        for (int i = 0; i < size; i++) {\n            Node* node = q.front();\n            q.pop();\n            if (i == size - 1) result.push_back(node->val);\n            if (node->left) q.push(node->left);\n            if (node->right) q.push(node->right);\n        }\n    }\n}\n\nint main() {\n    ios::sync_with_stdio(false);\n    cin.tie(NULL);\n    int T;\n    cin >> T;\n    while (T--) {\n        int N;\n        cin >> N;\n        vector<int> nodes(N);\n        for (int i = 0; i < N; i++) {\n            cin >> nodes[i];\n        }\n        Node* root = NULL;\n        // Create the binary tree from the level order traversal\n        if (nodes[0] != -1) {\n            root = createNode(nodes[0]);\n        }\n        queue<Node*> queue;\n        queue.push(root);\n        int nodeIndex = 1;\n        while (!queue.empty() && nodeIndex < N) {\n            Node* node = queue.front();\n            queue.pop();\n            if (nodes[nodeIndex] != -1) {\n                node->left = createNode(nodes[nodeIndex]);\n                queue.push(node->left);\n            }\n            nodeIndex++;\n            if (nodeIndex >= N) break;\n            if (nodes[nodeIndex] != -1) {\n                node->right = createNode(nodes[nodeIndex]);\n                queue.push(node->right);\n            }\n            nodeIndex++;\n        }\n        vector<int> result;\n        rightSideView(root, result);\n        for (int i = 0; i < result.size(); i++) {\n            cout << result[i] << \" \";\n        }\n        cout << endl;\n    }\n    return 0;\n}",
      "Java": "import java.util.Queue;\nimport java.util.LinkedList;\nimport java.util.Scanner;\n\npublic class Main {\n    // Define the structure for a tree node\n    static class Node {\n        int val;\n        Node left;\n        Node right;\n        Node(int val) {\n            this.val = val;\n            left = null;\n            right = null;\n        }\n    }\n\n    // Function to get the right side view of the binary tree\n    public static void rightSideView(Node root, int[] result, int[] index, int level) {\n        if (root == null) return;\n        if (index[0] == level) {\n            result[level] = root.val;\n            index[0]++;\n        }\n        rightSideView(root.right, result, index, level + 1);\n        rightSideView(root.left, result, index, level + 1);\n    }\n\n    public static void main(String[] args) {\n        Scanner scanner = new Scanner(System.in);\n        int T = scanner.nextInt();\n        while (T-- > 0) {\n            int N = scanner.nextInt();\n            int[] nodes = new int[N];\n            for (int i = 0; i < N; i++) {\n                nodes[i] = scanner.nextInt();\n            }\n            Node root = null;\n            // Create the binary tree from the level order traversal\n            if (nodes[0] != -1) {\n                root = new Node(nodes[0]);\n            }\n            Queue<Node> queue = new LinkedList<>();\n            queue.add(root);\n            int nodeIndex = 1;\n            while (!queue.isEmpty() && nodeIndex < N) {\n                Node node = queue.poll();\n                if (nodes[nodeIndex] != -1) {\n                    node.left = new Node(nodes[nodeIndex]);\n                    queue.add(node.left);\n                }\n                nodeIndex++;\n                if (nodeIndex >= N) break;\n                if (nodes[nodeIndex] != -1) {\n                    node.right = new Node(nodes[nodeIndex]);\n                    queue.add(node.right);\n                }\n                nodeIndex++;\n            }\n            int[] result = new int[N];\n            int[] index = new int[] {0};\n            rightSideView(root, result, index, 0);\n            for (int i = 0; i < index[0]; i++) {\n                System.out.print(result[i] + \" \");\n            }\n            System.out.println();\n        }\n    }\n}",
      "Python3": "# Define the structure for a tree node\nclass Node:\n    def __init__(self, val):\n        self.val = val\n        self.left = None\n        self.right = None\n\n# Function to get the right side view of the binary tree\ndef rightSideView(root):\n    if root is None:\n        return []\n    result = []\n    queue = [root]\n    while queue:\n        level_size = len(queue)\n        for i in range(level_size):\n            node = queue.pop(0)\n            if i == level_size - 1:\n                result.append(node.val)\n            if node.left:\n                queue.append(node.left)\n            if node.right:\n                queue.append(node.right)\n    return result\n\n# Read input\ndef read_input():\n    import sys\n    T = int(sys.stdin.readline())\n    for _ in range(T):\n        N = int(sys.stdin.readline())\n        nodes = list(map(int, sys.stdin.readline().split()))\n        root = None\n        # Create the binary tree from the level order traversal\n        if nodes[0] != -1:\n            root = Node(nodes[0])\n        queue = [root]\n        node_index = 1\n        while queue and node_index < N:\n            node = queue.pop(0)\n            if nodes[node_index] != -1:\n                node.left = Node(nodes[node_index])\n                queue.append(node.left)\n            node_index += 1\n            if node_index >= N:\n                break\n            if nodes[node_index] != -1:\n                node.right = Node(nodes[node_index])\n                queue.append(node.right)\n            node_index += 1\n        result = rightSideView(root)\n        print(' '.join(map(str, result)))\n\nread_input()",
      "JavaScript": "const lines = require('fs').readFileSync(0,'utf-8').trim().split('\\n');\nlet i=0;\n\n// Define the structure for a tree node\nfunction Node(val) {\n    this.val = val;\n    this.left = null;\n    this.right = null;\n}\n\n// Function to get the right side view of the binary tree\nfunction rightSideView(root) {\n    if (root === null) return [];\n    const result = [];\n    const queue = [root];\n    while (queue.length > 0) {\n        const levelSize = queue.length;\n        for (let i = 0; i < levelSize; i++) {\n            const node = queue.shift();\n            if (i === levelSize - 1) result.push(node.val);\n            if (node.left) queue.push(node.left);\n            if (node.right) queue.push(node.right);\n        }\n    }\n    return result;\n}\n\n// Read input\nfunction readInput() {\n    const T = parseInt(lines[i++]);\n    for (let i = 0; i < T; i++) {\n        const N = parseInt(lines[i++]);\n        const nodes = lines[i++].split(' ').map(Number);\n        let root = null;\n        // Create the binary tree from the level order traversal\n        if (nodes[0] !== -1) {\n            root = new Node(nodes[0]);\n        }\n        const queue = [root];\n        let nodeIndex = 1;\n        while (queue.length > 0 && nodeIndex < N) {\n            const node = queue.shift();\n            if (nodes[nodeIndex] !== -1) {\n                node.left = new Node(nodes[nodeIndex]);\n                queue.push(node.left);\n            }\n            nodeIndex++;\n            if (nodeIndex >= N) break;\n            if (nodes[nodeIndex] !== -1) {\n                node.right = new Node(nodes[nodeIndex]);\n                queue.push(node.right);\n            }\n            nodeIndex++;\n        }\n        const result = rightSideView(root);\n        console.log(result.join(' '));\n    }\n}\n\nreadInput();"
    }
  },
  "QBCategory": "CCD",
  "boilerPlates": {
    "C": "#include <stdio.h>\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "C++": "#include <iostream>\nusing namespace std;\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "Java": "import java.util.*;\nimport java.io.*;\n\npublic class Main {\n    public static void main(String[] args) throws IOException {\n        // your code goes here\n    }\n}",
    "Python3": "# your code goes here\n",
    "JavaScript": "// your code goes here\n"
  },
  "_internal": {
    "methodName": "",
    "params": []
  }
}