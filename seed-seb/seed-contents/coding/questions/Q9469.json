{
  "questionId": "Q9469",
  "title": "Longest Subarray",
  "slug": "longest-subarray",
  "version": 1,
  "createdAt": "2026-07-02",
  "updatedAt": "2026-07-04",
  "metadata": {
    "category": "Arrays | Bit Manipulation | Sliding Window",
    "difficulty": "Hard",
    "isPremium": true,
    "tags": [
      "Bitwise OR",
      "Essential positions",
      "Two‑pointer",
      "Prefix‑suffix",
      "Greedy"
    ],
    "companyTags": [],
    "topicTags": [
      "Two Pointers & Sliding Window"
    ],
    "QBCategory": "CCD"
  },
  "content": {
    "problemStatement": "You are given an array AAA of length NNN.\n\nFind the **maximum length** of any subarray of AAA, such that, the bitwise OR of all the elements of the subarray is equal to the bitwise OR of the remaining elements of the array.\n\nReport −1-1−1 if no such subarray exists.\n\nNote:\n\n\nA subarray is formed by deleting some (possibly zero) elements from the beginning of the array and some (possibly zero) elements from the end of the array.\nThe bitwise OR of an empty array is considered to be 000.\n\nInput Format\n\nThe first line of input contains a single integer TTT, denoting the number of test cases.\nEach test case consists of two lines of input.\n\nThe first line of each test case contains an integer NNN — the length of the array AAA.\nThe second line of each test case contains NNN space-separated integers A1,A2,…,ANA_1,A_2,\\ldots,A_NA1​,A2​,…,AN​ representing the array AAA.\n\n\n\n\nOutput Format\nFor each test case, output on a new line, the **length** of **longest** subarray such that the bitwise OR of all elements of the subarray is equal to the bitwise OR of the remaining elements of the array, or output −1-1−1 if no such subarray exists.",
    "constraints": [
      "1 <= T <= 10^5",
      "1 <= N <= 5·10^5 (sum of N over all test cases ≤ 5·10^5)",
      "1 <= Ai < 2^30"
    ],
    "inputFormat": "The first line contains an integer T, the number of test cases. Each test case consists of two lines: the first line contains an integer N, the length of the array. The second line contains N space‑separated integers A1 … AN.",
    "outputFormat": "For each test case output a single line containing the maximum possible length of a subarray whose bitwise OR equals the bitwise OR of the remaining elements, or -1 if no such subarray exists.",
    "sampleTestCases": [
      {
        "id": "sample_1",
        "input": "3\n3\n1 2 4\n3\n1 3 2\n4\n2 3 4 7",
        "output": "-1\n1\n3",
        "explanation": "Explanation:\nTest case 111: For no subarray of AAA, the bitwise OR of the elements of the subarray equals the bitwise OR of the remaining elements.\nTest case 222: Consider the subarray [3][3][3] having length 111. The bitwise OR of the subarray is 333 and the bitwise OR of the remaining elements of the array is 1∣2=31 | 2 = 31∣2=3. Thus, the bitwise OR of all elements of the subarray is equal to the bitwise OR of the remaining elements of the array.\nTest case 333: Consider the subarray [2,3,4][2, 3, 4][2,3,4] having length 333. The bitwise OR of the subarray is 2∣3∣4=72|3|4 = 72∣3∣4=7 and the bitwise OR of the remaining elements of the array is 777. Thus, the bitwise OR of all elements of the subarray is equal to the bitwise OR of the remaining elements of the array."
      }
    ]
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "label": "Case 1",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_2",
        "label": "Case 2",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_3",
        "label": "Case 3",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_4",
        "label": "Case 4",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_5",
        "label": "Case 5",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      }
    ],
    "totalTestCases": 5,
    "totalWeight": 10
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      2,
      2,
      2,
      2,
      2
    ],
    "maxScore": 10
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "For each test case compute the total OR of the whole array. A subarray can be removed without changing this total OR iff every bit that is set in the total OR appears at least once outside the subarray. Therefore any position that is the *only* occurrence of some bit is \"essential\" – the subarray must not contain any essential position. The problem reduces to finding the longest contiguous segment that contains no essential indices. If there are no essential indices, any subarray of length N‑1 works (the remaining part must be non‑empty). If essential indices exist, the answer is the maximum distance between two consecutive essential positions minus one, also considering the prefix before the first essential and the suffix after the last essential. If the maximum length is zero, output -1.",
    "timeComplexity": "O(N * B) per test case, where B = 30 (number of bits). Effectively O(N).",
    "spaceComplexity": "O(N) for storing the array and a boolean flag per position.",
    "code": {
      "C": "#include <stdio.h>\n#include <stdlib.h>\nint main(){\n    int T; if(scanf(\"%d\",&T)!=1) return 0;\n    while(T--){\n        int N; scanf(\"%d\",&N);\n        int *a = (int*)malloc(N*sizeof(int));\n        int cnt[30]={0};\n        for(int i=0;i<N;i++){\n            scanf(\"%d\",&a[i]);\n            for(int b=0;b<30;b++) if(a[i]>>b & 1) cnt[b]++;\n        }\n        int essentialPos[N];\n        for(int i=0;i<N;i++) essentialPos[i]=0;\n        for(int b=0;b<30;b++) if(cnt[b]==1){\n            for(int i=0;i<N;i++) if(a[i]>>b & 1){ essentialPos[i]=1; break; }\n        }\n        int maxLen=0, last=-1;\n        for(int i=0;i<N;i++){\n            if(essentialPos[i]){\n                int len=i-last-1; if(len>maxLen) maxLen=len; last=i;\n            }\n        }\n        int len=N-last-1; if(len>maxLen) maxLen=len;\n        if(maxLen==0) printf(\"-1\\n\");\n        else if(maxLen==N) printf(\"%d\\n\",N-1); // no essential, whole array not allowed\n        else printf(\"%d\\n\",maxLen);\n        free(a);\n    }\n    return 0;\n}",
      "C++": "#include <bits/stdc++.h>\nusing namespace std;\nint main(){\n    ios::sync_with_stdio(false);\n    cin.tie(nullptr);\n    int T; if(!(cin>>T)) return 0;\n    while(T--){\n        int N; cin>>N;\n        vector<int> a(N);\n        int cnt[30] = {};\n        for(int i=0;i<N;i++){\n            cin>>a[i];\n            for(int b=0;b<30;b++) if(a[i]>>b & 1) cnt[b]++;\n        }\n        vector<char> essential(N,0);\n        for(int b=0;b<30;b++) if(cnt[b]==1){\n            for(int i=0;i<N;i++) if(a[i]>>b & 1){ essential[i]=1; break; }\n        }\n        int maxLen=0, last=-1;\n        for(int i=0;i<N;i++){\n            if(essential[i]){\n                maxLen = max(maxLen, i-last-1);\n                last=i;\n            }\n        }\n        maxLen = max(maxLen, N-last-1);\n        if(maxLen==0) cout<<-1<<\"\\n\";\n        else if(maxLen==N) cout<<N-1<<\"\\n\"; // no essential positions\n        else cout<<maxLen<<\"\\n\";\n    }\n    return 0;\n}",
      "Java": "import java.io.*;\nimport java.util.*;\npublic class Main {\n    public static void main(String[] args) throws Exception {\n        FastScanner fs = new FastScanner(System.in);\n        int T = fs.nextInt();\n        StringBuilder sb = new StringBuilder();\n        while (T-- > 0) {\n            int N = fs.nextInt();\n            int[] a = new int[N];\n            int[] cnt = new int[30];\n            for (int i = 0; i < N; i++) {\n                a[i] = fs.nextInt();\n                for (int b = 0; b < 30; b++) if ((a[i] >> b & 1) == 1) cnt[b]++;\n            }\n            boolean[] essential = new boolean[N];\n            for (int b = 0; b < 30; b++) if (cnt[b] == 1) {\n                for (int i = 0; i < N; i++) if ((a[i] >> b & 1) == 1) { essential[i] = true; break; }\n            }\n            int maxLen = 0, last = -1;\n            for (int i = 0; i < N; i++) {\n                if (essential[i]) {\n                    maxLen = Math.max(maxLen, i - last - 1);\n                    last = i;\n                }\n            }\n            maxLen = Math.max(maxLen, N - last - 1);\n            if (maxLen == 0) sb.append(-1).append('\\n');\n            else if (maxLen == N) sb.append(N - 1).append('\\n');\n            else sb.append(maxLen).append('\\n');\n        }\n        System.out.print(sb.toString());\n    }\n    // Fast scanner for large input\n    static class FastScanner {\n        private final InputStream in;\n        private final byte[] buffer = new byte[1 << 16];\n        private int ptr = 0, len = 0;\n        FastScanner(InputStream in) { this.in = in; }\n        private int readByte() throws IOException {\n            if (ptr >= len) {\n                len = in.read(buffer);\n                ptr = 0;\n                if (len <= 0) return -1;\n            }\n            return buffer[ptr++];\n        }\n        int nextInt() throws IOException {\n            int c, sign = 1, val = 0;\n            do { c = readByte(); } while (c <= ' ');\n            if (c == '-') { sign = -1; c = readByte(); }\n            while (c > ' ') { val = val * 10 + (c - '0'); c = readByte(); }\n            return val * sign;\n        }\n    }\n}",
      "Python3": "#!/usr/bin/env python3\nimport sys\n\ndef solve():\n    data = sys.stdin.read().strip().split()\n    if not data:\n        return\n    it = iter(data)\n    T = int(next(it))\n    out = []\n    for _ in range(T):\n        N = int(next(it))\n        a = [int(next(it)) for _ in range(N)]\n        cnt = [0]*30\n        for val in a:\n            for b in range(30):\n                if val>>b & 1:\n                    cnt[b] += 1\n        essential = [False]*N\n        for b in range(30):\n            if cnt[b]==1:\n                for i, val in enumerate(a):\n                    if val>>b & 1:\n                        essential[i] = True\n                        break\n        max_len = 0\n        last = -1\n        for i, e in enumerate(essential):\n            if e:\n                max_len = max(max_len, i-last-1)\n                last = i\n        max_len = max(max_len, N-last-1)\n        if max_len == 0:\n            out.append('-1')\n        elif max_len == N:\n            out.append(str(N-1))\n        else:\n            out.append(str(max_len))\n    sys.stdout.write('\\n'.join(out))\n\nif __name__ == \"__main__\":\n    solve()\n",
      "JavaScript": "const fs = require('fs');\nconst input = fs.readFileSync(0, 'utf-8').trim().split(/\\s+/);\nlet idx = 0;\nconst T = Number(input[idx++]);\nlet results = [];\nfor (let t = 0; t < T; t++) {\n  const N = Number(input[idx++]);\n  const a = new Array(N);\n  const cnt = new Array(30).fill(0);\n  for (let i = 0; i < N; i++) {\n    a[i] = Number(input[idx++]);\n    for (let b = 0; b < 30; b++) if ((a[i] >> b) & 1) cnt[b]++;\n  }\n  const essential = new Array(N).fill(false);\n  for (let b = 0; b < 30; b++) if (cnt[b] === 1) {\n    for (let i = 0; i < N; i++) if ((a[i] >> b) & 1) { essential[i] = true; break; }\n  }\n  let maxLen = 0, last = -1;\n  for (let i = 0; i < N; i++) {\n    if (essential[i]) {\n      maxLen = Math.max(maxLen, i - last - 1);\n      last = i;\n    }\n  }\n  maxLen = Math.max(maxLen, N - last - 1);\n  if (maxLen === 0) results.push('-1');\n  else if (maxLen === N) results.push(String(N - 1));\n  else results.push(String(maxLen));\n}\nconsole.log(results.join('\\n'));\n"
    }
  },
  "QBCategory": "CCD",
  "boilerPlates": {
    "C": "#include <stdio.h>\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "C++": "#include <iostream>\nusing namespace std;\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "Java": "import java.util.*;\nimport java.io.*;\n\npublic class Main {\n    public static void main(String[] args) throws IOException {\n        // your code goes here\n    }\n}",
    "Python3": "# your code goes here\n",
    "JavaScript": "// your code goes here\n"
  },
  "_internal": {
    "methodName": "",
    "params": []
  }
}