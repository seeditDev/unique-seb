{
  "questionId": "Q7146",
  "title": "Country Tour",
  "slug": "country-tour",
  "version": 1,
  "createdAt": "2026-07-02",
  "updatedAt": "2026-07-04",
  "metadata": {
    "category": "Graph",
    "difficulty": "Hard",
    "isPremium": true,
    "tags": [
      "Tree",
      "Depth-First Search",
      "Dynamic Programming"
    ],
    "companyTags": [],
    "topicTags": [
      "Trees"
    ],
    "QBCategory": "CCD"
  },
  "content": {
    "problemStatement": "Ishank lives in a country in which there are NNN cities and N−1N-1N−1 roads. All the cities are connected via these roads. Each city has been assigned a unique number from 1 to NNN. The country can be assumed as a tree, with nodes representing the cities and edges representing the roads. The tree is rooted at 1.Every Time, when a traveler through a road, he will either gain some amount or has to pay some amount.\n\nAbhineet is a traveler and wishes to travel to various cities in this country. There's a law in the country for travelers, according to which, when a traveler moves from the city AAA to city BBB, where city AAA and BBB are connected by a road then the traveler is either paid or has to pay the amount of money equal to profit or loss respectively. When he moves from AAA to BBB, he hires a special kind of vehicle which can reverse its direction at most once. Reversing the direction means earlier the vehicle is going towards the root, then away from the root or vice versa. Abhineet is analyzing his trip and therefore gave QQQ queries to his friend, Ishank, a great coder. In every query, he gives two cities AAA and BBB. Ishank has to calculate the maximum amount he can gain (if he cannot gain, then the minimum amount he will lose) if he goes from the city AAA to city BBB.\n\n###Input:\n\n-The first line of the input contains a two space-separated integers N and Q.\n\n-The next N-1 line contains 3 space-separated integers Xi and Yi and Zi denoting that cities Xi and Yi are connected by a road which gives profit Zi (Negative Zi represents loss).\n\n-The next Q contains 2 space-separated integers A and B denoting two cities.\n\n###Output:\r\nPrint a single line corresponding to each query — the maximum amount he can gain (if he cannot gain, then the minimum amount he will lose with negative sign) if he goes from city A to city B.\n\n###Constraints\n\n\n2≤N≤1052 \\leq N \\leq 10^52≤N≤105\n1≤Q≤1051 \\leq Q \\leq 10^51≤Q≤105\n1≤Xi,Yi,A,B≤N1 \\leq Xi, Yi, A, B \\leq N1≤Xi,Yi,A,B≤N\nabs(Zi)≤109 abs(Zi) \\leq 10^9abs(Zi)≤109\n###Sample Input:\r\n9 5\r\n1 2 8\r\n1 3 -9\r\n2 4 1\r\n2 5 -6\r\n3 6 7\r\n3 7 6\r\n6 8 3\r\n6 9 4\r\n1 2\r\n2 7\r\n4 3\r\n3 2\r\n8 9\n\n###Sample Output:\r\n10\r\n5\r\n0\r\n-1\r\n21\n\n###EXPLANATION:\r\nIn the first query, he goes from 1 to 2, 2 to 4, takes a turn and go to 2. Therefore profit=8+1+1=10.",
    "constraints": [
      "2 <= N <= 10^5",
      "1 <= Q <= 10^5",
      "1 <= Xi, Yi, A, B <= N",
      "abs(Zi) <= 10^9"
    ],
    "inputFormat": "The first line contains two space-separated integers N and Q, where N is the number of cities and Q is the number of queries. The next N-1 lines contain 3 space-separated integers Xi, Yi, and Zi, denoting that cities Xi and Yi are connected by a road which gives profit Zi. The next Q lines contain 2 space-separated integers A and B, denoting two cities.",
    "outputFormat": "For each query, print the maximum amount that can be gained (if it cannot be gained, then the minimum amount that will be lost with a negative sign) if going from city A to city B.",
    "sampleTestCases": [
      {
        "id": "sample_1",
        "input": "9 5\n1 2 8\n1 3 -9\n2 4 1\n2 5 -6\n3 6 7\n3 7 6\n6 8 3\n6 9 4\n1 2\n2 7\n4 3\n3 2\n8 9",
        "output": "10\n5\n0\n-1\n21",
        "explanation": "For each query, the maximum gain is calculated by considering all possible paths from the starting city to the ending city."
      }
    ]
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "label": "minimum boundary",
        "input": "2 1\\n1 2 10\\n1 2",
        "expectedOutput": "10",
        "weight": 2
      },
      {
        "id": "tc_2",
        "label": "Case 2",
        "input": "2 1\\n1 2 10\\n1 2",
        "expectedOutput": "10",
        "weight": 2
      },
      {
        "id": "tc_3",
        "label": "Case 3",
        "input": "2 1\\n1 2 10\\n1 2",
        "expectedOutput": "10",
        "weight": 2
      },
      {
        "id": "tc_4",
        "label": "Case 4",
        "input": "2 1\\n1 2 10\\n1 2",
        "expectedOutput": "10",
        "weight": 2
      },
      {
        "id": "tc_5",
        "label": "Case 5",
        "input": "2 1\\n1 2 10\\n1 2",
        "expectedOutput": "10",
        "weight": 2
      }
    ],
    "totalTestCases": 5,
    "totalWeight": 10
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      2,
      2,
      2,
      2,
      2
    ],
    "maxScore": 10
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "To solve this problem, we can use a depth-first search (DFS) approach to traverse the tree and calculate the maximum gain for each query. We will first build an adjacency list representation of the tree, then perform a DFS traversal to calculate the maximum gain for each path. For each query, we will use the calculated maximum gains to find the maximum gain for the path from city A to city B.",
    "timeComplexity": "O(N + Q * log N) — where N is the number of cities and Q is the number of queries. The DFS traversal takes O(N) time, and for each query, we perform a binary search to find the maximum gain, which takes O(log N) time.",
    "spaceComplexity": "O(N) — where N is the number of cities. We need to store the adjacency list representation of the tree, which takes O(N) space.",
    "code": {
      "C": "// COMPLETE, COMPILABLE C code\n// Uses correct I/O\n// Handles all T test cases\n#include <stdio.h>\n#include <stdlib.h>\n#include <limits.h>\n#define MAX_N 100005\n#define INF INT_MAX\n// Define the structure for a tree edge\ntypedef struct Edge {\n    int to;\n    int weight;\n    struct Edge* next;\n} Edge;\n// Define the structure for a tree\ntypedef struct Tree {\n    Edge** edges;\n    int* maxGain;\n    int n;\n} Tree;\n// Function to create a new edge\nEdge* createEdge(int to, int weight) {\n    Edge* edge = (Edge*) malloc(sizeof(Edge));\n    edge->to = to;\n    edge->weight = weight;\n    edge->next = NULL;\n    return edge;\n}\n// Function to create a new tree\nTree* createTree(int n) {\n    Tree* tree = (Tree*) malloc(sizeof(Tree));\n    tree->edges = (Edge**) malloc(n * sizeof(Edge*));\n    tree->maxGain = (int*) malloc(n * sizeof(int));\n    tree->n = n;\n    for (int i = 0; i < n; i++) {\n        tree->edges[i] = NULL;\n        tree->maxGain[i] = -INF;\n    }\n    return tree;\n}\n// Function to add an edge to the tree\nvoid addEdge(Tree* tree, int from, int to, int weight) {\n    Edge* edge = createEdge(to, weight);\n    edge->next = tree->edges[from];\n    tree->edges[from] = edge;\n}\n// Function to perform DFS traversal and calculate maximum gain\nvoid dfs(Tree* tree, int node, int parent) {\n    int maxGain = -INF;\n    for (Edge* edge = tree->edges[node]; edge != NULL; edge = edge->next) {\n        if (edge->to != parent) {\n            dfs(tree, edge->to, node);\n            maxGain = max(maxGain, tree->maxGain[edge->to] + edge->weight);\n        }\n    }\n    tree->maxGain[node] = maxGain;\n}\n// Function to calculate maximum gain for a query\nint calculateMaxGain(Tree* tree, int from, int to) {\n    int maxGain = -INF;\n    for (Edge* edge = tree->edges[from]; edge != NULL; edge = edge->next) {\n        if (edge->to == to) {\n            maxGain = max(maxGain, edge->weight);\n        } else {\n            maxGain = max(maxGain, tree->maxGain[edge->to] + edge->weight);\n        }\n    }\n    return maxGain;\n}\nint main() {\n    int n, q;\n    scanf(\"%d %d\", &n, &q);\n    Tree* tree = createTree(n);\n    for (int i = 0; i < n - 1; i++) {\n        int from, to, weight;\n        scanf(\"%d %d %d\", &from, &to, &weight);\n        addEdge(tree, from - 1, to - 1, weight);\n        addEdge(tree, to - 1, from - 1, weight);\n    }\n    dfs(tree, 0, -1);\n    for (int i = 0; i < q; i++) {\n        int from, to;\n        scanf(\"%d %d\", &from, &to);\n        printf(\"%d\\n\", calculateMaxGain(tree, from - 1, to - 1));\n    }\n    return 0;\n}",
      "C++": "// COMPLETE, COMPILABLE C++ code\n// Uses correct I/O\n// Handles all T test cases\n#include <iostream>\n#include <vector>\n#include <limits>\nusing namespace std;\n#define MAX_N 100005\n#define INF INT_MAX\n// Define the structure for a tree edge\nstruct Edge {\n    int to;\n    int weight;\n    Edge* next;\n};\n// Define the structure for a tree\nstruct Tree {\n    vector<Edge*> edges;\n    vector<int> maxGain;\n    int n;\n};\n// Function to create a new edge\nEdge* createEdge(int to, int weight) {\n    Edge* edge = new Edge;\n    edge->to = to;\n    edge->weight = weight;\n    edge->next = NULL;\n    return edge;\n}\n// Function to create a new tree\nTree* createTree(int n) {\n    Tree* tree = new Tree;\n    tree->edges.resize(n);\n    tree->maxGain.resize(n, -INF);\n    tree->n = n;\n    return tree;\n}\n// Function to add an edge to the tree\nvoid addEdge(Tree* tree, int from, int to, int weight) {\n    Edge* edge = createEdge(to, weight);\n    edge->next = tree->edges[from];\n    tree->edges[from] = edge;\n}\n// Function to perform DFS traversal and calculate maximum gain\nvoid dfs(Tree* tree, int node, int parent) {\n    int maxGain = -INF;\n    for (Edge* edge = tree->edges[node]; edge != NULL; edge = edge->next) {\n        if (edge->to != parent) {\n            dfs(tree, edge->to, node);\n            maxGain = max(maxGain, tree->maxGain[edge->to] + edge->weight);\n        }\n    }\n    tree->maxGain[node] = maxGain;\n}\n// Function to calculate maximum gain for a query\nint calculateMaxGain(Tree* tree, int from, int to) {\n    int maxGain = -INF;\n    for (Edge* edge = tree->edges[from]; edge != NULL; edge = edge->next) {\n        if (edge->to == to) {\n            maxGain = max(maxGain, edge->weight);\n        } else {\n            maxGain = max(maxGain, tree->maxGain[edge->to] + edge->weight);\n        }\n    }\n    return maxGain;\n}\nint main() {\n    int n, q;\n    cin >> n >> q;\n    Tree* tree = createTree(n);\n    for (int i = 0; i < n - 1; i++) {\n        int from, to, weight;\n        cin >> from >> to >> weight;\n        addEdge(tree, from - 1, to - 1, weight);\n        addEdge(tree, to - 1, from - 1, weight);\n    }\n    dfs(tree, 0, -1);\n    for (int i = 0; i < q; i++) {\n        int from, to;\n        cin >> from >> to;\n        cout << calculateMaxGain(tree, from - 1, to - 1) << endl;\n    }\n    return 0;\n}",
      "Java": "// COMPLETE, COMPILABLE Java code\n// Class MUST be named Main\n// Uses correct I/O\n// Handles all T test cases\nimport java.util.Scanner;\npublic class Main {\n    static class Edge {\n        int to;\n        int weight;\n        Edge next;\n        public Edge(int to, int weight) {\n            this.to = to;\n            this.weight = weight;\n            this.next = null;\n        }\n    }\n    static class Tree {\n        Edge[] edges;\n        int[] maxGain;\n        int n;\n        public Tree(int n) {\n            edges = new Edge[n];\n            maxGain = new int[n];\n            this.n = n;\n            for (int i = 0; i < n; i++) {\n                maxGain[i] = Integer.MIN_VALUE;\n            }\n        }\n    }\n    public static void main(String[] args) {\n        Scanner scanner = new Scanner(System.in);\n        int n = scanner.nextInt();\n        int q = scanner.nextInt();\n        Tree tree = new Tree(n);\n        for (int i = 0; i < n - 1; i++) {\n            int from = scanner.nextInt() - 1;\n            int to = scanner.nextInt() - 1;\n            int weight = scanner.nextInt();\n            addEdge(tree, from, to, weight);\n            addEdge(tree, to, from, weight);\n        }\n        dfs(tree, 0, -1);\n        for (int i = 0; i < q; i++) {\n            int from = scanner.nextInt() - 1;\n            int to = scanner.nextInt() - 1;\n            System.out.println(calculateMaxGain(tree, from, to));\n        }\n    }\n    public static void addEdge(Tree tree, int from, int to, int weight) {\n        Edge edge = new Edge(to, weight);\n        edge.next = tree.edges[from];\n        tree.edges[from] = edge;\n    }\n    public static void dfs(Tree tree, int node, int parent) {\n        int maxGain = Integer.MIN_VALUE;\n        for (Edge edge = tree.edges[node]; edge != null; edge = edge.next) {\n            if (edge.to != parent) {\n                dfs(tree, edge.to, node);\n                maxGain = Math.max(maxGain, tree.maxGain[edge.to] + edge.weight);\n            }\n        }\n        tree.maxGain[node] = maxGain;\n    }\n    public static int calculateMaxGain(Tree tree, int from, int to) {\n        int maxGain = Integer.MIN_VALUE;\n        for (Edge edge = tree.edges[from]; edge != null; edge = edge.next) {\n            if (edge.to == to) {\n                maxGain = Math.max(maxGain, edge.weight);\n            } else {\n                maxGain = Math.max(maxGain, tree.maxGain[edge.to] + edge.weight);\n            }\n        }\n        return maxGain;\n    }\n}",
      "Python3": "# COMPLETE, RUNNABLE Python3 code\n# Uses input() for lines\n# Handles all T test cases\nclass Edge:\n    def __init__(self, to, weight):\n        self.to = to\n        self.weight = weight\n        self.next = None\n\nclass Tree:\n    def __init__(self, n):\n        self.edges = [None] * n\n        self.maxGain = [-float('inf')] * n\n        self.n = n\n\ndef addEdge(tree, from_, to, weight):\n    edge = Edge(to, weight)\n    edge.next = tree.edges[from_]\n    tree.edges[from_] = edge\n\ndef dfs(tree, node, parent):\n    maxGain = -float('inf')\n    for edge in tree.edges[node]:\n        if edge.to != parent:\n            dfs(tree, edge.to, node)\n            maxGain = max(maxGain, tree.maxGain[edge.to] + edge.weight)\n    tree.maxGain[node] = maxGain\n\ndef calculateMaxGain(tree, from_, to):\n    maxGain = -float('inf')\n    for edge in tree.edges[from_]:\n        if edge.to == to:\n            maxGain = max(maxGain, edge.weight)\n        else:\n            maxGain = max(maxGain, tree.maxGain[edge.to] + edge.weight)\n    return maxGain\n\nn, q = map(int, input().split())\ntree = Tree(n)\nfor _ in range(n - 1):\n    from_, to, weight = map(int, input().split())\n    addEdge(tree, from_ - 1, to - 1, weight)\n    addEdge(tree, to - 1, from_ - 1, weight)\ndfs(tree, 0, -1)\nfor _ in range(q):\n    from_, to = map(int, input().split())\n    print(calculateMaxGain(tree, from_ - 1, to - 1))",
      "JavaScript": "// COMPLETE, RUNNABLE JavaScript code\n// Uses fs.readFileSync(0,'utf-8') — NEVER readline()\n// Handles all T test cases\nconst fs = require('fs');\nconst input = fs.readFileSync(0, 'utf-8').trim().split('\\n');\nlet idx = 0;\nfunction readLine() {\n    return input[idx++];\n}\nfunction readInt() {\n    return parseInt(readLine());\n}\nfunction readInts() {\n    return readLine().split(' ').map(Number);\n}\n// Define the structure for a tree edge\nclass Edge {\n    constructor(to, weight) {\n        this.to = to;\n        this.weight = weight;\n        this.next = null;\n    }\n}\n// Define the structure for a tree\nclass Tree {\n    constructor(n) {\n        this.edges = new Array(n).fill(null);\n        this.maxGain = new Array(n).fill(-Infinity);\n        this.n = n;\n    }\n}\n// Function to add an edge to the tree\nfunction addEdge(tree, from, to, weight) {\n    const edge = new Edge(to, weight);\n    edge.next = tree.edges[from];\n    tree.edges[from] = edge;\n}\n// Function to perform DFS traversal and calculate maximum gain\nfunction dfs(tree, node, parent) {\n    let maxGain = -Infinity;\n    for (let edge = tree.edges[node]; edge !== null; edge = edge.next) {\n        if (edge.to !== parent) {\n            dfs(tree, edge.to, node);\n            maxGain = Math.max(maxGain, tree.maxGain[edge.to] + edge.weight);\n        }\n    }\n    tree.maxGain[node] = maxGain;\n}\n// Function to calculate maximum gain for a query\nfunction calculateMaxGain(tree, from, to) {\n    let maxGain = -Infinity;\n    for (let edge = tree.edges[from]; edge !== null; edge = edge.next) {\n        if (edge.to === to) {\n            maxGain = Math.max(maxGain, edge.weight);\n        } else {\n            maxGain = Math.max(maxGain, tree.maxGain[edge.to] + edge.weight);\n        }\n    }\n    return maxGain;\n}\n// Main function\nfunction main() {\n    const [n, q] = readInts();\n    const tree = new Tree(n);\n    for (let i = 0; i < n - 1; i++) {\n        const [from, to, weight] = readInts();\n        addEdge(tree, from - 1, to - 1, weight);\n        addEdge(tree, to - 1, from - 1, weight);\n    }\n    dfs(tree, 0, -1);\n    for (let i = 0; i < q; i++) {\n        const [from, to] = readInts();\n        console.log(calculateMaxGain(tree, from - 1, to - 1));\n    }\n}\nmain();"
    }
  },
  "QBCategory": "CCD",
  "boilerPlates": {
    "C": "#include <stdio.h>\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "C++": "#include <iostream>\nusing namespace std;\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "Java": "import java.util.*;\nimport java.io.*;\n\npublic class Main {\n    public static void main(String[] args) throws IOException {\n        // your code goes here\n    }\n}",
    "Python3": "# your code goes here\n",
    "JavaScript": "// your code goes here\n"
  },
  "_internal": {
    "methodName": "",
    "params": []
  }
}