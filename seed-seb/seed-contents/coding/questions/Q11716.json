{
  "questionId": "Q11716",
  "title": "4 Sum",
  "slug": "4sum",
  "version": 1,
  "createdAt": "2026-07-05",
  "updatedAt": "2026-07-05",
  "metadata": {
    "category": "Array",
    "difficulty": "Medium",
    "isPremium": true,
    "tags": [
      "Quadruplets",
      "Target Sum",
      "Unique Quadruplets"
    ],
    "companyTags": [],
    "topicTags": [],
    "QBCategory": "CCD"
  },
  "content": {
    "problemStatement": "Given an array of N integers, your task is to find unique quads that add up to give a target value. In short, you need to return an array of all the unique quadruplets [arr[a], arr[b], arr[c], arr[d]] such that their sum is equal to a given target.",
    "constraints": [
      "1 <= N <= 10^4",
      "-10^9 <= arr[i] <= 10^9",
      "-10^9 <= target <= 10^9"
    ],
    "inputFormat": "The input will contain an array of integers and a target sum. The array will be given as a list of space-separated integers, and the target sum will be given as a single integer.",
    "outputFormat": "The output should be a list of all unique quadruplets in the array that sum up to the target value. Each quadruplet should be given as a list of four space-separated integers.",
    "sampleTestCases": []
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "label": "min boundary",
        "input": "[1, 2, 3, 4] 10",
        "expectedOutput": "[[1, 2, 3, 4]]",
        "weight": 2
      },
      {
        "id": "tc_2",
        "label": "max boundary",
        "input": "[1000, 2000, 3000, 4000] 10000",
        "expectedOutput": "[[1000, 2000, 3000, 4000]]",
        "weight": 2
      },
      {
        "id": "tc_3",
        "label": "uniform/edge cases",
        "input": "[0, 0, 0, 0] 0",
        "expectedOutput": "[[0, 0, 0, 0]]",
        "weight": 2
      },
      {
        "id": "tc_4",
        "label": "tricky case / empty-like / negative if allowed",
        "input": "[-1, -2, -3, -4] -10",
        "expectedOutput": "[[-1, -2, -3, -4]]",
        "weight": 2
      },
      {
        "id": "tc_5",
        "label": "general mixed case",
        "input": "[1, -2, 3, -4, 5] 3",
        "expectedOutput": "[[-2, -4, 1, 8]]",
        "weight": 2
      }
    ],
    "totalTestCases": 5,
    "totalWeight": 10
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      2,
      2,
      2,
      2,
      2
    ],
    "maxScore": 10
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "Use four nested loops to generate all possible quadruplets and check if their sum equals the target value. Use a set to store unique quadruplets.",
    "timeComplexity": "O(N^4)",
    "spaceComplexity": "O(N^4)",
    "code": {
      "C": "#include <stdio.h>\n#include <stdlib.h>\n\n// Function to find quadruplets with sum = target\nvoid fourSum(int arr[], int n, int target) {\n    // Use four nested loops to generate all possible quadruplets\n    for (int i = 0; i < n; i++) {\n        for (int j = i + 1; j < n; j++) {\n            for (int k = j + 1; k < n; k++) {\n                for (int l = k + 1; l < n; l++) {\n                    // Calculate sum of four chosen numbers\n                    int sum = arr[i] + arr[j] + arr[k] + arr[l];\n                    // Check if sum matches target\n                    if (sum == target) {\n                        printf(\"[%d, %d, %d, %d]\", arr[i], arr[j], arr[k], arr[l]);\n                    }\n                }\n            }\n        }\n    }\n}\n\nint main() {\n    int arr[] = {1, 0, -1, 0, -2, 2};\n    int n = sizeof(arr) / sizeof(arr[0]);\n    int target = 0;\n    fourSum(arr, n, target);\n    return 0;\n}",
      "C++": "#include <bits/stdc++.h>\nusing namespace std;\n\nclass Solution {\npublic:\n    // Function to find quadruplets with sum = target\n    vector<vector<int>> fourSum(vector<int>& arr, int target) {\n        // Get size of array\n        int n = arr.size();\n        // Use set to avoid duplicate quadruplets\n        set<vector<int>> st;\n\n        // First loop - pick first element\n        for (int i = 0; i < n; i++) {\n            // Second loop - pick second element\n            for (int j = i + 1; j < n; j++) {\n                // Third loop - pick third element\n                for (int k = j + 1; k < n; k++) {\n                    // Fourth loop - pick fourth element\n                    for (int l = k + 1; l < n; l++) {\n                        // Calculate sum of four chosen numbers\n                        long long sum = (long long)arr[i] + arr[j] + arr[k] + arr[l];\n                        // Check if sum matches target\n                        if (sum == target) {\n                            // Store quadruplet in sorted order\n                            vector<int> temp = {arr[i], arr[j], arr[k], arr[l]};\n                            sort(temp.begin(), temp.end());\n                            // Insert into set to ensure uniqueness\n                            st.insert(temp);\n                        }\n                    }\n                }\n            }\n        }\n        // Convert set into vector of quadruplets\n        return vector<vector<int>>(st.begin(), st.end());\n    }\n};\n\nint main() {\n    // Input array\n    vector<int> arr = {1, 0, -1, 0, -2, 2};\n    // Target sum\n    int target = 0;\n\n    // Create object\n    Solution obj;\n    // Get all quadruplets\n    vector<vector<int>> ans = obj.fourSum(arr, target);\n\n    // Print result\n    for (auto quad : ans) {\n        for (int num : quad) cout << num << \" \";\n        cout << endl;\n    }\n    return 0;\n}",
      "Java": "import java.util.*;\n\nclass Solution {\n    // Function to find quadruplets with sum = target\n    public List<List<Integer>> fourSum(int[] arr, int target) {\n        // Get size of array\n        int n = arr.length;\n        // Use set to avoid duplicate quadruplets\n        Set<List<Integer>> set = new HashSet<>();\n\n        // First loop - pick first element\n        for (int i = 0; i < n; i++) {\n            // Second loop - pick second element\n            for (int j = i + 1; j < n; j++) {\n                // Third loop - pick third element\n                for (int k = j + 1; k < n; k++) {\n                    // Fourth loop - pick fourth element\n                    for (int l = k + 1; l < n; l++) {\n                        // Calculate sum of four numbers\n                        long sum = (long) arr[i] + arr[j] + arr[k] + arr[l];\n                        // If sum matches target\n                        if (sum == target) {\n                            // Create quadruplet\n                            List<Integer> temp = Arrays.asList(arr[i], arr[j], arr[k], arr[l]);\n                            // Sort to maintain uniqueness\n                            Collections.sort(temp);\n                            // Add to set\n                            set.add(temp);\n                        }\n                    }\n                }\n            }\n        }\n        // Convert set to list and return\n        return new ArrayList<>(set);\n    }\n}\n\npublic class Main {\n    public static void main(String[] args) {\n        // Input array\n        int[] arr = {1, 0, -1, 0, -2, 2};\n        // Target sum\n        int target = 0;\n\n        // Create object\n        Solution obj = new Solution();\n        // Get result\n        List<List<Integer>> ans = obj.fourSum(arr, target);\n\n        // Print result\n        for (List<Integer> quad : ans) {\n            System.out.println(quad);\n        }\n    }\n}",
      "Python3": "class Solution:\n    # Function to find quadruplets with sum = target\n    def fourSum(self, arr, target):\n        # Get size of array\n        n = len(arr)\n        # Use set to store unique quadruplets\n        st = set()\n\n        # First loop - pick first element\n        for i in range(n):\n            # Second loop - pick second element\n            for j in range(i + 1, n):\n                # Third loop - pick third element\n                for k in range(j + 1, n):\n                    # Fourth loop - pick fourth element\n                    for l in range(k + 1, n):\n                        # If sum equals target\n                        if arr[i] + arr[j] + arr[k] + arr[l] == target:\n                            # Store sorted quadruplet as tuple\n                            temp = tuple(sorted([arr[i], arr[j], arr[k], arr[l]]))\n                            st.add(temp)\n\n        # Convert set to list of lists\n        return [list(quad) for quad in st]\n\n\n# Driver code\narr = [1, 0, -1, 0, -2, 2]\ntarget = 0\n\nobj = Solution()\nans = obj.fourSum(arr, target)\nprint(ans)",
      "JavaScript": "class Solution {\n    // Function to find quadruplets with sum = target\n    fourSum(arr, target) {\n        // Get size of array\n        let n = arr.length;\n        // Use set to avoid duplicates\n        let st = new Set();\n\n        // First loop - pick first element\n        for (let i = 0; i < n; i++) {\n            // Second loop - pick second element\n            for (let j = i + 1; j < n; j++) {\n                // Third loop - pick third element\n                for (let k = j + 1; k < n; k++) {\n                    // Fourth loop - pick fourth element\n                    for (let l = k + 1; l < n; l++) {\n                        // Calculate sum\n                        let sum = arr[i] + arr[j] + arr[k] + arr[l];\n                        // If sum equals target\n                        if (sum === target) {\n                            // Create quadruplet\n                            let temp = [arr[i], arr[j], arr[k], arr[l]];\n                            // Sort it to avoid duplicates\n                            temp.sort((a, b) => a - b);\n                            // Store as string in set\n                            st.add(JSON.stringify(temp));\n                        }\n                    }\n                }\n            }\n        }\n\n        // Convert set back to array of arrays\n        return Array.from(st).map(JSON.parse);\n    }\n}\n\n// Driver code\nlet arr = [1, 0, -1, 0, -2, 2];\nlet target = 0;\n\nlet obj = new Solution();\nlet ans = obj.fourSum(arr, target);\nconsole.log(ans);"
    }
  },
  "QBCategory": "CCD",
  "boilerPlates": {
    "C": "#include <stdio.h>\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "C++": "#include <iostream>\nusing namespace std;\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "Java": "import java.util.*;\nimport java.io.*;\n\npublic class Main {\n    public static void main(String[] args) throws IOException {\n        // your code goes here\n    }\n}",
    "Python3": "# your code goes here\n",
    "JavaScript": "// your code goes here\n"
  },
  "_internal": {
    "methodName": "",
    "params": []
  }
}