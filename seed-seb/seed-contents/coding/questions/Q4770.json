{
  "questionId": "Q4770",
  "title": "Chef Builds Queue Using Stacks",
  "slug": "chef-builds-queue-using-stacks",
  "version": 1,
  "createdAt": "2026-07-01",
  "updatedAt": "2026-07-04",
  "metadata": {
    "category": "Data Structures",
    "difficulty": "Medium",
    "isPremium": true,
    "tags": [
      "Stacks",
      "Queues",
      "Amortized Analysis"
    ],
    "companyTags": [],
    "topicTags": [
      "Stacks & Queues"
    ],
    "QBCategory": "CCD"
  },
  "content": {
    "problemStatement": "Chef wants to implement a queue that follows the **First-In-First-Out (FIFO)** principle using only **two stacks**.\n\nStacks support operations such as **push**, **pop**, **peek**, **size**, and **isEmpty**.\n\nChef needs to design a queue using only stack operations such that it behaves exactly like a normal queue and each operation runs in **amortized O(1)O(1)O(1) time**.\n\n**Class Description**Chef will implement a class named **QueueUsingStacksQueueUsingStacksQueueUsingStacks**.\nThe class supports the following operations:\n\n\n\n**pushElement(x)pushElement(x)pushElement(x)**\n\nInserts the element xxx at the back of the queue.\n\n\n\n\n**popElement()popElement()popElement()**\n\nRemoves and returns the front element of the queue.\n\n\n\n\n**peekElement()peekElement()peekElement()**\n\nReturns the front element of the queue without removing it.\n\n\n\n\n**isEmptyResult()isEmptyResult()isEmptyResult()**\n\nReturns **true** if the queue is empty, otherwise returns **false**.\n\n\n\n**Constraints**\n1≤T≤1051 \\le T \\le 10^51≤T≤105\n1≤N≤2×1051 \\le N \\le 2 \\times 10^51≤N≤2×105\n1≤x≤1091 \\le x \\le 10^91≤x≤109\nThe total number of operations across all test cases will not exceed 10610^6106\nAll popElement()popElement()popElement() and peekElement()peekElement()peekElement() operations are guaranteed to be valid\n\nInput Format\n\nThe first line contains an integer TTT — the number of test cases.\nFor each test case:\n\nThe first line contains an integer NNN — the number of operations.\nThe next NNN lines contain one of the following operations:\n\n`push x`\n`pop`\n`peek`\n`empty`\n\n\n\n\n\n\nOutput Format\n\nFor each test case, print the result of each operation on a new line.\nFor popElement()popElement()popElement() and peekElement()peekElement()peekElement(), print the integer value.\nFor isEmptyResult()isEmptyResult()isEmptyResult(), print **true** if the queue is empty, otherwise print **false**.\nFor pushElement(x)pushElement(x)pushElement(x), print **None**.",
    "constraints": [
      "1 ≤ T ≤ 10^5",
      "1 ≤ N ≤ 2 * 10^5",
      "1 ≤ x ≤ 10^9",
      "The total number of operations across all test cases will not exceed 10^6"
    ],
    "inputFormat": "The first line contains an integer T, the number of test cases. For each test case: The first line contains an integer N, the number of operations. The next N lines contain one of the following operations: `push x`, `pop`, `peek`, `empty`",
    "outputFormat": "For each test case, print the result of each operation on a new line. For `pop` and `peek`, print the integer value. For `empty`, print **true** if the queue is empty, otherwise print **false**. For `push x`, print **None**",
    "sampleTestCases": [
      {
        "id": "sample_1",
        "input": "1\n6\npush 10\npush 20\npeek\npop\nempty\npop",
        "output": "None\nNone\n10\n10\nfalse\n20",
        "explanation": "Explanation: push 10: Added 10 to the queue, output None as push returns nothing. push 20: Added 20 to the queue, output None again. peek: Front element is 10, so output is 10. pop: Removed and returned front element 10. empty: Queue still has 20, so output is false. pop: Removed and returned 20, queue is now empty."
      },
      {
        "id": "sample_2",
        "input": "1\n8\npush 5\npush 3\npush 7\npop\npeek\npop\npop\nempty",
        "output": "None\nNone\nNone\n5\n3\n3\n7\ntrue",
        "explanation": "Explanation: push 5: Queue = [5], output None as push doesn't return value. push 3: Queue = [5, 3], output None. push 7: Queue = [5, 3, 7], output None. pop: Removes front 5, output 5. Queue = [3, 7]. peek: Front is 3, output 3. Queue unchanged. pop: Removes front 3, output 3. Queue = [7]. pop: Removes front 7, output 7. Queue = []. empty: Queue is empty, output true."
      },
      {
        "id": "sample_3",
        "input": "1\n4\npush 10\nempty\npop\nempty",
        "output": "None\nfalse\n10\ntrue",
        "explanation": "Explanation: push 10: Added 10 to the queue, output None as push returns nothing. empty: Queue has 10, so output is false. pop: Removed and returned 10, queue is now empty. empty: Queue is empty, output true."
      }
    ]
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "label": "minimum boundary",
        "input": "1\n1\npush 1",
        "expectedOutput": "None",
        "weight": 2
      },
      {
        "id": "tc_2",
        "label": "maximum boundary",
        "input": "1\n10\npush 1\npush 2\npush 3\npush 4\npush 5\npush 6\npush 7\npush 8\npush 9\npush 10",
        "expectedOutput": "None\nNone\nNone\nNone\nNone\nNone\nNone\nNone\nNone\nNone",
        "weight": 2
      },
      {
        "id": "tc_3",
        "label": "uniform/edge case",
        "input": "1\n5\npush 1\npush 1\npush 1\npush 1\npush 1",
        "expectedOutput": "None\nNone\nNone\nNone\nNone",
        "weight": 2
      },
      {
        "id": "tc_4",
        "label": "tricky case",
        "input": "1\n5\npush 1\npop\npush 2\npop\nempty",
        "expectedOutput": "None\n1\nNone\n2\ntrue",
        "weight": 2
      },
      {
        "id": "tc_5",
        "label": "stress/general",
        "input": "2\n5\npush 1\npush 2\npush 3\npop\nempty\n5\npush 4\npush 5\npush 6\npop\nempty",
        "expectedOutput": "None\nNone\nNone\n1\nfalse\nNone\nNone\nNone\n4\nfalse",
        "weight": 2
      }
    ],
    "totalTestCases": 5,
    "totalWeight": 10
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      2,
      2,
      2,
      2,
      2
    ],
    "maxScore": 10
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "We use two stacks to implement a queue. The `inStack` is used to store the elements when they are pushed into the queue, and the `outStack` is used to store the elements when they are popped from the queue. When an element is pushed into the queue, it is pushed onto the `inStack`. When an element is popped from the queue, if the `outStack` is empty, we pop all elements from the `inStack` and push them onto the `outStack`, then pop the top element from the `outStack`. If the `outStack` is not empty, we simply pop the top element from the `outStack`. The `peek` operation is similar to the `pop` operation, but instead of popping the top element, we return its value. The `isEmpty` operation checks if both stacks are empty.",
    "timeComplexity": "O(1) amortized per operation, O(N) total for N operations",
    "spaceComplexity": "O(N) for storing the elements in the two stacks",
    "code": {
      "C": "#include <stdio.h>\n#include <stdlib.h>\ntypedef struct StackNode {\n    int data;\n    struct StackNode* next;\n} StackNode;\ntypedef struct {\n    StackNode* top;\n} Stack;\nvoid push(Stack* stack, int x) {\n    StackNode* newNode = (StackNode*)malloc(sizeof(StackNode));\n    newNode->data = x;\n    newNode->next = stack->top;\n    stack->top = newNode;\n}\nint pop(Stack* stack) {\n    if (stack->top == NULL) return -1;\n    int x = stack->top->data;\n    StackNode* temp = stack->top;\n    stack->top = stack->top->next;\n    free(temp);\n    return x;\n}\nint peek(Stack* stack) {\n    if (stack->top == NULL) return -1;\n    return stack->top->data;\n}\nint isEmpty(Stack* stack) {\n    return stack->top == NULL;\n}\nvoid queueUsingStacks(int N) {\n    Stack inStack, outStack;\n    inStack.top = NULL;\n    outStack.top = NULL;\n    for (int i = 0; i < N; i++) {\n        char op[10];\n        int x;\n        scanf(\"%s\", op);\n        if (strcmp(op, \"push\") == 0) {\n            scanf(\"%d\", &x);\n            push(&inStack, x);\n            printf(\"None\\n\");\n        } else if (strcmp(op, \"pop\") == 0) {\n            if (outStack.top == NULL) {\n                while (inStack.top != NULL) {\n                    push(&outStack, pop(&inStack));\n                }\n            }\n            if (outStack.top != NULL) {\n                printf(\"%d\\n\", pop(&outStack));\n            } else {\n                printf(\"-1\\n\");\n            }\n        } else if (strcmp(op, \"peek\") == 0) {\n            if (outStack.top == NULL) {\n                while (inStack.top != NULL) {\n                    push(&outStack, pop(&inStack));\n                }\n            }\n            if (outStack.top != NULL) {\n                printf(\"%d\\n\", peek(&outStack));\n            } else {\n                printf(\"-1\\n\");\n            }\n        } else if (strcmp(op, \"empty\") == 0) {\n            if (inStack.top == NULL && outStack.top == NULL) {\n                printf(\"true\\n\");\n            } else {\n                printf(\"false\\n\");\n            }\n        }\n    }\n}\nint main() {\n    int T;\n    scanf(\"%d\", &T);\n    for (int i = 0; i < T; i++) {\n        int N;\n        scanf(\"%d\", &N);\n        queueUsingStacks(N);\n    }\n    return 0;\n}",
      "C++": "#include <iostream>\n#include <stack>\nusing namespace std;\nclass QueueUsingStacks {\npublic:\n    void pushElement(int x) {\n        inStack.push(x);\n    }\n    int popElement() {\n        if (outStack.empty()) {\n            while (!inStack.empty()) {\n                outStack.push(inStack.top());\n                inStack.pop();\n            }\n        }\n        if (!outStack.empty()) {\n            int x = outStack.top();\n            outStack.pop();\n            return x;\n        } else {\n            return -1;\n        }\n    }\n    int peekElement() {\n        if (outStack.empty()) {\n            while (!inStack.empty()) {\n                outStack.push(inStack.top());\n                inStack.pop();\n            }\n        }\n        if (!outStack.empty()) {\n            return outStack.top();\n        } else {\n            return -1;\n        }\n    }\n    bool isEmptyResult() {\n        return inStack.empty() && outStack.empty();\n    }\nprivate:\n    stack<int> inStack;\n    stack<int> outStack;\n};\nint main() {\n    int T;\n    cin >> T;\n    for (int i = 0; i < T; i++) {\n        int N;\n        cin >> N;\n        QueueUsingStacks queue;\n        for (int j = 0; j < N; j++) {\n            string op;\n            cin >> op;\n            if (op == \"push\") {\n                int x;\n                cin >> x;\n                queue.pushElement(x);\n                cout << \"None\" << endl;\n            } else if (op == \"pop\") {\n                cout << queue.popElement() << endl;\n            } else if (op == \"peek\") {\n                cout << queue.peekElement() << endl;\n            } else if (op == \"empty\") {\n                cout << (queue.isEmptyResult() ? \"true\" : \"false\") << endl;\n            }\n        }\n    }\n    return 0;\n}",
      "Java": "import java.util.Stack;\npublic class Main {\n    public static void main(String[] args) {\n        java.util.Scanner scanner = new java.util.Scanner(System.in);\n        int T = scanner.nextInt();\n        for (int i = 0; i < T; i++) {\n            int N = scanner.nextInt();\n            Stack<Integer> inStack = new Stack<>();\n            Stack<Integer> outStack = new Stack<>();\n            for (int j = 0; j < N; j++) {\n                String op = scanner.next();\n                if (op.equals(\"push\")) {\n                    int x = scanner.nextInt();\n                    inStack.push(x);\n                    System.out.println(\"None\");\n                } else if (op.equals(\"pop\")) {\n                    if (outStack.isEmpty()) {\n                        while (!inStack.isEmpty()) {\n                            outStack.push(inStack.pop());\n                        }\n                    }\n                    if (!outStack.isEmpty()) {\n                        System.out.println(outStack.pop());\n                    } else {\n                        System.out.println(\"-1\");\n                    }\n                } else if (op.equals(\"peek\")) {\n                    if (outStack.isEmpty()) {\n                        while (!inStack.isEmpty()) {\n                            outStack.push(inStack.pop());\n                        }\n                    }\n                    if (!outStack.isEmpty()) {\n                        System.out.println(outStack.peek());\n                    } else {\n                        System.out.println(\"-1\");\n                    }\n                } else if (op.equals(\"empty\")) {\n                    System.out.println(inStack.isEmpty() && outStack.isEmpty());\n                }\n            }\n        }\n    }\n}",
      "Python3": "import sys\ninput = sys.stdin.readline\nfor _ in range(int(input())):\n    N = int(input())\n    inStack, outStack = [], []\n    for _ in range(N):\n        op = input().split()\n        if op[0] == 'push':\n            inStack.append(int(op[1]))\n            print('None')\n        elif op[0] == 'pop':\n            if not outStack:\n                while inStack:\n                    outStack.append(inStack.pop())\n            if outStack:\n                print(outStack.pop())\n            else:\n                print('-1')\n        elif op[0] == 'peek':\n            if not outStack:\n                while inStack:\n                    outStack.append(inStack.pop())\n            if outStack:\n                print(outStack[-1])\n            else:\n                print('-1')\n        elif op[0] == 'empty':\n            print(not inStack and not outStack)\n",
      "JavaScript": "const fs = require('fs');\nconst input = fs.readFileSync(0, 'utf-8').trim().split('\\n');\nlet idx = 0;\nconst readline = () => input[idx++];\nconst T = parseInt(readline());\nfor (let i = 0; i < T; i++) {\n    const N = parseInt(readline());\n    let inStack = [], outStack = [];\n    for (let j = 0; j < N; j++) {\n        const op = readline().split(' ');\n        if (op[0] === 'push') {\n            inStack.push(parseInt(op[1]));\n            console.log('None');\n        } else if (op[0] === 'pop') {\n            if (outStack.length === 0) {\n                while (inStack.length > 0) {\n                    outStack.push(inStack.pop());\n                }\n            }\n            if (outStack.length > 0) {\n                console.log(outStack.pop());\n            } else {\n                console.log('-1');\n            }\n        } else if (op[0] === 'peek') {\n            if (outStack.length === 0) {\n                while (inStack.length > 0) {\n                    outStack.push(inStack.pop());\n                }\n            }\n            if (outStack.length > 0) {\n                console.log(outStack[outStack.length - 1]);\n            } else {\n                console.log('-1');\n            }\n        } else if (op[0] === 'empty') {\n            console.log(inStack.length === 0 && outStack.length === 0);\n        }\n    }\n}"
    }
  },
  "QBCategory": "CCD",
  "boilerPlates": {
    "C": "#include <stdio.h>\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "C++": "#include <iostream>\nusing namespace std;\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "Java": "import java.util.*;\nimport java.io.*;\n\npublic class Main {\n    public static void main(String[] args) throws IOException {\n        // your code goes here\n    }\n}",
    "Python3": "# your code goes here\n",
    "JavaScript": "// your code goes here\n"
  },
  "_internal": {
    "methodName": "",
    "params": []
  }
}