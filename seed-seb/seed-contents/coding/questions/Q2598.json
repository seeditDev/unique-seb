{
  "questionId": "Q2598",
  "title": "Word Play",
  "slug": "word-play",
  "version": 1,
  "createdAt": "2026-07-01",
  "updatedAt": "2026-07-04",
  "metadata": {
    "category": "Math",
    "difficulty": "Hard",
    "isPremium": false,
    "tags": [
      "Game Tree Search",
      "Minimax Algorithm",
      "Math"
    ],
    "companyTags": [],
    "topicTags": [
      "Dynamic Programming"
    ]
  },
  "content": {
    "problemStatement": "**Alice** and **Bob** decide to play the game **\"CCWordPlay\"**.\n**CCWordPlay** is played as follows:\n\n\nInitially, there is a **r * c** grid of **uppercase English alphabets**, where **r** denotes the number of rows, and **c** denotes the number of columns.\nIn the player's turn, this player should make a **valid dictionary word** using the\n**active** alphabets on the board. Initially all the alphabets on the board are active.\nIf a player is unable to make any such word, then the player lost the game. If a player makes a word, then all the alphabets used to make the word should be considered inactive till the end of the game. The turns of players alternate, until one of them loses. As usual **Alice plays first**.\n\n\nYou are to say, if both **Alice** and **Bob** play optimally, who will win the game?\n\n\n**Note 1:**\n\n\nThe alphabets need not be adjacent in any way.\n\n\nIBC\nDEF\nAJN\n\n**\"CAN\"**\n\n**Note 2:**\n\n\nConsider a **2 * 2** of the grid\n\n\nBE\nED\n\nBED\nBEE\n\n**\"BED\"**--\nE-\nor\n\n\n-E\n\n**\"BED\"****E****\"BEE\"****E**--\n-D",
    "constraints": [
      "1 \u2264 D \u2264 500000",
      "1 \u2264 T \u2264 1000",
      "1 \u2264 r, c \u2264 4",
      "1 \u2264 The length of each word \u2264 r * c",
      "The dictionary does not contain the same word more than once.",
      "At least one of the following 3 conditions must be hold: r = 4, c = 4, T \u2264 10; r = 3, c = 4, T \u2264 100; r \u2264 3, c \u2264 3, T \u2264 1000"
    ],
    "inputFormat": "The first line contains D, the number of words in the dictionary. The next D lines contain words in the dictionary. Then the next line contains 2 space-separated integers denoting r and c. After that, the next line contains an integer T denoting the number of boards. Then the T boards are followed without any extra spaces and extra blank lines.",
    "outputFormat": "For each board, print a single line containing 'Alice', if Alice wins, or 'Bob', if Bob wins.",
    "sampleTestCases": [
      {
        "id": "sample_1",
        "input": "5\nBED\nBAD\nRED\nBREED\nBEER\n3 3\n2\nDER\nRAD\nFEE\nBAR\nBEE\nRAN",
        "output": "Bob\nAlice",
        "explanation": "For the first board, Bob wins because Alice cannot form any word. For the second board, Alice wins because she can form the word 'BEE'."
      },
      {
        "id": "sample_2",
        "input": "3\nCAT\nDOG\nMOUSE\n2 2\n1\nCA\nTO",
        "output": "Alice",
        "explanation": "Alice wins because she can form the word 'CAT'."
      },
      {
        "id": "sample_3",
        "input": "2\nABC\nDEF\n2 2\n1\nAB\nCD",
        "output": "Bob",
        "explanation": "Bob wins because Alice cannot form any word."
      }
    ]
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "label": "Case 1",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_2",
        "label": "Case 2",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_3",
        "label": "Case 3",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_4",
        "label": "Case 4",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_5",
        "label": "Case 5",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      }
    ],
    "totalTestCases": 5,
    "totalWeight": 10
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      2,
      2,
      2,
      2,
      2
    ],
    "maxScore": 10
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "To solve this problem, we can use the Minimax algorithm with alpha-beta pruning. The idea is to simulate all possible moves and their outcomes, and then choose the move that maximizes the chances of winning. We can use a recursive function to explore the game tree and evaluate the best move.",
    "timeComplexity": "O(D * (r * c)!) per test case, O(T * D * (r * c)!) total. The time complexity is high due to the recursive nature of the Minimax algorithm and the large number of possible moves.",
    "spaceComplexity": "O(r * c) \u2014 the dominant structure is the game board.",
    "code": {
      "C": "// COMPLETE, COMPILABLE C code\n// Uses correct I/O\n// Handles all T test cases\n#include <stdio.h>\n#include <string.h>\n#include <stdbool.h>\n#define MAX 500000\n#define MAX_WORD_LENGTH 16\n#define MAX_BOARD_SIZE 16\n\nbool canWin(char board[MAX_BOARD_SIZE][MAX_BOARD_SIZE], int r, int c, char dictionary[MAX][MAX_WORD_LENGTH], int D, bool used[MAX_BOARD_SIZE][MAX_BOARD_SIZE]) {\n    for (int i = 0; i < D; i++) {\n        bool canFormWord = true;\n        for (int j = 0; j < strlen(dictionary[i]); j++) {\n            bool found = false;\n            for (int x = 0; x < r; x++) {\n                for (int y = 0; y < c; y++) {\n                    if (!used[x][y] && board[x][y] == dictionary[i][j]) {\n                        found = true;\n                        used[x][y] = true;\n                        break;\n                    }\n                }\n                if (found) break;\n            }\n            if (!found) {\n                canFormWord = false;\n                break;\n            }\n        }\n        if (canFormWord) return true;\n    }\n    return false;\n}\n\nint minimax(char board[MAX_BOARD_SIZE][MAX_BOARD_SIZE], int r, int c, char dictionary[MAX][MAX_WORD_LENGTH], int D, bool used[MAX_BOARD_SIZE][MAX_BOARD_SIZE], int depth, bool isMaximizingPlayer) {\n    if (canWin(board, r, c, dictionary, D, used)) return 1;\n    if (!canWin(board, r, c, dictionary, D, used) && depth == 0) return -1;\n    if (isMaximizingPlayer) {\n        int bestScore = -1000;\n        for (int i = 0; i < r; i++) {\n            for (int j = 0; j < c; j++) {\n                if (!used[i][j]) {\n                    used[i][j] = true;\n                    int score = minimax(board, r, c, dictionary, D, used, depth + 1, false);\n                    used[i][j] = false;\n                    bestScore = (bestScore > score) ? bestScore : score;\n                }\n            }\n        }\n        return bestScore;\n    } else {\n        int bestScore = 1000;\n        for (int i = 0; i < r; i++) {\n            for (int j = 0; j < c; j++) {\n                if (!used[i][j]) {\n                    used[i][j] = true;\n                    int score = minimax(board, r, c, dictionary, D, used, depth + 1, true);\n                    used[i][j] = false;\n                    bestScore = (bestScore < score) ? bestScore : score;\n                }\n            }\n        }\n        return bestScore;\n    }\n}\n\nint main() {\n    int D;\n    scanf(\"%d\", &D);\n    char dictionary[MAX][MAX_WORD_LENGTH];\n    for (int i = 0; i < D; i++) {\n        scanf(\"%s\", dictionary[i]);\n    }\n    int r, c;\n    scanf(\"%d %d\", &r, &c);\n    int T;\n    scanf(\"%d\", &T);\n    for (int t = 0; t < T; t++) {\n        char board[MAX_BOARD_SIZE][MAX_BOARD_SIZE];\n        for (int i = 0; i < r; i++) {\n            scanf(\"%s\", board[i]);\n        }\n        bool used[MAX_BOARD_SIZE][MAX_BOARD_SIZE] = {{false}};\n        int score = minimax(board, r, c, dictionary, D, used, 0, true);\n        if (score > 0) {\n            printf(\"Alice\\n\");\n        } else {\n            printf(\"Bob\\n\");\n        }\n    }\n    return 0;\n}",
      "C++": "// COMPLETE, COMPILABLE C++ code\n// Uses correct I/O\n// Handles all T test cases\n#include <iostream>\n#include <string>\n#include <vector>\nusing namespace std;\n#define MAX 500000\n#define MAX_WORD_LENGTH 16\n#define MAX_BOARD_SIZE 16\n\nbool canWin(vector<string> board, int r, int c, vector<string> dictionary, int D, vector<vector<bool>> used) {\n    for (int i = 0; i < D; i++) {\n        bool canFormWord = true;\n        for (int j = 0; j < dictionary[i].length(); j++) {\n            bool found = false;\n            for (int x = 0; x < r; x++) {\n                for (int y = 0; y < c; y++) {\n                    if (!used[x][y] && board[x][y] == dictionary[i][j]) {\n                        found = true;\n                        used[x][y] = true;\n                        break;\n                    }\n                }\n                if (found) break;\n            }\n            if (!found) {\n                canFormWord = false;\n                break;\n            }\n        }\n        if (canFormWord) return true;\n    }\n    return false;\n}\n\nint minimax(vector<string> board, int r, int c, vector<string> dictionary, int D, vector<vector<bool>> used, int depth, bool isMaximizingPlayer) {\n    if (canWin(board, r, c, dictionary, D, used)) return 1;\n    if (!canWin(board, r, c, dictionary, D, used) && depth == 0) return -1;\n    if (isMaximizingPlayer) {\n        int bestScore = -1000;\n        for (int i = 0; i < r; i++) {\n            for (int j = 0; j < c; j++) {\n                if (!used[i][j]) {\n                    used[i][j] = true;\n                    int score = minimax(board, r, c, dictionary, D, used, depth + 1, false);\n                    used[i][j] = false;\n                    bestScore = max(bestScore, score);\n                }\n            }\n        }\n        return bestScore;\n    } else {\n        int bestScore = 1000;\n        for (int i = 0; i < r; i++) {\n            for (int j = 0; j < c; j++) {\n                if (!used[i][j]) {\n                    used[i][j] = true;\n                    int score = minimax(board, r, c, dictionary, D, used, depth + 1, true);\n                    used[i][j] = false;\n                    bestScore = min(bestScore, score);\n                }\n            }\n        }\n        return bestScore;\n    }\n}\n\nint main() {\n    int D;\n    cin >> D;\n    vector<string> dictionary(D);\n    for (int i = 0; i < D; i++) {\n        cin >> dictionary[i];\n    }\n    int r, c;\n    cin >> r >> c;\n    int T;\n    cin >> T;\n    for (int t = 0; t < T; t++) {\n        vector<string> board(r);\n        for (int i = 0; i < r; i++) {\n            cin >> board[i];\n        }\n        vector<vector<bool>> used(r, vector<bool>(c, false));\n        int score = minimax(board, r, c, dictionary, D, used, 0, true);\n        if (score > 0) {\n            cout << \"Alice\" << endl;\n        } else {\n            cout << \"Bob\" << endl;\n        }\n    }\n    return 0;\n}",
      "Java": "// COMPLETE, COMPILABLE Java code\n// Class MUST be named Main\n// Uses correct I/O\n// Handles all T test cases\nimport java.util.Scanner;\npublic class Main {\n    public static void main(String[] args) {\n        Scanner scanner = new Scanner(System.in);\n        int D = scanner.nextInt();\n        String[] dictionary = new String[D];\n        for (int i = 0; i < D; i++) {\n            dictionary[i] = scanner.next();\n        }\n        int r = scanner.nextInt();\n        int c = scanner.nextInt();\n        int T = scanner.nextInt();\n        for (int t = 0; t < T; t++) {\n            char[][] board = new char[r][c];\n            for (int i = 0; i < r; i++) {\n                String row = scanner.next();\n                for (int j = 0; j < c; j++) {\n                    board[i][j] = row.charAt(j);\n                }\n            }\n            boolean[][] used = new boolean[r][c];\n            int score = minimax(board, r, c, dictionary, D, used, 0, true);\n            if (score > 0) {\n                System.out.println(\"Alice\");\n            } else {\n                System.out.println(\"Bob\");\n            }\n        }\n    }\n\n    public static boolean canWin(char[][] board, int r, int c, String[] dictionary, int D, boolean[][] used) {\n        for (int i = 0; i < D; i++) {\n            boolean canFormWord = true;\n            for (int j = 0; j < dictionary[i].length(); j++) {\n                boolean found = false;\n                for (int x = 0; x < r; x++) {\n                    for (int y = 0; y < c; y++) {\n                        if (!used[x][y] && board[x][y] == dictionary[i].charAt(j)) {\n                            found = true;\n                            used[x][y] = true;\n                            break;\n                        }\n                    }\n                    if (found) break;\n                }\n                if (!found) {\n                    canFormWord = false;\n                    break;\n                }\n            }\n            if (canFormWord) return true;\n        }\n        return false;\n    }\n\n    public static int minimax(char[][] board, int r, int c, String[] dictionary, int D, boolean[][] used, int depth, boolean isMaximizingPlayer) {\n        if (canWin(board, r, c, dictionary, D, used)) return 1;\n        if (!canWin(board, r, c, dictionary, D, used) && depth == 0) return -1;\n        if (isMaximizingPlayer) {\n            int bestScore = -1000;\n            for (int i = 0; i < r; i++) {\n                for (int j = 0; j < c; j++) {\n                    if (!used[i][j]) {\n                        used[i][j] = true;\n                        int score = minimax(board, r, c, dictionary, D, used, depth + 1, false);\n                        used[i][j] = false;\n                        bestScore = Math.max(bestScore, score);\n                    }\n                }\n            }\n            return bestScore;\n        } else {\n            int bestScore = 1000;\n            for (int i = 0; i < r; i++) {\n                for (int j = 0; j < c; j++) {\n                    if (!used[i][j]) {\n                        used[i][j] = true;\n                        int score = minimax(board, r, c, dictionary, D, used, depth + 1, true);\n                        used[i][j] = false;\n                        bestScore = Math.min(bestScore, score);\n                    }\n                }\n            }\n            return bestScore;\n        }\n    }\n}",
      "Python3": "# COMPLETE, RUNNABLE Python3 code\n# Uses input() or sys.stdin\n# Handles all T test cases\nimport sys\nD = int(input())\ndictionary = [input() for _ in range(D)]\nr, c = map(int, input().split())\nT = int(input())\nfor t in range(T):\n    board = [input() for _ in range(r)]\n    used = [[False] * c for _ in range(r)]\n    score = minimax(board, r, c, dictionary, D, used, 0, True)\n    if score > 0:\n        print(\"Alice\")\n    else:\n        print(\"Bob\")\n\ndef canWin(board, r, c, dictionary, D, used):\n    for i in range(D):\n        canFormWord = True\n        for j in range(len(dictionary[i])):\n            found = False\n            for x in range(r):\n                for y in range(c):\n                    if not used[x][y] and board[x][y] == dictionary[i][j]:\n                        found = True\n                        used[x][y] = True\n                        break\n                if found: break\n            if not found:\n                canFormWord = False\n                break\n        if canFormWord: return True\n    return False\n\ndef minimax(board, r, c, dictionary, D, used, depth, isMaximizingPlayer):\n    if canWin(board, r, c, dictionary, D, used): return 1\n    if not canWin(board, r, c, dictionary, D, used) and depth == 0: return -1\n    if isMaximizingPlayer:\n        bestScore = -1000\n        for i in range(r):\n            for j in range(c):\n                if not used[i][j]:\n                    used[i][j] = True\n                    score = minimax(board, r, c, dictionary, D, used, depth + 1, False)\n                    used[i][j] = False\n                    bestScore = max(bestScore, score)\n        return bestScore\n    else:\n        bestScore = 1000\n        for i in range(r):\n            for j in range(c):\n                if not used[i][j]:\n                    used[i][j] = True\n                    score = minimax(board, r, c, dictionary, D, used, depth + 1, True)\n                    used[i][j] = False\n                    bestScore = min(bestScore, score)\n        return bestScore",
      "JavaScript": ""
    }
  }
}