{
  "questionId": "Q11736",
  "title": "Painter's Partition",
  "slug": "painterspartition",
  "version": 1,
  "createdAt": "2026-07-05",
  "updatedAt": "2026-07-05",
  "metadata": {
    "category": "Greedy",
    "difficulty": "Medium",
    "isPremium": true,
    "tags": [
      "Painter's Partition",
      "Minimum Time",
      "Greedy Algorithm"
    ],
    "companyTags": [],
    "topicTags": [],
    "QBCategory": "CCD"
  },
  "content": {
    "problemStatement": "Given an array/list of length ‘N’, where the array/list represents the boards and each element of the given array/list represents the length of each board. Some ‘K’ numbers of painters are available to paint these boards. Consider that each unit of a board takes 1 unit of time to paint. You are supposed to return the area of the minimum time to get this job done of painting all the ‘N’ boards under the constraint that any painter will only paint the continuous sections of boards.",
    "constraints": [
      "1 <= N <= 10^5",
      "1 <= K <= 10^5",
      "1 <= boards[i] <= 10^5",
      "1 <= T <= 100"
    ],
    "inputFormat": "The input will contain two parameters: an array of integers representing the lengths of the boards and an integer representing the number of painters available.",
    "outputFormat": "The output should be the minimum time required to paint all the boards with the given number of painters.",
    "sampleTestCases": []
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "label": "min boundary",
        "input": {
          "boards": [
            1
          ],
          "k": 1
        },
        "expectedOutput": 1,
        "weight": 2
      },
      {
        "id": "tc_2",
        "label": "max boundary",
        "input": {
          "boards": [
            100000
          ],
          "k": 1
        },
        "expectedOutput": 100000,
        "weight": 2
      },
      {
        "id": "tc_3",
        "label": "uniform case",
        "input": {
          "boards": [
            10,
            10,
            10,
            10
          ],
          "k": 4
        },
        "expectedOutput": 10,
        "weight": 2
      },
      {
        "id": "tc_4",
        "label": "tricky case",
        "input": {
          "boards": [
            1,
            2,
            3,
            4,
            5
          ],
          "k": 3
        },
        "expectedOutput": 6,
        "weight": 2
      },
      {
        "id": "tc_5",
        "label": "general mixed case",
        "input": {
          "boards": [
            10,
            20,
            30,
            40,
            50
          ],
          "k": 3
        },
        "expectedOutput": 60,
        "weight": 2
      }
    ],
    "totalTestCases": 5,
    "totalWeight": 10
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      2,
      2,
      2,
      2,
      2
    ],
    "maxScore": 10
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "Use a binary search approach to find the minimum time required to paint all the boards with the given number of painters.",
    "timeComplexity": "O(N log M)",
    "spaceComplexity": "O(1)",
    "code": {
      "C": "#include <stdio.h>\n#include <stdlib.h>\n\nint countPainters(int* boards, int boardsSize, int time) {\n    int painters = 1;\n    int boardsPainter = 0;\n    for (int i = 0; i < boardsSize; i++) {\n        if (boardsPainter + boards[i] <= time) {\n            boardsPainter += boards[i];\n        } else {\n            painters++;\n            boardsPainter = boards[i];\n        }\n    }\n    return painters;\n}\n\nint findLargestMinDistance(int* boards, int boardsSize, int k) {\n    int low = boards[0];\n    int high = 0;\n    for (int i = 0; i < boardsSize; i++) {\n        if (boards[i] > low) low = boards[i];\n        high += boards[i];\n    }\n    while (low < high) {\n        int mid = low + (high - low) / 2;\n        if (countPainters(boards, boardsSize, mid) <= k) {\n            high = mid;\n        } else {\n            low = mid + 1;\n        }\n    }\n    return low;\n}\n\nint main() {\n    int boards[] = {10, 20, 30, 40, 50};\n    int k = 3;\n    int boardsSize = sizeof(boards) / sizeof(boards[0]);\n    int ans = findLargestMinDistance(boards, boardsSize, k);\n    printf(\"The answer is: %d\\n\", ans);\n    return 0;\n}",
      "C++": "#include <bits/stdc++.h>\nusing namespace std;\n\n// Class to solve the Painter Partition Problem\nclass PainterPartition {\npublic:\n    // Helper function to count how many painters are required to paint the boards within the given time limit\n    int countPainters(vector<int> &boards, int time) {\n        int n = boards.size();\n        int painters = 1; // Start with one painter\n        long long boardsPainter = 0; // Current total board length assigned to a painter\n\n        for (int i = 0; i < n; i++) {\n            if (boardsPainter + boards[i] <= time) {\n                // If current board can be assigned to the same painter\n                boardsPainter += boards[i];\n            } else {\n                // Otherwise, assign it to the next painter\n                painters++;\n                boardsPainter = boards[i];\n            }\n        }\n        return painters;\n    }\n\n    // Function to find the minimum time to paint all boards using at most k painters\n    int findLargestMinDistance(vector<int> &boards, int k) {\n        int low = *max_element(boards.begin(), boards.end()); // Min possible time = largest board\n        int high = accumulate(boards.begin(), boards.end(), 0); // Max time = total sum of boards\n\n        while (low < high) {\n            int mid = low + (high - low) / 2;\n            if (countPainters(boards, mid) <= k) {\n                high = mid;\n            } else {\n                low = mid + 1;\n            }\n        }\n        return low;\n    }\n};\n\nint main() {\n    vector<int> boards = {10, 20, 30, 40, 50};\n    int k = 3;\n\n    PainterPartition obj;\n    int ans = obj.findLargestMinDistance(boards, k);\n    cout << \"The answer is: \" << ans << \"\\n\";\n\n    return 0;\n}",
      "Java": "import java.util.*;\n\npublic class PainterPartition {\n\n    // Function to count how many painters are required if each painter paints at most 'time' units\n    public int countPainters(int[] boards, int time) {\n        int painters = 1;         // Start with one painter\n        int boardsPainter = 0;    // Time consumed by current painter\n\n        for (int board : boards) {\n            if (boardsPainter + board <= time) {\n                // Assign board to current painter\n                boardsPainter += board;\n            } else {\n                // Assign board to next painter\n                painters++;\n                boardsPainter = board;\n            }\n        }\n\n        return painters;\n    }\n\n    // Function to find the minimum possible maximum time to paint all boards using k painters\n    public int findLargestMinDistance(int[] boards, int k) {\n        int low = Arrays.stream(boards).max().getAsInt();  // Largest single board\n        int high = Arrays.stream(boards).sum();            // Sum of all boards\n\n        while (low < high) {\n            int mid = low + (high - low) / 2;\n            if (countPainters(boards, mid) <= k) {\n                high = mid;\n            } else {\n                low = mid + 1;\n            }\n        }\n        return low;  // Fallback (shouldn't usually reach here)\n    }\n\n    public static void main(String[] args) {\n        int[] boards = {10, 20, 30, 40, 50};  // Length of boards\n        int k = 3;  // Number of painters\n\n        PainterPartition pp = new PainterPartition();\n        int ans = pp.findLargestMinDistance(boards, k);\n\n        System.out.println(\"The answer is: \" + ans);  // Expected: 60\n    }\n}",
      "Python3": "from typing import List\n\nclass PainterPartition:\n    # Helper to count how many painters are required for a given max time\n    def count_painters(self, boards: List[int], time: int) -> int:\n        painters = 1              # Start with one painter\n        boards_painter = 0        # Current load on a painter\n\n        for board in boards:\n            if boards_painter + board <= time:\n                # Assign board to the current painter\n                boards_painter += board\n            else:\n                # Assign board to a new painter\n                painters += 1\n                boards_painter = board\n\n        return painters\n\n    # Function to find the minimum maximum time to paint all boards with k painters\n    def find_largest_min_distance(self, boards: List[int], k: int) -> int:\n        low = max(boards)         # No painter can paint less than the largest board\n        high = sum(boards)        # All boards painted by one painter (max possible)\n\n        while low < high:\n            mid = low + (high - low) // 2\n            if self.count_painters(boards, mid) <= k:\n                high = mid\n            else:\n                low = mid + 1\n        return low\n\n# Test case\nboards = [10, 20, 30, 40, 50]\nk = 3\n\npp = PainterPartition()\nans = pp.find_largest_min_distance(boards, k)\n\nprint(\"The answer is:\", ans)  # Expected: 60",
      "JavaScript": "class PainterPartition {\n    // Count how many painters are required if each painter can paint at most 'time' units\n    countPainters(boards, time) {\n        let painters = 1;         // Start with one painter\n        let boardsPainter = 0;    // Current painter's load\n\n        for (let board of boards) {\n            if (boardsPainter + board <= time) {\n                // Assign board to current painter\n                boardsPainter += board;\n            } else {\n                // Assign board to next painter\n                painters++;\n                boardsPainter = board;\n            }\n        }\n\n        return painters;\n    }\n\n    // Find the minimum time needed to paint all boards using k painters\n    findLargestMinDistance(boards, k) {\n        const low = Math.max(...boards);             // No painter can handle less than max board\n        const high = boards.reduce((a, b) => a + b); // Sum of all boards (worst case)\n\n        let left = low;\n        let right = high;\n        while (left < right) {\n            const mid = Math.floor((left + right) / 2);\n            if (this.countPainters(boards, mid) <= k) {\n                right = mid;\n            } else {\n                left = mid + 1;\n            }\n        }\n        return left;  // Fallback\n    }\n}\n\n// Test example\nconst boards = [10, 20, 30, 40, 50];\nconst k = 3;\n\nconst pp = new PainterPartition();\nconst ans = pp.findLargestMinDistance(boards, k);\n\nconsole.log(\"The answer is:\", ans);  // Expected: 60"
    }
  },
  "QBCategory": "CCD",
  "boilerPlates": {
    "C": "#include <stdio.h>\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "C++": "#include <iostream>\nusing namespace std;\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "Java": "import java.util.*;\nimport java.io.*;\n\npublic class Main {\n    public static void main(String[] args) throws IOException {\n        // your code goes here\n    }\n}",
    "Python3": "# your code goes here\n",
    "JavaScript": "// your code goes here\n"
  },
  "_internal": {
    "methodName": "",
    "params": []
  }
}