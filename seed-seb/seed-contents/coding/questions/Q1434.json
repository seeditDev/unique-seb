{
  "questionId": "Q1434",
  "title": "Count Array Triplets",
  "slug": "count-array-triplets",
  "version": 1,
  "createdAt": "2026-07-01",
  "updatedAt": "2026-07-04",
  "metadata": {
    "category": "Arrays",
    "difficulty": "Medium",
    "isPremium": false,
    "tags": [
      "Frequency Array",
      "Arrays",
      "Counting Triplets",
      "Two Sum"
    ],
    "companyTags": [],
    "topicTags": [
      "Two Pointers & Sliding Window"
    ]
  },
  "content": {
    "problemStatement": "Given an array of integers, count all triplets where sum of any two elements equals the third element. A triplet can have any arrangement of elements (i.e., sum of first two equals third, or sum of first and third equals second, etc.).\n\n\nInput Format\n\nFirst line contains TTT, the number of test cases\nFor each test case:\n\nFirst line contains NNN\nSecond line contains NNN space-separated integers\n\n\n\n\nOutput Format\n\nFor each test case, print the count of valid triplets.",
    "constraints": [
      "1 <= T <= 100",
      "1 <= N <= 1000",
      "1 <= ai <= 100000"
    ],
    "inputFormat": "The first line contains an integer T, the number of test cases. For each test case, the first line contains an integer N, the size of the array. The second line contains N space\u2011separated integers a1, a2, \u2026, aN.",
    "outputFormat": "For each test case output a single line containing the number of unordered triplets (i,\u202fj,\u202fk) with distinct indices such that the sum of any two of the three elements equals the third element.",
    "sampleTestCases": [
      {
        "id": "sample_1",
        "input": "3\n4\n1 5 3 2\n3\n2 3 4\n5\n1 2 3 4 5",
        "output": "2\n0\n4",
        "explanation": "Test case 1: Triplets are (1,2,3) and (2,3,5).\\nTest case 2: No two numbers sum to a third.\\nTest case 3: Valid triplets are (1,2,3), (1,3,4), (1,4,5) and (2,3,5)."
      },
      {
        "id": "sample_2",
        "input": "1\n3\n1 1 2",
        "output": "1",
        "explanation": "The only unordered pair of distinct indices is the two 1's; their sum is 2, which exists once, giving one triplet."
      },
      {
        "id": "sample_3",
        "input": "1\n4\n2 2 4 4",
        "output": "2",
        "explanation": "The pair of the two 2's sums to 4, and there are two distinct indices holding 4, so two different triplets are formed."
      }
    ]
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "label": "Case 1",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_2",
        "label": "Case 2",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_3",
        "label": "Case 3",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_4",
        "label": "Case 4",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_5",
        "label": "Case 5",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      }
    ],
    "totalTestCases": 5,
    "totalWeight": 10
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      2,
      2,
      2,
      2,
      2
    ],
    "maxScore": 10
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "For each test case we store the frequency of every value (the maximum value is 100000, so a simple array works). Then we iterate over all unordered pairs of indices (i < j). Let s = a[i] + a[j]. If s is within the allowed range we add freq[s] to the answer. Because all ai are positive, s is strictly larger than a[i] and a[j]; therefore any index counted in freq[s] is automatically different from i and j, guaranteeing distinct indices. Each valid unordered triplet has exactly one ordered pair (the two smaller numbers) that sums to the largest one, so it is counted exactly once. The algorithm runs in O(N^2) time per test case and uses O(MAXV) extra memory for the frequency array.",
    "timeComplexity": "O(N^2) per test case (N \u2264 1000, so at most 10^6 operations).",
    "spaceComplexity": "O(MAXV) = O(100000) for the frequency array.",
    "code": {
      "C": "#include <stdio.h>\n#include <string.h>\n\n#define MAXV 100000\n\nint main() {\n    int T; if (scanf(\"%d\", &T) != 1) return 0;\n    while (T--) {\n        int N; scanf(\"%d\", &N);\n        int a[1000];\n        static int freq[MAXV + 1];\n        memset(freq, 0, sizeof(freq));\n        for (int i = 0; i < N; ++i) {\n            scanf(\"%d\", &a[i]);\n            ++freq[a[i]];\n        }\n        long long ans = 0;\n        for (int i = 0; i < N; ++i) {\n            for (int j = i + 1; j < N; ++j) {\n                int sum = a[i] + a[j];\n                if (sum <= MAXV) ans += freq[sum];\n            }\n        }\n        printf(\"%lld\\n\", ans);\n    }\n    return 0;\n}",
      "C++": "#include <bits/stdc++.h>\nusing namespace std;\n\nint main() {\n    ios::sync_with_stdio(false);\n    cin.tie(nullptr);\n    int T; if (!(cin >> T)) return 0;\n    const int MAXV = 100000;\n    while (T--) {\n        int N; cin >> N;\n        vector<int> a(N);\n        vector<int> freq(MAXV + 1, 0);\n        for (int i = 0; i < N; ++i) {\n            cin >> a[i];\n            ++freq[a[i]];\n        }\n        long long ans = 0;\n        for (int i = 0; i < N; ++i) {\n            for (int j = i + 1; j < N; ++j) {\n                int sum = a[i] + a[j];\n                if (sum <= MAXV) ans += freq[sum];\n            }\n        }\n        cout << ans << '\\n';\n    }\n    return 0;\n}",
      "Java": "import java.io.*;\nimport java.util.*;\n\npublic class Main {\n    public static void main(String[] args) throws Exception {\n        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));\n        StringTokenizer st;\n        int T = Integer.parseInt(br.readLine().trim());\n        final int MAXV = 100000;\n        StringBuilder sb = new StringBuilder();\n        while (T-- > 0) {\n            int N = Integer.parseInt(br.readLine().trim());\n            int[] a = new int[N];\n            int[] freq = new int[MAXV + 1];\n            st = new StringTokenizer(br.readLine());\n            for (int i = 0; i < N; ++i) {\n                a[i] = Integer.parseInt(st.nextToken());\n                ++freq[a[i]];\n            }\n            long ans = 0L;\n            for (int i = 0; i < N; ++i) {\n                for (int j = i + 1; j < N; ++j) {\n                    int sum = a[i] + a[j];\n                    if (sum <= MAXV) ans += freq[sum];\n                }\n            }\n            sb.append(ans).append('\\n');\n        }\n        System.out.print(sb.toString());\n    }\n}",
      "Python3": "# COMPLETE, RUNNABLE Python3 code\nimport sys\n\ndef solve():\n    data = sys.stdin.read().strip().split()\n    if not data:\n        return\n    it = iter(data)\n    T = int(next(it))\n    MAXV = 100000\n    out = []\n    for _ in range(T):\n        N = int(next(it))\n        arr = [int(next(it)) for _ in range(N)]\n        freq = [0] * (MAXV + 1)\n        for v in arr:\n            freq[v] += 1\n        ans = 0\n        for i in range(N):\n            ai = arr[i]\n            for j in range(i + 1, N):\n                s = ai + arr[j]\n                if s <= MAXV:\n                    ans += freq[s]\n        out.append(str(ans))\n    sys.stdout.write('\\n'.join(out))\n\nif __name__ == \"__main__\":\n    solve()",
      "JavaScript": "// COMPLETE, RUNNABLE JavaScript code\nconst fs = require('fs');\nconst input = fs.readFileSync(0, 'utf-8').trim().split(/\\s+/);\nlet idx = 0;\nconst T = Number(input[idx++]);\nconst MAXV = 100000;\nlet results = [];\nfor (let t = 0; t < T; ++t) {\n  const N = Number(input[idx++]);\n  const arr = new Array(N);\n  const freq = new Array(MAXV + 1).fill(0);\n  for (let i = 0; i < N; ++i) {\n    arr[i] = Number(input[idx++]);\n    ++freq[arr[i]];\n  }\n  let ans = 0;\n  for (let i = 0; i < N; ++i) {\n    for (let j = i + 1; j < N; ++j) {\n      const sum = arr[i] + arr[j];\n      if (sum <= MAXV) ans += freq[sum];\n    }\n  }\n  results.push(String(ans));\n}\nconsole.log(results.join('\\n'));\n"
    }
  }
}