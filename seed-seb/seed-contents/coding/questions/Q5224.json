{
  "questionId": "Q5224",
  "title": "Generalized Spanning Trees",
  "slug": "generalized-spanning-trees",
  "version": 1,
  "createdAt": "2026-07-01",
  "updatedAt": "2026-07-04",
  "metadata": {
    "category": "Graph",
    "difficulty": "Hard",
    "isPremium": true,
    "tags": [
      "Generalized Spanning Trees",
      "Graph Theory",
      "Subset Sum"
    ],
    "companyTags": [],
    "topicTags": [
      "Flow & Matching"
    ],
    "QBCategory": "CCD"
  },
  "content": {
    "problemStatement": "A spanning tree of an undirected, connected graph (V,E) with vertex set V and edge set E is a subset of edges F such that the graph (V,F) is connected and has no cycles. After a few moments thought, one realizes that an equivalent way to say this is to say:\n\n\n\n   1) |F| = |V| - 1\n   2) For any non-empty subset of nodes S, the number of edges with **both** endpoints in S is does not exceed |S|-1.\n\n\n\nThere is a fairly simple algorithm that can determine if a set of edges F is indeed a spanning tree of a graph.  Simply check that |F| = |V| - 1 and that the graph (V,F) is connected using your favorite connectivity algorithm.\n\n\n\nAs in all topics in mathematics, the next step is to generalize what is known.  So, consider the following generalization of spanning trees.  Say a generalized spanning tree (GST) of a graph (V,E) is a function f from the set of edges E = {e1, e2, ..., eM} to the real interval [0,1] such that the following conditions hold:\n\n\n\n   1) f(e1) + f(e2) + ... + f(eM) = |V| - 1\n   2) For any non-empty subset of nodes S, the sum of the f(e) values for edges e with **both** endpoints in S does not exceed |S|-1.\n\n\n\nOne can see that this generalizes the notion of a spanning tree since spanning trees (in the classical sense) correspond to GSTs by setting f(e) = 1 if e is in the spanning tree and f(e) = 0 if e is not in the spanning tree.\n\n\n\nYour task is to determine if a given function is a GST of a given graph.\n\n\nInput:\nThe first line contains a single integer T (about 20) indicating the number of test cases to follow.\n\n\n\nEach test case begins with two integers N and M where N is the number of nodes and M is the number of edges. The following M lines describe the edges, one per line. Each line contains three values u, v, f where 1 &lt;= u, v &lt;= n are the endpoints of the edge and f is a rational number between 0 and 1 (inclusive).  This rational number is the value of the edge in the proposed GST.\n\n\n\nThe rational numbers are always presented as n/d (even if d=1) where both n and d are non-negative and have no common factors.\n\n\n\nThe input graphs have no parallel edges or loops and are, of course, undirected. Consecutive cases will be separated by a blank line (including a blank line preceding the first case).\n\n\n\nBounds: 2 &lt;= N &lt;= 50, 1 &lt;= M &lt;= 100, and no denominator of any fraction in the input exceeds 30.\n\n\nOutput:\nIf the function is a GST, you are to simply print a single line saying \"GST\".\n\n\n\nIf the first constraint (the sum of the values of all edges is N-1) is violated, then you should print a single line displaying just the number 1.\n\n\n\nOtherwise, if constraint 1) holds but constraint 2) is violated for some non-empty set S, you should print three lines. The first line simply contains the integer 2. The second contains the size of a non-empty \nsubset of the nodes whose corresponding constraint is violated. The third line lists the nodes (in any order) that are part of such a set with a violated constraint (the number of nodes is as reported in the second line).\n\n\n\nIf there are multiple non-empty sets whose constraints are violated, then any will do. Print a blank line after the output for each case.\n\n\nExample:Input:3\n3 3\r\n1 2 2/3\r\n2 3 2/3\r\n3 1 2/3\n\n\n3 3\r\n1 2 1/1\r\n2 3 1/1\r\n3 1 1/1\n\n\n4 6\r\n1 2 1/5\r\n1 3 1/5\r\n1 4 1/5\r\n2 3 4/5\r\n2 4 4/5\r\n3 4 4/5\r\n\n\n\n\nOutput:GST\n1\n\n\n2\r\n3\r\n2 3 4",
    "constraints": [
      "1 <= T <= 20",
      "2 <= N <= 50",
      "1 <= M <= 100",
      "No denominator of any fraction in the input exceeds 30"
    ],
    "inputFormat": "The first line contains a single integer T, the number of test cases. Each test case begins with two integers N and M where N is the number of nodes and M is the number of edges. The following M lines describe the edges, one per line. Each line contains three values u, v, f where 1 <= u, v <= n are the endpoints of the edge and f is a rational number between 0 and 1 (inclusive).",
    "outputFormat": "If the function is a GST, print a single line saying 'GST'. If the first constraint (the sum of the values of all edges is N-1) is violated, then print a single line displaying just the number 1. Otherwise, if constraint 1) holds but constraint 2) is violated for some non-empty set S, print three lines. The first line simply contains the integer 2. The second contains the size of a non-empty subset of the nodes whose corresponding constraint is violated. The third line lists the nodes (in any order) that are part of such a set with a violated constraint (the number of nodes is as reported in the second line).",
    "sampleTestCases": [
      {
        "id": "sample_1",
        "input": "3\n3 3\n1 2 2/3\n2 3 2/3\n3 1 2/3\n\n3 3\n1 2 1/1\n2 3 1/1\n3 1 1/1\n\n4 6\n1 2 1/5\n1 3 1/5\n1 4 1/5\n2 3 4/5\n2 4 4/5\n3 4 4/5",
        "output": "GST\n1\n2\n3\n2 3 4",
        "explanation": "The first test case is a valid GST, the second test case violates the first condition, and the third test case violates the second condition."
      },
      {
        "id": "sample_2",
        "input": "1\n2 1\n1 2 1/1",
        "output": "GST",
        "explanation": "This is a simple GST with two nodes and one edge."
      },
      {
        "id": "sample_3",
        "input": "1\n3 3\n1 2 1/1\n2 3 1/1\n3 1 1/1",
        "output": "1",
        "explanation": "This test case violates the first condition because the sum of the edge values is 3, not 2."
      }
    ]
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "label": "minimum boundary",
        "input": "1\n2 1\n1 2 1/1",
        "expectedOutput": "GST",
        "weight": 2
      },
      {
        "id": "tc_2",
        "label": "maximum boundary",
        "input": "1\n50 100\n1 2 1/1\n2 3 1/1\n... (100 edges)",
        "expectedOutput": "1",
        "weight": 2
      },
      {
        "id": "tc_3",
        "label": "uniform/edge case",
        "input": "1\n3 3\n1 2 1/1\n2 3 1/1\n3 1 1/1",
        "expectedOutput": "1",
        "weight": 2
      },
      {
        "id": "tc_4",
        "label": "tricky case",
        "input": "1\n4 6\n1 2 1/5\n1 3 1/5\n1 4 1/5\n2 3 4/5\n2 4 4/5\n3 4 4/5",
        "expectedOutput": "2\n3\n2 3 4",
        "weight": 2
      },
      {
        "id": "tc_5",
        "label": "stress/general",
        "input": "5\n... (5 test cases with varying inputs)",
        "expectedOutput": "... (5 outputs for each test case)",
        "weight": 2
      }
    ],
    "totalTestCases": 5,
    "totalWeight": 10
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      2,
      2,
      2,
      2,
      2
    ],
    "maxScore": 10
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "To solve this problem, we first check if the sum of the values of all edges is N-1. If not, we print 1. Otherwise, we check for each non-empty subset of nodes if the sum of the values of edges with both endpoints in the subset exceeds the size of the subset minus 1. If we find such a subset, we print 2, the size of the subset, and the nodes in the subset. If no such subset is found, we print 'GST'.",
    "timeComplexity": "O(T * 2^N * M) per test case, O(T * 2^N * M) total.",
    "spaceComplexity": "O(M) — for storing the edges.",
    "code": {
      "C": "#include <stdio.h>\n#include <stdlib.h>\n#include <string.h>\n#define MAX_N 50\n#define MAX_M 100\n#define MAX_DENOM 30\n\n// Function to calculate the greatest common divisor\nint gcd(int a, int b) {\n    if (b == 0) return a;\n    return gcd(b, a % b);\n}\n\n// Function to simplify a fraction\nvoid simplify(int *num, int *denom) {\n    int g = gcd(*num, *denom);\n    *num /= g;\n    *denom /= g;\n}\n\nint main() {\n    int T;\n    scanf(\"%d\", &T);\n    while (T--) {\n        int N, M;\n        scanf(\"%d %d\", &N, &M);\n        int edges[M][3];\n        for (int i = 0; i < M; i++) {\n            scanf(\"%d %d %d/%d\", &edges[i][0], &edges[i][1], &edges[i][2], &edges[i][3]);\n            simplify(&edges[i][2], &edges[i][3]);\n        }\n        // Check the first condition\n        int sum = 0;\n        for (int i = 0; i < M; i++) {\n            sum += edges[i][2];\n        }\n        if (sum != N - 1) {\n            printf(\"1\\n\");\n            continue;\n        }\n        // Check the second condition\n        for (int mask = 1; mask < (1 << N); mask++) {\n            int subset_sum = 0;\n            for (int i = 0; i < M; i++) {\n                if ((mask & (1 << (edges[i][0] - 1))) && (mask & (1 << (edges[i][1] - 1)))) {\n                    subset_sum += edges[i][2];\n                }\n            }\n            int subset_size = 0;\n            for (int i = 0; i < N; i++) {\n                if (mask & (1 << i)) subset_size++;\n            }\n            if (subset_sum > subset_size - 1) {\n                printf(\"2\\n\");\n                printf(\"%d\\n\", subset_size);\n                for (int i = 0; i < N; i++) {\n                    if (mask & (1 << i)) printf(\"%d \", i + 1);\n                }\n                printf(\"\\n\");\n                break;\n            }\n        }\n        if (mask == (1 << N)) printf(\"GST\\n\");\n    }\n    return 0;\n}",
      "C++": "#include <iostream>\n#include <vector>\nusing namespace std;\n#define MAX_N 50\n#define MAX_M 100\n#define MAX_DENOM 30\n\n// Function to calculate the greatest common divisor\nint gcd(int a, int b) {\n    if (b == 0) return a;\n    return gcd(b, a % b);\n}\n\n// Function to simplify a fraction\nvoid simplify(int &num, int &denom) {\n    int g = gcd(num, denom);\n    num /= g;\n    denom /= g;\n}\n\nint main() {\n    int T;\n    cin >> T;\n    while (T--) {\n        int N, M;\n        cin >> N >> M;\n        vector<vector<int>> edges(M, vector<int>(4));\n        for (int i = 0; i < M; i++) {\n            cin >> edges[i][0] >> edges[i][1] >> edges[i][2] >> edges[i][3];\n            simplify(edges[i][2], edges[i][3]);\n        }\n        // Check the first condition\n        int sum = 0;\n        for (int i = 0; i < M; i++) {\n            sum += edges[i][2];\n        }\n        if (sum != N - 1) {\n            cout << \"1\" << endl;\n            continue;\n        }\n        // Check the second condition\n        for (int mask = 1; mask < (1 << N); mask++) {\n            int subset_sum = 0;\n            for (int i = 0; i < M; i++) {\n                if ((mask & (1 << (edges[i][0] - 1))) && (mask & (1 << (edges[i][1] - 1)))) {\n                    subset_sum += edges[i][2];\n                }\n            }\n            int subset_size = 0;\n            for (int i = 0; i < N; i++) {\n                if (mask & (1 << i)) subset_size++;\n            }\n            if (subset_sum > subset_size - 1) {\n                cout << \"2\" << endl;\n                cout << subset_size << endl;\n                for (int i = 0; i < N; i++) {\n                    if (mask & (1 << i)) cout << i + 1 << \" \";\n                }\n                cout << endl;\n                break;\n            }\n        }\n        if (mask == (1 << N)) cout << \"GST\" << endl;\n    }\n    return 0;\n}",
      "Java": "import java.util.Scanner;\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int T = sc.nextInt();\n        while (T-- > 0) {\n            int N = sc.nextInt();\n            int M = sc.nextInt();\n            int[][] edges = new int[M][4];\n            for (int i = 0; i < M; i++) {\n                edges[i][0] = sc.nextInt();\n                edges[i][1] = sc.nextInt();\n                edges[i][2] = sc.nextInt();\n                edges[i][3] = sc.nextInt();\n                simplify(edges[i][2], edges[i][3]);\n            }\n            // Check the first condition\n            int sum = 0;\n            for (int i = 0; i < M; i++) {\n                sum += edges[i][2];\n            }\n            if (sum != N - 1) {\n                System.out.println(\"1\");\n                continue;\n            }\n            // Check the second condition\n            for (int mask = 1; mask < (1 << N); mask++) {\n                int subset_sum = 0;\n                for (int i = 0; i < M; i++) {\n                    if ((mask & (1 << (edges[i][0] - 1))) && (mask & (1 << (edges[i][1] - 1)))) {\n                        subset_sum += edges[i][2];\n                    }\n                }\n                int subset_size = 0;\n                for (int i = 0; i < N; i++) {\n                    if ((mask & (1 << i)) != 0) subset_size++;\n                }\n                if (subset_sum > subset_size - 1) {\n                    System.out.println(\"2\");\n                    System.out.println(subset_size);\n                    for (int i = 0; i < N; i++) {\n                        if ((mask & (1 << i)) != 0) System.out.print((i + 1) + \" \");\n                    }\n                    System.out.println();\n                    break;\n                }\n            }\n            if (mask == (1 << N)) System.out.println(\"GST\");\n        }\n    }\n    public static void simplify(int num, int denom) {\n        int g = gcd(num, denom);\n        num /= g;\n        denom /= g;\n    }\n    public static int gcd(int a, int b) {\n        if (b == 0) return a;\n        return gcd(b, a % b);\n    }\n}",
      "Python3": "import sys\ninput = sys.stdin.readline\n\ndef gcd(a, b):\n    if b == 0: return a\n    return gcd(b, a % b)\n\ndef simplify(num, denom):\n    g = gcd(num, denom)\n    return num // g, denom // g\n\nT = int(input())\nwhile T > 0:\n    N, M = map(int, input().split())\n    edges = [list(map(int, input().split())) for _ in range(M)]\n    for i in range(M):\n        edges[i][2], edges[i][3] = simplify(edges[i][2], edges[i][3])\n    # Check the first condition\n    sum_val = sum(edges[i][2] for i in range(M))\n    if sum_val != N - 1:\n        print(1)\n        T -= 1\n        continue\n    # Check the second condition\n    for mask in range(1, 1 << N):\n        subset_sum = 0\n        for i in range(M):\n            if (mask & (1 << (edges[i][0] - 1))) and (mask & (1 << (edges[i][1] - 1))):\n                subset_sum += edges[i][2]\n        subset_size = bin(mask).count('1')\n        if subset_sum > subset_size - 1:\n            print(2)\n            print(subset_size)\n            for i in range(N):\n                if mask & (1 << i): print(i + 1, end=' ')\n            print()\n            break\n    else:\n        print('GST')\n    T -= 1",
      "JavaScript": "const fs = require('fs');\nconst input = fs.readFileSync(0, 'utf-8').trim().split('\\n');\nlet idx = 0;\n\nfunction gcd(a, b) {\n    if (b === 0) return a;\n    return gcd(b, a % b);\n}\n\nfunction simplify(num, denom) {\n    const g = gcd(num, denom);\n    return [num / g, denom / g];\n}\n\nconst T = parseInt(input[idx++]);\nfor (let t = 0; t < T; t++) {\n    const [N, M] = input[idx++].split(' ').map(Number);\n    const edges = [];\n    for (let i = 0; i < M; i++) {\n        const [u, v, num, denom] = input[idx++].split(' ').map(Number);\n        const [simplifiedNum, simplifiedDenom] = simplify(num, denom);\n        edges.push([u, v, simplifiedNum, simplifiedDenom]);\n    }\n    // Check the first condition\n    let sum = 0;\n    for (let i = 0; i < M; i++) {\n        sum += edges[i][2];\n    }\n    if (sum !== N - 1) {\n        console.log(1);\n        continue;\n    }\n    // Check the second condition\n    for (let mask = 1; mask < (1 << N); mask++) {\n        let subsetSum = 0;\n        for (let i = 0; i < M; i++) {\n            if ((mask & (1 << (edges[i][0] - 1))) && (mask & (1 << (edges[i][1] - 1)))) {\n                subsetSum += edges[i][2];\n            }\n        }\n        let subsetSize = 0;\n        for (let i = 0; i < N; i++) {\n            if (mask & (1 << i)) subsetSize++;\n        }\n        if (subsetSum > subsetSize - 1) {\n            console.log(2);\n            console.log(subsetSize);\n            for (let i = 0; i < N; i++) {\n                if (mask & (1 << i)) process.stdout.write((i + 1) + ' ');\n            }\n            console.log();\n            break;\n        }\n    }\n    if (mask === (1 << N)) console.log('GST');\n}"
    }
  },
  "QBCategory": "CCD",
  "boilerPlates": {
    "C": "#include <stdio.h>\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "C++": "#include <iostream>\nusing namespace std;\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "Java": "import java.util.*;\nimport java.io.*;\n\npublic class Main {\n    public static void main(String[] args) throws IOException {\n        // your code goes here\n    }\n}",
    "Python3": "# your code goes here\n",
    "JavaScript": "// your code goes here\n"
  },
  "_internal": {
    "methodName": "",
    "params": []
  }
}