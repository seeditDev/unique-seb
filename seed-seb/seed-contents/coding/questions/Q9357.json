{
  "questionId": "Q9357",
  "title": "Dynamic Programming - Minimum Jumps",
  "slug": "dynamic-programming-minimum-jumps",
  "version": 1,
  "createdAt": "2026-07-02",
  "updatedAt": "2026-07-04",
  "metadata": {
    "category": "Greedy",
    "difficulty": "Medium",
    "isPremium": true,
    "tags": [
      "Array",
      "Greedy",
      "Minimum Jumps",
      "Jump Game"
    ],
    "companyTags": [],
    "topicTags": [
      "Greedy Algorithms"
    ],
    "QBCategory": "CCD"
  },
  "content": {
    "problemStatement": "Chef has an array AAA of size NNN.\n\nOn standing at the ithi^{th}ith index of the array (1≤i≤N)(1\\le i \\le N)(1≤i≤N), Chef can make a jump of length **less than or equal to** AiA_iAi​.\n\nIf Chef starts from the **first** index of the array, find the **minimum** number of jumps required by Chef to reach the **last** index of the array.\n\nIf it is impossible to reach the last index, print −1-1−1 instead.\n\n\nInput Format\n\nThe first line of input will contain a single integer TTT, denoting the number of test cases.\nEach test case consists of multiple lines of input.\n\nThe first line of each test case contains a single integer NNN — the number elements in the array.\nThe next line contains NNN space-separated integers - the elements of the array AAA.\n\n\n\n\nOutput Format\nFor each test case, output on a new line, the **minimum** number of jumps required by Chef to reach the **last** element of the array.\n\n\nIf it is impossible to reach the last index, print −1-1−1 instead.",
    "constraints": [
      "1 ≤ T ≤ 100",
      "1 ≤ N ≤ 100000",
      "0 ≤ Ai < N",
      "The sum of N over all test cases does not exceed 2·10^5"
    ],
    "inputFormat": "The first line contains an integer T, the number of test cases. For each test case:\n- The first line contains an integer N, the size of the array.\n- The second line contains N space‑separated integers A1, A2, …, AN.\nIndices are 1‑based. Chef starts at index 1 and wants to reach index N.",
    "outputFormat": "For each test case output a single line containing the minimum number of jumps required to reach the last index. If it is impossible, output -1.",
    "sampleTestCases": [
      {
        "id": "sample_1",
        "input": "4\n4\n1 1 2 2\n4\n1 2 0 2\n4\n2 1 0 2\n4\n0 2 0 2",
        "output": "3\n2\n-1\n-1",
        "explanation": "Explanation:\nTest case 111: Minimum number of moves required to reach the last index is 333:\n\nMove 111: Make a jump of length 1≤A11 \\le A_11≤A1​. Thus, Chef moves from index 111 to index 222.\nMove 222: Make a jump of length 1≤A21 \\le A_21≤A2​. Thus, Chef moves from index 222 to index 333.\nMove 333: Make a jump of length 1≤A31 \\le A_31≤A3​. Thus, Chef moves from index 333 to index 444.\n\nTest case 222: Minimum number of moves required to reach the last index is 222:\n\nMove 111: Make a jump of length 1≤A11 \\le A_11≤A1​. Thus, Chef moves from index 111 to index 222.\nMove 222: Make a jump of length 2≤A22 \\le A_22≤A2​. Thus, Chef moves from index 222 to index 444.\n\nTest case 333: It is not possible to reach the last index using any number of moves.\nTest case 444: It is not possible to reach the last index using any number of moves."
      }
    ]
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "label": "Case 1",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_2",
        "label": "Case 2",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_3",
        "label": "Case 3",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_4",
        "label": "Case 4",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_5",
        "label": "Case 5",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      }
    ],
    "totalTestCases": 5,
    "totalWeight": 10
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      2,
      2,
      2,
      2,
      2
    ],
    "maxScore": 10
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "The classic \"minimum jumps\" problem can be solved in O(N) using a greedy scan. While traversing the array we keep:\n- `farthest`: the farthest index reachable from any position within the current jump range.\n- `currentEnd`: the rightmost index that can be reached with the current number of jumps.\n- `jumps`: the number of jumps performed so far.\nIterate i from 1 to N‑1 (1‑based). At each i update `farthest = max(farthest, i + A[i])`. When i reaches `currentEnd` we must increase the jump count because we have exhausted the current range; set `currentEnd = farthest`. If at any point `currentEnd` does not move forward (i.e., `farthest` ≤ i) and we are not yet at the last index, the destination is unreachable. Edge cases: if N == 1 answer is 0; if the first element is 0 and N > 1 answer is -1.",
    "timeComplexity": "O(N) per test case, where N is the length of the array. The total over all test cases is O(ΣN) ≤ O(2·10^5).",
    "spaceComplexity": "O(1) extra space besides the input array.",
    "code": {
      "C": "#include <stdio.h>\n#include <stdlib.h>\n\nint main() {\n    int T; if (scanf(\"%d\", &T)!=1) return 0;\n    while (T--) {\n        int N; scanf(\"%d\", &N);\n        int *A = (int*)malloc((N+1)*sizeof(int));\n        for (int i = 1; i <= N; ++i) scanf(\"%d\", &A[i]);\n        if (N == 1) { printf(\"0\\n\"); free(A); continue; }\n        if (A[1] == 0) { printf(\"-1\\n\"); free(A); continue; }\n        int jumps = 0, currentEnd = 1, farthest = 1;\n        int possible = 1;\n        for (int i = 1; i < N; ++i) {\n            if (i + A[i] > farthest) farthest = i + A[i];\n            if (i == currentEnd) {\n                jumps++;\n                if (farthest <= i) { possible = 0; break; }\n                currentEnd = farthest;\n                if (currentEnd >= N) break;\n            }\n        }\n        if (!possible || currentEnd < N) printf(\"-1\\n\");\n        else printf(\"%d\\n\", jumps);\n        free(A);\n    }\n    return 0;\n}",
      "C++": "#include <bits/stdc++.h>\nusing namespace std;\n\nint main() {\n    ios::sync_with_stdio(false);\n    cin.tie(nullptr);\n    int T; if(!(cin>>T)) return 0;\n    while(T--) {\n        int N; cin>>N;\n        vector<int> A(N+1);\n        for(int i=1;i<=N;++i) cin>>A[i];\n        if(N==1){ cout<<0<<\"\\n\"; continue; }\n        if(A[1]==0){ cout<<-1<<\"\\n\"; continue; }\n        int jumps=0, currentEnd=1, farthest=1;\n        bool possible=true;\n        for(int i=1;i<N;++i){\n            farthest = max(farthest, i + A[i]);\n            if(i==currentEnd){\n                ++jumps;\n                if(farthest<=i){ possible=false; break; }\n                currentEnd = farthest;\n                if(currentEnd>=N) break;\n            }\n        }\n        if(!possible || currentEnd<N) cout<<-1<<\"\\n\";\n        else cout<<jumps<<\"\\n\";\n    }\n    return 0;\n}",
      "Java": "import java.io.*;\nimport java.util.*;\n\npublic class Main {\n    public static void main(String[] args) throws Exception {\n        FastScanner fs = new FastScanner(System.in);\n        int T = fs.nextInt();\n        StringBuilder sb = new StringBuilder();\n        while (T-- > 0) {\n            int N = fs.nextInt();\n            int[] A = new int[N + 1];\n            for (int i = 1; i <= N; i++) A[i] = fs.nextInt();\n            if (N == 1) {\n                sb.append(0).append('\\n');\n                continue;\n            }\n            if (A[1] == 0) {\n                sb.append(-1).append('\\n');\n                continue;\n            }\n            int jumps = 0, currentEnd = 1, farthest = 1;\n            boolean possible = true;\n            for (int i = 1; i < N; i++) {\n                farthest = Math.max(farthest, i + A[i]);\n                if (i == currentEnd) {\n                    jumps++;\n                    if (farthest <= i) { possible = false; break; }\n                    currentEnd = farthest;\n                    if (currentEnd >= N) break;\n                }\n            }\n            if (!possible || currentEnd < N) sb.append(-1).append('\\n');\n            else sb.append(jumps).append('\\n');\n        }\n        System.out.print(sb.toString());\n    }\n\n    // Fast scanner for competitive programming\n    static class FastScanner {\n        private final BufferedInputStream in;\n        private final byte[] buffer = new byte[1 << 16];\n        private int ptr = 0, len = 0;\n        FastScanner(InputStream stream) { in = new BufferedInputStream(stream); }\n        private int readByte() throws IOException {\n            if (ptr >= len) {\n                len = in.read(buffer);\n                ptr = 0;\n                if (len <= 0) return -1;\n            }\n            return buffer[ptr++];\n        }\n        int nextInt() throws IOException {\n            int c, sign = 1, val = 0;\n            do { c = readByte(); } while (c <= ' ');\n            if (c == '-') { sign = -1; c = readByte(); }\n            while (c > ' ') {\n                val = val * 10 + (c - '0');\n                c = readByte();\n            }\n            return val * sign;\n        }\n    }\n}",
      "Python3": "#!/usr/bin/env python3\nimport sys\n\ndef min_jumps(arr):\n    n = len(arr) - 1  # arr is 1‑based\n    if n == 1:\n        return 0\n    if arr[1] == 0:\n        return -1\n    jumps = 0\n    current_end = 1\n    farthest = 1\n    for i in range(1, n):\n        farthest = max(farthest, i + arr[i])\n        if i == current_end:\n            jumps += 1\n            if farthest <= i:\n                return -1\n            current_end = farthest\n            if current_end >= n:\n                break\n    return jumps if current_end >= n else -1\n\n\ndef main():\n    data = sys.stdin.read().strip().split()\n    if not data:\n        return\n    it = iter(data)\n    t = int(next(it))\n    out_lines = []\n    for _ in range(t):\n        n = int(next(it))\n        a = [0] + [int(next(it)) for _ in range(n)]\n        out_lines.append(str(min_jumps(a)))\n    sys.stdout.write('\\n'.join(out_lines))\n\nif __name__ == \"__main__\":\n    main()",
      "JavaScript": "const fs = require('fs');\nconst input = fs.readFileSync(0, 'utf-8').trim().split(/\\s+/).map(Number);\nlet idx = 0;\nconst T = input[idx++];\nlet outputs = [];\nfunction minJumps(arr) {\n  const n = arr.length - 1; // 1‑based\n  if (n === 1) return 0;\n  if (arr[1] === 0) return -1;\n  let jumps = 0, currentEnd = 1, farthest = 1;\n  for (let i = 1; i < n; i++) {\n    farthest = Math.max(farthest, i + arr[i]);\n    if (i === currentEnd) {\n      jumps++;\n      if (farthest <= i) return -1;\n      currentEnd = farthest;\n      if (currentEnd >= n) break;\n    }\n  }\n  return currentEnd >= n ? jumps : -1;\n}\nfor (let tc = 0; tc < T; tc++) {\n  const N = input[idx++];\n  const A = new Array(N + 1);\n  A[0] = 0; // unused\n  for (let i = 1; i <= N; i++) A[i] = input[idx++];\n  outputs.push(String(minJumps(A)));\n}\nconsole.log(outputs.join('\\n'));"
    }
  },
  "QBCategory": "CCD",
  "boilerPlates": {
    "C": "#include <stdio.h>\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "C++": "#include <iostream>\nusing namespace std;\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "Java": "import java.util.*;\nimport java.io.*;\n\npublic class Main {\n    public static void main(String[] args) throws IOException {\n        // your code goes here\n    }\n}",
    "Python3": "# your code goes here\n",
    "JavaScript": "// your code goes here\n"
  },
  "_internal": {
    "methodName": "",
    "params": []
  }
}