{
  "questionId": "Q11826",
  "title": "Most stones removed with same row or column",
  "slug": "most-stones-removed-with-same-row-or-column",
  "version": 1,
  "createdAt": "2026-07-05",
  "updatedAt": "2026-07-05",
  "metadata": {
    "category": "Graph",
    "difficulty": "Medium",
    "isPremium": true,
    "tags": [
      "Union Find",
      "Graph Theory"
    ],
    "companyTags": [],
    "topicTags": [],
    "QBCategory": "CCD"
  },
  "content": {
    "problemStatement": "There are n stones at some integer coordinate points on a 2D plane. Each coordinate point may have at most one stone.\n\nYou need to remove some stones. \n\nA stone can be removed if it shares either the same row or the same column as another stone that has not been removed.\n\nGiven an array of stones of length n where stones[i] = [xi, yi] represents the location of the ith stone, return the maximum possible number of stones that you can remove.",
    "constraints": [
      "1 <= stones.length <= 10000",
      "0 <= xi, yi <= 10000",
      "No two stones are at the same coordinate point."
    ],
    "inputFormat": "An array of stones where each stone is represented as an array of two integers [x, y] representing the coordinates of the stone.",
    "outputFormat": "The maximum possible number of stones that can be removed.",
    "sampleTestCases": []
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "label": "min boundary",
        "input": "[[0, 0]]",
        "expectedOutput": "0",
        "weight": 2
      },
      {
        "id": "tc_2",
        "label": "max boundary",
        "input": "[[0, 0], [0, 10000], [10000, 0], [10000, 10000]]",
        "expectedOutput": "4",
        "weight": 2
      },
      {
        "id": "tc_3",
        "label": "uniform/edge cases",
        "input": "[[0, 0], [0, 1], [1, 0], [1, 1]]",
        "expectedOutput": "4",
        "weight": 2
      },
      {
        "id": "tc_4",
        "label": "tricky case",
        "input": "[[0, 0], [0, 1], [1, 0], [1, 2], [2, 1], [2, 2]]",
        "expectedOutput": "5",
        "weight": 2
      },
      {
        "id": "tc_5",
        "label": "general mixed case",
        "input": "[[0, 0], [0, 2], [1, 1], [2, 0], [2, 2]]",
        "expectedOutput": "5",
        "weight": 2
      }
    ],
    "totalTestCases": 5,
    "totalWeight": 10
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      2,
      2,
      2,
      2,
      2
    ],
    "maxScore": 10
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "Use a Union Find data structure to group stones that are in the same row or column. Then, count the number of connected components and subtract it from the total number of stones to get the maximum possible number of stones that can be removed.",
    "timeComplexity": "O(n * alpha(n)) where n is the number of stones and alpha(n) is the inverse Ackermann function.",
    "spaceComplexity": "O(n)",
    "code": {
      "C": "#include <stdio.h>\n#include <stdlib.h>\n\n// Define the structure for a stone\ntypedef struct Stone {\n    int x;\n    int y;\n} Stone;\n\n// Define the structure for a Union Find data structure\ntypedef struct UnionFind {\n    int* parent;\n    int size;\n} UnionFind;\n\n// Function to create a new Union Find data structure\nUnionFind* createUnionFind(int size) {\n    UnionFind* uf = (UnionFind*) malloc(sizeof(UnionFind));\n    uf->parent = (int*) malloc(size * sizeof(int));\n    uf->size = size;\n    for (int i = 0; i < size; i++) {\n        uf->parent[i] = i;\n    }\n    return uf;\n}\n\n// Function to find the parent of a node in the Union Find data structure\nint find(UnionFind* uf, int x) {\n    if (uf->parent[x] != x) {\n        uf->parent[x] = find(uf, uf->parent[x]);\n    }\n    return uf->parent[x];\n}\n\n// Function to union two nodes in the Union Find data structure\nvoid unionNodes(UnionFind* uf, int x, int y) {\n    int rootX = find(uf, x);\n    int rootY = find(uf, y);\n    if (rootX != rootY) {\n        uf->parent[rootX] = rootY;\n    }\n}\n\n// Function to remove stones\nint removeStones(Stone* stones, int stonesSize) {\n    UnionFind* uf = createUnionFind(2 * stonesSize);\n    for (int i = 0; i < stonesSize; i++) {\n        unionNodes(uf, stones[i].x, stones[i].y + stonesSize);\n    }\n    int count = 0;\n    for (int i = 0; i < stonesSize; i++) {\n        if (find(uf, stones[i].x) == stones[i].x) {\n            count++;\n        }\n    }\n    return stonesSize - count;\n}\n\nint main() {\n    Stone stones[] = {{0, 0}, {0, 1}, {1, 0}, {1, 2}, {2, 1}, {2, 2}};\n    int stonesSize = sizeof(stones) / sizeof(stones[0]);\n    printf(\"%d\\n\", removeStones(stones, stonesSize));\n    return 0;\n}",
      "C++": "#include <bits/stdc++.h>\nusing namespace std;\n\nclass DSU {\npublic:\n    unordered_map<int, int> parent;\n\n    // Function to find the ultimate parent of node x\n    int find(int x) {\n        // If x is not present, make it its own parent\n        if (parent.find(x) == parent.end())\n            parent[x] = x;\n\n        // Path compression to optimize future finds\n        if (x != parent[x])\n            parent[x] = find(parent[x]);\n\n        return parent[x];\n    }\n\n    // Function to connect two nodes\n    void unite(int x, int y) {\n        // Set parent of x's root to y's root\n        parent[find(x)] = find(y);\n    }\n};\n\nclass Solution {\npublic:\n    int removeStones(vector<vector<int>>& stones) {\n        DSU dsu;\n\n        // Connect each stone's row and column (offset to avoid collision)\n        for (auto& stone : stones) {\n            dsu.unite(stone[0], stone[1] + 10001);\n        }\n\n        unordered_set<int> components;\n\n        // Count unique connected components\n        for (auto& stone : stones) {\n            components.insert(dsu.find(stone[0]));\n        }\n\n        // Max stones removed = total stones - number of components\n        return stones.size() - components.size();\n    }\n};\n\nint main() {\n    vector<vector<int>> stones = {\n        {0, 0}, {0, 1}, {1, 0}, {1, 2}, {2, 1}, {2, 2}\n    };\n\n    Solution obj;\n    cout << obj.removeStones(stones) << endl;\n    return 0;\n}",
      "Java": "import java.util.*;\n\nclass DSU {\n    Map<Integer, Integer> parent = new HashMap<>();\n\n    // Function to find the ultimate parent of x\n    public int find(int x) {\n        // If x is not present, make it its own parent\n        parent.putIfAbsent(x, x);\n\n        // Path compression\n        if (x != parent.get(x))\n            parent.put(x, find(parent.get(x)));\n\n        return parent.get(x);\n    }\n\n    // Function to connect two nodes\n    public void union(int x, int y) {\n        parent.put(find(x), find(y));\n    }\n}\n\nclass Solution {\n    public int removeStones(int[][] stones) {\n        DSU dsu = new DSU();\n\n        // Connect each stone's row and column (offset column by 10001)\n        for (int[] stone : stones) {\n            dsu.union(stone[0], stone[1] + 10001);\n        }\n\n        Set<Integer> components = new HashSet<>();\n\n        // Count unique connected components\n        for (int[] stone : stones) {\n            components.add(dsu.find(stone[0]));\n        }\n\n        // Max stones removed = total stones - number of components\n        return stones.length - components.size();\n    }\n}\n\nclass Main {\n    public static void main(String[] args) {\n        int[][] stones = {\n            {0, 0}, {0, 1}, {1, 0}, {1, 2}, {2, 1}, {2, 2}\n        };\n\n        Solution obj = new Solution();\n        System.out.println(obj.removeStones(stones));\n    }\n}",
      "Python3": "class DSU:\n    def __init__(self):\n        self.parent = {}\n\n    # Function to find the ultimate parent of x\n    def find(self, x):\n        # If x is not present, make it its own parent\n        if x not in self.parent:\n            self.parent[x] = x\n\n        # Path compression\n        if self.parent[x] != x:\n            self.parent[x] = self.find(self.parent[x])\n\n        return self.parent[x]\n\n    # Function to connect two nodes\n    def union(self, x, y):\n        self.parent[self.find(x)] = self.find(y)\n\nclass Solution:\n    def removeStones(self, stones):\n        dsu = DSU()\n\n        # Connect row and column for each stone\n        for x, y in stones:\n            dsu.union(x, y + 10001)\n\n        components = set()\n\n        # Count number of connected components\n        for x, y in stones:\n            components.add(dsu.find(x))\n\n        # Max stones removed = total stones - number of components\n        return len(stones) - len(components)\n\n# Driver code\nstones = [[0, 0], [0, 1], [1, 0], [1, 2], [2, 1], [2, 2]]\nobj = Solution()\nprint(obj.removeStones(stones))",
      "JavaScript": "class DSU {\n    constructor() {\n        this.parent = {};\n    }\n\n    // Function to find the ultimate parent of x\n    find(x) {\n        // If x is not present, make it its own parent\n        if (!(x in this.parent)) {\n            this.parent[x] = x;\n        }\n\n        // Path compression\n        if (this.parent[x] !== x) {\n            this.parent[x] = this.find(this.parent[x]);\n        }\n\n        return this.parent[x];\n    }\n\n    // Function to connect two nodes\n    union(x, y) {\n        this.parent[this.find(x)] = this.find(y);\n    }\n}\n\nclass Solution {\n    removeStones(stones) {\n        let dsu = new DSU();\n\n        // Connect row and column for each stone\n        for (let [x, y] of stones) {\n            dsu.union(x, y + 10001);\n        }\n\n        let components = new Set();\n\n        // Count number of connected components\n        for (let [x, y] of stones) {\n            components.add(dsu.find(x));\n        }\n\n        // Max stones removed = total stones - number of components\n        return stones.length - components.size;\n    }\n}\n\n// Driver code\nlet stones = [\n    [0, 0], [0, 1], [1, 0], [1, 2], [2, 1], [2, 2]\n];\nlet obj = new Solution();\nconsole.log(obj.removeStones(stones));"
    }
  },
  "QBCategory": "CCD",
  "boilerPlates": {
    "C": "#include <stdio.h>\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "C++": "#include <iostream>\nusing namespace std;\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "Java": "import java.util.*;\nimport java.io.*;\n\npublic class Main {\n    public static void main(String[] args) throws IOException {\n        // your code goes here\n    }\n}",
    "Python3": "# your code goes here\n",
    "JavaScript": "// your code goes here\n"
  },
  "_internal": {
    "methodName": "",
    "params": []
  }
}