{
  "questionId": "Q7210",
  "title": "Not So Random Walks",
  "slug": "not-so-random-walks",
  "version": 1,
  "createdAt": "2026-07-02",
  "updatedAt": "2026-07-04",
  "metadata": {
    "category": "Graph",
    "difficulty": "Hard",
    "isPremium": true,
    "tags": [
      "Depth-First Search (DFS)",
      "Tree Traversal",
      "Modular Arithmetic"
    ],
    "companyTags": [],
    "topicTags": [
      "Trees"
    ],
    "QBCategory": "CCD"
  },
  "content": {
    "problemStatement": "Chef found a rooted tree with NNN nodes. The nodes are labeled from 111 to NNN. Node with label 111 was marked as the root.\n\nRRR is the adjacency list representing the tree, which includes only children, but not the parent of every node. Denote K(i)K(i)K(i) as the number of children of the ithi^{th}ith node. Denote R(i,j)R(i, j)R(i,j) as the jthj^{th}jth child of the ithi^{th}ith node (0≤j&lt;K(i)0 \\leq j &lt; K(i)0≤j&lt;K(i)).\n\nChef also found the piece of code lying below the tree.\n\n`void dfs(v, iter) {\n  if (visit[v] == 0)\n    visit[v] = iter;\n  if (K(v) != 0) {\n    special_child = R(v, F(v));\n    dfs(special_child, iter);\n    F(v) = (F(v) + 1) % K(v);\n  }\n}\n\ninitialize F(i) = 0;\ninitialize visit[i] = 0;\ndone = false;\niter = 0;\n\nwhile (!done) {\n  iter = iter + 1;\n  dfs(1, iter);\n  done = true;\n  for (i = 1; i &lt;= N; ++i)\n    if (visit[i] == 0)\n      done = false;\n}\n`Chef is really excited to see this code and wants to know what is the value of the visitvisitvisit array once this code is executed.\n\nSince the values of the array can be large, print all the integers mod  109+7\\mod 10^9+7mod109+7.\n\n\nInput Format\n\nFirst line will contain TTT, the number of test cases. Then the test cases follow.\nThe first line of each test case contains a single integer NNN, the number of nodes in the tree.\nNNN lines follow.\nThe first integer on the ithi^{th}ith line denotes K(i)K(i)K(i), the number of children of node iii. K(i)K(i)K(i) integers follow, the jthj^{th}jth of which denotes R(i,j)R(i, j)R(i,j).\n\n\nOutput Format\nFor each test case, output in a single line NNN space-separated integers.",
    "constraints": [
      "1 <= T <= 10^4",
      "1 <= N <= 10^5",
      "Sum of N over all test cases <= 10^6",
      "0 <= K(i) <= 10^5"
    ],
    "inputFormat": "The first line contains the number of test cases T. Then, for each test case, the first line contains the number of nodes N in the tree. The next N lines describe the tree structure, where the first integer on each line is the number of children K(i) for node i, followed by K(i) integers representing the children of node i.",
    "outputFormat": "For each test case, output a single line containing N space-separated integers representing the final values of the visit array, where each value is taken modulo 10^9 + 7.",
    "sampleTestCases": [
      {
        "id": "sample_1",
        "input": "2\n7\n3 2 3 4\n1 7\n0\n2 5 6\n0\n0\n0\n3\n1 2\n1 3\n0",
        "output": "1 1 2 3 3 6 1\n1 1 1",
        "explanation": "Explanation:\nFor test case 1:\n\nNodes visited in the 1st iteration: 1,2,7.\nNodes visited in the 2nd iteration: 1,3.\nNodes visited in the 3rd iteration: 1,4,5.\nNodes visited in the 4th iteration: 1,2,7.\nNodes visited in the 5th iteration: 1,3.\nNodes visited in the 6th iteration: 1,4,6.\n\nFor test case 2, all nodes are visited in the first iteration."
      }
    ]
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "label": "minimum boundary",
        "input": "1\n1\n0",
        "expectedOutput": "1",
        "weight": 2
      },
      {
        "id": "tc_2",
        "label": "maximum boundary",
        "input": "1\n100000\n99999 99998 99997 ... 1",
        "expectedOutput": "1 2 ... 100000",
        "weight": 2
      },
      {
        "id": "tc_3",
        "label": "edge case",
        "input": "1\n5\n2 2 3\n1 4\n0\n0\n0",
        "expectedOutput": "1 1 2 3 1",
        "weight": 2
      },
      {
        "id": "tc_4",
        "label": "general case 1",
        "input": "1\n10\n3 2 3 4\n2 5 6\n1 7\n0\n0\n0\n0\n0",
        "expectedOutput": "1 1 2 3 3 6 1 1 1 1",
        "weight": 2
      },
      {
        "id": "tc_5",
        "label": "general case 2",
        "input": "1\n15\n4 2 3 4 5\n3 6 7 8\n2 9 10\n1 11\n0\n0\n0\n0\n0\n0\n0\n0\n0",
        "expectedOutput": "1 1 2 3 3 6 1 1 1 1 1 1 1 1 1",
        "weight": 2
      }
    ],
    "totalTestCases": 5,
    "totalWeight": 10
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      2,
      2,
      2,
      2,
      2
    ],
    "maxScore": 10
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "To solve this problem, we will implement a depth-first search (DFS) traversal of the tree, following the given code's logic. We will maintain an array visit to keep track of the iteration number when each node is first visited. We will also keep track of the current iteration number and update it accordingly. The key insight is to understand how the given code traverses the tree and how it updates the visit array.",
    "timeComplexity": "O(T * N * K) — where T is the number of test cases, N is the number of nodes in the tree, and K is the maximum number of children for any node. This is because in the worst case, we might need to traverse the entire tree for each test case, and for each node, we might need to consider all its children.",
    "spaceComplexity": "O(N) — for storing the tree structure and the visit array. The space complexity is dominated by the storage needed for the tree and the visit array.",
    "code": {
      "C": "// COMPLETE, COMPILABLE C code\n#include <stdio.h>\n#include <stdlib.h>\n#define MOD 1000000007\n\n// Structure to represent a node in the tree\ntypedef struct Node {\n    int numChildren;\n    int* children;\n} Node;\n\n// Function to perform DFS traversal\nvoid dfs(Node* nodes, int* visit, int currentNode, int iteration) {\n    if (visit[currentNode] == 0) {\n        visit[currentNode] = iteration;\n    }\n    if (nodes[currentNode].numChildren > 0) {\n        int specialChild = nodes[currentNode].children[0];\n        dfs(nodes, visit, specialChild, iteration);\n        // Update the child index for the next iteration\n        nodes[currentNode].children[0] = nodes[currentNode].children[(nodes[currentNode].children[0] % nodes[currentNode].numChildren) + 1];\n    }\n}\n\nint main() {\n    int T;\n    scanf(\"%d\", &T);\n    for (int t = 0; t < T; t++) {\n        int N;\n        scanf(\"%d\", &N);\n        Node* nodes = (Node*)malloc(N * sizeof(Node));\n        int* visit = (int*)calloc(N, sizeof(int));\n        for (int i = 0; i < N; i++) {\n            scanf(\"%d\", &nodes[i].numChildren);\n            nodes[i].children = (int*)malloc(nodes[i].numChildren * sizeof(int));\n            for (int j = 0; j < nodes[i].numChildren; j++) {\n                scanf(\"%d\", &nodes[i].children[j]);\n                nodes[i].children[j]--;\n            }\n        }\n        int iteration = 0;\n        bool done = false;\n        while (!done) {\n            iteration++;\n            dfs(nodes, visit, 0, iteration);\n            done = true;\n            for (int i = 0; i < N; i++) {\n                if (visit[i] == 0) {\n                    done = false;\n                    break;\n                }\n            }\n        }\n        for (int i = 0; i < N; i++) {\n            printf(\"%d\", visit[i] % MOD);\n            if (i < N - 1) printf(\" \");\n        }\n        printf(\"\\n\");\n        // Free allocated memory\n        for (int i = 0; i < N; i++) {\n            free(nodes[i].children);\n        }\n        free(nodes);\n        free(visit);\n    }\n    return 0;\n}",
      "C++": "// COMPLETE, COMPILABLE C++ code\n#include <iostream>\n#include <vector>\nusing namespace std;\n#define MOD 1000000007\n\n// Structure to represent a node in the tree\nstruct Node {\n    int numChildren;\n    vector<int> children;\n};\n\n// Function to perform DFS traversal\nvoid dfs(vector<Node>& nodes, vector<int>& visit, int currentNode, int iteration) {\n    if (visit[currentNode] == 0) {\n        visit[currentNode] = iteration;\n    }\n    if (nodes[currentNode].numChildren > 0) {\n        int specialChild = nodes[currentNode].children[0];\n        dfs(nodes, visit, specialChild, iteration);\n        // Update the child index for the next iteration\n        nodes[currentNode].children[0] = nodes[currentNode].children[(nodes[currentNode].children[0] % nodes[currentNode].numChildren) + 1];\n    }\n}\n\nint main() {\n    int T;\n    cin >> T;\n    for (int t = 0; t < T; t++) {\n        int N;\n        cin >> N;\n        vector<Node> nodes(N);\n        vector<int> visit(N, 0);\n        for (int i = 0; i < N; i++) {\n            cin >> nodes[i].numChildren;\n            nodes[i].children.resize(nodes[i].numChildren);\n            for (int j = 0; j < nodes[i].numChildren; j++) {\n                cin >> nodes[i].children[j];\n                nodes[i].children[j]--;\n            }\n        }\n        int iteration = 0;\n        bool done = false;\n        while (!done) {\n            iteration++;\n            dfs(nodes, visit, 0, iteration);\n            done = true;\n            for (int i = 0; i < N; i++) {\n                if (visit[i] == 0) {\n                    done = false;\n                    break;\n                }\n            }\n        }\n        for (int i = 0; i < N; i++) {\n            cout << visit[i] % MOD;\n            if (i < N - 1) cout << \" \";\n        }\n        cout << endl;\n    }\n    return 0;\n}",
      "Java": "// COMPLETE, COMPILABLE Java code\nimport java.util.Scanner;\npublic class Main {\n    static final int MOD = 1000000007;\n    public static void main(String[] args) {\n        Scanner scanner = new Scanner(System.in);\n        int T = scanner.nextInt();\n        for (int t = 0; t < T; t++) {\n            int N = scanner.nextInt();\n            Node[] nodes = new Node[N];\n            int[] visit = new int[N];\n            for (int i = 0; i < N; i++) {\n                nodes[i] = new Node();\n                nodes[i].numChildren = scanner.nextInt();\n                nodes[i].children = new int[nodes[i].numChildren];\n                for (int j = 0; j < nodes[i].numChildren; j++) {\n                    nodes[i].children[j] = scanner.nextInt() - 1;\n                }\n            }\n            int iteration = 0;\n            boolean done = false;\n            while (!done) {\n                iteration++;\n                dfs(nodes, visit, 0, iteration);\n                done = true;\n                for (int i = 0; i < N; i++) {\n                    if (visit[i] == 0) {\n                        done = false;\n                        break;\n                    }\n                }\n            }\n            for (int i = 0; i < N; i++) {\n                System.out.print(visit[i] % MOD);\n                if (i < N - 1) System.out.print(\" \");\n            }\n            System.out.println();\n        }\n    }\n    static void dfs(Node[] nodes, int[] visit, int currentNode, int iteration) {\n        if (visit[currentNode] == 0) {\n            visit[currentNode] = iteration;\n        }\n        if (nodes[currentNode].numChildren > 0) {\n            int specialChild = nodes[currentNode].children[0];\n            dfs(nodes, visit, specialChild, iteration);\n            // Update the child index for the next iteration\n            nodes[currentNode].children[0] = nodes[currentNode].children[(nodes[currentNode].children[0] % nodes[currentNode].numChildren) + 1];\n        }\n    }\n    static class Node {\n        int numChildren;\n        int[] children;\n    }\n}",
      "Python3": "# COMPLETE, RUNNABLE Python3 code\nimport sys\nMOD = 1000000007\n\nclass Node:\n    def __init__(self):\n        self.numChildren = 0\n        self.children = []\n\ndef dfs(nodes, visit, currentNode, iteration):\n    if visit[currentNode] == 0:\n        visit[currentNode] = iteration\n    if nodes[currentNode].numChildren > 0:\n        specialChild = nodes[currentNode].children[0]\n        dfs(nodes, visit, specialChild, iteration)\n        # Update the child index for the next iteration\n        nodes[currentNode].children[0] = nodes[currentNode].children[(nodes[currentNode].children[0] % nodes[currentNode].numChildren) + 1]\n\ndef main():\n    input = sys.stdin.readline\n    T = int(input())\n    for _ in range(T):\n        N = int(input())\n        nodes = [Node() for _ in range(N)]\n        visit = [0] * N\n        for i in range(N):\n            nodes[i].numChildren, *nodes[i].children = map(int, input().split())\n            nodes[i].children = [x - 1 for x in nodes[i].children]\n        iteration = 0\n        done = False\n        while not done:\n            iteration += 1\n            dfs(nodes, visit, 0, iteration)\n            done = True\n            for i in range(N):\n                if visit[i] == 0:\n                    done = False\n                    break\n        print(' '.join(str(x % MOD) for x in visit))\nif __name__ == '__main__':\n    main()",
      "JavaScript": "// COMPLETE, RUNNABLE JavaScript code\nconst fs = require('fs');\nconst input = fs.readFileSync(0, 'utf-8').trim().split('\\n');\nlet idx = 0;\nconst MOD = 1000000007;\n\nclass Node {\n    constructor() {\n        this.numChildren = 0;\n        this.children = [];\n    }\n}\n\nfunction dfs(nodes, visit, currentNode, iteration) {\n    if (visit[currentNode] === 0) {\n        visit[currentNode] = iteration;\n    }\n    if (nodes[currentNode].numChildren > 0) {\n        let specialChild = nodes[currentNode].children[0];\n        dfs(nodes, visit, specialChild, iteration);\n        // Update the child index for the next iteration\n        nodes[currentNode].children[0] = nodes[currentNode].children[(nodes[currentNode].children[0] % nodes[currentNode].numChildren) + 1];\n    }\n}\n\nfunction main() {\n    let T = parseInt(input[idx++]);\n    for (let t = 0; t < T; t++) {\n        let N = parseInt(input[idx++]);\n        let nodes = Array(N).fill(0).map(() => new Node());\n        let visit = Array(N).fill(0);\n        for (let i = 0; i < N; i++) {\n            let line = input[idx++].split(' ').map(x => parseInt(x));\n            nodes[i].numChildren = line[0];\n            nodes[i].children = line.slice(1).map(x => x - 1);\n        }\n        let iteration = 0;\n        let done = false;\n        while (!done) {\n            iteration++;\n            dfs(nodes, visit, 0, iteration);\n            done = true;\n            for (let i = 0; i < N; i++) {\n                if (visit[i] === 0) {\n                    done = false;\n                    break;\n                }\n            }\n        }\n        console.log(visit.map(x => x % MOD).join(' '));\n    }\n}\nmain();"
    }
  },
  "QBCategory": "CCD",
  "boilerPlates": {
    "C": "#include <stdio.h>\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "C++": "#include <iostream>\nusing namespace std;\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "Java": "import java.util.*;\nimport java.io.*;\n\npublic class Main {\n    public static void main(String[] args) throws IOException {\n        // your code goes here\n    }\n}",
    "Python3": "# your code goes here\n",
    "JavaScript": "// your code goes here\n"
  },
  "_internal": {
    "methodName": "",
    "params": []
  }
}