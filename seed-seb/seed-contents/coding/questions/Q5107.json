{
  "questionId": "Q5107",
  "title": "Song Playlist Manager",
  "slug": "song-playlist-manager",
  "version": 1,
  "createdAt": "2026-07-01",
  "updatedAt": "2026-07-04",
  "metadata": {
    "category": "Implementation",
    "difficulty": "Medium",
    "isPremium": true,
    "tags": [
      "Song Playlist Manager",
      "Class Methods"
    ],
    "companyTags": [],
    "topicTags": [
      "OOP in Python"
    ],
    "QBCategory": "CCD"
  },
  "content": {
    "problemStatement": "In this task, you will create a Song Playlist Manager using Python class methods. The Playlist class should have the following functionalities:\n\n\nAdd a song to the playlist\nRemove a song from the playlist\nGet the total duration of the playlist\nDisplay all songs in the playlist\nYou need to implement these functionalities using class methods. Class methods are methods that are bound to the class and not the instance of the class. They can be called on the class itself, rather than on an instance of the class.\n\nFor example, to add a song to the playlist, you might use:\nPlaylist.add_song(\"Song Title\", \"Artist Name\", 180)  # Duration in seconds\n\nYour task is to complete the Playlist class implementation using class methods.",
    "constraints": [
      "1 <= T <= 10",
      "Each playlist contains at most 100 commands",
      "Song title and artist are non-empty strings of length <= 100",
      "0 < duration <= 10^4"
    ],
    "inputFormat": "The first line contains an integer T, the number of test cases. Each test case starts with a sequence of commands, one command per line, terminated by the command 'exit'. The possible commands are: 'add', 'remove', 'display', 'duration', and 'exit'.",
    "outputFormat": "For each 'display' command, print the current playlist in the order songs were added. For each 'duration' command, print the total duration of all songs in seconds.",
    "sampleTestCases": [
      {
        "id": "sample_1",
        "input": "1\nadd\nSong 1;Artist 1;180\nadd\nSong 2;Artist 2;240\ndisplay\nduration\nremove\nSong 1\ndisplay\nduration\nexit",
        "output": "1. Song 1 by Artist 1 - 3:00\n2. Song 2 by Artist 2 - 4:00\n420\n1. Song 2 by Artist 2 - 4:00\n240",
        "explanation": "The first test case adds two songs to the playlist, displays the playlist, prints the total duration, removes the first song, displays the playlist again, and prints the total duration again."
      },
      {
        "id": "sample_2",
        "input": "1\nadd\nSong 1;Artist 1;300\nadd\nSong 2;Artist 2;300\ndisplay\nduration\nexit",
        "output": "1. Song 1 by Artist 1 - 5:00\n2. Song 2 by Artist 2 - 5:00\n600",
        "explanation": "The second test case adds two songs to the playlist, displays the playlist, and prints the total duration."
      },
      {
        "id": "sample_3",
        "input": "1\nadd\nSong 1;Artist 1;60\nremove\nSong 1\ndisplay\nduration\nexit",
        "output": "0\n0",
        "explanation": "The third test case adds a song to the playlist, removes the song, displays the playlist, and prints the total duration."
      }
    ]
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "label": "minimum boundary",
        "input": "1\nadd\nSong 1;Artist 1;1\ndisplay\nduration\nexit",
        "expectedOutput": "1. Song 1 by Artist 1 - 0:01\n1",
        "weight": 2
      },
      {
        "id": "tc_2",
        "label": "maximum boundary",
        "input": "1\nadd\nSong 1;Artist 1;10000\nadd\nSong 2;Artist 2;10000\ndisplay\nduration\nexit",
        "expectedOutput": "1. Song 1 by Artist 1 - 166:40\n2. Song 2 by Artist 2 - 166:40\n20000",
        "weight": 2
      },
      {
        "id": "tc_3",
        "label": "uniform/edge case",
        "input": "1\nadd\nSong 1;Artist 1;0\nadd\nSong 2;Artist 2;0\ndisplay\nduration\nexit",
        "expectedOutput": "1. Song 1 by Artist 1 - 0:00\n2. Song 2 by Artist 2 - 0:00\n0",
        "weight": 2
      },
      {
        "id": "tc_4",
        "label": "tricky case",
        "input": "1\nadd\nSong 1;Artist 1;60\nremove\nSong 1\nadd\nSong 2;Artist 2;60\ndisplay\nduration\nexit",
        "expectedOutput": "1. Song 2 by Artist 2 - 1:00\n60",
        "weight": 2
      },
      {
        "id": "tc_5",
        "label": "stress/general",
        "input": "2\nadd\nSong 1;Artist 1;60\nadd\nSong 2;Artist 2;60\ndisplay\nduration\nexit\nadd\nSong 3;Artist 3;60\nadd\nSong 4;Artist 4;60\ndisplay\nduration\nexit",
        "expectedOutput": "1. Song 1 by Artist 1 - 1:00\n2. Song 2 by Artist 2 - 1:00\n120\n1. Song 3 by Artist 3 - 1:00\n2. Song 4 by Artist 4 - 1:00\n120",
        "weight": 2
      }
    ],
    "totalTestCases": 5,
    "totalWeight": 10
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      2,
      2,
      2,
      2,
      2
    ],
    "maxScore": 10
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "The problem can be solved by implementing a Song Playlist Manager using class methods. The Playlist class should have methods to add a song to the playlist, remove a song from the playlist, get the total duration of the playlist, and display all songs in the playlist.",
    "timeComplexity": "O(T * N) where T is the number of test cases and N is the maximum number of commands in a test case.",
    "spaceComplexity": "O(N) where N is the maximum number of songs in a playlist.",
    "code": {
      "C": "#include <stdio.h>\n#include <string.h>\n#include <stdlib.h>\n\ntypedef struct {char title[105]; char artist[105]; int duration;} Song;\n\nstatic Song *songs = NULL;\nstatic int song_cnt = 0;\nstatic int song_cap = 0;\n\nvoid ensure_capacity(){\n    if(song_cnt>=song_cap){\n        song_cap = song_cap? song_cap*2 : 16;\n        songs = realloc(songs, song_cap*sizeof(Song));\n    }\n}\n\nvoid add_song(const char *title,const char *artist,int dur){\n    ensure_capacity();\n    strncpy(songs[song_cnt].title,title,104); songs[song_cnt].title[104]='\\0';\n    strncpy(songs[song_cnt].artist,artist,104); songs[song_cnt].artist[104]='\\0';\n    songs[song_cnt].duration=dur;\n    song_cnt++;\n}\n\nvoid remove_song(const char *title){\n    int i,j=0;\n    for(i=0;i<song_cnt;i++){\n        if(strcmp(songs[i].title,title)!=0){\n            if(i!=j){songs[j]=songs[i];}\n            j++;\n        }\n    }\n    song_cnt=j;\n}\n\nint total_duration(){\n    int sum=0,i;\n    for(i=0;i<song_cnt;i++) sum+=songs[i].duration;\n    return sum;\n}\n\nvoid display(){\n    int i;\n    for(i=0;i<song_cnt;i++){\n        int m = songs[i].duration/60;\n        int s = songs[i].duration%60;\n        printf(\"%d. %s by %s - %d:%02d\\n\", i+1, songs[i].title, songs[i].artist, m, s);\n    }\n}\n\nvoid clear_playlist(){\n    song_cnt=0;\n}\n\nint main(){\n    char line[256];\n    int T;\n    if(!fgets(line, sizeof(line), stdin)) return 0;\n    T = atoi(line);\n    while(T--){\n        while(fgets(line, sizeof(line), stdin)){\n            line[strcspn(line,\"\\n\")]=0; // strip newline\n            if(strcmp(line,\"exit\")==0) break;\n            if(strcmp(line,\"add\")==0){\n                char data[256];\n                fgets(data,sizeof(data),stdin);\n                data[strcspn(data,\"\\n\")]=0;\n                char *title=strtok(data,\";\");\n                char *artist=strtok(NULL,\";\");\n                char *durstr=strtok(NULL,\";\");\n                add_song(title,artist,atoi(durstr));\n            } else if(strcmp(line,\"remove\")==0){\n                char title[105];\n                fgets(title,sizeof(title),stdin);\n                title[strcspn(title,\"\\n\")]=0;\n                remove_song(title);\n            } else if(strcmp(line,\"duration\")==0){\n                printf(\"%d\\n\", total_duration());\n            } else if(strcmp(line,\"display\")==0){\n                display();\n            }\n        }\n        clear_playlist();\n    }\n    free(songs);\n    return 0;\n}\n",
      "C++": "#include <bits/stdc++.h>\nusing namespace std;\n\nstruct Song{string title,artist;int duration;};\nstatic vector<Song> songs;\n\nvoid add_song(const string& t,const string& a,int d){songs.push_back({t,a,d});}\nvoid remove_song(const string& t){\n    vector<Song> tmp;tmp.reserve(songs.size());\n    for(const auto& s:songs) if(s.title!=t) tmp.push_back(s);\n    songs.swap(tmp);\n}\nint total_duration(){int sum=0;for(const auto& s:songs) sum+=s.duration;return sum;}\nvoid display(){\n    for(size_t i=0;i<songs.size();++i){\n        int m=songs[i].duration/60, s=songs[i].duration%60;\n        cout<<i+1<<\". \"<<songs[i].title<<\" by \"<<songs[i].artist<<\" - \"<<m<<\":\"<<setw(2)<<setfill('0')<<s<<\"\\n\";\n    }\n}\nvoid clear_playlist(){songs.clear();}\n\nint main(){\n    ios::sync_with_stdio(false);\n    cin.tie(nullptr);\n    int T; if(!(cin>>T)) return 0; string cmd; getline(cin,cmd); // consume endline\n    while(T--){\n        while(true){\n            if(!getline(cin,cmd)) break;\n            if(cmd==\"exit\") break;\n            if(cmd==\"add\"){\n                string line; getline(cin,line);\n                size_t p1=line.find(';');\n                size_t p2=line.find(';',p1+1);\n                string title=line.substr(0,p1);\n                string artist=line.substr(p1+1,p2-p1-1);\n                int dur=stoi(line.substr(p2+1));\n                add_song(title,artist,dur);\n            } else if(cmd==\"remove\"){\n                string title; getline(cin,title);\n                remove_song(title);\n            } else if(cmd==\"duration\"){\n                cout<<total_duration()<<\"\\n\";\n            } else if(cmd==\"display\"){\n                display();\n            }\n        }\n        clear_playlist();\n    }\n    return 0;\n}\n",
      "Java": "import java.io.*;\nimport java.util.*;\n\npublic class Main {\n    static class Song {\n        String title, artist; int duration;\n        Song(String t,String a,int d){title=t;artist=a;duration=d;}\n    }\n    static List<Song> songs = new ArrayList<>();\n    static void addSong(String t,String a,int d){songs.add(new Song(t,a,d));}\n    static void removeSong(String t){\n        songs.removeIf(s->s.title.equals(t));\n    }\n    static int totalDuration(){\n        int sum=0; for(Song s: songs) sum+=s.duration; return sum;\n    }\n    static void display(PrintWriter out){\n        for(int i=0;i<songs.size();i++){\n            Song s=songs.get(i);\n            int m=s.duration/60, sec=s.duration%60;\n            out.printf(\"%d. %s by %s - %d:%02d\\n\", i+1, s.title, s.artist, m, sec);\n        }\n    }\n    static void clear(){songs.clear();}\n    public static void main(String[] args) throws Exception {\n        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));\n        PrintWriter out = new PrintWriter(new BufferedWriter(new OutputStreamWriter(System.out)));\n        int T = Integer.parseInt(br.readLine().trim());\n        while(T-- > 0){\n            String line;\n            while((line = br.readLine()) != null){\n                line = line.trim();\n                if(line.equals(\"exit\")) break;\n                switch(line){\n                    case \"add\":\n                        String data = br.readLine();\n                        String[] parts = data.split(\";\");\n                        addSong(parts[0], parts[1], Integer.parseInt(parts[2]));\n                        break;\n                    case \"remove\":\n                        String title = br.readLine();\n                        removeSong(title);\n                        break;\n                    case \"duration\":\n                        out.println(totalDuration());\n                        break;\n                    case \"display\":\n                        display(out);\n                        break;\n                }\n            }\n            clear();\n        }\n        out.flush();\n    }\n}\n",
      "Python3": "import sys\nclass Playlist:\n    songs = []\n    @classmethod\n    def add_song(cls, title, artist, duration):\n        cls.songs.append({\"title\": title, \"artist\": artist, \"duration\": duration})\n    @classmethod\n    def remove_song(cls, title):\n        cls.songs = [s for s in cls.songs if s[\"title\"] != title]\n    @classmethod\n    def get_total_duration(cls):\n        return sum(s[\"duration\"] for s in cls.songs)\n    @classmethod\n    def display_playlist(cls, out):\n        for i, s in enumerate(cls.songs, 1):\n            m, sec = divmod(s[\"duration\"], 60)\n            out.append(f\"{i}. {s['title']} by {s['artist']} - {m}:{sec:02d}\")\n    @classmethod\n    def clear(cls):\n        cls.songs.clear()\n\ndef main():\n    data = sys.stdin.read().splitlines()\n    if not data:\n        return\n    idx = 0\n    T = int(data[idx].strip()); idx += 1\n    out = []\n    for _ in range(T):\n        while idx < len(data):\n            cmd = data[idx].strip(); idx += 1\n            if cmd == \"exit\":\n                break\n            if cmd == \"add\":\n                title, artist, dur = data[idx].strip().split(';'); idx += 1\n                Playlist.add_song(title, artist, int(dur))\n            elif cmd == \"remove\":\n                title = data[idx].strip(); idx += 1\n                Playlist.remove_song(title)\n            elif cmd == \"duration\":\n                out.append(str(Playlist.get_total_duration()))\n            elif cmd == \"display\":\n                Playlist.display_playlist(out)\n        Playlist.clear()\n    sys.stdout.write(\"\\n\".join(out))\nif __name__ == \"__main__\":\n    main()\n",
      "JavaScript": "const fs = require('fs');\nconst lines = fs.readFileSync(0,'utf-8').trim().split(/\\n/);\nlet idx = 0;\nconst T = parseInt(lines[idx++].trim(),10);\nlet output = [];\nclass Playlist {\n  static songs = [];\n  static addSong(title, artist, duration){\n    this.songs.push({title, artist, duration});\n  }\n  static removeSong(title){\n    this.songs = this.songs.filter(s=>s.title!==title);\n  }\n  static totalDuration(){\n    return this.songs.reduce((a,s)=>a+s.duration,0);\n  }\n  static display(){\n    this.songs.forEach((s,i)=>{\n      const m = Math.floor(s.duration/60);\n      const sec = s.duration%60;\n      output.push(`${i+1}. ${s.title} by ${s.artist} - ${m}:${sec.toString().padStart(2,'0')}`);\n    });\n  }\n  static clear(){ this.songs = []; }\n}\nfor(let t=0;t<T;t++){\n  while(idx < lines.length){\n    const cmd = lines[idx++].trim();\n    if(cmd === 'exit') break;\n    if(cmd === 'add'){\n      const [title,artist,dur] = lines[idx++].trim().split(';');\n      Playlist.addSong(title,artist,parseInt(dur,10));\n    } else if(cmd === 'remove'){\n      const title = lines[idx++].trim();\n      Playlist.removeSong(title);\n    } else if(cmd === 'duration'){\n      output.push(String(Playlist.totalDuration()));\n    } else if(cmd === 'display'){\n      Playlist.display();\n    }\n  }\n  Playlist.clear();\n}\nconsole.log(output.join('\\n'));\n"
    }
  },
  "QBCategory": "CCD",
  "boilerPlates": {
    "C": "#include <stdio.h>\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "C++": "#include <iostream>\nusing namespace std;\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "Java": "import java.util.*;\nimport java.io.*;\n\npublic class Main {\n    public static void main(String[] args) throws IOException {\n        // your code goes here\n    }\n}",
    "Python3": "# your code goes here\n",
    "JavaScript": "// your code goes here\n"
  },
  "_internal": {
    "methodName": "",
    "params": []
  }
}