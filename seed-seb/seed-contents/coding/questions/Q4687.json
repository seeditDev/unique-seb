{
  "questionId": "Q4687",
  "title": "Group Anagrams",
  "slug": "group-anagrams",
  "version": 1,
  "createdAt": "2026-07-01",
  "updatedAt": "2026-07-04",
  "metadata": {
    "category": "Strings",
    "difficulty": "Medium",
    "isPremium": true,
    "tags": [
      "Hashing",
      "Sorting"
    ],
    "companyTags": [],
    "topicTags": [
      "Hashing"
    ],
    "QBCategory": "CCD"
  },
  "content": {
    "problemStatement": "You are given NNN strings. Your task is to group the strings that are anagrams of each other.\nComplete the given function.\n\n**Note**: An anagram is a string formed by rearranging the letters of a another string, using all the original letters exactly once. \n\nFor example, **\"listen\"** and **\"silent\"** are anagrams of each other because they contain the same letters.\n\n\nInput Format\n\nThe first line of input contains an integer NNN — the number of strings.\nThe next NNN lines contain strings.\n\n\nOutput Format\n\nComplete the function which return the vector of string vectors. each string vector are group of anagram strings.\nYou can return the groups of anagrams in any order. You will get a text **Nicely Grouped!** if grouping is done correctly, otherwise **WA**.",
    "constraints": [
      "1 <= N <= 100000",
      "All strings are of lowercase latin letters.",
      "Size of each string is at most 10"
    ],
    "inputFormat": "First line contains N, the number of strings. Each of the next N lines contains a string.",
    "outputFormat": "For each test case, return a vector of string vectors where each string vector is a group of anagram strings.",
    "sampleTestCases": [
      {
        "id": "sample_1",
        "input": "5\nabc\ndba\nacb\ncba\nbda",
        "output": "[['abc', 'acb', 'cba'], ['bda', 'dba']]",
        "explanation": "The anagram groups are ['abc', 'acb', 'cba'] and ['bda', 'dba']"
      },
      {
        "id": "sample_2",
        "input": "3\nlisten\nsilent\nenlist",
        "output": "[['listen', 'silent', 'enlist']]",
        "explanation": "The anagram group is ['listen', 'silent', 'enlist']"
      },
      {
        "id": "sample_3",
        "input": "2\nhello\nworld",
        "output": "[['hello'], ['world']]",
        "explanation": "The anagram groups are ['hello'] and ['world']"
      }
    ]
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "label": "minimum boundary",
        "input": "1\na",
        "expectedOutput": "[['a']]",
        "weight": 2
      },
      {
        "id": "tc_2",
        "label": "maximum boundary",
        "input": "100000\na\na\na\na\na",
        "expectedOutput": "[['a', 'a', 'a', 'a', 'a']]",
        "weight": 2
      },
      {
        "id": "tc_3",
        "label": "uniform/edge case",
        "input": "5\naaaa\naaaa\naaaa\naaaa\naaaa",
        "expectedOutput": "[['aaaa', 'aaaa', 'aaaa', 'aaaa', 'aaaa']]",
        "weight": 2
      },
      {
        "id": "tc_4",
        "label": "tricky case",
        "input": "5\nabc\nbca\ncab\nbac\ncba",
        "expectedOutput": "[['abc', 'bca', 'cab', 'bac', 'cba']]",
        "weight": 2
      },
      {
        "id": "tc_5",
        "label": "stress/general",
        "input": "100\na\nb\nc\nd\ne",
        "expectedOutput": "[['a'], ['b'], ['c'], ['d'], ['e']]",
        "weight": 2
      }
    ],
    "totalTestCases": 5,
    "totalWeight": 10
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      2,
      2,
      2,
      2,
      2
    ],
    "maxScore": 10
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "The key insight is to use a hash map where the sorted version of each string is the key and the value is a list of strings that are anagrams of the key. This approach works because anagrams will have the same characters when sorted. The time complexity is O(NMlogM) where N is the number of strings and M is the maximum length of a string, due to the sorting operation. The space complexity is O(NM) for storing the strings in the hash map.",
    "timeComplexity": "O(NMlogM) per test case, O(TNMlogM) total.",
    "spaceComplexity": "O(NM) — the dominant structure is the hash map.",
    "code": {
      "C": "#include <stdio.h>\n#include <stdlib.h>\n#include <string.h>\n#include <stdbool.h>\n#include <assert.h>\n\n#define MAX 11\n\ntypedef struct {\n    char **strings;\n    int size;\n    int capacity;\n} AnagramGroup;\n\ntypedef struct Node {\n    char *key;\n    AnagramGroup group;\n    struct Node *next;\n} HashNode;\n\ntypedef struct {\n    HashNode **table;\n    int size;\n} HashMap;\n\nunsigned long hash(const char *str) {\n    unsigned long hash = 5381;\n    int c;\n    while ((c = *str++)) {\n        hash = ((hash << 5) + hash) + c;  // hash * 33 + c\n    }\n    return hash;\n}\n\nHashMap* createHashMap(int size) {\n    HashMap *map = (HashMap *)malloc(sizeof(HashMap));\n    map->table = (HashNode **)calloc(size, sizeof(HashNode *));\n    map->size = size;\n    return map;\n}\n\nvoid insertHashMap(HashMap *map, const char *key, const char *originalStr) {\n    unsigned long idx = hash(key) % map->size;\n    HashNode *node = map->table[idx];\n    \n    while (node) {\n        if (strcmp(node->key, key) == 0) {\n            if (node->group.size == node->group.capacity) {\n                node->group.capacity *= 2;\n                node->group.strings = (char **)realloc(node->group.strings, node->group.capacity * sizeof(char *));\n            }\n            node->group.strings[node->group.size++] = strdup(originalStr);\n            return;\n        }\n        node = node->next;\n    }\n\n    node = (HashNode *)malloc(sizeof(HashNode));\n    node->key = strdup(key);\n    node->group.strings = (char **)malloc(10 * sizeof(char *));\n    node->group.capacity = 10;\n    node->group.size = 1;\n    node->group.strings[0] = strdup(originalStr);\n    node->next = map->table[idx];\n    map->table[idx] = node;\n}\n\nAnagramGroup* getAnagramGroups(HashMap *map, int *groupCount) {\n    int count = 0;\n    for (int i = 0; i < map->size; i++) {\n        HashNode *node = map->table[i];\n        while (node) {\n            count++;\n            node = node->next;\n        }\n    }\n\n    AnagramGroup *groups = (AnagramGroup *)malloc(count * sizeof(AnagramGroup));\n    int idx = 0;\n    for (int i = 0; i < map->size; i++) {\n        HashNode *node = map->table[i];\n        while (node) {\n            groups[idx++] = node->group;\n            node = node->next;\n        }\n    }\n\n    *groupCount = count;\n    return groups;\n}\n\nvoid freeHashMap(HashMap *map) {\n    for (int i = 0; i < map->size; i++) {\n        HashNode *node = map->table[i];\n        while (node) {\n            free(node->key);\n            for (int j = 0; j < node->group.size; j++) {\n                free(node->group.strings[j]);\n            }\n            free(node->group.strings);\n            HashNode *temp = node;\n            node = node->next;\n            free(temp);\n        }\n    }\n    free(map->table);\n    free(map);\n}\n\nvoid sortString(char *str) {\n    int len = strlen(str);\n    qsort(str, len, sizeof(char), (int (*)(const void *, const void *))strcmp);\n}\n\nint compareStrings(const void *a, const void *b) {\n    return strcmp(*(const char **)a, *(const char **)b);\n}\n\nbool checkAnagram(char **group, int size) {\n    char sorted[MAX];\n    sortString(strcpy(sorted, group[0]));\n    for (int i = 1; i < size; i++) {\n        char temp[MAX];\n        sortString(strcpy(temp, group[i]));\n        if (strcmp(sorted, temp) != 0) {\n            return false;\n        }\n    }\n    return true;\n}\n\nint main() {\n    int n;\n    scanf(\"%d\", &n);\n    assert(1 <= n && n <= 100000);\n\n    char **strings = (char **)malloc(n * sizeof(char *));\n    for (int i = 0; i < n; i++) {\n        strings[i] = (char *)malloc(MAX * sizeof(char));\n        scanf(\"%s\", strings[i]);\n        int sz = strlen(strings[i]);\n        assert(sz >= 1 && sz <= 10);\n    }\n\n    HashMap *map = createHashMap(n);\n    \n    // Group the anagrams using a hash map\n    for (int i = 0; i < n; i++) {\n        char sorted[MAX];\n        strcpy(sorted, strings[i]);\n        sortString(sorted);\n        insertHashMap(map, sorted, strings[i]);\n    }\n\n    // Collect anagram groups\n    int groupCount;\n    AnagramGroup *anagramGroups = getAnagramGroups(map, &groupCount);\n\n    char **anagramGroupsStrings = (char **)malloc(n * sizeof(char *));\n    int idx = 0;\n    for (int i = 0; i < groupCount; i++) {\n        assert(checkAnagram(anagramGroups[i].strings, anagramGroups[i].size) == true);\n        for (int j = 0; j < anagramGroups[i].size; j++) {\n            anagramGroupsStrings[idx++] = anagramGroups[i].strings[j];\n        }\n    }\n\n    qsort(strings, n, sizeof(char *), compareStrings);\n    qsort(anagramGroupsStrings, n, sizeof(char *), compareStrings);\n\n    for (int i = 0; i < n; i++) {\n        assert(strcmp(strings[i], anagramGroupsStrings[i]) == 0);\n    }\n\n    printf(\"Nicely Grouped!\\n\");\n\n    // Free allocated memory\n    for (int i = 0; i < n; i++) {\n        free(strings[i]);\n    }\n    free(strings);\n    free(anagramGroupsStrings);\n    freeHashMap(map);\n\n    return 0;\n}",
      "C++": "#include <bits/stdc++.h>\nusing namespace std;\n\nclass Solution {\n  public:\n    vector<vector<string> > groupAnagrams(const vector<string>& strings){\n        unordered_map<string, vector<string>> map;\n  \n        for (auto str : strings) {\n            string key = str;\n            sort(key.begin(), key.end());  \n            map[key].push_back(str);\n        }\n        \n        vector<vector<string>> result;\n        \n        for(const auto& pair : map) {\n            result.push_back(pair.second);\n        }\n        \n        return result; \n    }\n};\n",
      "Java": "import java.util.*;\n\nclass Main {\n    public List<List<String>> groupAnagrams(String[] strings) {\n        Map<String, List<String>> map = new HashMap<>();\n        for (String str : strings) {\n            char[] charArray = str.toCharArray();\n            Arrays.sort(charArray);\n            String key = new String(charArray);\n            map.computeIfAbsent(key, k -> new ArrayList<>()).add(str);\n        }\n        return new ArrayList<>(map.values());\n    }\n}\n",
      "Python3": "from collections import defaultdict\n\ndef group_anagrams(strings):\n    anagram_map = defaultdict(list)\n    for s in strings:\n        key = ''.join(sorted(s))\n        anagram_map[key].append(s)\n    return list(anagram_map.values())\n\n",
      "JavaScript": "const fs = require('fs');\nconst lines = fs.readFileSync(0, 'utf-8').trim().split('\\n');\nlet idx = 0;\n\nfunction groupAnagrams(strings) {\n    let map = {};\n    for (let str of strings) {\n        let key = str.split('').sort().join('');\n        if (!map[key]) {\n            map[key] = [];\n        }\n        map[key].push(str);\n    }\n    return Object.values(map);\n}\n\nfunction checkAnagram(group) {\n    let sortedForm = group[0].split('').sort().join('');\n    return group.every(str => str.split('').sort().join('') === sortedForm);\n}\n\nlet n = parseInt(lines[idx++]);\nlet strings = lines.slice(idx, idx + n);\nidx += n;\n\nif (n < 1 || n > 100000) throw new Error('Invalid n');\nfor (let str of strings) {\n    if (str.length < 1 || str.length > 10) throw new Error('Invalid string length');\n}\n\nlet anagramGroups = groupAnagrams(strings);\nlet anagramGroupsStrings = [];\nfor (let group of anagramGroups) {\n    if (!checkAnagram(group)) throw new Error('Invalid anagram group');\n    anagramGroupsStrings.push(...group);\n}\n\nstrings.sort();\nanagramGroupsStrings.sort();\n\nif (JSON.stringify(strings) !== JSON.stringify(anagramGroupsStrings)) throw new Error('Strings do not match');\n\nconsole.log('Nicely Grouped!');\n"
    }
  },
  "QBCategory": "CCD",
  "boilerPlates": {
    "C": "#include <stdio.h>\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "C++": "#include <iostream>\nusing namespace std;\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "Java": "import java.util.*;\nimport java.io.*;\n\npublic class Main {\n    public static void main(String[] args) throws IOException {\n        // your code goes here\n    }\n}",
    "Python3": "# your code goes here\n",
    "JavaScript": "// your code goes here\n"
  },
  "_internal": {
    "methodName": "",
    "params": []
  }
}