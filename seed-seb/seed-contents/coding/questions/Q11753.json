{
  "questionId": "Q11753",
  "title": "Count partitions with given difference",
  "slug": "countpartitionswithgivendifference",
  "version": 1,
  "createdAt": "2026-07-05",
  "updatedAt": "2026-07-05",
  "metadata": {
    "category": "Dynamic Programming",
    "difficulty": "Hard",
    "isPremium": true,
    "tags": [
      "Partition Problem",
      "Subset Sum Problem"
    ],
    "companyTags": [],
    "topicTags": [],
    "QBCategory": "CCD"
  },
  "content": {
    "problemStatement": "**Problem Statement :** Given an array with N positive integers and an integer D, count the number of ways we can partition the given array into two subsets, S1 and S2 such that S1 - S2 = D and S1 is always greater than or equal to S2.\n\n\n\n\n\n\n    \n        \n            **\n                    Examples\n                **\n            \n            \n                \n            \n        </summar",
    "constraints": [
      "1 <= N <= 10^5",
      "1 <= arr[i] <= 10^9",
      "0 <= D <= 10^9"
    ],
    "inputFormat": "The first line contains an integer N, the number of elements in the array. The second line contains N space-separated integers, the elements of the array. The third line contains an integer D, the target difference.",
    "outputFormat": "The number of ways to partition the array into two subsets with the given difference.",
    "sampleTestCases": []
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "label": "min boundary",
        "input": "1\n1\n0",
        "expectedOutput": "1",
        "weight": 2
      },
      {
        "id": "tc_2",
        "label": "max boundary",
        "input": "5\n1000000000 1000000000 1000000000 1000000000 1000000000\n0",
        "expectedOutput": "1",
        "weight": 2
      },
      {
        "id": "tc_3",
        "label": "uniform case",
        "input": "5\n1 1 1 1 1\n0",
        "expectedOutput": "16",
        "weight": 2
      },
      {
        "id": "tc_4",
        "label": "tricky case",
        "input": "5\n1 2 3 4 5\n0",
        "expectedOutput": "2",
        "weight": 2
      },
      {
        "id": "tc_5",
        "label": "mixed case",
        "input": "5\n1 2 3 4 10\n3",
        "expectedOutput": "2",
        "weight": 2
      }
    ],
    "totalTestCases": 5,
    "totalWeight": 10
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      2,
      2,
      2,
      2,
      2
    ],
    "maxScore": 10
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "Dynamic Programming",
    "timeComplexity": "O(N*K)",
    "spaceComplexity": "O(K)",
    "code": {
      "C": "#include <stdio.h>\nint countPartitions(int* arr, int arrSize, int d) {\n    int totalSum = 0;\n    for (int i = 0; i < arrSize; i++) totalSum += arr[i];\n    if ((totalSum + d) % 2 != 0 || d > totalSum) return 0;\n    int K = (totalSum + d) / 2;\n    int* dp = (int*)malloc((K + 1) * sizeof(int));\n    for (int i = 0; i <= K; i++) dp[i] = 0;\n    dp[0] = 1;\n    for (int i = 0; i < arrSize; i++) {\n        int* curr = (int*)malloc((K + 1) * sizeof(int));\n        for (int j = 0; j <= K; j++) curr[j] = 0;\n        curr[0] = 1;\n        for (int t = 0; t <= K; t++) {\n            int notTake = dp[t];\n            int take = 0;\n            if (arr[i] <= t) take = dp[t - arr[i]];\n            curr[t] = take + notTake;\n        }\n        for (int j = 0; j <= K; j++) dp[j] = curr[j];\n        free(curr);\n    }\n    int result = dp[K];\n    free(dp);\n    return result;\n}\nint main() {\n    int arr[] = {1, 2, 3, 4};\n    int d = 1;\n    printf(\"%d\\n\", countPartitions(arr, 4, d));\n    return 0;\n}",
      "C++": "#include <bits/stdc++.h>\nusing namespace std;\nclass Solution {\npublic:\n    // Function to count partitions with given difference\n    int countPartitions(vector<int>& arr, int d) {\n        // Calculate total sum of array\n        int totalSum = accumulate(arr.begin(), arr.end(), 0);\n\n        // Check if solution is possible\n        if ((totalSum + d) % 2 != 0 || d > totalSum) return 0;\n\n        // Calculate target sum\n        int K = (totalSum + d) / 2;\n\n        // Create dp array of size K+1\n        vector<int> dp(K + 1, 0);\n\n        // Base case: empty set makes sum 0\n        dp[0] = 1;\n\n        // If first element <= K, mark it\n        if (arr[0] <= K) dp[arr[0]] += 1;\n\n        // Process remaining elements\n        for (int i = 1; i < arr.size(); i++) {\n            vector<int> curr(K + 1, 0);\n            curr[0] = 1;\n\n            for (int t = 0; t <= K; t++) {\n                int notTake = dp[t];\n                int take = 0;\n                if (arr[i] <= t) {\n                    take = dp[t - arr[i]];\n                }\n                curr[t] = take + notTake;\n            }\n            dp = curr;\n        }\n        return dp[K];\n    }\n};\n\nint main() {\n    Solution sol;\n    vector<int> arr = {1, 2, 3, 4};\n    int d = 1;\n    cout << sol.countPartitions(arr, d) << endl;\n    return 0;\n}",
      "Java": "import java.util.*;\n\nclass Solution {\n    // Function to count partitions with given difference\n    public int countPartitions(int[] arr, int d) {\n        // Calculate total sum of array\n        int totalSum = 0;\n        for (int num : arr) totalSum += num;\n\n        // Check if solution is possible\n        if ((totalSum + d) % 2 != 0 || d > totalSum) return 0;\n\n        // Calculate target sum\n        int K = (totalSum + d) / 2;\n\n        // Create dp array of size K+1\n        int[] dp = new int[K + 1];\n        dp[0] = 1;\n\n        // If first element <= K, mark it\n        if (arr[0] <= K) dp[arr[0]] += 1;\n\n        // Process remaining elements\n        for (int i = 1; i < arr.length; i++) {\n            int[] curr = new int[K + 1];\n            curr[0] = 1;\n\n            for (int t = 0; t <= K; t++) {\n                int notTake = dp[t];\n                int take = 0;\n                if (arr[i] <= t) {\n                    take = dp[t - arr[i]];\n                }\n                curr[t] = take + notTake;\n            }\n            dp = curr;\n        }\n        return dp[K];\n    }\n}\n\npublic class Main {\n    public static void main(String[] args) {\n        Solution sol = new Solution();\n        int[] arr = {1, 2, 3, 4};\n        int d = 1;\n        System.out.println(sol.countPartitions(arr, d));\n    }\n}",
      "Python3": "class Solution:\n    # Function to count partitions with given difference\n    def countPartitions(self, arr, d):\n        # Calculate total sum of array\n        totalSum = sum(arr)\n\n        # Check if solution is possible\n        if (totalSum + d) % 2 != 0 or d > totalSum:\n            return 0\n\n        # Calculate target sum\n        K = (totalSum + d) // 2\n\n        # Create dp array of size K+1\n        dp = [0] * (K + 1)\n        dp[0] = 1\n\n        # If first element <= K, mark it\n        if arr[0] <= K:\n            dp[arr[0]] += 1\n\n        # Process remaining elements\n        for i in range(1, len(arr)):\n            curr = [0] * (K + 1)\n            curr[0] = 1\n            for t in range(K + 1):\n                notTake = dp[t]\n                take = 0\n                if arr[i] <= t:\n                    take = dp[t - arr[i]]\n                curr[t] = take + notTake\n            dp = curr\n\n        return dp[K]\n\n\nif __name__ == \"__main__\":\n    sol = Solution()\n    arr = [1, 2, 3, 4]\n    d = 1\n    print(sol.countPartitions(arr, d))",
      "JavaScript": "class Solution {\n    // Function to count partitions with given difference\n    countPartitions(arr, d) {\n        // Calculate total sum of array\n        let totalSum = arr.reduce((a, b) => a + b, 0);\n\n        // Check if solution is possible\n        if ((totalSum + d) % 2 !== 0 || d > totalSum) return 0;\n\n        // Calculate target sum\n        let K = (totalSum + d) / 2;\n\n        // Create dp array of size K+1\n        let dp = new Array(K + 1).fill(0);\n        dp[0] = 1;\n\n        // If first element <= K, mark it\n        if (arr[0] <= K) dp[arr[0]] += 1;\n\n        // Process remaining elements\n        for (let i = 1; i < arr.length; i++) {\n            let curr = new Array(K + 1).fill(0);\n            curr[0] = 1;\n\n            for (let t = 0; t <= K; t++) {\n                let notTake = dp[t];\n                let take = 0;\n                if (arr[i] <= t) {\n                    take = dp[t - arr[i]];\n                }\n                curr[t] = take + notTake;\n            }\n            dp = curr;\n        }\n\n        return dp[K];\n    }\n}\n\n// Driver code\nlet sol = new Solution();\nlet arr = [1, 2, 3, 4];\nlet d = 1;\nconsole.log(sol.countPartitions(arr, d));"
    }
  },
  "QBCategory": "CCD",
  "boilerPlates": {
    "C": "#include <stdio.h>\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "C++": "#include <iostream>\nusing namespace std;\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "Java": "import java.util.*;\nimport java.io.*;\n\npublic class Main {\n    public static void main(String[] args) throws IOException {\n        // your code goes here\n    }\n}",
    "Python3": "# your code goes here\n",
    "JavaScript": "// your code goes here\n"
  },
  "_internal": {
    "methodName": "",
    "params": []
  }
}