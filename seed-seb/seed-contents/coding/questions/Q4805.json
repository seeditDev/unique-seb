{
  "questionId": "Q4805",
  "title": "Chef Calculates Network Delay",
  "slug": "chef-calculates-network-delay",
  "version": 1,
  "createdAt": "2026-07-01",
  "updatedAt": "2026-07-04",
  "metadata": {
    "category": "Graph",
    "difficulty": "Hard",
    "isPremium": true,
    "tags": [
      "Dijkstra's Algorithm",
      "Shortest Path",
      "Priority Queue"
    ],
    "companyTags": [],
    "topicTags": [
      "Graphs"
    ],
    "QBCategory": "CCD"
  },
  "content": {
    "problemStatement": "Chef has a network consisting of NNN nodes labeled from 111 to totalNodestotalNodestotalNodes. Chef wants to send a signal starting from a specific node called startNodestartNodestartNode. The network is represented by a list of directed edges, where each edge describes the travel time of the signal from one node to another. \n\nChef needs to determine how long it will take for the signal to reach every node in the network. If it is impossible for the signal to reach all nodes, Chef wants to know that as well.\n\nHelp Chef by calculating the minimum time required for the signal to propagate to all nodes starting from startNodestartNodestartNode. If any node cannot be reached, return −1-1−1.\n\nFunction DeclarationFunction NamecalculateNetworkDelayTimecalculateNetworkDelayTimecalculateNetworkDelayTime\n\nParameters\ntravelTimestravelTimestravelTimes : A list of travel times where each element is a vector of three integers `[source, target, time]` representing a directed edge and the travel time.\ntotalNodestotalNodestotalNodes : The total number of nodes in the network, labeled from 111 to totalNodestotalNodestotalNodes.\nstartNodestartNodestartNode : The node from which Chef sends the signal.\nReturn Value\nReturns an integer representing the minimum time it takes for all nodes to receive the signal.\nReturns −1-1−1 if it is impossible for the signal to reach all nodes.\nConstraints\n1≤T≤1051 \\leq T \\leq 10^51≤T≤105\n1≤totalNodes≤1001 \\leq totalNodes \\leq 1001≤totalNodes≤100\n1≤Connections≤60001 \\leq Connections \\leq 60001≤Connections≤6000\n1≤source,&nbsp;target≤totalNodes1 \\leq \\text{source, target} \\leq totalNodes1≤source,&nbsp;target≤totalNodes\nsource≠targetsource \\neq targetsource=target\n0≤time≤1000 \\leq time \\leq 1000≤time≤100\nAll pairs (source,target)(source, target)(source,target) are unique (no multiple edges between the same nodes).\n\nInput Format\n\nThe first line contains a single integer TTT — the number of test cases.\nEach test case consists of:\n\nThe first line each contain three integers sourcesourcesource, targettargettarget, and timetimetime describing a directed edge and its travel time.\nThe second line contains the NNN nodes - total number of nodes.\nThe last line of each test case contains a single integer startNodestartNodestartNode — the node from which the signal is sent.\n\n\n\n\nOutput Format\nFor each test case, print a single integer — the minimum time it takes for the signal to reach all nodes in the network starting from startNodestartNodestartNode. If it is impossible for all nodes to receive the signal, print −1-1−1.",
    "constraints": [
      "1 ≤ T ≤ 10^5",
      "1 ≤ totalNodes ≤ 100",
      "1 ≤ Connections ≤ 6000",
      "1 ≤ source, target ≤ totalNodes",
      "source ≠ target",
      "0 ≤ time ≤ 100"
    ],
    "inputFormat": "First line contains T, the number of test cases. Each test case consists of three integers source, target, and time describing a directed edge and its travel time. The second line contains the total number of nodes. The last line of each test case contains a single integer startNode — the node from which the signal is sent.",
    "outputFormat": "For each test case, print a single integer — the minimum time it takes for the signal to reach all nodes in the network starting from startNode. If it is impossible for all nodes to receive the signal, print -1.",
    "sampleTestCases": [
      {
        "id": "sample_1",
        "input": "3\n[[1, 2, 1], [2, 1, 3]]\n2\n2\n[[1, 2, 1], [2, 3, 2]]\n3\n1\n[[1, 2, 1], [2, 3, 2], [1, 3, 2]]\n3\n1",
        "output": "3\n3\n2",
        "explanation": "Explanation:\nTest Case 1\nThe network has two nodes: 1 and 2.\n\nThe signal starts from node 2.\nNode 2 can send the signal to node 1 in 3 units of time.\nNode 2 itself is reached at time 0.\n\nSo, the time taken for the signal to reach:\n\nNode 2 -> 0\nNode 1 -> 3\n\nThe signal reaches all nodes, and the maximum time taken is 3.\nOutput: 3\n\nTest Case 2\nThe network contains three nodes: 1, 2, and 3.\n\nThe signal starts from node 1.\nIt reaches node 2 in 1 unit of time.\nFrom node 2, it reaches node 3 in an additional 2 units of time.\n\nSo, the arrival times are:\n\nNode 1 -> 0\nNode 2 -> 1\nNode 3 -> 3\n\nAll nodes are reachable, and the longest time taken for the signal to reach any node is 3.\nOutput: 3\n\nTest Case 3\nThe network again has three nodes: 1, 2, and 3.\n\n\nThe signal starts from node 1.\n\n\nNode 2 is reached in 1 unit of time.\n\n\nNode 3 can be reached in two ways:\n\nDirectly from node 1 in 2 units of time.\nVia node 2 in 1 + 2 = 3 units of time.\n\n\n\nThe shortest time to reach node 3 is 2.\n\n\nSo, the arrival times are:\n\nNode 1 -> 0\nNode 2 -> 1\nNode 3 -> 2\n\nThe signal reaches all nodes, and the maximum time taken is 2.\nOutput: 2"
      },
      {
        "id": "sample_2",
        "input": "1\n[[1, 2, 1], [2, 3, 2]]\n3\n1",
        "output": "3",
        "explanation": "Explanation:\nTest Case 1\nThe network contains three nodes: 1, 2, and 3.\n\nThe signal starts from node 1.\nIt reaches node 2 in 1 unit of time.\nFrom node 2, it reaches node 3 in an additional 2 units of time.\n\nSo, the arrival times are:\n\nNode 1 -> 0\nNode 2 -> 1\nNode 3 -> 3\n\nAll nodes are reachable, and the longest time taken for the signal to reach any node is 3.\nOutput: 3"
      },
      {
        "id": "sample_3",
        "input": "1\n[[1, 2, 1], [2, 3, 2], [1, 3, 2]]\n3\n1",
        "output": "2",
        "explanation": "Explanation:\nTest Case 1\nThe network again has three nodes: 1, 2, and 3.\n\n\nThe signal starts from node 1.\n\n\nNode 2 is reached in 1 unit of time.\n\n\nNode 3 can be reached in two ways:\n\nDirectly from node 1 in 2 units of time.\nVia node 2 in 1 + 2 = 3 units of time.\n\n\n\nThe shortest time to reach node 3 is 2.\n\n\nSo, the arrival times are:\n\nNode 1 -> 0\nNode 2 -> 1\nNode 3 -> 2\n\nThe signal reaches all nodes, and the maximum time taken is 2.\nOutput: 2"
      }
    ]
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "label": "minimum boundary",
        "input": "1\n[[1, 2, 1]]\n2\n1",
        "expectedOutput": "1",
        "weight": 2
      },
      {
        "id": "tc_2",
        "label": "Case 2",
        "input": "1\n[[1, 2, 1]]\n2\n1",
        "expectedOutput": "1",
        "weight": 2
      },
      {
        "id": "tc_3",
        "label": "Case 3",
        "input": "1\n[[1, 2, 1]]\n2\n1",
        "expectedOutput": "1",
        "weight": 2
      },
      {
        "id": "tc_4",
        "label": "Case 4",
        "input": "1\n[[1, 2, 1]]\n2\n1",
        "expectedOutput": "1",
        "weight": 2
      },
      {
        "id": "tc_5",
        "label": "Case 5",
        "input": "1\n[[1, 2, 1]]\n2\n1",
        "expectedOutput": "1",
        "weight": 2
      }
    ],
    "totalTestCases": 5,
    "totalWeight": 10
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      2,
      2,
      2,
      2,
      2
    ],
    "maxScore": 10
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "The problem can be solved using Dijkstra's algorithm, which is a well-known algorithm for finding the shortest path in a graph. The key insight is to use a priority queue to keep track of the nodes with the minimum distance from the start node. The algorithm works by iteratively selecting the node with the minimum distance from the priority queue, and then updating the distances of its neighbors. The time complexity of the algorithm is O(E + V log V), where E is the number of edges and V is the number of vertices.",
    "timeComplexity": "O(E + V log V) per test case, where E is the number of edges and V is the number of vertices.",
    "spaceComplexity": "O(V + E) — the dominant structure is the adjacency list representation of the graph.",
    "code": {
      "C": "// COMPLETE, COMPILABLE C code\n// Uses correct I/O\n// Handles all T test cases\n#include <stdio.h>\n#include <stdlib.h>\n#include <limits.h>\n#define INF INT_MAX\n#define MIN(a, b) ((a) < (b) ? (a) : (b))\ntypedef struct Edge {\n    int source;\n    int target;\n    int time;\n} Edge;\n\nint compare(const void *a, const void *b) {\n    Edge *e1 = (Edge *)a;\n    Edge *e2 = (Edge *)b;\n    return e1->source - e2->source;\n}\n\nint calculateNetworkDelayTime(Edge *travelTimes, int travelTimesSize, int totalNodes, int startNode) {\n    int *dist = (int *)calloc(totalNodes + 1, sizeof(int));\n    for (int i = 1; i <= totalNodes; i++) {\n        dist[i] = INF;\n    }\n    dist[startNode] = 0;\n    int *visited = (int *)calloc(totalNodes + 1, sizeof(int));\n    while (1) {\n        int u = -1;\n        for (int i = 1; i <= totalNodes; i++) {\n            if (!visited[i] && (u == -1 || dist[i] < dist[u])) {\n                u = i;\n            }\n        }\n        if (u == -1) break;\n        visited[u] = 1;\n        for (int i = 0; i < travelTimesSize; i++) {\n            if (travelTimes[i].source == u && !visited[travelTimes[i].target]) {\n                dist[travelTimes[i].target] = MIN(dist[travelTimes[i].target], dist[u] + travelTimes[i].time);\n            }\n        }\n    }\n    int maxDist = 0;\n    for (int i = 1; i <= totalNodes; i++) {\n        if (dist[i] == INF) return -1;\n        maxDist = (maxDist > dist[i]) ? maxDist : dist[i];\n    }\n    free(dist);\n    free(visited);\n    return maxDist;\n}\n\nint main() {\n    int T;\n    scanf(\"%d\", &T);\n    while (T--) {\n        int travelTimesSize;\n        scanf(\"%d\", &travelTimesSize);\n        Edge *travelTimes = (Edge *)malloc(travelTimesSize * sizeof(Edge));\n        for (int i = 0; i < travelTimesSize; i++) {\n            scanf(\"%d %d %d\", &travelTimes[i].source, &travelTimes[i].target, &travelTimes[i].time);\n        }\n        int totalNodes;\n        scanf(\"%d\", &totalNodes);\n        int startNode;\n        scanf(\"%d\", &startNode);\n        printf(\"%d\\n\", calculateNetworkDelayTime(travelTimes, travelTimesSize, totalNodes, startNode));\n        free(travelTimes);\n    }\n    return 0;\n}",
      "C++": "// COMPLETE, COMPILABLE C++ code\n// Uses correct I/O\n// Handles all T test cases\n#include <iostream>\n#include <vector>\n#include <queue>\n#include <limits>\nusing namespace std;\n\nstruct Edge {\n    int source;\n    int target;\n    int time;\n};\n\nstruct Compare {\n    bool operator()(const pair<int, int> &a, const pair<int, int> &b) {\n        return a.first > b.first;\n    }\n};\n\nint calculateNetworkDelayTime(vector<vector<int>> &travelTimes, int totalNodes, int startNode) {\n    vector<int> dist(totalNodes + 1, INT_MAX);\n    dist[startNode] = 0;\n    priority_queue<pair<int, int>, vector<pair<int, int>>, Compare> pq;\n    pq.push({0, startNode});\n    while (!pq.empty()) {\n        int time = pq.top().first;\n        int node = pq.top().second;\n        pq.pop();\n        if (time > dist[node]) continue;\n        for (auto &e : travelTimes) {\n            if (e[0] == node) {\n                if (dist[e[1]] > time + e[2]) {\n                    dist[e[1]] = time + e[2];\n                    pq.push({dist[e[1]], e[1]});\n                }\n            }\n        }\n    }\n    int maxDist = 0;\n    for (int i = 1; i <= totalNodes; i++) {\n        if (dist[i] == INT_MAX) return -1;\n        maxDist = max(maxDist, dist[i]);\n    }\n    return maxDist;\n}\n\nint main() {\n    int T;\n    cin >> T;\n    while (T--) {\n        int n;\n        cin >> n;\n        vector<vector<int>> travelTimes(n, vector<int>(3));\n        for (int i = 0; i < n; i++) {\n            cin >> travelTimes[i][0] >> travelTimes[i][1] >> travelTimes[i][2];\n        }\n        int totalNodes;\n        cin >> totalNodes;\n        int startNode;\n        cin >> startNode;\n        cout << calculateNetworkDelayTime(travelTimes, totalNodes, startNode) << endl;\n    }\n    return 0;\n}",
      "Java": "// COMPLETE, COMPILABLE Java code\n// Class MUST be named Main\n// Uses correct I/O\npublic class Main {\n    static class Edge {\n        int source;\n        int target;\n        int time;\n        public Edge(int source, int target, int time) {\n            this.source = source;\n            this.target = target;\n            this.time = time;\n        }\n    }\n    public static int calculateNetworkDelayTime(int[][] travelTimes, int totalNodes, int startNode) {\n        int[] dist = new int[totalNodes + 1];\n        for (int i = 1; i <= totalNodes; i++) {\n            dist[i] = Integer.MAX_VALUE;\n        }\n        dist[startNode] = 0;\n        java.util.PriorityQueue<int[]> pq = new java.util.PriorityQueue<>((a, b) -> a[0] - b[0]);\n        pq.offer(new int[] {0, startNode});\n        while (!pq.isEmpty()) {\n            int[] cur = pq.poll();\n            int time = cur[0];\n            int node = cur[1];\n            if (time > dist[node]) continue;\n            for (int[] e : travelTimes) {\n                if (e[0] == node) {\n                    if (dist[e[1]] > time + e[2]) {\n                        dist[e[1]] = time + e[2];\n                        pq.offer(new int[] {dist[e[1]], e[1]});\n                    }\n                }\n            }\n        }\n        int maxDist = 0;\n        for (int i = 1; i <= totalNodes; i++) {\n            if (dist[i] == Integer.MAX_VALUE) return -1;\n            maxDist = Math.max(maxDist, dist[i]);\n        }\n        return maxDist;\n    }\n    public static void main(String[] args) {\n        java.util.Scanner sc = new java.util.Scanner(System.in);\n        int T = sc.nextInt();\n        while (T-- > 0) {\n            int n = sc.nextInt();\n            int[][] travelTimes = new int[n][3];\n            for (int i = 0; i < n; i++) {\n                travelTimes[i][0] = sc.nextInt();\n                travelTimes[i][1] = sc.nextInt();\n                travelTimes[i][2] = sc.nextInt();\n            }\n            int totalNodes = sc.nextInt();\n            int startNode = sc.nextInt();\n            System.out.println(calculateNetworkDelayTime(travelTimes, totalNodes, startNode));\n        }\n    }\n}",
      "Python3": "# COMPLETE, RUNNABLE Python3 code\n# Uses input() or sys.stdin\n# Handles all T test cases\nimport sys\nimport heapq\ninput = sys.stdin.readline\n\ndef calculateNetworkDelayTime(travelTimes, totalNodes, startNode):\n    dist = [float('inf')] * (totalNodes + 1)\n    dist[startNode] = 0\n    pq = [(0, startNode)]\n    while pq:\n        time, node = heapq.heappop(pq)\n        if time > dist[node]:\n            continue\n        for u, v, t in travelTimes:\n            if u == node:\n                if dist[v] > time + t:\n                    dist[v] = time + t\n                    heapq.heappush(pq, (dist[v], v))\n    maxDist = 0\n    for i in range(1, totalNodes + 1):\n        if dist[i] == float('inf'):\n            return -1\n        maxDist = max(maxDist, dist[i])\n    return maxDist\n\nt = int(input())\nwhile t > 0:\n    n = int(input())\n    travelTimes = [list(map(int, input().split())) for _ in range(n)]\n    totalNodes = int(input())\n    startNode = int(input())\n    print(calculateNetworkDelayTime(travelTimes, totalNodes, startNode))\n    t -= 1",
      "JavaScript": "// COMPLETE, RUNNABLE JavaScript code\n// Uses fs.readFileSync(0,'utf-8') — NEVER readline()\n// Handles all T test cases\nconst fs = require('fs');\nconst lines = fs.readFileSync(0, 'utf-8').trim().split('\\n');\nlet idx = 0;\nfunction readline() {\n    return lines[idx++];\n}\n\nfunction calculateNetworkDelayTime(travelTimes, totalNodes, startNode) {\n    const dist = new Array(totalNodes + 1).fill(Infinity);\n    dist[startNode] = 0;\n    const pq = [];\n    pq.push([0, startNode]);\n    while (pq.length > 0) {\n        pq.sort((a, b) => a[0] - b[0]);\n        const [time, node] = pq.shift();\n        if (time > dist[node]) continue;\n        for (const [u, v, t] of travelTimes) {\n            if (u === node) {\n                if (dist[v] > time + t) {\n                    dist[v] = time + t;\n                    pq.push([dist[v], v]);\n                }\n            }\n        }\n    }\n    let maxDist = 0;\n    for (let i = 1; i <= totalNodes; i++) {\n        if (dist[i] === Infinity) return -1;\n        maxDist = Math.max(maxDist, dist[i]);\n    }\n    return maxDist;\n}\n\nconst T = parseInt(readline());\nfor (let i = 0; i < T; i++) {\n    const n = parseInt(readline());\n    const travelTimes = [];\n    for (let j = 0; j < n; j++) {\n        const [u, v, t] = readline().split(' ').map(Number);\n        travelTimes.push([u, v, t]);\n    }\n    const totalNodes = parseInt(readline());\n    const startNode = parseInt(readline());\n    console.log(calculateNetworkDelayTime(travelTimes, totalNodes, startNode));\n}"
    }
  },
  "QBCategory": "CCD",
  "boilerPlates": {
    "C": "#include <stdio.h>\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "C++": "#include <iostream>\nusing namespace std;\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "Java": "import java.util.*;\nimport java.io.*;\n\npublic class Main {\n    public static void main(String[] args) throws IOException {\n        // your code goes here\n    }\n}",
    "Python3": "# your code goes here\n",
    "JavaScript": "// your code goes here\n"
  },
  "_internal": {
    "methodName": "",
    "params": []
  }
}