{
  "questionId": "Q8438",
  "title": "Weird GCD Value",
  "slug": "weird-gcd-value",
  "version": 1,
  "createdAt": "2026-07-02",
  "updatedAt": "2026-07-04",
  "metadata": {
    "category": "Number Theory",
    "difficulty": "Hard",
    "isPremium": true,
    "tags": [
      "GCD",
      "Euler Totient",
      "Divisor Sum",
      "Precomputation"
    ],
    "companyTags": [],
    "topicTags": [
      "Mathematics"
    ],
    "QBCategory": "CCD"
  },
  "content": {
    "problemStatement": "You are given two positive integers aaa and bbb, we define f(a,b)f(a, b)f(a,b) as the maximum value of ∣gcd⁡(a,x)−gcd⁡(b,x)∣|\\gcd(a, x) - \\gcd(b, x)|∣gcd(a,x)−gcd(b,x)∣ where xxx is some natural number. Formally,\n\nf(a,b)=max⁡x∈N∣gcd⁡(a,x)−gcd⁡(b,x)∣f(a, b) = \\max\\limits_{x  \\in \\mathbb{N}}| \\gcd(a,x) - \\gcd(b,x)|f(a,b)=x∈Nmax​∣gcd(a,x)−gcd(b,x)∣\n\n, where N\\mathbb{N}N represents the set of natural numbers and gcd⁡(a,b)\\gcd(a, b)gcd(a,b) represents the greatest common divisor of aaa and bbb.\n\nYou are given an integer kkk. You need to find the number of ordered pairs (a,b)(a, b)(a,b) (where a,ba, ba,b are positive) such that f(a,b)=kf(a, b) = kf(a,b)=k.\n\nNote that pairs (a,b)(a, b)(a,b) and (b,a)(b, a)(b,a) are considered different if a≠ba \\neq ba=b. For example, pair (1,2)(1,2)(1,2) is not the same as (2,1)(2,1)(2,1).\n\n\nInput Format\n\nThe first line of the input contains a single integer TTT denoting the number of test cases. The description of TTT test cases follows.\nThe first and only line of each testcase contains a single integer kkk.\n\n\nOutput Format\nFor each testcase, output the number of ordered pairs (a,b)(a, b)(a,b) such that f(a,b)=kf(a, b) = kf(a,b)=k.",
    "constraints": [
      "1 ≤ T ≤ 10^6",
      "1 ≤ k ≤ 10^6"
    ],
    "inputFormat": "The first line contains an integer T, the number of test cases. Each of the next T lines contains a single integer k (1 ≤ k ≤ 10^6).",
    "outputFormat": "For each test case output a single line with the number of ordered pairs (a, b) of positive integers such that f(a, b) = k.",
    "sampleTestCases": [
      {
        "id": "sample_1",
        "input": "2\n1\n3",
        "output": "2\n6",
        "explanation": "Explanation:\nTest case 111: For k=1k = 1k=1, we see that f(1,2)=f(2,1)=1f(1, 2) = f(2, 1) = 1f(1,2)=f(2,1)=1. Therefore the answer is 222. It can be verified that there are no other pairs for which the fff value is 111.\nTest case 222: For k=3k = 3k=3, there exists 666 ordered pairs for which the fff value is 333. One of them is (4,3)(4, 3)(4,3)."
      }
    ]
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "label": "Case 1",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_2",
        "label": "Case 2",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_3",
        "label": "Case 3",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_4",
        "label": "Case 4",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_5",
        "label": "Case 5",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      }
    ],
    "totalTestCases": 5,
    "totalWeight": 10
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      2,
      2,
      2,
      2,
      2
    ],
    "maxScore": 10
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "We first prove that f(a,b)=max(a,b)-gcd(a,b). Let g=gcd(a,b) and write a=g·A, b=g·B with gcd(A,B)=1. If a≥b then the condition becomes g·(A-1)=k, i.e. g divides k and A = k/g + 1. For each divisor g of k, A is fixed and B can be any integer 1≤B≤A-1 that is coprime with A. The number of such B is φ(A). Therefore the number of ordered pairs with a≥b equals Σ_{g|k} φ(k/g + 1). By symmetry the same amount of pairs have a<b, and a=b never satisfies k>0. Hence the answer is 2· Σ_{d|k} φ(d+1). To answer up to 10^6 queries we pre‑compute φ(n) for n≤10^6+1 using a linear sieve. Then we build an array ans[k] = Σ_{d|k} φ(d+1) for all k in O(N log N) by iterating d from 1 to N and adding φ(d+1) to every multiple of d. Finally each query is answered in O(1) as 2·ans[k].",
    "timeComplexity": "Precomputation O(N log N) where N = 10^6. Each test case O(1).",
    "spaceComplexity": "O(N) for φ and ans arrays.",
    "code": {
      "C": "#include <stdio.h>\n#include <stdlib.h>\n\n#define MAXK 1000000\n\nint main(){\n    static int phi[MAXK+2];\n    static int ans[MAXK+1];\n    int i, j;\n    // linear sieve for phi up to MAXK+1\n    int primes[MAXK];\n    int pcnt=0;\n    phi[0]=0; phi[1]=1;\n    for(i=2;i<=MAXK+1;i++) phi[i]=0;\n    for(i=2;i<=MAXK+1;i++){\n        if(!phi[i]){ phi[i]=i-1; primes[pcnt++]=i; }\n        for(j=0;j<pcnt && (long long)i*primes[j]<=MAXK+1; j++){\n            int p=primes[j];\n            int v=i*p;\n            if(i%p==0){ phi[v]=phi[i]*p; break; }\n            else phi[v]=phi[i]*(p-1);\n        }\n    }\n    // build ans[k] = sum_{d|k} phi(d+1)\n    for(i=1;i<=MAXK;i++){\n        int add = phi[i+1];\n        for(j=i;j<=MAXK;j+=i) ans[j] += add;\n    }\n    int T; if(scanf(\"%d\", &T)!=1) return 0;\n    while(T--){\n        int k; scanf(\"%d\", &k);\n        long long res = 2LL * ans[k];\n        printf(\"%lld\\n\", res);\n    }\n    return 0;\n}",
      "C++": "#include <bits/stdc++.h>\nusing namespace std;\nconst int MAXK = 1000000;\nint main(){\n    ios::sync_with_stdio(false);\n    cin.tie(nullptr);\n    vector<int> phi(MAXK+2);\n    vector<int> primes;\n    phi[0]=0; phi[1]=1;\n    for(int i=2;i<=MAXK+1;i++) if(!phi[i]){\n        phi[i]=i-1; primes.push_back(i);\n        for(int p:primes){\n            long long v=1LL*i*p; if(v>MAXK+1) break;\n            if(i%p==0){ phi[v]=phi[i]*p; break; }\n            else phi[v]=phi[i]*(p-1);\n        }\n    }\n    vector<int> ans(MAXK+1,0);\n    for(int d=1; d<=MAXK; ++d){\n        int add = phi[d+1];\n        for(int k=d; k<=MAXK; k+=d) ans[k] += add;\n    }\n    int T; if(!(cin>>T)) return 0;\n    while(T--){\n        int k; cin>>k;\n        long long res = 2LL * ans[k];\n        cout<<res<<'\\n';\n    }\n    return 0;\n}",
      "Java": "import java.io.*;\nimport java.util.*;\n\npublic class Main {\n    static final int MAXK = 1_000_000;\n    public static void main(String[] args) throws Exception {\n        FastScanner fs = new FastScanner(System.in);\n        int[] phi = new int[MAXK + 2];\n        int[] ans = new int[MAXK + 1];\n        // linear sieve for phi\n        List<Integer> primes = new ArrayList<>();\n        phi[0] = 0; phi[1] = 1;\n        for (int i = 2; i <= MAXK + 1; i++) {\n            if (phi[i] == 0) {\n                phi[i] = i - 1;\n                primes.add(i);\n            }\n            for (int p : primes) {\n                long v = (long) i * p;\n                if (v > MAXK + 1) break;\n                if (i % p == 0) {\n                    phi[(int) v] = phi[i] * p;\n                    break;\n                } else {\n                    phi[(int) v] = phi[i] * (p - 1);\n                }\n            }\n        }\n        // build ans\n        for (int d = 1; d <= MAXK; d++) {\n            int add = phi[d + 1];\n            for (int k = d; k <= MAXK; k += d) ans[k] += add;\n        }\n        int T = fs.nextInt();\n        StringBuilder sb = new StringBuilder();\n        while (T-- > 0) {\n            int k = fs.nextInt();\n            long res = 2L * ans[k];\n            sb.append(res).append('\\n');\n        }\n        System.out.print(sb.toString());\n    }\n    // fast scanner\n    static class FastScanner {\n        private final InputStream in;\n        private final byte[] buffer = new byte[1 << 16];\n        private int ptr = 0, len = 0;\n        FastScanner(InputStream in) { this.in = in; }\n        private int readByte() throws IOException {\n            if (ptr >= len) {\n                len = in.read(buffer);\n                ptr = 0;\n                if (len <= 0) return -1;\n            }\n            return buffer[ptr++];\n        }\n        int nextInt() throws IOException {\n            int c, sign = 1, val = 0;\n            do { c = readByte(); } while (c <= ' ');\n            if (c == '-') { sign = -1; c = readByte(); }\n            while (c > ' ') {\n                val = val * 10 + (c - '0');\n                c = readByte();\n            }\n            return val * sign;\n        }\n    }\n}",
      "Python3": "import sys\nMAXK = 10**6\n# linear sieve for phi up to MAXK+1\nphi = [0] * (MAXK + 2)\nphi[1] = 1\nprimes = []\nfor i in range(2, MAXK + 2):\n    if phi[i] == 0:\n        phi[i] = i - 1\n        primes.append(i)\n    for p in primes:\n        v = i * p\n        if v > MAXK + 1:\n            break\n        if i % p == 0:\n            phi[v] = phi[i] * p\n            break\n        else:\n            phi[v] = phi[i] * (p - 1)\n# precompute answers\nans = [0] * (MAXK + 1)\nfor d in range(1, MAXK + 1):\n    add = phi[d + 1]\n    for k in range(d, MAXK + 1, d):\n        ans[k] += add\n\ndata = sys.stdin.read().strip().split()\nif not data:\n    sys.exit()\nit = iter(data)\nT = int(next(it))\nout = []\nfor _ in range(T):\n    k = int(next(it))\n    out.append(str(2 * ans[k]))\nsys.stdout.write('\\n'.join(out))",
      "JavaScript": "const fs = require('fs');\nconst input = fs.readFileSync(0, 'utf-8').trim().split(/\\s+/).map(Number);\nlet idx = 0;\nconst T = input[idx++];\nconst MAXK = 1000000;\n// linear sieve for phi up to MAXK+1\nconst phi = new Uint32Array(MAXK + 2);\nphi[1] = 1;\nconst primes = [];\nfor (let i = 2; i <= MAXK + 1; i++) {\n  if (phi[i] === 0) {\n    phi[i] = i - 1;\n    primes.push(i);\n  }\n  for (let pIdx = 0; pIdx < primes.length; pIdx++) {\n    const p = primes[pIdx];\n    const v = i * p;\n    if (v > MAXK + 1) break;\n    if (i % p === 0) {\n      phi[v] = phi[i] * p;\n      break;\n    } else {\n      phi[v] = phi[i] * (p - 1);\n    }\n  }\n}\n// precompute answers\nconst ans = new Uint32Array(MAXK + 1);\nfor (let d = 1; d <= MAXK; d++) {\n  const add = phi[d + 1];\n  for (let k = d; k <= MAXK; k += d) ans[k] += add;\n}\nlet out = [];\nfor (let t = 0; t < T; t++) {\n  const k = input[idx++];\n  out.push(String(2 * ans[k]));\n}\nconsole.log(out.join('\\n'));"
    }
  },
  "QBCategory": "CCD",
  "boilerPlates": {
    "C": "#include <stdio.h>\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "C++": "#include <iostream>\nusing namespace std;\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "Java": "import java.util.*;\nimport java.io.*;\n\npublic class Main {\n    public static void main(String[] args) throws IOException {\n        // your code goes here\n    }\n}",
    "Python3": "# your code goes here\n",
    "JavaScript": "// your code goes here\n"
  },
  "_internal": {
    "methodName": "",
    "params": []
  }
}