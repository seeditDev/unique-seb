{
  "questionId": "Q1637",
  "title": "Rotting Apples",
  "slug": "rotting-apples",
  "version": 1,
  "createdAt": "2026-07-01",
  "updatedAt": "2026-07-04",
  "metadata": {
    "category": "Graphs",
    "difficulty": "Hard",
    "isPremium": false,
    "tags": [
      "Graphs",
      "BFS",
      "Grid"
    ],
    "companyTags": [],
    "topicTags": [
      "Graphs"
    ]
  },
  "content": {
    "problemStatement": "Given an `N x M` matrix where each cell can have one of three values:\n\n\n`0` representing an empty cell,\n`1` representing a fresh apple, or\n`2` representing a rotten apple.\nEvery minute, any fresh apple that is `4`-directionally adjacent to a rotten apple becomes rotten.\n\n`4`-directionally adjacent cells of a cell (i, j) are cell (i - 1, j), (i + 1, j), (i, j - 1) and (i, j + 1).\n\nFind the minimum number of minutes that must elapse until no cell has a fresh apple. If this is impossible, return -1.\n\n\nInput Format\n\nThe first line of input will contain two space separated integers NNN and MMM, denoting the no. of rows and columns in the input matrix.\nNext NNN lines contains MMM space separated integers, the elements of the matrix.\n\n\nOutput Format\n\nOutput on a single line, the minimum number of minutes that must elapse until no cell has a fresh apple. If this is impossible, output -1.",
    "constraints": [
      "1 <= N <= 100",
      "1 <= M <= 100",
      "The elements of the grid are either 0, 1, or 2."
    ],
    "inputFormat": "The first line of input will contain two space-separated integers N and M, denoting the number of rows and columns in the grid. Each of the next N lines contains M space-separated integers, representing the elements of the grid.",
    "outputFormat": "Output on a single line, the minimum number of minutes that must elapse until no cell has a fresh apple. If this is impossible, output -1.",
    "sampleTestCases": [
      {
        "id": "sample_1",
        "input": "3 4\n0 2 1 1\n1 1 0 1\n1 1 0 1",
        "output": "4",
        "explanation": "Explanation: There is a rotten apple in cell (0, 1) (assuming 0-based indexing). After 1 minute: fresh apples in cells (0, 2) and (1, 1) gets rotten. After 2 minutes: fresh apples in cells (0, 3), (1, 0) and (2, 1) gets rotten. After 3 minutes: fresh apples in cells (2, 0) and (1, 3) gets rotten. After 4 minutes: the last fresh apple in cell (2, 3) gets rotten. Hence it takes minimum 4 minutes to rot all apples."
      },
      {
        "id": "sample_2",
        "input": "3 3\n2 1 1\n1 1 0\n0 1 1",
        "output": "4",
        "explanation": "Explanation: There is a rotten apple in cell (0, 0) (assuming 0-based indexing). After 1 minute: fresh apples in cells (0, 1) and (1, 0) gets rotten. After 2 minutes: fresh apples in cells (0, 2), (1, 1) and (2, 0) gets rotten. After 3 minutes: fresh apples in cells (1, 2) and (2, 1) gets rotten. After 4 minutes: the last fresh apple in cell (2, 2) gets rotten. Hence it takes minimum 4 minutes to rot all apples."
      },
      {
        "id": "sample_3",
        "input": "1 6\n2 1 1 1 1 1",
        "output": "6",
        "explanation": "Explanation: There is a rotten apple in cell (0, 0) (assuming 0-based indexing). After 1 minute: fresh apple in cell (0, 1) gets rotten. After 2 minutes: fresh apple in cell (0, 2) gets rotten. After 3 minutes: fresh apple in cell (0, 3) gets rotten. After 4 minutes: fresh apple in cell (0, 4) gets rotten. After 5 minutes: fresh apple in cell (0, 5) gets rotten. After 6 minutes: the last fresh apple in cell (0, 5) gets rotten. Hence it takes minimum 6 minutes to rot all apples."
      }
    ]
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "label": "minimum boundary",
        "input": "1 1\n0",
        "expectedOutput": "0",
        "weight": 2
      },
      {
        "id": "tc_2",
        "label": "Case 2",
        "input": "1 1\n0",
        "expectedOutput": "0",
        "weight": 2
      },
      {
        "id": "tc_3",
        "label": "Case 3",
        "input": "1 1\n0",
        "expectedOutput": "0",
        "weight": 2
      },
      {
        "id": "tc_4",
        "label": "Case 4",
        "input": "1 1\n0",
        "expectedOutput": "0",
        "weight": 2
      },
      {
        "id": "tc_5",
        "label": "Case 5",
        "input": "1 1\n0",
        "expectedOutput": "0",
        "weight": 2
      }
    ],
    "totalTestCases": 5,
    "totalWeight": 10
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      2,
      2,
      2,
      2,
      2
    ],
    "maxScore": 10
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "The problem can be solved using a Breadth-First Search (BFS) algorithm. We start by initializing a queue with all the rotten apples in the grid. Then, we perform BFS from each rotten apple, marking all the adjacent fresh apples as rotten and adding them to the queue. We repeat this process until the queue is empty. The number of minutes that must elapse until no cell has a fresh apple is the number of iterations of the BFS algorithm.",
    "timeComplexity": "O(N*M) per test case, where N is the number of rows and M is the number of columns in the grid.",
    "spaceComplexity": "O(N*M) \u2014 the space complexity is dominated by the queue used in the BFS algorithm.",
    "code": {
      "C": "#include <stdio.h>\n#include <stdlib.h>\n#include <assert.h>\n\n#define MAX_SIZE 100\n\ntypedef struct {\n    int x;\n    int y;\n} Pair;\n\ntypedef struct {\n    Pair queue[MAX_SIZE * MAX_SIZE];\n    int front;\n    int rear;\n} Queue;\n\nvoid enqueue(Queue *q, Pair p) {\n    q->queue[q->rear++] = p;\n}\n\nPair dequeue(Queue *q) {\n    return q->queue[q->front++];\n}\n\nint isEmpty(Queue *q) {\n    return q->front == q->rear;\n}\n\nint minTimeToRot(int grid[][MAX_SIZE], int n, int m) {\n    int rows = n;\n    int cols = m;\n    int visited[MAX_SIZE][MAX_SIZE];\n    \n    for (int i = 0; i < rows; i++) {\n        for (int j = 0; j < cols; j++) {\n            visited[i][j] = grid[i][j];\n        }\n    }\n    \n    Queue rottenQueue;\n    rottenQueue.front = 0;\n    rottenQueue.rear = 0;\n    int freshOrangeCount = 0;\n    \n    for (int i = 0; i < rows; i++) {\n        for (int j = 0; j < cols; j++) {\n            if (visited[i][j] == 2) {\n                enqueue(&rottenQueue, (Pair){i, j});\n            }\n            if (visited[i][j] == 1) {\n                freshOrangeCount++;\n            }\n        }\n    }\n    \n    if (freshOrangeCount == 0)\n        return 0;\n    if (isEmpty(&rottenQueue))\n        return -1;\n    \n    int minutes = -1;\n    Pair directions[] = {{1, 0},{-1, 0},{0, -1},{0, 1}};\n    \n    while (!isEmpty(&rottenQueue)) {\n        int size = rottenQueue.rear - rottenQueue.front;\n        while (size--) {\n            Pair it = dequeue(&rottenQueue);\n            int x = it.x;\n            int y = it.y;\n            for (int k = 0; k < 4; k++) {\n                int i = x + directions[k].x;\n                int j = y + directions[k].y;\n                if (i >= 0 && i < rows && j >= 0 && j < cols && visited[i][j] == 1) {\n                    visited[i][j] = 2;\n                    freshOrangeCount--;\n                    enqueue(&rottenQueue, (Pair){i, j});\n                }\n            }\n        }\n        minutes++;\n    }\n    \n    if (freshOrangeCount == 0)\n        return minutes;\n    return -1;\n}\n\nint main() {\n    int n, m;\n    scanf(\"%d %d\", &n, &m);\n\n    assert(1 <= n && n <= 100);\n    assert(1 <= m && m <= 100);\n\n    int mat[MAX_SIZE][MAX_SIZE];\n    \n    for (int i = 0; i < n; i++) {\n        for (int j = 0; j < m; j++) {\n            scanf(\"%d\", &mat[i][j]);\n            int x = mat[i][j];\n            assert(x == 0 || x == 1 || x == 2);\n        }\n    }\n\n    printf(\"%d\\n\", minTimeToRot(mat, n, m));\n\n    return 0;\n}",
      "C++": "#include <bits/stdc++.h>\nusing namespace std;\n\nint minTimeToRot(vector<vector<int>>& grid) {\n    int rows = grid.size();\n    int cols = grid[0].size();\n    vector<vector<int>> visited = grid;\n    \n    queue<pair<int, int>> rottenQueue;\n    int freshOrangeCount = 0;\n    \n    for (int i = 0; i < rows; i++) {\n        for (int j = 0; j < cols; j++) {\n            if (visited[i][j] == 2) {\n                rottenQueue.push({i, j});\n            }\n            if (visited[i][j] == 1) {\n                freshOrangeCount++;\n            }\n        }\n    }\n    \n    if (freshOrangeCount == 0)\n        return 0;\n    if (rottenQueue.empty())\n        return -1;\n    \n    int minutes = -1;\n    vector<pair<int, int>> directions = {{1, 0},{-1, 0},{0, -1},{0, 1}};\n    \n    while (!rottenQueue.empty()) {\n        int size = rottenQueue.size();\n        while (size--) {\n            auto it = rottenQueue.front();\n            int x = it.first;\n            int y = it.second;\n            rottenQueue.pop();\n            for (auto dir : directions) {\n                int i = x + dir.first;\n                int j = y + dir.second;\n                if (i >= 0 && i < rows && j >= 0 && j < cols && visited[i][j] == 1) {\n                    visited[i][j] = 2;\n                    freshOrangeCount--;\n                    rottenQueue.push({i, j});\n                }\n            }\n        }\n        minutes++;\n    }\n    \n    if (freshOrangeCount == 0)\n        return minutes;\n    return -1;\n}\n\nint main() {\n\n  int n, m;\n  cin >> n >> m;\n\n  assert(1 <= n && n <= 100);\n  assert(1 <= m && m <= 100);\n\n  vector<vector<int> > mat(n, vector<int> (m));\n  for(int i = 0; i < n; i++){\n    for(int j = 0; j < m; j++){\n        cin >> mat[i][j];\n        int x = mat[i][j];\n        assert(x == 0 || x == 1 || x == 2);\n    }\n  } \n  \n  cout << minTimeToRot(mat);\n\n\n  return 0;\n}",
      "Java": "import java.util.Scanner;\nimport java.util.Queue;\nimport java.util.LinkedList;\n\npublic class Main {\n    \n    public static int minTimeToRot(int[][] grid, int n, int m) {\n        int[][] visited = new int[n][m];\n        Queue<int[]> rottenQueue = new LinkedList<>();\n        int freshOrangeCount = 0;\n        int minutes = -1;\n\n        for (int i = 0; i < n; i++) {\n            for (int j = 0; j < m; j++) {\n                visited[i][j] = grid[i][j];\n                if (visited[i][j] == 2) {\n                    rottenQueue.offer(new int[]{i, j});\n                }\n                if (visited[i][j] == 1) {\n                    freshOrangeCount++;\n                }\n            }\n        }\n\n        if (freshOrangeCount == 0)\n            return 0;\n        if (rottenQueue.isEmpty())\n            return -1;\n\n        int[][] directions = {{1, 0}, {-1, 0}, {0, -1}, {0, 1}};\n        while (!rottenQueue.isEmpty()) {\n            int size = rottenQueue.size();\n            while (size-- > 0) {\n                int[] cell = rottenQueue.poll();\n                int x = cell[0], y = cell[1];\n                for (int[] dir : directions) {\n                    int i = x + dir[0];\n                    int j = y + dir[1];\n                    if (i >= 0 && i < n && j >= 0 && j < m && visited[i][j] == 1) {\n                        visited[i][j] = 2;\n                        freshOrangeCount--;\n                        rottenQueue.offer(new int[]{i, j});\n                    }\n                }\n            }\n            minutes++;\n        }\n\n        if (freshOrangeCount == 0)\n            return minutes;\n        return -1;\n    }\n\n    public static void main(String[] args) {\n        Scanner scanner = new Scanner(System.in);\n\n        int n = scanner.nextInt();\n        int m = scanner.nextInt();\n\n        assert (1 <= n && n <= 100);\n        assert (1 <= m && m <= 100);\n\n        int[][] grid = new int[n][m];\n\n        for (int i = 0; i < n; i++) {\n            for (int j = 0; j < m; j++) {\n                grid[i][j] = scanner.nextInt();\n                int x = grid[i][j];\n                assert (x == 0 || x == 1 || x == 2);\n            }\n        }\n\n        System.out.println(minTimeToRot(grid, n, m));\n    }\n}",
      "Python3": "from collections import deque\n\ndef minTimeToRot(grid):\n    rows, cols = len(grid), len(grid[0])\n    visited = [row[:] for row in grid]\n    \n    rottenQueue = deque()\n    freshOrangeCount = 0\n    \n    for i in range(rows):\n        for j in range(cols):\n            if visited[i][j] == 2:\n                rottenQueue.append((i, j))\n            if visited[i][j] == 1:\n                freshOrangeCount += 1\n    \n    if freshOrangeCount == 0:\n        return 0\n    if not rottenQueue:\n        return -1\n    \n    minutes = -1\n    directions = [(1, 0), (-1, 0), (0, -1), (0, 1)]\n    \n    while rottenQueue:\n        size = len(rottenQueue)\n        for _ in range(size):\n            x, y = rottenQueue.popleft()\n            for dx, dy in directions:\n                i, j = x + dx, y + dy\n                if 0 <= i < rows and 0 <= j < cols and visited[i][j] == 1:\n                    visited[i][j] = 2\n                    freshOrangeCount -= 1\n                    rottenQueue.append((i, j))\n        minutes += 1\n    \n    if freshOrangeCount == 0:\n        return minutes\n    return -1\n\nif __name__ == \"__main__\":\n    n, m = map(int, input().split())\n    \n    assert 1 <= n <= 100\n    assert 1 <= m <= 100\n\n    mat = [list(map(int, input().split())) for _ in range(n)]\n    \n    print(minTimeToRot(mat))\n",
      "JavaScript": "function minTimeToRot(grid) {\n    const rows = grid.length;\n    const cols = grid[0].length;\n    const directions = [[1, 0], [-1, 0], [0, 1], [0, -1]];\n    const queue = [];\n    let freshOrangeCount = 0;\n\n    // Initialize the queue with all rotten oranges and count fresh oranges\n    for (let i = 0; i < rows; i++) {\n        for (let j = 0; j < cols; j++) {\n            if (grid[i][j] === 2) {\n                queue.push([i, j]);\n            }\n            if (grid[i][j] === 1) {\n                freshOrangeCount++;\n            }\n        }\n    }\n\n    if (freshOrangeCount === 0) return 0; // No fresh oranges to rot\n    if (queue.length === 0) return -1;     // No rotten oranges to start with\n\n    let minutes = -1;\n\n    // Perform BFS from all initially rotten oranges\n    while (queue.length > 0) {\n        const size = queue.length;\n        for (let k = 0; k < size; k++) {\n            const [x, y] = queue.shift();\n            for (const [dx, dy] of directions) {\n                const i = x + dx;\n                const j = y + dy;\n                if (i >= 0 && i < rows && j >= 0 && j < cols && grid[i][j] === 1) {\n                    grid[i][j] = 2; // Rot the orange\n                    freshOrangeCount--;\n                    queue.push([i, j]); // Add new rotten orange to the queue\n                }\n            }\n        }\n        minutes++;\n    }\n\n    return freshOrangeCount === 0 ? minutes : -1; // If all fresh oranges are rotted, return time; else -1\n}\n\nconst fs = require('fs');\nconst input = fs.readFileSync(0, 'utf-8').trim().split('\\n');\nlet idx = 0;\n\nconst [n, m] = input[idx++].split(' ').map(Number);\nconst grid = [];\nfor (let i = 0; i < n; i++) {\n    grid.push(input[idx++].split(' ').map(Number));\n}\n\nconsole.log(minTimeToRot(grid));\n"
    }
  }
}