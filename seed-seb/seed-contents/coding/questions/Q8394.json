{
  "questionId": "Q8394",
  "title": "01 Subsequence",
  "slug": "01-subsequence",
  "version": 1,
  "createdAt": "2026-07-02",
  "updatedAt": "2026-07-04",
  "metadata": {
    "category": "Greedy | Sorting | Strings",
    "difficulty": "Medium",
    "isPremium": true,
    "tags": [
      "Greedy",
      "Sorting",
      "Prefix Sum",
      "Combinatorics"
    ],
    "companyTags": [],
    "topicTags": [
      "Constructive Algorithms"
    ],
    "QBCategory": "CCD"
  },
  "content": {
    "problemStatement": "You are handed a binary string SSS, but not in any ordinary form. Instead of being directly given the value of SSS, you are given an array CCC of size NNN representing the 01-compression representation of SSS. This means SSS first contains C1C_1C1​ 0\\texttt{0}0 characters, then C2C_2C2​ 1\\texttt{1}1 characters, then C3C_3C3​ 0\\texttt{0}0 characters, and so on. For example, the array C=[2,3,4,3]C = [2, 3, 4, 3]C=[2,3,4,3] corresponds with the binary string 001110000111\\texttt{001110000111}001110000111.\n\nYou are allowed to modify SSS by swapping elements of CCC. More specifically, you are allowed to swap CiC_iCi​ and CjC_jCj​ as long as CiC_iCi​ and CjC_jCj​ both encodes blocks of the same character. For example, from C=[2,3,4,3]C = [2, 3, 4, 3]C=[2,3,4,3], you can swap C1C_1C1​ and C3C_3C3​ because both of them encode blocks of 0\\texttt{0}0's, turning CCC to [4,3,2,3][4, 3, 2, 3][4,3,2,3] and SSS to 000011100111\\texttt{000011100111}000011100111. However, you cannot swap C1C_1C1​ and C2C_2C2​ because C1C_1C1​ encodes a block of 0\\texttt{0}0's, while C2C_2C2​ encodes a block of 1\\texttt{1}1s.\n\nPerform the swapping operation in any way as many times as you want (including zero times) so that the final string SSS has as many 01\\texttt{01}01 subsequences as possible. As a reminder, a subsequence of a string is a sequence that can be derived by deleting zero or more characters without changing the order of the remaining characters.\n\nYou need to find any optimal final array CCC, for which the number of 01\\texttt{01}01 subsequence will be the largest possible.\n\n\nInput Format\n\nThe first line contains TTT - the number of test cases. Then the test cases follow.\nThe first line of each test case contains a single integer NNN - the size of the array CCC.\nThe second line of each test case contains NNN integers C1,C2,…,CNC_1, C_2, \\dots, C_NC1​,C2​,…,CN​ - the 01\\texttt{01}01-compression representation of SSS.\n\n\nOutput Format\nFor each test case, output two lines: the first line contains NNN integers representing the optimal array CCC, and the second line contains the maximum number of 01\\texttt{01}01 subsequences.",
    "constraints": [
      "1 ≤ T ≤ 1000",
      "1 ≤ N ≤ 1000",
      "1 ≤ Ci ≤ 1000"
    ],
    "inputFormat": "The first line contains an integer T, the number of test cases. For each test case: the first line contains an integer N, the size of the compression array. The second line contains N space‑separated integers C1 … CN describing the lengths of consecutive blocks. The first block always consists of '0's, the second of '1's, and so on.",
    "outputFormat": "For each test case output two lines. The first line contains N integers – the optimal ordering of the array after any allowed swaps. The second line contains a single integer – the maximum possible number of subsequences \"01\" in the resulting binary string.",
    "sampleTestCases": [
      {
        "id": "sample_1",
        "input": "1\n4\n2 3 4 3",
        "output": "4 3 2 3\n30",
        "explanation": "Odd positions (0‑blocks) are [2,4]; sorting descending gives [4,2]. Even positions (1‑blocks) are [3,3]; sorting ascending keeps them [3,3]. The final array is [4,3,2,3]. Scanning: zeros after first block =4, first 1‑block adds 4*3=12, zeros become 6, second 1‑block adds 6*3=18, total 30."
      }
    ]
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "label": "Case 1",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_2",
        "label": "Case 2",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_3",
        "label": "Case 3",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_4",
        "label": "Case 4",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_5",
        "label": "Case 5",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      }
    ],
    "totalTestCases": 5,
    "totalWeight": 10
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      2,
      2,
      2,
      2,
      2
    ],
    "maxScore": 10
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "Only blocks of the same character may be permuted. The string alternates 0‑blocks (odd positions) and 1‑blocks (even positions). To maximise the number of pairs (i<j, Si='0', Sj='1'), we want as many zeros as possible as early as possible and as many ones as possible as late as possible. Therefore:\n* Sort the lengths of all 0‑blocks (odd indices) in **descending** order and place them back to the odd positions from left to right.\n* Sort the lengths of all 1‑blocks (even indices) in **ascending** order and place them back to the even positions from left to right.\nAfter this arrangement we compute the number of \"01\" subsequences by scanning the blocks: keep a running total `zeroCnt` of zeros seen so far; when a 1‑block of length L is encountered it adds `zeroCnt * L` to the answer, then continue. The algorithm runs in O(N log N) per test case because of the two sorts, and uses O(N) additional memory.",
    "timeComplexity": "O(N log N) per test case (dominant sorting step).",
    "spaceComplexity": "O(N) for storing the reordered array and prefix sums.",
    "code": {
      "C": "#include <stdio.h>\n#include <stdlib.h>\n\nint cmp_desc(const void *a, const void *b) {\n    return *(int *)b - *(int *)a; // descending\n}\nint cmp_asc(const void *a, const void *b) {\n    return *(int *)a - *(int *)b; // ascending\n}\n\nint main() {\n    int T; if (scanf(\"%d\", &T)!=1) return 0;\n    while (T--) {\n        int N; scanf(\"%d\", &N);\n        int *C = (int*)malloc(N * sizeof(int));\n        for (int i = 0; i < N; ++i) scanf(\"%d\", &C[i]);\n        // collect odd (0‑blocks) and even (1‑blocks) separately\n        int oddCnt = (N + 1) / 2;\n        int evenCnt = N / 2;\n        int *odd = (int*)malloc(oddCnt * sizeof(int));\n        int *even = (int*)malloc(evenCnt * sizeof(int));\n        int oi = 0, ei = 0;\n        for (int i = 0; i < N; ++i) {\n            if ((i & 1) == 0) odd[oi++] = C[i]; // 0‑block\n            else even[ei++] = C[i]; // 1‑block\n        }\n        qsort(odd, oddCnt, sizeof(int), cmp_desc);\n        qsort(even, evenCnt, sizeof(int), cmp_asc);\n        // rebuild optimal array\n        oi = ei = 0;\n        long long ans = 0;\n        long long zeroCnt = 0;\n        for (int i = 0; i < N; ++i) {\n            if ((i & 1) == 0) {\n                C[i] = odd[oi++];\n                zeroCnt += C[i];\n            } else {\n                C[i] = even[ei++];\n                ans += zeroCnt * (long long)C[i];\n            }\n        }\n        // output\n        for (int i = 0; i < N; ++i) {\n            if (i) printf(\" \");\n            printf(\"%d\", C[i]);\n        }\n        printf(\"\\n%lld\\n\", ans);\n        free(C); free(odd); free(even);\n    }\n    return 0;\n}\n",
      "C++": "#include <bits/stdc++.h>\nusing namespace std;\nint main(){\n    ios::sync_with_stdio(false);\n    cin.tie(nullptr);\n    int T; if(!(cin>>T)) return 0;\n    while(T--){\n        int N; cin>>N;\n        vector<long long> C(N);\n        for(int i=0;i<N;++i) cin>>C[i];\n        vector<long long> odd, even;\n        for(int i=0;i<N;++i){\n            if(i%2==0) odd.push_back(C[i]); // 0‑block\n            else even.push_back(C[i]); // 1‑block\n        }\n        sort(odd.begin(), odd.end(), greater<long long>()); // descending\n        sort(even.begin(), even.end()); // ascending\n        long long zeroCnt=0, ans=0;\n        int oi=0, ei=0;\n        for(int i=0;i<N;++i){\n            if(i%2==0){\n                C[i]=odd[oi++];\n                zeroCnt+=C[i];\n            }else{\n                C[i]=even[ei++];\n                ans+=zeroCnt*C[i];\n            }\n        }\n        for(int i=0;i<N;++i){\n            if(i) cout<<' ';\n            cout<<C[i];\n        }\n        cout<<\"\\n\"<<ans<<\"\\n\";\n    }\n    return 0;\n}\n",
      "Java": "import java.io.*;\nimport java.util.*;\n\npublic class Main {\n    public static void main(String[] args) throws Exception {\n        FastScanner fs = new FastScanner(System.in);\n        int T = fs.nextInt();\n        StringBuilder out = new StringBuilder();\n        while (T-- > 0) {\n            int N = fs.nextInt();\n            long[] C = new long[N];\n            for (int i = 0; i < N; i++) C[i] = fs.nextLong();\n            List<Long> odd = new ArrayList<>(); // 0‑blocks\n            List<Long> even = new ArrayList<>(); // 1‑blocks\n            for (int i = 0; i < N; i++) {\n                if ((i & 1) == 0) odd.add(C[i]); else even.add(C[i]);\n            }\n            odd.sort(Collections.reverseOrder()); // descending\n            Collections.sort(even); // ascending\n            long zeroCnt = 0, ans = 0;\n            int oi = 0, ei = 0;\n            for (int i = 0; i < N; i++) {\n                if ((i & 1) == 0) {\n                    C[i] = odd.get(oi++);\n                    zeroCnt += C[i];\n                } else {\n                    C[i] = even.get(ei++);\n                    ans += zeroCnt * C[i];\n                }\n            }\n            for (int i = 0; i < N; i++) {\n                if (i > 0) out.append(' ');\n                out.append(C[i]);\n            }\n            out.append('\\n').append(ans).append('\\n');\n        }\n        System.out.print(out.toString());\n    }\n\n    // Fast scanner for competitive programming\n    static class FastScanner {\n        private final InputStream in;\n        private final byte[] buffer = new byte[1 << 16];\n        private int ptr = 0, len = 0;\n        FastScanner(InputStream in) { this.in = in; }\n        private int readByte() throws IOException {\n            if (ptr >= len) {\n                len = in.read(buffer);\n                ptr = 0;\n                if (len <= 0) return -1;\n            }\n            return buffer[ptr++];\n        }\n        long nextLong() throws IOException { return Long.parseLong(next()); }\n        int nextInt() throws IOException { return Integer.parseInt(next()); }\n        String next() throws IOException {\n            StringBuilder sb = new StringBuilder();\n            int c;\n            while ((c = readByte()) != -1 && c <= ' ') {}\n            if (c == -1) return \"\";\n            do { sb.append((char) c); } while ((c = readByte()) != -1 && c > ' ');\n            return sb.toString();\n        }\n    }\n}\n",
      "Python3": "#!/usr/bin/env python3\nimport sys\n\ndef solve():\n    data = sys.stdin.read().strip().split()\n    if not data:\n        return\n    it = iter(data)\n    T = int(next(it))\n    out_lines = []\n    for _ in range(T):\n        N = int(next(it))\n        C = [int(next(it)) for _ in range(N)]\n        odd = [C[i] for i in range(0, N, 2)]   # 0‑blocks\n        even = [C[i] for i in range(1, N, 2)]  # 1‑blocks\n        odd.sort(reverse=True)   # descending\n        even.sort()               # ascending\n        zero_cnt = 0\n        ans = 0\n        oi = ei = 0\n        for i in range(N):\n            if i % 2 == 0:\n                C[i] = odd[oi]\n                oi += 1\n                zero_cnt += C[i]\n            else:\n                C[i] = even[ei]\n                ei += 1\n                ans += zero_cnt * C[i]\n        out_lines.append(' '.join(map(str, C)))\n        out_lines.append(str(ans))\n    sys.stdout.write('\\n'.join(out_lines))\n\nif __name__ == \"__main__\":\n    solve()\n",
      "JavaScript": "const fs = require('fs');\nconst input = fs.readFileSync(0, 'utf-8').trim().split(/\\s+/);\nlet idx = 0;\nconst T = Number(input[idx++]);\nlet output = [];\nfor (let t = 0; t < T; ++t) {\n  const N = Number(input[idx++]);\n  const C = new Array(N);\n  for (let i = 0; i < N; ++i) C[i] = Number(input[idx++]);\n  const odd = [];\n  const even = [];\n  for (let i = 0; i < N; ++i) {\n    if (i % 2 === 0) odd.push(C[i]); else even.push(C[i]);\n  }\n  odd.sort((a, b) => b - a); // descending\n  even.sort((a, b) => a - b); // ascending\n  let zeroCnt = 0n;\n  let ans = 0n;\n  let oi = 0, ei = 0;\n  for (let i = 0; i < N; ++i) {\n    if (i % 2 === 0) {\n      C[i] = odd[oi++];\n      zeroCnt += BigInt(C[i]);\n    } else {\n      C[i] = even[ei++];\n      ans += zeroCnt * BigInt(C[i]);\n    }\n  }\n  output.push(C.join(' '));\n  output.push(ans.toString());\n}\nconsole.log(output.join('\\n'));\n"
    }
  },
  "QBCategory": "CCD",
  "boilerPlates": {
    "C": "#include <stdio.h>\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "C++": "#include <iostream>\nusing namespace std;\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "Java": "import java.util.*;\nimport java.io.*;\n\npublic class Main {\n    public static void main(String[] args) throws IOException {\n        // your code goes here\n    }\n}",
    "Python3": "# your code goes here\n",
    "JavaScript": "// your code goes here\n"
  },
  "_internal": {
    "methodName": "",
    "params": []
  }
}