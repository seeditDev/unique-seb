{
  "questionId": "Q5596",
  "title": "Queries on tree again!",
  "slug": "queries-on-tree-again",
  "version": 1,
  "createdAt": "2026-07-01",
  "updatedAt": "2026-07-04",
  "metadata": {
    "category": "Graph",
    "difficulty": "Hard",
    "isPremium": true,
    "tags": [
      "Shortest Path",
      "Dijkstra's Algorithm",
      "Graph Traversal"
    ],
    "companyTags": [],
    "topicTags": [
      "Trees"
    ],
    "QBCategory": "CCD"
  },
  "content": {
    "problemStatement": "You are given a simple connected graph **G** with **N** vertices and **N** edges (a simple graph is an un-directed graph that has no loops and no more than one edge between any two different vertices).\nIt is obvious that the graph **G** contains exactly one cycle and you can assume that the length of this cycle is odd (there are odd number of vertices in this cycle).\nThe vertices are numbered from 1 to **N**. Each edge is assigned a corresponding integer weight. \nYour mission is to stimulate two types of queries :\n\n\n\n\nUpdate query represented by **f u v**: change the sign of all the weights of the edges on the shortest path (you can see the definition of shortest path in this problem later on) from vertex **u** to vertex **v**.\n\n\nFind query represented by **? u v**: On the shortest path from vertex **u** to vertex **v**, find the (possibly empty) set of consecutive edges such that the sum of the weights is maximal. In other words, let's define the shortest path from **u** to **v** as **a_1, a_2, ..., a_k** (where **a_1** = **u** and **a_k** = **v**). You have to find **a_i** and **a_j** such that **i** &lt;= **j** and the sum of the weights of the edges of the path **a_i, a_(i + 1), ..., a_j** is as large as possible. You just have to find that sum.\n\n\n\n\nThe shortest path between two vertices **u** and **v** is the path connecting them with the minimal number of vertices. In this problem, it is obvious that there is only one shortest path between any pair of vertices of **G**.",
    "constraints": [
      "1 ≤ N ≤ 100,000",
      "1 ≤ u, v ≤ N",
      "-10,000 ≤ c ≤ 10,000",
      "1 ≤ Q ≤ 100,000"
    ],
    "inputFormat": "First line contains N, the number of vertices in the graph. Each of the next N lines represents an edge of the graph with three integers u v c, which mean there is an edge connecting vertices u and v and it has weight c. The next line contains an integer Q, the number of queries. Each of the next Q lines represents a query in one of the two forms: f u v to change the sign of all the weights of the edges on the shortest path from vertex u to vertex v, or ? u v to find the maximal sum of the weights of the edges on the shortest path from vertex u to vertex v.",
    "outputFormat": "For each find query, print the result of the query (the maximal sum) on a new line.",
    "sampleTestCases": [
      {
        "id": "sample_1",
        "input": "",
        "output": "Input:\n6\n1 2 1\n2 3 1\n3 1 1\n2 4 1\n4 5 1\n3 6 1\n3\n? 5 6\nf 2 3\n? 5 6\nOutput:\r\n4\r\n2",
        "explanation": ""
      }
    ]
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "label": "Case 1",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_2",
        "label": "Case 2",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_3",
        "label": "Case 3",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_4",
        "label": "Case 4",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_5",
        "label": "Case 5",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      }
    ],
    "totalTestCases": 5,
    "totalWeight": 10
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      2,
      2,
      2,
      2,
      2
    ],
    "maxScore": 10
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "To solve this problem, we can use Dijkstra's algorithm to find the shortest path between two vertices. We will maintain a graph with the given edges and weights. For each query, we will either update the weights of the edges on the shortest path or find the maximal sum of the weights on the shortest path.",
    "timeComplexity": "O(Q * N^2) per test case, O(T * Q * N^2) total. The time complexity is dominated by the Dijkstra's algorithm which is run for each query.",
    "spaceComplexity": "O(N^2) — the dominant structure is the adjacency matrix of the graph.",
    "code": {
      "C": "// COMPLETE, COMPILABLE C code\n// Uses correct I/O\n// Handles all T test cases\n#include <stdio.h>\n#include <stdlib.h>\n#include <limits.h>\n#define INF INT_MAX\n#define MIN(a, b) ((a) < (b) ? (a) : (b))\ntypedef struct Edge {\n    int u, v, w;\n} Edge;\n\n// Function to find the shortest path using Dijkstra's algorithm\nvoid dijkstra(int **graph, int *dist, int *prev, int n, int start) {\n    for (int i = 0; i < n; i++) {\n        dist[i] = INF;\n        prev[i] = -1;\n    }\n    dist[start] = 0;\n    for (int i = 0; i < n - 1; i++) {\n        for (int j = 0; j < n; j++) {\n            for (int k = 0; k < n; k++) {\n                if (graph[j][k] != INF && dist[j] != INF && dist[j] + graph[j][k] < dist[k]) {\n                    dist[k] = dist[j] + graph[j][k];\n                    prev[k] = j;\n                }\n            }\n        }\n    }\n}\n\nint main() {\n    int n, q;\n    scanf(\"%d\", &n);\n    int **graph = (int **)malloc(n * sizeof(int *));\n    for (int i = 0; i < n; i++) {\n        graph[i] = (int *)malloc(n * sizeof(int));\n        for (int j = 0; j < n; j++) {\n            graph[i][j] = INF;\n        }\n    }\n    for (int i = 0; i < n; i++) {\n        int u, v, w;\n        scanf(\"%d %d %d\", &u, &v, &w);\n        u--; v--;\n        graph[u][v] = w;\n        graph[v][u] = w;\n    }\n    scanf(\"%d\", &q);\n    for (int i = 0; i < q; i++) {\n        char query;\n        scanf(\" %c\", &query);\n        if (query == 'f') {\n            int u, v;\n            scanf(\"%d %d\", &u, &v);\n            u--; v--;\n            int *dist = (int *)malloc(n * sizeof(int));\n            int *prev = (int *)malloc(n * sizeof(int));\n            dijkstra(graph, dist, prev, n, u);\n            int current = v;\n            while (current != u) {\n                int next = prev[current];\n                graph[current][next] = -graph[current][next];\n                graph[next][current] = -graph[next][current];\n                current = next;\n            }\n            free(dist);\n            free(prev);\n        } else if (query == '?') {\n            int u, v;\n            scanf(\"%d %d\", &u, &v);\n            u--; v--;\n            int *dist = (int *)malloc(n * sizeof(int));\n            int *prev = (int *)malloc(n * sizeof(int));\n            dijkstra(graph, dist, prev, n, u);\n            int current = v;\n            int sum = 0;\n            int max_sum = INT_MIN;\n            int start = current;\n            while (current != u) {\n                int next = prev[current];\n                sum += graph[current][next];\n                max_sum = max_sum > sum ? max_sum : sum;\n                current = next;\n            }\n            for (int j = start; j != u; j = prev[j]) {\n                sum = 0;\n                current = j;\n                while (current != u) {\n                    int next = prev[current];\n                    sum += graph[current][next];\n                    max_sum = max_sum > sum ? max_sum : sum;\n                    current = next;\n                }\n            }\n            printf(\"%d\\n\", max_sum);\n            free(dist);\n            free(prev);\n        }\n    }\n    for (int i = 0; i < n; i++) {\n        free(graph[i]);\n    }\n    free(graph);\n    return 0;\n}",
      "C++": "// COMPLETE, COMPILABLE C++ code\n// Uses correct I/O\n// Handles all T test cases\n#include <iostream>\n#include <vector>\n#include <limits>\n#include <algorithm>\nusing namespace std;\n#define INF INT_MAX\n#define MIN(a, b) ((a) < (b) ? (a) : (b))\ntypedef pair<int, int> pii;\n\n// Function to find the shortest path using Dijkstra's algorithm\nvoid dijkstra(vector<vector<int>> &graph, vector<int> &dist, vector<int> &prev, int n, int start) {\n    for (int i = 0; i < n; i++) {\n        dist[i] = INF;\n        prev[i] = -1;\n    }\n    dist[start] = 0;\n    for (int i = 0; i < n - 1; i++) {\n        for (int j = 0; j < n; j++) {\n            for (int k = 0; k < n; k++) {\n                if (graph[j][k] != INF && dist[j] != INF && dist[j] + graph[j][k] < dist[k]) {\n                    dist[k] = dist[j] + graph[j][k];\n                    prev[k] = j;\n                }\n            }\n        }\n    }\n}\n\nint main() {\n    int n, q;\n    cin >> n;\n    vector<vector<int>> graph(n, vector<int>(n, INF));\n    for (int i = 0; i < n; i++) {\n        int u, v, w;\n        cin >> u >> v >> w;\n        u--; v--;\n        graph[u][v] = w;\n        graph[v][u] = w;\n    }\n    cin >> q;\n    for (int i = 0; i < q; i++) {\n        char query;\n        cin >> query;\n        if (query == 'f') {\n            int u, v;\n            cin >> u >> v;\n            u--; v--;\n            vector<int> dist(n, INF);\n            vector<int> prev(n, -1);\n            dijkstra(graph, dist, prev, n, u);\n            int current = v;\n            while (current != u) {\n                int next = prev[current];\n                graph[current][next] = -graph[current][next];\n                graph[next][current] = -graph[next][current];\n                current = next;\n            }\n        } else if (query == '?') {\n            int u, v;\n            cin >> u >> v;\n            u--; v--;\n            vector<int> dist(n, INF);\n            vector<int> prev(n, -1);\n            dijkstra(graph, dist, prev, n, u);\n            int current = v;\n            int sum = 0;\n            int max_sum = INT_MIN;\n            int start = current;\n            while (current != u) {\n                int next = prev[current];\n                sum += graph[current][next];\n                max_sum = max_sum > sum ? max_sum : sum;\n                current = next;\n            }\n            for (int j = start; j != u; j = prev[j]) {\n                sum = 0;\n                current = j;\n                while (current != u) {\n                    int next = prev[current];\n                    sum += graph[current][next];\n                    max_sum = max_sum > sum ? max_sum : sum;\n                    current = next;\n                }\n            }\n            cout << max_sum << endl;\n        }\n    }\n    return 0;\n}",
      "Java": "// COMPLETE, COMPILABLE Java code\n// Class MUST be named Main\n// Uses correct I/O\n// Handles all T test cases\nimport java.util.Scanner;\nimport java.util.Arrays;\npublic class Main {\n    static final int INF = Integer.MAX_VALUE;\n    static final int MIN = -INF;\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int n = sc.nextInt();\n        int[][] graph = new int[n][n];\n        for (int i = 0; i < n; i++) {\n            Arrays.fill(graph[i], INF);\n        }\n        for (int i = 0; i < n; i++) {\n            int u = sc.nextInt() - 1;\n            int v = sc.nextInt() - 1;\n            int w = sc.nextInt();\n            graph[u][v] = w;\n            graph[v][u] = w;\n        }\n        int q = sc.nextInt();\n        for (int i = 0; i < q; i++) {\n            char query = sc.next().charAt(0);\n            if (query == 'f') {\n                int u = sc.nextInt() - 1;\n                int v = sc.nextInt() - 1;\n                int[] dist = new int[n];\n                int[] prev = new int[n];\n                Arrays.fill(dist, INF);\n                Arrays.fill(prev, -1);\n                dijkstra(graph, dist, prev, n, u);\n                int current = v;\n                while (current != u) {\n                    int next = prev[current];\n                    graph[current][next] = -graph[current][next];\n                    graph[next][current] = -graph[next][current];\n                    current = next;\n                }\n            } else if (query == '?') {\n                int u = sc.nextInt() - 1;\n                int v = sc.nextInt() - 1;\n                int[] dist = new int[n];\n                int[] prev = new int[n];\n                Arrays.fill(dist, INF);\n                Arrays.fill(prev, -1);\n                dijkstra(graph, dist, prev, n, u);\n                int current = v;\n                int sum = 0;\n                int max_sum = MIN;\n                int start = current;\n                while (current != u) {\n                    int next = prev[current];\n                    sum += graph[current][next];\n                    max_sum = Math.max(max_sum, sum);\n                    current = next;\n                }\n                for (int j = start; j != u; j = prev[j]) {\n                    sum = 0;\n                    current = j;\n                    while (current != u) {\n                        int next = prev[current];\n                        sum += graph[current][next];\n                        max_sum = Math.max(max_sum, sum);\n                        current = next;\n                    }\n                }\n                System.out.println(max_sum);\n            }\n        }\n    }\n    static void dijkstra(int[][] graph, int[] dist, int[] prev, int n, int start) {\n        dist[start] = 0;\n        for (int i = 0; i < n - 1; i++) {\n            for (int j = 0; j < n; j++) {\n                for (int k = 0; k < n; k++) {\n                    if (graph[j][k] != INF && dist[j] != INF && dist[j] + graph[j][k] < dist[k]) {\n                        dist[k] = dist[j] + graph[j][k];\n                        prev[k] = j;\n                    }\n                }\n            }\n        }\n    }\n}",
      "Python3": "# COMPLETE, RUNNABLE Python3 code\n# Uses input() or sys.stdin\n# Handles all T test cases\nimport sys\ninput = sys.stdin.readline\nINF = float('inf')\ndef dijkstra(graph, dist, prev, n, start):\n    dist[start] = 0\n    for _ in range(n - 1):\n        for j in range(n):\n            for k in range(n):\n                if graph[j][k] != INF and dist[j] != INF and dist[j] + graph[j][k] < dist[k]:\n                    dist[k] = dist[j] + graph[j][k]\n                    prev[k] = j\nn = int(input())\ngraph = [[INF] * n for _ in range(n)]\nfor _ in range(n):\n    u, v, w = map(int, input().split())\n    u -= 1\n    v -= 1\n    graph[u][v] = w\n    graph[v][u] = w\nq = int(input())\nfor _ in range(q):\n    query = input().split()[0]\n    if query == 'f':\n        u, v = map(int, input().split())\n        u -= 1\n        v -= 1\n        dist = [INF] * n\n        prev = [-1] * n\n        dijkstra(graph, dist, prev, n, u)\n        current = v\n        while current != u:\n            next = prev[current]\n            graph[current][next] = -graph[current][next]\n            graph[next][current] = -graph[next][current]\n            current = next\n    elif query == '?':\n        u, v = map(int, input().split())\n        u -= 1\n        v -= 1\n        dist = [INF] * n\n        prev = [-1] * n\n        dijkstra(graph, dist, prev, n, u)\n        current = v\n        sum = 0\n        max_sum = -INF\n        start = current\n        while current != u:\n            next = prev[current]\n            sum += graph[current][next]\n            max_sum = max(max_sum, sum)\n            current = next\n        for j in range(start, u, -1):\n            sum = 0\n            current = j\n            while current != u:\n                next = prev[current]\n                sum += graph[current][next]\n                max_sum = max(max_sum, sum)\n                current = next\n        print(max_sum)",
      "JavaScript": ""
    }
  },
  "QBCategory": "CCD",
  "boilerPlates": {
    "C": "#include <stdio.h>\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "C++": "#include <iostream>\nusing namespace std;\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "Java": "import java.util.*;\nimport java.io.*;\n\npublic class Main {\n    public static void main(String[] args) throws IOException {\n        // your code goes here\n    }\n}",
    "Python3": "# your code goes here\n",
    "JavaScript": "// your code goes here\n"
  },
  "_internal": {
    "methodName": "",
    "params": []
  }
}