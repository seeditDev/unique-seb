{
  "questionId": "Q11376",
  "title": "Merge Intervals",
  "slug": "merge-intervals",
  "version": 1,
  "createdAt": "2026-07-05",
  "updatedAt": "2026-07-05",
  "metadata": {
    "category": "Array",
    "difficulty": "Medium",
    "isPremium": true,
    "tags": [
      "Sorting",
      "Greedy"
    ],
    "companyTags": [],
    "topicTags": [],
    "QBCategory": "CCD"
  },
  "content": {
    "problemStatement": "Given an array of intervals where intervals[i] = [starti, endi], merge all overlapping intervals and return an array of the non-overlapping intervals that cover all the intervals in the input.",
    "constraints": [
      "1 <= intervals.length <= 10^4",
      "intervals[i].length == 2",
      "0 <= starti <= endi <= 10^4"
    ],
    "inputFormat": "intervals = [[start1, end1], [start2, end2], ... , [startN, endN]] where starti and endi are integers",
    "outputFormat": "[[start1, end1], [start2, end2], ... , [startM, endM]] where starti and endi are integers and M <= N",
    "sampleTestCases": []
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "label": "min boundary",
        "input": "[[1, 2]]",
        "expectedOutput": "[[1, 2]]",
        "weight": 2
      },
      {
        "id": "tc_2",
        "label": "max boundary",
        "input": "[[0, 10000], [1, 10000], [2, 10000]]",
        "expectedOutput": "[[0, 10000]]",
        "weight": 2
      },
      {
        "id": "tc_3",
        "label": "uniform/edge cases",
        "input": "[[1, 1], [2, 2], [3, 3]]",
        "expectedOutput": "[[1, 1], [2, 2], [3, 3]]",
        "weight": 2
      },
      {
        "id": "tc_4",
        "label": "tricky case / empty-like",
        "input": "[]",
        "expectedOutput": "[]",
        "weight": 2
      },
      {
        "id": "tc_5",
        "label": "general mixed case",
        "input": "[[1, 3], [2, 6], [8, 10], [15, 18]]",
        "expectedOutput": "[[1, 6], [8, 10], [15, 18]]",
        "weight": 2
      }
    ],
    "totalTestCases": 5,
    "totalWeight": 10
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      2,
      2,
      2,
      2,
      2
    ],
    "maxScore": 10
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "Sort intervals by start time and then merge overlapping intervals",
    "timeComplexity": "O(N log N) due to sorting",
    "spaceComplexity": "O(N) for storing the result",
    "code": {
      "C": "#include <stdio.h>\n#include <stdlib.h>\n\n// Structure for an interval\ntypedef struct Interval {\n    int start;\n    int end;\n} Interval;\n\n// Function to compare intervals based on start time\nint compareIntervals(const void *a, const void *b) {\n    Interval *intervalA = (Interval *)a;\n    Interval *intervalB = (Interval *)b;\n    return intervalA->start - intervalB->start;\n}\n\n// Function to merge overlapping intervals\nInterval* mergeIntervals(Interval *intervals, int intervalsSize, int *returnSize) {\n    // Sort intervals based on start time\n    qsort(intervals, intervalsSize, sizeof(Interval), compareIntervals);\n\n    // Initialize result array\n    Interval *result = (Interval *)malloc(intervalsSize * sizeof(Interval));\n    *returnSize = 0;\n\n    // Loop through each interval\n    int i = 0;\n    while (i < intervalsSize) {\n        // Start of current merged interval\n        int start = intervals[i].start;\n        int end = intervals[i].end;\n\n        // Merge with all overlapping intervals\n        int j = i + 1;\n        while (j < intervalsSize && intervals[j].start <= end) {\n            // Update end to the maximum of current end and overlapping interval's end\n            end = (end > intervals[j].end) ? end : intervals[j].end;\n            j++;\n        }\n\n        // Add the merged interval to result\n        result[(*returnSize)++] = (Interval){start, end};\n\n        // Move to the next non-overlapping interval\n        i = j;\n    }\n\n    return result;\n}\n\nint main() {\n    // Example usage\n    Interval intervals[] = {{1, 3}, {2, 6}, {8, 10}, {15, 18}};\n    int intervalsSize = sizeof(intervals) / sizeof(intervals[0]);\n    int returnSize;\n    Interval *result = mergeIntervals(intervals, intervalsSize, &returnSize);\n\n    // Print the result\n    for (int i = 0; i < returnSize; i++) {\n        printf(\"[%d, %d] \", result[i].start, result[i].end);\n    }\n    printf(\"\\n\");\n\n    free(result);\n    return 0;\n}",
      "C++": "#include <bits/stdc++.h>\nusing namespace std;\n\nclass Solution {\npublic:\n    // Function to merge overlapping intervals using brute force\n    vector<vector<int>> merge(vector<vector<int>>& intervals) {\n\n        // Sort intervals based on start time\n        sort(intervals.begin(), intervals.end());\n\n        // Result array to store merged intervals\n        vector<vector<int>> ans;\n\n        // Loop through each interval\n        int n = intervals.size();\n        for (int i = 0; i < n; ) {\n\n            // Start of current merged interval\n            int start = intervals[i][0];\n            int end = intervals[i][1];\n\n            // Merge with all overlapping intervals\n            int j = i + 1;\n            while (j < n && intervals[j][0] <= end) {\n                // Update end to the maximum of current end and overlapping interval's end\n                end = max(end, intervals[j][1]);\n                j++;\n            }\n\n            // Add the merged interval to result\n            ans.push_back({start, end});\n\n            // Move to the next non-overlapping interval\n            i = j;\n        }\n\n        return ans;\n    }\n};\n\nint main() {\n    Solution sol;\n    vector<vector<int>> intervals = {{1,3}, {2,6}, {8,10}, {15,18}};\n    vector<vector<int>> result = sol.merge(intervals);\n\n    for (auto interval : result) {\n        cout << \"[\" << interval[0] << \",\" << interval[1] << \"] \";\n    }\n    return 0;\n}",
      "Java": "import java.util.*;\n\nclass Solution {\n    // Function to merge overlapping intervals using brute force\n    public List<List<Integer>> merge(int[][] intervals) {\n\n        // Sort intervals based on starting point\n        Arrays.sort(intervals, (a, b) -> a[0] - b[0]);\n\n        List<List<Integer>> ans = new ArrayList<>();\n\n        int n = intervals.length;\n        int i = 0;\n\n        // Loop through intervals\n        while (i < n) {\n            // Start of merged interval\n            int start = intervals[i][0];\n            int end = intervals[i][1];\n\n            int j = i + 1;\n\n            // Check all overlapping intervals\n            while (j < n && intervals[j][0] <= end) {\n                // Extend the end of current interval\n                end = Math.max(end, intervals[j][1]);\n                j++;\n            }\n\n            // Add merged interval to result\n            ans.add(Arrays.asList(start, end));\n\n            // Move to next non-overlapping interval\n            i = j;\n        }\n\n        return ans;\n    }\n}\n\npublic class Main {\n    public static void main(String[] args) {\n        Solution sol = new Solution();\n        int[][] intervals = {{1, 3}, {2, 6}, {8, 10}, {15, 18}};\n        List<List<Integer>> result = sol.merge(intervals);\n        for (List<Integer> interval : result) {\n            System.out.print(interval + \" \");\n        }\n    }\n}",
      "Python3": "from typing import List\n\nclass Solution:\n    # Function to merge overlapping intervals using brute force\n    def merge(self, intervals: List[List[int]]) -> List[List[int]]:\n\n        # Sort the intervals based on the start time\n        intervals.sort()\n\n        ans = []\n        n = len(intervals)\n        i = 0\n\n        # Loop through each interval\n        while i < n:\n            # Take current interval's start and end\n            start = intervals[i][0]\n            end = intervals[i][1]\n\n            j = i + 1\n\n            # Merge with all intervals that overlap\n            while j < n and intervals[j][0] <= end:\n                # Extend the end if overlapping\n                end = max(end, intervals[j][1])\n                j += 1\n\n            # Append merged interval to result\n            ans.append([start, end])\n\n            # Move to the next non-overlapping interval\n            i = j\n\n        return ans\n\n# Driver code\nsol = Solution()\nintervals = [[1,3],[2,6],[8,10],[15,18]]\nprint(sol.merge(intervals))",
      "JavaScript": "class Solution {\n    // Function to merge overlapping intervals using brute force\n    merge(intervals) {\n        // Sort intervals by starting point\n        intervals.sort((a, b) => a[0] - b[0]);\n\n        const ans = [];\n        let i = 0;\n        const n = intervals.length;\n\n        // Loop through all intervals\n        while (i < n) {\n            // Take current interval's start and end\n            let start = intervals[i][0];\n            let end = intervals[i][1];\n\n            let j = i + 1;\n\n            // Merge overlapping intervals\n            while (j < n && intervals[j][0] <= end) {\n                // Extend the end as needed\n                end = Math.max(end, intervals[j][1]);\n                j++;\n            }\n\n            // Push merged interval to result\n            ans.push([start, end]);\n\n            // Move to next non-overlapping interval\n            i = j;\n        }\n\n        return ans;\n    }\n}\n\n// Driver code\nconst sol = new Solution();\nconst intervals = [[1, 3], [2, 6], [8, 10], [15, 18]];\nconsole.log(sol.merge(intervals));"
    }
  },
  "QBCategory": "CCD",
  "boilerPlates": {
    "C": "#include <stdio.h>\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "C++": "#include <iostream>\nusing namespace std;\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "Java": "import java.util.*;\nimport java.io.*;\n\npublic class Main {\n    public static void main(String[] args) throws IOException {\n        // your code goes here\n    }\n}",
    "Python3": "# your code goes here\n",
    "JavaScript": "// your code goes here\n"
  },
  "_internal": {
    "methodName": "",
    "params": []
  }
}