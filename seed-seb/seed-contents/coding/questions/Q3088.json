{
  "questionId": "Q3088",
  "title": "Devu and Manhattan Distance",
  "slug": "devu-and-manhattan-distance",
  "version": 1,
  "createdAt": "2026-07-01",
  "updatedAt": "2026-07-04",
  "metadata": {
    "category": "Math",
    "difficulty": "Hard",
    "isPremium": false,
    "tags": [
      "Coordinate Geometry",
      "Manhattan Distance",
      "Math"
    ],
    "companyTags": [],
    "topicTags": [
      "Data Structures"
    ]
  },
  "content": {
    "problemStatement": "Devu has taken the course of computational geometry in the university of Coderpur. He liked this course very much. Today, he got to know about the Manhattan distance and got an assignment for the same topic. Manhattan distance between two points **(x1,y1)** and **(x2,y2)** can be defined as:\n\n\n\n**\nManhattan Distance = abs(x1-x2) + abs(y1-y2)\n**\n\n\n\nDevu has N points in the Co-ordinate plane. Co-ordinate of **ith** point is **(xi,yi)**. Points are indexed from **0** to **(N-1)**(both inclusive).\n\n\n \nHe has **Q** updates or queries, for each update **U[i, x,y]**, change the co-ordinate of **ith** point to **(x,y)**. \n\n\n\nFor each query **[L,R]**, find pair of points whose indices are between **L** and **R**(both inclusive) , such that Manhattan distance between them is maximized. Report the maximum Manhattan distance for each of the query. \n\n\n\nDevu has to submit the assignment on the Monday, so help him in solving the problem.",
    "constraints": [
      "1 <= N <= 10^5",
      "1 <= Q <= 10^5",
      "-10^9 <= x, y <= 10^9",
      "0 <= L <= R <= N-1"
    ],
    "inputFormat": "The first line contains an integer N, the number of points in the Cartesian plane. The next N lines contain the coordinates of the N points. The next line contains an integer Q, the number of queries. The next Q lines contain either 'U' followed by the index and new coordinates of a point, or 'Q' followed by the range of indices for a query.",
    "outputFormat": "For each query, the maximum Manhattan distance between any two points in the given range of indices.",
    "sampleTestCases": [
      {
        "id": "sample_1",
        "input": "4\n10 10\n10 20\n20 20\n20 10\n2\nU 1 20 20\nQ 0 3",
        "output": "20",
        "explanation": "After the first update, the points are (10,10), (20,20), (20,20), and (20,10). The maximum Manhattan distance between any two points in the range [0,3] is 20, which is the distance between points (10,10) and (20,20)."
      },
      {
        "id": "sample_2",
        "input": "3\n0 0\n1 1\n2 2\n2\nQ 0 2\nU 1 3 3\nQ 0 2",
        "output": "2\n5",
        "explanation": "Initially, the maximum Manhattan distance between any two points in the range [0,2] is 2, which is the distance between points (0,0) and (2,2). After the update, the point (1,1) is moved to (3,3), so the maximum Manhattan distance between any two points in the range [0,2] is 5, which is the distance between points (0,0) and (3,3)."
      }
    ]
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "label": "minimum boundary",
        "input": "1\n0 0\n1\nQ 0 0",
        "expectedOutput": "0",
        "weight": 2
      },
      {
        "id": "tc_2",
        "label": "maximum boundary",
        "input": "100000\n0 0\n1 1\n2 2\n... 99999 99999\n1\nQ 0 99999",
        "expectedOutput": "199998",
        "weight": 2
      },
      {
        "id": "tc_3",
        "label": "edge case",
        "input": "2\n0 0\n1 1\n2\nQ 0 1\nU 0 2 2\nQ 0 1",
        "expectedOutput": "2\n4",
        "weight": 2
      },
      {
        "id": "tc_4",
        "label": "Case 4",
        "input": "1\n0 0\n1\nQ 0 0",
        "expectedOutput": "0",
        "weight": 2
      },
      {
        "id": "tc_5",
        "label": "Case 5",
        "input": "1\n0 0\n1\nQ 0 0",
        "expectedOutput": "0",
        "weight": 2
      }
    ],
    "totalTestCases": 5,
    "totalWeight": 10
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      2,
      2,
      2,
      2,
      2
    ],
    "maxScore": 10
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "To solve this problem, we can use a simple iterative approach. For each query, we iterate over all pairs of points in the given range of indices and calculate the Manhattan distance between them. We keep track of the maximum Manhattan distance found so far. If the query is an update, we update the coordinates of the point at the given index.",
    "timeComplexity": "O(Q * N^2) \u2014 for each query, we iterate over all pairs of points in the given range of indices",
    "spaceComplexity": "O(N) \u2014 we need to store the coordinates of all points",
    "code": {
      "C": "#include <stdio.h>\n#include <stdlib.h>\n#include <math.h>\n\nint maxManhattanDistance(int** points, int N, int L, int R) {\n    int maxDistance = 0;\n    for (int i = L; i <= R; i++) {\n        for (int j = i + 1; j <= R; j++) {\n            int distance = abs(points[i][0] - points[j][0]) + abs(points[i][1] - points[j][1]);\n            if (distance > maxDistance) {\n                maxDistance = distance;\n            }\n        }\n    }\n    return maxDistance;\n}\n\nint main() {\n    int N;\n    scanf(\"%d\", &N);\n    int** points = (int**)malloc(N * sizeof(int*));\n    for (int i = 0; i < N; i++) {\n        points[i] = (int*)malloc(2 * sizeof(int));\n        scanf(\"%d %d\", &points[i][0], &points[i][1]);\n    }\n    int Q;\n    scanf(\"%d\", &Q);\n    for (int i = 0; i < Q; i++) {\n        char query;\n        scanf(\" %c\", &query);\n        if (query == 'U') {\n            int index, x, y;\n            scanf(\"%d %d %d\", &index, &x, &y);\n            points[index][0] = x;\n            points[index][1] = y;\n        } else if (query == 'Q') {\n            int L, R;\n            scanf(\"%d %d\", &L, &R);\n            printf(\"%d\\n\", maxManhattanDistance(points, N, L, R));\n        }\n    }\n    return 0;\n}",
      "C++": "#include <iostream>\n#include <vector>\n#include <cmath>\nusing namespace std;\n\nint maxManhattanDistance(vector<vector<int>>& points, int L, int R) {\n    int maxDistance = 0;\n    for (int i = L; i <= R; i++) {\n        for (int j = i + 1; j <= R; j++) {\n            int distance = abs(points[i][0] - points[j][0]) + abs(points[i][1] - points[j][1]);\n            if (distance > maxDistance) {\n                maxDistance = distance;\n            }\n        }\n    }\n    return maxDistance;\n}\n\nint main() {\n    int N;\n    cin >> N;\n    vector<vector<int>> points(N, vector<int>(2));\n    for (int i = 0; i < N; i++) {\n        cin >> points[i][0] >> points[i][1];\n    }\n    int Q;\n    cin >> Q;\n    for (int i = 0; i < Q; i++) {\n        char query;\n        cin >> query;\n        if (query == 'U') {\n            int index, x, y;\n            cin >> index >> x >> y;\n            points[index][0] = x;\n            points[index][1] = y;\n        } else if (query == 'Q') {\n            int L, R;\n            cin >> L >> R;\n            cout << maxManhattanDistance(points, L, R) << endl;\n        }\n    }\n    return 0;\n}",
      "Java": "import java.util.Scanner;\npublic class Main {\n    public static int maxManhattanDistance(int[][] points, int L, int R) {\n        int maxDistance = 0;\n        for (int i = L; i <= R; i++) {\n            for (int j = i + 1; j <= R; j++) {\n                int distance = Math.abs(points[i][0] - points[j][0]) + Math.abs(points[i][1] - points[j][1]);\n                if (distance > maxDistance) {\n                    maxDistance = distance;\n                }\n            }\n        }\n        return maxDistance;\n    }\n\n    public static void main(String[] args) {\n        Scanner scanner = new Scanner(System.in);\n        int N = scanner.nextInt();\n        int[][] points = new int[N][2];\n        for (int i = 0; i < N; i++) {\n            points[i][0] = scanner.nextInt();\n            points[i][1] = scanner.nextInt();\n        }\n        int Q = scanner.nextInt();\n        for (int i = 0; i < Q; i++) {\n            char query = scanner.next().charAt(0);\n            if (query == 'U') {\n                int index = scanner.nextInt();\n                int x = scanner.nextInt();\n                int y = scanner.nextInt();\n                points[index][0] = x;\n                points[index][1] = y;\n            } else if (query == 'Q') {\n                int L = scanner.nextInt();\n                int R = scanner.nextInt();\n                System.out.println(maxManhattanDistance(points, L, R));\n            }\n        }\n    }\n}",
      "Python3": "def maxManhattanDistance(points, L, R):\n    maxDistance = 0\n    for i in range(L, R + 1):\n        for j in range(i + 1, R + 1):\n            distance = abs(points[i][0] - points[j][0]) + abs(points[i][1] - points[j][1])\n            if distance > maxDistance:\n                maxDistance = distance\n    return maxDistance\n\nN = int(input())\npoints = [list(map(int, input().split())) for _ in range(N)]\nQ = int(input())\nfor _ in range(Q):\n    query = input().split()\n    if query[0] == 'U':\n        index, x, y = map(int, query[1:])\n        points[index] = [x, y]\n    elif query[0] == 'Q':\n        L, R = map(int, query[1:])\n        print(maxManhattanDistance(points, L, R))",
      "JavaScript": "const fs = require('fs');\nconst lines = fs.readFileSync(0, 'utf-8').trim().split('\\n');\nlet idx = 0;\n\nfunction maxManhattanDistance(points, L, R) {\n    let maxDistance = 0;\n    for (let i = L; i <= R; i++) {\n        for (let j = i + 1; j <= R; j++) {\n            let distance = Math.abs(points[i][0] - points[j][0]) + Math.abs(points[i][1] - points[j][1]);\n            if (distance > maxDistance) {\n                maxDistance = distance;\n            }\n        }\n    }\n    return maxDistance;\n}\n\nlet N = parseInt(lines[idx++]);\nlet points = [];\nfor (let i = 0; i < N; i++) {\n    points.push(lines[idx++].split(' ').map(Number));\n}\nlet Q = parseInt(lines[idx++]);\nfor (let i = 0; i < Q; i++) {\n    let query = lines[idx++].split(' ');\n    if (query[0] === 'U') {\n        let index = parseInt(query[1]);\n        let x = parseInt(query[2]);\n        let y = parseInt(query[3]);\n        points[index] = [x, y];\n    } else if (query[0] === 'Q') {\n        let L = parseInt(query[1]);\n        let R = parseInt(query[2]);\n        console.log(maxManhattanDistance(points, L, R));\n    }\n}"
    }
  }
}