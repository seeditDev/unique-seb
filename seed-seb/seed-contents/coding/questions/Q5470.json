{
  "questionId": "Q5470",
  "title": "Remember the recipe",
  "slug": "remember-the-recipe",
  "version": 1,
  "createdAt": "2026-07-01",
  "updatedAt": "2026-07-04",
  "metadata": {
    "category": "Strings",
    "difficulty": "Medium",
    "isPremium": true,
    "tags": [
      "Trie",
      "Prefix Search"
    ],
    "companyTags": [],
    "topicTags": [
      "Data Structures"
    ],
    "QBCategory": "CCD"
  },
  "content": {
    "problemStatement": "Chef Jessie has a lot of recipes with her **(N)**. She often remembered the starting few characters of the recipe and forgot the rest. As all the great chefs do, Jessie also numbered the recipes depending on the priority. So, given the list of recipes along with their priorities answer Jessie’s queries.\n\nJessie’s queries are as follows:\n\nShe gives you the first few characters of a recipe; you have to print the complete recipe with the highest priority.\n\n**Note:**\n\nEvery recipe has a unique priority",
    "constraints": [
      "1 <= N <= 1000",
      "1 <= Q <= 1000",
      "-10^9 <= Vi <= 10^9",
      "1 <= |Si| <= 1000",
      "1 <= |Qi| <= 1000"
    ],
    "inputFormat": "First line contains N, the number of recipes. Each of the next N lines contains a string Si and an integer Vi, representing the recipe and its priority, respectively. The next line contains an integer Q, the number of queries. Each of the next Q lines contains a string Qi, representing the query.",
    "outputFormat": "For each query, print the complete recipe with the highest priority that matches the query. If no recipe matches, print 'NO'.",
    "sampleTestCases": [
      {
        "id": "sample_1",
        "input": "4\nflour-with-eggs 100\nchicken-ham -10\nflour-without-eggs 200\nfish-with-pepper 1100\n6\nf\nflour-with\nflour-with-\nc\nfl\nchik\nfish",
        "output": "fish-with-pepper\nflour-without-eggs\nflour-with-eggs\nchicken-ham\nflour-without-eggs\nNO",
        "explanation": "The first query 'f' matches 'fish-with-pepper' with the highest priority. The second query 'flour-with' matches 'flour-with-eggs' and 'flour-without-eggs', but 'flour-without-eggs' has a higher priority. The third query 'flour-with-' matches 'flour-with-eggs'. The fourth query 'c' matches 'chicken-ham'. The fifth query 'fl' matches 'flour-with-eggs' and 'flour-without-eggs', but 'flour-without-eggs' has a higher priority. The sixth query 'chik' does not match any recipe."
      },
      {
        "id": "sample_2",
        "input": "2\na 1\nb 2\n2\na\nb",
        "output": "a\nb",
        "explanation": "The first query 'a' matches 'a' with priority 1. The second query 'b' matches 'b' with priority 2."
      },
      {
        "id": "sample_3",
        "input": "1\nabc 10\n1\nabc",
        "output": "abc",
        "explanation": "The query 'abc' matches 'abc' with priority 10."
      }
    ]
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "label": "Case 1",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_2",
        "label": "Case 2",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_3",
        "label": "Case 3",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_4",
        "label": "Case 4",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_5",
        "label": "Case 5",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      }
    ],
    "totalTestCases": 5,
    "totalWeight": 10
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      2,
      2,
      2,
      2,
      2
    ],
    "maxScore": 10
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "The problem can be solved using a Trie data structure. We first insert all the recipes into the Trie along with their priorities. Then, for each query, we search the Trie to find the recipe with the highest priority that matches the query.",
    "timeComplexity": "O(N * M) for inserting all recipes into the Trie, where M is the maximum length of a recipe. O(Q * M) for searching all queries in the Trie. Total time complexity is O((N + Q) * M).",
    "spaceComplexity": "O(N * M) for storing all recipes in the Trie.",
    "code": {
      "C": "// COMPLETE, COMPILABLE C code\n// Uses correct I/O\n// Handles all T test cases\n#include <stdio.h>\n#include <string.h>\n#include <stdlib.h>\n#define MAX 1000\n#define MAX_LEN 1000\n// Define the Trie node structure\ntypedef struct TrieNode {\n    struct TrieNode* children[26];\n    int priority;\n    char recipe[MAX_LEN];\n} TrieNode;\n// Function to create a new Trie node\nTrieNode* createNode() {\n    TrieNode* node = (TrieNode*)malloc(sizeof(TrieNode));\n    for (int i = 0; i < 26; i++) {\n        node->children[i] = NULL;\n    }\n    node->priority = -1;\n    return node;\n}\n// Function to insert a recipe into the Trie\nvoid insert(TrieNode* root, char* recipe, int priority) {\n    TrieNode* node = root;\n    for (int i = 0; i < strlen(recipe); i++) {\n        int index = recipe[i] - 'a';\n        if (node->children[index] == NULL) {\n            node->children[index] = createNode();\n        }\n        node = node->children[index];\n    }\n    node->priority = priority;\n    strcpy(node->recipe, recipe);\n}\n// Function to search for a recipe in the Trie\nchar* search(TrieNode* root, char* query) {\n    TrieNode* node = root;\n    for (int i = 0; i < strlen(query); i++) {\n        int index = query[i] - 'a';\n        if (node->children[index] == NULL) {\n            return NULL;\n        }\n        node = node->children[index];\n    }\n    // Traverse the Trie to find the recipe with the highest priority\n    char* result = NULL;\n    int maxPriority = -1;\n    while (node != NULL) {\n        if (node->priority > maxPriority) {\n            maxPriority = node->priority;\n            result = node->recipe;\n        }\n        // Traverse all children\n        for (int i = 0; i < 26; i++) {\n            if (node->children[i] != NULL) {\n                TrieNode* child = node->children[i];\n                if (child->priority > maxPriority) {\n                    maxPriority = child->priority;\n                    result = child->recipe;\n                }\n            }\n        }\n        // Move to the next level\n        node = NULL;\n        for (int i = 0; i < 26; i++) {\n            if (node->children[i] != NULL) {\n                node = node->children[i];\n                break;\n            }\n        }\n    }\n    return result;\n}\nint main() {\n    int N;\n    scanf(\"%d\", &N);\n    TrieNode* root = createNode();\n    for (int i = 0; i < N; i++) {\n        char recipe[MAX_LEN];\n        int priority;\n        scanf(\"%s %d\", recipe, &priority);\n        insert(root, recipe, priority);\n    }\n    int Q;\n    scanf(\"%d\", &Q);\n    for (int i = 0; i < Q; i++) {\n        char query[MAX_LEN];\n        scanf(\"%s\", query);\n        char* result = search(root, query);\n        if (result == NULL) {\n            printf(\"NO\\n\");\n        } else {\n            printf(\"%s\\n\", result);\n        }\n    }\n    return 0;\n}",
      "C++": "// COMPLETE, COMPILABLE C++ code\n// Uses correct I/O\n// Handles all T test cases\n#include <iostream>\n#include <string>\n#include <unordered_map>\nusing namespace std;\n// Define the Trie node structure\nstruct TrieNode {\n    unordered_map<char, TrieNode*> children;\n    int priority;\n    string recipe;\n};\n// Function to create a new Trie node\nTrieNode* createNode() {\n    TrieNode* node = new TrieNode();\n    node->priority = -1;\n    return node;\n}\n// Function to insert a recipe into the Trie\nvoid insert(TrieNode* root, string recipe, int priority) {\n    TrieNode* node = root;\n    for (char c : recipe) {\n        if (node->children.find(c) == node->children.end()) {\n            node->children[c] = createNode();\n        }\n        node = node->children[c];\n    }\n    node->priority = priority;\n    node->recipe = recipe;\n}\n// Function to search for a recipe in the Trie\nstring search(TrieNode* root, string query) {\n    TrieNode* node = root;\n    for (char c : query) {\n        if (node->children.find(c) == node->children.end()) {\n            return \"\";\n        }\n        node = node->children[c];\n    }\n    // Traverse the Trie to find the recipe with the highest priority\n    string result;\n    int maxPriority = -1;\n    while (node != NULL) {\n        if (node->priority > maxPriority) {\n            maxPriority = node->priority;\n            result = node->recipe;\n        }\n        // Traverse all children\n        for (auto child : node->children) {\n            TrieNode* childNode = child.second;\n            if (childNode->priority > maxPriority) {\n                maxPriority = childNode->priority;\n                result = childNode->recipe;\n            }\n        }\n        // Move to the next level\n        node = NULL;\n        for (auto child : node->children) {\n            TrieNode* childNode = child.second;\n            if (childNode != NULL) {\n                node = childNode;\n                break;\n            }\n        }\n    }\n    return result;\n}\nint main() {\n    int N;\n    cin >> N;\n    TrieNode* root = createNode();\n    for (int i = 0; i < N; i++) {\n        string recipe;\n        int priority;\n        cin >> recipe >> priority;\n        insert(root, recipe, priority);\n    }\n    int Q;\n    cin >> Q;\n    for (int i = 0; i < Q; i++) {\n        string query;\n        cin >> query;\n        string result = search(root, query);\n        if (result.empty()) {\n            cout << \"NO\" << endl;\n        } else {\n            cout << result << endl;\n        }\n    }\n    return 0;\n}",
      "Java": "// COMPLETE, COMPILABLE Java code\n// Class MUST be named Main\n// Uses correct I/O\n// Handles all T test cases\nimport java.util.HashMap;\nimport java.util.Map;\nimport java.util.Scanner;\npublic class Main {\n    // Define the Trie node structure\n    static class TrieNode {\n        Map<Character, TrieNode> children;\n        int priority;\n        String recipe;\n        public TrieNode() {\n            children = new HashMap<>();\n            priority = -1;\n        }\n    }\n    // Function to create a new Trie node\n    public static TrieNode createNode() {\n        return new TrieNode();\n    }\n    // Function to insert a recipe into the Trie\n    public static void insert(TrieNode root, String recipe, int priority) {\n        TrieNode node = root;\n        for (char c : recipe.toCharArray()) {\n            if (!node.children.containsKey(c)) {\n                node.children.put(c, createNode());\n            }\n            node = node.children.get(c);\n        }\n        node.priority = priority;\n        node.recipe = recipe;\n    }\n    // Function to search for a recipe in the Trie\n    public static String search(TrieNode root, String query) {\n        TrieNode node = root;\n        for (char c : query.toCharArray()) {\n            if (!node.children.containsKey(c)) {\n                return \"\";\n            }\n            node = node.children.get(c);\n        }\n        // Traverse the Trie to find the recipe with the highest priority\n        String result = \"\";\n        int maxPriority = -1;\n        while (node != null) {\n            if (node.priority > maxPriority) {\n                maxPriority = node.priority;\n                result = node.recipe;\n            }\n            // Traverse all children\n            for (Map.Entry<Character, TrieNode> child : node.children.entrySet()) {\n                TrieNode childNode = child.getValue();\n                if (childNode.priority > maxPriority) {\n                    maxPriority = childNode.priority;\n                    result = childNode.recipe;\n                }\n            }\n            // Move to the next level\n            node = null;\n            for (Map.Entry<Character, TrieNode> child : node.children.entrySet()) {\n                TrieNode childNode = child.getValue();\n                if (childNode != null) {\n                    node = childNode;\n                    break;\n                }\n            }\n        }\n        return result;\n    }\n    public static void main(String[] args) {\n        Scanner scanner = new Scanner(System.in);\n        int N = scanner.nextInt();\n        TrieNode root = createNode();\n        for (int i = 0; i < N; i++) {\n            String recipe = scanner.next();\n            int priority = scanner.nextInt();\n            insert(root, recipe, priority);\n        }\n        int Q = scanner.nextInt();\n        for (int i = 0; i < Q; i++) {\n            String query = scanner.next();\n            String result = search(root, query);\n            if (result.isEmpty()) {\n                System.out.println(\"NO\");\n            } else {\n                System.out.println(result);\n            }\n        }\n    }\n}",
      "Python3": "# COMPLETE, RUNNABLE Python3 code\n# Uses input() or sys.stdin\n# Handles all T test cases\nimport sys\n# Define the Trie node structure\nclass TrieNode:\n    def __init__(self):\n        self.children = {}\n        self.priority = -1\n        self.recipe = \"\"\n# Function to create a new Trie node\ndef createNode():\n    return TrieNode()\n# Function to insert a recipe into the Trie\ndef insert(root, recipe, priority):\n    node = root\n    for c in recipe:\n        if c not in node.children:\n            node.children[c] = createNode()\n        node = node.children[c]\n    node.priority = priority\n    node.recipe = recipe\n# Function to search for a recipe in the Trie\ndef search(root, query):\n    node = root\n    for c in query:\n        if c not in node.children:\n            return \"\"\n        node = node.children[c]\n    # Traverse the Trie to find the recipe with the highest priority\n    result = \"\"\n    maxPriority = -1\n    while node:\n        if node.priority > maxPriority:\n            maxPriority = node.priority\n            result = node.recipe\n        # Traverse all children\n        for child in node.children.values():\n            if child.priority > maxPriority:\n                maxPriority = child.priority\n                result = child.recipe\n        # Move to the next level\n        node = None\n        for child in node.children.values():\n            if child:\n                node = child\n                break\n    return result\n# Read input from stdin\nN = int(input())\nroot = createNode()\nfor _ in range(N):\n    recipe, priority = input().split()\n    priority = int(priority)\n    insert(root, recipe, priority)\nQ = int(input())\nfor _ in range(Q):\n    query = input()\n    result = search(root, query)\n    if not result:\n        print(\"NO\")\n    else:\n        print(result)",
      "JavaScript": "// COMPLETE, RUNNABLE JavaScript code\n// Uses fs.readFileSync(0,'utf-8') — NEVER readline()\n// Handles all T test cases\nconst fs = require('fs');\nconst input = fs.readFileSync(0, 'utf-8').trim().split('\\n');\nlet idx = 0;\n// Define the Trie node structure\nclass TrieNode {\n    constructor() {\n        this.children = {};\n        this.priority = -1;\n        this.recipe = \"\";\n    }\n}\n// Function to create a new Trie node\nfunction createNode() {\n    return new TrieNode();\n}\n// Function to insert a recipe into the Trie\nfunction insert(root, recipe, priority) {\n    let node = root;\n    for (let c of recipe) {\n        if (!(c in node.children)) {\n            node.children[c] = createNode();\n        }\n        node = node.children[c];\n    }\n    node.priority = priority;\n    node.recipe = recipe;\n}\n// Function to search for a recipe in the Trie\nfunction search(root, query) {\n    let node = root;\n    for (let c of query) {\n        if (!(c in node.children)) {\n            return \"\";\n        }\n        node = node.children[c];\n    }\n    // Traverse the Trie to find the recipe with the highest priority\n    let result = \"\";\n    let maxPriority = -1;\n    while (node) {\n        if (node.priority > maxPriority) {\n            maxPriority = node.priority;\n            result = node.recipe;\n        }\n        // Traverse all children\n        for (let child in node.children) {\n            let childNode = node.children[child];\n            if (childNode.priority > maxPriority) {\n                maxPriority = childNode.priority;\n                result = childNode.recipe;\n            }\n        }\n        // Move to the next level\n        node = null;\n        for (let child in node.children) {\n            let childNode = node.children[child];\n            if (childNode) {\n                node = childNode;\n                break;\n            }\n        }\n    }\n    return result;\n}\n// Read input from stdin\nlet N = parseInt(input[idx++]);\nlet root = createNode();\nfor (let i = 0; i < N; i++) {\n    let [recipe, priority] = input[idx++].split(' ');\n    priority = parseInt(priority);\n    insert(root, recipe, priority);\n}\nlet Q = parseInt(input[idx++]);\nfor (let i = 0; i < Q; i++) {\n    let query = input[idx++];\n    let result = search(root, query);\n    if (!result) {\n        console.log('NO');\n    } else {\n        console.log(result);\n    }\n}"
    }
  },
  "QBCategory": "CCD",
  "boilerPlates": {
    "C": "#include <stdio.h>\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "C++": "#include <iostream>\nusing namespace std;\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "Java": "import java.util.*;\nimport java.io.*;\n\npublic class Main {\n    public static void main(String[] args) throws IOException {\n        // your code goes here\n    }\n}",
    "Python3": "# your code goes here\n",
    "JavaScript": "// your code goes here\n"
  },
  "_internal": {
    "methodName": "",
    "params": []
  }
}