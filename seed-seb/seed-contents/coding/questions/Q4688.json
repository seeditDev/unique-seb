{
  "questionId": "Q4688",
  "title": "Minimum Depth of Binary Tree",
  "slug": "minimum-depth-of-binary-tree",
  "version": 1,
  "createdAt": "2026-07-01",
  "updatedAt": "2026-07-04",
  "metadata": {
    "category": "Graph",
    "difficulty": "Medium",
    "isPremium": true,
    "tags": [
      "BFS",
      "Tree Traversal"
    ],
    "companyTags": [],
    "topicTags": [
      "Trees"
    ],
    "QBCategory": "CCD"
  },
  "content": {
    "problemStatement": "Given a binary tree, print its minimum depth.\nThe minimum depth is the number of nodes along the shortest path from the root node down to the nearest leaf node.\n\nFor example, the minimum depth of the following binary tree is: 2\n\n\n\n\nInput Format\n\nThe first line of the input contains a single integer NNN — the number of nodes in the binary tree.\nNext N−1N - 1N−1 lines contain three space separated characters pi,ci,Rip_i, c_i, R_ipi​,ci​,Ri​, describing the tree with edge informations - pip_ipi​ is an integer denoting the parent node of the iiith edge, cic_ici​ is child node, and the letter RiR_iRi​ denotes whether the child is left child or right child, it's value is `L` if cic_ici​ is left child of pip_ipi​, else `R`.\n\n\nOutput Format\nOutput on the single line - the minimum depth of the given binary tree.",
    "constraints": [
      "1 <= N <= 10000",
      "1 <= pi, ci <= N",
      "Ri = 'L' or 'R'",
      "All pi's and ci's are distinct"
    ],
    "inputFormat": "The first line contains a single integer N, the number of nodes in the binary tree. Each of the next N-1 lines contains three space-separated values: pi, ci, and Ri, where pi is the parent node, ci is the child node, and Ri is 'L' if ci is the left child of pi, and 'R' otherwise.",
    "outputFormat": "Output the minimum depth of the binary tree on a single line.",
    "sampleTestCases": [
      {
        "id": "sample_1",
        "input": "5\n1 2 L\n1 3 R\n3 4 L\n3 5 R",
        "output": "2",
        "explanation": "The minimum depth of the given binary tree is 2, which is the path from the root node (1) to the leaf node (2) or (3)."
      },
      {
        "id": "sample_2",
        "input": "3\n1 2 L\n1 3 R",
        "output": "2",
        "explanation": "The minimum depth of the given binary tree is 2, which is the path from the root node (1) to the leaf nodes (2) or (3)."
      },
      {
        "id": "sample_3",
        "input": "4\n1 2 L\n1 3 R\n2 4 L",
        "output": "2",
        "explanation": "The minimum depth of the given binary tree is 2, which is the path from the root node (1) to the leaf node (3)."
      }
    ]
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "label": "minimum boundary",
        "input": "1",
        "expectedOutput": "1",
        "weight": 2
      },
      {
        "id": "tc_2",
        "label": "maximum boundary",
        "input": "10000\n1 2 L\n1 3 R\n2 4 L\n2 5 R\n3 6 L\n3 7 R\n4 8 L\n4 9 R\n5 10 L\n5 11 R\n6 12 L\n6 13 R\n7 14 L\n7 15 R",
        "expectedOutput": "4",
        "weight": 2
      },
      {
        "id": "tc_3",
        "label": "uniform/edge case",
        "input": "5\n1 2 L\n1 3 R\n2 4 L\n2 5 R",
        "expectedOutput": "2",
        "weight": 2
      },
      {
        "id": "tc_4",
        "label": "tricky case",
        "input": "5\n1 2 L\n1 3 R\n3 4 L\n3 5 R",
        "expectedOutput": "2",
        "weight": 2
      },
      {
        "id": "tc_5",
        "label": "stress/general",
        "input": "10\n1 2 L\n1 3 R\n2 4 L\n2 5 R\n3 6 L\n3 7 R\n4 8 L\n4 9 R\n5 10 L",
        "expectedOutput": "3",
        "weight": 2
      }
    ],
    "totalTestCases": 5,
    "totalWeight": 10
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      2,
      2,
      2,
      2,
      2
    ],
    "maxScore": 10
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "To find the minimum depth of a binary tree, we can use a breadth-first search (BFS) approach. We start by adding the root node to a queue. Then, we enter a loop where we keep removing nodes from the queue and adding their children until we find a leaf node. The depth of the first leaf node we encounter is the minimum depth of the tree.",
    "timeComplexity": "O(N) per test case, where N is the number of nodes in the tree. This is because in the worst case, we might need to visit every node in the tree.",
    "spaceComplexity": "O(N) for storing the nodes in the queue.",
    "code": {
      "C": "#include <stdio.h>\n#include <stdlib.h>\n#define MAX 10000\n// Define the structure for a tree node\ntypedef struct Node {\n    int val;\n    struct Node* left;\n    struct Node* right;\n} Node;\n// Function to create a new node\nNode* newNode(int key) {\n    Node* node = (Node*) malloc(sizeof(Node));\n    node->val = key;\n    node->left = node->right = NULL;\n    return node;\n}\n// Function to find the minimum depth of a tree\nint minDepth(Node* root) {\n    if (root == NULL) return 0;\n    // Create a queue for BFS\n    Node** queue = (Node**) malloc(sizeof(Node*) * MAX);\n    int front = 0, rear = 0;\n    queue[rear++] = root;\n    int depth = 0;\n    while (front < rear) {\n        int size = rear - front;\n        depth++;\n        for (int i = 0; i < size; i++) {\n            Node* node = queue[front++];\n            if (node->left == NULL && node->right == NULL) return depth;\n            if (node->left != NULL) queue[rear++] = node->left;\n            if (node->right != NULL) queue[rear++] = node->right;\n        }\n    }\n    return depth;\n}\nint main() {\n    int T;\n    scanf(\"%d\", &T);\n    while (T--) {\n        int N;\n        scanf(\"%d\", &N);\n        Node* root = NULL;\n        Node* nodes[N];\n        for (int i = 0; i < N; i++) {\n            nodes[i] = newNode(i + 1);\n        }\n        for (int i = 0; i < N - 1; i++) {\n            int pi, ci;\n            char Ri;\n            scanf(\"%d %d %c\", &pi, &ci, &Ri);\n            // Create nodes and assign them as children of the parent node\n            if (Ri == 'L') nodes[pi - 1]->left = nodes[ci - 1];\n            else nodes[pi - 1]->right = nodes[ci - 1];\n            if (root == NULL) root = nodes[0];\n        }\n        printf(\"%d\\n\", minDepth(root));\n    }\n    return 0;\n}",
      "C++": "#include <iostream>\n#include <queue>\nusing namespace std;\n// Define the structure for a tree node\nstruct Node {\n    int val;\n    Node* left;\n    Node* right;\n    Node(int key) {\n        val = key;\n        left = right = NULL;\n    }\n};\n// Function to find the minimum depth of a tree\nint minDepth(Node* root) {\n    if (root == NULL) return 0;\n    queue<Node*> q;\n    q.push(root);\n    int depth = 0;\n    while (!q.empty()) {\n        int size = q.size();\n        depth++;\n        for (int i = 0; i < size; i++) {\n            Node* node = q.front(); q.pop();\n            if (node->left == NULL && node->right == NULL) return depth;\n            if (node->left != NULL) q.push(node->left);\n            if (node->right != NULL) q.push(node->right);\n        }\n    }\n    return depth;\n}\nint main() {\n    ios::sync_with_stdio(false);\n    cin.tie(NULL);\n    int T;\n    cin >> T;\n    while (T--) {\n        int N;\n        cin >> N;\n        Node* root = NULL;\n        Node* nodes[N];\n        for (int i = 0; i < N; i++) {\n            nodes[i] = new Node(i + 1);\n        }\n        for (int i = 0; i < N - 1; i++) {\n            int pi, ci;\n            char Ri;\n            cin >> pi >> ci >> Ri;\n            // Create nodes and assign them as children of the parent node\n            if (Ri == 'L') nodes[pi - 1]->left = nodes[ci - 1];\n            else nodes[pi - 1]->right = nodes[ci - 1];\n            if (root == NULL) root = nodes[0];\n        }\n        cout << minDepth(root) << endl;\n    }\n    return 0;\n}",
      "Java": "import java.util.Queue;\nimport java.util.LinkedList;\nimport java.io.BufferedReader;\nimport java.io.InputStreamReader;\nimport java.io.IOException;\n// Define the structure for a tree node\nclass Node {\n    int val;\n    Node left;\n    Node right;\n    Node(int key) {\n        val = key;\n        left = right = null;\n    }\n}\npublic class Main {\n    // Function to find the minimum depth of a tree\n    public static int minDepth(Node root) {\n        if (root == null) return 0;\n        Queue<Node> q = new LinkedList<Node>();\n        q.add(root);\n        int depth = 0;\n        while (!q.isEmpty()) {\n            int size = q.size();\n            depth++;\n            for (int i = 0; i < size; i++) {\n                Node node = q.poll();\n                if (node.left == null && node.right == null) return depth;\n                if (node.left != null) q.add(node.left);\n                if (node.right != null) q.add(node.right);\n            }\n        }\n        return depth;\n    }\n    public static void main(String[] args) throws IOException {\n        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));\n        int T = Integer.parseInt(br.readLine());\n        while (T-- > 0) {\n            int N = Integer.parseInt(br.readLine());\n            Node[] nodes = new Node[N];\n            for (int i = 0; i < N; i++) {\n                nodes[i] = new Node(i + 1);\n            }\n            Node root = null;\n            for (int i = 0; i < N - 1; i++) {\n                String[] input = br.readLine().split(\" \");\n                int pi = Integer.parseInt(input[0]);\n                int ci = Integer.parseInt(input[1]);\n                char Ri = input[2].charAt(0);\n                // Create nodes and assign them as children of the parent node\n                if (Ri == 'L') nodes[pi - 1].left = nodes[ci - 1];\n                else nodes[pi - 1].right = nodes[ci - 1];\n                if (root == null) root = nodes[0];\n            }\n            System.out.println(minDepth(root));\n        }\n    }\n}",
      "Python3": "import sys\nfrom collections import deque\nclass Node:\n    def __init__(self, key):\n        self.val = key\n        self.left = None\n        self.right = None\ndef minDepth(root):\n    if root is None: return 0\n    q = deque()\n    q.append(root)\n    depth = 0\n    while q:\n        size = len(q)\n        depth += 1\n        for _ in range(size):\n            node = q.popleft()\n            if node.left is None and node.right is None: return depth\n            if node.left is not None: q.append(node.left)\n            if node.right is not None: q.append(node.right)\n    return depth\nT = int(sys.stdin.readline())\nfor _ in range(T):\n    N = int(sys.stdin.readline())\n    nodes = [Node(i + 1) for i in range(N)]\n    root = None\n    for i in range(N - 1):\n        pi, ci, Ri = sys.stdin.readline().split()\n        pi, ci = int(pi), int(ci)\n        # Create nodes and assign them as children of the parent node\n        if Ri == 'L': nodes[pi - 1].left = nodes[ci - 1]\n        else: nodes[pi - 1].right = nodes[ci - 1]\n        if root is None: root = nodes[0]\n    print(minDepth(root))",
      "JavaScript": "const lines = require('fs').readFileSync(0, 'utf-8').trim().split('\\n');\nlet i = 0;\nfunction readLine() { return lines[i++]; }\nfunction readInt() { return parseInt(readLine()); }\nfunction readInts() { return readLine().split(' ').map(Number); }\nfunction readString() { return readLine(); }\nfunction readStrings() { return readLine().split(' '); }\nclass Node {\n    constructor(key) {\n        this.val = key;\n        this.left = null;\n        this.right = null;\n    }\n}\nfunction minDepth(root) {\n    if (root === null) return 0;\n    let q = [];\n    q.push(root);\n    let depth = 0;\n    while (q.length > 0) {\n        let size = q.length;\n        depth++;\n        for (let i = 0; i < size; i++) {\n            let node = q.shift();\n            if (node.left === null && node.right === null) return depth;\n            if (node.left !== null) q.push(node.left);\n            if (node.right !== null) q.push(node.right);\n        }\n    }\n    return depth;\n}\nlet T = readInt();\nfor (let i = 0; i < T; i++) {\n    let N = readInt();\n    let nodes = [];\n    for (let j = 0; j < N; j++) {\n        nodes.push(new Node(j + 1));\n    }\n    let root = null;\n    for (let j = 0; j < N - 1; j++) {\n        let [pi, ci, Ri] = readStrings();\n        pi = parseInt(pi);\n        ci = parseInt(ci);\n        // Create nodes and assign them as children of the parent node\n        if (Ri === 'L') nodes[pi - 1].left = nodes[ci - 1];\n        else nodes[pi - 1].right = nodes[ci - 1];\n        if (root === null) root = nodes[0];\n    }\n    console.log(minDepth(root));\n}"
    }
  },
  "QBCategory": "CCD",
  "boilerPlates": {
    "C": "#include <stdio.h>\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "C++": "#include <iostream>\nusing namespace std;\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "Java": "import java.util.*;\nimport java.io.*;\n\npublic class Main {\n    public static void main(String[] args) throws IOException {\n        // your code goes here\n    }\n}",
    "Python3": "# your code goes here\n",
    "JavaScript": "// your code goes here\n"
  },
  "_internal": {
    "methodName": "",
    "params": []
  }
}