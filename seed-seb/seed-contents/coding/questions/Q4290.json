{
  "questionId": "Q4290",
  "title": "Distance between two nodes",
  "slug": "distance-between-two-nodes",
  "version": 1,
  "createdAt": "2026-07-01",
  "updatedAt": "2026-07-04",
  "metadata": {
    "category": "Graph",
    "difficulty": "Hard",
    "isPremium": true,
    "tags": [
      "Lowest Common Ancestor",
      "Tree Traversal",
      "Depth-First Search"
    ],
    "companyTags": [],
    "topicTags": [
      "Trees"
    ],
    "QBCategory": "CCD"
  },
  "content": {
    "problemStatement": "Given an undirected connected tree with **N** nodes, numbered from **1** to **N**, and rooted at node **1**, and two nodes uuu and vvv, find the distance between these two nodes. (**Note:** the distance between two nodes is the no. of edges in the simple path between them.)\n\nFor example, in the following tree, the distance between nodes 333 and 777 is 444.\n\n\n\n\nInput Format\n\nThe first line of the input contains three space separated integers NNN, uuu and vvv — the number of nodes, and two given nodes.\nThe next N−1N - 1N−1 lines describe the edges. The iii-th of these N−1N - 1N−1 lines contains two space-separated integers uiu_iui​ and viv_ivi​, denoting a bidirectional edge between uiu_iui​ and viv_ivi​.\n\n\nOutput Format\n\nOutput on the single line, the distance between the nodes uuu and vvv.",
    "constraints": [
      "1 <= N <= 100000",
      "1 <= u, v <= N",
      "1 <= ui, vi <= N"
    ],
    "inputFormat": "First line contains three space-separated integers N, u, and v, where N is the number of nodes in the tree, and u and v are the two nodes for which the distance is to be calculated. The next N-1 lines describe the edges of the tree, with each line containing two space-separated integers ui and vi, denoting a bidirectional edge between ui and vi.",
    "outputFormat": "The distance between nodes u and v, which is the number of edges in the simple path between them.",
    "sampleTestCases": [
      {
        "id": "sample_1",
        "input": "7 3 7\n1 2\n1 4\n2 5\n2 3\n2 6\n4 7",
        "output": "4",
        "explanation": "The distance between nodes 3 and 7 is 4, as the path from 3 to 7 is 3 -> 2 -> 1 -> 4 -> 7."
      },
      {
        "id": "sample_2",
        "input": "5 1 5\n1 2\n1 3\n2 4\n3 5",
        "output": "2",
        "explanation": "The distance between nodes 1 and 5 is 2, as the path from 1 to 5 is 1 -> 3 -> 5."
      },
      {
        "id": "sample_3",
        "input": "10 1 10\n1 2\n1 3\n2 4\n2 5\n3 6\n3 7\n4 8\n5 9\n6 10",
        "output": "3",
        "explanation": "The distance between nodes 1 and 10 is 3, as the path from 1 to 10 is 1 -> 3 -> 6 -> 10."
      }
    ]
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "label": "minimum boundary",
        "input": "2 1 2\n1 2",
        "expectedOutput": "1",
        "weight": 2
      },
      {
        "id": "tc_2",
        "label": "Case 2",
        "input": "2 1 2\n1 2",
        "expectedOutput": "1",
        "weight": 2
      },
      {
        "id": "tc_3",
        "label": "Case 3",
        "input": "2 1 2\n1 2",
        "expectedOutput": "1",
        "weight": 2
      },
      {
        "id": "tc_4",
        "label": "Case 4",
        "input": "2 1 2\n1 2",
        "expectedOutput": "1",
        "weight": 2
      },
      {
        "id": "tc_5",
        "label": "Case 5",
        "input": "2 1 2\n1 2",
        "expectedOutput": "1",
        "weight": 2
      }
    ],
    "totalTestCases": 5,
    "totalWeight": 10
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      2,
      2,
      2,
      2,
      2
    ],
    "maxScore": 10
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "To find the distance between two nodes in a tree, we can use the concept of the lowest common ancestor (LCA). The LCA of two nodes is the node that is farthest from the root and is an ancestor of both nodes. The distance between two nodes is then equal to the sum of the distances from each node to their LCA. We can use a depth-first search (DFS) to traverse the tree and find the LCA of the two nodes.",
    "timeComplexity": "O(N log N) per test case, O(T * N log N) total. The DFS traversal takes O(N) time, and the LCA calculation takes O(log N) time.",
    "spaceComplexity": "O(N) — the dominant structure is the adjacency list representation of the tree.",
    "code": {
      "C": "#include <stdio.h>\n#include <stdlib.h>\n#define N 100001\n#define LG 17\nint adj[N][20];\nint adj_size[N];\nint level[N], ancestors[N][LG], n;\nvoid dfs(int node, int parent, int lvl) {\n    level[node] = lvl;\n    ancestors[node][0] = parent;\n    for(int i = 0; i < adj_size[node]; i++) {\n        int child = adj[node][i];\n        if(child != parent) {\n            dfs(child, node, lvl + 1);\n        }\n    }\n}\nvoid preprocess() {\n    for(int j = 1; j < LG; j++) {\n        for(int node = 1; node <= n; node++) {\n            int ancestor = ancestors[node][j - 1];\n            if(ancestor == -1) {\n                ancestors[node][j] = -1;\n            } else {\n                ancestors[node][j] = ancestors[ancestor][j - 1];\n            }\n        }\n    }\n}\nint lca(int u, int v) {\n    if(level[u] < level[v]) {\n        int temp = u;\n        u = v;\n        v = temp;\n    }\n    int k = level[u] - level[v];\n    for(int j = 0; j < LG; j++) {\n        if((k & (1 << j)) != 0) {\n            u = ancestors[u][j];\n        }\n    }\n    if(u == v)\n        return v;\n    for(int j = LG - 1; j >= 0; j--) {\n        if(ancestors[u][j] != -1 && ancestors[u][j] != ancestors[v][j]) {\n            u = ancestors[u][j];\n            v = ancestors[v][j];\n        }\n    }\n    return ancestors[u][0];\n}\nint get_distance(int u, int v) {\n    int lca_of_uv = lca(u, v);\n    return level[u] + level[v] - 2 * level[lca_of_uv];\n}\nint main() {\n    int u, v;\n    scanf(\"%d %d %d\", &n, &u, &v);\n    for(int i = 1; i <= n; i++) {\n        adj_size[i] = 0;\n    }\n    for(int i = 1; i < n; i++) {\n        int ui, vi;\n        scanf(\"%d %d\", &ui, &vi);\n        adj[ui][adj_size[ui]++] = vi;\n        adj[vi][adj_size[vi]++] = ui;\n    }\n    dfs(1, -1, 0);\n    preprocess();\n    printf(\"%d\\n\", get_distance(u, v));\n    return 0;\n}",
      "C++": "#include <bits/stdc++.h>\nusing namespace std;\nconst int N = 100001;\nconst int LG = 17;\nvector<int> adj[N];\nint level[N], ancestors[N][LG], n;\nvoid dfs(int node, int parent = -1, int lvl = 0) {\n    level[node] = lvl;\n    ancestors[node][0] = parent;\n    for(int child: adj[node]) {\n        if(child != parent) {\n            dfs(child, node, lvl + 1);\n        }\n    }\n}\nvoid preprocess() {\n    for(int j = 1; j < LG; j++) {\n        for(int node = 1; node <= n; node++) {\n            int ancestor = ancestors[node][j - 1];\n            if(ancestor == -1)\n                ancestors[node][j] = -1;\n            else\n                ancestors[node][j] = ancestors[ancestor][j - 1];\n        }\n    }\n}\nint lca(int u, int v) {\n    if(level[u] <= level[v]) {\n        swap(u, v);\n    }\n    int diff = level[u] - level[v];\n    for(int j = 0; j < LG; j++) {\n        if((diff >> j) & 1){\n            u = ancestors[u][j];\n        }\n    }\n    if(u == v)\n        return v;\n    for(int j = LG - 1; j >= 0; j--) {\n        if(ancestors[u][j] != -1 && ancestors[u][j] != ancestors[v][j]) {\n            u = ancestors[u][j];\n            v = ancestors[v][j];\n        }\n    }\n    return ancestors[u][0];\n}\nint get_distance(int u, int v) {\n    int lca_of_uv = lca(u, v);\n    return level[u] + level[v] - 2 * level[lca_of_uv];\n}\nint main() {\n    int u, v;\n    cin >> n >> u >> v;\n    for(int i = 1; i < n; i){\n        int ui, vi;\n        cin >> ui >> vi;\n        adj[ui].push_back(vi);\n        adj[vi].push_back(ui);\n    }\n    dfs(1);\n    preprocess();\n    cout << get_distance(u, v);\n    return 0;\n}",
      "Java": "import java.io.*;\nimport java.util.*;\npublic class Main {\n    static final int N = 100001;\n    static final int LG = 17;\n    static ArrayList<Integer>[] adj = new ArrayList[N];\n    static int[] level = new int[N];\n    static int[][] ancestors = new int[N][LG];\n    static int n;\n    static {\n        for (int i = 0; i < N; i++) {\n            adj[i] = new ArrayList<>();\n            Arrays.fill(ancestors[i], -1);\n        }\n    }\n    static void dfs(int node, int parent, int lvl) {\n        level[node] = lvl;\n        ancestors[node][0] = parent;\n        for (int child : adj[node]) {\n            if (child != parent) {\n                dfs(child, node, lvl + 1);\n            }\n        }\n    }\n    static void preprocess() {\n        for (int j = 1; j < LG; j++) {\n            for (int node = 1; node <= n; node++) {\n                int ancestor = ancestors[node][j - 1];\n                ancestors[node][j] = (ancestor == -1) ? -1 : ancestors[ancestor][j - 1];\n            }\n        }\n    }\n    static int lca(int u, int v) {\n        if (level[u] < level[v]) {\n            int temp = u;\n            u = v;\n            v = temp;\n        }\n        int diff = level[u] - level[v];\n        for (int j = 0; j < LG; j++) {\n            if ((diff & (1 << j)) != 0) {\n                u = ancestors[u][j];\n            }\n        }\n        if (u == v) {\n            return u;\n        }\n        for (int j = LG - 1; j >= 0; j--) {\n            if (ancestors[u][j] != -1 && ancestors[u][j] != ancestors[v][j]) {\n                u = ancestors[u][j];\n                v = ancestors[v][j];\n            }\n        }\n        return ancestors[u][0];\n    }\n    static int getDistance(int u, int v) {\n        int lcaOfUV = lca(u, v);\n        return level[u] + level[v] - 2 * level[lcaOfUV];\n    }\n    public static void main(String[] args) throws IOException {\n        BufferedReader reader = new BufferedReader(new InputStreamReader(System.in));\n        StringTokenizer tokenizer = new StringTokenizer(reader.readLine());\n        n = Integer.parseInt(tokenizer.nextToken());\n        int u = Integer.parseInt(tokenizer.nextToken());\n        int v = Integer.parseInt(tokenizer.nextToken());\n        for (int i = 1; i < n; i++) {\n            tokenizer = new StringTokenizer(reader.readLine());\n            int ui = Integer.parseInt(tokenizer.nextToken());\n            int vi = Integer.parseInt(tokenizer.nextToken());\n            adj[ui].add(vi);\n            adj[vi].add(ui);\n        }\n        dfs(1, -1, 0);\n        preprocess();\n        System.out.println(getDistance(u, v));\n    }\n}",
      "Python3": "import sys\nfrom collections import defaultdict, deque\nimport math\nN = 100001\nLG = 17\nadj = defaultdict(list)\nlevel = [0] * N\nancestors = [[-1] * LG for _ in range(N)]\nn = 0\ndef dfs(node, parent = -1, lvl = 0):\n    level[node] = lvl\n    ancestors[node][0] = parent\n    for child in adj[node]:\n        if child != parent:\n            dfs(child, node, lvl + 1)\ndef preprocess():\n    for j in range(1, LG):\n        for node in range(1, n + 1):\n            ancestor = ancestors[node][j - 1]\n            if ancestor == -1:\n                ancestors[node][j] = -1\n            else:\n                ancestors[node][j] = ancestors[ancestor][j - 1]\ndef lca(u, v):\n    if level[u] < level[v]:\n        u, v = v, u\n    diff = level[u] - level[v]\n    for j in range(LG):\n        if (diff >> j) & 1:\n            u = ancestors[u][j]\n    if u == v:\n        return u\n    for j in range(LG - 1, -1, -1):\n        if ancestors[u][j] != -1 and ancestors[u][j] != ancestors[v][j]:\n            u = ancestors[u][j]\n            v = ancestors[v][j]\n    return ancestors[u][0]\ndef get_distance(u, v):\n    lca_of_uv = lca(u, v)\n    return level[u] + level[v] - 2 * level[lca_of_uv]\ndef main():\n    input = sys.stdin.read\n    data = input().split()\n    global n\n    index = 0\n    n = int(data[index])\n    index += 1\n    u = int(data[index])\n    index += 1\n    v = int(data[index])\n    index += 1\n    for _ in range(n - 1):\n        ui = int(data[index])\n        index += 1\n        vi = int(data[index])\n        index += 1\n        adj[ui].append(vi)\n        adj[vi].append(ui)\n    dfs(1)\n    preprocess()\n    print(get_distance(u, v))\nif __name__ == \"__main__\":\n    main()",
      "JavaScript": "const readline = require('readline');\nconst rl = readline.createInterface({\n    input: process.stdin,\n    output: process.stdout\n});\nconst lines = [];\nrl.on('line', (input) => {\n    lines.push(input);\n});\nrl.on('close', () => {\n    let l = 0;\n    let n = +lines[l++];\n    let [u, v] = lines[l++].trim().split(' ').map(Number);\n    let adj = Array.from({ length: n + 1 }, () => []);\n    for (let i = 0; i < n - 1; i++) {\n        let [x, y] = lines[l++].trim().split(' ').map(Number);\n        adj[x].push(y);\n        adj[y].push(x);\n    }\n    let level = Array(n + 1).fill(0);\n    let ancestors = Array.from({ length: n + 1 }, () => Array(17).fill(-1));\n    function dfs(node, parent = -1, lvl = 0) {\n        level[node] = lvl;\n        ancestors[node][0] = parent;\n        for (let child of adj[node]) {\n            if (child !== parent) {\n                dfs(child, node, lvl + 1);\n            }\n        }\n    }\n    function preprocess() {\n        for (let j = 1; j < 17; j++) {\n            for (let node = 1; node <= n; node++) {\n                let ancestor = ancestors[node][j - 1];\n                if (ancestor !== -1) {\n                    ancestors[node][j] = ancestors[ancestor][j - 1];\n                }\n            }\n        }\n    }\n    function lca(u, v) {\n        if (level[u] < level[v]) {\n            [u, v] = [v, u];\n        }\n        let diff = level[u] - level[v];\n        for (let j = 0; j < 17; j++) {\n            if ((diff >> j) & 1) {\n                u = ancestors[u][j];\n            }\n        }\n        if (u === v) {\n            return u;\n        }\n        for (let j = 16; j >= 0; j--) {\n            if (ancestors[u][j] !== -1 && ancestors[u][j] !== ancestors[v][j]) {\n                u = ancestors[u][j];\n                v = ancestors[v][j];\n            }\n        }\n        return ancestors[u][0];\n    }\n    function get_distance(u, v) {\n        let lca_of_uv = lca(u, v);\n        return level[u] + level[v] - 2 * level[lca_of_uv];\n    }\n    dfs(1);\n    preprocess();\n    console.log(get_distance(u, v));\n});"
    }
  },
  "QBCategory": "CCD",
  "boilerPlates": {
    "C": "#include <stdio.h>\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "C++": "#include <iostream>\nusing namespace std;\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "Java": "import java.util.*;\nimport java.io.*;\n\npublic class Main {\n    public static void main(String[] args) throws IOException {\n        // your code goes here\n    }\n}",
    "Python3": "# your code goes here\n",
    "JavaScript": "// your code goes here\n"
  },
  "_internal": {
    "methodName": "",
    "params": []
  }
}