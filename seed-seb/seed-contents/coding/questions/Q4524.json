{
  "questionId": "Q4524",
  "title": "Truck Driver Problem",
  "slug": "truck-driver-problem",
  "version": 1,
  "createdAt": "2026-07-01",
  "updatedAt": "2026-07-04",
  "metadata": {
    "category": "Math",
    "difficulty": "Hard",
    "isPremium": true,
    "tags": [
      "Binary Search"
    ],
    "companyTags": [],
    "topicTags": [
      "Binary Search"
    ],
    "QBCategory": "CCD"
  },
  "content": {
    "problemStatement": "A logistics company needs to deliver goods across several cities within daysdaysdays days.\n\nThe ithi^\\text{th}ith container has a weight of weights[i]weights[i]weights[i]. Each day, a truck is loaded with containers **in the given order**. The truck has a maximum load capacity, and it cannot carry more than that in one trip.\n\nYour task is to determine the **minimum truck capacity** required so that **all containers are delivered within daysdaysdays days**.\n\nFunction DeclarationFunction NameshipWithinDaysshipWithinDaysshipWithinDays – This function determines the minimum truck capacity required to deliver all containers within the given number of days.\n\nParameters\nweightsweightsweights : An array representing the weights of the containers, in delivery order.\ndaysdaysdays : The number of days within which all containers must be delivered.\nReturn Value\nReturns a single integer — the **minimum truck capacity** needed.\nConstraints\n1≤T≤1001 \\leq T \\leq 1001≤T≤100\n1≤days≤weights.length≤5×1041 \\leq days \\leq weights.length \\leq 5 × 10^41≤days≤weights.length≤5×104\n1≤weights[i]≤5001 \\leq weights[i] \\leq 5001≤weights[i]≤500\nContainers must be shipped **in order**\nEach container must be shipped **entirely in one day**\n\nInput Format\n\nThe first line contains the number of test cases T\nFor each test case:\n\nThe first line contains two integers — NNN and daysdaysdays, where NNN is the number of containers.\nThe second line contains NNN space-separated integers representing the container weights.\n\n\n\n\nOutput Format\n\nPrint a single integer — the **minimum truck capacity** required.",
    "constraints": [
      "1 <= T <= 100",
      "1 <= days <= N <= 5 * 10^4",
      "1 <= weights[i] <= 500"
    ],
    "inputFormat": "The first line contains an integer T, the number of test cases. For each test case: The first line contains two integers N and days, where N is the number of containers. The second line contains N space-separated integers representing the container weights.",
    "outputFormat": "For each test case, output a single line containing the minimum truck capacity required to ship all containers within the given number of days.",
    "sampleTestCases": [
      {
        "id": "sample_1",
        "input": "1\\n7 4\\n5 8 3 2 7 4 6",
        "output": "11",
        "explanation": "With capacity 11 the containers can be shipped in 4 days as described in the statement, and any smaller capacity would need more than 4 days."
      },
      {
        "id": "sample_2",
        "input": "1\\n5 5\\n1 2 3 4 5",
        "output": "5",
        "explanation": "When days equals the number of containers, the truck only needs to carry the heaviest container each day, i.e., capacity = max weight = 5."
      },
      {
        "id": "sample_3",
        "input": "1\\n5 1\\n10 20 30 40 50",
        "output": "150",
        "explanation": "All containers must be shipped in a single day, so the capacity must be the total sum of weights: 10+20+30+40+50 = 150."
      }
    ]
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "label": "minimum boundary",
        "input": "1\\n1 1\\n1",
        "expectedOutput": "1",
        "weight": 2
      },
      {
        "id": "tc_2",
        "label": "maximum boundary",
        "input": "1\\n10 1\\n500 500 500 500 500 500 500 500 500 500",
        "expectedOutput": "5000",
        "weight": 2
      },
      {
        "id": "tc_3",
        "label": "uniform/edge case",
        "input": "1\\n5 5\\n1 1 1 1 1",
        "expectedOutput": "1",
        "weight": 2
      },
      {
        "id": "tc_4",
        "label": "tricky case",
        "input": "1\\n5 3\\n1 2 3 4 5",
        "expectedOutput": "6",
        "weight": 2
      },
      {
        "id": "tc_5",
        "label": "stress/general",
        "input": "2\\n5 3\\n1 2 3 4 5\\n5 5\\n1 2 3 4 5",
        "expectedOutput": "6\\n5",
        "weight": 2
      }
    ],
    "totalTestCases": 5,
    "totalWeight": 10
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      2,
      2,
      2,
      2,
      2
    ],
    "maxScore": 10
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "The problem can be solved using binary search. We need to find the minimum truck capacity that can deliver all containers within the given number of days. We can start by finding the maximum weight of a container, which will be the minimum possible capacity. Then, we can perform a binary search between this minimum capacity and the total weight of all containers. For each mid capacity, we calculate the number of days required to deliver all containers. If the number of days is less than or equal to the given days, we update our answer and continue the search in the lower half. Otherwise, we continue the search in the upper half.",
    "timeComplexity": "O(T * N * log(sum of weights)) per test case, O(T * N * log(sum of weights)) total.",
    "spaceComplexity": "O(N) — for storing the weights of containers.",
    "code": {
      "C": "#include <stdio.h>\n#include <stdlib.h>\n\nlong long neededDays(int *w, int n, long long cap) {\n    long long days = 1, cur = 0;\n    for (int i = 0; i < n; ++i) {\n        if (cur + w[i] <= cap) {\n            cur += w[i];\n        } else {\n            ++days;\n            cur = w[i];\n        }\n    }\n    return days;\n}\n\nint main() {\n    int T; if (scanf(\"%d\", &T) != 1) return 0;\n    while (T--) {\n        int N, days; scanf(\"%d %d\", &N, &days);\n        int *weights = (int*)malloc(N * sizeof(int));\n        long long lo = 0, hi = 0;\n        for (int i = 0; i < N; ++i) {\n            scanf(\"%d\", &weights[i]);\n            if (weights[i] > lo) lo = weights[i];\n            hi += weights[i];\n        }\n        long long ans = hi, L = lo, R = hi;\n        while (L <= R) {\n            long long mid = L + (R - L) / 2;\n            if (neededDays(weights, N, mid) <= days) {\n                ans = mid;\n                R = mid - 1;\n            } else {\n                L = mid + 1;\n            }\n        }\n        printf(\"%lld\\n\", ans);\n        free(weights);\n    }\n    return 0;\n}",
      "C++": "#include <bits/stdc++.h>\nusing namespace std;\n\nlong long neededDays(const vector<int>& w, long long cap) {\n    long long days = 1, cur = 0;\n    for (int weight : w) {\n        if (cur + weight <= cap) cur += weight;\n        else { ++days; cur = weight; }\n    }\n    return days;\n}\n\nint main() {\n    ios::sync_with_stdio(false);\n    cin.tie(nullptr);\n    int T; if (!(cin >> T)) return 0;\n    while (T--) {\n        int N, days; cin >> N >> days;\n        vector<int> weights(N);\n        long long lo = 0, hi = 0;\n        for (int i = 0; i < N; ++i) {\n            cin >> weights[i];\n            lo = max<long long>(lo, weights[i]);\n            hi += weights[i];\n        }\n        long long L = lo, R = hi, ans = hi;\n        while (L <= R) {\n            long long mid = L + (R - L) / 2;\n            if (neededDays(weights, mid) <= days) {\n                ans = mid;\n                R = mid - 1;\n            } else {\n                L = mid + 1;\n            }\n        }\n        cout << ans << '\\n';\n    }\n    return 0;\n}",
      "Java": "import java.io.*;\nimport java.util.*;\n\npublic class Main {\n    private static long neededDays(int[] w, long cap) {\n        long days = 1, cur = 0;\n        for (int weight : w) {\n            if (cur + weight <= cap) cur += weight;\n            else { days++; cur = weight; }\n        }\n        return days;\n    }\n\n    public static void main(String[] args) throws Exception {\n        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));\n        StringTokenizer st = new StringTokenizer(br.readLine());\n        int T = Integer.parseInt(st.nextToken());\n        StringBuilder sb = new StringBuilder();\n        while (T-- > 0) {\n            st = new StringTokenizer(br.readLine());\n            int N = Integer.parseInt(st.nextToken());\n            int days = Integer.parseInt(st.nextToken());\n            int[] w = new int[N];\n            st = new StringTokenizer(br.readLine());\n            long lo = 0, hi = 0;\n            for (int i = 0; i < N; ++i) {\n                w[i] = Integer.parseInt(st.nextToken());\n                lo = Math.max(lo, w[i]);\n                hi += w[i];\n            }\n            long L = lo, R = hi, ans = hi;\n            while (L <= R) {\n                long mid = L + (R - L) / 2;\n                if (neededDays(w, mid) <= days) {\n                    ans = mid;\n                    R = mid - 1;\n                } else {\n                    L = mid + 1;\n                }\n            }\n            sb.append(ans).append('\\n');\n        }\n        System.out.print(sb.toString());\n    }\n}",
      "Python3": "import sys\n\ndef needed_days(weights, cap):\n    days = 1\n    cur = 0\n    for w in weights:\n        if cur + w <= cap:\n            cur += w\n        else:\n            days += 1\n            cur = w\n    return days\n\ndef solve():\n    data = sys.stdin.read().strip().split()\n    if not data:\n        return\n    it = iter(data)\n    T = int(next(it))\n    out_lines = []\n    for _ in range(T):\n        N = int(next(it))\n        days = int(next(it))\n        weights = [int(next(it)) for _ in range(N)]\n        lo = max(weights)\n        hi = sum(weights)\n        while lo < hi:\n            mid = (lo + hi) // 2\n            if needed_days(weights, mid) <= days:\n                hi = mid\n            else:\n                lo = mid + 1\n        out_lines.append(str(lo))\n    sys.stdout.write('\\n'.join(out_lines))\n\nif __name__ == \"__main__\":\n    solve()\n",
      "JavaScript": "const lines = require('fs').readFileSync(0,'utf-8').trim().split('\\n');\nlet i=0;\nfunction neededDays(weights, cap) {\n    let days = 1, cur = 0;\n    for (const w of weights) {\n        if (cur + w <= cap) cur += w;\n        else { days++; cur = w; }\n    }\n    return days;\n}\nconst T = Number(lines[i++]);\nlet outputs = [];\nfor (let t = 0; t < T; ++t) {\n    const [N, days] = lines[i++].trim().split(' ').map(Number);\n    const weights = lines[i++].trim().split(' ').map(Number);\n    let lo = 0, hi = 0;\n    for (let i = 0; i < N; ++i) {\n        const w = weights[i];\n        if (w > lo) lo = w;\n        hi += w;\n    }\n    let L = lo, R = hi, ans = hi;\n    while (L <= R) {\n        const mid = Math.floor((L + R) / 2);\n        if (neededDays(weights, mid) <= days) {\n            ans = mid;\n            R = mid - 1;\n        } else {\n            L = mid + 1;\n        }\n    }\n    outputs.push(String(ans));\n}\nconsole.log(outputs.join('\\n'));\n"
    }
  },
  "QBCategory": "CCD",
  "boilerPlates": {
    "C": "#include <stdio.h>\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "C++": "#include <iostream>\nusing namespace std;\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "Java": "import java.util.*;\nimport java.io.*;\n\npublic class Main {\n    public static void main(String[] args) throws IOException {\n        // your code goes here\n    }\n}",
    "Python3": "# your code goes here\n",
    "JavaScript": "// your code goes here\n"
  },
  "_internal": {
    "methodName": "",
    "params": []
  }
}