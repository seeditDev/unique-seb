{
  "questionId": "Q5591",
  "title": "ChefGame",
  "slug": "chefgame",
  "version": 1,
  "createdAt": "2026-07-01",
  "updatedAt": "2026-07-04",
  "metadata": {
    "category": "Graph",
    "difficulty": "Hard",
    "isPremium": true,
    "tags": [
      "Kruskal's Algorithm",
      "Union-Find",
      "MST"
    ],
    "companyTags": [],
    "topicTags": [
      "Graphs"
    ],
    "QBCategory": "CCD"
  },
  "content": {
    "problemStatement": "Everyone knows that ChefLand is the best country in the world and all of its people are the happiest. However, nobody knows their secret of being happy.\n\n\nSuraj is an Indian scientist and his goal is to research the life of ChefLandian people. He has already arrived in ChefTown, the capital of ChefLand, and met Chef. He promised to reveal their secret to Suraj if he gets maximal score in a special ChefLandian game. The game has following rules:\n\n\nIn **D**-dimensional Euclidean space, **N** lattice points are given. Every pair of points are not connected initially. The points are **not** guaranteed to be distinct.\nLet **(A1, A2, ..., AD)** and **(B1, B2, ..., BD)** be the coordinates of the points **A** and **B** respectively, then the distance between **A** and **B** is defined as **( (A1−B1)2 + (A2−B2)2 + ... + (AD−BD)2 )1/2**.\nThe initial Suraj's score is **1**.\nSuraj is allowed to take as many turns as he wants.\nIn every turn, Suraj can connect an unconnected pair of two given points, if this new connection does not form a cycle. That is, Suraj cannot connect the pair of points **A** and **B**, if there exist points **X1, X2, ..., Xk** such that **A** connected to **X1**, **X1** connected to **X2**, **X2** connected to **X3**,..., and **Xk** connected to **B**.\nIn every time Suraj connects some points, his score is multiplied by the square of the distance between them.\nNow Suraj wonders what is the maximal score he can get. Write a program that will find this score. Since this number can be huge you should output it modulo **747474747**.",
    "constraints": [
      "1 ≤ T ≤ 6666",
      "1 ≤ N ≤ 6666",
      "1 ≤ D ≤ 5",
      "The absolute value of any integer given in the input does not exceed 100000000 (10^8)",
      "The sum of N in one input file does not exceed 6666"
    ],
    "inputFormat": "First line contains T, the number of test cases. Each test case starts with a line containing two space-separated integers N and D, where N is the number of lattice points and D is the dimension of the Euclidean space. The next N lines contain D space-separated integers, denoting the coordinates of the given lattice points.",
    "outputFormat": "For each test case, output the maximal score modulo 747474747.",
    "sampleTestCases": [
      {
        "id": "sample_1",
        "input": "1\n3 2\n0 0\n-1 -1\n1 -1",
        "output": "8",
        "explanation": "The distance between the first point and the second point is sqrt((0-(-1))^2 + (0-(-1))^2) = sqrt(2). The distance between the first point and the third point is sqrt((0-1)^2 + (0-(-1))^2) = sqrt(2). The distance between the second point and the third point is sqrt(((-1)-1)^2 + ((-1)-(-1))^2) = 2. One of the optimal ways is that Suraj connects the third and the second points, and then connects the first and the second points. The maximum score is 2^2 * (sqrt(2))^2 = 8."
      }
    ]
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "label": "Case 1",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_2",
        "label": "Case 2",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_3",
        "label": "Case 3",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_4",
        "label": "Case 4",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_5",
        "label": "Case 5",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      }
    ],
    "totalTestCases": 5,
    "totalWeight": 10
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      2,
      2,
      2,
      2,
      2
    ],
    "maxScore": 10
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "To solve this problem, we can use Kruskal's algorithm to find the maximum spanning tree of the given points. We will calculate the distance between each pair of points and store them in a list of edges. Then, we will sort the edges in descending order of their weights (distances). We will use a Union-Find data structure to keep track of the connected components. We will iterate over the sorted edges and add them to the MST if they do not form a cycle. The product of the weights of the edges in the MST will give us the maximal score.",
    "timeComplexity": "O(T * (N^2 * log(N) + N * D)) per test case, O(T * (N^2 * log(N) + N * D)) total.",
    "spaceComplexity": "O(N^2 + N * D) — for storing the edges and the Union-Find data structure.",
    "code": {
      "C": "#include <stdio.h>\n#include <stdlib.h>\n#include <math.h>\n#define MOD 747474747\n#define MAX_N 6666\n#define MAX_D 5\n\n// Define the structure for an edge\ntypedef struct {\n    int u, v;\n    long long w;\n} Edge;\n\n// Define the structure for the Union-Find data structure\ntypedef struct {\n    int parent[MAX_N];\n    int rank[MAX_N];\n} UnionFind;\n\n// Function to find the parent of a node in the Union-Find data structure\nint find(UnionFind *uf, int x) {\n    if (uf->parent[x] != x) {\n        uf->parent[x] = find(uf, uf->parent[x]);\n    }\n    return uf->parent[x];\n}\n\n// Function to union two nodes in the Union-Find data structure\nvoid unionNodes(UnionFind *uf, int x, int y) {\n    int rootX = find(uf, x);\n    int rootY = find(uf, y);\n    if (rootX != rootY) {\n        if (uf->rank[rootX] < uf->rank[rootY]) {\n            uf->parent[rootX] = rootY;\n        } else if (uf->rank[rootX] > uf->rank[rootY]) {\n            uf->parent[rootY] = rootX;\n        } else {\n            uf->parent[rootY] = rootX;\n            uf->rank[rootX]++;\n        }\n    }\n}\n\nint main() {\n    int T;\n    scanf(\"%d\", &T);\n    while (T--) {\n        int N, D;\n        scanf(\"%d %d\", &N, &D);\n        int points[N][D];\n        for (int i = 0; i < N; i++) {\n            for (int j = 0; j < D; j++) {\n                scanf(\"%d\", &points[i][j]);\n            }\n        }\n        Edge edges[N * (N - 1) / 2];\n        int edgeCount = 0;\n        for (int i = 0; i < N; i++) {\n            for (int j = i + 1; j < N; j++) {\n                long long distance = 0;\n                for (int k = 0; k < D; k++) {\n                    distance += (long long)(points[i][k] - points[j][k]) * (points[i][k] - points[j][k]);\n                }\n                edges[edgeCount].u = i;\n                edges[edgeCount].v = j;\n                edges[edgeCount].w = distance;\n                edgeCount++;\n            }\n        }\n        // Sort the edges in descending order of their weights\n        for (int i = 0; i < edgeCount - 1; i++) {\n            for (int j = i + 1; j < edgeCount; j++) {\n                if (edges[i].w < edges[j].w) {\n                    Edge temp = edges[i];\n                    edges[i] = edges[j];\n                    edges[j] = temp;\n                }\n            }\n        }\n        UnionFind uf;\n        for (int i = 0; i < N; i++) {\n            uf.parent[i] = i;\n            uf.rank[i] = 0;\n        }\n        long long score = 1;\n        for (int i = 0; i < edgeCount; i++) {\n            if (find(&uf, edges[i].u) != find(&uf, edges[i].v)) {\n                unionNodes(&uf, edges[i].u, edges[i].v);\n                score = (score * edges[i].w) % MOD;\n            }\n        }\n        printf(\"%lld\\n\", score);\n    }\n    return 0;\n}",
      "C++": "#include <iostream>\n#include <vector>\n#include <algorithm>\n#include <cmath>\n#define MOD 747474747\n#define MAX_N 6666\n#define MAX_D 5\nusing namespace std;\n\n// Define the structure for an edge\nstruct Edge {\n    int u, v;\n    long long w;\n};\n\n// Define the structure for the Union-Find data structure\nstruct UnionFind {\n    int parent[MAX_N];\n    int rank[MAX_N];\n};\n\n// Function to find the parent of a node in the Union-Find data structure\nint find(UnionFind *uf, int x) {\n    if (uf->parent[x] != x) {\n        uf->parent[x] = find(uf, uf->parent[x]);\n    }\n    return uf->parent[x];\n}\n\n// Function to union two nodes in the Union-Find data structure\nvoid unionNodes(UnionFind *uf, int x, int y) {\n    int rootX = find(uf, x);\n    int rootY = find(uf, y);\n    if (rootX != rootY) {\n        if (uf->rank[rootX] < uf->rank[rootY]) {\n            uf->parent[rootX] = rootY;\n        } else if (uf->rank[rootX] > uf->rank[rootY]) {\n            uf->parent[rootY] = rootX;\n        } else {\n            uf->parent[rootY] = rootX;\n            uf->rank[rootX]++;\n        }\n    }\n}\n\nint main() {\n    ios::sync_with_stdio(false);\n    cin.tie(NULL);\n    int T;\n    cin >> T;\n    while (T--) {\n        int N, D;\n        cin >> N >> D;\n        int points[N][D];\n        for (int i = 0; i < N; i++) {\n            for (int j = 0; j < D; j++) {\n                cin >> points[i][j];\n            }\n        }\n        vector<Edge> edges;\n        for (int i = 0; i < N; i++) {\n            for (int j = i + 1; j < N; j++) {\n                long long distance = 0;\n                for (int k = 0; k < D; k++) {\n                    distance += (long long)(points[i][k] - points[j][k]) * (points[i][k] - points[j][k]);\n                }\n                edges.push_back({i, j, distance});\n            }\n        }\n        // Sort the edges in descending order of their weights\n        sort(edges.begin(), edges.end(), [](const Edge &a, const Edge &b) {\n            return a.w > b.w;\n        });\n        UnionFind uf;\n        for (int i = 0; i < N; i++) {\n            uf.parent[i] = i;\n            uf.rank[i] = 0;\n        }\n        long long score = 1;\n        for (const auto &edge : edges) {\n            if (find(&uf, edge.u) != find(&uf, edge.v)) {\n                unionNodes(&uf, edge.u, edge.v);\n                score = (score * edge.w) % MOD;\n            }\n        }\n        cout << score << endl;\n    }\n    return 0;\n}",
      "Java": "import java.util.Arrays;\nimport java.util.Scanner;\npublic class Main {\n    static final int MOD = 747474747;\n    static final int MAX_N = 6666;\n    static final int MAX_D = 5;\n    public static void main(String[] args) {\n        Scanner scanner = new Scanner(System.in);\n        int T = scanner.nextInt();\n        while (T-- > 0) {\n            int N = scanner.nextInt();\n            int D = scanner.nextInt();\n            int[][] points = new int[N][D];\n            for (int i = 0; i < N; i++) {\n                for (int j = 0; j < D; j++) {\n                    points[i][j] = scanner.nextInt();\n                }\n            }\n            Edge[] edges = new Edge[N * (N - 1) / 2];\n            int edgeCount = 0;\n            for (int i = 0; i < N; i++) {\n                for (int j = i + 1; j < N; j++) {\n                    long distance = 0;\n                    for (int k = 0; k < D; k++) {\n                        distance += (long)(points[i][k] - points[j][k]) * (points[i][k] - points[j][k]);\n                    }\n                    edges[edgeCount] = new Edge(i, j, distance);\n                    edgeCount++;\n                }\n            }\n            // Sort the edges in descending order of their weights\n            Arrays.sort(edges, 0, edgeCount, (a, b) -> Long.compare(b.w, a.w));\n            UnionFind uf = new UnionFind(N);\n            long score = 1;\n            for (int i = 0; i < edgeCount; i++) {\n                if (uf.find(edges[i].u) != uf.find(edges[i].v)) {\n                    uf.union(edges[i].u, edges[i].v);\n                    score = (score * edges[i].w) % MOD;\n                }\n            }\n            System.out.println(score);\n        }\n    }\n    static class Edge {\n        int u, v;\n        long w;\n        Edge(int u, int v, long w) {\n            this.u = u;\n            this.v = v;\n            this.w = w;\n        }\n    }\n    static class UnionFind {\n        int[] parent;\n        int[] rank;\n        UnionFind(int n) {\n            parent = new int[n];\n            rank = new int[n];\n            for (int i = 0; i < n; i++) {\n                parent[i] = i;\n                rank[i] = 0;\n            }\n        }\n        int find(int x) {\n            if (parent[x] != x) {\n                parent[x] = find(parent[x]);\n            }\n            return parent[x];\n        }\n        void union(int x, int y) {\n            int rootX = find(x);\n            int rootY = find(y);\n            if (rootX != rootY) {\n                if (rank[rootX] < rank[rootY]) {\n                    parent[rootX] = rootY;\n                } else if (rank[rootX] > rank[rootY]) {\n                    parent[rootY] = rootX;\n                } else {\n                    parent[rootY] = rootX;\n                    rank[rootX]++;\n                }\n            }\n        }\n    }\n}",
      "Python3": "import sys\nimport math\nMOD = 747474747\nMAX_N = 6666\nMAX_D = 5\n\n# Define the structure for an edge\nclass Edge:\n    def __init__(self, u, v, w):\n        self.u = u\n        self.v = v\n        self.w = w\n\n# Define the structure for the Union-Find data structure\nclass UnionFind:\n    def __init__(self, n):\n        self.parent = list(range(n))\n        self.rank = [0] * n\n\n    def find(self, x):\n        if self.parent[x] != x:\n            self.parent[x] = self.find(self.parent[x])\n        return self.parent[x]\n\n    def union(self, x, y):\n        rootX = self.find(x)\n        rootY = self.find(y)\n        if rootX != rootY:\n            if self.rank[rootX] < self.rank[rootY]:\n                self.parent[rootX] = rootY\n            elif self.rank[rootX] > self.rank[rootY]:\n                self.parent[rootY] = rootX\n            else:\n                self.parent[rootY] = rootX\n                self.rank[rootX] += 1\n\ndef main():\n    input = sys.stdin.readline\n    T = int(input())\n    for _ in range(T):\n        N, D = map(int, input().split())\n        points = [list(map(int, input().split())) for _ in range(N)]\n        edges = []\n        for i in range(N):\n            for j in range(i + 1, N):\n                distance = sum((points[i][k] - points[j][k]) ** 2 for k in range(D))\n                edges.append(Edge(i, j, distance))\n        # Sort the edges in descending order of their weights\n        edges.sort(key=lambda x: x.w, reverse=True)\n        uf = UnionFind(N)\n        score = 1\n        for edge in edges:\n            if uf.find(edge.u) != uf.find(edge.v):\n                uf.union(edge.u, edge.v)\n                score = (score * edge.w) % MOD\n        print(score)\n\nif __name__ == '__main__':\n    main()",
      "JavaScript": "const fs = require('fs');\nconst input = fs.readFileSync(0, 'utf-8').trim().split('\\n');\nlet i = 0;\nconst MOD = 747474747;\nconst MAX_N = 6666;\nconst MAX_D = 5;\n\n// Define the structure for an edge\nclass Edge {\n    constructor(u, v, w) {\n        this.u = u;\n        this.v = v;\n        this.w = w;\n    }\n}\n\n// Define the structure for the Union-Find data structure\nclass UnionFind {\n    constructor(n) {\n        this.parent = Array.from({ length: n }, (_, i) => i);\n        this.rank = Array(n).fill(0);\n    }\n\n    find(x) {\n        if (this.parent[x] !== x) {\n            this.parent[x] = this.find(this.parent[x]);\n        }\n        return this.parent[x];\n    }\n\n    union(x, y) {\n        const rootX = this.find(x);\n        const rootY = this.find(y);\n        if (rootX !== rootY) {\n            if (this.rank[rootX] < this.rank[rootY]) {\n                this.parent[rootX] = rootY;\n            } else if (this.rank[rootX] > this.rank[rootY]) {\n                this.parent[rootY] = rootX;\n            } else {\n                this.parent[rootY] = rootX;\n                this.rank[rootX]++;\n            }\n        }\n    }\n}\n\nfunction main() {\n    const T = parseInt(input[i++]);\n    for (let t = 0; t < T; t++) {\n        const [N, D] = input[i++].trim().split(' ').map(Number);\n        const points = [];\n        for (let j = 0; j < N; j++) {\n            points.push(input[i++].trim().split(' ').map(Number));\n        }\n        const edges = [];\n        for (let j = 0; j < N; j++) {\n            for (let k = j + 1; k < N; k++) {\n                let distance = 0;\n                for (let l = 0; l < D; l++) {\n                    distance += (points[j][l] - points[k][l]) ** 2;\n                }\n                edges.push(new Edge(j, k, distance));\n            }\n        }\n        // Sort the edges in descending order of their weights\n        edges.sort((a, b) => b.w - a.w);\n        const uf = new UnionFind(N);\n        let score = 1;\n        for (const edge of edges) {\n            if (uf.find(edge.u) !== uf.find(edge.v)) {\n                uf.union(edge.u, edge.v);\n                score = (score * edge.w) % MOD;\n            }\n        }\n        console.log(score);\n    }\n}\n\nmain();"
    }
  },
  "QBCategory": "CCD",
  "boilerPlates": {
    "C": "#include <stdio.h>\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "C++": "#include <iostream>\nusing namespace std;\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "Java": "import java.util.*;\nimport java.io.*;\n\npublic class Main {\n    public static void main(String[] args) throws IOException {\n        // your code goes here\n    }\n}",
    "Python3": "# your code goes here\n",
    "JavaScript": "// your code goes here\n"
  },
  "_internal": {
    "methodName": "",
    "params": []
  }
}