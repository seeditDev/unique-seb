{
  "questionId": "Q7973",
  "title": "Prefix And",
  "slug": "prefix-and",
  "version": 1,
  "createdAt": "2026-07-02",
  "updatedAt": "2026-07-04",
  "metadata": {
    "category": "Bit Manipulation | Greedy",
    "difficulty": "Hard",
    "isPremium": true,
    "tags": [
      "Greedy",
      "Bitwise AND",
      "Prefix",
      "Sorting"
    ],
    "companyTags": [],
    "topicTags": [
      "Dynamic Programming"
    ],
    "QBCategory": "CCD"
  },
  "content": {
    "problemStatement": "After learning about the bitwise AND operation, Ridbit challenged Ashishgup to solve the following problem.\n\nLet's denote the bitwise AND of one or more integers a1,a2,…,aka_1, a_2, \\ldots, a_ka1​,a2​,…,ak​ by AND(a1,a2,…,ak)AND(a_1, a_2, \\ldots, a_k)AND(a1​,a2​,…,ak​). For example, AND(7,3,5)=1AND(7, 3, 5) = 1AND(7,3,5)=1. In particular, for an integer xxx, AND(x)=xAND(x) = xAND(x)=x.\n\nGiven a sequence of integers A1,A2,…,ANA_1, A_2, \\ldots, A_NA1​,A2​,…,AN​, Ridbit wants Ashishgup to reorder its elements (possibly leaving the sequence unchanged) in such a way that in the resulting sequence AAA, the sum of the values of bitwise AND for all prefixes of the sequence, i.e. ∑i=1NAND(A1,A2,…Ai)\\sum_{i=1}^N AND(A_1, A_2, \\ldots A_i)∑i=1N​AND(A1​,A2​,…Ai​), is maximum possible.\n\nFind the maximum value of this sum.",
    "constraints": [
      "1 <= T <= 1000",
      "1 <= N <= 2^20",
      "0 <= Ai < N",
      "Sum of N over all test cases does not exceed 2^20"
    ],
    "inputFormat": "The first line contains an integer T, the number of test cases. Each test case starts with an integer N, the length of the array. The next line contains N space‑separated integers A1 … AN.",
    "outputFormat": "For each test case output a single line with the maximum possible sum of bitwise AND values of all prefixes after an optimal reordering.",
    "sampleTestCases": [
      {
        "id": "sample_1",
        "input": "2\n3\n0 1 2\n4\n1 3 1 2",
        "output": "2\n5",
        "explanation": "First test: order [2,1,0] gives prefix ANDs 2,0,0 sum=2. Second test: order [3,1,1,2] gives 3,1,1,0 sum=5."
      }
    ]
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "label": "minimum boundary",
        "input": "1\n1\n5",
        "expectedOutput": "5",
        "weight": 2
      },
      {
        "id": "tc_2",
        "label": "all equal non‑zero",
        "input": "1\n3\n7 7 7",
        "expectedOutput": "21",
        "weight": 2
      },
      {
        "id": "tc_3",
        "label": "all zeros",
        "input": "1\n4\n0 0 0 0",
        "expectedOutput": "0",
        "weight": 2
      },
      {
        "id": "tc_4",
        "label": "mixed decreasing bits",
        "input": "1\n5\n6 4 2 1 0",
        "expectedOutput": "10",
        "weight": 2
      },
      {
        "id": "tc_5",
        "label": "varied values",
        "input": "1\n5\n15 8 7 3 1",
        "expectedOutput": "23",
        "weight": 2
      }
    ],
    "totalTestCases": 5,
    "totalWeight": 10
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      2,
      2,
      2,
      2,
      2
    ],
    "maxScore": 10
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "The AND of a prefix can only stay the same or lose bits when we add more elements. To keep the sum large we want the prefix AND to stay as high as possible for as many steps as possible. Greedy strategy: 1) Place the largest element first – it gives the biggest first prefix value. 2) While the current prefix AND is non‑zero, repeatedly choose an unused element that maximises (current AND element). This keeps the most bits alive. After the current AND becomes zero, any remaining order contributes 0, so we can stop. The loop runs at most the number of bits (≤31 for the given limits) because each iteration must drop at least one bit, giving O(N·bits) time.",
    "timeComplexity": "O(N * B) per test case, where B ≤ 31 is the number of bits (practically ≤ 31).",
    "spaceComplexity": "O(N) for the array and a used‑flag array.",
    "code": {
      "C": "#include <stdio.h>\n#include <stdbool.h>\nint main(){\n    int T; if(scanf(\"%d\",&T)!=1) return 0;\n    while(T--){\n        int N; scanf(\"%d\",&N);\n        int a[N];\n        for(int i=0;i<N;i++) scanf(\"%d\",&a[i]);\n        bool used[N]; for(int i=0;i<N;i++) used[i]=false;\n        int maxIdx=0; for(int i=1;i<N;i++) if(a[i]>a[maxIdx]) maxIdx=i;\n        int cur=a[maxIdx]; used[maxIdx]=true; long long ans=cur;\n        while(cur!=0){\n            int best=-1, bestIdx=-1;\n            for(int i=0;i<N;i++) if(!used[i]){\n                int val=cur & a[i];\n                if(val>best){ best=val; bestIdx=i; }\n            }\n            if(bestIdx==-1) break;\n            used[bestIdx]=true; cur=best; ans+=cur;\n        }\n        printf(\"%lld\\n\",ans);\n    }\n    return 0;\n}",
      "C++": "#include <bits/stdc++.h>\nusing namespace std;\nint main(){\n    ios::sync_with_stdio(false);\n    cin.tie(nullptr);\n    int T; if(!(cin>>T)) return 0;\n    while(T--){\n        int N; cin>>N;\n        vector<int> a(N);\n        for(int &x:a) cin>>x;\n        vector<char> used(N,0);\n        int maxIdx=0;\n        for(int i=1;i<N;i++) if(a[i]>a[maxIdx]) maxIdx=i;\n        int cur=a[maxIdx]; used[maxIdx]=1;\n        long long ans=cur;\n        while(cur!=0){\n            int best=-1,bestIdx=-1;\n            for(int i=0;i<N;i++) if(!used[i]){\n                int val=cur & a[i];\n                if(val>best){ best=val; bestIdx=i; }\n            }\n            if(bestIdx==-1) break;\n            used[bestIdx]=1;\n            cur=best; ans+=cur;\n        }\n        cout<<ans<<\"\\n\";\n    }\n    return 0;\n}",
      "Java": "import java.io.*;\nimport java.util.*;\npublic class Main {\n    public static void main(String[] args) throws Exception {\n        FastScanner fs = new FastScanner(System.in);\n        int T = fs.nextInt();\n        StringBuilder sb = new StringBuilder();\n        while (T-- > 0) {\n            int N = fs.nextInt();\n            int[] a = new int[N];\n            for (int i = 0; i < N; i++) a[i] = fs.nextInt();\n            boolean[] used = new boolean[N];\n            int maxIdx = 0;\n            for (int i = 1; i < N; i++) if (a[i] > a[maxIdx]) maxIdx = i;\n            int cur = a[maxIdx];\n            used[maxIdx] = true;\n            long ans = cur;\n            while (cur != 0) {\n                int best = -1, bestIdx = -1;\n                for (int i = 0; i < N; i++) if (!used[i]) {\n                    int val = cur & a[i];\n                    if (val > best) { best = val; bestIdx = i; }\n                }\n                if (bestIdx == -1) break;\n                used[bestIdx] = true;\n                cur = best;\n                ans += cur;\n            }\n            sb.append(ans).append('\\n');\n        }\n        System.out.print(sb.toString());\n    }\n    // Fast scanner for speed\n    static class FastScanner {\n        private final InputStream in;\n        private final byte[] buffer = new byte[1 << 16];\n        private int ptr = 0, len = 0;\n        FastScanner(InputStream in) { this.in = in; }\n        private int readByte() throws IOException {\n            if (ptr >= len) {\n                len = in.read(buffer);\n                ptr = 0;\n                if (len <= 0) return -1;\n            }\n            return buffer[ptr++];\n        }\n        int nextInt() throws IOException {\n            int c, sign = 1, val = 0;\n            do { c = readByte(); } while (c <= ' ');\n            if (c == '-') { sign = -1; c = readByte(); }\n            while (c > ' ') { val = val * 10 + (c - '0'); c = readByte(); }\n            return val * sign;\n        }\n    }\n}",
      "Python3": "#!/usr/bin/env python3\nimport sys\n\ndef solve():\n    data = list(map(int, sys.stdin.read().split()))\n    it = iter(data)\n    t = next(it)\n    out = []\n    for _ in range(t):\n        n = next(it)\n        a = [next(it) for _ in range(n)]\n        used = [False] * n\n        max_idx = 0\n        for i in range(1, n):\n            if a[i] > a[max_idx]:\n                max_idx = i\n        cur = a[max_idx]\n        used[max_idx] = True\n        ans = cur\n        while cur != 0:\n            best = -1\n            best_idx = -1\n            for i in range(n):\n                if not used[i]:\n                    val = cur & a[i]\n                    if val > best:\n                        best = val\n                        best_idx = i\n            if best_idx == -1:\n                break\n            used[best_idx] = True\n            cur = best\n            ans += cur\n        out.append(str(ans))\n    sys.stdout.write('\\n'.join(out))\n\nif __name__ == \"__main__\":\n    solve()\n",
      "JavaScript": "const fs = require('fs');\nconst data = fs.readFileSync(0, 'utf-8').trim().split(/\\s+/).map(Number);\nlet idx = 0;\nconst T = data[idx++];\nlet out = [];\nfor (let tc = 0; tc < T; tc++) {\n  const N = data[idx++];\n  const a = data.slice(idx, idx + N);\n  idx += N;\n  const used = new Array(N).fill(false);\n  let maxIdx = 0;\n  for (let i = 1; i < N; i++) if (a[i] > a[maxIdx]) maxIdx = i;\n  let cur = a[maxIdx];\n  used[maxIdx] = true;\n  let ans = cur;\n  while (cur !== 0) {\n    let best = -1, bestIdx = -1;\n    for (let i = 0; i < N; i++) if (!used[i]) {\n      const val = cur & a[i];\n      if (val > best) { best = val; bestIdx = i; }\n    }\n    if (bestIdx === -1) break;\n    used[bestIdx] = true;\n    cur = best;\n    ans += cur;\n  }\n  out.push(String(ans));\n}\nconsole.log(out.join('\\n'));\n"
    }
  },
  "QBCategory": "CCD",
  "boilerPlates": {
    "C": "#include <stdio.h>\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "C++": "#include <iostream>\nusing namespace std;\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "Java": "import java.util.*;\nimport java.io.*;\n\npublic class Main {\n    public static void main(String[] args) throws IOException {\n        // your code goes here\n    }\n}",
    "Python3": "# your code goes here\n",
    "JavaScript": "// your code goes here\n"
  },
  "_internal": {
    "methodName": "",
    "params": []
  }
}