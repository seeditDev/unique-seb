{
  "questionId": "Q1153",
  "title": "CSV Data Analyzer - Final Touch",
  "slug": "csv-data-analyzer-final-touch",
  "version": 1,
  "createdAt": "2026-07-01",
  "updatedAt": "2026-07-04",
  "metadata": {
    "category": "Math",
    "difficulty": "Easy",
    "isPremium": false,
    "tags": [
      "Variance",
      "Math",
      "Mode",
      "Statistics",
      "Median"
    ],
    "companyTags": [],
    "topicTags": [
      "Strings & Streams"
    ]
  },
  "content": {
    "problemStatement": "**Awesome - You have successfully built a Powerful CSV Data Analyzer!**\n\nNow that you've successfully implemented all core functionalities, here are some exciting improvements you can explore to make your project even better:\n\n**1. Enhanced Filtering Capabilities:**\n\n\n\nUpgrade the filter function to support:\n\n\n\n\nNumerical comparisons (`&gt;`, `&lt;`, `&gt;=`, `&lt;=`) for numeric columns\n\n\n\n\nPartial text matches (contains, starts with, ends with)\n\n\n\n\n\n**2. Sorting Options:**\n\n\nImplement sorting functionality based on a specific column.\n**3. Calculate additional statistics:**\n\n\n\nMedian of Data\n\n\n\n\nVariance and mode",
    "constraints": [
      "1 <= T <= 10",
      "1 <= N <= 1000",
      "-10^6 <= value <= 10^6"
    ],
    "inputFormat": "The first line contains an integer T, the number of test cases. For each test case: the first line contains a column name (a string without spaces) and an integer N, the number of rows, separated by a space. The next N lines each contain a single integer value belonging to that column.",
    "outputFormat": "For each test case output two lines: 1. All N values sorted in non-decreasing order, separated by a single space and ending with a newline. 2. \"Median: M Variance: V Mode: X\" where M is the median (printed with one digit after the decimal point), V is the population variance (one digit after the decimal point) and X is the mode (the smallest value among those with maximal frequency).",
    "sampleTestCases": [
      {
        "id": "sample_1",
        "input": "1\\nage 5\\n30\\n20\\n40\\n20\\n50\\n",
        "output": "20 20 30 40 50\\nMedian: 30.0 Variance: 140.0 Mode: 20\\n",
        "explanation": "The five ages are sorted. Median is the middle element (30). Mean = 32, variance = ((20-32)^2*2 + (30-32)^2 + (40-32)^2 + (50-32)^2)/5 = 140.0. The value 20 appears twice, more than any other, so mode = 20."
      },
      {
        "id": "sample_2",
        "input": "1\\nscore 4\\n5\\n5\\n5\\n5\\n",
        "output": "5 5 5 5\\nMedian: 5.0 Variance: 0.0 Mode: 5\\n",
        "explanation": "All scores are identical, so sorted list is unchanged. Median = (5+5)/2 = 5.0, variance = 0, mode = 5."
      },
      {
        "id": "sample_3",
        "input": "1\\ntemp 6\\n-3\\n7\\n-3\\n2\\n7\\n0\\n",
        "output": "-3 -3 0 2 7 7\\nMedian: 1.0 Variance: 20.0 Mode: -3\\n",
        "explanation": "Sorted temperatures: -3 -3 0 2 7 7. Median = (0+2)/2 = 1.0. Mean = ( -3-3+0+2+7+7 )/6 = 1.0, variance = sum((x-1)^2)/6 = 20.0. Both -3 and 7 appear twice; the smaller value -3 is chosen as mode."
      }
    ]
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "label": "minimum boundary",
        "input": "1\\nvalue 1\\n0\\n",
        "expectedOutput": "0\\nMedian: 0.0 Variance: 0.0 Mode: 0\\n",
        "weight": 2
      },
      {
        "id": "tc_2",
        "label": "maximum boundary",
        "input": "1\\nvalue 10\\n1000000\\n1000000\\n1000000\\n1000000\\n1000000\\n1000000\\n1000000\\n1000000\\n1000000\\n",
        "expectedOutput": "1000000 1000000 1000000 1000000 1000000 1000000 1000000 1000000 1000000 1000000\\nMedian: 1000000.0 Variance: 0.0 Mode: 1000000",
        "weight": 2
      },
      {
        "id": "tc_3",
        "label": "uniform/edge case",
        "input": "1\\nvalue 5\\n0\\n0\\n0\\n0\\n0\\n",
        "expectedOutput": "0 0 0 0 0\\nMedian: 0.0 Variance: 0.0 Mode: 0",
        "weight": 2
      },
      {
        "id": "tc_4",
        "label": "tricky case",
        "input": "1\\nvalue 5\\n-3\\n-3\\n-3\\n-3\\n-3\\n",
        "expectedOutput": "-3 -3 -3 -3 -3\\nMedian: -3.0 Variance: 0.0 Mode: -3",
        "weight": 2
      },
      {
        "id": "tc_5",
        "label": "stress/general",
        "input": "2\\nvalue 3\\n1\\n2\\n3\\nvalue 4\\n4\\n5\\n6\\n7\\n",
        "expectedOutput": "1 2 3\\nMedian: 2.0 Variance: 1.0 Mode: 1\\n4 5 6 7\\nMedian: 5.5 Variance: 2.5 Mode: 4",
        "weight": 2
      }
    ],
    "totalTestCases": 5,
    "totalWeight": 10
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      2,
      2,
      2,
      2,
      2
    ],
    "maxScore": 10
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "To solve this problem, we first read the input and store the values in an array. Then, we sort the array in non-decreasing order. Next, we calculate the median, variance, and mode of the array. The median is the middle value in the sorted array. The variance is the average of the squared differences from the mean. The mode is the value that appears most frequently in the array.",
    "timeComplexity": "O(T * N log N) per test case, O(T * N log N) total. The sorting operation dominates the time complexity.",
    "spaceComplexity": "O(N) \u2014 the dominant structure is the array to store the input values.",
    "code": {
      "C": "#include <stdio.h>\n#include <stdlib.h>\n#include <string.h>\n#include <limits.h>\n\nint cmp_int(const void *a, const void *b) {\n    int x = *(const int *)a;\n    int y = *(const int *)b;\n    return (x > y) - (x < y);\n}\n\nint main() {\n    int T;\n    if (scanf(\"%d\", &T) != 1) return 0;\n    while (T--) {\n        char col[101];\n        int n;\n        scanf(\"%s %d\", col, &n);\n        int *arr = (int *)malloc(n * sizeof(int));\n        for (int i = 0; i < n; ++i) {\n            scanf(\"%d\", &arr[i]);\n        }\n        qsort(arr, n, sizeof(int), cmp_int);\n        // output sorted values\n        for (int i = 0; i < n; ++i) {\n            printf(\"%d\", arr[i]);\n            if (i + 1 < n) printf(\" \");\n        }\n        printf(\"\\n\");\n        // median\n        double median;\n        if (n % 2 == 1) median = arr[n/2];\n        else median = (arr[n/2 - 1] + arr[n/2]) / 2.0;\n        // mean for variance\n        double sum = 0.0;\n        for (int i = 0; i < n; ++i) sum += arr[i];\n        double mean = sum / n;\n        double var = 0.0;\n        for (int i = 0; i < n; ++i) {\n            double diff = arr[i] - mean;\n            var += diff * diff;\n        }\n        var /= n;\n        // mode\n        // frequency map using simple array offset (values are within [-1e6,1e6])\n        const int OFFSET = 1000000;\n        static int freq[2000001];\n        memset(freq, 0, sizeof(freq));\n        int maxFreq = 0;\n        int mode = INT_MAX;\n        for (int i = 0; i < n; ++i) {\n            int idx = arr[i] + OFFSET;\n            freq[idx]++;\n            if (freq[idx] > maxFreq || (freq[idx] == maxFreq && arr[i] < mode)) {\n                maxFreq = freq[idx];\n                mode = arr[i];\n            }\n        }\n        printf(\"Median: %.1f Variance: %.1f Mode: %d\\n\", median, var, mode);\n        free(arr);\n    }\n    return 0;\n}",
      "C++": "#include <bits/stdc++.h>\nusing namespace std;\n\nint main() {\n    ios::sync_with_stdio(false);\n    cin.tie(nullptr);\n    int T; if(!(cin>>T)) return 0;\n    while(T--) {\n        string col; int n; cin>>col>>n;\n        vector<long long> a(n);\n        for(int i=0;i<n;++i) cin>>a[i];\n        sort(a.begin(), a.end());\n        for(int i=0;i<n;++i){\n            if(i) cout<<' ';\n            cout<<a[i];\n        }\n        cout<<\"\\n\";\n        double median;\n        if(n%2) median = a[n/2];\n        else median = (a[n/2-1]+a[n/2])/2.0;\n        double sum=0.0; for(auto v:a) sum+=v;\n        double mean=sum/n;\n        double var=0.0; for(auto v:a){ double d=v-mean; var+=d*d; }\n        var/=n;\n        unordered_map<long long,int> mp;\n        int maxFreq=0; long long mode=LLONG_MAX;\n        for(auto v:a){\n            int f=++mp[v];\n            if(f>maxFreq || (f==maxFreq && v<mode)){\n                maxFreq=f; mode=v;\n            }\n        }\n        cout<<fixed<<setprecision(1);\n        cout<<\"Median: \"<<median<<\" Variance: \"<<var<<\" Mode: \"<<mode<<\"\\n\";\n    }\n    return 0;\n}",
      "Java": "import java.io.*;\nimport java.util.*;\n\npublic class Main {\n    public static void main(String[] args) throws Exception {\n        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));\n        StringTokenizer st = new StringTokenizer(br.readLine());\n        int T = Integer.parseInt(st.nextToken());\n        StringBuilder out = new StringBuilder();\n        for (int tc = 0; tc < T; tc++) {\n            st = new StringTokenizer(br.readLine());\n            String col = st.nextToken();\n            int n = Integer.parseInt(st.nextToken());\n            long[] a = new long[n];\n            for (int i = 0; i < n; i++) {\n                a[i] = Long.parseLong(br.readLine().trim());\n            }\n            Arrays.sort(a);\n            for (int i = 0; i < n; i++) {\n                if (i > 0) out.append(' ');\n                out.append(a[i]);\n            }\n            out.append('\\n');\n            double median;\n            if (n % 2 == 1) median = a[n/2];\n            else median = (a[n/2 - 1] + a[n/2]) / 2.0;\n            double sum = 0.0;\n            for (long v : a) sum += v;\n            double mean = sum / n;\n            double var = 0.0;\n            for (long v : a) {\n                double d = v - mean;\n                var += d * d;\n            }\n            var /= n;\n            Map<Long,Integer> freq = new HashMap<>();\n            int maxFreq = 0;\n            long mode = Long.MAX_VALUE;\n            for (long v : a) {\n                int f = freq.getOrDefault(v, 0) + 1;\n                freq.put(v, f);\n                if (f > maxFreq || (f == maxFreq && v < mode)) {\n                    maxFreq = f;\n                    mode = v;\n                }\n            }\n            out.append(String.format(Locale.US, \"Median: %.1f Variance: %.1f Mode: %d\\n\", median, var, mode));\n        }\n        System.out.print(out.toString());\n    }\n}",
      "Python3": "import sys\n\ndef solve():\n    data = sys.stdin.read().strip().splitlines()\n    if not data:\n        return\n    it = iter(data)\n    T = int(next(it))\n    out_lines = []\n    for _ in range(T):\n        col_n = next(it).split()\n        col = col_n[0]\n        n = int(col_n[1])\n        values = []\n        for _ in range(n):\n            values.append(int(next(it).strip()))\n        values.sort()\n        out_lines.append(' '.join(map(str, values)))\n        # median\n        if n % 2 == 1:\n            median = float(values[n//2])\n        else:\n            median = (values[n//2 - 1] + values[n//2]) / 2.0\n        # variance (population)\n        mean = sum(values) / n\n        var = sum((x - mean) ** 2 for x in values) / n\n        # mode (smallest with highest frequency)\n        from collections import Counter\n        cnt = Counter(values)\n        max_freq = max(cnt.values())\n        mode = min(v for v, f in cnt.items() if f == max_freq)\n        out_lines.append(f\"Median: {median:.1f} Variance: {var:.1f} Mode: {mode}\")\n    sys.stdout.write('\\n'.join(out_lines))\n\nif __name__ == \"__main__\":\n    solve()",
      "JavaScript": "const fs = require('fs');\nconst input = fs.readFileSync(0, 'utf-8').trim().split('\\n');\nlet idx = 0;\nconst T = Number(input[idx++]);\nlet output = [];\nfor (let t = 0; t < T; t++) {\n  const [col, nStr] = input[idx++].trim().split(/\\s+/);\n  const n = Number(nStr);\n  let arr = new Array(n);\n  for (let i = 0; i < n; i++) {\n    arr[i] = Number(input[idx++].trim());\n  }\n  arr.sort((a, b) => a - b);\n  output.push(arr.join(' '));\n  // median\n  let median;\n  if (n % 2 === 1) median = arr[Math.floor(n / 2)];\n  else median = (arr[n / 2 - 1] + arr[n / 2]) / 2.0;\n  // variance\n  const mean = arr.reduce((s, v) => s + v, 0) / n;\n  const variance = arr.reduce((s, v) => {\n    const d = v - mean;\n    return s + d * d;\n  }, 0) / n;\n  // mode\n  const freq = new Map();\n  let maxFreq = 0;\n  let mode = Infinity;\n  for (const v of arr) {\n    const f = (freq.get(v) || 0) + 1;\n    freq.set(v, f);\n    if (f > maxFreq || (f === maxFreq && v < mode)) {\n      maxFreq = f;\n      mode = v;\n    }\n  }\n  output.push(`Median: ${median.toFixed(1)} Variance: ${variance.toFixed(1)} Mode: ${mode}`);\n}\nconsole.log(output.join('\\n'));\n"
    }
  }
}