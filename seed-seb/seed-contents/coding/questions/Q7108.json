{
  "questionId": "Q7108",
  "title": "Medu Vada Maze",
  "slug": "medu-vada-maze",
  "version": 1,
  "createdAt": "2026-07-02",
  "updatedAt": "2026-07-04",
  "metadata": {
    "category": "Graph",
    "difficulty": "Medium",
    "isPremium": true,
    "tags": [
      "BFS",
      "Maze",
      "Pathfinding"
    ],
    "companyTags": [],
    "topicTags": [
      "Graphs"
    ],
    "QBCategory": "CCD"
  },
  "content": {
    "problemStatement": "A maze, for our purposes, consists of a rectangular grid of cells, where some of the cells are blocked and the remaining cells are empty. A mouse is placed in one of the empty cells and a dosa is placed at a different empty cell. From a cell, the mouse can move to any empty neighbouring cell according to the rules given below. Your aim is to determine whether the mouse can walk through this maze and reach the dosa.\n\nA maze with RRR rows and CCC columns will be presented as a sequence of RRR lines, each with CCC characters. The character # denotes a blocked cell and the character . denotes an empty cell). The mouse and the dosa sit in distinct empty cells and those cells are marked by M and D instead of .\n\nHere is a maze with 666 rows and 121212 columns.\n\n \n\n`  ####.#..####\n  .M.#..#..D.#\n  #.#...#....#\n  ...#..#..#..\n  ...#.#.###.#\n  .........###\n` \n\nWe will refer to a cell by its row and column position. The rows are numbered from top to bottom and the columns from left to right. For example, in the maze above, the mouse is initially placed at position (222,222) indicating second row and second column and the dosa is at position (222,101010) indicating second row and tenth column.\n\nThe mouse can move from a cell to the one above it, the one below it, the one to its left or the one to its right. Further, from a cell in the leftmost column the mouse can move to the cell in the same row in the rightmost column and from a cell in the rightmost column it can move to the cell in the same row in the leftmost column. Similarly, from a cell in the top row the mouse can move to the cell in the same column in the bottom row and from a cell in the bottom row it can move to the cell in the same column in the top row. Thus, in the maze above, the mouse can move from (444,111) to (444,121212) and from (666,555) to (111,555) and so on.\n\nYou should think of the maze as having the shape of a solid ring, like a Medu Vada. It is as if the paper on which the Maze is drawn is rolled back and stuck at the edge so that the bottom row touches the top row. The resulting cylinder is rolled around so that the right and left columns, which now form circles, touch each other. Thus, the left most column and right most column touch each other, as do the top row and the bottom row.\n\nIn the example above, the mouse can reach the dosa by walking through the following sequence of cells: (222,222), (333,222), (444,222), (444,111), (444,121212), (444,111111), (333,111111), (333,101010) and (222,101010). This path is marked with with `x`'s below:\n\n \n\n`  ####.#..####\n  .M.#..#..D.#\n  #x#...#..xx# \n  xx.#..#..#xx\n  ...#.#.###.# \n  .........###\n` \n\nAlternatively it can also take the following path:\n\n \n\n`  ####.#.x####\n  .M.#..#xxD.#\n  #x#...#....# \n  .x.#..#..#..\n  .x.#.#.###.#\n  .xxxxxxx.###\n` \n\nYour task is to determine whether it possible for the mouse to reach the dosa.\n\nInput:The first line of the input will contain two numbers RRR and CCC denoting the number and rows and columns respectively. This is followed by RRR lines, each with CCC characters, each of which is # or . or M or D. There is exactly one M and one D in the input.\n\nOutput:If there is no path from the mouse to the dosa print a single line containing the word NO. If there are paths from the mouse to the dosa, then the first line of the output should consist of the word YES. Following this must be R lines of C characters each, where each character is # or . or x or M or D, describing a path from M to D using the x's as illustrated above. There may be many paths and it suffices to describe one path.",
    "constraints": [
      "1 <= R <= 1000",
      "1 <= C <= 1000",
      "In 80% of the inputs, 3 <= R <= 60 and 3 <= C <= 60"
    ],
    "inputFormat": "The first line contains two integers R and C, denoting the number of rows and columns in the maze. This is followed by R lines, each containing C characters, which can be '#', '.', 'M', or 'D'.",
    "outputFormat": "If there is no path from the mouse to the dosa, print 'NO'. Otherwise, print 'YES' followed by R lines of C characters each, where each character is '#', '.', 'x', 'M', or 'D', describing a path from M to D using 'x's.",
    "sampleTestCases": [
      {
        "id": "sample_1",
        "input": "6 12\n####.#..####\n.M.#..#..D.#\n#.#...#....#\n...#..#..#..\n...#.#.###.#\n.........###",
        "output": "YES\n####.#..####\n.M.#..#..D.#\n#x#...#..xx#\nxx.#..#..#xx\n...#.#.###.#\n.........###",
        "explanation": ""
      },
      {
        "id": "sample_2",
        "input": "4 6\n##.#..\n.M.#.#\n#.#.D.\n...#.#",
        "output": "NO",
        "explanation": ""
      }
    ]
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "label": "Case 1",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_2",
        "label": "Case 2",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_3",
        "label": "Case 3",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_4",
        "label": "Case 4",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_5",
        "label": "Case 5",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      }
    ],
    "totalTestCases": 5,
    "totalWeight": 10
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      2,
      2,
      2,
      2,
      2
    ],
    "maxScore": 10
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "To solve this problem, we can use a breadth-first search (BFS) algorithm. We start by finding the positions of the mouse and the dosa in the maze. Then, we perform a BFS from the mouse's position, exploring all possible paths in the maze. If we reach the dosa's position during the BFS, we can construct the path taken and print it. Otherwise, we print 'NO'.",
    "timeComplexity": "O(R*C) — the time complexity is linear with respect to the size of the maze, as in the worst case, we need to visit every cell in the maze.",
    "spaceComplexity": "O(R*C) — the space complexity is also linear with respect to the size of the maze, as we need to store the visited cells and the path taken.",
    "code": {
      "C": "// COMPLETE, COMPILABLE C code\n#include <stdio.h>\n#include <stdlib.h>\n#include <string.h>\n#define MAX_R 1000\n#define MAX_C 1000\n\n// Structure to represent a cell in the maze\ntypedef struct {\n    int r, c;\n} Cell;\n\n// Function to perform BFS\nvoid bfs(char maze[MAX_R][MAX_C], int R, int C, Cell start, Cell end) {\n    // Create a queue to hold the cells to be visited\n    Cell queue[MAX_R * MAX_C];\n    int front = 0, rear = 0;\n    queue[rear++] = start;\n\n    // Create a 2D array to keep track of visited cells\n    int visited[MAX_R][MAX_C] = {{0}};\n    visited[start.r][start.c] = 1;\n\n    // Create a 2D array to store the parent of each cell\n    Cell parent[MAX_R][MAX_C];\n\n    // Perform BFS\n    while (front < rear) {\n        Cell current = queue[front++];\n        if (current.r == end.r && current.c == end.c) {\n            // If we reach the end, construct the path and print it\n            printf(\"YES\\n\");\n            while (current.r != start.r || current.c != start.c) {\n                maze[current.r][current.c] = 'x';\n                current = parent[current.r][current.c];\n            }\n            for (int i = 0; i < R; i++) {\n                printf(\"%s\\n\", maze[i]);\n            }\n            return;\n        }\n\n        // Explore all possible directions from the current cell\n        int dr[] = {-1, 1, 0, 0};\n        int dc[] = {0, 0, -1, 1};\n        for (int i = 0; i < 4; i++) {\n            int nr = current.r + dr[i];\n            int nc = current.c + dc[i];\n\n            // Check if the new cell is within the maze boundaries\n            if (nr < 0 || nr >= R || nc < 0 || nc >= C) {\n                continue;\n            }\n\n            // Check if the new cell is a wall or has been visited before\n            if (maze[nr][nc] == '#' || visited[nr][nc]) {\n                continue;\n            }\n\n            // Mark the new cell as visited and add it to the queue\n            visited[nr][nc] = 1;\n            queue[rear++] = (Cell){nr, nc};\n            parent[nr][nc] = current;\n        }\n    }\n\n    // If we reach this point, it means there is no path from the start to the end\n    printf(\"NO\\n\");\n}\n\nint main() {\n    int R, C;\n    scanf(\"%d %d\", &R, &C);\n\n    // Read the maze\n    char maze[MAX_R][MAX_C];\n    for (int i = 0; i < R; i++) {\n        scanf(\"%s\", maze[i]);\n    }\n\n    // Find the start and end positions\n    Cell start, end;\n    for (int i = 0; i < R; i++) {\n        for (int j = 0; j < C; j++) {\n            if (maze[i][j] == 'M') {\n                start = (Cell){i, j};\n            } else if (maze[i][j] == 'D') {\n                end = (Cell){i, j};\n            }\n        }\n    }\n\n    // Perform BFS\n    bfs(maze, R, C, start, end);\n\n    return 0;\n}",
      "C++": "// COMPLETE, COMPILABLE C++ code\n#include <iostream>\n#include <queue>\n#include <vector>\nusing namespace std;\n\n// Structure to represent a cell in the maze\nstruct Cell {\n    int r, c;\n};\n\n// Function to perform BFS\nvoid bfs(vector<string>& maze, int R, int C, Cell start, Cell end) {\n    // Create a queue to hold the cells to be visited\n    queue<Cell> q;\n    q.push(start);\n\n    // Create a 2D array to keep track of visited cells\n    vector<vector<bool>> visited(R, vector<bool>(C, false));\n    visited[start.r][start.c] = true;\n\n    // Create a 2D array to store the parent of each cell\n    vector<vector<Cell>> parent(R, vector<Cell>(C));\n\n    // Perform BFS\n    while (!q.empty()) {\n        Cell current = q.front();\n        q.pop();\n        if (current.r == end.r && current.c == end.c) {\n            // If we reach the end, construct the path and print it\n            cout << \"YES\" << endl;\n            while (current.r != start.r || current.c != start.c) {\n                maze[current.r][current.c] = 'x';\n                current = parent[current.r][current.c];\n            }\n            for (int i = 0; i < R; i++) {\n                cout << maze[i] << endl;\n            }\n            return;\n        }\n\n        // Explore all possible directions from the current cell\n        int dr[] = {-1, 1, 0, 0};\n        int dc[] = {0, 0, -1, 1};\n        for (int i = 0; i < 4; i++) {\n            int nr = current.r + dr[i];\n            int nc = current.c + dc[i];\n\n            // Check if the new cell is within the maze boundaries\n            if (nr < 0 || nr >= R || nc < 0 || nc >= C) {\n                continue;\n            }\n\n            // Check if the new cell is a wall or has been visited before\n            if (maze[nr][nc] == '#' || visited[nr][nc]) {\n                continue;\n            }\n\n            // Mark the new cell as visited and add it to the queue\n            visited[nr][nc] = true;\n            q.push((Cell){nr, nc});\n            parent[nr][nc] = current;\n        }\n    }\n\n    // If we reach this point, it means there is no path from the start to the end\n    cout << \"NO\" << endl;\n}\n\nint main() {\n    int R, C;\n    cin >> R >> C;\n\n    // Read the maze\n    vector<string> maze(R);\n    for (int i = 0; i < R; i++) {\n        cin >> maze[i];\n    }\n\n    // Find the start and end positions\n    Cell start, end;\n    for (int i = 0; i < R; i++) {\n        for (int j = 0; j < C; j++) {\n            if (maze[i][j] == 'M') {\n                start = (Cell){i, j};\n            } else if (maze[i][j] == 'D') {\n                end = (Cell){i, j};\n            }\n        }\n    }\n\n    // Perform BFS\n    bfs(maze, R, C, start, end);\n\n    return 0;\n}",
      "Java": "// COMPLETE, COMPILABLE Java code\nimport java.util.LinkedList;\nimport java.util.Queue;\nimport java.util.Scanner;\n\npublic class Main {\n    // Structure to represent a cell in the maze\n    static class Cell {\n        int r, c;\n        Cell(int r, int c) {\n            this.r = r;\n            this.c = c;\n        }\n    }\n\n    // Function to perform BFS\n    public static void bfs(char[][] maze, int R, int C, Cell start, Cell end) {\n        // Create a queue to hold the cells to be visited\n        Queue<Cell> q = new LinkedList<>();\n        q.add(start);\n\n        // Create a 2D array to keep track of visited cells\n        boolean[][] visited = new boolean[R][C];\n        visited[start.r][start.c] = true;\n\n        // Create a 2D array to store the parent of each cell\n        Cell[][] parent = new Cell[R][C];\n\n        // Perform BFS\n        while (!q.isEmpty()) {\n            Cell current = q.poll();\n            if (current.r == end.r && current.c == end.c) {\n                // If we reach the end, construct the path and print it\n                System.out.println(\"YES\");\n                while (current.r != start.r || current.c != start.c) {\n                    maze[current.r][current.c] = 'x';\n                    current = parent[current.r][current.c];\n                }\n                for (int i = 0; i < R; i++) {\n                    System.out.println(new String(maze[i]));\n                }\n                return;\n            }\n\n            // Explore all possible directions from the current cell\n            int dr[] = {-1, 1, 0, 0};\n            int dc[] = {0, 0, -1, 1};\n            for (int i = 0; i < 4; i++) {\n                int nr = current.r + dr[i];\n                int nc = current.c + dc[i];\n\n                // Check if the new cell is within the maze boundaries\n                if (nr < 0 || nr >= R || nc < 0 || nc >= C) {\n                    continue;\n                }\n\n                // Check if the new cell is a wall or has been visited before\n                if (maze[nr][nc] == '#' || visited[nr][nc]) {\n                    continue;\n                }\n\n                // Mark the new cell as visited and add it to the queue\n                visited[nr][nc] = true;\n                q.add(new Cell(nr, nc));\n                parent[nr][nc] = current;\n            }\n        }\n\n        // If we reach this point, it means there is no path from the start to the end\n        System.out.println(\"NO\");\n    }\n\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int R = sc.nextInt();\n        int C = sc.nextInt();\n        sc.nextLine();\n\n        // Read the maze\n        char[][] maze = new char[R][C];\n        for (int i = 0; i < R; i++) {\n            maze[i] = sc.nextLine().toCharArray();\n        }\n\n        // Find the start and end positions\n        Cell start = null, end = null;\n        for (int i = 0; i < R; i++) {\n            for (int j = 0; j < C; j++) {\n                if (maze[i][j] == 'M') {\n                    start = new Cell(i, j);\n                } else if (maze[i][j] == 'D') {\n                    end = new Cell(i, j);\n                }\n            }\n        }\n\n        // Perform BFS\n        bfs(maze, R, C, start, end);\n    }\n}",
      "Python3": "# COMPLETE, RUNNABLE Python3 code\nfrom collections import deque\n\n# Structure to represent a cell in the maze\nclass Cell:\n    def __init__(self, r, c):\n        self.r = r\n        self.c = c\n\n# Function to perform BFS\ndef bfs(maze, R, C, start, end):\n    # Create a queue to hold the cells to be visited\n    q = deque([start])\n\n    # Create a 2D array to keep track of visited cells\n    visited = [[False]*C for _ in range(R)]\n    visited[start.r][start.c] = True\n\n    # Create a 2D array to store the parent of each cell\n    parent = [[None]*C for _ in range(R)]\n\n    # Perform BFS\n    while q:\n        current = q.popleft()\n        if current.r == end.r and current.c == end.c:\n            # If we reach the end, construct the path and print it\n            print(\"YES\")\n            while current.r != start.r or current.c != start.c:\n                maze[current.r][current.c] = 'x'\n                current = parent[current.r][current.c]\n            for row in maze:\n                print(''.join(row))\n            return\n\n        # Explore all possible directions from the current cell\n        dr = [-1, 1, 0, 0]\n        dc = [0, 0, -1, 1]\n        for i in range(4):\n            nr = current.r + dr[i]\n            nc = current.c + dc[i]\n\n            # Check if the new cell is within the maze boundaries\n            if nr < 0 or nr >= R or nc < 0 or nc >= C:\n                continue\n\n            # Check if the new cell is a wall or has been visited before\n            if maze[nr][nc] == '#' or visited[nr][nc]:\n                continue\n\n            # Mark the new cell as visited and add it to the queue\n            visited[nr][nc] = True\n            q.append(Cell(nr, nc))\n            parent[nr][nc] = current\n\n    # If we reach this point, it means there is no path from the start to the end\n    print(\"NO\")\n\n# Read the maze\nR, C = map(int, input().split())\nmaze = [list(input()) for _ in range(R)]\n\n# Find the start and end positions\nstart = None\nend = None\nfor i in range(R):\n    for j in range(C):\n        if maze[i][j] == 'M':\n            start = Cell(i, j)\n        elif maze[i][j] == 'D':\n            end = Cell(i, j)\n\n# Perform BFS\nbfs(maze, R, C, start, end)",
      "JavaScript": ""
    }
  },
  "QBCategory": "CCD",
  "boilerPlates": {
    "C": "#include <stdio.h>\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "C++": "#include <iostream>\nusing namespace std;\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "Java": "import java.util.*;\nimport java.io.*;\n\npublic class Main {\n    public static void main(String[] args) throws IOException {\n        // your code goes here\n    }\n}",
    "Python3": "# your code goes here\n",
    "JavaScript": "// your code goes here\n"
  },
  "_internal": {
    "methodName": "",
    "params": []
  }
}