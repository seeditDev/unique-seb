{
  "questionId": "Q7805",
  "title": "Help Martha",
  "slug": "help-martha",
  "version": 1,
  "createdAt": "2026-07-02",
  "updatedAt": "2026-07-04",
  "metadata": {
    "category": "Greedy | Strings",
    "difficulty": "Medium",
    "isPremium": true,
    "tags": [
      "Subsequence",
      "Counting",
      "Greedy",
      "Prefix sums not needed"
    ],
    "companyTags": [],
    "topicTags": [
      "Data Structures"
    ],
    "QBCategory": "CCD"
  },
  "content": {
    "problemStatement": "Jonas asked Martha to stand at point (x1,y1)(x_1,y_1)(x1​,y1​) on an infinite cartesian plane. He gave her a string SSS which consists of operations in the form of characters **'R'**, **'L'**, **'U'**, and\n**'D'**.\n\nEach character of the string corresponds to the following movements:\n\n\n**'R'** means moving right by 111 unit i.e, (x,y)(x,y)(x,y) to (x+1,y)(x+1,y)(x+1,y).\n**'L'** means moving left by 111 unit i.e, (x,y)(x,y)(x,y) to (x−1,y)(x-1,y)(x−1,y).\n**'U'** means moving up by 111 unit i.e, (x,y)(x,y)(x,y) to (x,y+1)(x,y+1)(x,y+1).\n**'D'** means moving down by 111 unit i.e, (x,y)(x,y)(x,y) to (x,y−1)(x,y-1)(x,y−1).\nNow, Jonas asks Martha QQQ queries. In each query, coordinates are given of form (x2,y2)(x_2,y_2)(x2​,y2​). Martha needs to tell if it is possible to move from (x1,y1)(x_1,y_1)(x1​,y1​) to (x2,y2)(x_2,y_2)(x2​,y2​) by choosing a **subsequence** of given string SSS and performing the operations in sequential order.Starting point (x1,y1)(x_1,y_1)(x1​,y1​) remains same for all queries.\n\nIf it is possible, print **\"YES\"** and the length of the smallest possible subsequence of string, else print **\"NO\"**.\n\nRecall that string aaa is a **subsequence** of a string bbb if aaa can be obtained from bbb by deletion of several (possibly zero or all) characters. For example, for the string aaa=\"codechef\", the following strings are subsequences: \"code\", \"ee\", \"odchef\", \"df\", \"\", and others, but the following are not subsequences: \"ced\", \"eehc\", \"cofe\".\n\n**Note**: Use of Fast IO is recommended.\n\n###Input:\n\n\nFirst line will contain TTT, number of test cases. Then test cases follow.\nFor each test case, first line will contain string SSS, string of operations.\nSecond line will contain coordinates x1x_1x1​ and y1y_1y1​.\nThird line will contain QQQ, number of queries.\nQQQ lines follow. Each line will contain coordinates x2x_2x2​ and y2y_2y2​.\n###Output:\nFor each query if subsequence exists then print \"YES\" (without quotes) and length of smallest possible subsequence separated by a space in new line, else print \"NO\" (without quotes) in new line.\n\n###Constraints\n\n\n1≤T≤101 \\leq T \\leq 101≤T≤10\n1≤∣S∣≤1061 \\leq |S| \\leq 10^61≤∣S∣≤106\n−108≤x1,x2,y1,y2≤108-10^8 \\leq x_1,x_2,y_1,y_2 \\leq 10^8−108≤x1​,x2​,y1​,y2​≤108\n1≤Q≤1051 \\leq Q \\leq 10^51≤Q≤105",
    "constraints": [
      "1 <= T <= 10",
      "1 <= |S| <= 10^6",
      "-10^8 <= x1, y1, x2, y2 <= 10^8",
      "1 <= Q <= 10^5",
      "Sum of |S| over all test cases ≤ 10^6",
      "Sum of Q over all test cases ≤ 10^5"
    ],
    "inputFormat": "The first line contains an integer T, the number of test cases. For each test case:\n- A line with a string S consisting only of characters 'R', 'L', 'U', 'D'.\n- A line with two integers x1 y1, the starting coordinates.\n- A line with an integer Q, the number of queries.\n- Q lines follow, each containing two integers x2 y2, the target coordinates for that query.",
    "outputFormat": "For each query, output a line:\n- If it is possible to reach (x2, y2) from the fixed start (x1, y1) using a subsequence of S, print \"YES\" followed by a space and the minimum possible length of such a subsequence.\n- Otherwise print \"NO\".\nEach answer is printed on its own line, in the order of the queries.",
    "sampleTestCases": [
      {
        "id": "sample_1",
        "input": "1\nRLUDRLUD\n1 2\n2\n2 3\n1 -2",
        "output": "YES 2\nNO",
        "explanation": "The string contains two R, two L, two U, two D. From (1,2) to (2,3) we need one R and one U, both are available, minimal length = 2. From (1,2) to (1,-2) we need 4 D moves but only 2 D are present, so impossible."
      }
    ]
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "label": "Case 1",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_2",
        "label": "Case 2",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_3",
        "label": "Case 3",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_4",
        "label": "Case 4",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_5",
        "label": "Case 5",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      }
    ],
    "totalTestCases": 5,
    "totalWeight": 10
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      2,
      2,
      2,
      2,
      2
    ],
    "maxScore": 10
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "For each test case we only need the total number of each operation present in S. Let cntR, cntL, cntU, cntD be these counts.\nFor a query we compute the required displacement:\n    dx = x2 - x1, dy = y2 - y1.\nThe needed moves are:\n    needR = max(dx, 0), needL = max(-dx, 0), needU = max(dy, 0), needD = max(-dy, 0).\nIf cntR >= needR && cntL >= needL && cntU >= needU && cntD >= needD then the target is reachable. The smallest subsequence length is the total number of moves we actually have to perform, i.e. needR+needL+needU+needD = |dx| + |dy|. Otherwise the answer is \"NO\".\nThe order of the chosen moves does not matter because any ordering of the required moves yields the same final coordinates, and a subsequence can always be formed by picking the needed characters in the order they appear in S.\nThe algorithm therefore runs in O(|S| + Q) per test case.\nImplementation details:\n- Count the four characters in a single pass over S.\n- Process each query independently using the formula above.\n- Use fast I/O because the input size can be large.",
    "timeComplexity": "O(|S| + Q) per test case. Over all test cases O(total |S| + total Q).",
    "spaceComplexity": "O(1) besides the input storage (only four counters are needed).",
    "code": {
      "C": "#include <stdio.h>\n#include <string.h>\n#include <stdlib.h>\n\nint main(){\n    int T; if(scanf(\"%d\", &T)!=1) return 0;\n    while(T--){\n        char S[1000005];\n        scanf(\"%s\", S);\n        long long x1,y1; scanf(\"%lld %lld\", &x1, &y1);\n        long long cntR=0,cntL=0,cntU=0,cntD=0;\n        for(char *p=S; *p; ++p){\n            if(*p=='R') cntR++;\n            else if(*p=='L') cntL++;\n            else if(*p=='U') cntU++;\n            else if(*p=='D') cntD++;\n        }\n        int Q; scanf(\"%d\", &Q);\n        while(Q--){\n            long long x2,y2; scanf(\"%lld %lld\", &x2, &y2);\n            long long dx = x2 - x1;\n            long long dy = y2 - y1;\n            long long needR = dx>0?dx:0;\n            long long needL = dx<0?-dx:0;\n            long long needU = dy>0?dy:0;\n            long long needD = dy<0?-dy:0;\n            if(cntR>=needR && cntL>=needL && cntU>=needU && cntD>=needD){\n                printf(\"YES %lld\\n\", needR+needL+needU+needD);\n            }else{\n                printf(\"NO\\n\");\n            }\n        }\n    }\n    return 0;\n}\n",
      "C++": "#include <bits/stdc++.h>\nusing namespace std;\nint main(){\n    ios::sync_with_stdio(false);\n    cin.tie(nullptr);\n    int T; if(!(cin>>T)) return 0;\n    while(T--){\n        string S; cin>>S;\n        long long x1,y1; cin>>x1>>y1;\n        long long cntR=0,cntL=0,cntU=0,cntD=0;\n        for(char c: S){\n            if(c=='R') ++cntR;\n            else if(c=='L') ++cntL;\n            else if(c=='U') ++cntU;\n            else if(c=='D') ++cntD;\n        }\n        int Q; cin>>Q;\n        while(Q--){\n            long long x2,y2; cin>>x2>>y2;\n            long long dx = x2 - x1;\n            long long dy = y2 - y1;\n            long long needR = max(dx, 0LL);\n            long long needL = max(-dx, 0LL);\n            long long needU = max(dy, 0LL);\n            long long needD = max(-dy, 0LL);\n            if(cntR>=needR && cntL>=needL && cntU>=needU && cntD>=needD){\n                cout << \"YES \" << (needR+needL+needU+needD) << '\\n';\n            }else{\n                cout << \"NO\\n\";\n            }\n        }\n    }\n    return 0;\n}\n",
      "Java": "import java.io.*;\nimport java.util.*;\n\npublic class Main {\n    public static void main(String[] args) throws Exception {\n        FastScanner fs = new FastScanner(System.in);\n        int T = fs.nextInt();\n        StringBuilder out = new StringBuilder();\n        while (T-- > 0) {\n            String S = fs.next();\n            long x1 = fs.nextLong();\n            long y1 = fs.nextLong();\n            long cntR = 0, cntL = 0, cntU = 0, cntD = 0;\n            for (int i = 0; i < S.length(); i++) {\n                char c = S.charAt(i);\n                switch (c) {\n                    case 'R': cntR++; break;\n                    case 'L': cntL++; break;\n                    case 'U': cntU++; break;\n                    case 'D': cntD++; break;\n                }\n            }\n            int Q = fs.nextInt();\n            while (Q-- > 0) {\n                long x2 = fs.nextLong();\n                long y2 = fs.nextLong();\n                long dx = x2 - x1;\n                long dy = y2 - y1;\n                long needR = Math.max(dx, 0);\n                long needL = Math.max(-dx, 0);\n                long needU = Math.max(dy, 0);\n                long needD = Math.max(-dy, 0);\n                if (cntR >= needR && cntL >= needL && cntU >= needU && cntD >= needD) {\n                    out.append(\"YES \").append(needR + needL + needU + needD).append('\\n');\n                } else {\n                    out.append(\"NO\\n\");\n                }\n            }\n        }\n        System.out.print(out.toString());\n    }\n\n    // Fast scanner for large input\n    static class FastScanner {\n        private final InputStream in;\n        private final byte[] buffer = new byte[1 << 16];\n        private int ptr = 0, len = 0;\n        FastScanner(InputStream in) { this.in = in; }\n        private int readByte() throws IOException {\n            if (ptr >= len) {\n                len = in.read(buffer);\n                ptr = 0;\n                if (len <= 0) return -1;\n            }\n            return buffer[ptr++];\n        }\n        private boolean isSpace(int c) { return c <= ' '; }\n        String next() throws IOException {\n            StringBuilder sb = new StringBuilder();\n            int c;\n            while ((c = readByte()) != -1 && isSpace(c));\n            while (c != -1 && !isSpace(c)) {\n                sb.append((char) c);\n                c = readByte();\n            }\n            return sb.toString();\n        }\n        int nextInt() throws IOException { return (int) nextLong(); }\n        long nextLong() throws IOException {\n            int c;\n            while ((c = readByte()) != -1 && isSpace(c));\n            boolean neg = false;\n            if (c == '-') { neg = true; c = readByte(); }\n            long val = 0;\n            while (c != -1 && !isSpace(c)) {\n                val = val * 10 + (c - '0');\n                c = readByte();\n            }\n            return neg ? -val : val;\n        }\n    }\n}\n",
      "Python3": "import sys\n\ndef solve():\n    data = sys.stdin.read().split()\n    it = iter(data)\n    t = int(next(it))\n    out_lines = []\n    for _ in range(t):\n        S = next(it)\n        x1 = int(next(it)); y1 = int(next(it))\n        cntR = S.count('R')\n        cntL = S.count('L')\n        cntU = S.count('U')\n        cntD = S.count('D')\n        q = int(next(it))\n        for __ in range(q):\n            x2 = int(next(it)); y2 = int(next(it))\n            dx = x2 - x1\n            dy = y2 - y1\n            needR = dx if dx > 0 else 0\n            needL = -dx if dx < 0 else 0\n            needU = dy if dy > 0 else 0\n            needD = -dy if dy < 0 else 0\n            if cntR >= needR and cntL >= needL and cntU >= needU and cntD >= needD:\n                out_lines.append(f\"YES {needR+needL+needU+needD}\")\n            else:\n                out_lines.append(\"NO\")\n    sys.stdout.write('\\n'.join(out_lines))\n\nif __name__ == \"__main__\":\n    solve()\n",
      "JavaScript": "const fs = require('fs');\nconst input = fs.readFileSync(0, 'utf-8').trim().split(/\\s+/);\nlet idx = 0;\nconst T = Number(input[idx++]);\nlet output = [];\nfor (let tc = 0; tc < T; tc++) {\n  const S = input[idx++];\n  const x1 = Number(input[idx++]);\n  const y1 = Number(input[idx++]);\n  let cntR = 0, cntL = 0, cntU = 0, cntD = 0;\n  for (let ch of S) {\n    if (ch === 'R') cntR++;\n    else if (ch === 'L') cntL++;\n    else if (ch === 'U') cntU++;\n    else if (ch === 'D') cntD++;\n  }\n  const Q = Number(input[idx++]);\n  for (let i = 0; i < Q; i++) {\n    const x2 = Number(input[idx++]);\n    const y2 = Number(input[idx++]);\n    const dx = x2 - x1;\n    const dy = y2 - y1;\n    const needR = dx > 0 ? dx : 0;\n    const needL = dx < 0 ? -dx : 0;\n    const needU = dy > 0 ? dy : 0;\n    const needD = dy < 0 ? -dy : 0;\n    if (cntR >= needR && cntL >= needL && cntU >= needU && cntD >= needD) {\n      output.push('YES ' + (needR + needL + needU + needD));\n    } else {\n      output.push('NO');\n    }\n  }\n}\nconsole.log(output.join('\\n'));\n"
    }
  },
  "QBCategory": "CCD",
  "boilerPlates": {
    "C": "#include <stdio.h>\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "C++": "#include <iostream>\nusing namespace std;\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "Java": "import java.util.*;\nimport java.io.*;\n\npublic class Main {\n    public static void main(String[] args) throws IOException {\n        // your code goes here\n    }\n}",
    "Python3": "# your code goes here\n",
    "JavaScript": "// your code goes here\n"
  },
  "_internal": {
    "methodName": "",
    "params": []
  }
}