{
  "questionId": "Q8519",
  "title": "RGB Construction",
  "slug": "rgb-construction",
  "version": 1,
  "createdAt": "2026-07-02",
  "updatedAt": "2026-07-04",
  "metadata": {
    "category": "Graph",
    "difficulty": "Hard",
    "isPremium": true,
    "tags": [
      "Tree Construction",
      "Constructive Algorithms",
      "Greedy"
    ],
    "companyTags": [],
    "topicTags": [
      "Constructive Algorithms"
    ],
    "QBCategory": "CCD"
  },
  "content": {
    "problemStatement": "Chef's friend JJ challenged him to construct a tree which has exactly RRR red coloured nodes, GGG green coloured nodes and BBB blue coloured nodes.\n\nTo make the task interesting he added an extra condition: the simple path between any two blue nodes should contain **at least** one red node and **at least** one green node.\n\nCan you help Chef to complete JJ's challenge by constructing any valid tree that satisfies these conditions?\n\n\nInput Format\n\nThe first line contains TTT - the number of test cases. Then the test cases follow.\nThe first line of each test case contains RRR, GGG, and BBB - the number of red, green and blue nodes respectively in the tree.\n\n\nOutput Format\n\nIf a tree can be constructed that fulfils all the requirements then\n\nIn the first line, output a string of length R+G+BR + G + BR+G+B in which the ithi^{th}ith character (1-based indexing) is either `R`, `G` or `B` denoting the colour of the ithi^{th}ith node as red, green or blue respectively.\nIn the following R+G+B−1R + G + B - 1R+G+B−1 lines, output two integers UUU and VVV denoting there is an edge between UthU^{th}Uth and VthV^{th}Vth node.\n\n\nIf no tree fulfils the requirements print `-1` in a single line.",
    "constraints": [
      "1 <= T <= 100",
      "1 <= R, G, B <= 1000",
      "3 <= R+G+B <= 1000"
    ],
    "inputFormat": "The first line contains an integer T, the number of test cases. Each of the next T lines contains three integers R, G and B — the required numbers of red, green and blue nodes respectively.",
    "outputFormat": "For each test case, if a required tree does not exist output a single line containing -1. Otherwise output a string of length N=R+G+B where the i‑th character is 'R', 'G' or 'B' denoting the colour of node i. Then output N‑1 lines, each containing two integers u v (1‑based) describing an edge of the tree.",
    "sampleTestCases": [
      {
        "id": "sample_1",
        "input": "3\n1 1 1\n1 1 2\n1 2 5",
        "output": "RBG\n1 2\n1 3\nBRGB\n1 2\n2 3\n3 4\n-1",
        "explanation": "Explanation:\nTest case 111: There is only one blue node. Hence, any valid tree with 111 red, 111 green and 111 blue node will pass the conditions.\nTest case 222: There are two blue nodes numbered 111 and 444 and the simple path between them passes through one red node numbered 222 and one green node numbered 333. Hence, this tree is valid.\nTest case 333: No valid tree can be constructed under the given constraints."
      }
    ]
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "label": "Case 1",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_2",
        "label": "Case 2",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_3",
        "label": "Case 3",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_4",
        "label": "Case 4",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_5",
        "label": "Case 5",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      }
    ],
    "totalTestCases": 5,
    "totalWeight": 10
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      2,
      2,
      2,
      2,
      2
    ],
    "maxScore": 10
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "The only restriction is that the simple path between any two blue nodes must contain at least one red and at least one green node. If two blue nodes share the same red parent, their path would miss a green node, therefore each blue node needs a distinct red parent. Consequently a necessary condition is R >= B. This condition is also sufficient. Construction: 1) create a chain of all G green nodes (if G>1) to keep the tree connected. 2) Choose the first green node (id 1) as the central green node. 3) For every red node (total R) connect it directly to the central green node. 4) For the first B red nodes, attach one blue leaf each. All remaining red nodes (if any) stay as leaves of the central green node. The resulting tree has N‑1 edges, is connected, and any path between two blue leaves goes: blue_i – red_i – central_green – red_j – blue_j, which certainly contains a red (red_i or red_j) and a green (central_green).",
    "timeComplexity": "O(R+G+B) per test case – we create each node and each edge once.",
    "spaceComplexity": "O(R+G+B) for storing the colour string and the edge list.",
    "code": {
      "C": "#include <stdio.h>\n#include <stdlib.h>\nint main(){\n    int T; if(scanf(\"%d\",&T)!=1) return 0;\n    while(T--){\n        int R,G,B; scanf(\"%d %d %d\",&R,&G,&B);\n        if(R<B){\n            printf(\"-1\\n\");\n            continue;\n        }\n        int N = R+G+B;\n        // colour string\n        char *col = (char*)malloc(N+1);\n        int idx=0;\n        for(int i=0;i<G;i++) col[idx++]='G';\n        for(int i=0;i<R;i++) col[idx++]='R';\n        for(int i=0;i<B;i++) col[idx++]='B';\n        col[N]='\\0';\n        printf(\"%s\\n\",col);\n        // edges\n        // green chain\n        for(int i=2;i<=G;i++) printf(\"%d %d\\n\",i-1,i);\n        int central = 1; // first green node\n        // connect all red nodes to central green\n        for(int i=1;i<=R;i++){\n            int red_id = G + i;\n            printf(\"%d %d\\n\",central,red_id);\n        }\n        // attach blue leaves to first B red nodes\n        for(int i=1;i<=B;i++){\n            int red_id = G + i;\n            int blue_id = G + R + i;\n            printf(\"%d %d\\n\",red_id,blue_id);\n        }\n        free(col);\n    }\n    return 0;\n}",
      "C++": "#include <bits/stdc++.h>\nusing namespace std;\nint main(){\n    ios::sync_with_stdio(false);\n    cin.tie(nullptr);\n    int T; if(!(cin>>T)) return 0;\n    while(T--){\n        int R,G,B; cin>>R>>G>>B;\n        if(R<B){\n            cout<<-1<<\"\\n\";\n            continue;\n        }\n        int N=R+G+B;\n        string col; col.reserve(N);\n        col.append(G,'G');\n        col.append(R,'R');\n        col.append(B,'B');\n        cout<<col<<\"\\n\";\n        // green chain\n        for(int i=2;i<=G;i++) cout<<i-1<<' '<<i<<\"\\n\";\n        int central=1; // first green node\n        // connect all red nodes to central green\n        for(int i=1;i<=R;i++){\n            int red_id = G + i;\n            cout<<central<<' '<<red_id<<\"\\n\";\n        }\n        // attach blue leaves\n        for(int i=1;i<=B;i++){\n            int red_id = G + i;\n            int blue_id = G + R + i;\n            cout<<red_id<<' '<<blue_id<<\"\\n\";\n        }\n    }\n    return 0;\n}",
      "Java": "import java.io.*;\nimport java.util.*;\npublic class Main {\n    public static void main(String[] args) throws Exception {\n        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));\n        StringBuilder out = new StringBuilder();\n        int T = Integer.parseInt(br.readLine().trim());\n        while (T-- > 0) {\n            StringTokenizer st = new StringTokenizer(br.readLine());\n            int R = Integer.parseInt(st.nextToken());\n            int G = Integer.parseInt(st.nextToken());\n            int B = Integer.parseInt(st.nextToken());\n            if (R < B) {\n                out.append(\"-1\\n\");\n                continue;\n            }\n            int N = R + G + B;\n            StringBuilder col = new StringBuilder(N);\n            for (int i = 0; i < G; i++) col.append('G');\n            for (int i = 0; i < R; i++) col.append('R');\n            for (int i = 0; i < B; i++) col.append('B');\n            out.append(col).append('\\n');\n            // green chain\n            for (int i = 2; i <= G; i++) out.append(i-1).append(' ').append(i).append('\\n');\n            int central = 1; // first green node\n            // connect all red nodes to central green\n            for (int i = 1; i <= R; i++) {\n                int redId = G + i;\n                out.append(central).append(' ').append(redId).append('\\n');\n            }\n            // attach blue leaves to first B red nodes\n            for (int i = 1; i <= B; i++) {\n                int redId = G + i;\n                int blueId = G + R + i;\n                out.append(redId).append(' ').append(blueId).append('\\n');\n            }\n        }\n        System.out.print(out.toString());\n    }\n}",
      "Python3": "#!/usr/bin/env python3\nimport sys\n\ndef solve():\n    data = sys.stdin.read().strip().split()\n    if not data:\n        return\n    it = iter(data)\n    T = int(next(it))\n    out_lines = []\n    for _ in range(T):\n        R = int(next(it)); G = int(next(it)); B = int(next(it))\n        if R < B:\n            out_lines.append('-1')\n            continue\n        N = R + G + B\n        col = 'G'*G + 'R'*R + 'B'*B\n        out_lines.append(col)\n        # green chain\n        for i in range(2, G+1):\n            out_lines.append(f\"{i-1} {i}\")\n        central = 1  # first green node\n        # connect all red nodes to central green\n        for i in range(1, R+1):\n            red_id = G + i\n            out_lines.append(f\"{central} {red_id}\")\n        # attach blue leaves to first B red nodes\n        for i in range(1, B+1):\n            red_id = G + i\n            blue_id = G + R + i\n            out_lines.append(f\"{red_id} {blue_id}\")\n    sys.stdout.write('\\n'.join(out_lines))\n\nif __name__ == \"__main__\":\n    solve()\n",
      "JavaScript": "const fs = require('fs');\nconst input = fs.readFileSync(0, 'utf-8').trim().split(/\\s+/);\nlet idx = 0;\nconst T = Number(input[idx++]);\nlet out = [];\nfor (let t = 0; t < T; t++) {\n  const R = Number(input[idx++]);\n  const G = Number(input[idx++]);\n  const B = Number(input[idx++]);\n  if (R < B) {\n    out.push('-1');\n    continue;\n  }\n  const N = R + G + B;\n  const col = 'G'.repeat(G) + 'R'.repeat(R) + 'B'.repeat(B);\n  out.push(col);\n  // green chain\n  for (let i = 2; i <= G; i++) out.push(`${i-1} ${i}`);\n  const central = 1; // first green node\n  // connect all red nodes to central green\n  for (let i = 1; i <= R; i++) {\n    const redId = G + i;\n    out.push(`${central} ${redId}`);\n  }\n  // attach blue leaves to first B red nodes\n  for (let i = 1; i <= B; i++) {\n    const redId = G + i;\n    const blueId = G + R + i;\n    out.push(`${redId} ${blueId}`);\n  }\n}\nconsole.log(out.join('\\n'));\n"
    }
  },
  "QBCategory": "CCD",
  "boilerPlates": {
    "C": "#include <stdio.h>\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "C++": "#include <iostream>\nusing namespace std;\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "Java": "import java.util.*;\nimport java.io.*;\n\npublic class Main {\n    public static void main(String[] args) throws IOException {\n        // your code goes here\n    }\n}",
    "Python3": "# your code goes here\n",
    "JavaScript": "// your code goes here\n"
  },
  "_internal": {
    "methodName": "",
    "params": []
  }
}