{
  "questionId": "Q8293",
  "title": "Magical Flips",
  "slug": "magical-flips",
  "version": 1,
  "createdAt": "2026-07-02",
  "updatedAt": "2026-07-04",
  "metadata": {
    "category": "Bit Manipulation | Greedy",
    "difficulty": "Hard",
    "isPremium": true,
    "tags": [
      "Bitwise AND",
      "Greedy",
      "Bit Manipulation"
    ],
    "companyTags": [],
    "topicTags": [
      "Bit Manipulation"
    ],
    "QBCategory": "CCD"
  },
  "content": {
    "problemStatement": "Chef found NNN magical cards in his drawer. Each card has a number written on each of its faces. He places all the cards on the table in a front face-up manner.\n\nChef denotes the numbers on the front face by A1,A2,...,ANA_1, A_2,..., A_NA1​,A2​,...,AN​ and on the back face by B1,B2,...,BNB_1, B_2,..., B_NB1​,B2​,...,BN​. Chef can choose some (possibly zero or all) cards and flip them, then he will calculate the bitwise AND of all the numbers currently in a face-up manner.\n\nNow Chef wonders what is the maximum bitwise AND that he can achieve and what is the minimum number of cards he has to flip to achieve this value. Can you help him find it?\n\n\nInput Format\n\nThe first line of the input contains a single integer TTT denoting the number of test cases. The description of TTT test cases follows.\nEach test case contains three lines of input.\nThe first line contains a single integer NNN - the number of cards.\nThe second line contains NNN space-separated integers A1,A2,...,ANA_1, A_2,..., A_NA1​,A2​,...,AN​ - the numbers on the front face of the cards\nThe third line contains NNN space-separated integers B1,B2,...,BNB_1, B_2,..., B_NB1​,B2​,...,BN​ - the numbers on the back face of the cards\n\n\nOutput Format\nFor each test case, print a single line containing two space-separated integers, first denoting the maximum bitwise AND possible and second denoting the minimum number of flips required to achieve it.",
    "constraints": [
      "1 ≤ T ≤ 10^4",
      "1 ≤ N ≤ 10^5",
      "0 ≤ Ai, Bi ≤ 2^30‑1",
      "Sum of N over all test cases ≤ 10^5"
    ],
    "inputFormat": "The first line contains an integer T, the number of test cases. For each test case: the first line contains an integer N, the number of cards. The second line contains N space‑separated integers A1 … AN – the numbers on the front faces. The third line contains N space‑separated integers B1 … BN – the numbers on the back faces.",
    "outputFormat": "For each test case output a single line with two integers separated by a space: the maximum possible bitwise AND of the visible numbers after any set of flips, and the minimum number of flips required to obtain that maximum.",
    "sampleTestCases": [
      {
        "id": "sample_1",
        "input": "3\n3\n4 6 8\n2 1 2\n3\n0 2 0\n2 0 8\n3\n1 2 1\n2 3 6",
        "output": "2 2\n0 0\n2 2",
        "explanation": "Explanation:\nTest case 111: The maximum AND is obtained after flipping the first and third cards achieving a configuration of [2,6,2][2, 6, 2][2,6,2] which yields a bitwise AND of 222.\nTest case 222: Every possible configuration of the cards yields a bitwise AND equal to 000. So to ensure the minimum number of operations, Chef performs no flip."
      }
    ]
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "label": "Case 1",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_2",
        "label": "Case 2",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_3",
        "label": "Case 3",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_4",
        "label": "Case 4",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_5",
        "label": "Case 5",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      }
    ],
    "totalTestCases": 5,
    "totalWeight": 10
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      2,
      2,
      2,
      2,
      2
    ],
    "maxScore": 10
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "For a candidate value X we can achieve it iff for every card i at least one of the two numbers (Ai or Bi) contains all bits of X, i.e. (Ai & X) == X or (Bi & X) == X. Starting with answer = 0 we try to set bits from the most significant (30) down to 0. For each bit we form a temporary candidate = answer | (1<<bit) and test the feasibility condition above in O(N). If feasible we keep the bit, otherwise we discard it. After the loop answer holds the maximum possible AND. To obtain the minimum number of flips we simply count the cards where the front face does not satisfy the final answer but the back face does – those cards must be flipped; if the front already satisfies the answer we keep it to avoid unnecessary flips. The algorithm runs in O(N·B) per test case where B = 31 (the number of bits), well within limits.",
    "timeComplexity": "O(N · 31) per test case, i.e. O(N) overall because the number of bits is constant.",
    "spaceComplexity": "O(1) extra besides the input arrays.",
    "code": {
      "C": "#include <stdio.h>\n#include <stdlib.h>\nint main(){\n    int T; if(scanf(\"%d\",&T)!=1) return 0;\n    while(T--){\n        int N; scanf(\"%d\",&N);\n        int *A = (int*)malloc(N*sizeof(int));\n        int *B = (int*)malloc(N*sizeof(int));\n        for(int i=0;i<N;i++) scanf(\"%d\",&A[i]);\n        for(int i=0;i<N;i++) scanf(\"%d\",&B[i]);\n        int ans=0;\n        for(int bit=30;bit>=0;--bit){\n            int cand = ans | (1<<bit);\n            int ok=1;\n            for(int i=0;i<N;i++){\n                if( ((A[i]&cand)!=cand) && ((B[i]&cand)!=cand) ) { ok=0; break; }\n            }\n            if(ok) ans=cand;\n        }\n        int flips=0;\n        for(int i=0;i<N;i++){\n            if( (A[i]&ans)!=ans && (B[i]&ans)==ans ) flips++;\n        }\n        printf(\"%d %d\\n\",ans,flips);\n        free(A); free(B);\n    }\n    return 0;\n}",
      "C++": "#include <bits/stdc++.h>\nusing namespace std;\nint main(){\n    ios::sync_with_stdio(false);\n    cin.tie(nullptr);\n    int T; if(!(cin>>T)) return 0;\n    while(T--){\n        int N; cin>>N;\n        vector<int> A(N), B(N);\n        for(int &x: A) cin>>x;\n        for(int &x: B) cin>>x;\n        int ans=0;\n        for(int bit=30; bit>=0; --bit){\n            int cand = ans | (1<<bit);\n            bool ok=true;\n            for(int i=0;i<N;i++){\n                if( ((A[i]&cand)!=cand) && ((B[i]&cand)!=cand) ) { ok=false; break; }\n            }\n            if(ok) ans=cand;\n        }\n        int flips=0;\n        for(int i=0;i<N;i++) if( (A[i]&ans)!=ans && (B[i]&ans)==ans ) ++flips;\n        cout<<ans<<' '<<flips<<\"\\n\";\n    }\n    return 0;\n}",
      "Java": "import java.io.*;\nimport java.util.*;\npublic class Main {\n    public static void main(String[] args) throws Exception {\n        FastScanner fs = new FastScanner(System.in);\n        int T = fs.nextInt();\n        StringBuilder sb = new StringBuilder();\n        while (T-- > 0) {\n            int N = fs.nextInt();\n            int[] A = new int[N];\n            int[] B = new int[N];\n            for (int i = 0; i < N; i++) A[i] = fs.nextInt();\n            for (int i = 0; i < N; i++) B[i] = fs.nextInt();\n            int ans = 0;\n            for (int bit = 30; bit >= 0; --bit) {\n                int cand = ans | (1 << bit);\n                boolean ok = true;\n                for (int i = 0; i < N; i++) {\n                    if ( (A[i] & cand) != cand && (B[i] & cand) != cand ) { ok = false; break; }\n                }\n                if (ok) ans = cand;\n            }\n            int flips = 0;\n            for (int i = 0; i < N; i++) {\n                if ( (A[i] & ans) != ans && (B[i] & ans) == ans ) flips++;\n            }\n            sb.append(ans).append(' ').append(flips).append('\\n');\n        }\n        System.out.print(sb.toString());\n    }\n    static class FastScanner {\n        private final BufferedInputStream in;\n        private final byte[] buffer = new byte[1 << 16];\n        private int ptr = 0, len = 0;\n        FastScanner(InputStream is){ in = new BufferedInputStream(is); }\n        private int readByte() throws IOException {\n            if (ptr >= len) { len = in.read(buffer); ptr = 0; if (len <= 0) return -1; }\n            return buffer[ptr++];\n        }\n        int nextInt() throws IOException {\n            int c, sign = 1, val = 0;\n            do { c = readByte(); } while (c <= ' ');\n            if (c == '-') { sign = -1; c = readByte(); }\n            while (c > ' ') { val = val * 10 + (c - '0'); c = readByte(); }\n            return val * sign;\n        }\n    }\n}",
      "Python3": "import sys\n\ndef solve():\n    data = list(map(int, sys.stdin.read().split()))\n    it = iter(data)\n    T = next(it)\n    out_lines = []\n    for _ in range(T):\n        N = next(it)\n        A = [next(it) for _ in range(N)]\n        B = [next(it) for _ in range(N)]\n        ans = 0\n        for bit in range(30, -1, -1):\n            cand = ans | (1 << bit)\n            ok = True\n            for a, b in zip(A, B):\n                if (a & cand) != cand and (b & cand) != cand:\n                    ok = False\n                    break\n            if ok:\n                ans = cand\n        flips = 0\n        for a, b in zip(A, B):\n            if (a & ans) != ans and (b & ans) == ans:\n                flips += 1\n        out_lines.append(f\"{ans} {flips}\")\n    sys.stdout.write(\"\\n\".join(out_lines))\n\nif __name__ == \"__main__\":\n    solve()",
      "JavaScript": "const fs = require('fs');\nconst input = fs.readFileSync(0, 'utf-8').trim().split(/\\s+/).map(Number);\nlet idx = 0;\nconst T = input[idx++];\nlet out = [];\nfor (let t = 0; t < T; ++t) {\n  const N = input[idx++];\n  const A = new Array(N);\n  const B = new Array(N);\n  for (let i = 0; i < N; ++i) A[i] = input[idx++];\n  for (let i = 0; i < N; ++i) B[i] = input[idx++];\n  let ans = 0;\n  for (let bit = 30; bit >= 0; --bit) {\n    const cand = ans | (1 << bit);\n    let ok = true;\n    for (let i = 0; i < N; ++i) {\n      if ((A[i] & cand) !== cand && (B[i] & cand) !== cand) { ok = false; break; }\n    }\n    if (ok) ans = cand;\n  }\n  let flips = 0;\n  for (let i = 0; i < N; ++i) {\n    if ((A[i] & ans) !== ans && (B[i] & ans) === ans) flips++;\n  }\n  out.push(ans + ' ' + flips);\n}\nconsole.log(out.join('\\n'));"
    }
  },
  "QBCategory": "CCD",
  "boilerPlates": {
    "C": "#include <stdio.h>\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "C++": "#include <iostream>\nusing namespace std;\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "Java": "import java.util.*;\nimport java.io.*;\n\npublic class Main {\n    public static void main(String[] args) throws IOException {\n        // your code goes here\n    }\n}",
    "Python3": "# your code goes here\n",
    "JavaScript": "// your code goes here\n"
  },
  "_internal": {
    "methodName": "",
    "params": []
  }
}