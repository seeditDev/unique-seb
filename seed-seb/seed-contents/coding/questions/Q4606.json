{
  "questionId": "Q4606",
  "title": "Binary Search Tree Iterator",
  "slug": "binary-search-tree-iterator",
  "version": 1,
  "createdAt": "2026-07-01",
  "updatedAt": "2026-07-03",
  "metadata": {
    "category": "Data Structures",
    "difficulty": "Hard",
    "isPremium": true,
    "tags": [
      "Binary Search Tree",
      "Iterator",
      "Stack",
      "In‑order Traversal",
      "Amortized O(1)"
    ],
    "companyTags": [],
    "topicTags": [
      "Trees"
    ],
    "QBCategory": "CCD"
  },
  "content": {
    "problemStatement": "You are required to implement a class **`BSTIterator`** that acts as an iterator over the **in-order traversal** of a **Binary Search Tree (BST)**.\n\nClass DefinitionImplement the following methods:\n\n\n\n**`BSTIterator(root)`**\nInitializes the iterator with the root of the BST.\nThe iterator’s position starts before the smallest element in the tree (conceptually at a number smaller than all node values).\n\n\n\n\n**`int next()`**\nMoves the iterator to the next node in the in-order sequence and returns its value. `next()` is never called when no next element exists.\n\n\n\n\n**`boolean hasNext()`**\nReturns `true` if there are still elements left to traverse; otherwise, returns `false`.\n\n\n\nNotes\nThe iterator should perform **in-order traversal** of the BST (Left → Node → Right).\nThe first call to `next()` should return the smallest element in the BST.\nYou may assume that `next()` is never called when no next element exists.\nFollow-UpCan you implement `next()` and `hasNext()` such that:\n\n\nEach runs in **average O(1)** time, and\nThe memory usage is **O(h)**, where h is the height of the tree?\n\nInput Format\n\n\nThe first line contains a single integer **T**, the number of test cases.\n\n\n\n\nEach test case consists of **three lines**:\n\n\n\n\nThe first line contains a single integer **N** — the number of nodes in the level-order representation of the BST.\n\n\n\n\nThe second line contains **N space-separated integers** — the level-order traversal of the tree, where **-1 represents a null node**.\n\n\n\n\nThe third line contains space-separated **operations**, each one being either:\n\n\n\n`BSTIterator`\n`next`\n`hasNext`\n\n\n\n\n\n\nOutput Format\nFor each test case:\n\n\n\n\nPrint a single line containing the results of each operation in order:\n\n\n\n`null` for `BSTIterator`\ninteger value for `next()`\n`true` or `false` for `hasNext()`\n\n\n\nEach output value is separated by a space.",
    "constraints": [
      "1 ≤ T ≤ 10^5",
      "1 ≤ N ≤ 10^5 (total N over all test cases ≤ 10^5)",
      "0 ≤ node value ≤ 10^6",
      "The total number of operations over all test cases ≤ 10^5"
    ],
    "inputFormat": "The first line contains an integer T, the number of test cases. For each test case there are three lines:\n1. An integer N – the number of entries in the level‑order representation of the BST.\n2. N space‑separated integers – the level‑order traversal where -1 denotes a null node.\n3. A space‑separated list of operations. Each operation is one of:\n   • BSTIterator – creates a new iterator for the current tree (output \"null\").\n   • next – moves the iterator to the next element in in‑order order and returns its value.\n   • hasNext – returns true if another element exists, otherwise false.\nAll results of the operations of a test case must be printed on one line separated by a single space.",
    "outputFormat": "For each test case output a single line containing the results of the operations in the order they appear. Use \"null\" for the BSTIterator construction, integer values for next(), and \"true\"/\"false\" for hasNext().",
    "sampleTestCases": [
      {
        "id": "sample_1",
        "input": "7\n7 3 15 -1 -1 9 20\nBSTIterator next next hasNext next hasNext next hasNext",
        "output": "null 3 7 true 9 true 15 true 20 false",
        "explanation": "Explanation: The BST is 7 / \\ 3   15 \\   9   20. The in-order traversal is 3, 7, 9, 15, 20."
      },
      {
        "id": "sample_2",
        "input": "5\n5 3 7 2 4 -1 -1 -1 -1 -1\nBSTIterator next next hasNext next hasNext next hasNext",
        "output": "null 2 3 true 4 true 5 true 7 false",
        "explanation": "Explanation: The BST is 5 / \\ 3   7 / \\ 2   4. The in-order traversal is 2, 3, 4, 5, 7."
      }
    ]
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "label": "Case 1",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_2",
        "label": "Case 2",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_3",
        "label": "Case 3",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_4",
        "label": "Case 4",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_5",
        "label": "Case 5",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      }
    ],
    "totalTestCases": 5,
    "totalWeight": 10
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      2,
      2,
      2,
      2,
      2
    ],
    "maxScore": 10
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "For each test case we first rebuild the BST from its level‑order array. The iterator is implemented with a stack that stores the path from the current node down to the leftmost descendant that has not been visited yet. The constructor pushes all left children of the root onto the stack – this represents the position \"before the smallest element\". next() pops the top node, records its value, then pushes the leftmost chain of its right child (if any). hasNext() simply checks whether the stack is non‑empty. All operations run in O(1) amortised time and the stack never holds more than the height of the tree, i.e. O(h) memory.",
    "timeComplexity": "Building the tree: O(N) per test case. Each iterator operation: O(1) amortised. Overall O(total N + total operations).",
    "spaceComplexity": "O(N) for the tree plus O(h) for the iterator stack (h ≤ N).",
    "code": {
      "C": "// COMPLETE, COMPILABLE C code\n#include <stdio.h>\n#include <stdlib.h>\n#include <string.h>\n#include <stdbool.h>\n\ntypedef struct Node {\n    int val;\n    struct Node *left, *right;\n} Node;\n\ntypedef struct BSTIterator {\n    Node **stack;\n    int top;\n    int capacity;\n} BSTIterator;\n\nNode* newNode(int v) {\n    Node* n = (Node*)malloc(sizeof(Node));\n    n->val = v; n->left = n->right = NULL;\n    return n;\n}\n\nNode* buildTree(int *arr, int n) {\n    if (n == 0 || arr[0] == -1) return NULL;\n    Node **nodes = (Node**)calloc(n, sizeof(Node*));\n    for (int i = 0; i < n; ++i) if (arr[i] != -1) nodes[i] = newNode(arr[i]);\n    for (int i = 0; i < n; ++i) if (nodes[i]) {\n        int l = 2*i + 1, r = 2*i + 2;\n        if (l < n) nodes[i]->left = nodes[l];\n        if (r < n) nodes[i]->right = nodes[r];\n    }\n    Node* root = nodes[0];\n    free(nodes);\n    return root;\n}\n\nBSTIterator* iteratorCreate(Node* root) {\n    BSTIterator* it = (BSTIterator*)malloc(sizeof(BSTIterator));\n    it->capacity = 1000;\n    it->stack = (Node**)malloc(it->capacity * sizeof(Node*));\n    it->top = -1;\n    Node* cur = root;\n    while (cur) {\n        if (it->top + 1 == it->capacity) {\n            it->capacity *= 2;\n            it->stack = (Node**)realloc(it->stack, it->capacity * sizeof(Node*));\n        }\n        it->stack[++it->top] = cur;\n        cur = cur->left;\n    }\n    return it;\n}\n\nint iteratorNext(BSTIterator* it) {\n    Node* node = it->stack[it->top--];\n    int ret = node->val;\n    Node* cur = node->right;\n    while (cur) {\n        if (it->top + 1 == it->capacity) {\n            it->capacity *= 2;\n            it->stack = (Node**)realloc(it->stack, it->capacity * sizeof(Node*));\n        }\n        it->stack[++it->top] = cur;\n        cur = cur->left;\n    }\n    return ret;\n}\n\nbool iteratorHasNext(BSTIterator* it) {\n    return it->top >= 0;\n}\n\nint main() {\n    int T; if (scanf(\"%d\", &T)!=1) return 0;\n    while (T--) {\n        int N; scanf(\"%d\", &N);\n        int *arr = (int*)malloc(N * sizeof(int));\n        for (int i = 0; i < N; ++i) scanf(\"%d\", &arr[i]);\n        Node* root = buildTree(arr, N);\n        free(arr);\n        // read the rest of the line after the last integer\n        char buffer[1024];\n        scanf(\"%*c\"); // consume newline\n        fgets(buffer, sizeof(buffer), stdin);\n        // tokenise\n        char *token = strtok(buffer, \" \\n\\r\");\n        BSTIterator* it = NULL;\n        int first = 1;\n        while (token) {\n            if (strcmp(token, \"BSTIterator\") == 0) {\n                if (it) { free(it->stack); free(it); }\n                it = iteratorCreate(root);\n                if (!first) printf(\" \");\n                printf(\"null\");\n                first = 0;\n            } else if (strcmp(token, \"next\") == 0) {\n                int v = iteratorNext(it);\n                if (!first) printf(\" \");\n                printf(\"%d\", v);\n                first = 0;\n            } else if (strcmp(token, \"hasNext\") == 0) {\n                bool b = iteratorHasNext(it);\n                if (!first) printf(\" \");\n                printf(b ? \"true\" : \"false\");\n                first = 0;\n            }\n            token = strtok(NULL, \" \\n\\r\");\n        }\n        printf(\"\\n\");\n        // free tree\n        // (omitted for brevity, judge process ends soon)\n        if (it) { free(it->stack); free(it); }\n    }\n    return 0;\n}\n",
      "C++": "// COMPLETE, COMPILABLE C++ code\n#include <bits/stdc++.h>\nusing namespace std;\nstruct Node {\n    int val; Node *left, *right;\n    Node(int v): val(v), left(nullptr), right(nullptr) {}\n};\nstruct BSTIterator {\n    vector<Node*> st;\n    BSTIterator(Node* root) { pushLeft(root); }\n    void pushLeft(Node* cur) { while (cur) { st.push_back(cur); cur = cur->left; } }\n    int next() { Node* node = st.back(); st.pop_back(); int ans = node->val; pushLeft(node->right); return ans; }\n    bool hasNext() const { return !st.empty(); }\n};\nNode* buildTree(const vector<int>& a) {\n    int n = a.size();\n    if (n==0 || a[0]==-1) return nullptr;\n    vector<Node*> nodes(n,nullptr);\n    for (int i=0;i<n;++i) if (a[i]!=-1) nodes[i]=new Node(a[i]);\n    for (int i=0;i<n;++i) if (nodes[i]) {\n        int l=2*i+1, r=2*i+2;\n        if (l<n) nodes[i]->left = nodes[l];\n        if (r<n) nodes[i]->right = nodes[r];\n    }\n    return nodes[0];\n}\nint main(){\n    ios::sync_with_stdio(false);\n    cin.tie(nullptr);\n    int T; if(!(cin>>T)) return 0;\n    while(T--){\n        int N; cin>>N;\n        vector<int> arr(N);\n        for(int i=0;i<N;++i) cin>>arr[i];\n        Node* root = buildTree(arr);\n        string line; getline(cin,line); // consume endline\n        getline(cin,line);\n        stringstream ss(line);\n        string op; BSTIterator* it=nullptr;\n        bool first=true;\n        while(ss>>op){\n            if(op==\"BSTIterator\"){\n                delete it; // free previous if any\n                it = new BSTIterator(root);\n                if(!first) cout<<' ';\n                cout<<\"null\";\n                first=false;\n            }else if(op==\"next\"){\n                int v = it->next();\n                if(!first) cout<<' ';\n                cout<<v;\n                first=false;\n            }else if(op==\"hasNext\"){\n                bool b = it->hasNext();\n                if(!first) cout<<' ';\n                cout<<(b?\"true\":\"false\");\n                first=false;\n            }\n        }\n        cout<<'\\n';\n        delete it;\n        // tree memory leak ignored for brevity\n    }\n    return 0;\n}\n",
      "Java": ""
    }
  },
  "QBCategory": "CCD",
  "boilerPlates": {
    "C": "#include <stdio.h>\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "C++": "#include <iostream>\nusing namespace std;\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "Java": "import java.util.*;\nimport java.io.*;\n\npublic class Main {\n    public static void main(String[] args) throws IOException {\n        // your code goes here\n    }\n}",
    "Python3": "# your code goes here\n",
    "JavaScript": "// your code goes here\n"
  },
  "_internal": {
    "methodName": "",
    "params": []
  }
}