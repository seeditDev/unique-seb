{
  "questionId": "Q7081",
  "title": "Minimize Digitsum",
  "slug": "minimize-digitsum",
  "version": 1,
  "createdAt": "2026-07-02",
  "updatedAt": "2026-07-04",
  "metadata": {
    "category": "Math",
    "difficulty": "Medium",
    "isPremium": true,
    "tags": [
      "Digitsum",
      "BFS"
    ],
    "companyTags": [],
    "topicTags": [
      "Graphs"
    ],
    "QBCategory": "CCD"
  },
  "content": {
    "problemStatement": "You are given positive integers NNN and DDD. You may perform operations of the following two types:\n\n\nadd DDD to NNN, i.e. change NNN to N+DN+DN+D\nchange NNN to digitsum(N)\\mathop{\\mathrm{digitsum}}(N)digitsum(N)\nHere, digitsum(x)\\mathop{\\mathrm{digitsum}}(x)digitsum(x) is the sum of decimal digits of xxx. For example, digitsum(123)=1+2+3=6\\mathop{\\mathrm{digitsum}}(123)=1+2+3=6digitsum(123)=1+2+3=6, digitsum(100)=1+0+0=1\\mathop{\\mathrm{digitsum}}(100)=1+0+0=1digitsum(100)=1+0+0=1, digitsum(365)=3+6+5=14\\mathop{\\mathrm{digitsum}}(365)=3+6+5=14digitsum(365)=3+6+5=14.\n\nYou may perform any number of operations (including zero) in any order. Please find the minimum obtainable value of NNN and the minimum number of operations required to obtain this value.",
    "constraints": [
      "1 <= T <= 10",
      "1 <= N, D <= 10^10"
    ],
    "inputFormat": "The first line of the input contains a single integer T denoting the number of test cases. The description of T test cases follows. The first and only line of each test case contains two space-separated integers N and D.",
    "outputFormat": "For each test case, print a single line containing two space-separated integers — the minimum value of N and the minimum required number of operations.",
    "sampleTestCases": [
      {
        "id": "sample_1",
        "input": "3\n2 1\n9 3\n11 13",
        "output": "1 9\n3 2\n1 4",
        "explanation": "Explanation: Example case 1: The value N=1 can be achieved by 8 successive \"add\" operations (changing N to 10) and one \"digit-sum\" operation. Example case 2: You can prove that you cannot obtain N=1 and N=2, and you can obtain N=3. The value N=3 can be achieved by one \"add\" and one \"digitsum\" operation, changing 9 to 12 and 12 to 3. Example case 3: N=1 can be achieved by operations \"add\", \"add\", \"digitsum\", \"digitsum\": 11→24→37→10→1."
      }
    ]
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "label": "minimum boundary",
        "input": "1\n1 1",
        "expectedOutput": "1 0",
        "weight": 2
      },
      {
        "id": "tc_2",
        "label": "maximum boundary",
        "input": "1\n1000000000 1000000000",
        "expectedOutput": "1 999999999",
        "weight": 2
      },
      {
        "id": "tc_3",
        "label": "edge case",
        "input": "1\n10 1",
        "expectedOutput": "1 9",
        "weight": 2
      },
      {
        "id": "tc_4",
        "label": "general case 1",
        "input": "1\n123 4",
        "expectedOutput": "3 3",
        "weight": 2
      },
      {
        "id": "tc_5",
        "label": "general case 2",
        "input": "1\n987 6",
        "expectedOutput": "3 4",
        "weight": 2
      }
    ],
    "totalTestCases": 5,
    "totalWeight": 10
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      2,
      2,
      2,
      2,
      2
    ],
    "maxScore": 10
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "To solve this problem, we can use a breadth-first search (BFS) algorithm. We start with the initial value of N and explore all possible operations (adding D to N and calculating the digitsum of N) in a level-order manner. We keep track of the minimum value of N and the minimum number of operations required to achieve it.",
    "timeComplexity": "O(T * (log(N) + log(D))) — the time complexity is dominated by the BFS algorithm, which explores all possible operations in a level-order manner. The log(N) and log(D) terms come from the fact that we need to calculate the digitsum of N and D, which takes logarithmic time.",
    "spaceComplexity": "O(T * (log(N) + log(D))) — the space complexity is also dominated by the BFS algorithm, which stores all possible operations in a queue. The log(N) and log(D) terms come from the fact that we need to store the digitsum of N and D, which takes logarithmic space.",
    "code": {
      "C": "#include <stdio.h>\n#include <stdlib.h>\n#include <string.h>\n#define MAX 1000000000\n\nint min(int a, int b) { return a < b ? a : b; }\n\nint digitsum(int n) {\n    int sum = 0;\n    while (n > 0) {\n        sum += n % 10;\n        n /= 10;\n    }\n    return sum;\n}\n\nint main() {\n    int T;\n    scanf(\"%d\", &T);\n    while (T--) {\n        int N, D;\n        scanf(\"%d %d\", &N, &D);\n        int min_val = N;\n        int min_ops = 0;\n        int queue[MAX];\n        int front = 0, rear = 0;\n        queue[rear++] = N;\n        int visited[MAX] = {0};\n        visited[N] = 1;\n        while (front < rear) {\n            int curr = queue[front++];\n            if (curr < min_val) {\n                min_val = curr;\n                min_ops = visited[curr];\n            }\n            int next1 = curr + D;\n            if (next1 <= MAX && !visited[next1]) {\n                queue[rear++] = next1;\n                visited[next1] = visited[curr] + 1;\n            }\n            int next2 = digitsum(curr);\n            if (next2 <= MAX && !visited[next2]) {\n                queue[rear++] = next2;\n                visited[next2] = visited[curr] + 1;\n            }\n        }\n        printf(\"%d %d\\n\", min_val, min_ops);\n    }\n    return 0;\n}",
      "C++": "#include <iostream>\n#include <queue>\n#include <unordered_set>\nusing namespace std;\n\nint digitsum(int n) {\n    int sum = 0;\n    while (n > 0) {\n        sum += n % 10;\n        n /= 10;\n    }\n    return sum;\n}\n\nint main() {\n    int T;\n    cin >> T;\n    while (T--) {\n        int N, D;\n        cin >> N >> D;\n        int min_val = N;\n        int min_ops = 0;\n        queue<int> q;\n        unordered_set<int> visited;\n        q.push(N);\n        visited.insert(N);\n        while (!q.empty()) {\n            int curr = q.front();\n            q.pop();\n            if (curr < min_val) {\n                min_val = curr;\n                min_ops = visited.size();\n            }\n            int next1 = curr + D;\n            if (next1 <= 1000000000 && visited.find(next1) == visited.end()) {\n                q.push(next1);\n                visited.insert(next1);\n            }\n            int next2 = digitsum(curr);\n            if (next2 <= 1000000000 && visited.find(next2) == visited.end()) {\n                q.push(next2);\n                visited.insert(next2);\n            }\n        }\n        cout << min_val << \" \" << min_ops << endl;\n    }\n    return 0;\n}",
      "Java": "import java.util.Queue;\nimport java.util.LinkedList;\nimport java.util.HashSet;\nimport java.util.Scanner;\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int T = sc.nextInt();\n        while (T-- > 0) {\n            int N = sc.nextInt();\n            int D = sc.nextInt();\n            int min_val = N;\n            int min_ops = 0;\n            Queue<Integer> q = new LinkedList<>();\n            HashSet<Integer> visited = new HashSet<>();\n            q.add(N);\n            visited.add(N);\n            while (!q.isEmpty()) {\n                int curr = q.poll();\n                if (curr < min_val) {\n                    min_val = curr;\n                    min_ops = visited.size();\n                }\n                int next1 = curr + D;\n                if (next1 <= 1000000000 && !visited.contains(next1)) {\n                    q.add(next1);\n                    visited.add(next1);\n                }\n                int next2 = digitsum(curr);\n                if (next2 <= 1000000000 && !visited.contains(next2)) {\n                    q.add(next2);\n                    visited.add(next2);\n                }\n            }\n            System.out.println(min_val + \" \" + min_ops);\n        }\n    }\n    public static int digitsum(int n) {\n        int sum = 0;\n        while (n > 0) {\n            sum += n % 10;\n            n /= 10;\n        }\n        return sum;\n    }\n}",
      "Python3": "from collections import deque\n\ndef digitsum(n):\n    return sum(int(digit) for digit in str(n))\n\nT = int(input())\nfor _ in range(T):\n    N, D = map(int, input().split())\n    min_val = N\n    min_ops = 0\n    q = deque([N])\n    visited = {N}\n    while q:\n        curr = q.popleft()\n        if curr < min_val:\n            min_val = curr\n            min_ops = len(visited)\n        next1 = curr + D\n        if next1 <= 10**10 and next1 not in visited:\n            q.append(next1)\n            visited.add(next1)\n        next2 = digitsum(curr)\n        if next2 <= 10**10 and next2 not in visited:\n            q.append(next2)\n            visited.add(next2)\n    print(min_val, min_ops)",
      "JavaScript": "const fs = require('fs');\nconst lines = fs.readFileSync(0, 'utf-8').trim().split('\\n');\nlet idx = 0;\n\nfunction digitsum(n) {\n    return n.toString().split('').reduce((a, b) => a + parseInt(b), 0);\n}\n\nlet T = parseInt(lines[idx++]);\nwhile (T-- > 0) {\n    let [N, D] = lines[idx++].trim().split(' ').map(x => parseInt(x));\n    let min_val = N;\n    let min_ops = 0;\n    let q = [N];\n    let visited = new Set([N]);\n    while (q.length > 0) {\n        let curr = q.shift();\n        if (curr < min_val) {\n            min_val = curr;\n            min_ops = visited.size;\n        }\n        let next1 = curr + D;\n        if (next1 <= 10**10 && !visited.has(next1)) {\n            q.push(next1);\n            visited.add(next1);\n        }\n        let next2 = digitsum(curr);\n        if (next2 <= 10**10 && !visited.has(next2)) {\n            q.push(next2);\n            visited.add(next2);\n        }\n    }\n    console.log(min_val + ' ' + min_ops);\n}"
    }
  },
  "QBCategory": "CCD",
  "boilerPlates": {
    "C": "#include <stdio.h>\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "C++": "#include <iostream>\nusing namespace std;\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "Java": "import java.util.*;\nimport java.io.*;\n\npublic class Main {\n    public static void main(String[] args) throws IOException {\n        // your code goes here\n    }\n}",
    "Python3": "# your code goes here\n",
    "JavaScript": "// your code goes here\n"
  },
  "_internal": {
    "methodName": "",
    "params": []
  }
}