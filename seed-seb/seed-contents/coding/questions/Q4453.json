{
  "questionId": "Q4453",
  "title": "Mirror Tree",
  "slug": "mirror-tree",
  "version": 1,
  "createdAt": "2026-07-01",
  "updatedAt": "2026-07-04",
  "metadata": {
    "category": "Graph",
    "difficulty": "Medium",
    "isPremium": true,
    "tags": [
      "Tree",
      "Symmetry",
      "Recursion"
    ],
    "companyTags": [],
    "topicTags": [
      "Trees"
    ],
    "QBCategory": "CCD"
  },
  "content": {
    "problemStatement": "Given a binary tree, check whether it is a structural mirror of itself (i.e., structurally symmetric around its center).\n\nFor example, the following binary tree is structurally symmetric:\n\n\n\n\nInput Format\n\nThe first line of the input contains a single integer NNN — the number of nodes in the binary tree.\nNext N−1N - 1N−1 lines contain three space separated characters pi,ci,Rip_i, c_i, R_ipi​,ci​,Ri​, describing the tree with edge informations - pip_ipi​ is an integer denoting the parent node of the iiith edge, cic_ici​ is child node, and the letter RiR_iRi​ denotes whether the child is left child or right child, it's value is `L` if cic_ici​ is left child of pip_ipi​, else `R`.\n\n\nOutput Format\nOutput on the single line, `YES` if the given binary tree is structurally symmetric to itself, `NO` otherwise.",
    "constraints": [
      "1 ≤ T ≤ 10",
      "1 ≤ N ≤ 10000",
      "1 ≤ pi, ci ≤ N",
      "Ri ∈ {'L','R'}",
      "All pi and ci values are distinct, forming a proper binary tree"
    ],
    "inputFormat": "The first line contains an integer T, the number of test cases. For each test case: The first line contains an integer N, the number of nodes in the binary tree (1 ≤ N ≤ 10000). The next N-1 lines each contain three space-separated values: pi ci Ri, where pi is the parent node index, ci is the child node index, and Ri is either 'L' (ci is the left child of pi) or 'R' (ci is the right child of pi). All node indices are between 1 and N and are distinct. The tree is guaranteed to be a valid binary tree.",
    "outputFormat": "For each test case, output 'YES' if the given binary tree is structurally symmetric, otherwise output 'NO'.",
    "sampleTestCases": [
      {
        "id": "sample_1",
        "input": "1\\n7\\n1 2 L\\n1 3 R\\n3 4 L\\n3 5 R\\n2 6 R\\n2 7 L",
        "output": "YES",
        "explanation": "The tree is symmetric: left subtree of node 1 (nodes 2,6,7) mirrors the right subtree (nodes 3,4,5)."
      },
      {
        "id": "sample_2",
        "input": "1\\n5\\n1 2 L\\n1 3 R\\n2 4 L\\n3 5 R",
        "output": "NO",
        "explanation": "Node 2 has a left child while node 3 has only a right child, breaking mirror symmetry."
      },
      {
        "id": "sample_3",
        "input": "1\\n3\\n1 2 L\\n1 3 R",
        "output": "YES",
        "explanation": "Both left and right subtrees consist of a single leaf, so the tree is symmetric."
      }
    ]
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "label": "minimum boundary",
        "input": "1\\n1",
        "expectedOutput": "YES",
        "weight": 2
      },
      {
        "id": "tc_2",
        "label": "maximum boundary",
        "input": "1\\n10000\\n1 2 L\\n1 3 R\\n2 4 L\\n2 5 R\\n3 6 L\\n3 7 R",
        "expectedOutput": "YES",
        "weight": 2
      },
      {
        "id": "tc_3",
        "label": "uniform/edge case",
        "input": "1\\n5\\n1 2 L\\n1 3 R\\n2 4 L\\n2 5 R",
        "expectedOutput": "YES",
        "weight": 2
      },
      {
        "id": "tc_4",
        "label": "tricky case",
        "input": "1\\n5\\n1 2 L\\n1 3 R\\n2 4 L",
        "expectedOutput": "NO",
        "weight": 2
      },
      {
        "id": "tc_5",
        "label": "stress/general",
        "input": "2\\n5\\n1 2 L\\n1 3 R\\n2 4 L\\n2 5 R\\n5\\n1 2 L\\n1 3 R\\n2 4 L",
        "expectedOutput": "YES\\nNO",
        "weight": 2
      }
    ],
    "totalTestCases": 5,
    "totalWeight": 10
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      2,
      2,
      2,
      2,
      2
    ],
    "maxScore": 10
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "To determine if a binary tree is structurally symmetric, we can compare the left subtree with the mirrored right subtree. This can be achieved by using a recursive function that checks if two trees are mirror images of each other. The function should return true if both trees are null, or if they are not null and the left child of the first tree is a mirror of the right child of the second tree, and the right child of the first tree is a mirror of the left child of the second tree.",
    "timeComplexity": "O(N) per test case, O(T*N) total, where N is the number of nodes in the tree and T is the number of test cases.",
    "spaceComplexity": "O(N) for the recursive call stack in the worst case (when the tree is skewed).",
    "code": {
      "C": "#include <stdio.h>\n#include <stdlib.h>\n\ntypedef struct Node {\n    int val;\n    struct Node* left;\n    struct Node* right;\n} Node;\n\nNode* getNode(Node** pool, int id) {\n    if (pool[id] == NULL) {\n        pool[id] = (Node*)malloc(sizeof(Node));\n        pool[id]->val = id;\n        pool[id]->left = pool[id]->right = NULL;\n    }\n    return pool[id];\n}\n\nint isMirror(Node* a, Node* b) {\n    if (!a && !b) return 1;\n    if (!a || !b) return 0;\n    return isMirror(a->left, b->right) && isMirror(a->right, b->left);\n}\n\nint main() {\n    int T; if (scanf(\"%d\", &T)!=1) return 0;\n    while (T--) {\n        int N; scanf(\"%d\", &N);\n        Node* nodes[10001] = {0}; // N ≤ 10000\n        int isChild[10001] = {0};\n        for (int i = 0; i < N-1; ++i) {\n            int p, c; char dir; scanf(\"%d %d %c\", &p, &c, &dir);\n            Node* parent = getNode(nodes, p);\n            Node* child  = getNode(nodes, c);\n            if (dir == 'L') parent->left = child; else parent->right = child;\n            isChild[c] = 1;\n        }\n        int rootId = 1;\n        for (int i = 1; i <= N; ++i) if (!isChild[i]) { rootId = i; break; }\n        Node* root = nodes[rootId];\n        if (isMirror(root ? root->left : NULL, root ? root->right : NULL))\n            printf(\"YES\\n\");\n        else\n            printf(\"NO\\n\");\n        // free memory (optional for contest)\n        for (int i = 1; i <= N; ++i) if (nodes[i]) free(nodes[i]);\n    }\n    return 0;\n}",
      "C++": "#include <bits/stdc++.h>\nusing namespace std;\n\nstruct Node {\n    int val;\n    Node* left = nullptr;\n    Node* right = nullptr;\n    Node(int v): val(v) {}\n};\n\nNode* getNode(vector<Node*>& pool, int id) {\n    if (!pool[id]) pool[id] = new Node(id);\n    return pool[id];\n}\n\nbool isMirror(Node* a, Node* b) {\n    if (!a && !b) return true;\n    if (!a || !b) return false;\n    return isMirror(a->left, b->right) && isMirror(a->right, b->left);\n}\n\nint main() {\n    ios::sync_with_stdio(false);\n    cin.tie(nullptr);\n    int T; if(!(cin>>T)) return 0;\n    while(T--) {\n        int N; cin>>N;\n        vector<Node*> nodes(N+1, nullptr);\n        vector<int> isChild(N+1, 0);\n        for(int i=0;i<N-1;++i){\n            int p,c; char dir; cin>>p>>c>>dir;\n            Node* parent = getNode(nodes,p);\n            Node* child  = getNode(nodes,c);\n            if(dir=='L') parent->left = child; else parent->right = child;\n            isChild[c]=1;\n        }\n        int rootId=1; for(int i=1;i<=N;++i) if(!isChild[i]){rootId=i;break;}\n        Node* root = nodes[rootId];\n        bool ok = isMirror(root?root->left:nullptr, root?root->right:nullptr);\n        cout << (ok?\"YES\":\"NO\") << '\\n';\n        for(Node* nd: nodes) delete nd; // clean up\n    }\n    return 0;\n}",
      "Java": "import java.io.*;\nimport java.util.*;\n\npublic class Main {\n    static class Node {\n        int val;\n        Node left, right;\n        Node(int v) { this.val = v; }\n    }\n\n    static Node getNode(Node[] pool, int id) {\n        if (pool[id] == null) pool[id] = new Node(id);\n        return pool[id];\n    }\n\n    static boolean isMirror(Node a, Node b) {\n        if (a == null && b == null) return true;\n        if (a == null || b == null) return false;\n        return isMirror(a.left, b.right) && isMirror(a.right, b.left);\n    }\n\n    public static void main(String[] args) throws Exception {\n        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));\n        StringTokenizer st = new StringTokenizer(br.readLine());\n        int T = Integer.parseInt(st.nextToken());\n        StringBuilder out = new StringBuilder();\n        while (T-- > 0) {\n            int N = Integer.parseInt(br.readLine().trim());\n            Node[] nodes = new Node[N + 1];\n            boolean[] isChild = new boolean[N + 1];\n            for (int i = 0; i < N - 1; i++) {\n                st = new StringTokenizer(br.readLine());\n                int p = Integer.parseInt(st.nextToken());\n                int c = Integer.parseInt(st.nextToken());\n                char dir = st.nextToken().charAt(0);\n                Node parent = getNode(nodes, p);\n                Node child = getNode(nodes, c);\n                if (dir == 'L') parent.left = child; else parent.right = child;\n                isChild[c] = true;\n            }\n            int rootId = 1;\n            for (int i = 1; i <= N; i++) if (!isChild[i]) { rootId = i; break; }\n            Node root = nodes[rootId];\n            boolean ok = isMirror(root == null ? null : root.left, root == null ? null : root.right);\n            out.append(ok ? \"YES\" : \"NO\").append('\\n');\n        }\n        System.out.print(out.toString());\n    }\n}",
      "Python3": "import sys\n\ndef is_mirror(a, b):\n    if a is None and b is None:\n        return True\n    if a is None or b is None:\n        return False\n    return is_mirror(a[1], b[2]) and is_mirror(a[2], b[1])\n\n# node representation: [val, left, right] where left/right are either None or similar list\n\ndef solve():\n    data = sys.stdin.read().strip().splitlines()\n    if not data:\n        return\n    idx = 0\n    T = int(data[idx].strip()); idx += 1\n    out_lines = []\n    for _ in range(T):\n        N = int(data[idx].strip()); idx += 1\n        nodes = [None] * (N + 1)\n        is_child = [False] * (N + 1)\n        for __ in range(N - 1):\n            p_str, c_str, dir = data[idx].split(); idx += 1\n            p = int(p_str); c = int(c_str)\n            if nodes[p] is None:\n                nodes[p] = [p, None, None]\n            if nodes[c] is None:\n                nodes[c] = [c, None, None]\n            if dir == 'L':\n                nodes[p][1] = nodes[c]\n            else:\n                nodes[p][2] = nodes[c]\n            is_child[c] = True\n        root_id = 1\n        for i in range(1, N + 1):\n            if not is_child[i]:\n                root_id = i\n                break\n        root = nodes[root_id]\n        if root is None:\n            out_lines.append('YES')\n        else:\n            ok = is_mirror(root[1], root[2])\n            out_lines.append('YES' if ok else 'NO')\n    sys.stdout.write('\\n'.join(out_lines))\n\nif __name__ == \"__main__\":\n    solve()",
      "JavaScript": "const fs = require('fs');\nconst lines = fs.readFileSync(0, 'utf-8').trim().split('\\n');\nlet idx = 0;\nconst T = parseInt(lines[idx++].trim(), 10);\nlet outputs = [];\nfunction isMirror(a, b) {\n    if (!a && !b) return true;\n    if (!a || !b) return false;\n    return isMirror(a.left, b.right) && isMirror(a.right, b.left);\n}\nfor (let t = 0; t < T; ++t) {\n    const N = parseInt(lines[idx++].trim(), 10);\n    const nodes = new Array(N + 1).fill(null);\n    const isChild = new Array(N + 1).fill(false);\n    function getNode(id) {\n        if (!nodes[id]) nodes[id] = { val: id, left: null, right: null };\n        return nodes[id];\n    }\n    for (let i = 0; i < N - 1; ++i) {\n        const [pStr, cStr, dir] = lines[idx++].trim().split(/\\s+/);\n        const p = parseInt(pStr, 10);\n        const c = parseInt(cStr, 10);\n        const parent = getNode(p);\n        const child = getNode(c);\n        if (dir === 'L') parent.left = child; else parent.right = child;\n        isChild[c] = true;\n    }\n    let rootId = 1;\n    for (let i = 1; i <= N; ++i) if (!isChild[i]) { rootId = i; break; }\n    const root = nodes[rootId];\n    const ok = isMirror(root ? root.left : null, root ? root.right : null);\n    outputs.push(ok ? 'YES' : 'NO');\n}\nconsole.log(outputs.join('\\n'));\n"
    }
  },
  "QBCategory": "CCD",
  "boilerPlates": {
    "C": "#include <stdio.h>\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "C++": "#include <iostream>\nusing namespace std;\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "Java": "import java.util.*;\nimport java.io.*;\n\npublic class Main {\n    public static void main(String[] args) throws IOException {\n        // your code goes here\n    }\n}",
    "Python3": "# your code goes here\n",
    "JavaScript": "// your code goes here\n"
  },
  "_internal": {
    "methodName": "",
    "params": []
  }
}