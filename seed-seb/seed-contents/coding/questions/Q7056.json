{
  "questionId": "Q7056",
  "title": "Standard Tree Task",
  "slug": "standard-tree-task",
  "version": 1,
  "createdAt": "2026-07-02",
  "updatedAt": "2026-07-04",
  "metadata": {
    "category": "Graph | Dynamic Programming",
    "difficulty": "Hard",
    "isPremium": true,
    "tags": [
      "Tree",
      "Coin Assignment",
      "GCD",
      "Modular Arithmetic"
    ],
    "companyTags": [],
    "topicTags": [
      "Trees"
    ],
    "QBCategory": "CCD"
  },
  "content": {
    "problemStatement": "There's no story in this problem, it's time for trees!\n\nThe company structure of ShareChat can be viewed as a tree with NNN nodes (numbered 111 through NNN); node 111 is the root of this tree. You should choose a (possibly empty) subset of nodes and place one coin in each node from this set. We call the tree proper if the following condition holds:\n\n\nConsider two arbitrary disjont subtrees. Let's denote the total number of coins in one subtree by XXX and the total number of coins in the other subtree by YYY.\nIf X&gt;0X &gt; 0X&gt;0 and Y&gt;0Y &gt; 0Y&gt;0, the greatest common divisior (GCD) of XXX and YYY must be 111.\nPlease calculate the number of different assignments of coins to nodes (different sets of nodes containing coins) such that the resulting tree is proper. Since the answer could be very big, compute it modulo 109+710^9+7109+7.\n\n**Note:** A subtree is the tree formed by some node vvv and all (direct or indirect) descendants of vvv. Two subtrees are disjont if they do not have any common node.",
    "constraints": [
      "1 <= N <= 70",
      "1 <= pi < i for each valid i"
    ],
    "inputFormat": "The first line contains a single integer N denoting the number of nodes in the tree. The second line contains N-1 space-separated integers p2, p3, …, pN, where pi denotes the parent of node i.",
    "outputFormat": "Print a single line containing one integer — the number of assignments of coins to nodes which form a proper tree, modulo 10^9 + 7.",
    "sampleTestCases": [
      {
        "id": "sample_1",
        "input": "5\n1 1 2 3",
        "output": "30",
        "explanation": "There are only two ways to assign coins to nodes such that the resulting tree is not proper. Both contain coins in nodes 2, 3, 4, 5 and one of them contains a coin in node 1 too."
      }
    ]
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "label": "minimum boundary",
        "input": "1",
        "expectedOutput": "2",
        "weight": 2
      },
      {
        "id": "tc_2",
        "label": "Case 2",
        "input": "1",
        "expectedOutput": "2",
        "weight": 2
      },
      {
        "id": "tc_3",
        "label": "Case 3",
        "input": "1",
        "expectedOutput": "2",
        "weight": 2
      },
      {
        "id": "tc_4",
        "label": "Case 4",
        "input": "1",
        "expectedOutput": "2",
        "weight": 2
      },
      {
        "id": "tc_5",
        "label": "Case 5",
        "input": "1",
        "expectedOutput": "2",
        "weight": 2
      }
    ],
    "totalTestCases": 5,
    "totalWeight": 10
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      2,
      2,
      2,
      2,
      2
    ],
    "maxScore": 10
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "To solve this problem, we can use a dynamic programming approach. We will calculate the number of ways to assign coins to each subtree such that the resulting tree is proper. We will use a recursive function to calculate this for each node in the tree. The base case will be when the node is a leaf node, in which case there are two possibilities: either the node has a coin or it does not. For each internal node, we will calculate the number of ways to assign coins to its subtrees and then combine these results to get the total number of ways to assign coins to the subtree rooted at the current node.",
    "timeComplexity": "O(N) — where N is the number of nodes in the tree. This is because we are visiting each node once and performing a constant amount of work for each node.",
    "spaceComplexity": "O(N) — where N is the number of nodes in the tree. This is because we are storing the results of the recursive function calls in a table to avoid redundant calculations.",
    "code": {
      "C": "#include <stdio.h>\n#include <stdlib.h>\n#define MOD 1000000007\n\nint* calculateAssignments(int** tree, int* subtreeSizes, int node, int N) {\n    int* assignments = (int*) malloc(2 * sizeof(int));\n    if (subtreeSizes[node] == 1) {\n        assignments[0] = 1;\n        assignments[1] = 1;\n        return assignments;\n    }\n    int totalAssignments = 1;\n    for (int i = 0; i < tree[node][0]; i++) {\n        int child = tree[node][i + 1];\n        int* childAssignments = calculateAssignments(tree, subtreeSizes, child, N);\n        totalAssignments = (totalAssignments * (childAssignments[0] + childAssignments[1])) % MOD;\n    }\n    assignments[0] = totalAssignments;\n    totalAssignments = 1;\n    for (int i = 0; i < tree[node][0]; i++) {\n        int child = tree[node][i + 1];\n        int* childAssignments = calculateAssignments(tree, subtreeSizes, child, N);\n        totalAssignments = (totalAssignments * childAssignments[1]) % MOD;\n    }\n    assignments[1] = totalAssignments;\n    return assignments;\n}\n\nint main() {\n    int N;\n    scanf(\"%d\", &N);\n    int** tree = (int**) malloc(N * sizeof(int*));\n    for (int i = 0; i < N; i++) {\n        tree[i] = (int*) malloc(2 * sizeof(int));\n        tree[i][0] = 0;\n    }\n    for (int i = 1; i < N; i++) {\n        int parent;\n        scanf(\"%d\", &parent);\n        parent--;\n        tree[parent][0]++;\n        tree[parent] = (int*) realloc(tree[parent], (tree[parent][0] + 1) * sizeof(int));\n        tree[parent][tree[parent][0]] = i;\n    }\n    int* subtreeSizes = (int*) malloc(N * sizeof(int));\n    for (int i = 0; i < N; i++) {\n        subtreeSizes[i] = 1;\n    }\n    for (int i = N - 1; i >= 0; i--) {\n        for (int j = 0; j < tree[i][0]; j++) {\n            int child = tree[i][j + 1];\n            subtreeSizes[i] += subtreeSizes[child];\n        }\n    }\n    int* assignments = calculateAssignments(tree, subtreeSizes, 0, N);\n    printf(\"%d\\n\", (assignments[0] + assignments[1]) % MOD);\n    return 0;\n}",
      "C++": "#include <iostream>\n#include <vector>\nusing namespace std;\n#define MOD 1000000007\n\ntypedef long long ll;\n\nll calculateAssignments(vector<vector<int>>& tree, vector<int>& subtreeSizes, int node) {\n    if (subtreeSizes[node] == 1) {\n        return 2;\n    }\n    ll totalAssignments = 1;\n    for (int child : tree[node]) {\n        totalAssignments = (totalAssignments * calculateAssignments(tree, subtreeSizes, child)) % MOD;\n    }\n    return totalAssignments;\n}\n\nint main() {\n    int N;\n    cin >> N;\n    vector<vector<int>> tree(N);\n    for (int i = 1; i < N; i++) {\n        int parent;\n        cin >> parent;\n        parent--;\n        tree[parent].push_back(i);\n    }\n    vector<int> subtreeSizes(N, 1);\n    for (int i = N - 1; i >= 0; i--) {\n        for (int child : tree[i]) {\n            subtreeSizes[i] += subtreeSizes[child];\n        }\n    }\n    ll assignments = calculateAssignments(tree, subtreeSizes, 0);\n    cout << assignments % MOD << endl;\n    return 0;\n}",
      "Java": "import java.util.Scanner;\npublic class Main {\n    static final int MOD = 1000000007;\n    public static void main(String[] args) {\n        Scanner scanner = new Scanner(System.in);\n        int N = scanner.nextInt();\n        int[][] tree = new int[N][2];\n        for (int i = 0; i < N; i++) {\n            tree[i][0] = 0;\n        }\n        for (int i = 1; i < N; i++) {\n            int parent = scanner.nextInt() - 1;\n            tree[parent][0]++;\n            tree[parent][tree[parent][0]] = i;\n        }\n        int[] subtreeSizes = new int[N];\n        for (int i = 0; i < N; i++) {\n            subtreeSizes[i] = 1;\n        }\n        for (int i = N - 1; i >= 0; i--) {\n            for (int j = 0; j < tree[i][0]; j++) {\n                int child = tree[i][j + 1];\n                subtreeSizes[i] += subtreeSizes[child];\n            }\n        }\n        long assignments = calculateAssignments(tree, subtreeSizes, 0, N);\n        System.out.println(assignments % MOD);\n    }\n    public static long calculateAssignments(int[][] tree, int[] subtreeSizes, int node, int N) {\n        if (subtreeSizes[node] == 1) {\n            return 2;\n        }\n        long totalAssignments = 1;\n        for (int i = 0; i < tree[node][0]; i++) {\n            int child = tree[node][i + 1];\n            totalAssignments = (totalAssignments * calculateAssignments(tree, subtreeSizes, child, N)) % MOD;\n        }\n        return totalAssignments;\n    }\n}",
      "Python3": "import sys\nMOD = 1000000007\n\ndef calculate_assignments(tree, subtree_sizes, node):\n    if subtree_sizes[node] == 1:\n        return 2\n    total_assignments = 1\n    for child in tree[node]:\n        total_assignments = (total_assignments * calculate_assignments(tree, subtree_sizes, child)) % MOD\n    return total_assignments\n\ndef main():\n    N = int(sys.stdin.readline().strip())\n    tree = [[] for _ in range(N)]\n    for i in range(1, N):\n        parent = int(sys.stdin.readline().strip()) - 1\n        tree[parent].append(i)\n    subtree_sizes = [1] * N\n    for i in range(N - 1, -1, -1):\n        for child in tree[i]:\n            subtree_sizes[i] += subtree_sizes[child]\n    assignments = calculate_assignments(tree, subtree_sizes, 0)\n    print(assignments % MOD)\nif __name__ == '__main__':\n    main()",
      "JavaScript": "const fs = require('fs');\nconst input = fs.readFileSync(0, 'utf-8').trim().split('\\n');\nlet idx = 0;\nconst MOD = 1000000007;\n\nfunction calculateAssignments(tree, subtreeSizes, node) {\n    if (subtreeSizes[node] === 1) {\n        return 2;\n    }\n    let totalAssignments = 1;\n    for (let child of tree[node]) {\n        totalAssignments = (totalAssignments * calculateAssignments(tree, subtreeSizes, child)) % MOD;\n    }\n    return totalAssignments;\n}\n\nfunction main() {\n    const N = parseInt(input[idx++]);\n    const tree = Array(N).fill(0).map(() => []);\n    for (let i = 1; i < N; i++) {\n        const parent = parseInt(input[idx++]) - 1;\n        tree[parent].push(i);\n    }\n    const subtreeSizes = Array(N).fill(1);\n    for (let i = N - 1; i >= 0; i--) {\n        for (let child of tree[i]) {\n            subtreeSizes[i] += subtreeSizes[child];\n        }\n    }\n    const assignments = calculateAssignments(tree, subtreeSizes, 0);\n    console.log(assignments % MOD);\n}\nmain();"
    }
  },
  "QBCategory": "CCD",
  "boilerPlates": {
    "C": "#include <stdio.h>\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "C++": "#include <iostream>\nusing namespace std;\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "Java": "import java.util.*;\nimport java.io.*;\n\npublic class Main {\n    public static void main(String[] args) throws IOException {\n        // your code goes here\n    }\n}",
    "Python3": "# your code goes here\n",
    "JavaScript": "// your code goes here\n"
  },
  "_internal": {
    "methodName": "",
    "params": []
  }
}