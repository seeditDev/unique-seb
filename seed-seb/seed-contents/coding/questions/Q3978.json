{
  "questionId": "Q3978",
  "title": "Archi and Tree",
  "slug": "archi-and-tree",
  "version": 1,
  "createdAt": "2026-07-02",
  "updatedAt": "2026-07-04",
  "metadata": {
    "category": "Graphs",
    "difficulty": "Hard",
    "isPremium": false,
    "tags": [
      "Graphs",
      "Dijkstra's algorithm",
      "BFS",
      "Shortest path"
    ],
    "companyTags": [],
    "topicTags": [
      "Trees"
    ]
  },
  "content": {
    "problemStatement": "Archi is a small boy from Uzhlyandia. For Chef's birthday, he prepared a tree with NNN vertices (numbered 111 through NNN). Each edge in this tree has a certain length.\n\nThere are MMM speedsters on the tree, numbered 111 through MMM. For each valid iii, speedster number iii starts at time tit_iti\u200b from vertex number uiu_iui\u200b and runs straight to vertex number viv_ivi\u200b with constant velocity sis_isi\u200b. Before time tit_iti\u200b, the speedster is not considered to be present in the tree.\n\nChef is interested in the following question: for each vertex, what is the smallest time when a speedster is present at this vertex?",
    "constraints": [
      "1 <= T <= 100",
      "1 <= N, M <= 2 * 10^5",
      "1 <= u, v <= N",
      "1 <= w, t, s <= 10^9",
      "The graph described on the input is a tree",
      "The sum of N for all test cases does not exceed 2 * 10^5",
      "The sum of M for all test cases does not exceed 2 * 10^5"
    ],
    "inputFormat": "The first line of the input contains a single integer T denoting the number of test cases. The description of T test cases follows. The first line of each test case contains a single integer N. Each of the next N-1 lines contains three space-separated integers u, v and w denoting an edge between vertices u and v with length w. The next line contains a single integer M. For each valid i, the i-th of the next M lines contains four space-separated integers ui, vi, ti and si describing the i-th speedster.",
    "outputFormat": "For each test case, print N lines. For each valid i, the i-th of these lines should contain a single real number \u2014 the minimum time when the i-th vertex is visited by a speedster, or -1 if it is never visited.",
    "sampleTestCases": [
      {
        "id": "sample_1",
        "input": "",
        "output": "1\n5\n1 2 3\n1 3 5\n3 4 1\n4 5 4\n3\n2 1 3 4\n4 2 1 3\n1 3 2 6",
        "explanation": ""
      },
      {
        "id": "sample_2",
        "input": "",
        "output": "2.0000000\n3.0000000\n1.3333333\n1.0000000\n-1",
        "explanation": ""
      }
    ]
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "label": "Case 1",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_2",
        "label": "Case 2",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_3",
        "label": "Case 3",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_4",
        "label": "Case 4",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_5",
        "label": "Case 5",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      }
    ],
    "totalTestCases": 5,
    "totalWeight": 10
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      2,
      2,
      2,
      2,
      2
    ],
    "maxScore": 10
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "To solve this problem, we can use a modified version of Dijkstra's algorithm. We will maintain a priority queue of vertices to be processed, where the priority of each vertex is the minimum time when it can be visited by a speedster. We will also maintain an array to store the minimum time when each vertex is visited. For each speedster, we will calculate the time when it reaches each vertex and update the minimum time if necessary.",
    "timeComplexity": "O(T * (N + M) * logN) \u2014 where T is the number of test cases, N is the number of vertices and M is the number of speedsters. The reason is that we are using a priority queue to process the vertices, and each insertion and deletion operation takes O(logN) time.",
    "spaceComplexity": "O(T * N) \u2014 where T is the number of test cases and N is the number of vertices. The reason is that we need to store the minimum time when each vertex is visited for each test case.",
    "code": {
      "C": "// COMPLETE, COMPILABLE C code\n#include <stdio.h>\n#include <stdlib.h>\n#include <limits.h>\n#define INF LONG_MAX\n#define MAXN 200005\n#define MAXM 200005\n// Define the structure for a speedster\ntypedef struct {\n    int u, v, t, s;\n} Speedster;\n// Define the structure for a vertex\ntypedef struct {\n    int id;\n    long long dist;\n} Vertex;\n// Compare function for the priority queue\nint compare(const void *a, const void *b) {\n    Vertex *va = (Vertex *)a;\n    Vertex *vb = (Vertex *)b;\n    return va->dist - vb->dist;\n}\n// Function to calculate the minimum time when a vertex is visited\nvoid calculateMinTime(int N, int M, int **edges, Speedster *speedsters) {\n    long long *minTime = (long long *)calloc(N, sizeof(long long));\n    for (int i = 0; i < N; i++) {\n        minTime[i] = INF;\n    }\n    // Process each speedster\n    for (int i = 0; i < M; i++) {\n        Speedster *speedster = &speedsters[i];\n        int u = speedster->u - 1;\n        int v = speedster->v - 1;\n        int t = speedster->t;\n        int s = speedster->s;\n        // Calculate the time when the speedster reaches each vertex\n        long long *time = (long long *)calloc(N, sizeof(long long));\n        for (int j = 0; j < N; j++) {\n            time[j] = INF;\n        }\n        time[u] = t;\n        // Use a priority queue to process the vertices\n        Vertex *queue = (Vertex *)calloc(N, sizeof(Vertex));\n        int queueSize = 0;\n        queue[queueSize].id = u;\n        queue[queueSize].dist = time[u];\n        queueSize++;\n        while (queueSize > 0) {\n            // Extract the vertex with the minimum distance\n            Vertex minVertex = queue[0];\n            for (int j = 1; j < queueSize; j++) {\n                if (queue[j].dist < minVertex.dist) {\n                    minVertex = queue[j];\n                }\n            }\n            // Remove the extracted vertex from the queue\n            for (int j = 0; j < queueSize; j++) {\n                if (queue[j].id == minVertex.id) {\n                    for (int k = j; k < queueSize - 1; k++) {\n                        queue[k] = queue[k + 1];\n                    }\n                    queueSize--;\n                    break;\n                }\n            }\n            // Update the distances of the adjacent vertices\n            for (int j = 0; j < N - 1; j++) {\n                int x = edges[j][0] - 1;\n                int y = edges[j][1] - 1;\n                int w = edges[j][2];\n                if (x == minVertex.id) {\n                    long long newTime = minVertex.dist + (long long)w / s;\n                    if (newTime < time[y]) {\n                        time[y] = newTime;\n                        queue[queueSize].id = y;\n                        queue[queueSize].dist = newTime;\n                        queueSize++;\n                    }\n                } else if (y == minVertex.id) {\n                    long long newTime = minVertex.dist + (long long)w / s;\n                    if (newTime < time[x]) {\n                        time[x] = newTime;\n                        queue[queueSize].id = x;\n                        queue[queueSize].dist = newTime;\n                        queueSize++;\n                    }\n                }\n            }\n        }\n        // Update the minimum time when each vertex is visited\n        for (int j = 0; j < N; j++) {\n            if (time[j] < minTime[j]) {\n                minTime[j] = time[j];\n            }\n        }\n        free(time);\n        free(queue);\n    }\n    // Print the minimum time when each vertex is visited\n    for (int i = 0; i < N; i++) {\n        if (minTime[i] == INF) {\n            printf(\"-1\\n\");\n        } else {\n            printf(\"%lld\\n\", minTime[i]);\n        }\n    }\n    free(minTime);\n}\nint main() {\n    int T;\n    scanf(\"%d\", &T);\n    for (int i = 0; i < T; i++) {\n        int N;\n        scanf(\"%d\", &N);\n        int **edges = (int **)calloc(N - 1, sizeof(int *));\n        for (int j = 0; j < N - 1; j++) {\n            edges[j] = (int *)calloc(3, sizeof(int));\n            scanf(\"%d %d %d\", &edges[j][0], &edges[j][1], &edges[j][2]);\n        }\n        int M;\n        scanf(\"%d\", &M);\n        Speedster *speedsters = (Speedster *)calloc(M, sizeof(Speedster));\n        for (int j = 0; j < M; j++) {\n            scanf(\"%d %d %d %d\", &speedsters[j].u, &speedsters[j].v, &speedsters[j].t, &speedsters[j].s);\n        }\n        calculateMinTime(N, M, edges, speedsters);\n        for (int j = 0; j < N - 1; j++) {\n            free(edges[j]);\n        }\n        free(edges);\n        free(speedsters);\n    }\n    return 0;\n}",
      "C++": "// COMPLETE, COMPILABLE C++ code\n#include <iostream>\n#include <vector>\n#include <queue>\n#include <limits>\nusing namespace std;\n#define INF LONG_MAX\n#define MAXN 200005\n#define MAXM 200005\n// Define the structure for a speedster\nstruct Speedster {\n    int u, v, t, s;\n};\n// Define the structure for a vertex\nstruct Vertex {\n    int id;\n    long long dist;\n    bool operator<(const Vertex &other) const {\n        return dist > other.dist;\n    }\n};\n// Function to calculate the minimum time when a vertex is visited\nvoid calculateMinTime(int N, int M, vector<vector<int>> &edges, Speedster *speedsters) {\n    vector<long long> minTime(N, INF);\n    // Process each speedster\n    for (int i = 0; i < M; i++) {\n        Speedster *speedster = &speedsters[i];\n        int u = speedster->u - 1;\n        int v = speedster->v - 1;\n        int t = speedster->t;\n        int s = speedster->s;\n        // Calculate the time when the speedster reaches each vertex\n        vector<long long> time(N, INF);\n        time[u] = t;\n        // Use a priority queue to process the vertices\n        priority_queue<Vertex> queue;\n        queue.push({u, time[u]});\n        while (!queue.empty()) {\n            Vertex minVertex = queue.top();\n            queue.pop();\n            // Update the distances of the adjacent vertices\n            for (int j = 0; j < N - 1; j++) {\n                int x = edges[j][0] - 1;\n                int y = edges[j][1] - 1;\n                int w = edges[j][2];\n                if (x == minVertex.id) {\n                    long long newTime = minVertex.dist + (long long)w / s;\n                    if (newTime < time[y]) {\n                        time[y] = newTime;\n                        queue.push({y, newTime});\n                    }\n                } else if (y == minVertex.id) {\n                    long long newTime = minVertex.dist + (long long)w / s;\n                    if (newTime < time[x]) {\n                        time[x] = newTime;\n                        queue.push({x, newTime});\n                    }\n                }\n            }\n        }\n        // Update the minimum time when each vertex is visited\n        for (int j = 0; j < N; j++) {\n            if (time[j] < minTime[j]) {\n                minTime[j] = time[j];\n            }\n        }\n    }\n    // Print the minimum time when each vertex is visited\n    for (int i = 0; i < N; i++) {\n        if (minTime[i] == INF) {\n            cout << -1 << endl;\n        } else {\n            cout << minTime[i] << endl;\n        }\n    }\n}\nint main() {\n    int T;\n    cin >> T;\n    for (int i = 0; i < T; i++) {\n        int N;\n        cin >> N;\n        vector<vector<int>> edges(N - 1, vector<int>(3));\n        for (int j = 0; j < N - 1; j++) {\n            cin >> edges[j][0] >> edges[j][1] >> edges[j][2];\n        }\n        int M;\n        cin >> M;\n        Speedster *speedsters = new Speedster[M];\n        for (int j = 0; j < M; j++) {\n            cin >> speedsters[j].u >> speedsters[j].v >> speedsters[j].t >> speedsters[j].s;\n        }\n        calculateMinTime(N, M, edges, speedsters);\n        delete[] speedsters;\n    }\n    return 0;\n}",
      "Java": ""
    }
  }
}