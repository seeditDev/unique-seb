{
  "questionId": "Q4655",
  "title": "Chef Counts Primes",
  "slug": "chef-counts-primes",
  "version": 1,
  "createdAt": "2026-07-01",
  "updatedAt": "2026-07-04",
  "metadata": {
    "category": "Number Theory",
    "difficulty": "Hard",
    "isPremium": true,
    "tags": [
      "Prime Numbers",
      "Sieve of Eratosthenes",
      "Prefix Sum",
      "Precomputation"
    ],
    "companyTags": [],
    "topicTags": [
      "Combinatorics & Number Theory"
    ],
    "QBCategory": "CCD"
  },
  "content": {
    "problemStatement": "Chef loves exploring numbers and wants to study prime numbers in a new way.  Given an integer limit limitlimitlimit, Chef wants to know: **How many prime numbers are strictly less than limitlimitlimit ?**\n\nYour job is to help Chef count these primes efficiently.\n\nFunction Declaration\n\n**Function Name**\n\n\n\ncountPrimesLessThancountPrimesLessThancountPrimesLessThan\n\n\n\n**Parameters**\n\n\n\nlimitlimitlimit — A non-negative integer (upper bound, exclusive)\n\n\n\n**Return Value**\n\n\n\nReturns an integer:\n\nthe number of prime numbers **strictly less than** limitlimitlimit",
    "constraints": [
      "1 ≤ T ≤ 100000",
      "0 ≤ limit ≤ 5000000"
    ],
    "inputFormat": "The first line contains an integer T, the number of test cases. Each of the next T lines contains a single integer limit (0 ≤ limit ≤ 5 000 000).",
    "outputFormat": "For each test case output a single line containing the number of prime numbers strictly less than the given limit.",
    "sampleTestCases": [
      {
        "id": "sample_1",
        "input": "5\n0\n2\n3\n20\n100",
        "output": "0\n0\n1\n8\n25",
        "explanation": "For each limit we count primes smaller than it: <0 → 0, <2 → 0, <3 → {2} → 1, <20 → 8 primes, <100 → 25 primes."
      },
      {
        "id": "sample_2",
        "input": "3\n1\n5\n5000000",
        "output": "0\n2\n348513",
        "explanation": "π(1)=0, π(5)=2 (2,3), π(5 000 000)=348513."
      },
      {
        "id": "sample_3",
        "input": "2\n10\n11",
        "output": "4\n4",
        "explanation": "Primes <10 are 2,3,5,7 →4. Primes <11 are the same four because 11 itself is not counted."
      }
    ]
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "label": "minimum boundary",
        "input": "1\n0",
        "expectedOutput": "0",
        "weight": 2
      },
      {
        "id": "tc_2",
        "label": "maximum boundary",
        "input": "1\n5000000",
        "expectedOutput": "348513",
        "weight": 2
      },
      {
        "id": "tc_3",
        "label": "edge case",
        "input": "1\n1",
        "expectedOutput": "0",
        "weight": 2
      },
      {
        "id": "tc_4",
        "label": "general case 1",
        "input": "3\n10\n100\n1000",
        "expectedOutput": "4\n25\n168",
        "weight": 2
      },
      {
        "id": "tc_5",
        "label": "general case 2",
        "input": "4\n2\n3\n4\n5",
        "expectedOutput": "0\n1\n2\n2",
        "weight": 2
      }
    ],
    "totalTestCases": 5,
    "totalWeight": 10
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      2,
      2,
      2,
      2,
      2
    ],
    "maxScore": 10
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "Pre‑compute primality for every integer up to the maximum possible limit (5 000 000) using the Sieve of Eratosthenes. While sieving, also build a prefix array pref where pref[i] = number of primes ≤ i. For a query limit, the answer is pref[limit‑1] (or 0 when limit is 0). This makes each test case O(1) after an O(N log log N) preprocessing step.",
    "timeComplexity": "Preprocessing: O(N log log N) where N = 5 000 000. Each query: O(1). Total: O(N log log N + T).",
    "spaceComplexity": "O(N) for the boolean sieve and the prefix array.",
    "code": {
      "C": "#include <stdio.h>\n#include <stdbool.h>\n#define MAX_LIMIT 5000001\nstatic bool isPrime[MAX_LIMIT];\nstatic int pref[MAX_LIMIT];\nvoid preprocess() {\n    for (int i = 0; i < MAX_LIMIT; ++i) isPrime[i] = true;\n    isPrime[0] = isPrime[1] = false;\n    for (int i = 2; i * i < MAX_LIMIT; ++i) {\n        if (isPrime[i]) {\n            for (int j = i * i; j < MAX_LIMIT; j += i) {\n                isPrime[j] = false;\n            }\n        }\n    }\n    pref[0] = 0;\n    for (int i = 1; i < MAX_LIMIT; ++i) {\n        pref[i] = pref[i-1] + (isPrime[i] ? 1 : 0);\n    }\n}\nint main() {\n    preprocess();\n    int T;\n    if (scanf(\"%d\", &T) != 1) return 0;\n    while (T--) {\n        int limit;\n        scanf(\"%d\", &limit);\n        if (limit == 0) {\n            printf(\"0\\n\");\n        } else {\n            printf(\"%d\\n\", pref[limit-1]);\n        }\n    }\n    return 0;\n}",
      "C++": "#include <bits/stdc++.h>\nusing namespace std;\nconst int MAX_LIMIT = 5000001;\nstatic vector<bool> isPrime(MAX_LIMIT, true);\nstatic vector<int> pref(MAX_LIMIT);\nvoid preprocess() {\n    isPrime[0] = isPrime[1] = false;\n    for (int i = 2; i * i < MAX_LIMIT; ++i) {\n        if (isPrime[i]) {\n            for (int j = i * i; j < MAX_LIMIT; j += i) isPrime[j] = false;\n        }\n    }\n    pref[0] = 0;\n    for (int i = 1; i < MAX_LIMIT; ++i) pref[i] = pref[i-1] + (isPrime[i] ? 1 : 0);\n}\nint main() {\n    ios::sync_with_stdio(false);\n    cin.tie(nullptr);\n    preprocess();\n    int T;\n    if(!(cin >> T)) return 0;\n    while (T--) {\n        int limit;\n        cin >> limit;\n        if (limit == 0) cout << 0 << '\\n';\n        else cout << pref[limit-1] << '\\n';\n    }\n    return 0;\n}",
      "Java": "import java.io.*;\nimport java.util.*;\npublic class Main {\n    private static final int MAX_LIMIT = 5000001;\n    private static final boolean[] isPrime = new boolean[MAX_LIMIT];\n    private static final int[] pref = new int[MAX_LIMIT];\n    private static void preprocess() {\n        Arrays.fill(isPrime, true);\n        isPrime[0] = isPrime[1] = false;\n        for (int i = 2; i * i < MAX_LIMIT; ++i) {\n            if (isPrime[i]) {\n                for (int j = i * i; j < MAX_LIMIT; j += i) isPrime[j] = false;\n            }\n        }\n        pref[0] = 0;\n        for (int i = 1; i < MAX_LIMIT; ++i) {\n            pref[i] = pref[i-1] + (isPrime[i] ? 1 : 0);\n        }\n    }\n    public static void main(String[] args) throws Exception {\n        preprocess();\n        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));\n        int T = Integer.parseInt(br.readLine().trim());\n        StringBuilder sb = new StringBuilder();\n        for (int t = 0; t < T; ++t) {\n            int limit = Integer.parseInt(br.readLine().trim());\n            if (limit == 0) sb.append(0);\n            else sb.append(pref[limit-1]);\n            sb.append('\\n');\n        }\n        System.out.print(sb.toString());\n    }\n}",
      "Python3": "import sys\nMAX_LIMIT = 5000001\nis_prime = [True] * MAX_LIMIT\nis_prime[0] = is_prime[1] = False\nfor i in range(2, int(MAX_LIMIT ** 0.5) + 1):\n    if is_prime[i]:\n        step = i\n        start = i * i\n        is_prime[start:MAX_LIMIT:step] = [False] * ((MAX_LIMIT - start - 1) // step + 1)\n# prefix where pref[i] = number of primes <= i\npref = [0] * MAX_LIMIT\ncnt = 0\nfor i in range(MAX_LIMIT):\n    if is_prime[i]:\n        cnt += 1\n    pref[i] = cnt\n\ndef main():\n    data = sys.stdin.read().strip().split()\n    if not data:\n        return\n    it = iter(data)\n    T = int(next(it))\n    out = []\n    for _ in range(T):\n        limit = int(next(it))\n        if limit == 0:\n            out.append('0')\n        else:\n            out.append(str(pref[limit-1]))\n    sys.stdout.write('\\n'.join(out))\n\nif __name__ == \"__main__\":\n    main()",
      "JavaScript": "const fs = require('fs');\nconst input = fs.readFileSync(0, 'utf-8').trim().split(/\\s+/);\nlet idx = 0;\nconst MAX_LIMIT = 5000001;\nconst isPrime = new Uint8Array(MAX_LIMIT);\nfor (let i = 2; i < MAX_LIMIT; ++i) isPrime[i] = 1;\nfor (let i = 2; i * i < MAX_LIMIT; ++i) {\n    if (isPrime[i]) {\n        for (let j = i * i; j < MAX_LIMIT; j += i) isPrime[j] = 0;\n    }\n}\nconst pref = new Uint32Array(MAX_LIMIT);\nlet cnt = 0;\nfor (let i = 0; i < MAX_LIMIT; ++i) {\n    if (isPrime[i]) cnt++;\n    pref[i] = cnt;\n}\nconst T = Number(input[idx++]);\nlet out = [];\nfor (let t = 0; t < T; ++t) {\n    const limit = Number(input[idx++]);\n    if (limit === 0) out.push('0');\n    else out.push(String(pref[limit - 1]));\n}\nconsole.log(out.join('\\n'));"
    }
  },
  "QBCategory": "CCD",
  "boilerPlates": {
    "C": "#include <stdio.h>\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "C++": "#include <iostream>\nusing namespace std;\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "Java": "import java.util.*;\nimport java.io.*;\n\npublic class Main {\n    public static void main(String[] args) throws IOException {\n        // your code goes here\n    }\n}",
    "Python3": "# your code goes here\n",
    "JavaScript": "// your code goes here\n"
  },
  "_internal": {
    "methodName": "",
    "params": []
  }
}