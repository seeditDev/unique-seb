{
  "questionId": "Q9545",
  "title": "Prefix Ones",
  "slug": "prefix-ones",
  "version": 1,
  "createdAt": "2026-07-02",
  "updatedAt": "2026-07-04",
  "metadata": {
    "category": "Strings | Greedy",
    "difficulty": "Medium",
    "isPremium": true,
    "tags": [
      "Prefix",
      "Substring Removal",
      "Greedy",
      "Linear Scan"
    ],
    "companyTags": [],
    "topicTags": [
      "Two Pointers & Sliding Window"
    ],
    "QBCategory": "CCD"
  },
  "content": {
    "problemStatement": "Chef has a binary string SSS of length NNN.\n\nChef can perform the following operation on the string:\n\n\nChoose any substring of SSS;\nRemove the chosen substring from SSS;\nConcatenate the remaining parts of the string SSS, obtained after removing the substring.\nFind the length of the longest prefix having **all** 111s you can achieve, by performing the above operation **at most** once.\n\nNote: A prefix of a string is obtained by deleting some (possibly zero) characters from the end of string SSS.\n\n\nInput Format\n\nThe first line of input will contain a single integer TTT, denoting the number of test cases.\nEach test case consists of multiple lines of input.\n\nThe first line of each test case contains a single integer NNN, the length of the binary string.\nThe next line contains the binary string SSS.\n\n\n\n\nOutput Format\nFor each test case, print in a new line, the length of the longest prefix having **all** 111s you can achieve, by performing the operation **at most** once.",
    "constraints": [
      "1 <= T <= 1000",
      "2 <= N <= 2*10^5",
      "The sum of N over all test cases does not exceed 2*10^5",
      "S contains only characters '0' and '1'"
    ],
    "inputFormat": "The first line contains an integer T, the number of test cases. For each test case: the first line contains an integer N, the length of the binary string. The second line contains the binary string S of length N consisting only of characters '0' and '1'.",
    "outputFormat": "For each test case output a single line containing the maximum possible length of a prefix consisting only of '1's after performing at most one substring removal operation.",
    "sampleTestCases": [
      {
        "id": "sample_1",
        "input": "3\n4\n1010\n6\n011011\n11\n01011011101",
        "output": "2\n2\n3",
        "explanation": "For each test case we keep the leading ones, then add the longest block of ones that appears after the first zero. The computed lengths are 2, 2 and 3 respectively."
      },
      {
        "id": "sample_2",
        "input": "2\n5\n11111\n5\n00000",
        "output": "5\n0",
        "explanation": "First string is already all ones, so answer is 5. Second string has no ones, so even after removal the longest prefix of ones is length 0."
      },
      {
        "id": "sample_3",
        "input": "1\n9\n111001111",
        "output": "7",
        "explanation": "Leading ones = 3. After the first zero the longest block of consecutive ones is of length 4. 3+4=7."
      }
    ]
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "label": "minimum boundary",
        "input": "1\n2\n00",
        "expectedOutput": "0",
        "weight": 2
      },
      {
        "id": "tc_2",
        "label": "maximum boundary (shortened for format)",
        "input": "1\n15\n111111111111111",
        "expectedOutput": "15",
        "weight": 2
      },
      {
        "id": "tc_3",
        "label": "edge case with leading ones then zeros then long ones",
        "input": "1\n9\n111001111",
        "expectedOutput": "7",
        "weight": 2
      },
      {
        "id": "tc_4",
        "label": "general case 1",
        "input": "1\n7\n1010101",
        "expectedOutput": "2",
        "weight": 2
      },
      {
        "id": "tc_5",
        "label": "general case 2",
        "input": "1\n6\n001100",
        "expectedOutput": "2",
        "weight": 2
      }
    ],
    "totalTestCases": 5,
    "totalWeight": 10
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      2,
      2,
      2,
      2,
      2
    ],
    "maxScore": 10
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "Let cnt1 be the number of consecutive '1's from the beginning of S. These leading ones can never be harmed because removing any character before them would only shorten the prefix. The first zero appears at position cnt1 (if it exists). By deleting a substring that starts at this first zero and ends just before some later position k, we can bring the block of ones that starts at k to the front, directly after the kept leading ones. Therefore the best we can achieve is cnt1 plus the length of the longest contiguous block of '1's that starts somewhere after the first zero. We obtain this by a single linear scan: after counting the leading ones, scan the rest of the string, keep the length of the current run of '1's, and track the maximum run length. The answer is cnt1 + maxRun. If the whole string is already all '1's, the answer is N.",
    "timeComplexity": "O(N) per test case, where N is the length of the string. The total over all test cases is O( Σ N ) ≤ O(2·10^5).",
    "spaceComplexity": "O(1) additional memory.",
    "code": {
      "C": "#include <stdio.h>\n#include <string.h>\n\nint main() {\n    int T; if (scanf(\"%d\", &T)!=1) return 0;\n    while (T--) {\n        int N; scanf(\"%d\", &N);\n        char s[200005]; scanf(\"%s\", s);\n        int i = 0;\n        while (i < N && s[i] == '1') i++;\n        int cnt1 = i;\n        int maxRun = 0, cur = 0;\n        for (; i < N; ++i) {\n            if (s[i] == '1') {\n                cur++;\n            } else {\n                if (cur > maxRun) maxRun = cur;\n                cur = 0;\n            }\n        }\n        if (cur > maxRun) maxRun = cur;\n        printf(\"%d\\n\", cnt1 + maxRun);\n    }\n    return 0;\n}",
      "C++": "#include <bits/stdc++.h>\nusing namespace std;\nint main(){\n    ios::sync_with_stdio(false);\n    cin.tie(nullptr);\n    int T; if(!(cin>>T)) return 0;\n    while(T--){\n        int N; string s; cin>>N>>s;\n        int i=0; while(i<N && s[i]=='1') ++i;\n        int cnt1=i;\n        int maxRun=0, cur=0;\n        for(; i<N; ++i){\n            if(s[i]=='1') ++cur;\n            else { maxRun=max(maxRun,cur); cur=0; }\n        }\n        maxRun=max(maxRun,cur);\n        cout<<cnt1+maxRun<<\"\\n\";\n    }\n    return 0;\n}",
      "Java": "import java.io.*;\nimport java.util.*;\n\npublic class Main {\n    public static void main(String[] args) throws Exception {\n        FastScanner fs = new FastScanner(System.in);\n        int T = fs.nextInt();\n        StringBuilder sb = new StringBuilder();\n        while (T-- > 0) {\n            int N = fs.nextInt();\n            char[] s = fs.next().toCharArray();\n            int i = 0;\n            while (i < N && s[i] == '1') i++;\n            int cnt1 = i;\n            int maxRun = 0, cur = 0;\n            for (; i < N; i++) {\n                if (s[i] == '1') cur++;\n                else { maxRun = Math.max(maxRun, cur); cur = 0; }\n            }\n            maxRun = Math.max(maxRun, cur);\n            sb.append(cnt1 + maxRun).append('\\n');\n        }\n        System.out.print(sb.toString());\n    }\n\n    // Fast scanner for competitive programming\n    static class FastScanner {\n        private final BufferedInputStream in;\n        private final byte[] buffer = new byte[1 << 16];\n        private int ptr = 0, len = 0;\n        FastScanner(InputStream stream) { in = new BufferedInputStream(stream); }\n        private int readByte() throws IOException {\n            if (ptr >= len) {\n                len = in.read(buffer);\n                ptr = 0;\n                if (len <= 0) return -1;\n            }\n            return buffer[ptr++];\n        }\n        String next() throws IOException {\n            StringBuilder sb = new StringBuilder();\n            int c;\n            while ((c = readByte()) != -1 && c <= ' ') {}\n            while (c != -1 && c > ' ') {\n                sb.append((char) c);\n                c = readByte();\n            }\n            return sb.toString();\n        }\n        int nextInt() throws IOException { return Integer.parseInt(next()); }\n    }\n}",
      "Python3": "#!/usr/bin/env python3\nimport sys\n\ndef solve():\n    data = sys.stdin.read().strip().split()\n    if not data:\n        return\n    it = iter(data)\n    T = int(next(it))\n    out_lines = []\n    for _ in range(T):\n        N = int(next(it))\n        s = next(it).strip()\n        i = 0\n        while i < N and s[i] == '1':\n            i += 1\n        cnt1 = i\n        max_run = cur = 0\n        while i < N:\n            if s[i] == '1':\n                cur += 1\n            else:\n                if cur > max_run:\n                    max_run = cur\n                cur = 0\n            i += 1\n        if cur > max_run:\n            max_run = cur\n        out_lines.append(str(cnt1 + max_run))\n    sys.stdout.write('\\n'.join(out_lines))\n\nif __name__ == \"__main__\":\n    solve()\n",
      "JavaScript": "const fs = require('fs');\nconst input = fs.readFileSync(0, 'utf-8').trim().split(/\\s+/);\nlet idx = 0;\nconst T = Number(input[idx++]);\nlet output = [];\nfor (let t = 0; t < T; t++) {\n  const N = Number(input[idx++]);\n  const s = input[idx++];\n  let i = 0;\n  while (i < N && s[i] === '1') i++;\n  const cnt1 = i;\n  let maxRun = 0, cur = 0;\n  for (; i < N; i++) {\n    if (s[i] === '1') cur++;\n    else { if (cur > maxRun) maxRun = cur; cur = 0; }\n  }\n  if (cur > maxRun) maxRun = cur;\n  output.push(String(cnt1 + maxRun));\n}\nconsole.log(output.join('\\n'));\n"
    }
  },
  "QBCategory": "CCD",
  "boilerPlates": {
    "C": "#include <stdio.h>\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "C++": "#include <iostream>\nusing namespace std;\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "Java": "import java.util.*;\nimport java.io.*;\n\npublic class Main {\n    public static void main(String[] args) throws IOException {\n        // your code goes here\n    }\n}",
    "Python3": "# your code goes here\n",
    "JavaScript": "// your code goes here\n"
  },
  "_internal": {
    "methodName": "",
    "params": []
  }
}