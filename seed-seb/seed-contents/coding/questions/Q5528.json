{
  "questionId": "Q5528",
  "title": "ChefTown Parade",
  "slug": "cheftown-parade",
  "version": 1,
  "createdAt": "2026-07-01",
  "updatedAt": "2026-07-04",
  "metadata": {
    "category": "Arrays | Sliding Window | Monotonic Queue",
    "difficulty": "Medium",
    "isPremium": true,
    "tags": [
      "Sliding Window",
      "Monotonic Queue",
      "Min Max in Window",
      "O(N)"
    ],
    "companyTags": [],
    "topicTags": [
      "Two Pointers & Sliding Window"
    ],
    "QBCategory": "CCD"
  },
  "content": {
    "problemStatement": "ChefTown is the biggest city and the capital of ChefLand. There are N beautiful buildings: restaurants, museums, living houses with large kitchens and so on. Every building has its height. For every i (1&lt;=i&lt;=N) there is exactly one building with height i. The buildings are located in a single line from left to right. The height of ith building is H(i). The Mayor of ChefTown wants to organize a parade, where all great chefs will take part. A parade depends of its location. The location of a parade is a segment of consecutive buildings beginning near the building number L and ending near the building number R (1&lt;=L&lt;=R&lt;=N). Any parade won't be interesting if it is not hold on an interesting segment of buildings. The segment of buildings is interesting if following are hold:\n\n\nImagine, that we have a segment [L,R].\nLet K=R-L+1 be the length of this segment, and B be a list of heights of the buildings that belong to this segment.\nLet's sort B in non-decreasing order.\nSegment [L,R] is interesting if B[i]-B[i-1]=1 for every i (2&lt;=i&lt;=K).\n\n\n\nNow the Mayor of ChefTown is interested how many ways to organize an interesting parade of length W for ChefTown citizens. Help him and find out the number of different parades of length W, which can be hold in the city. Two parades ([L1,R1] and [L2,R2]) are considered to be different, if L1≠L2 or R1≠R2.",
    "constraints": [
      "1 ≤ T ≤ 10",
      "1 ≤ N ≤ 400000",
      "1 ≤ W ≤ N",
      "The array H is a permutation of 1…N"
    ],
    "inputFormat": "The first line contains an integer T, the number of test cases. Each test case consists of two lines. The first line of a test case contains two integers N and W (1 ≤ W ≤ N ≤ 400000) – the number of buildings and the required parade length. The second line contains N distinct integers H1 … HN, a permutation of 1…N, where Hi is the height of the i‑th building.",
    "outputFormat": "For each test case output a single line containing the number of different interesting parades of length W.",
    "sampleTestCases": [
      {
        "id": "sample_1",
        "input": "1\n2 1\n2 1",
        "output": "2",
        "explanation": "W=1, every single building forms an interesting segment, so both positions give a parade."
      },
      {
        "id": "sample_2",
        "input": "1\\n4 2\\n1 2 3 4",
        "output": "3",
        "explanation": "All length‑2 windows have max‑min = 1, therefore the three windows [1,2], [2,3] and [3,4] are interesting."
      },
      {
        "id": "sample_3",
        "input": "1\\n5 3\\n2 5 3 4 1",
        "output": "1",
        "explanation": "Only the window [5,3,4] (positions 2‑4) has max=5, min=3, diff=2 = W‑1, so exactly one parade is possible."
      }
    ]
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "label": "minimum boundary",
        "input": "1\n1 1\n1",
        "expectedOutput": "1",
        "weight": 2
      },
      {
        "id": "tc_2",
        "label": "Case 2",
        "input": "1\n1 1\n1",
        "expectedOutput": "1",
        "weight": 2
      },
      {
        "id": "tc_3",
        "label": "Case 3",
        "input": "1\n1 1\n1",
        "expectedOutput": "1",
        "weight": 2
      },
      {
        "id": "tc_4",
        "label": "Case 4",
        "input": "1\n1 1\n1",
        "expectedOutput": "1",
        "weight": 2
      },
      {
        "id": "tc_5",
        "label": "Case 5",
        "input": "1\n1 1\n1",
        "expectedOutput": "1",
        "weight": 2
      }
    ],
    "totalTestCases": 5,
    "totalWeight": 10
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      2,
      2,
      2,
      2,
      2
    ],
    "maxScore": 10
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "For a segment of length W the heights after sorting are consecutive iff max‑min = W‑1 (all heights are distinct because the whole array is a permutation). Therefore we only need to know the minimum and maximum of every window of size W. Both can be maintained in O(1) amortised time using two monotonic deques: one decreasing for the maximum, one increasing for the minimum. While sliding the window we push the new index, pop indices that are out of the window, and keep the deques monotonic. For each window we check if max‑min == W‑1 and increase the answer. The whole algorithm runs in O(N) per test case and uses O(N) extra memory for the deques.",
    "timeComplexity": "O(N) per test case",
    "spaceComplexity": "O(N) in the worst case for the two deques (actually O(W))",
    "code": {
      "C": "#include <stdio.h>\n#include <stdlib.h>\n\ntypedef struct {\n    int *data;\n    int front;\n    int back;\n    int cap;\n} Deque;\n\nDeque* newDeque(int cap) {\n    Deque *dq = (Deque*)malloc(sizeof(Deque));\n    dq->data = (int*)malloc(sizeof(int)*cap);\n    dq->front = dq->back = 0;\n    dq->cap = cap;\n    return dq;\n}\n\nint empty(Deque *dq) { return dq->front == dq->back; }\nint front(Deque *dq) { return dq->data[dq->front]; }\nint back(Deque *dq) { return dq->data[(dq->back-1+ dq->cap)%dq->cap]; }\nvoid pushBack(Deque *dq, int val) { dq->data[dq->back] = val; dq->back = (dq->back+1)%dq->cap; }\nvoid popFront(Deque *dq) { dq->front = (dq->front+1)%dq->cap; }\nvoid popBack(Deque *dq) { dq->back = (dq->back-1+dq->cap)%dq->cap; }\n\nint main(){\n    int T; if(scanf(\"%d\", &T)!=1) return 0;\n    while(T--){\n        int N,W; scanf(\"%d %d\", &N, &W);\n        int *H = (int*)malloc(sizeof(int)*N);\n        for(int i=0;i<N;i++) scanf(\"%d\", &H[i]);\n        Deque *dqMin = newDeque(N+5), *dqMax = newDeque(N+5);\n        long long ans = 0;\n        for(int i=0;i<N;i++){\n            // maintain min deque (increasing)\n            while(!empty(dqMin) && H[back(dqMin)] >= H[i]) popBack(dqMin);\n            pushBack(dqMin, i);\n            // maintain max deque (decreasing)\n            while(!empty(dqMax) && H[back(dqMax)] <= H[i]) popBack(dqMax);\n            pushBack(dqMax, i);\n            // remove out of window\n            if(i>=W){\n                if(front(dqMin) <= i-W) popFront(dqMin);\n                if(front(dqMax) <= i-W) popFront(dqMax);\n            }\n            if(i>=W-1){\n                int curMin = H[front(dqMin)];\n                int curMax = H[front(dqMax)];\n                if(curMax - curMin == W-1) ans++;\n            }\n        }\n        printf(\"%lld\\n\", ans);\n        free(H);\n        free(dqMin->data); free(dqMin);\n        free(dqMax->data); free(dqMax);\n    }\n    return 0;\n}",
      "C++": "#include <bits/stdc++.h>\nusing namespace std;\nint main(){\n    ios::sync_with_stdio(false);\n    cin.tie(nullptr);\n    int T; if(!(cin>>T)) return 0;\n    while(T--){\n        int N,W; cin>>N>>W;\n        vector<int> H(N);\n        for(int &x:H) cin>>x;\n        deque<int> dqMin, dqMax; // store indices\n        long long ans=0;\n        for(int i=0;i<N;i++){\n            while(!dqMin.empty() && H[dqMin.back()]>=H[i]) dqMin.pop_back();\n            dqMin.push_back(i);\n            while(!dqMax.empty() && H[dqMax.back()]<=H[i]) dqMax.pop_back();\n            dqMax.push_back(i);\n            if(i>=W){\n                if(dqMin.front()<=i-W) dqMin.pop_front();\n                if(dqMax.front()<=i-W) dqMax.pop_front();\n            }\n            if(i>=W-1){\n                int curMin=H[dqMin.front()];\n                int curMax=H[dqMax.front()];\n                if(curMax - curMin == W-1) ++ans;\n            }\n        }\n        cout<<ans<<\"\\n\";\n    }\n    return 0;\n}",
      "Java": "import java.io.*;\nimport java.util.*;\npublic class Main {\n    public static void main(String[] args) throws Exception {\n        FastScanner fs = new FastScanner(System.in);\n        int T = fs.nextInt();\n        StringBuilder sb = new StringBuilder();\n        while (T-- > 0) {\n            int N = fs.nextInt();\n            int W = fs.nextInt();\n            int[] H = new int[N];\n            for (int i = 0; i < N; i++) H[i] = fs.nextInt();\n            Deque<Integer> dqMin = new ArrayDeque<>();\n            Deque<Integer> dqMax = new ArrayDeque<>();\n            long ans = 0;\n            for (int i = 0; i < N; i++) {\n                while (!dqMin.isEmpty() && H[dqMin.peekLast()] >= H[i]) dqMin.pollLast();\n                dqMin.addLast(i);\n                while (!dqMax.isEmpty() && H[dqMax.peekLast()] <= H[i]) dqMax.pollLast();\n                dqMax.addLast(i);\n                if (i >= W) {\n                    if (dqMin.peekFirst() <= i - W) dqMin.pollFirst();\n                    if (dqMax.peekFirst() <= i - W) dqMax.pollFirst();\n                }\n                if (i >= W - 1) {\n                    int curMin = H[dqMin.peekFirst()];\n                    int curMax = H[dqMax.peekFirst()];\n                    if (curMax - curMin == W - 1) ans++;\n                }\n            }\n            sb.append(ans).append('\\n');\n        }\n        System.out.print(sb.toString());\n    }\n    // fast scanner\n    static class FastScanner {\n        private final InputStream in;\n        private final byte[] buffer = new byte[1 << 16];\n        private int ptr = 0, len = 0;\n        FastScanner(InputStream in) { this.in = in; }\n        private int readByte() throws IOException {\n            if (ptr >= len) {\n                len = in.read(buffer);\n                ptr = 0;\n                if (len <= 0) return -1;\n            }\n            return buffer[ptr++];\n        }\n        int nextInt() throws IOException {\n            int c, sign = 1, val = 0;\n            do { c = readByte(); } while (c <= ' ');\n            if (c == '-') { sign = -1; c = readByte(); }\n            while (c > ' ') { val = val * 10 + (c - '0'); c = readByte(); }\n            return val * sign;\n        }\n    }\n}",
      "Python3": "import sys\nfrom collections import deque\n\ndef solve():\n    data = list(map(int, sys.stdin.read().strip().split()))\n    if not data:\n        return\n    it = iter(data)\n    T = next(it)\n    out_lines = []\n    for _ in range(T):\n        N = next(it); W = next(it)\n        H = [next(it) for _ in range(N)]\n        dq_min = deque()\n        dq_max = deque()\n        ans = 0\n        for i, val in enumerate(H):\n            while dq_min and H[dq_min[-1]] >= val:\n                dq_min.pop()\n            dq_min.append(i)\n            while dq_max and H[dq_max[-1]] <= val:\n                dq_max.pop()\n            dq_max.append(i)\n            if i >= W:\n                if dq_min[0] <= i - W:\n                    dq_min.popleft()\n                if dq_max[0] <= i - W:\n                    dq_max.popleft()\n            if i >= W - 1:\n                cur_min = H[dq_min[0]]\n                cur_max = H[dq_max[0]]\n                if cur_max - cur_min == W - 1:\n                    ans += 1\n        out_lines.append(str(ans))\n    sys.stdout.write('\\n'.join(out_lines))\n\nif __name__ == \"__main__\":\n    solve()",
      "JavaScript": "const fs = require('fs');\nconst data = fs.readFileSync(0, 'utf-8').trim().split(/\\s+/).map(Number);\nlet idx = 0;\nif (data.length === 0) process.exit(0);\nconst T = data[idx++];\nlet outputs = [];\nfor (let t = 0; t < T; t++) {\n  const N = data[idx++];\n  const W = data[idx++];\n  const H = data.slice(idx, idx + N);\n  idx += N;\n  const dqMin = [];\n  const dqMax = [];\n  let ans = 0;\n  const push = (dq, i, comp) => {\n    while (dq.length && comp(H[dq[dq.length - 1]], H[i])) dq.pop();\n    dq.push(i);\n  };\n  const popOut = (dq, limit) => {\n    if (dq.length && dq[0] <= limit) dq.shift();\n  };\n  for (let i = 0; i < N; i++) {\n    push(dqMin, i, (a, b) => a >= b); // increasing\n    push(dqMax, i, (a, b) => a <= b); // decreasing\n    if (i >= W) {\n      popOut(dqMin, i - W);\n      popOut(dqMax, i - W);\n    }\n    if (i >= W - 1) {\n      const curMin = H[dqMin[0]];\n      const curMax = H[dqMax[0]];\n      if (curMax - curMin === W - 1) ans++;\n    }\n  }\n  outputs.push(String(ans));\n}\nconsole.log(outputs.join('\\n'));"
    }
  },
  "QBCategory": "CCD",
  "boilerPlates": {
    "C": "#include <stdio.h>\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "C++": "#include <iostream>\nusing namespace std;\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "Java": "import java.util.*;\nimport java.io.*;\n\npublic class Main {\n    public static void main(String[] args) throws IOException {\n        // your code goes here\n    }\n}",
    "Python3": "# your code goes here\n",
    "JavaScript": "// your code goes here\n"
  },
  "_internal": {
    "methodName": "",
    "params": []
  }
}