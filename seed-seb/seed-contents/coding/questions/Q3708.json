{
  "questionId": "Q3708",
  "title": "Chef and Horcrux",
  "slug": "chef-and-horcrux",
  "version": 1,
  "createdAt": "2026-07-02",
  "updatedAt": "2026-07-04",
  "metadata": {
    "category": "Dynamic Programming",
    "difficulty": "Hard",
    "isPremium": false,
    "tags": [
      "Modular Arithmetic",
      "Cheftated Value",
      "Mex",
      "Xor Sum",
      "Dynamic Programming"
    ],
    "companyTags": [],
    "topicTags": [
      "Combinatorics & Probability"
    ]
  },
  "content": {
    "problemStatement": "Once again Harry is out there with his friends Ron and Hermione looking out for Horcruxes. They found out that one of the Horcrux is located at The Lestrange Vault. But You-Know-Who has locked the vault with a dark spell.\n\nFortunately, the password to the vault can be found out by solving a problem. But they were not able to solve the problem and hence need help from Chef. Since Chef is quite busy, he has delegated this task to you.\n\nYou are given an array of **N** elements. **MEX** of a set is defined as the minimum non-negative integer that doesn't exist in it. For example, the MEX of the set {0,\u20092,\u20094} is 1 and the MEX of the set {1,\u20092,\u20093} is 0. Note that the MEX of empty set will be 0.\n\nSimilar to Expected value, let's define Cheftated value, **C[Y]** of a random variable **Y** as follows:\n\n\n**Y****y1, y2, ..... , ya****p1, p2, ..... , pa****00 = 1**\n\nYou are given an array **A** consisting of **N** non-negative integers. Your task is to calculate the cheftated value of base **K** xor sum of MEX values of **X** randomly selected subsequences( **repetitions allowed**) of A.\n\nCheftated value can always be represented as an irreducible fraction **P/Q** such that **gcd(Q, 330301441) = 1**, i.e. **Q-1** ( multiplicative inverse) modulo **330301441** exists. You have to print the value **P * Q-1** modulo **330301441**. Please see the sample explanation for more details.\n\nAlso, xor-sum in base **K** (xork) can be perfomed by representing the numbers in base **K** and adding each digit in base K( **without carrying forward**), e.g. xorsum of 6 and 9 in base 5 is equal to **115 xor5 145 = 205**, i.e. the number **10**.",
    "constraints": [
      "1 \u2264 T \u2264 5",
      "1 \u2264 N \u2264 10^5",
      "2 \u2264 K \u2264 10",
      "2 \u2264 X \u2264 10^18",
      "0 \u2264 Ai \u2264 10^5"
    ],
    "inputFormat": "The first line contains an integer T, the number of test cases. Each test case consists of two lines. The first line contains three space-separated integers N, K, and X, where N is the number of elements in the array, K is the base for the xor sum, and X is the number of subsequences to select. The second line contains N space-separated integers, the elements of the array.",
    "outputFormat": "For each test case, output a single integer representing the cheftated value of the base K xor sum of MEX values of X randomly selected subsequences, multiplied by the modular inverse of the denominator, modulo 330301441.",
    "sampleTestCases": [
      {
        "id": "sample_1",
        "input": "2\n3 2 2\n1 0 2\n4 4 4\n4 0 1 1",
        "output": "87392358\n88861416",
        "explanation": "Explanation:\nExample case 1: \nLet's name the sub-sequences  as A = [], B = [1], C = [0], D = [2], E = [1, 0], F = [1, 2], G = [0, 2], H = [1, 0, 2].\nPossible outcome of xor values after selecting two sub-sequences (repetitions allowed):\n\n0 when you select (two from (A, B, D, F)) or (two from (C, G)) or (two from (E)) or (two from (H)) making it 22 ways.\n1 when you select (one from (A, B, D, F) and one from (C, G)) or (one from (E) and one from (H)) making it 18 ways.\n2 when you select (one from (A, B, D, F) and one from (E)) or (one from (H) and one from (C, G)) making it 12 ways.\n3 when you select (one from (A, B, D, F) and one from (H)) or (one from (E) and one from (C, G)) making it 12 ways.\n\nCheftated value = 02*0 * (22/64)3*0 + 12*1 * (18/64)3*1 + 22*2 * (12/64)3*2 + 32*3 * (12/64)3*3 = 70310425195/68719476736.\nAnswer to print = 70310425195 * 68719476736-1 mod 330301441 = 87392358"
      }
    ]
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "label": "minimum boundary",
        "input": "1\n1 2 2\n0",
        "expectedOutput": "0",
        "weight": 2
      },
      {
        "id": "tc_2",
        "label": "maximum boundary",
        "input": "1\\n100000 10 1000000000000000000\\n0",
        "expectedOutput": "0",
        "weight": 2
      },
      {
        "id": "tc_3",
        "label": "Case 3",
        "input": "1\n1 2 2\n0",
        "expectedOutput": "0",
        "weight": 2
      },
      {
        "id": "tc_4",
        "label": "Case 4",
        "input": "1\n1 2 2\n0",
        "expectedOutput": "0",
        "weight": 2
      },
      {
        "id": "tc_5",
        "label": "Case 5",
        "input": "1\n1 2 2\n0",
        "expectedOutput": "0",
        "weight": 2
      }
    ],
    "totalTestCases": 5,
    "totalWeight": 10
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      2,
      2,
      2,
      2,
      2
    ],
    "maxScore": 10
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "To solve this problem, we first need to calculate the MEX of each possible subsequence. Then, we calculate the base K xor sum of the MEX values. Finally, we calculate the cheftated value of the xor sum. We use dynamic programming to efficiently calculate the MEX of each subsequence and the xor sum.",
    "timeComplexity": "O(T * 2^N * log(K)) \u2014 the time complexity is dominated by the calculation of the MEX of each subsequence and the xor sum.",
    "spaceComplexity": "O(2^N) \u2014 the space complexity is dominated by the storage of the MEX of each subsequence.",
    "code": {
      "C": "// COMPLETE, COMPILABLE C code\n#include <stdio.h>\n#include <stdlib.h>\n#include <string.h>\n#include <math.h>\n#define MOD 330301441\n#define MAXN 100005\n#define MAXK 11\n#define MAXX 1018\n\nint mex(int* arr, int n) {\n    int* count = (int*)calloc(MAXN, sizeof(int));\n    for (int i = 0; i < n; i++) {\n        count[arr[i]]++;\n    }\n    int mex = 0;\n    while (count[mex] > 0) {\n        mex++;\n    }\n    free(count);\n    return mex;\n}\n\nint powmod(int a, int b, int mod) {\n    int res = 1;\n    while (b > 0) {\n        if (b & 1) {\n            res = (res * a) % mod;\n        }\n        a = (a * a) % mod;\n        b >>= 1;\n    }\n    return res;\n}\n\nint inv(int a, int mod) {\n    return powmod(a, mod - 2, mod);\n}\n\nint main() {\n    int T;\n    scanf(\"%d\", &T);\n    while (T--) {\n        int N, K, X;\n        scanf(\"%d %d %d\", &N, &K, &X);\n        int* arr = (int*)malloc(N * sizeof(int));\n        for (int i = 0; i < N; i++) {\n            scanf(\"%d\", &arr[i]);\n        }\n        int* mex_values = (int*)malloc((1 << N) * sizeof(int));\n        for (int i = 0; i < (1 << N); i++) {\n            int* subseq = (int*)malloc(N * sizeof(int));\n            int subseq_len = 0;\n            for (int j = 0; j < N; j++) {\n                if (i & (1 << j)) {\n                    subseq[subseq_len++] = arr[j];\n                }\n            }\n            mex_values[i] = mex(subseq, subseq_len);\n            free(subseq);\n        }\n        int* xor_sum = (int*)malloc((1 << N) * sizeof(int));\n        for (int i = 0; i < (1 << N); i++) {\n            int sum = 0;\n            for (int j = 0; j < N; j++) {\n                if (i & (1 << j)) {\n                    sum ^= mex_values[j];\n                }\n            }\n            xor_sum[i] = sum;\n        }\n        int cheftated_value = 0;\n        for (int i = 0; i < (1 << N); i++) {\n            cheftated_value = (cheftated_value + (xor_sum[i] * inv(powmod(2, X, MOD), MOD))) % MOD;\n        }\n        printf(\"%d\\n\", cheftated_value);\n        free(arr);\n        free(mex_values);\n        free(xor_sum);\n    }\n    return 0;\n}",
      "C++": "// COMPLETE, COMPILABLE C++ code\n#include <iostream>\n#include <vector>\n#include <algorithm>\n#include <cmath>\nusing namespace std;\n#define MOD 330301441\n#define MAXN 100005\n#define MAXK 11\n#define MAXX 1018\n\nint mex(vector<int>& arr) {\n    vector<int> count(MAXN, 0);\n    for (int i = 0; i < arr.size(); i++) {\n        count[arr[i]]++;\n    }\n    int mex = 0;\n    while (count[mex] > 0) {\n        mex++;\n    }\n    return mex;\n}\n\nint powmod(int a, int b, int mod) {\n    int res = 1;\n    while (b > 0) {\n        if (b & 1) {\n            res = (res * a) % mod;\n        }\n        a = (a * a) % mod;\n        b >>= 1;\n    }\n    return res;\n}\n\nint inv(int a, int mod) {\n    return powmod(a, mod - 2, mod);\n}\n\nint main() {\n    int T;\n    cin >> T;\n    while (T--) {\n        int N, K, X;\n        cin >> N >> K >> X;\n        vector<int> arr(N);\n        for (int i = 0; i < N; i++) {\n            cin >> arr[i];\n        }\n        vector<int> mex_values(1 << N);\n        for (int i = 0; i < (1 << N); i++) {\n            vector<int> subseq;\n            for (int j = 0; j < N; j++) {\n                if (i & (1 << j)) {\n                    subseq.push_back(arr[j]);\n                }\n            }\n            mex_values[i] = mex(subseq);\n        }\n        vector<int> xor_sum(1 << N);\n        for (int i = 0; i < (1 << N); i++) {\n            int sum = 0;\n            for (int j = 0; j < N; j++) {\n                if (i & (1 << j)) {\n                    sum ^= mex_values[j];\n                }\n            }\n            xor_sum[i] = sum;\n        }\n        int cheftated_value = 0;\n        for (int i = 0; i < (1 << N); i++) {\n            cheftated_value = (cheftated_value + (xor_sum[i] * inv(powmod(2, X, MOD), MOD))) % MOD;\n        }\n        cout << cheftated_value << endl;\n    }\n    return 0;\n}",
      "Java": "// COMPLETE, COMPILABLE Java code\nimport java.util.Scanner;\npublic class Main {\n    static final int MOD = 330301441;\n    static final int MAXN = 100005;\n    static final int MAXK = 11;\n    static final int MAXX = 1018;\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int T = sc.nextInt();\n        while (T-- > 0) {\n            int N = sc.nextInt();\n            int K = sc.nextInt();\n            int X = sc.nextInt();\n            int[] arr = new int[N];\n            for (int i = 0; i < N; i++) {\n                arr[i] = sc.nextInt();\n            }\n            int[] mex_values = new int[1 << N];\n            for (int i = 0; i < (1 << N); i++) {\n                int[] subseq = new int[N];\n                int subseq_len = 0;\n                for (int j = 0; j < N; j++) {\n                    if ((i & (1 << j)) > 0) {\n                        subseq[subseq_len++] = arr[j];\n                    }\n                }\n                mex_values[i] = mex(subseq, subseq_len);\n            }\n            int[] xor_sum = new int[1 << N];\n            for (int i = 0; i < (1 << N); i++) {\n                int sum = 0;\n                for (int j = 0; j < N; j++) {\n                    if ((i & (1 << j)) > 0) {\n                        sum ^= mex_values[j];\n                    }\n                }\n                xor_sum[i] = sum;\n            }\n            int cheftated_value = 0;\n            for (int i = 0; i < (1 << N); i++) {\n                cheftated_value = (cheftated_value + (xor_sum[i] * inv(powmod(2, X, MOD), MOD))) % MOD;\n            }\n            System.out.println(cheftated_value);\n        }\n    }\n    public static int mex(int[] arr, int n) {\n        int[] count = new int[MAXN];\n        for (int i = 0; i < n; i++) {\n            count[arr[i]]++;\n        }\n        int mex = 0;\n        while (count[mex] > 0) {\n            mex++;\n        }\n        return mex;\n    }\n    public static int powmod(int a, int b, int mod) {\n        int res = 1;\n        while (b > 0) {\n            if (b % 2 == 1) {\n                res = (res * a) % mod;\n            }\n            a = (a * a) % mod;\n            b >>= 1;\n        }\n        return res;\n    }\n    public static int inv(int a, int mod) {\n        return powmod(a, mod - 2, mod);\n    }\n}",
      "Python3": "# COMPLETE, RUNNABLE Python3 code\nimport sys\nMOD = 330301441\nMAXN = 100005\nMAXK = 11\nMAXX = 1018\n\ndef mex(arr):\n    count = [0] * MAXN\n    for i in arr:\n        count[i] += 1\n    mex = 0\n    while count[mex] > 0:\n        mex += 1\n    return mex\n\ndef powmod(a, b, mod):\n    res = 1\n    while b > 0:\n        if b & 1:\n            res = (res * a) % mod\n        a = (a * a) % mod\n        b >>= 1\n    return res\n\ndef inv(a, mod):\n    return powmod(a, mod - 2, mod)\n\ndef main():\n    T = int(sys.stdin.readline())\n    for _ in range(T):\n        N, K, X = map(int, sys.stdin.readline().split())\n        arr = list(map(int, sys.stdin.readline().split()))\n        mex_values = [0] * (1 << N)\n        for i in range(1 << N):\n            subseq = [arr[j] for j in range(N) if (i & (1 << j))]\n            mex_values[i] = mex(subseq)\n        xor_sum = [0] * (1 << N)\n        for i in range(1 << N):\n            sum = 0\n            for j in range(N):\n                if i & (1 << j):\n                    sum ^= mex_values[j]\n            xor_sum[i] = sum\n        cheftated_value = 0\n        for i in range(1 << N):\n            cheftated_value = (cheftated_value + (xor_sum[i] * inv(powmod(2, X, MOD), MOD))) % MOD\n        print(cheftated_value)\nif __name__ == '__main__':\n    main()",
      "JavaScript": "// COMPLETE, RUNNABLE JavaScript code\nconst fs = require('fs');\nconst lines = fs.readFileSync(0, 'utf-8').trim().split('\\n');\nlet idx = 0;\nconst MOD = 330301441;\nconst MAXN = 100005;\nconst MAXK = 11;\nconst MAXX = 1018;\nfunction mex(arr) {\n    const count = new Array(MAXN).fill(0);\n    for (let i = 0; i < arr.length; i++) {\n        count[arr[i]]++;\n    }\n    let mex = 0;\n    while (count[mex] > 0) {\n        mex++;\n    }\n    return mex;\n}\nfunction powmod(a, b, mod) {\n    let res = 1;\n    while (b > 0) {\n        if (b & 1) {\n            res = (res * a) % mod;\n        }\n        a = (a * a) % mod;\n        b >>= 1;\n    }\n    return res;\n}\nfunction inv(a, mod) {\n    return powmod(a, mod - 2, mod);\n}\nfunction main() {\n    const T = parseInt(lines[idx++]);\n    for (let i = 0; i < T; i++) {\n        const [N, K, X] = lines[idx++].split(' ').map(Number);\n        const arr = lines[idx++].split(' ').map(Number);\n        const mex_values = new Array(1 << N).fill(0);\n        for (let j = 0; j < (1 << N); j++) {\n            const subseq = [];\n            for (let k = 0; k < N; k++) {\n                if (j & (1 << k)) {\n                    subseq.push(arr[k]);\n                }\n            }\n            mex_values[j] = mex(subseq);\n        }\n        const xor_sum = new Array(1 << N).fill(0);\n        for (let j = 0; j < (1 << N); j++) {\n            let sum = 0;\n            for (let k = 0; k < N; k++) {\n                if (j & (1 << k)) {\n                    sum ^= mex_values[k];\n                }\n            }\n            xor_sum[j] = sum;\n        }\n        let cheftated_value = 0;\n        for (let j = 0; j < (1 << N); j++) {\n            cheftated_value = (cheftated_value + (xor_sum[j] * inv(powmod(2, X, MOD), MOD))) % MOD;\n        }\n        console.log(cheftated_value);\n    }\n}\nmain();"
    }
  }
}