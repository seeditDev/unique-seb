{
  "questionId": "Q8084",
  "title": "Sleep Cycle",
  "slug": "sleep-cycle",
  "version": 1,
  "createdAt": "2026-07-02",
  "updatedAt": "2026-07-04",
  "metadata": {
    "category": "Greedy | Strings",
    "difficulty": "Medium",
    "isPremium": true,
    "tags": [
      "Greedy",
      "Sorting",
      "String processing"
    ],
    "companyTags": [],
    "topicTags": [
      "Ad-hoc & Implementation"
    ],
    "QBCategory": "CCD"
  },
  "content": {
    "problemStatement": "Chef has been studying the human sleep cycle for years. He divides a day into LLL units of time (numbered 111 through LLL). Chef's research shows that his body requires spending only HHH continuous units of time per day on sleep — then, he can stay active during work. However, in case of travel or any other responsibilities, he might not be able to achieve this ideal state.\n\nNext, Chef noticed that if he sleeps for xxx (x&lt;Hx \\lt Hx&lt;H) continuous units of time and then performs some other activity, he can still meet his daily sleep quota, but the remaining time he needs to spend on continuously sleeping becomes 2⋅(H−x)2\\cdot(H-x)2⋅(H−x). He can further split this sleeping time in the same way, i.e. if he now sleeps for yyy more (y&lt;2⋅(H−x)y \\lt 2\\cdot(H-x)y&lt;2⋅(H−x)) continuous units of time, his required sleeping time becomes 2⋅(2⋅(H−x)−y)2\\cdot(2\\cdot(H-x)-y)2⋅(2⋅(H−x)−y), and so on.\n\nYou are given a string SSS with length LLL; for each valid iii, the iii-th character of this string is '0' if the iii-th unit of time is free (so Chef can use it to sleep) or '1' if it is occupied.\n\nCan Chef ever get enough sleep to feel fresh during his work, given that he can choose when to sleep in his free time?",
    "constraints": [
      "1 <= T <= 10",
      "1 <= H <= L <= 10^5",
      "S consists only of characters '0' and '1'"
    ],
    "inputFormat": "The first line contains an integer T, the number of test cases. Each test case consists of two lines. The first line contains two integers L and H. The second line contains a binary string S of length L, where '0' denotes a free time unit and '1' denotes an occupied unit.",
    "outputFormat": "For each test case output a single line containing \"YES\" if Chef can obtain enough sleep, otherwise output \"NO\".",
    "sampleTestCases": [
      {
        "id": "sample_1",
        "input": "4\n3 1\n111\n3 1\n101\n7 5\n0000100\n6 5\n000010",
        "output": "NO\nYES\nYES\nNO",
        "explanation": "Case 1: No free unit, cannot sleep. Case 2: Exactly one free unit, meets H=1. Case 3: Use first 4 free units, remaining requirement becomes 2, which is satisfied by the last two free units. Case 4: After using first 4 units, remaining requirement is 2 but only one free unit remains, so impossible."
      }
    ]
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "label": "minimum boundary",
        "input": "1\n1 1\n0",
        "expectedOutput": "YES",
        "weight": 2
      },
      {
        "id": "tc_2",
        "label": "all occupied",
        "input": "1\n5 5\n11111",
        "expectedOutput": "NO",
        "weight": 2
      },
      {
        "id": "tc_3",
        "label": "splitting does not help",
        "input": "1\n6 4\n001001",
        "expectedOutput": "NO",
        "weight": 2
      },
      {
        "id": "tc_4",
        "label": "sample like successful split",
        "input": "1\n7 5\n0000100",
        "expectedOutput": "YES",
        "weight": 2
      },
      {
        "id": "tc_5",
        "label": "cannot reduce requirement",
        "input": "1\n8 6\n00111100",
        "expectedOutput": "NO",
        "weight": 2
      }
    ],
    "totalTestCases": 5,
    "totalWeight": 10
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      2,
      2,
      2,
      2,
      2
    ],
    "maxScore": 10
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "Collect lengths of all maximal consecutive '0' segments. Sort these lengths in descending order. Let required = H. Iterate over the sorted lengths: if the current length >= required, Chef can finish sleeping → answer YES. Otherwise, if the length is more than half of required (i.e., length*2 > required), using this segment reduces the remaining required sleep to required = 2*(required - length). Continue with the next longest segment. If no segment can reduce the requirement and none is long enough, answer NO. This greedy choice is optimal because using the longest possible segment that actually reduces the requirement yields the smallest possible new requirement; any shorter segment would either not reduce the requirement or would lead to a larger requirement later.",
    "timeComplexity": "O(L log L) per test case due to sorting the zero‑segment lengths.",
    "spaceComplexity": "O(L) for storing segment lengths.",
    "code": {
      "C": "#include <stdio.h>\n#include <stdlib.h>\n\nint cmp_desc(const void *a, const void *b) {\n    int x = *(int*)b - *(int*)a;\n    return x;\n}\n\nint main(){\n    int T; if(scanf(\"%d\", &T)!=1) return 0;\n    while(T--){\n        int L, H; scanf(\"%d %d\", &L, &H);\n        char *S = (char*)malloc(L+5);\n        scanf(\"%s\", S);\n        int *segs = (int*)malloc(L * sizeof(int));\n        int cnt=0, segCnt=0;\n        for(int i=0;i<L;i++){\n            if(S[i]=='0') cnt++;\n            else{ if(cnt>0){ segs[segCnt++]=cnt; cnt=0; } }\n        }\n        if(cnt>0) segs[segCnt++]=cnt;\n        qsort(segs, segCnt, sizeof(int), cmp_desc);\n        int req = H;\n        int ok = 0;\n        for(int i=0;i<segCnt;i++){\n            int len = segs[i];\n            if(len >= req){ ok = 1; break; }\n            if(len*2 > req) req = 2*(req - len);\n        }\n        printf(ok?\"YES\\n\":\"NO\\n\");\n        free(S); free(segs);\n    }\n    return 0;\n}",
      "C++": "#include <bits/stdc++.h>\nusing namespace std;\nint main(){\n    ios::sync_with_stdio(false);\n    cin.tie(nullptr);\n    int T; if(!(cin>>T)) return 0;\n    while(T--){\n        int L,H; cin>>L>>H; string S; cin>>S;\n        vector<int> segs; int cnt=0;\n        for(char c: S){\n            if(c=='0') cnt++; else { if(cnt) segs.push_back(cnt), cnt=0; }\n        }\n        if(cnt) segs.push_back(cnt);\n        sort(segs.rbegin(), segs.rend());\n        int req=H; bool ok=false;\n        for(int len: segs){\n            if(len>=req){ ok=true; break; }\n            if(len*2>req) req = 2*(req - len);\n        }\n        cout<<(ok?\"YES\":\"NO\")<<\"\\n\";\n    }\n    return 0;\n}",
      "Java": "import java.io.*;\nimport java.util.*;\npublic class Main {\n    public static void main(String[] args) throws Exception {\n        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));\n        StringBuilder out = new StringBuilder();\n        int T = Integer.parseInt(br.readLine().trim());\n        while (T-- > 0) {\n            StringTokenizer st = new StringTokenizer(br.readLine());\n            int L = Integer.parseInt(st.nextToken());\n            int H = Integer.parseInt(st.nextToken());\n            String S = br.readLine().trim();\n            List<Integer> segs = new ArrayList<>();\n            int cnt = 0;\n            for (int i = 0; i < L; i++) {\n                if (S.charAt(i) == '0') cnt++; else { if (cnt > 0) { segs.add(cnt); cnt = 0; } }\n            }\n            if (cnt > 0) segs.add(cnt);\n            segs.sort(Collections.reverseOrder());\n            int req = H; boolean ok = false;\n            for (int len : segs) {\n                if (len >= req) { ok = true; break; }\n                if (len * 2 > req) req = 2 * (req - len);\n            }\n            out.append(ok ? \"YES\" : \"NO\").append('\\n');\n        }\n        System.out.print(out.toString());\n    }\n}",
      "Python3": "#!/usr/bin/env python3\nimport sys\n\ndef can_sleep(H, s):\n    segs = []\n    cnt = 0\n    for ch in s:\n        if ch == '0':\n            cnt += 1\n        else:\n            if cnt:\n                segs.append(cnt)\n                cnt = 0\n    if cnt:\n        segs.append(cnt)\n    segs.sort(reverse=True)\n    req = H\n    for length in segs:\n        if length >= req:\n            return True\n        if length * 2 > req:\n            req = 2 * (req - length)\n    return False\n\ndef main():\n    data = sys.stdin.read().strip().split()\n    if not data:\n        return\n    it = iter(data)\n    T = int(next(it))\n    out_lines = []\n    for _ in range(T):\n        L = int(next(it)); H = int(next(it))\n        S = next(it)\n        out_lines.append('YES' if can_sleep(H, S) else 'NO')\n    sys.stdout.write('\\n'.join(out_lines))\n\nif __name__ == \"__main__\":\n    main()\n",
      "JavaScript": "const fs = require('fs');\nconst input = fs.readFileSync(0, 'utf-8').trim().split(/\\s+/);\nlet idx = 0;\nconst T = Number(input[idx++]);\nlet out = [];\nfor (let t = 0; t < T; t++) {\n  const L = Number(input[idx++]);\n  let H = Number(input[idx++]);\n  const S = input[idx++];\n  let segs = [];\n  let cnt = 0;\n  for (let i = 0; i < L; i++) {\n    if (S[i] === '0') cnt++; else { if (cnt) { segs.push(cnt); cnt = 0; } }\n  }\n  if (cnt) segs.push(cnt);\n  segs.sort((a, b) => b - a);\n  let req = H;\n  let ok = false;\n  for (let len of segs) {\n    if (len >= req) { ok = true; break; }\n    if (len * 2 > req) req = 2 * (req - len);\n  }\n  out.push(ok ? 'YES' : 'NO');\n}\nconsole.log(out.join('\\n'));\n"
    }
  },
  "QBCategory": "CCD",
  "boilerPlates": {
    "C": "#include <stdio.h>\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "C++": "#include <iostream>\nusing namespace std;\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "Java": "import java.util.*;\nimport java.io.*;\n\npublic class Main {\n    public static void main(String[] args) throws IOException {\n        // your code goes here\n    }\n}",
    "Python3": "# your code goes here\n",
    "JavaScript": "// your code goes here\n"
  },
  "_internal": {
    "methodName": "",
    "params": []
  }
}