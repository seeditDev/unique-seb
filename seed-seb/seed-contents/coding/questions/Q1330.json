{
  "questionId": "Q1330",
  "title": "Good subarrays",
  "slug": "good-subarrays",
  "version": 1,
  "createdAt": "2026-07-01",
  "updatedAt": "2026-07-05",
  "metadata": {
    "category": "Arrays",
    "difficulty": "Medium",
    "isPremium": false,
    "tags": [
      "Subarray Sum",
      "Hash Map",
      "Prefix Sum",
      "Arrays"
    ],
    "companyTags": [],
    "topicTags": [
      "Prefix Sums & Difference Arrays"
    ]
  },
  "content": {
    "problemStatement": "You are given an array A1A_1A1\u200b, A2A_2A2\u200b, . . ., ANA_NAN\u200b consisting of NNN integers.\n\nA subarray AlA_lAl\u200b, Al+1A_{l+1}Al+1\u200b, Al+2A_{l+2}Al+2\u200b, . . ., Ar\u22121A_{r\u22121}Ar\u22121\u200b, ArA_rAr\u200b is good if the sum of elements of this subarray is equal to an integer KKK.\n\nFind the number of good subarrays present in the array.\n\n\nInput Format\n\nThe first line of input will contain two integers NNN and KKK, NNN denoting the length of the array and KKK denoting the sum for a good subarray.\nThe second line of input will contain NNN integers A1A_1A1\u200b, A2A_2A2\u200b, . . . ANA_NAN\u200b.",
    "constraints": [
      "1 <= T <= 10",
      "1 <= N <= 200000",
      "0 <= K <= 10^14",
      "0 <= Ai <= 10^9"
    ],
    "inputFormat": "First line contains an integer T, the number of test cases. Each test case consists of two lines: the first line contains two integers N and K \u2014 the length of the array and the required sum. The second line contains N space\u2011separated integers A1, A2, \u2026, AN.",
    "outputFormat": "For each test case output a single line containing the number of subarrays whose elements sum exactly to K.",
    "sampleTestCases": [
      {
        "id": "sample_1",
        "input": "1\n3 1\n1 1 3",
        "output": "2",
        "explanation": "Subarrays with sum 1 are the single elements A1 and A2, giving a total of 2 good subarrays."
      },
      {
        "id": "sample_2",
        "input": "1\n5 5\n1 2 3 2 1",
        "output": "2",
        "explanation": "The subarrays that sum to 5 are [A2, A3] (2+3) and [A3, A4] (3+2)."
      },
      {
        "id": "sample_3",
        "input": "1\n4 10\n5 5 5 5",
        "output": "3",
        "explanation": "Each pair of consecutive 5s gives sum 10: (A1,A2), (A2,A3) and (A3,A4)."
      }
    ]
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "label": "minimum boundary",
        "input": "1\n1 5\n5",
        "expectedOutput": "1",
        "weight": 2
      },
      {
        "id": "tc_2",
        "label": "maximum boundary",
        "input": "1\n15 5000\n1000 1000 1000 1000 1000 1000 1000 1000 1000 1000 1000 1000 1000 1000 1000",
        "expectedOutput": "11",
        "weight": 2
      },
      {
        "id": "tc_3",
        "label": "uniform/edge case",
        "input": "1\n4 4\n2 2 2 2",
        "expectedOutput": "3",
        "weight": 2
      },
      {
        "id": "tc_4",
        "label": "tricky case",
        "input": "1\n3 200000\n100000 100000 100000",
        "expectedOutput": "2",
        "weight": 2
      },
      {
        "id": "tc_5",
        "label": "stress/general",
        "input": "3\n3 3\n1 2 3\n5 5\n1 1 1 1 1\n6 7\n3 4 3 4 3 4",
        "expectedOutput": "2\n1\n5",
        "weight": 2
      }
    ],
    "totalTestCases": 5,
    "totalWeight": 10
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      2,
      2,
      2,
      2,
      2
    ],
    "maxScore": 10
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "For each test case we compute prefix sums. A subarray [l, r] has sum K iff prefix[r] - prefix[l-1] = K. While scanning the array we keep a hash map that stores how many times each prefix sum has appeared so far. For the current prefix sum P we add to the answer the count of (P - K) already seen. Then we increment the count of P in the map. This runs in O(N) time and O(N) extra space per test case.",
    "timeComplexity": "O(N) per test case, O(total N) for the whole input.",
    "spaceComplexity": "O(N) auxiliary space for the hash map of prefix sums.",
    "code": {
      "C": "#include <stdio.h>\n#include <stdlib.h>\n#include <stdint.h>\n\n/* Simple open\u2011addressing hash table for 64\u2011bit keys */\ntypedef struct {\n    int64_t key;\n    int64_t cnt;\n    char used;\n} Entry;\n\nstatic uint64_t hash_uint64(uint64_t x) {\n    const uint64_t MULT = 11400714819323198485ULL;\n    return x * MULT;\n}\n\nstatic void ht_insert(Entry *table, size_t size, int64_t key) {\n    uint64_t h = hash_uint64((uint64_t)key);\n    size_t idx = (size_t)(h & (size - 1));\n    while (table[idx].used && table[idx].key != key) {\n        idx = (idx + 1) & (size - 1);\n    }\n    if (!table[idx].used) {\n        table[idx].used = 1;\n        table[idx].key = key;\n        table[idx].cnt = 1;\n    } else {\n        table[idx].cnt++;\n    }\n}\n\nstatic int64_t ht_get(const Entry *table, size_t size, int64_t key) {\n    uint64_t h = hash_uint64((uint64_t)key);\n    size_t idx = (size_t)(h & (size - 1));\n    while (table[idx].used && table[idx].key != key) {\n        idx = (idx + 1) & (size - 1);\n    }\n    if (!table[idx].used) return 0;\n    return table[idx].cnt;\n}\n\nint main(void) {\n    int T;\n    if (scanf(\"%d\", &T) != 1) return 0;\n    while (T--) {\n        int N;\n        long long K;\n        scanf(\"%d %lld\", &N, &K);\n        long long *a = (long long *)malloc(N * sizeof(long long));\n        for (int i = 0; i < N; ++i) scanf(\"%lld\", &a[i]);\n\n        /* hash table size: next power of two >= 2*(N+1) */\n        size_t size = 1;\n        while (size < 2 * (size_t)(N + 1)) size <<= 1;\n        Entry *table = (Entry *)calloc(size, sizeof(Entry));\n\n        ht_insert(table, size, 0); // prefix sum 0 occurs once\n        long long pref = 0;\n        long long ans = 0;\n        for (int i = 0; i < N; ++i) {\n            pref += a[i];\n            ans += ht_get(table, size, pref - K);\n            ht_insert(table, size, pref);\n        }\n        printf(\"%lld\\n\", ans);\n        free(a);\n        free(table);\n    }\n    return 0;\n}",
      "C++": "#include <bits/stdc++.h>\nusing namespace std;\nint main() {\n    ios::sync_with_stdio(false);\n    cin.tie(nullptr);\n    int T;\n    if(!(cin >> T)) return 0;\n    while (T--) {\n        int N; long long K;\n        cin >> N >> K;\n        vector<long long> a(N);\n        for (auto &x : a) cin >> x;\n        unordered_map<long long,long long> freq;\n        freq.reserve(N * 2);\n        freq[0] = 1;\n        long long pref = 0, ans = 0;\n        for (long long x : a) {\n            pref += x;\n            ans += freq[pref - K];\n            ++freq[pref];\n        }\n        cout << ans << '\\n';\n    }\n    return 0;\n}",
      "Java": "import java.io.*;\nimport java.util.*;\npublic class Main {\n    public static void main(String[] args) throws Exception {\n        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));\n        StringTokenizer st = new StringTokenizer(br.readLine());\n        int T = Integer.parseInt(st.nextToken());\n        StringBuilder sb = new StringBuilder();\n        while (T-- > 0) {\n            st = new StringTokenizer(br.readLine());\n            int N = Integer.parseInt(st.nextToken());\n            long K = Long.parseLong(st.nextToken());\n            long[] a = new long[N];\n            st = new StringTokenizer(br.readLine());\n            for (int i = 0; i < N; i++) a[i] = Long.parseLong(st.nextToken());\n            Map<Long, Long> map = new HashMap<>();\n            map.put(0L, 1L);\n            long pref = 0, ans = 0;\n            for (long v : a) {\n                pref += v;\n                ans += map.getOrDefault(pref - K, 0L);\n                map.put(pref, map.getOrDefault(pref, 0L) + 1);\n            }\n            sb.append(ans).append('\\n');\n        }\n        System.out.print(sb.toString());\n    }\n}",
      "Python3": "import sys\n\ndef solve():\n    data = sys.stdin.buffer.read().split()\n    if not data:\n        return\n    it = iter(data)\n    T = int(next(it))\n    out = []\n    for _ in range(T):\n        N = int(next(it))\n        K = int(next(it))\n        pref = 0\n        cnt = {0: 1}\n        ans = 0\n        for _ in range(N):\n            pref += int(next(it))\n            ans += cnt.get(pref - K, 0)\n            cnt[pref] = cnt.get(pref, 0) + 1\n        out.append(str(ans))\n    sys.stdout.write('\\n'.join(out))\n\nif __name__ == \"__main__\":\n    solve()",
      "JavaScript": "const fs = require('fs');\nconst data = fs.readFileSync(0, 'utf-8').trim().split(/\\s+/);\nlet idx = 0;\nconst T = Number(data[idx++]);\nlet results = [];\nfor (let t = 0; t < T; ++t) {\n  const N = Number(data[idx++]);\n  const K = Number(data[idx++]);\n  const map = new Map();\n  map.set(0, 1);\n  let pref = 0;\n  let ans = 0;\n  for (let i = 0; i < N; ++i) {\n    pref += Number(data[idx++]);\n    const need = pref - K;\n    if (map.has(need)) ans += map.get(need);\n    map.set(pref, (map.get(pref) || 0) + 1);\n  }\n  results.push(String(ans));\n}\nconsole.log(results.join('\\n'));"
    }
  }
}