{
  "questionId": "Q4685",
  "title": "Identical Binary Trees",
  "slug": "identical-binary-trees",
  "version": 1,
  "createdAt": "2026-07-01",
  "updatedAt": "2026-07-04",
  "metadata": {
    "category": "Graph",
    "difficulty": "Medium",
    "isPremium": true,
    "tags": [
      "Tree",
      "Graph Traversal"
    ],
    "companyTags": [],
    "topicTags": [
      "Trees"
    ],
    "QBCategory": "CCD"
  },
  "content": {
    "problemStatement": "Given two undirected binary trees T1 and T2, check if they are the identical or not.\n\nTwo binary trees are considered identical if they are structurally same, and the nodes have the same value.\n\nFor example, the following two binary trees are identical:\n\n\n\n\nInput Format\n\nThe first line of the input contains two space separated integers NNN and MMM — the number of nodes in the two binary trees.\nNext N−1N - 1N−1 lines contain three space separated characters p1i,c1i,Rip1_i, c1_i, R_ip1i​,c1i​,Ri​, describing the first tree with edge informations - p1ip1_ip1i​ is an integer denoting the parent node of the iiith edge, c1ic1_ic1i​ is child node, and the letter RiR_iRi​ denotes whether the child is left child or right child, it's value is `L` if c1ic1_ic1i​ is left child of p1ip1_ip1i​, else `R`.\nSimilarly, next M−1M - 1M−1 lines contain three space separated characters p2i,c2i,Rip2_i, c2_i, R_ip2i​,c2i​,Ri​, describing the second tree with edge informations - p2ip2_ip2i​ is an integer denoting the parent node of the iiith edge, c2ic2_ic2i​ is child node, and the letter RiR_iRi​ denotes whether the child is left child or right child, it's value is `L` if c2ic2_ic2i​ is left child of p2ip2_ip2i​, else `R`.\n\n\nOutput Format\nOutput on the single line, `YES` if the two trees are identical else `NO` otherwise.",
    "constraints": [
      "1 <= N, M <= 10000",
      "1 <= p1i, p2i, c1i, c2i <= 100000",
      "Ri = 'L' or 'R'",
      "All p1i's and c1i's are distinct"
    ],
    "inputFormat": "First line contains two space-separated integers N and M, the number of nodes in the two binary trees. Next N-1 lines contain three space-separated characters p1i, c1i, Ri, describing the first tree with edge information - p1i is an integer denoting the parent node of the ith edge, c1i is the child node, and the letter Ri denotes whether the child is the left child or right child, its value is 'L' if c1i is the left child of p1i, else 'R'. Similarly, next M-1 lines contain three space-separated characters p2i, c2i, Ri, describing the second tree with edge information - p2i is an integer denoting the parent node of the ith edge, c2i is the child node, and the letter Ri denotes whether the child is the left child or right child, its value is 'L' if c2i is the left child of p2i, else 'R'.",
    "outputFormat": "For each test case, print 'YES' if the two trees are identical, else 'NO' otherwise.",
    "sampleTestCases": [
      {
        "id": "sample_1",
        "input": "5 5\n1 2 L\n1 3 R\n3 4 L\n3 5 R\n1 2 L\n1 3 R\n3 4 L\n3 5 R",
        "output": "YES",
        "explanation": "The two trees are identical because they have the same structure and node values."
      },
      {
        "id": "sample_2",
        "input": "3 3\n1 2 L\n1 3 R\n1 2 L\n1 3 R\n2 4 L",
        "output": "NO",
        "explanation": "The two trees are not identical because they have different structures."
      },
      {
        "id": "sample_3",
        "input": "2 2\n1 2 L\n1 2 L",
        "output": "YES",
        "explanation": "The two trees are identical because they have the same structure and node values."
      }
    ]
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "label": "minimum boundary",
        "input": "1 1\n",
        "expectedOutput": "YES",
        "weight": 2
      },
      {
        "id": "tc_2",
        "label": "maximum boundary",
        "input": "10000 10000\n1 2 L\n1 3 R\n3 4 L\n3 5 R\n1 2 L\n1 3 R\n3 4 L\n3 5 R",
        "expectedOutput": "YES",
        "weight": 2
      },
      {
        "id": "tc_3",
        "label": "uniform/edge case",
        "input": "5 5\n1 2 L\n1 3 R\n3 4 L\n3 5 R\n1 2 L\n1 3 R\n3 4 L\n3 5 R",
        "expectedOutput": "YES",
        "weight": 2
      },
      {
        "id": "tc_4",
        "label": "tricky case",
        "input": "3 3\n1 2 L\n1 3 R\n1 2 L\n1 3 R\n2 4 L",
        "expectedOutput": "NO",
        "weight": 2
      },
      {
        "id": "tc_5",
        "label": "stress/general",
        "input": "10 10\n1 2 L\n1 3 R\n3 4 L\n3 5 R\n1 2 L\n1 3 R\n3 4 L\n3 5 R\n5 6 L\n5 7 R",
        "expectedOutput": "YES",
        "weight": 2
      }
    ],
    "totalTestCases": 5,
    "totalWeight": 10
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      2,
      2,
      2,
      2,
      2
    ],
    "maxScore": 10
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "To check if two binary trees are identical, we can use a recursive approach. We start by checking if the roots of the two trees are null. If both are null, the trees are identical. If one is null and the other is not, the trees are not identical. If both roots are not null, we check if the values of the roots are equal. If they are not, the trees are not identical. If they are equal, we recursively check the left and right subtrees of the roots.",
    "timeComplexity": "O(N + M) per test case, where N and M are the number of nodes in the two binary trees.",
    "spaceComplexity": "O(N + M) for storing the nodes of the two binary trees.",
    "code": {
      "C": "#include <stdio.h>\n#include <stdlib.h>\n\n// Define the structure for a tree node\ntypedef struct Node {\n    int val;\n    struct Node* left;\n    struct Node* right;\n} Node;\n\n// Function to create a new node\nNode* createNode(int val) {\n    Node* newNode = (Node*) malloc(sizeof(Node));\n    if (!newNode) {\n        printf(\"Memory error\\n\");\n        return NULL;\n    }\n    newNode->val = val;\n    newNode->left = newNode->right = NULL;\n    return newNode;\n}\n\n// Function to check if two trees are identical\nint areIdenticalTrees(Node* root1, Node* root2) {\n    if (root1 == NULL && root2 == NULL) {\n        return 1;\n    }\n    if (root1 == NULL || root2 == NULL) {\n        return 0;\n    }\n    return (root1->val == root2->val) &&\n           areIdenticalTrees(root1->left, root2->left) &&\n           areIdenticalTrees(root1->right, root2->right);\n}\n\nint main() {\n    int N, M;\n    scanf(\"%d %d\", &N, &M);\n    Node* root1 = createNode(1);\n    Node* root2 = createNode(1);\n    Node** nodes1 = (Node**)malloc(N * sizeof(Node*));\n    Node** nodes2 = (Node**)malloc(M * sizeof(Node*));\n    nodes1[0] = root1;\n    nodes2[0] = root2;\n    // Create the first tree\n    for (int i = 0; i < N - 1; i++) {\n        int p, c;\n        char R;\n        scanf(\"%d %d %c\", &p, &c, &R);\n        Node* newNode = createNode(c);\n        nodes1[c - 1] = newNode;\n        if (R == 'L') {\n            nodes1[p - 1]->left = newNode;\n        } else {\n            nodes1[p - 1]->right = newNode;\n        }\n    }\n    // Create the second tree\n    for (int i = 0; i < M - 1; i++) {\n        int p, c;\n        char R;\n        scanf(\"%d %d %c\", &p, &c, &R);\n        Node* newNode = createNode(c);\n        nodes2[c - 1] = newNode;\n        if (R == 'L') {\n            nodes2[p - 1]->left = newNode;\n        } else {\n            nodes2[p - 1]->right = newNode;\n        }\n    }\n    if (areIdenticalTrees(root1, root2)) {\n        printf(\"YES\\n\");\n    } else {\n        printf(\"NO\\n\");\n    }\n    return 0;\n}",
      "C++": "#include <iostream>\nusing namespace std;\n\n// Define the structure for a tree node\nstruct Node {\n    int val;\n    Node* left;\n    Node* right;\n};\n\n// Function to create a new node\nNode* createNode(int val) {\n    Node* newNode = new Node();\n    newNode->val = val;\n    newNode->left = newNode->right = NULL;\n    return newNode;\n}\n\n// Function to check if two trees are identical\nbool areIdenticalTrees(Node* root1, Node* root2) {\n    if (root1 == NULL && root2 == NULL) {\n        return true;\n    }\n    if (root1 == NULL || root2 == NULL) {\n        return false;\n    }\n    return (root1->val == root2->val) &&\n           areIdenticalTrees(root1->left, root2->left) &&\n           areIdenticalTrees(root1->right, root2->right);\n}\n\nint main() {\n    int N, M;\n    cin >> N >> M;\n    Node* root1 = createNode(1);\n    Node* root2 = createNode(1);\n    Node** nodes1 = new Node*[N];\n    Node** nodes2 = new Node*[M];\n    nodes1[0] = root1;\n    nodes2[0] = root2;\n    // Create the first tree\n    for (int i = 0; i < N - 1; i++) {\n        int p, c;\n        char R;\n        cin >> p >> c >> R;\n        Node* newNode = createNode(c);\n        nodes1[c - 1] = newNode;\n        if (R == 'L') {\n            nodes1[p - 1]->left = newNode;\n        } else {\n            nodes1[p - 1]->right = newNode;\n        }\n    }\n    // Create the second tree\n    for (int i = 0; i < M - 1; i++) {\n        int p, c;\n        char R;\n        cin >> p >> c >> R;\n        Node* newNode = createNode(c);\n        nodes2[c - 1] = newNode;\n        if (R == 'L') {\n            nodes2[p - 1]->left = newNode;\n        } else {\n            nodes2[p - 1]->right = newNode;\n        }\n    }\n    if (areIdenticalTrees(root1, root2)) {\n        cout << \"YES\";\n    } else {\n        cout << \"NO\";\n    }\n    return 0;\n}",
      "Java": "import java.util.Scanner;\n\n// Define the class for a tree node\nclass Node {\n    int val;\n    Node left;\n    Node right;\n    Node(int val) {\n        this.val = val;\n        left = right = null;\n    }\n}\n\npublic class Main {\n    // Function to check if two trees are identical\n    public static boolean areIdenticalTrees(Node root1, Node root2) {\n        if (root1 == null && root2 == null) {\n            return true;\n        }\n        if (root1 == null || root2 == null) {\n            return false;\n        }\n        return (root1.val == root2.val) &&\n               areIdenticalTrees(root1.left, root2.left) &&\n               areIdenticalTrees(root1.right, root2.right);\n    }\n\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int N = sc.nextInt();\n        int M = sc.nextInt();\n        Node root1 = new Node(1);\n        Node root2 = new Node(1);\n        Node[] nodes1 = new Node[N];\n        Node[] nodes2 = new Node[M];\n        nodes1[0] = root1;\n        nodes2[0] = root2;\n        // Create the first tree\n        for (int i = 0; i < N - 1; i++) {\n            int p = sc.nextInt();\n            int c = sc.nextInt();\n            char R = sc.next().charAt(0);\n            Node newNode = new Node(c);\n            nodes1[c - 1] = newNode;\n            if (R == 'L') {\n                nodes1[p - 1].left = newNode;\n            } else {\n                nodes1[p - 1].right = newNode;\n            }\n        }\n        // Create the second tree\n        for (int i = 0; i < M - 1; i++) {\n            int p = sc.nextInt();\n            int c = sc.nextInt();\n            char R = sc.next().charAt(0);\n            Node newNode = new Node(c);\n            nodes2[c - 1] = newNode;\n            if (R == 'L') {\n                nodes2[p - 1].left = newNode;\n            } else {\n                nodes2[p - 1].right = newNode;\n            }\n        }\n        if (areIdenticalTrees(root1, root2)) {\n            System.out.println(\"YES\");\n        } else {\n            System.out.println(\"NO\");\n        }\n    }\n}",
      "Python3": "class Node:\n    def __init__(self, val):\n        self.val = val\n        self.left = None\n        self.right = None\n\n# Function to check if two trees are identical\ndef are_identical_trees(root1, root2):\n    if root1 is None and root2 is None:\n        return True\n    if root1 is None or root2 is None:\n        return False\n    return (root1.val == root2.val) and           are_identical_trees(root1.left, root2.left) and           are_identical_trees(root1.right, root2.right)\n\n# Create the first tree\nN = int(input().split()[0])\nM = int(input().split()[1])\nroot1 = Node(1)\nroot2 = Node(1)\n# Create the first tree\nnodes1 = [None] * N\nnodes2 = [None] * M\nnodes1[0] = root1\nnodes2[0] = root2\nfor i in range(N - 1):\n    p, c, R = input().split()\n    p = int(p)\n    c = int(c)\n    newNode = Node(c)\n    nodes1[c - 1] = newNode\n    if R == 'L':\n        nodes1[p - 1].left = newNode\n    else:\n        nodes1[p - 1].right = newNode\n# Create the second tree\nfor i in range(M - 1):\n    p, c, R = input().split()\n    p = int(p)\n    c = int(c)\n    newNode = Node(c)\n    nodes2[c - 1] = newNode\n    if R == 'L':\n        nodes2[p - 1].left = newNode\n    else:\n        nodes2[p - 1].right = newNode\nif are_identical_trees(root1, root2):\n    print(\"YES\")\nelse:\n    print(\"NO\")",
      "JavaScript": "const fs = require('fs');\nconst lines = fs.readFileSync(0, 'utf-8').trim().split('\\n');\nlet idx = 0;\n\n// Define the class for a tree node\nclass Node {\n    constructor(val) {\n        this.val = val;\n        this.left = null;\n        this.right = null;\n    }\n}\n\n// Function to check if two trees are identical\nfunction areIdenticalTrees(root1, root2) {\n    if (root1 === null && root2 === null) {\n        return true;\n    }\n    if (root1 === null || root2 === null) {\n        return false;\n    }\n    return (root1.val === root2.val) &&\n           areIdenticalTrees(root1.left, root2.left) &&\n           areIdenticalTrees(root1.right, root2.right);\n}\n\n// Create the first tree\nlet N = parseInt(lines[idx++].split(' ')[0]);\nlet M = parseInt(lines[idx++].split(' ')[1]);\nlet root1 = new Node(1);\nlet root2 = new Node(1);\nlet nodes1 = new Array(N);\nlet nodes2 = new Array(M);\nnodes1[0] = root1;\nnodes2[0] = root2;\n// Create the first tree\nfor (let i = 0; i < N - 1; i++) {\n    let [p, c, R] = lines[idx++].split(' ');\n    p = parseInt(p);\n    c = parseInt(c);\n    let newNode = new Node(c);\n    nodes1[c - 1] = newNode;\n    if (R === 'L') {\n        nodes1[p - 1].left = newNode;\n    } else {\n        nodes1[p - 1].right = newNode;\n    }\n}\n// Create the second tree\nfor (let i = 0; i < M - 1; i++) {\n    let [p, c, R] = lines[idx++].split(' ');\n    p = parseInt(p);\n    c = parseInt(c);\n    let newNode = new Node(c);\n    nodes2[c - 1] = newNode;\n    if (R === 'L') {\n        nodes2[p - 1].left = newNode;\n    } else {\n        nodes2[p - 1].right = newNode;\n    }\n}\nif (areIdenticalTrees(root1, root2)) {\n    console.log('YES');\n} else {\n    console.log('NO');\n}"
    }
  },
  "QBCategory": "CCD",
  "boilerPlates": {
    "C": "#include <stdio.h>\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "C++": "#include <iostream>\nusing namespace std;\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "Java": "import java.util.*;\nimport java.io.*;\n\npublic class Main {\n    public static void main(String[] args) throws IOException {\n        // your code goes here\n    }\n}",
    "Python3": "# your code goes here\n",
    "JavaScript": "// your code goes here\n"
  },
  "_internal": {
    "methodName": "",
    "params": []
  }
}