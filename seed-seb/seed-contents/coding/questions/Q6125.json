{
  "questionId": "Q6125",
  "title": "Bored of Nim",
  "slug": "bored-of-nim",
  "version": 1,
  "createdAt": "2026-07-01",
  "updatedAt": "2026-07-04",
  "metadata": {
    "category": "Arrays | Bit Manipulation | Game Theory",
    "difficulty": "Hard",
    "isPremium": true,
    "tags": [
      "XOR",
      "Prefix XOR",
      "Sliding Window",
      "Brute Force Optimization"
    ],
    "companyTags": [],
    "topicTags": [
      "Divide & Conquer"
    ],
    "QBCategory": "CCD"
  },
  "content": {
    "problemStatement": "Rahul and Rashi are bored of playing the game of  Nim all day. More so, Rahul has been losing all the games. Actually, the array of stone piles for a game is always selected by Rashi from a huge **N**-length array **A**. This selected array is always a subarray of **A**.\n\nRahul, now frustrated by his losing streak, insists that he would play the next game only if she chooses a game array of length **m**. \n\nCan you find the number of such subarrays that Rashi can choose so that Rahul still loses? Moreover, since the value of **m** is decided by Rahul, can you do this for all possible values of **m**?\n\nPlease note that Rahul is always the first player.\n\nInput:The first line contains an integer **T**, the number of test cases to follow.\nIn each of the test cases, the first line contains a single integer **N**, and the next line contains **N** integers, representing the array **A**.\n\n\nOutput:Output the results of each test case on a new line. For each test case, output the results for all values of **m**, that is, **m** = 1, **m** = 2 ... **m** = **N**, separated by a single space.",
    "constraints": [
      "1 ≤ T ≤ 10",
      "1 ≤ N ≤ 1000",
      "0 ≤ A[i] ≤ 10^9"
    ],
    "inputFormat": "The first line contains an integer T, the number of test cases.\nFor each test case:\n- A line with integer N, the length of the array.\n- A line with N integers A[1] … A[N] (0 ≤ A[i] ≤ 10^9).",
    "outputFormat": "For each test case output N integers on one line. The i‑th integer (1‑based) is the number of sub‑arrays of length i whose XOR is zero. Separate the numbers by a single space.",
    "sampleTestCases": [
      {
        "id": "sample_1",
        "input": "2\n6\n1 2 3 2 1 3\n4\n1 1 1 1",
        "output": "0 0 3 0 0 1\n0 3 0 1",
        "explanation": "For the first test case, sub‑arrays of length 3 with XOR zero are [1,2,3], [3,2,1] and [2,1,3]; no other length yields a zero XOR. For the second test case, only length 2 sub‑arrays have XOR zero (three of them) and length 4 has one (the whole array)."
      }
    ]
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "label": "Case 1",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_2",
        "label": "Case 2",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_3",
        "label": "Case 3",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_4",
        "label": "Case 4",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_5",
        "label": "Case 5",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      }
    ],
    "totalTestCases": 5,
    "totalWeight": 10
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      2,
      2,
      2,
      2,
      2
    ],
    "maxScore": 10
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "For a sub‑array A[l … r] (1‑based) the XOR is pref[r] ^ pref[l‑1] where pref[i] is the XOR of the first i elements (pref[0] = 0). The sub‑array has XOR zero iff pref[l‑1] == pref[r].\nFor a fixed length m we need to count indices i (0‑based) such that pref[i] == pref[i+m].\nWe compute the prefix XOR array once (O(N)). Then for each possible length m from 1 to N we scan i = 0 … N‑m and compare the two prefix values, incrementing the answer for that m when they are equal. This yields an O(N^2) algorithm, which is fast enough for the given constraint N ≤ 1000.\nTime Complexity: O(N^2) per test case.\nSpace Complexity: O(N) for the prefix array and the answer array.",
    "timeComplexity": "O(N^2) per test case",
    "spaceComplexity": "O(N)",
    "code": {
      "C": "#include <stdio.h>\n#include <stdlib.h>\n\nint main() {\n    int T; scanf(\"%d\", &T);\n    while (T--) {\n        int N; scanf(\"%d\", &N);\n        long long *A = (long long*)malloc(N * sizeof(long long));\n        for (int i = 0; i < N; ++i) scanf(\"%lld\", &A[i]);\n        long long *pref = (long long*)malloc((N + 1) * sizeof(long long));\n        pref[0] = 0;\n        for (int i = 1; i <= N; ++i) pref[i] = pref[i-1] ^ A[i-1];\n        long long *ans = (long long*)calloc(N, sizeof(long long));\n        for (int m = 1; m <= N; ++m) {\n            long long cnt = 0;\n            for (int i = 0; i + m <= N; ++i) {\n                if (pref[i] == pref[i + m]) ++cnt;\n            }\n            ans[m-1] = cnt;\n        }\n        for (int i = 0; i < N; ++i) {\n            printf(\"%lld\", ans[i]);\n            if (i + 1 < N) printf(\" \");\n        }\n        printf(\"\\n\");\n        free(A); free(pref); free(ans);\n    }\n    return 0;\n}",
      "C++": "#include <bits/stdc++.h>\nusing namespace std;\nint main(){\n    ios::sync_with_stdio(false);\n    cin.tie(nullptr);\n    int T; if(!(cin>>T)) return 0;\n    while(T--){\n        int N; cin>>N;\n        vector<long long> A(N);\n        for(int i=0;i<N;++i) cin>>A[i];\n        vector<long long> pref(N+1,0);\n        for(int i=1;i<=N;++i) pref[i]=pref[i-1]^A[i-1];\n        vector<long long> ans(N,0);\n        for(int m=1;m<=N;++m){\n            long long cnt=0;\n            for(int i=0;i+m<=N;++i){\n                if(pref[i]==pref[i+m]) ++cnt;\n            }\n            ans[m-1]=cnt;\n        }\n        for(int i=0;i<N;++i){\n            if(i) cout<<' ';\n            cout<<ans[i];\n        }\n        cout<<'\\n';\n    }\n    return 0;\n}",
      "Java": "import java.io.*;\nimport java.util.*;\n\npublic class Main {\n    public static void main(String[] args) throws Exception {\n        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));\n        StringTokenizer st;\n        int T = Integer.parseInt(br.readLine().trim());\n        StringBuilder out = new StringBuilder();\n        while (T-- > 0) {\n            int N = Integer.parseInt(br.readLine().trim());\n            long[] A = new long[N];\n            st = new StringTokenizer(br.readLine());\n            for (int i = 0; i < N; i++) A[i] = Long.parseLong(st.nextToken());\n            long[] pref = new long[N + 1];\n            pref[0] = 0;\n            for (int i = 1; i <= N; i++) pref[i] = pref[i - 1] ^ A[i - 1];\n            long[] ans = new long[N];\n            for (int m = 1; m <= N; m++) {\n                long cnt = 0;\n                for (int i = 0; i + m <= N; i++) {\n                    if (pref[i] == pref[i + m]) cnt++;\n                }\n                ans[m - 1] = cnt;\n            }\n            for (int i = 0; i < N; i++) {\n                if (i > 0) out.append(' ');\n                out.append(ans[i]);\n            }\n            out.append('\\n');\n        }\n        System.out.print(out.toString());\n    }\n}",
      "Python3": "import sys\n\ndef solve():\n    data = sys.stdin.read().strip().split()\n    if not data:\n        return\n    it = iter(data)\n    T = int(next(it))\n    out_lines = []\n    for _ in range(T):\n        N = int(next(it))\n        A = [int(next(it)) for _ in range(N)]\n        pref = [0]\n        for x in A:\n            pref.append(pref[-1] ^ x)\n        ans = []\n        for m in range(1, N + 1):\n            cnt = 0\n            for i in range(N - m + 1):\n                if pref[i] == pref[i + m]:\n                    cnt += 1\n            ans.append(str(cnt))\n        out_lines.append(' '.join(ans))\n    sys.stdout.write('\\n'.join(out_lines))\n\nif __name__ == \"__main__\":\n    solve()",
      "JavaScript": "const fs = require('fs');\nconst input = fs.readFileSync(0, 'utf-8').trim().split(/\\s+/);\nlet idx = 0;\nconst T = Number(input[idx++]);\nlet outputs = [];\nfor (let t = 0; t < T; ++t) {\n  const N = Number(input[idx++]);\n  const A = new Array(N);\n  for (let i = 0; i < N; ++i) A[i] = Number(input[idx++]);\n  const pref = new Array(N + 1);\n  pref[0] = 0;\n  for (let i = 1; i <= N; ++i) pref[i] = pref[i - 1] ^ A[i - 1];\n  const ans = new Array(N);\n  for (let m = 1; m <= N; ++m) {\n    let cnt = 0;\n    for (let i = 0; i + m <= N; ++i) {\n      if (pref[i] === pref[i + m]) cnt++;\n    }\n    ans[m - 1] = cnt;\n  }\n  outputs.push(ans.join(' '));\n}\nconsole.log(outputs.join('\\n'));"
    }
  },
  "QBCategory": "CCD",
  "boilerPlates": {
    "C": "#include <stdio.h>\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "C++": "#include <iostream>\nusing namespace std;\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "Java": "import java.util.*;\nimport java.io.*;\n\npublic class Main {\n    public static void main(String[] args) throws IOException {\n        // your code goes here\n    }\n}",
    "Python3": "# your code goes here\n",
    "JavaScript": "// your code goes here\n"
  },
  "_internal": {
    "methodName": "",
    "params": []
  }
}