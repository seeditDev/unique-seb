{
  "questionId": "Q8714",
  "title": "Remove Duplicates",
  "slug": "remove-duplicates",
  "version": 1,
  "createdAt": "2026-07-02",
  "updatedAt": "2026-07-04",
  "metadata": {
    "category": "Data Structures",
    "difficulty": "Easy",
    "isPremium": true,
    "tags": [
      "Linked List",
      "Two Pointers",
      "In‑place Modification"
    ],
    "companyTags": [],
    "topicTags": [
      "Linked Lists"
    ],
    "QBCategory": "CCD"
  },
  "content": {
    "problemStatement": "Chef gave you the headheadhead of a sorted linked list which may contain duplicate values. You've to remove all the duplicate values and return the sorted linked list such that it does not contain any duplicates.\n\n\nInput Format\n\nFirst-line will contain TTT, the number of test cases. Then the test cases follow.\nEach test case contains two lines of input.\nThe first line of every test case contains an integer NNN - the length of array.\nThe second line of every test case contains NNN integers - A1,A2,..,AN A_1,A_2,..,A_NA1​,A2​,..,AN​ denoting the integers in the array.\nIt is guaranteed that the array is sorted\nYou don't need to read or print anything. Just complete the function removeDuplicates() which takes the head of the linked list as input.\n\n\nOutput Format\nReturn the head of the sorted linked list without duplicates.",
    "constraints": [
      "1 <= T <= 1000",
      "1 <= N <= 10^5",
      "1 <= Ai <= 10^9",
      "Sum of N over all test cases does not exceed 5·10^5"
    ],
    "inputFormat": "The first line contains an integer T, the number of test cases. For each test case, the first line contains an integer N, the length of the linked list. The second line contains N space‑separated integers A1 … AN in non‑decreasing order.",
    "outputFormat": "For each test case output the values of the linked list after removing all duplicate nodes. Print the values in a single line separated by a single space. Print a newline after each test case.",
    "sampleTestCases": [
      {
        "id": "sample_1",
        "input": "4\n4\n6 15 22 22\n5\n13 17 17 17 20\n4\n5 6 6 6\n2\n9 9",
        "output": "6 15 22\n13 17 20\n5 6\n9",
        "explanation": "Each test case is a sorted list. Duplicates are removed by keeping only the first occurrence of each value. For example, the first list [6,15,22,22] becomes [6,15,22]."
      },
      {
        "id": "sample_2",
        "input": "1\n6\n1 1 2 3 3 3",
        "output": "1 2 3",
        "explanation": "The list contains duplicates of 1 and 3. After removal only distinct values remain."
      },
      {
        "id": "sample_3",
        "input": "1\n1\n42",
        "output": "42",
        "explanation": "A single‑element list has no duplicates, so it is printed unchanged."
      }
    ]
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "label": "minimum boundary",
        "input": "1\n1\n5",
        "expectedOutput": "5",
        "weight": 2
      },
      {
        "id": "tc_2",
        "label": "maximum boundary",
        "input": "1\n5\n1000000000 1000000000 1000000000 1000000000 1000000000",
        "expectedOutput": "1000000000",
        "weight": 2
      },
      {
        "id": "tc_3",
        "label": "edge case – all distinct",
        "input": "1\n5\n2 4 6 8 10",
        "expectedOutput": "2 4 6 8 10",
        "weight": 2
      },
      {
        "id": "tc_4",
        "label": "general case 1",
        "input": "1\n8\n1 1 2 2 3 4 4 5",
        "expectedOutput": "1 2 3 4 5",
        "weight": 2
      },
      {
        "id": "tc_5",
        "label": "general case 2",
        "input": "1\n7\n7 7 7 8 9 9 10",
        "expectedOutput": "7 8 9 10",
        "weight": 2
      }
    ],
    "totalTestCases": 5,
    "totalWeight": 10
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      2,
      2,
      2,
      2,
      2
    ],
    "maxScore": 10
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "Because the list is already sorted, duplicate values appear consecutively. Traverse the list with a single pointer. For each node, compare its value with the next node's value. If they are equal, bypass the next node (current->next = current->next->next) and delete it (or let the garbage collector free it). Otherwise move the pointer forward. This runs in O(N) time and O(1) extra space per test case.",
    "timeComplexity": "O(N) per test case, where N is the length of the list. Overall O(∑N).",
    "spaceComplexity": "O(1) auxiliary space (the list itself is reused).",
    "code": {
      "C": "#include <stdio.h>\n#include <stdlib.h>\n\ntypedef struct Node {\n    int val;\n    struct Node *next;\n} Node;\n\nNode* build(int n) {\n    Node *head = NULL, *tail = NULL;\n    for (int i = 0; i < n; ++i) {\n        int x; scanf(\"%d\", &x);\n        Node *tmp = (Node*)malloc(sizeof(Node));\n        tmp->val = x; tmp->next = NULL;\n        if (!head) head = tail = tmp; else { tail->next = tmp; tail = tmp; }\n    }\n    return head;\n}\n\nNode* removeDuplicates(Node* head) {\n    Node *cur = head;\n    while (cur && cur->next) {\n        if (cur->val == cur->next->val) {\n            Node *dup = cur->next;\n            cur->next = dup->next;\n            free(dup);\n        } else {\n            cur = cur->next;\n        }\n    }\n    return head;\n}\n\nvoid printList(Node* head) {\n    for (Node* p = head; p; p = p->next) {\n        printf(\"%d\", p->val);\n        if (p->next) printf(\" \");\n    }\n    printf(\"\\n\");\n}\n\nvoid freeList(Node* head) {\n    while (head) { Node* tmp = head; head = head->next; free(tmp); }\n}\n\nint main() {\n    int T; if (scanf(\"%d\", &T)!=1) return 0;\n    while (T--) {\n        int N; scanf(\"%d\", &N);\n        Node* head = build(N);\n        head = removeDuplicates(head);\n        printList(head);\n        freeList(head);\n    }\n    return 0;\n}",
      "C++": "#include <bits/stdc++.h>\nusing namespace std;\n\nstruct Node {\n    long long val;\n    Node *next;\n    Node(long long v): val(v), next(nullptr) {}\n};\n\nNode* build(int n) {\n    Node *head = nullptr, *tail = nullptr;\n    for (int i = 0; i < n; ++i) {\n        long long x; cin >> x;\n        Node *tmp = new Node(x);\n        if (!head) head = tail = tmp; else { tail->next = tmp; tail = tmp; }\n    }\n    return head;\n}\n\nNode* removeDuplicates(Node* head) {\n    Node *cur = head;\n    while (cur && cur->next) {\n        if (cur->val == cur->next->val) {\n            Node *dup = cur->next;\n            cur->next = dup->next;\n            delete dup;\n        } else {\n            cur = cur->next;\n        }\n    }\n    return head;\n}\n\nvoid printList(Node* head) {\n    for (Node* p = head; p; p = p->next) {\n        cout << p->val;\n        if (p->next) cout << ' ';\n    }\n    cout << '\\n';\n}\n\nvoid freeList(Node* head) {\n    while (head) { Node* tmp = head; head = head->next; delete tmp; }\n}\n\nint main() {\n    ios::sync_with_stdio(false);\n    cin.tie(nullptr);\n    int T; if(!(cin >> T)) return 0;\n    while (T--) {\n        int N; cin >> N;\n        Node* head = build(N);\n        head = removeDuplicates(head);\n        printList(head);\n        freeList(head);\n    }\n    return 0;\n}",
      "Java": "import java.io.*;\nimport java.util.*;\n\nclass Main {\n    static class Node {\n        long val;\n        Node next;\n        Node(long v) { val = v; }\n    }\n\n    private static Node build(int n, FastScanner fs) throws IOException {\n        Node head = null, tail = null;\n        for (int i = 0; i < n; i++) {\n            long x = fs.nextLong();\n            Node tmp = new Node(x);\n            if (head == null) head = tail = tmp; else { tail.next = tmp; tail = tmp; }\n        }\n        return head;\n    }\n\n    private static Node removeDuplicates(Node head) {\n        Node cur = head;\n        while (cur != null && cur.next != null) {\n            if (cur.val == cur.next.val) {\n                cur.next = cur.next.next;\n            } else {\n                cur = cur.next;\n            }\n        }\n        return head;\n    }\n\n    private static void print(Node head, StringBuilder sb) {\n        for (Node p = head; p != null; p = p.next) {\n            sb.append(p.val);\n            if (p.next != null) sb.append(' ');\n        }\n        sb.append('\\n');\n    }\n\n    public static void main(String[] args) throws Exception {\n        FastScanner fs = new FastScanner(System.in);\n        StringBuilder out = new StringBuilder();\n        int T = fs.nextInt();\n        while (T-- > 0) {\n            int N = fs.nextInt();\n            Node head = build(N, fs);\n            head = removeDuplicates(head);\n            print(head, out);\n        }\n        System.out.print(out.toString());\n    }\n\n    // Fast scanner for speed\n    static class FastScanner {\n        private final BufferedInputStream in;\n        private final byte[] buffer = new byte[1 << 16];\n        private int ptr = 0, len = 0;\n        FastScanner(InputStream is) { in = new BufferedInputStream(is); }\n        private int readByte() throws IOException {\n            if (ptr >= len) {\n                len = in.read(buffer);\n                ptr = 0;\n                if (len <= 0) return -1;\n            }\n            return buffer[ptr++];\n        }\n        long nextLong() throws IOException { return Long.parseLong(next()); }\n        int nextInt() throws IOException { return Integer.parseInt(next()); }\n        String next() throws IOException {\n            StringBuilder sb = new StringBuilder();\n            int c;\n            while ((c = readByte()) != -1 && c <= ' ') {}\n            while (c != -1 && c > ' ') {\n                sb.append((char) c);\n                c = readByte();\n            }\n            return sb.toString();\n        }\n    }\n}",
      "Python3": "#!/usr/bin/env python3\nimport sys\n\ndef removeDuplicates(head):\n    cur = head\n    while cur and cur.next:\n        if cur.val == cur.next.val:\n            cur.next = cur.next.next\n        else:\n            cur = cur.next\n    return head\n\nclass ListNode:\n    __slots__ = ('val', 'next')\n    def __init__(self, val=0, next=None):\n        self.val = val\n        self.next = next\n\ndef build(arr):\n    if not arr:\n        return None\n    head = ListNode(arr[0])\n    cur = head\n    for v in arr[1:]:\n        cur.next = ListNode(v)\n        cur = cur.next\n    return head\n\ndef to_list(head):\n    res = []\n    while head:\n        res.append(str(head.val))\n        head = head.next\n    return ' '.join(res)\n\ndef main():\n    data = sys.stdin.buffer.read().split()\n    if not data:\n        return\n    t = int(data[0])\n    idx = 1\n    out_lines = []\n    for _ in range(t):\n        n = int(data[idx]); idx += 1\n        arr = list(map(int, data[idx:idx+n])); idx += n\n        head = build(arr)\n        head = removeDuplicates(head)\n        out_lines.append(to_list(head))\n    sys.stdout.write('\\n'.join(out_lines))\n\nif __name__ == \"__main__\":\n    main()\n",
      "JavaScript": "const fs = require('fs');\nconst input = fs.readFileSync(0, 'utf-8').trim().split(/\\s+/).map(Number);\nlet idx = 0;\nconst T = input[idx++];\nlet outputs = [];\nfunction removeDuplicates(arr) {\n  // arr is already sorted, just filter consecutive duplicates\n  const res = [];\n  for (let i = 0; i < arr.length; i++) {\n    if (i === 0 || arr[i] !== arr[i-1]) res.push(arr[i]);\n  }\n  return res;\n}\nfor (let tc = 0; tc < T; tc++) {\n  const N = input[idx++];\n  const arr = input.slice(idx, idx + N);\n  idx += N;\n  const uniq = removeDuplicates(arr);\n  outputs.push(uniq.join(' '));\n}\nconsole.log(outputs.join('\\n'));\n"
    }
  },
  "QBCategory": "CCD",
  "boilerPlates": {
    "C": "#include <stdio.h>\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "C++": "#include <iostream>\nusing namespace std;\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "Java": "import java.util.*;\nimport java.io.*;\n\npublic class Main {\n    public static void main(String[] args) throws IOException {\n        // your code goes here\n    }\n}",
    "Python3": "# your code goes here\n",
    "JavaScript": "// your code goes here\n"
  },
  "_internal": {
    "methodName": "",
    "params": []
  }
}