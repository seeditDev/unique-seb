{
  "questionId": "Q11731",
  "title": "Find the smallest divisor",
  "slug": "find-the-smallest-divisor-given-a-threshold",
  "version": 1,
  "createdAt": "2026-07-05",
  "updatedAt": "2026-07-05",
  "metadata": {
    "category": "Math",
    "difficulty": "Medium",
    "isPremium": true,
    "tags": [
      "Divisor",
      "Ceiling",
      "Threshold"
    ],
    "companyTags": [],
    "topicTags": [],
    "QBCategory": "CCD"
  },
  "content": {
    "problemStatement": "You are given an array of integers 'arr' and an integer i.e. a threshold value 'limit'. Your task is to find the smallest positive integer divisor, such that upon dividing all the elements of the given array by it, the sum of the division's result is less than or equal to the given threshold value.",
    "constraints": [
      "1 <= arr.length <= 10^5",
      "1 <= arr[i] <= 10^9",
      "1 <= limit <= 10^9"
    ],
    "inputFormat": "The input will contain an array of integers 'arr' and an integer 'limit'.",
    "outputFormat": "The output will be the smallest positive integer divisor such that upon dividing all the elements of the given array by it, the sum of the division's result is less than or equal to the given threshold value.",
    "sampleTestCases": []
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "label": "min boundary",
        "input": {
          "arr": [
            1
          ],
          "limit": 1
        },
        "expectedOutput": 1,
        "weight": 2
      },
      {
        "id": "tc_2",
        "label": "max boundary",
        "input": {
          "arr": [
            1000000000,
            1000000000
          ],
          "limit": 2000000000
        },
        "expectedOutput": 1000000000,
        "weight": 2
      },
      {
        "id": "tc_3",
        "label": "uniform/edge cases",
        "input": {
          "arr": [
            10,
            10,
            10
          ],
          "limit": 3
        },
        "expectedOutput": 10,
        "weight": 2
      },
      {
        "id": "tc_4",
        "label": "tricky case",
        "input": {
          "arr": [
            1,
            2,
            3,
            4,
            5
          ],
          "limit": 0
        },
        "expectedOutput": -1,
        "weight": 2
      },
      {
        "id": "tc_5",
        "label": "general mixed case",
        "input": {
          "arr": [
            12,
            15,
            18,
            20
          ],
          "limit": 10
        },
        "expectedOutput": 5,
        "weight": 2
      }
    ],
    "totalTestCases": 5,
    "totalWeight": 10
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      2,
      2,
      2,
      2,
      2
    ],
    "maxScore": 10
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "Try all possible divisors from 1 to the maximum element in the array and check if the sum of the ceiling divisions is within the limit.",
    "timeComplexity": "O(n * max_val)",
    "spaceComplexity": "O(1)",
    "code": {
      "C": "#include <stdio.h>\n#include <math.h>\n\nint smallestDivisor(int* arr, int arrSize, int limit) {\n    int max_val = arr[0];\n    for (int i = 1; i < arrSize; i++) {\n        if (arr[i] > max_val) {\n            max_val = arr[i];\n        }\n    }\n\n    for (int d = 1; d <= max_val; d++) {\n        int sum = 0;\n        for (int i = 0; i < arrSize; i++) {\n            sum += (int)ceil((double)arr[i] / (double)d);\n        }\n        if (sum <= limit) {\n            return d;\n        }\n    }\n\n    return -1;\n}\n\nint main() {\n    int arr[] = {1, 2, 3, 4, 5};\n    int limit = 8;\n    int ans = smallestDivisor(arr, 5, limit);\n    printf(\"The minimum divisor is: %d\\n\", ans);\n    return 0;\n}",
      "C++": "#include <bits/stdc++.h>\nusing namespace std;\n\nclass Solution {\npublic:\n    // Function to find the smallest divisor such that\n    // the sum of ceil(arr[i] / d) over all i <= limit\n    int smallestDivisor(vector<int>& arr, int limit) {\n        int n = arr.size(); // Size of the array\n\n        // Find the maximum element in the array\n        int maxi = *max_element(arr.begin(), arr.end());\n\n        // Try every possible divisor from 1 to maxi\n        for (int d = 1; d <= maxi; d++) {\n            int sum = 0;\n\n            // For each element, compute ceil(arr[i] / d) and add to sum\n            for (int i = 0; i < n; i++) {\n                sum += ceil((double)(arr[i]) / (double)(d));\n            }\n\n            // If the total sum is within the limit, return this divisor\n            if (sum <= limit) {\n                return d;\n            }\n        }\n\n        // If no valid divisor is found, return -1\n        return -1;\n    }\n};\n\nint main() {\n    vector<int> arr = {1, 2, 3, 4, 5};\n    int limit = 8;\n\n    Solution obj;\n    int ans = obj.smallestDivisor(arr, limit);\n\n    cout << \"The minimum divisor is: \" << ans << \"\\n\";\n    return 0;\n}",
      "Java": "class Solution {\n    // Method to find the smallest divisor such that the sum of ceiling divisions <= limit\n    public int smallestDivisor(int[] arr, int limit) {\n        int n = arr.length;\n\n        // Find the maximum element in the array\n        int max = Integer.MIN_VALUE;\n        for (int num : arr) {\n            max = Math.max(max, num);\n        }\n\n        // Try all possible divisors from 1 to max\n        for (int d = 1; d <= max; d++) {\n            int sum = 0;\n            for (int i = 0; i < n; i++) {\n                // Divide each number by d and take the ceiling\n                sum += (int) Math.ceil((double) arr[i] / d);\n            }\n\n            // If the total sum is within the limit, return this divisor\n            if (sum <= limit) {\n                return d;\n            }\n        }\n\n        return -1; // No valid divisor found\n    }\n}\n\npublic class Main {\n    public static void main(String[] args) {\n        int[] arr = {1, 2, 3, 4, 5};\n        int limit = 8;\n\n        Solution obj = new Solution();\n        int ans = obj.smallestDivisor(arr, limit);\n        System.out.println(\"The minimum divisor is: \" + ans);\n    }\n}",
      "Python3": "import math\n\nclass Solution:\n    def smallestDivisor(self, arr, limit):\n        n = len(arr)\n\n        # Find the maximum element in the array\n        max_val = max(arr)\n\n        # Try all possible divisors from 1 to max_val\n        for d in range(1, max_val + 1):\n            total = 0\n            for num in arr:\n                # Divide each number by d and round up\n                total += math.ceil(num / d)\n            \n            # If the total sum is within the limit, return this divisor\n            if total <= limit:\n                return d\n\n        return -1  # If no such divisor found\n\n# Driver code\narr = [1, 2, 3, 4, 5]\nlimit = 8\nobj = Solution()\nans = obj.smallestDivisor(arr, limit)\nprint(\"The minimum divisor is:\", ans)",
      "JavaScript": "class Solution {\n    // Method to find the smallest divisor\n    smallestDivisor(arr, limit) {\n        const n = arr.length;\n\n        // Find the maximum value in the array\n        let maxVal = Math.max(...arr);\n\n        // Try all possible divisors from 1 to maxVal\n        for (let d = 1; d <= maxVal; d++) {\n            let total = 0;\n            for (let i = 0; i < n; i++) {\n                // Divide each element by d and take ceiling\n                total += Math.ceil(arr[i] / d);\n            }\n\n            // Check if total is within limit\n            if (total <= limit) {\n                return d;\n            }\n        }\n\n        return -1; // No valid divisor found\n    }\n}\n\n// Driver code\nconst arr = [1, 2, 3, 4, 5];\nconst limit = 8;\nconst obj = new Solution();\nconst ans = obj.smallestDivisor(arr, limit);\nconsole.log(\"The minimum divisor is:\", ans);"
    }
  },
  "QBCategory": "CCD",
  "boilerPlates": {
    "C": "#include <stdio.h>\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "C++": "#include <iostream>\nusing namespace std;\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "Java": "import java.util.*;\nimport java.io.*;\n\npublic class Main {\n    public static void main(String[] args) throws IOException {\n        // your code goes here\n    }\n}",
    "Python3": "# your code goes here\n",
    "JavaScript": "// your code goes here\n"
  },
  "_internal": {
    "methodName": "",
    "params": []
  }
}