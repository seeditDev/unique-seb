{
  "questionId": "Q11784",
  "title": "Partition Array for Maximum Sum",
  "slug": "partition-array-for-maximum-sum",
  "version": 1,
  "createdAt": "2026-07-05",
  "updatedAt": "2026-07-05",
  "metadata": {
    "category": "Dynamic Programming",
    "difficulty": "Medium",
    "isPremium": true,
    "tags": [
      "Partition",
      "Memoization"
    ],
    "companyTags": [],
    "topicTags": [],
    "QBCategory": "CCD"
  },
  "content": {
    "problemStatement": "**Problem Description: **Given an integer array arr with length n, and an integer k, you may partition arr into one or more contiguous sub-arrays, where each sub-array has length in the range [1, k] (both inclusive).\n\nAfter you pick a partition, replace every element in each sub-array with the maximum value found in that sub-array. The array is modified in-place for the purpose of computing the total. Return the largest possible sum of the entire array after performing exactly one such partition-and-replace operation.\n\n\n\n\n\n\n\n    \n        \n            **\n                    Examples",
    "constraints": [
      "1 <= k <= n",
      "1 <= n <= 500",
      "1 <= arr[i] <= 10^4"
    ],
    "inputFormat": "arr = [int], k = int",
    "outputFormat": "int",
    "sampleTestCases": []
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "label": "min boundary",
        "input": {
          "arr": [
            1
          ],
          "k": 1
        },
        "expectedOutput": 1,
        "weight": 2
      },
      {
        "id": "tc_2",
        "label": "max boundary",
        "input": {
          "arr": [
            1,
            2,
            3,
            4,
            5
          ],
          "k": 5
        },
        "expectedOutput": 15,
        "weight": 2
      },
      {
        "id": "tc_3",
        "label": "uniform case",
        "input": {
          "arr": [
            5,
            5,
            5,
            5,
            5
          ],
          "k": 3
        },
        "expectedOutput": 25,
        "weight": 2
      },
      {
        "id": "tc_4",
        "label": "tricky case",
        "input": {
          "arr": [
            1,
            1,
            1,
            1,
            1
          ],
          "k": 1
        },
        "expectedOutput": 5,
        "weight": 2
      },
      {
        "id": "tc_5",
        "label": "mixed case",
        "input": {
          "arr": [
            1,
            15,
            7,
            9,
            2,
            5,
            10
          ],
          "k": 3
        },
        "expectedOutput": 84,
        "weight": 2
      }
    ],
    "totalTestCases": 5,
    "totalWeight": 10
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      2,
      2,
      2,
      2,
      2
    ],
    "maxScore": 10
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "Use dynamic programming with memoization to find the maximum sum after partitioning the array.",
    "timeComplexity": "O(n * k)",
    "spaceComplexity": "O(n)",
    "code": {
      "C": "#include <stdio.h>\n#include <stdlib.h>\n\nint maxSumAfterPartitioning(int* arr, int arrSize, int k) {\n    int* memo = (int*)malloc(arrSize * sizeof(int));\n    for (int i = 0; i < arrSize; i++) {\n        memo[i] = -1;\n    }\n\n    int helper(int start) {\n        if (start == arrSize) return 0;\n        if (memo[start] != -1) return memo[start];\n\n        int maxSum = 0;\n        int maxElem = 0;\n        for (int length = 1; length <= k && start + length <= arrSize; length++) {\n            maxElem = maxElem > arr[start + length - 1] ? maxElem : arr[start + length - 1];\n            int currentSum = maxElem * length + helper(start + length);\n            maxSum = maxSum > currentSum ? maxSum : currentSum;\n        }\n        memo[start] = maxSum;\n        return maxSum;\n    }\n\n    return helper(0);\n}\n\nint main() {\n    int arr[] = {1, 15, 7, 9, 2, 5, 10};\n    int k = 3;\n    printf(\"Maximum sum after partitioning: %d\\n\", maxSumAfterPartitioning(arr, 7, k));\n    return 0;\n}",
      "C++": "#include <bits/stdc++.h>\nusing namespace std;\n\nclass Solution {\nprivate:\n    // Recursive helper function to find max sum from 'start' index\n    int helper(const vector<int>& arr, int k, int start, vector<int>& memo) {\n        int n = (int)arr.size();\n\n        // Base case: if start reached end, no elements left to partition\n        if (start == n) return 0;\n\n        // Return cached result if already computed for this start index\n        if (memo[start] != -1) return memo[start];\n\n        int maxSum = 0;    \n        int maxElem = 0;   \n\n        // Try partitions of length 1 to k starting at 'start'\n        for (int length = 1; length <= k && start + length <= n; length++) {\n            // Update max element for this partition length\n            maxElem = max(maxElem, arr[start + length - 1]);\n\n            // Calculate current sum: maxElem * length + recursive result for remaining array\n            int currentSum = maxElem * length + helper(arr, k, start + length, memo);\n\n            // Update maxSum if current partition leads to higher sum\n            maxSum = max(maxSum, currentSum);\n        }\n\n        // Memoize and return the best max sum for this start index\n        return memo[start] = maxSum;\n    }\n\npublic:\n    // Public function to initiate recursive memoization\n    int maxSumAfterPartitioning(vector<int>& arr, int k) {\n        int n = (int)arr.size();\n\n        // Initialize memo array with -1 to indicate unvisited states\n        vector<int> memo(n, -1);\n\n        // Start recursion from index 0\n        return helper(arr, k, 0, memo);\n    }\n};\n\nint main() {\n    vector<int> arr = {1, 15, 7, 9, 2, 5, 10};\n    int k = 3;\n\n    Solution sol;\n    cout << \"Maximum sum after partitioning: \" << sol.maxSumAfterPartitioning(arr, k) << \"\\n\";\n\n    return 0;\n}",
      "Java": "class Solution {\n    // Recursive helper function to find max sum from 'start' index\n    private int helper(int[] arr, int k, int start, int[] memo) {\n        int n = arr.length;\n\n        // Base case: if start reached end, no elements left to partition\n        if (start == n) return 0;\n\n        // Return cached result if already computed for this start index\n        if (memo[start] != -1) return memo[start];\n\n        int maxSum = 0;    \n        int maxElem = 0;   \n\n        // Try partitions of length 1 to k starting at 'start'\n        for (int length = 1; length <= k && start + length <= n; length++) {\n            // Update max element for this partition length\n            maxElem = Math.max(maxElem, arr[start + length - 1]);\n\n            // Calculate current sum: maxElem * length + recursive result for remaining array\n            int currentSum = maxElem * length + helper(arr, k, start + length, memo);\n\n            // Update maxSum if current partition leads to higher sum\n            maxSum = Math.max(maxSum, currentSum);\n        }\n\n        // Memoize and return the best max sum for this start index\n        return memo[start] = maxSum;\n    }\n\n    // Public method to start the recursion with memoization\n    public int maxSumAfterPartitioning(int[] arr, int k) {\n        int n = arr.length;\n\n        // Initialize memo array with -1 to indicate unvisited states\n        int[] memo = new int[n];\n        java.util.Arrays.fill(memo, -1);\n\n        // Start recursion from index 0\n        return helper(arr, k, 0, memo);\n    }\n}\n\npublic class Main {\n    public static void main(String[] args) {\n        int[] arr = {1, 15, 7, 9, 2, 5, 10};\n        int k = 3;\n\n        Solution sol = new Solution();\n        System.out.println(\"Maximum sum after partitioning: \" + sol.maxSumAfterPartitioning(arr, k));\n    }\n}",
      "Python3": "class Solution:\n    # Recursive helper function to find max sum from 'start' index\n    def helper(self, arr, k, start, memo):\n        n = len(arr)\n\n        # Base case: if start reached end, no elements left to partition\n        if start == n:\n            return 0\n\n        # Return cached result if already computed for this start index\n        if memo[start] != -1:\n            return memo[start]\n\n        maxSum = 0     \n        maxElem = 0    \n\n        # Try partitions of length 1 to k starting at 'start'\n        for length in range(1, k + 1):\n            if start + length > n:\n                break\n\n            # Update max element for this partition length\n            maxElem = max(maxElem, arr[start + length - 1])\n\n            # Calculate current sum: maxElem * length + recursive result for remaining array\n            currentSum = maxElem * length + self.helper(arr, k, start + length, memo)\n\n            # Update maxSum if current partition leads to higher sum\n            maxSum = max(maxSum, currentSum)\n\n        # Memoize and return the best max sum for this start index\n        memo[start] = maxSum\n        return maxSum\n\n    # Public method to start the recursion with memoization\n    def maxSumAfterPartitioning(self, arr, k):\n        n = len(arr)\n\n        # Initialize memo list with -1 to indicate unvisited states\n        memo = [-1] * n\n\n        # Start recursion from index 0\n        return self.helper(arr, k, 0, memo)\n\n\nif __name__ == \"__main__\":\n    arr = [1, 15, 7, 9, 2, 5, 10]\n    k = 3\n\n    sol = Solution()\n    print(\"Maximum sum after partitioning:\", sol.maxSumAfterPartitioning(arr, k))",
      "JavaScript": "class Solution {\n    // Recursive helper function to find max sum from 'start' index\n    helper(arr, k, start, memo) {\n        const n = arr.length;\n\n        // Base case: if start reached end, no elements left to partition\n        if (start === n) return 0;\n\n        // Return cached result if already computed for this start index\n        if (memo[start] !== -1) return memo[start];\n\n        let maxSum = 0;   \n        let maxElem = 0;  \n\n        // Try partitions of length 1 to k starting at 'start'\n        for (let length = 1; length <= k && start + length <= n; length++) {\n            // Update max element for this partition length\n            maxElem = Math.max(maxElem, arr[start + length - 1]);\n\n            // Calculate current sum: maxElem * length + recursive result for remaining array\n            let currentSum = maxElem * length + this.helper(arr, k, start + length, memo);\n\n            // Update maxSum if current partition leads to higher sum\n            maxSum = Math.max(maxSum, currentSum);\n        }\n\n        // Memoize and return the best max sum for this start index\n        memo[start] = maxSum;\n        return maxSum;\n    }\n\n    // Public method to start the recursion with memoization\n    maxSumAfterPartitioning(arr, k) {\n        const n = arr.length;\n\n        // Initialize memo array with -1 to indicate unvisited states\n        const memo = new Array(n).fill(-1);\n\n        // Start recursion from index 0\n        return this.helper(arr, k, 0, memo);\n    }\n}\n\n// Driver code\nconst arr = [1, 15, 7, 9, 2, 5, 10];\nconst k = 3;\nconst sol = new Solution();\n\nconsole.log(\"Maximum sum after partitioning:\", sol.maxSumAfterPartitioning(arr, k));"
    }
  },
  "QBCategory": "CCD",
  "boilerPlates": {
    "C": "#include <stdio.h>\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "C++": "#include <iostream>\nusing namespace std;\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "Java": "import java.util.*;\nimport java.io.*;\n\npublic class Main {\n    public static void main(String[] args) throws IOException {\n        // your code goes here\n    }\n}",
    "Python3": "# your code goes here\n",
    "JavaScript": "// your code goes here\n"
  },
  "_internal": {
    "methodName": "",
    "params": []
  }
}