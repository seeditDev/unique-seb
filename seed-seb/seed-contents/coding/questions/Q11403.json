{
  "questionId": "Q11403",
  "title": "Trie Implementation and Advanced Operations",
  "slug": "trieimplementationandadvancedoperations",
  "version": 1,
  "createdAt": "2026-07-05",
  "updatedAt": "2026-07-05",
  "metadata": {
    "category": "Data Structures",
    "difficulty": "Hard",
    "isPremium": true,
    "tags": [
      "Trie",
      "String Manipulation"
    ],
    "companyTags": [],
    "topicTags": [],
    "QBCategory": "CCD"
  },
  "content": {
    "problemStatement": "Implement \"TRIE” data structure from scratch with the following functions.\n\n\nTrie(): Initialize the object of this “TRIE” data structure.\ninsert(“WORD”): Insert the string “WORD” into this “TRIE” data structure.\ncountWordsEqualTo(“WORD”): Return how many times this “WORD” is present in this “TRIE”.\ncountWordsStartingWith(“PREFIX”): Return how many words are there in this “TRIE” that have the string “PREFIX” as a prefix.\nerase(“WORD”): Delete one occurrence of the string “WORD” from the “TRIE”.",
    "constraints": [
      "1 <= number of operations <= 10000",
      "1 <= length of word <= 100",
      "All words will consist of lowercase English letters"
    ],
    "inputFormat": "The input will contain a series of operations to be performed on the Trie data structure. The operations will be one of the following: 'Trie', 'insert', 'countWordsEqualTo', 'countWordsStartingWith', 'erase'. The 'Trie' operation will initialize a new Trie. The 'insert' operation will insert a word into the Trie. The 'countWordsEqualTo' operation will return the number of times a word is present in the Trie. The 'countWordsStartingWith' operation will return the number of words that have a given prefix. The 'erase' operation will delete one occurrence of a word from the Trie.",
    "outputFormat": "The output will be the result of each operation performed on the Trie. For the 'Trie' operation, there will be no output. For the 'insert' operation, there will be no output. For the 'countWordsEqualTo' operation, the output will be the number of times the word is present in the Trie. For the 'countWordsStartingWith' operation, the output will be the number of words that have the given prefix. For the 'erase' operation, there will be no output.",
    "sampleTestCases": [
      {
        "id": "sample_1",
        "input": "[\"Trie\", \"insert\", \"countWordsEqualTo\", \"insert\", \"countWordsStartingWith\", \"erase\", \"countWordsStartingWith\"]  \n[ [], [\"apple\"], [\"apple\"], [\"app\"], [\"app\"], [\"apple\"], [\"app\"] ]",
        "output": "[null, null, 1, null, 2, null, 1]",
        "explanation": "- Trie trie = new Trie()  \n- trie.insert(\"apple\")  \n- trie.countWordsEqualTo(\"apple\")  // returns 1  \n- trie.insert(\"app\")  \n- trie.countWordsStartingWith(\"app\") // returns 2  \n- trie.erase(\"apple\")  \n- trie.countWordsStartingWith(\"app\")   // returns 1  \n\n**Example 2:**"
      },
      {
        "id": "sample_2",
        "input": "[\"Trie\", \"insert\", \"countWordsEqualTo\", \"insert\", \"erase\", \"countWordsStartingWith\"]  \n[ [], [\"mango\"], [\"apple\"], [\"app\"], [\"app\"], [\"mango\"] ]",
        "output": "[null, null, 0, null, null, 1]",
        "explanation": "- Trie trie = new Trie()  \n- trie.insert(\"mango\")  \n- trie.countWordsEqualTo(\"apple\")  // returns 0  \n- trie.insert(\"app\")  \n- trie.erase(\"app\")  \n- trie.countWordsStartingWith(\"mango\") // returns 1\n        \n    \n\n\n**Disclaimer**: Here is the practice link to help you assess your knowledge better. It's highly recommend trying to solve it before looking at the solution."
      }
    ]
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "label": "min boundary",
        "input": "[\"Trie\", \"insert\", \"countWordsEqualTo\"]\n[ [], [\"a\"], [\"a\"] ]",
        "expectedOutput": "[null, null, 1]",
        "weight": 2
      },
      {
        "id": "tc_2",
        "label": "max boundary",
        "input": "[\"Trie\", \"insert\", \"countWordsEqualTo\", \"countWordsStartingWith\", \"erase\"]\n[ [], [\"abcdefghijklmnopqrstuvwxyz\"], [\"abcdefghijklmnopqrstuvwxyz\"], [\"abc\"], [\"abcdefghijklmnopqrstuvwxyz\"] ]",
        "expectedOutput": "[null, null, 1, 1, null]",
        "weight": 2
      },
      {
        "id": "tc_3",
        "label": "uniform/edge cases",
        "input": "[\"Trie\", \"insert\", \"insert\", \"countWordsEqualTo\", \"countWordsStartingWith\"]\n[ [], [\"aaa\"], [\"aaa\"], [\"aaa\"], [\"aa\"] ]",
        "expectedOutput": "[null, null, null, 2, 2]",
        "weight": 2
      },
      {
        "id": "tc_4",
        "label": "tricky case / empty-like / negative if allowed",
        "input": "[\"Trie\", \"insert\", \"countWordsEqualTo\", \"countWordsStartingWith\", \"erase\"]\n[ [], [\"\"], [\"\"], [\"\"], [\"\"] ]",
        "expectedOutput": "[null, null, 1, 1, null]",
        "weight": 2
      },
      {
        "id": "tc_5",
        "label": "general mixed case",
        "input": "[\"Trie\", \"insert\", \"insert\", \"countWordsEqualTo\", \"countWordsStartingWith\", \"erase\", \"countWordsEqualTo\"]\n[ [], [\"apple\"], [\"app\"], [\"apple\"], [\"app\"], [\"apple\"], [\"apple\"] ]",
        "expectedOutput": "[null, null, null, 1, 2, null, 0]",
        "weight": 2
      }
    ],
    "totalTestCases": 5,
    "totalWeight": 10
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      2,
      2,
      2,
      2,
      2
    ],
    "maxScore": 10
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "The approach to solve this problem is to implement a Trie data structure and perform the given operations on it. The Trie will be implemented using a Node class that has an array of child nodes and a counter for the number of words that end at the node. The 'insert' operation will iterate over each character in the word and create a new node if the character is not already in the Trie. The 'countWordsEqualTo' operation will iterate over each character in the word and return the counter of the last node. The 'countWordsStartingWith' operation will iterate over each character in the prefix and return the counter of the last node. The 'erase' operation will iterate over each character in the word and decrement the counter of the last node.",
    "timeComplexity": "O(N * M) where N is the number of operations and M is the maximum length of a word",
    "spaceComplexity": "O(N * M) where N is the number of operations and M is the maximum length of a word",
    "code": {
      "C": "#include <stdio.h>\n#include <stdlib.h>\n#include <string.h>\n\n// Define the structure for a Trie node\ntypedef struct Node {\n    struct Node* links[26];\n    int cntEndWith;\n    int cntPrefix;\n} Node;\n\n// Function to create a new Trie node\nNode* createNode() {\n    Node* node = (Node*) malloc(sizeof(Node));\n    for (int i = 0; i < 26; i++) {\n        node->links[i] = NULL;\n    }\n    node->cntEndWith = 0;\n    node->cntPrefix = 0;\n    return node;\n}\n\n// Function to insert a word into the Trie\nvoid insert(Node* root, char* word) {\n    Node* node = root;\n    for (int i = 0; i < strlen(word); i++) {\n        if (node->links[word[i] - 'a'] == NULL) {\n            node->links[word[i] - 'a'] = createNode();\n        }\n        node = node->links[word[i] - 'a'];\n        node->cntPrefix++;\n    }\n    node->cntEndWith++;\n}\n\n// Function to count the number of words equal to a given word\nint countWordsEqualTo(Node* root, char* word) {\n    Node* node = root;\n    for (int i = 0; i < strlen(word); i++) {\n        if (node->links[word[i] - 'a'] == NULL) {\n            return 0;\n        }\n        node = node->links[word[i] - 'a'];\n    }\n    return node->cntEndWith;\n}\n\n// Function to count the number of words starting with a given prefix\nint countWordsStartingWith(Node* root, char* word) {\n    Node* node = root;\n    for (int i = 0; i < strlen(word); i++) {\n        if (node->links[word[i] - 'a'] == NULL) {\n            return 0;\n        }\n        node = node->links[word[i] - 'a'];\n    }\n    return node->cntPrefix;\n}\n\n// Function to erase a word from the Trie\nvoid erase(Node* root, char* word) {\n    Node* node = root;\n    for (int i = 0; i < strlen(word); i++) {\n        node = node->links[word[i] - 'a'];\n        node->cntPrefix--;\n    }\n    node->cntEndWith--;\n}\n\nint main() {\n    Node* root = createNode();\n    // Perform operations on the Trie\n    insert(root, \"apple\");\n    insert(root, \"app\");\n    printf(\"Count Words Equal to 'apple': %d\\n\", countWordsEqualTo(root, \"apple\"));\n    printf(\"Count Words Starting With 'app': %d\\n\", countWordsStartingWith(root, \"app\"));\n    erase(root, \"apple\");\n    printf(\"Count Words Equal to 'apple': %d\\n\", countWordsEqualTo(root, \"apple\"));\n    return 0;\n}",
      "C++": "#include <iostream>\n#include <string>\nusing namespace std;\n\n// Define the structure for a Trie node\nstruct Node {\n    Node* links[26];\n    int cntEndWith;\n    int cntPrefix;\n    Node() {\n        for (int i = 0; i < 26; i++) {\n            links[i] = NULL;\n        }\n        cntEndWith = 0;\n        cntPrefix = 0;\n    }\n};\n\n// Function to insert a word into the Trie\nvoid insert(Node* root, string word) {\n    Node* node = root;\n    for (int i = 0; i < word.size(); i++) {\n        if (node->links[word[i] - 'a'] == NULL) {\n            node->links[word[i] - 'a'] = new Node();\n        }\n        node = node->links[word[i] - 'a'];\n        node->cntPrefix++;\n    }\n    node->cntEndWith++;\n}\n\n// Function to count the number of words equal to a given word\nint countWordsEqualTo(Node* root, string word) {\n    Node* node = root;\n    for (int i = 0; i < word.size(); i++) {\n        if (node->links[word[i] - 'a'] == NULL) {\n            return 0;\n        }\n        node = node->links[word[i] - 'a'];\n    }\n    return node->cntEndWith;\n}\n\n// Function to count the number of words starting with a given prefix\nint countWordsStartingWith(Node* root, string word) {\n    Node* node = root;\n    for (int i = 0; i < word.size(); i++) {\n        if (node->links[word[i] - 'a'] == NULL) {\n            return 0;\n        }\n        node = node->links[word[i] - 'a'];\n    }\n    return node->cntPrefix;\n}\n\n// Function to erase a word from the Trie\nvoid erase(Node* root, string word) {\n    Node* node = root;\n    for (int i = 0; i < word.size(); i++) {\n        node = node->links[word[i] - 'a'];\n        node->cntPrefix--;\n    }\n    node->cntEndWith--;\n}\n\nint main() {\n    Node* root = new Node();\n    // Perform operations on the Trie\n    insert(root, \"apple\");\n    insert(root, \"app\");\n    cout << \"Count Words Equal to 'apple': \" << countWordsEqualTo(root, \"apple\") << endl;\n    cout << \"Count Words Starting With 'app': \" << countWordsStartingWith(root, \"app\") << endl;\n    erase(root, \"apple\");\n    cout << \"Count Words Equal to 'apple': \" << countWordsEqualTo(root, \"apple\") << endl;\n    return 0;\n}",
      "Java": "import java.util.*;\n\nclass Node {\n    Node[] links;\n    int cntEndWith;\n    int cntPrefix;\n    Node() {\n        links = new Node[26];\n        cntEndWith = 0;\n        cntPrefix = 0;\n    }\n}\n\nclass Trie {\n    Node root;\n    Trie() {\n        root = new Node();\n    }\n\n    void insert(String word) {\n        Node node = root;\n        for (int i = 0; i < word.length(); i++) {\n            if (node.links[word.charAt(i) - 'a'] == null) {\n                node.links[word.charAt(i) - 'a'] = new Node();\n            }\n            node = node.links[word.charAt(i) - 'a'];\n            node.cntPrefix++;\n        }\n        node.cntEndWith++;\n    }\n\n    int countWordsEqualTo(String word) {\n        Node node = root;\n        for (int i = 0; i < word.length(); i++) {\n            if (node.links[word.charAt(i) - 'a'] == null) {\n                return 0;\n            }\n            node = node.links[word.charAt(i) - 'a'];\n        }\n        return node.cntEndWith;\n    }\n\n    int countWordsStartingWith(String word) {\n        Node node = root;\n        for (int i = 0; i < word.length(); i++) {\n            if (node.links[word.charAt(i) - 'a'] == null) {\n                return 0;\n            }\n            node = node.links[word.charAt(i) - 'a'];\n        }\n        return node.cntPrefix;\n    }\n\n    void erase(String word) {\n        Node node = root;\n        for (int i = 0; i < word.length(); i++) {\n            node = node.links[word.charAt(i) - 'a'];\n            node.cntPrefix--;\n        }\n        node.cntEndWith--;\n    }\n\n    public static void main(String[] args) {\n        Trie trie = new Trie();\n        trie.insert(\"apple\");\n        trie.insert(\"app\");\n        System.out.println(\"Count Words Equal to 'apple': \" + trie.countWordsEqualTo(\"apple\"));\n        System.out.println(\"Count Words Starting With 'app': \" + trie.countWordsStartingWith(\"app\"));\n        trie.erase(\"apple\");\n        System.out.println(\"Count Words Equal to 'apple': \" + trie.countWordsEqualTo(\"apple\"));\n    }\n}",
      "Python3": "class Node:\n    def __init__(self):\n        self.links = [None] * 26\n        self.cntEndWith = 0\n        self.cntPrefix = 0\n\nclass Trie:\n    def __init__(self):\n        self.root = Node()\n\n    def insert(self, word):\n        node = self.root\n        for ch in word:\n            if node.links[ord(ch) - ord('a')] is None:\n                node.links[ord(ch) - ord('a')] = Node()\n            node = node.links[ord(ch) - ord('a')]\n            node.cntPrefix += 1\n        node.cntEndWith += 1\n\n    def countWordsEqualTo(self, word):\n        node = self.root\n        for ch in word:\n            if node.links[ord(ch) - ord('a')] is None:\n                return 0\n            node = node.links[ord(ch) - ord('a')]\n        return node.cntEndWith\n\n    def countWordsStartingWith(self, word):\n        node = self.root\n        for ch in word:\n            if node.links[ord(ch) - ord('a')] is None:\n                return 0\n            node = node.links[ord(ch) - ord('a')]\n        return node.cntPrefix\n\n    def erase(self, word):\n        node = self.root\n        for ch in word:\n            node = node.links[ord(ch) - ord('a')]\n            node.cntPrefix -= 1\n        node.cntEndWith -= 1\n\n# Testing the Trie class\ntrie = Trie()\ntrie.insert(\"apple\")\ntrie.insert(\"app\")\nprint(\"Count Words Equal to 'apple':\", trie.countWordsEqualTo(\"apple\"))\nprint(\"Count Words Starting With 'app':\", trie.countWordsStartingWith(\"app\"))\ntrie.erase(\"apple\")\nprint(\"Count Words Equal to 'apple':\", trie.countWordsEqualTo(\"apple\"))",
      "JavaScript": "class Node {\n    constructor() {\n        this.links = new Array(26).fill(null);\n        this.cntEndWith = 0;\n        this.cntPrefix = 0;\n    }\n}\n\nclass Trie {\n    constructor() {\n        this.root = new Node();\n    }\n\n    insert(word) {\n        let node = this.root;\n        for (let i = 0; i < word.length; i++) {\n            if (node.links[word.charCodeAt(i) - 'a'.charCodeAt(0)] === null) {\n                node.links[word.charCodeAt(i) - 'a'.charCodeAt(0)] = new Node();\n            }\n            node = node.links[word.charCodeAt(i) - 'a'.charCodeAt(0)];\n            node.cntPrefix++;\n        }\n        node.cntEndWith++;\n    }\n\n    countWordsEqualTo(word) {\n        let node = this.root;\n        for (let i = 0; i < word.length; i++) {\n            if (node.links[word.charCodeAt(i) - 'a'.charCodeAt(0)] === null) {\n                return 0;\n            }\n            node = node.links[word.charCodeAt(i) - 'a'.charCodeAt(0)];\n        }\n        return node.cntEndWith;\n    }\n\n    countWordsStartingWith(word) {\n        let node = this.root;\n        for (let i = 0; i < word.length; i++) {\n            if (node.links[word.charCodeAt(i) - 'a'.charCodeAt(0)] === null) {\n                return 0;\n            }\n            node = node.links[word.charCodeAt(i) - 'a'.charCodeAt(0)];\n        }\n        return node.cntPrefix;\n    }\n\n    erase(word) {\n        let node = this.root;\n        for (let i = 0; i < word.length; i++) {\n            node = node.links[word.charCodeAt(i) - 'a'.charCodeAt(0)];\n            node.cntPrefix--;\n        }\n        node.cntEndWith--;\n    }\n}\n\n// Testing the Trie class\nconst trie = new Trie();\ntrie.insert(\"apple\");\ntrie.insert(\"app\");\nconsole.log(\"Count Words Equal to 'apple':\", trie.countWordsEqualTo(\"apple\"));\nconsole.log(\"Count Words Starting With 'app':\", trie.countWordsStartingWith(\"app\"));\ntrie.erase(\"apple\");\nconsole.log(\"Count Words Equal to 'apple':\", trie.countWordsEqualTo(\"apple\"));"
    }
  },
  "QBCategory": "CCD",
  "boilerPlates": {
    "C": "#include <stdio.h>\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "C++": "#include <iostream>\nusing namespace std;\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "Java": "import java.util.*;\nimport java.io.*;\n\npublic class Main {\n    public static void main(String[] args) throws IOException {\n        // your code goes here\n    }\n}",
    "Python3": "# your code goes here\n",
    "JavaScript": "// your code goes here\n"
  },
  "_internal": {
    "methodName": "",
    "params": []
  }
}