{
  "questionId": "Q8465",
  "title": "List of Lists",
  "slug": "list-of-lists",
  "version": 1,
  "createdAt": "2026-07-02",
  "updatedAt": "2026-07-04",
  "metadata": {
    "category": "Greedy",
    "difficulty": "Medium",
    "isPremium": true,
    "tags": [
      "Frequency Counting",
      "Greedy",
      "Math"
    ],
    "companyTags": [],
    "topicTags": [
      "Ad-hoc & Implementation"
    ],
    "QBCategory": "CCD"
  },
  "content": {
    "problemStatement": "You are given a positive integer NNN and an array AAA of size NNN. There are NNN lists L1,L2…LNL_1, L_2 \\ldots L_NL1​,L2​…LN​. Initially, Li=[Ai]L_i = [A_i]Li​=[Ai​].\n\nYou can perform the following operation any number of times as long as there are at least 222 lists:\n\n\nSelect 222 (non-empty) lists LiL_iLi​ and LjL_jLj​ (i≠ji \\neq ji=j)\nAppend LjL_jLj​ to LiL_iLi​ and remove the list LjL_jLj​. Note that this means LjL_jLj​ cannot be chosen in any future operation.\nFind the minimum number of operations required to obtain a set of lists that satisfies the following conditions:\n\n\nThe first element and last element of each list are equal.\nThe first element of all the lists is the same.\nPrint −1-1−1 if it is not possible to achieve this via any sequence of operations.\n\n\nInput Format\n\nThe first line of input contains a single integer TTT, denoting the number of test cases. The description of TTT test cases follows.\nThe first line of each test case contains an integer NNN.\nThe second line of each test case contains NNN space-separated integers A1,A2,…,ANA_1, A_2, \\ldots, A_NA1​,A2​,…,AN​.\n\n\nOutput Format\nFor each test case, print a single line containing one integer: the minimum number of operations required to obtain an array of lists that satisfies the given conditions.\n\n\nPrint −1-1−1 if it is impossible to achieve such an array of lists.",
    "constraints": [
      "1 ≤ T ≤ 10^5",
      "1 ≤ N ≤ 2·10^5",
      "1 ≤ Ai ≤ N",
      "Sum of N over all test cases ≤ 2·10^5"
    ],
    "inputFormat": "The first line contains an integer T, the number of test cases. Each test case consists of two lines: the first line contains an integer N, the number of elements. The second line contains N space‑separated integers A1, A2, …, AN.",
    "outputFormat": "For each test case output a single integer on its own line – the minimum number of operations required to obtain a set of lists satisfying the conditions, or -1 if it is impossible.",
    "sampleTestCases": [
      {
        "id": "sample_1",
        "input": "3\n1\n1\n2\n1 2\n3\n1 1 2",
        "output": "0\n-1\n2",
        "explanation": "Test case 1: only one list, already satisfies conditions → 0 operations.\\nTest case 2: all values appear once, no value with frequency ≥2, impossible → -1.\\nTest case 3: value 1 appears twice (maxFreq=2). Other elements = 1 (the 2). Minimum operations = N - maxFreq + 1 = 3 - 2 + 1 = 2."
      },
      {
        "id": "sample_2",
        "input": "1\\n5\\n4 4 4 4 4",
        "output": "0",
        "explanation": "All elements are the same, no operation needed."
      }
    ]
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "label": "Case 1",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_2",
        "label": "Case 2",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_3",
        "label": "Case 3",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_4",
        "label": "Case 4",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_5",
        "label": "Case 5",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      }
    ],
    "totalTestCases": 5,
    "totalWeight": 10
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      2,
      2,
      2,
      2,
      2
    ],
    "maxScore": 10
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "For each test case count how many times each value appears. Let maxFreq be the largest frequency. • If maxFreq == N, all elements are already equal, no operation is needed → answer 0. • If maxFreq == 1, every value appears only once, so we cannot find a value that appears at least twice to act as the common first/last element → answer -1. • Otherwise we can choose the value with maxFreq as the common element X. All other elements (N - maxFreq of them) can be placed between two X's in a single list, using two X's as its ends. The remaining (maxFreq - 2) X's can stay as singleton lists. The number of operations equals the number of merges performed: we merge the (N - maxFreq) non‑X elements plus one extra merge to attach the first X at the end, i.e. (N - maxFreq) + 1. This is minimal because using more than one list that contains non‑X elements would require additional merges. The final formula is: answer = N - maxFreq + 1 (when maxFreq ≥ 2 and maxFreq < N). The algorithm runs in O(N) per test case using a hash map for frequencies.",
    "timeComplexity": "O(N) per test case, total O(ΣN) ≤ 2·10^5",
    "spaceComplexity": "O(K) where K is the number of distinct values (≤ N), at most O(N)",
    "code": {
      "C": "#include <stdio.h>\n#include <stdlib.h>\n\ntypedef struct Node {int key; int cnt; struct Node *next;} Node;\n\n#define MOD 1000003\nNode *table[MOD];\nunsigned int hash_int(int x){return ((unsigned)x * 2654435761u) % MOD;}\nNode* find(int x){unsigned int h=hash_int(x); for(Node* p=table[h];p;p=p->next) if(p->key==x) return p; return NULL;}\nvoid inc(int x){Node* p=find(x); if(p){p->cnt++; return;} unsigned int h=hash_int(x); Node* n= (Node*)malloc(sizeof(Node)); n->key=x; n->cnt=1; n->next=table[h]; table[h]=n;}\nvoid clear(){for(int i=0;i<MOD;i++){Node* p=table[i]; while(p){Node* q=p; p=p->next; free(q);} table[i]=NULL;}}\n\nint main(){\n    int T; if(scanf(\"%d\", &T)!=1) return 0;\n    while(T--){\n        int N; scanf(\"%d\", &N);\n        clear();\n        int maxFreq=0;\n        for(int i=0;i<N;i++){\n            int a; scanf(\"%d\", &a);\n            inc(a);\n        }\n        for(int i=0;i<MOD;i++){\n            for(Node* p=table[i]; p; p=p->next){\n                if(p->cnt>maxFreq) maxFreq=p->cnt;\n            }\n        }\n        if(maxFreq==N) printf(\"0\\n\");\n        else if(maxFreq==1) printf(\"-1\\n\");\n        else printf(\"%d\\n\", N - maxFreq + 1);\n    }\n    return 0;\n}\n",
      "C++": "#include <bits/stdc++.h>\nusing namespace std;\nint main(){\n    ios::sync_with_stdio(false);\n    cin.tie(nullptr);\n    int T; if(!(cin>>T)) return 0;\n    while(T--){\n        int N; cin>>N;\n        unordered_map<int,int> freq;\n        freq.reserve(N*2);\n        int maxFreq=0, x;\n        for(int i=0;i<N;i++){\n            cin>>x;\n            int c=++freq[x];\n            if(c>maxFreq) maxFreq=c;\n        }\n        if(maxFreq==N) cout<<0<<'\\n';\n        else if(maxFreq==1) cout<<-1<<'\\n';\n        else cout<<N - maxFreq + 1<<'\\n';\n    }\n    return 0;\n}\n",
      "Java": "import java.io.*;\nimport java.util.*;\npublic class Main {\n    public static void main(String[] args) throws Exception {\n        FastScanner fs = new FastScanner(System.in);\n        StringBuilder sb = new StringBuilder();\n        int T = fs.nextInt();\n        while (T-- > 0) {\n            int N = fs.nextInt();\n            HashMap<Integer, Integer> map = new HashMap<>(N*2);\n            int maxFreq = 0;\n            for (int i = 0; i < N; i++) {\n                int a = fs.nextInt();\n                int cnt = map.getOrDefault(a, 0) + 1;\n                map.put(a, cnt);\n                if (cnt > maxFreq) maxFreq = cnt;\n            }\n            if (maxFreq == N) sb.append(0);\n            else if (maxFreq == 1) sb.append(-1);\n            else sb.append(N - maxFreq + 1);\n            sb.append('\\n');\n        }\n        System.out.print(sb.toString());\n    }\n    static class FastScanner {\n        private final InputStream in;\n        private final byte[] buffer = new byte[1 << 16];\n        private int ptr = 0, len = 0;\n        FastScanner(InputStream in) { this.in = in; }\n        private int readByte() throws IOException {\n            if (ptr >= len) {\n                len = in.read(buffer);\n                ptr = 0;\n                if (len <= 0) return -1;\n            }\n            return buffer[ptr++];\n        }\n        int nextInt() throws IOException {\n            int c, sign = 1, val = 0;\n            do { c = readByte(); } while (c <= ' ');\n            if (c == '-') { sign = -1; c = readByte(); }\n            while (c > ' ') {\n                val = val * 10 + (c - '0');\n                c = readByte();\n            }\n            return val * sign;\n        }\n    }\n}\n",
      "Python3": "#!/usr/bin/env python3\nimport sys\n\ndef solve():\n    data = sys.stdin.read().strip().split()\n    if not data:\n        return\n    it = iter(data)\n    T = int(next(it))\n    out = []\n    for _ in range(T):\n        N = int(next(it))\n        freq = {}\n        max_freq = 0\n        for _ in range(N):\n            a = int(next(it))\n            cnt = freq.get(a, 0) + 1\n            freq[a] = cnt\n            if cnt > max_freq:\n                max_freq = cnt\n        if max_freq == N:\n            out.append('0')\n        elif max_freq == 1:\n            out.append('-1')\n        else:\n            out.append(str(N - max_freq + 1))\n    sys.stdout.write('\\n'.join(out))\n\nif __name__ == \"__main__\":\n    solve()\n",
      "JavaScript": "const fs = require('fs');\nconst input = fs.readFileSync(0, 'utf-8').trim().split(/\\s+/);\nlet idx = 0;\nconst T = Number(input[idx++]);\nlet out = [];\nfor (let t = 0; t < T; t++) {\n  const N = Number(input[idx++]);\n  const freq = new Map();\n  let maxFreq = 0;\n  for (let i = 0; i < N; i++) {\n    const a = Number(input[idx++]);\n    const cnt = (freq.get(a) || 0) + 1;\n    freq.set(a, cnt);\n    if (cnt > maxFreq) maxFreq = cnt;\n  }\n  if (maxFreq === N) out.push('0');\n  else if (maxFreq === 1) out.push('-1');\n  else out.push(String(N - maxFreq + 1));\n}\nconsole.log(out.join('\\n'));\n"
    }
  },
  "QBCategory": "CCD",
  "boilerPlates": {
    "C": "#include <stdio.h>\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "C++": "#include <iostream>\nusing namespace std;\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "Java": "import java.util.*;\nimport java.io.*;\n\npublic class Main {\n    public static void main(String[] args) throws IOException {\n        // your code goes here\n    }\n}",
    "Python3": "# your code goes here\n",
    "JavaScript": "// your code goes here\n"
  },
  "_internal": {
    "methodName": "",
    "params": []
  }
}