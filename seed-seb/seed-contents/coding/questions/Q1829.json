{
  "questionId": "Q1829",
  "title": "Job Sequencing Problem",
  "slug": "job-sequencing-problem",
  "version": 1,
  "createdAt": "2026-07-01",
  "updatedAt": "2026-07-04",
  "metadata": {
    "category": "Greedy",
    "difficulty": "Hard",
    "isPremium": false,
    "tags": [
      "Deadline Scheduling",
      "Greedy Algorithm",
      "Greedy",
      "Reward Maximization"
    ],
    "companyTags": [],
    "topicTags": [
      "Disjoint Set Union"
    ]
  },
  "content": {
    "problemStatement": "Chef is managing a busy workshop where multiple **taskDeadlinestaskDeadlinestaskDeadlines** and **taskRewardstaskRewardstaskRewards** determine how work gets scheduled.\n\nEach task requires exactly **one unit of time**, and only **one task can be executed at a time slot**.\n\nHowever, every task must be finished **on or before its deadline** to earn its reward.\n\nChef wants to plan the schedule in a way that:\n\n\nMaximizes the **total number of completed tasks**\nAchieves the **highest possible reward**\n**NOTE:** During conflicts, tasks with higher rewards are prioritized over lower-reward tasks.\n\nYour job is to help Chef build the most efficient schedule.\n\nFunction Declaration\n\n**Function Name**\n\n\n\nscheduleTasksscheduleTasksscheduleTasks\n\n\n\n**Parameters**\n\n\n\ntaskDeadlinestaskDeadlinestaskDeadlines: An array of integers where each value represents the deadline of a task.\ntaskRewardstaskRewardstaskRewards: An array of integers where each value represents the reward earned upon completing the corresponding task.\n\n\n\n**Return Value**\n\n\n\nReturns a list of two integers:\n\nThe maximum number of tasks completed.\nThe total reward earned.\n\n\n\n\nConstraints\n1\u2264N\u22641051 \\le N \\le 10^51\u2264N\u2264105\n1\u2264taskDeadlines[i]\u2264N1 \\le taskDeadlines[i] \\le N1\u2264taskDeadlines[i]\u2264N\n1\u2264taskRewards[i]\u22645001 \\le taskRewards[i] \\le 5001\u2264taskRewards[i]\u2264500\nInput Format\nThe first line contains an integer NNN \u2014 the number of tasks.\nThe second line contains NNN space-separated integers representing the taskDeadlinestaskDeadlinestaskDeadlines.\nThe third line contains NNN space-separated integers representing the taskRewardstaskRewardstaskRewards.\nOutput Format\nPrint two space-separated integers:\n\nMaximum tasks completed\nMaximum reward earned",
    "constraints": [
      "1 <= T <= 10",
      "1 <= N <= 10^5",
      "1 <= taskDeadlines[i] <= N",
      "1 <= taskRewards[i] <= 500"
    ],
    "inputFormat": "The first line contains an integer T, the number of test cases. Each test case contains three lines: the first line contains an integer N, the number of tasks. The second line contains N space-separated integers representing the task deadlines. The third line contains N space-separated integers representing the task rewards.",
    "outputFormat": "For each test case, print two space-separated integers: the maximum number of tasks that can be completed and the maximum total reward obtained.",
    "sampleTestCases": [
      {
        "id": "sample_1",
        "input": "4\n4 1 1 1\n20 10 40 30",
        "output": "2 60",
        "explanation": "Schedule the task with reward 40 at slot 1 since it has the highest reward among tasks with deadline 1. Tasks with rewards 30 and 10 cannot be scheduled due to slot conflict. The task with reward 20 fits in a later available slot."
      },
      {
        "id": "sample_2",
        "input": "3\n2 2 2\n10 20 30",
        "output": "2 50",
        "explanation": "Schedule the tasks with rewards 30 and 20 at slots 1 and 2 respectively, since they have the highest rewards among tasks with deadline 2."
      },
      {
        "id": "sample_3",
        "input": "5\n1 2 3 4 5\n10 20 30 40 50",
        "output": "5 150",
        "explanation": "Schedule each task at its deadline, since there are no conflicts."
      }
    ]
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "label": "minimum boundary",
        "input": "1\n1\n1\n1",
        "expectedOutput": "1 1",
        "weight": 2
      },
      {
        "id": "tc_2",
        "label": "maximum boundary",
        "input": "1\n10\n10 10 10 10 10 10 10 10 10 10\n10 10 10 10 10 10 10 10 10 10",
        "expectedOutput": "10 100",
        "weight": 2
      },
      {
        "id": "tc_3",
        "label": "uniform/edge case",
        "input": "1\n5\n1 1 1 1 1\n10 10 10 10 10",
        "expectedOutput": "1 10",
        "weight": 2
      },
      {
        "id": "tc_4",
        "label": "tricky case",
        "input": "1\n5\n2 2 2 2 2\n10 20 30 40 50",
        "expectedOutput": "2 90",
        "weight": 2
      },
      {
        "id": "tc_5",
        "label": "stress/general",
        "input": "2\n5\n1 2 3 4 5\n10 20 30 40 50\n5\n2 2 2 2 2\n10 20 30 40 50",
        "expectedOutput": "5 150\n2 90",
        "weight": 2
      }
    ],
    "totalTestCases": 5,
    "totalWeight": 10
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      2,
      2,
      2,
      2,
      2
    ],
    "maxScore": 10
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "The problem can be solved using a greedy algorithm. First, we sort the tasks based on their rewards in descending order. Then, we iterate over the sorted tasks and try to schedule each task at its deadline. If the deadline is available, we schedule the task and update the deadline. If the deadline is not available, we try to schedule the task at the earliest available time slot before its deadline. We keep track of the maximum number of tasks that can be completed and the maximum total reward obtained.",
    "timeComplexity": "O(T * N log N) per test case, O(T * N log N) total. The time complexity is dominated by the sorting of tasks.",
    "spaceComplexity": "O(N) \u2014 the dominant structure is the array of tasks.",
    "code": {
      "C": "#include <stdio.h>\n#include <stdlib.h>\n\ntypedef struct {int reward; int deadline;} Task;\n\nint compareDesc(const void *a, const void *b) {\n    const Task *ta = (const Task *)a;\n    const Task *tb = (const Task *)b;\n    return tb->reward - ta->reward; // descending reward\n}\n\nint find(int x, int parent[]) {\n    if (parent[x] == x) return x;\n    return parent[x] = find(parent[x], parent);\n}\n\nint main() {\n    int T; if (scanf(\"%d\", &T)!=1) return 0;\n    while (T--) {\n        int N; scanf(\"%d\", &N);\n        int *deadlines = (int*)malloc(sizeof(int)*N);\n        int *rewards   = (int*)malloc(sizeof(int)*N);\n        for (int i=0;i<N;i++) scanf(\"%d\", &deadlines[i]);\n        for (int i=0;i<N;i++) scanf(\"%d\", &rewards[i]);\n        Task *tasks = (Task*)malloc(sizeof(Task)*N);\n        int maxD = 0;\n        for (int i=0;i<N;i++) {\n            tasks[i].reward = rewards[i];\n            tasks[i].deadline = deadlines[i];\n            if (deadlines[i] > maxD) maxD = deadlines[i];\n        }\n        qsort(tasks, N, sizeof(Task), compareDesc);\n        int *parent = (int*)malloc(sizeof(int)*(maxD+1));\n        for (int i=0;i<=maxD;i++) parent[i]=i;\n        int cnt=0, total=0;\n        for (int i=0;i<N;i++) {\n            int slot = find(tasks[i].deadline, parent);\n            if (slot>0) {\n                parent[slot] = find(slot-1, parent);\n                cnt++; total += tasks[i].reward;\n            }\n        }\n        printf(\"%d %d\\n\", cnt, total);\n        free(deadlines); free(rewards); free(tasks); free(parent);\n    }\n    return 0;\n}",
      "C++": "#include <bits/stdc++.h>\nusing namespace std;\n\nint findSet(int v, vector<int>& parent) {\n    if (parent[v]==v) return v;\n    return parent[v]=findSet(parent[v], parent);\n}\n\nint main(){\n    ios::sync_with_stdio(false);\n    cin.tie(nullptr);\n    int T; if(!(cin>>T)) return 0;\n    while(T--){\n        int N; cin>>N;\n        vector<int> deadline(N), reward(N);\n        for(int &d:deadline) cin>>d;\n        for(int &r:reward) cin>>r;\n        vector<pair<int,int>> tasks(N);\n        int maxD=0;\n        for(int i=0;i<N;i++){\n            tasks[i]={reward[i], deadline[i]};\n            maxD=max(maxD, deadline[i]);\n        }\n        sort(tasks.begin(), tasks.end(), greater<>()); // by reward descending\n        vector<int> parent(maxD+1);\n        iota(parent.begin(), parent.end(), 0);\n        int cnt=0, total=0;\n        for(auto &t:tasks){\n            int slot=findSet(t.second, parent);\n            if(slot>0){\n                parent[slot]=findSet(slot-1, parent);\n                cnt++; total+=t.first;\n            }\n        }\n        cout<<cnt<<' '<<total<<\"\\n\";\n    }\n    return 0;\n}",
      "Java": "import java.io.*;\nimport java.util.*;\n\npublic class Main {\n    private static int find(int x, int[] parent) {\n        if (parent[x] == x) return x;\n        return parent[x] = find(parent[x], parent);\n    }\n    public static void main(String[] args) throws Exception {\n        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));\n        StringTokenizer st;\n        int T = Integer.parseInt(br.readLine().trim());\n        StringBuilder sb = new StringBuilder();\n        while (T-- > 0) {\n            int N = Integer.parseInt(br.readLine().trim());\n            int[] deadline = new int[N];\n            int[] reward = new int[N];\n            st = new StringTokenizer(br.readLine());\n            for (int i = 0; i < N; i++) deadline[i] = Integer.parseInt(st.nextToken());\n            st = new StringTokenizer(br.readLine());\n            for (int i = 0; i < N; i++) reward[i] = Integer.parseInt(st.nextToken());\n            int[][] tasks = new int[N][2]; // {reward, deadline}\n            int maxD = 0;\n            for (int i = 0; i < N; i++) {\n                tasks[i][0] = reward[i];\n                tasks[i][1] = deadline[i];\n                if (deadline[i] > maxD) maxD = deadline[i];\n            }\n            Arrays.sort(tasks, (a, b) -> b[0] - a[0]); // descending reward\n            int[] parent = new int[maxD + 1];\n            for (int i = 0; i <= maxD; i++) parent[i] = i;\n            int cnt = 0, total = 0;\n            for (int[] t : tasks) {\n                int slot = find(t[1], parent);\n                if (slot > 0) {\n                    parent[slot] = find(slot - 1, parent);\n                    cnt++; total += t[0];\n                }\n            }\n            sb.append(cnt).append(' ').append(total).append('\\n');\n        }\n        System.out.print(sb.toString());\n    }\n}",
      "Python3": "import sys\n\ndef find(x, parent):\n    while parent[x] != x:\n        parent[x] = parent[parent[x]]\n        x = parent[x]\n    return x\n\ndef solve():\n    data = list(map(int, sys.stdin.read().strip().split()))\n    if not data:\n        return\n    it = iter(data)\n    T = next(it)\n    out_lines = []\n    for _ in range(T):\n        N = next(it)\n        deadlines = [next(it) for _ in range(N)]\n        rewards = [next(it) for _ in range(N)]\n        tasks = sorted(zip(rewards, deadlines), reverse=True)\n        max_dead = max(deadlines)\n        parent = list(range(max_dead + 1))\n        def find_rec(x):\n            if parent[x] != x:\n                parent[x] = find_rec(parent[x])\n            return parent[x]\n        cnt = 0\n        total = 0\n        for r, d in tasks:\n            slot = find_rec(d)\n            if slot > 0:\n                parent[slot] = find_rec(slot - 1)\n                cnt += 1\n                total += r\n        out_lines.append(f\"{cnt} {total}\")\n    sys.stdout.write(\"\\n\".join(out_lines))\n\nif __name__ == \"__main__\":\n    solve()",
      "JavaScript": "const fs = require('fs');\nconst input = fs.readFileSync(0, 'utf-8').trim().split(/\\s+/).map(Number);\nlet idx = 0;\nconst T = input[idx++];\nlet outputs = [];\nfor (let t = 0; t < T; t++) {\n  const N = input[idx++];\n  const deadlines = new Array(N);\n  const rewards = new Array(N);\n  for (let i = 0; i < N; i++) deadlines[i] = input[idx++];\n  for (let i = 0; i < N; i++) rewards[i] = input[idx++];\n  const tasks = [];\n  let maxD = 0;\n  for (let i = 0; i < N; i++) {\n    tasks.push([rewards[i], deadlines[i]]);\n    if (deadlines[i] > maxD) maxD = deadlines[i];\n  }\n  tasks.sort((a, b) => b[0] - a[0]); // descending reward\n  const parent = new Array(maxD + 1);\n  for (let i = 0; i <= maxD; i++) parent[i] = i;\n  const find = (x) => {\n    if (parent[x] !== x) parent[x] = find(parent[x]);\n    return parent[x];\n  };\n  let cnt = 0, total = 0;\n  for (const [r, d] of tasks) {\n    let slot = find(d);\n    if (slot > 0) {\n      parent[slot] = find(slot - 1);\n      cnt++; total += r;\n    }\n  }\n  outputs.push(cnt + ' ' + total);\n}\nconsole.log(outputs.join('\\n'));"
    }
  }
}