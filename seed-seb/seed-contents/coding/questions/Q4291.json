{
  "questionId": "Q4291",
  "title": "Height Balanced Binary Tree",
  "slug": "height-balanced-binary-tree",
  "version": 1,
  "createdAt": "2026-07-01",
  "updatedAt": "2026-07-04",
  "metadata": {
    "category": "Graph",
    "difficulty": "Medium",
    "isPremium": true,
    "tags": [
      "Tree",
      "Height-Balanced Tree"
    ],
    "companyTags": [],
    "topicTags": [
      "Trees"
    ],
    "QBCategory": "CCD"
  },
  "content": {
    "problemStatement": "Given a connected binary tree with **N** nodes, determine if it is height-balanced. (**Note:** A binary tree is height-balanced if the depth of the two subtrees of every node never differs by more than one.)\n\nFor example, the following binary tree is height balanced:\n\n\n\n\nInput Format\n\nThe first line of the input contains a single integer NNN — the number of nodes in the binary tree.\nNext N−1N - 1N−1 lines contain three space separated characters pi,ci,Rip_i, c_i, R_ipi​,ci​,Ri​, describing the tree with edge informations - pip_ipi​ is an integer denoting the parent node of the iiith edge, cic_ici​ is child node, and the letter RiR_iRi​ denotes whether the child is left child or right child, it's value is `L` if cic_ici​ is left child of pip_ipi​, else `R`.\n\n\nOutput Format\nOutput on the single line, `YES` if the tree is height balanced, else `NO`.",
    "constraints": [
      "1 <= T <= 100",
      "1 <= N <= 10000",
      "1 <= pi, ci <= N",
      "Ri = 'L' or 'R'",
      "All pi's and ci's are distinct"
    ],
    "inputFormat": "The first line contains a single integer T, the number of test cases. Each test case contains an integer N, the number of nodes in the binary tree, followed by N-1 lines containing three space-separated characters pi, ci, Ri, describing the tree with edge information.",
    "outputFormat": "For each test case, print YES if the tree is height-balanced, else print NO.",
    "sampleTestCases": [
      {
        "id": "sample_1",
        "input": "1\n8\n6 1 L\n7 6 L\n7 8 R\n10 7 L\n10 14 R\n14 13 L\n14 15 R",
        "output": "YES",
        "explanation": "The given binary tree is height-balanced."
      },
      {
        "id": "sample_2",
        "input": "1\n3\n1 2 L\n1 3 R",
        "output": "YES",
        "explanation": "The given binary tree is height-balanced."
      },
      {
        "id": "sample_3",
        "input": "1\n4\n1 2 L\n1 3 R\n2 4 L",
        "output": "NO",
        "explanation": "The given binary tree is not height-balanced."
      }
    ]
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "label": "minimum boundary",
        "input": "1\n1",
        "expectedOutput": "YES",
        "weight": 2
      },
      {
        "id": "tc_2",
        "label": "maximum boundary",
        "input": "1\n10000\n1 2 L\n1 3 R\n2 4 L\n2 5 R\n3 6 L\n3 7 R",
        "expectedOutput": "NO",
        "weight": 2
      },
      {
        "id": "tc_3",
        "label": "uniform/edge case",
        "input": "1\n5\n1 2 L\n1 3 R\n2 4 L\n2 5 R",
        "expectedOutput": "NO",
        "weight": 2
      },
      {
        "id": "tc_4",
        "label": "tricky case",
        "input": "1\n10\n1 2 L\n1 3 R\n2 4 L\n2 5 R\n3 6 L\n3 7 R\n4 8 L\n4 9 L\n4 10 R",
        "expectedOutput": "NO",
        "weight": 2
      },
      {
        "id": "tc_5",
        "label": "stress/general",
        "input": "5\n10\n1 2 L\n1 3 R\n2 4 L\n2 5 R\n3 6 L\n3 7 R\n4 8 L\n4 9 L\n4 10 R\n10\n1 2 L\n1 3 R\n2 4 L\n2 5 R\n3 6 L\n3 7 R\n4 8 L\n4 9 L\n4 10 R\n10\n1 2 L\n1 3 R\n2 4 L\n2 5 R\n3 6 L\n3 7 R\n4 8 L\n4 9 L\n4 10 R\n10\n1 2 L\n1 3 R\n2 4 L\n2 5 R\n3 6 L\n3 7 R\n4 8 L\n4 9 L\n4 10 R\n10\n1 2 L\n1 3 R\n2 4 L\n2 5 R\n3 6 L\n3 7 R\n4 8 L\n4 9 L\n4 10 R",
        "expectedOutput": "NO\nNO\nNO\nNO\nNO",
        "weight": 2
      }
    ],
    "totalTestCases": 5,
    "totalWeight": 10
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      2,
      2,
      2,
      2,
      2
    ],
    "maxScore": 10
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "To determine if a binary tree is height-balanced, we can use a recursive approach to calculate the height of each subtree and check if the difference in heights is greater than 1. We start by creating a tree from the given edges and then use a recursive function to calculate the height of each subtree.",
    "timeComplexity": "O(T * N) per test case, O(T * N) total, where N is the number of nodes in the tree.",
    "spaceComplexity": "O(N) — the dominant structure is the tree itself.",
    "code": {
      "C": "#include <stdio.h>\n#include <stdlib.h>\n\n// Define the structure for a tree node\ntypedef struct Node {\n    int val;\n    struct Node* left;\n    struct Node* right;\n} Node;\n\n// Function to create a new node\nNode* newNode(int key) {\n    Node* node = (Node*) malloc(sizeof(Node));\n    node->val = key;\n    node->left = node->right = NULL;\n    return node;\n}\n\n// Function to get the height of a tree\nint getHeight(Node* root) {\n    if (root == NULL)\n        return 0;\n\n    int leftHeight = getHeight(root->left);\n    if (leftHeight == -1)\n        return -1;\n\n    int rightHeight = getHeight(root->right);\n    if (rightHeight == -1)\n        return -1;\n\n    if (abs(leftHeight - rightHeight) > 1)\n        return -1;\n\n    return 1 + (leftHeight > rightHeight ? leftHeight : rightHeight);\n}\n\n// Function to check if a tree is height-balanced\nint isHeightBalanced(Node* root) {\n    return getHeight(root) != -1;\n}\n\nint main() {\n    int T;\n    scanf(\"%d\", &T);\n\n    for (int t = 0; t < T; t++) {\n        int N;\n        scanf(\"%d\", &N);\n\n        Node* nodes[N];\n        for (int i = 0; i < N; i++) {\n            nodes[i] = newNode(i + 1);\n        }\n\n        for (int i = 0; i < N - 1; i++) {\n            int pi, ci;\n            char Ri;\n            scanf(\"%d %d %c\", &pi, &ci, &Ri);\n            pi--; ci--;\n            if (Ri == 'L') {\n                nodes[pi]->left = nodes[ci];\n            } else {\n                nodes[pi]->right = nodes[ci];\n            }\n        }\n\n        if (isHeightBalanced(nodes[0])) {\n            printf(\"YES\\n\");\n        } else {\n            printf(\"NO\\n\");\n        }\n    }\n\n    return 0;\n}",
      "C++": "#include <iostream>\nusing namespace std;\n\n// Define the structure for a tree node\nstruct Node {\n    int val;\n    Node* left;\n    Node* right;\n};\n\n// Function to create a new node\nNode* newNode(int key) {\n    Node* node = new Node();\n    node->val = key;\n    node->left = node->right = NULL;\n    return node;\n}\n\n// Function to get the height of a tree\nint getHeight(Node* root) {\n    if (root == NULL)\n        return 0;\n\n    int leftHeight = getHeight(root->left);\n    if (leftHeight == -1)\n        return -1;\n\n    int rightHeight = getHeight(root->right);\n    if (rightHeight == -1)\n        return -1;\n\n    if (abs(leftHeight - rightHeight) > 1)\n        return -1;\n\n    return 1 + max(leftHeight, rightHeight);\n}\n\n// Function to check if a tree is height-balanced\nbool isHeightBalanced(Node* root) {\n    return getHeight(root) != -1;\n}\n\nint main() {\n    ios::sync_with_stdio(false);\n    cin.tie(NULL);\n    int T;\n    cin >> T;\n\n    for (int t = 0; t < T; t++) {\n        int N;\n        cin >> N;\n\n        Node* nodes[N];\n        for (int i = 0; i < N; i++) {\n            nodes[i] = newNode(i + 1);\n        }\n\n        for (int i = 0; i < N - 1; i++) {\n            int pi, ci;\n            char Ri;\n            cin >> pi >> ci >> Ri;\n            pi--; ci--;\n            if (Ri == 'L') {\n                nodes[pi]->left = nodes[ci];\n            } else {\n                nodes[pi]->right = nodes[ci];\n            }\n        }\n\n        if (isHeightBalanced(nodes[0])) {\n            cout << \"YES\" << endl;\n        } else {\n            cout << \"NO\" << endl;\n        }\n    }\n\n    return 0;\n}",
      "Java": "import java.util.Scanner;\n\n// Define the class for a tree node\nclass Node {\n    int val;\n    Node left;\n    Node right;\n\n    public Node(int key) {\n        val = key;\n        left = right = null;\n    }\n}\n\npublic class Main {\n    // Function to get the height of a tree\n    public static int getHeight(Node root) {\n        if (root == null)\n            return 0;\n\n        int leftHeight = getHeight(root.left);\n        if (leftHeight == -1)\n            return -1;\n\n        int rightHeight = getHeight(root.right);\n        if (rightHeight == -1)\n            return -1;\n\n        if (Math.abs(leftHeight - rightHeight) > 1)\n            return -1;\n\n        return 1 + Math.max(leftHeight, rightHeight);\n    }\n\n    // Function to check if a tree is height-balanced\n    public static boolean isHeightBalanced(Node root) {\n        return getHeight(root) != -1;\n    }\n\n    public static void main(String[] args) {\n        Scanner scanner = new Scanner(System.in);\n        int T = scanner.nextInt();\n\n        for (int t = 0; t < T; t++) {\n            int N = scanner.nextInt();\n\n            Node[] nodes = new Node[N];\n            for (int i = 0; i < N; i++) {\n                nodes[i] = new Node(i + 1);\n            }\n\n            for (int i = 0; i < N - 1; i++) {\n                int pi = scanner.nextInt() - 1;\n                int ci = scanner.nextInt() - 1;\n                char Ri = scanner.next().charAt(0);\n                if (Ri == 'L') {\n                    nodes[pi].left = nodes[ci];\n                } else {\n                    nodes[pi].right = nodes[ci];\n                }\n            }\n\n            if (isHeightBalanced(nodes[0])) {\n                System.out.println(\"YES\");\n            } else {\n                System.out.println(\"NO\");\n            }\n        }\n    }\n}",
      "Python3": "import sys\n\nclass Node:\n    def __init__(self, key):\n        self.val = key\n        self.left = None\n        self.right = None\n\n# Function to get the height of a tree\ndef getHeight(root):\n    if root is None:\n        return 0\n\n    leftHeight = getHeight(root.left)\n    if leftHeight == -1:\n        return -1\n\n    rightHeight = getHeight(root.right)\n    if rightHeight == -1:\n        return -1\n\n    if abs(leftHeight - rightHeight) > 1:\n        return -1\n\n    return 1 + max(leftHeight, rightHeight)\n\n# Function to check if a tree is height-balanced\ndef isHeightBalanced(root):\n    return getHeight(root) != -1\n\n# Read input\nT = int(sys.stdin.readline())\n\nfor t in range(T):\n    N = int(sys.stdin.readline())\n\n    # Create nodes\n    nodes = [Node(i + 1) for i in range(N)]\n\n    # Read edges and construct the tree\n    for _ in range(N - 1):\n        pi, ci, Ri = sys.stdin.readline().split()\n        pi, ci = int(pi) - 1, int(ci) - 1\n        if Ri == 'L':\n            nodes[pi].left = nodes[ci]\n        else:\n            nodes[pi].right = nodes[ci]\n\n    # Check if the tree is height-balanced\n    if isHeightBalanced(nodes[0]):\n        print(\"YES\")\n    else:\n        print(\"NO\")",
      "JavaScript": "const lines = require('fs').readFileSync(0, 'utf-8').trim().split('\\n');\nlet i = 0;\n\n// Define the class for a tree node\nclass Node {\n    constructor(key) {\n        this.val = key;\n        this.left = null;\n        this.right = null;\n    }\n}\n\n// Function to get the height of a tree\nfunction getHeight(root) {\n    if (root === null)\n        return 0;\n\n    let leftHeight = getHeight(root.left);\n    if (leftHeight === -1)\n        return -1;\n\n    let rightHeight = getHeight(root.right);\n    if (rightHeight === -1)\n        return -1;\n\n    if (Math.abs(leftHeight - rightHeight) > 1)\n        return -1;\n\n    return 1 + Math.max(leftHeight, rightHeight);\n}\n\n// Function to check if a tree is height-balanced\nfunction isHeightBalanced(root) {\n    return getHeight(root) !== -1;\n}\n\n// Read input\nlet T = parseInt(lines[i++]);\n\nfor (let t = 0; t < T; t++) {\n    let N = parseInt(lines[i++]);\n\n    // Create nodes\n    let nodes = [];\n    for (let j = 0; j < N; j++) {\n        nodes.push(new Node(j + 1));\n    }\n\n    // Read edges and construct the tree\n    for (let j = 0; j < N - 1; j++) {\n        let [pi, ci, Ri] = lines[i++].split(' ');\n        pi = parseInt(pi) - 1;\n        ci = parseInt(ci) - 1;\n        if (Ri === 'L') {\n            nodes[pi].left = nodes[ci];\n        } else {\n            nodes[pi].right = nodes[ci];\n        }\n    }\n\n    // Check if the tree is height-balanced\n    if (isHeightBalanced(nodes[0])) {\n        console.log('YES');\n    } else {\n        console.log('NO');\n    }\n}"
    }
  },
  "QBCategory": "CCD",
  "boilerPlates": {
    "C": "#include <stdio.h>\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "C++": "#include <iostream>\nusing namespace std;\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "Java": "import java.util.*;\nimport java.io.*;\n\npublic class Main {\n    public static void main(String[] args) throws IOException {\n        // your code goes here\n    }\n}",
    "Python3": "# your code goes here\n",
    "JavaScript": "// your code goes here\n"
  },
  "_internal": {
    "methodName": "",
    "params": []
  }
}