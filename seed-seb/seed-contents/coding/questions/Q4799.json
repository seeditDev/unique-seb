{
  "questionId": "Q4799",
  "title": "Chef Finds Cheapest Flights",
  "slug": "chef-finds-cheapest-flights",
  "version": 1,
  "createdAt": "2026-07-01",
  "updatedAt": "2026-07-03",
  "metadata": {
    "category": "Graph | Dynamic Programming",
    "difficulty": "Hard",
    "isPremium": true,
    "tags": [
      "Dijkstra's Algorithm",
      "Shortest Path",
      "Flight Routes"
    ],
    "companyTags": [],
    "topicTags": [
      "Graphs"
    ],
    "QBCategory": "CCD"
  },
  "content": {
    "problemStatement": "Chef has cityCountcityCountcityCount cities connected by several flights. Each flight connects one city to another with a certain cost. Chef wants to travel from a starting city to a destination city, but he wants to minimize the total cost of his journey. \n\nHowever, Chef has a strict rule: he can make at most maxStopsmaxStopsmaxStops stops along the way (excluding the starting city). Help Chef find the lowest possible cost to reach his destination city from the source city without exceeding the allowed number of stops. \n\nIf it is impossible for Chef to reach the destination under these conditions, help him by returning −1-1−1.\n\nFunction DeclarationFunction NamefindLowestFlightCostfindLowestFlightCostfindLowestFlightCost - This function calculates the lowest cost to travel from a source city to a destination city with up to a maximum number of stops.\n\nParameters\ncityCountcityCountcityCount : The total number of cities, numbered from 000 to cityCount−1cityCount - 1cityCount−1.\nflightListflightListflightList : Each element represents a flight as `[sourceCity, destinationCity, price]`.\nsourceCitysourceCitysourceCity : The starting city for the journey.\ndestinationCitydestinationCitydestinationCity : The target city to reach.\nmaxStopsmaxStopsmaxStops : The maximum number of stops allowed between source and destination.\nReturn Value\nReturns an integer representing the minimum cost to reach the destination city from the source city within maxStopsmaxStopsmaxStops.\nIf no such route exists, returns −1-1−1.\nConstraints\n2≤cityCount≤1002 \\leq cityCount \\leq 1002≤cityCount≤100\n0≤flightCount≤cityCount×(cityCount−1)20 \\leq flightCount \\leq \\frac{cityCount \\times (cityCount - 1)}{2}0≤flightCount≤2cityCount×(cityCount−1)​\nEach `flightList[i]` has exactly 3 integers: [sourceCity,destinationCity,price][sourceCity, destinationCity, price][sourceCity,destinationCity,price]\n0≤sourceCity,destinationCity&lt;cityCount0 \\leq sourceCity, destinationCity &lt; cityCount0≤sourceCity,destinationCity&lt;cityCount\nsourceCity≠destinationCitysourceCity \\neq destinationCitysourceCity=destinationCity\n1≤price≤1041 \\leq price \\leq 10^41≤price≤104\nThere are no multiple flights between the same pair of cities.\n0≤maxStops&lt;cityCount0 \\leq maxStops &lt; cityCount0≤maxStops&lt;cityCount\n\nInput Format\n\nThe first line contains a single integer TTT — the number of test cases.\nEach test case consists of:\n\nThe first line contains the integer cityCountcityCountcityCount — the number of cities.\nSecond line → flightList. Each flight represented as [sourceCity, destinationCity, price]\nThe last three lines of each test case contains three integers: sourceCitysourceCitysourceCity, destinationCitydestinationCitydestinationCity, and maxStopsmaxStopsmaxStops.\n\n\n\n\nOutput Format\n\nFor each test case, print a single integer — the minimum cost for Chef to travel from the source city to the destination city with at most maxStopsmaxStopsmaxStops stops.\nIf no such route exists, print −1-1−1.",
    "constraints": [
      "2 ≤ cityCount ≤ 100",
      "0 ≤ flightCount ≤ cityCount × (cityCount - 1) / 2",
      "Each flightList[i] has exactly 3 integers: [sourceCity, destinationCity, price]",
      "0 ≤ sourceCity, destinationCity < cityCount",
      "sourceCity ≠ destinationCity",
      "1 ≤ price ≤ 10^4",
      "0 ≤ maxStops < cityCount"
    ],
    "inputFormat": "The first line contains a single integer T — the number of test cases. Each test case consists of: The first line contains the integer cityCount — the number of cities. Second line → flightList. Each flight represented as [sourceCity, destinationCity, price]. The last three lines of each test case contains three integers: sourceCity, destinationCity, and maxStops.",
    "outputFormat": "For each test case, print a single integer — the minimum cost for Chef to travel from the source city to the destination city with at most maxStops stops. If no such route exists, print −1.",
    "sampleTestCases": [
      {
        "id": "sample_1",
        "input": "2\n5\n[[0, 1, 300], [1, 2, 200], [2, 3, 400], [3, 4, 100], [0, 4, 1000]]\n0\n4\n2\n3\n[[0, 1, 150], [1, 2, 150], [0, 2, 400]]\n0\n2\n0",
        "output": "1000\n400",
        "explanation": "Explanation:\nFor the first test case, the cheapest flight from 0 to 4 with up to 2 stops is the direct flight costing 1000, since any route with stops exceeds the allowed number or is more expensive.\nFor the second test case, the cheapest flight from 0 to 2 with up to 0 stops is the direct flight costing 400, as no stops are allowed and the direct flight is the only option."
      }
    ]
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "label": "Case 1",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_2",
        "label": "Case 2",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_3",
        "label": "Case 3",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_4",
        "label": "Case 4",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_5",
        "label": "Case 5",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      }
    ],
    "totalTestCases": 5,
    "totalWeight": 10
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      2,
      2,
      2,
      2,
      2
    ],
    "maxScore": 10
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "To solve this problem, we can use a modified version of Dijkstra's algorithm to find the shortest path from the source city to the destination city with at most maxStops stops. We will maintain a priority queue of cities to visit, where the priority is the minimum cost to reach that city. We will also keep track of the number of stops made to reach each city.",
    "timeComplexity": "O(T * (cityCount + flightCount * log(cityCount))) — where T is the number of test cases, cityCount is the number of cities, and flightCount is the number of flights.",
    "spaceComplexity": "O(cityCount + flightCount) — for storing the graph and the priority queue.",
    "code": {
      "C": "#include <stdio.h>\n#include <stdlib.h>\n#include <limits.h>\n#define MAX_CITY_COUNT 100\n#define MAX_FLIGHT_COUNT 5000\n#define MAX_PRICE 10000\n#define MAX_STOPS 100\n\n// Define the structure for a flight\ntypedef struct {\n    int sourceCity;\n    int destinationCity;\n    int price;\n} Flight;\n\n// Define the structure for a city\ntypedef struct {\n    int city;\n    int cost;\n    int stops;\n} City;\n\n// Compare function for the priority queue\nint compare(const void *a, const void *b) {\n    City *cityA = (City *)a;\n    City *cityB = (City *)b;\n    if (cityA->cost < cityB->cost) return -1;\n    if (cityA->cost > cityB->cost) return 1;\n    return 0;\n}\n\n// Function to find the lowest flight cost\nint findLowestFlightCost(int cityCount, Flight *flightList, int flightCount, int sourceCity, int destinationCity, int maxStops) {\n    // Create a graph from the flight list\n    int **graph = (int **)malloc(cityCount * sizeof(int *));\n    for (int i = 0; i < cityCount; i++) {\n        graph[i] = (int *)malloc(cityCount * sizeof(int));\n        for (int j = 0; j < cityCount; j++) {\n            graph[i][j] = -1;\n        }\n    }\n    for (int i = 0; i < flightCount; i++) {\n        graph[flightList[i].sourceCity][flightList[i].destinationCity] = flightList[i].price;\n    }\n\n    // Create a priority queue to store the cities to visit\n    City *priorityQueue = (City *)malloc(cityCount * sizeof(City));\n    int priorityQueueSize = 0;\n\n    // Add the source city to the priority queue\n    priorityQueue[priorityQueueSize].city = sourceCity;\n    priorityQueue[priorityQueueSize].cost = 0;\n    priorityQueue[priorityQueueSize].stops = 0;\n    priorityQueueSize++;\n\n    // Loop until the priority queue is empty\n    while (priorityQueueSize > 0) {\n        // Sort the priority queue\n        qsort(priorityQueue, priorityQueueSize, sizeof(City), compare);\n\n        // Get the city with the minimum cost\n        City currentCity = priorityQueue[0];\n        priorityQueueSize--;\n        for (int i = 0; i < priorityQueueSize; i++) {\n            priorityQueue[i] = priorityQueue[i + 1];\n        }\n\n        // If the current city is the destination city, return the cost\n        if (currentCity.city == destinationCity) {\n            return currentCity.cost;\n        }\n\n        // If the number of stops exceeds the maximum, skip this city\n        if (currentCity.stops > maxStops) {\n            continue;\n        }\n\n        // Add the neighboring cities to the priority queue\n        for (int i = 0; i < cityCount; i++) {\n            if (graph[currentCity.city][i] != -1) {\n                City newCity;\n                newCity.city = i;\n                newCity.cost = currentCity.cost + graph[currentCity.city][i];\n                newCity.stops = currentCity.stops + 1;\n                priorityQueue[priorityQueueSize] = newCity;\n                priorityQueueSize++;\n            }\n        }\n    }\n\n    // If no path is found, return -1\n    return -1;\n}\n\nint main() {\n    int T;\n    scanf(\"%d\", &T);\n    for (int t = 0; t < T; t++) {\n        int cityCount;\n        scanf(\"%d\", &cityCount);\n        int flightCount = 0;\n        Flight *flightList = (Flight *)malloc(MAX_FLIGHT_COUNT * sizeof(Flight));\n        for (int i = 0; i < cityCount; i++) {\n            for (int j = 0; j < cityCount; j++) {\n                int price;\n                scanf(\"%d\", &price);\n                if (price != -1) {\n                    flightList[flightCount].sourceCity = i;\n                    flightList[flightCount].destinationCity = j;\n                    flightList[flightCount].price = price;\n                    flightCount++;\n                }\n            }\n        }\n        int sourceCity, destinationCity, maxStops;\n        scanf(\"%d %d %d\", &sourceCity, &destinationCity, &maxStops);\n        int lowestCost = findLowestFlightCost(cityCount, flightList, flightCount, sourceCity, destinationCity, maxStops);\n        printf(\"%d\\n\", lowestCost);\n    }\n    return 0;\n}",
      "C++": "#include <iostream>\n#include <vector>\n#include <queue>\n#include <limits>\nusing namespace std;\n\n// Define the structure for a flight\nstruct Flight {\n    int sourceCity;\n    int destinationCity;\n    int price;\n};\n\n// Define the structure for a city\nstruct City {\n    int city;\n    int cost;\n    int stops;\n};\n\n// Compare function for the priority queue\nstruct compare {\n    bool operator()(const City &a, const City &b) {\n        return a.cost > b.cost;\n    }\n};\n\n// Function to find the lowest flight cost\nint findLowestFlightCost(int cityCount, vector<Flight> &flightList, int sourceCity, int destinationCity, int maxStops) {\n    // Create a graph from the flight list\n    vector<vector<int>> graph(cityCount, vector<int>(cityCount, -1));\n    for (int i = 0; i < flightList.size(); i++) {\n        graph[flightList[i].sourceCity][flightList[i].destinationCity] = flightList[i].price;\n    }\n\n    // Create a priority queue to store the cities to visit\n    priority_queue<City, vector<City>, compare> priorityQueue;\n\n    // Add the source city to the priority queue\n    City currentCity;\n    currentCity.city = sourceCity;\n    currentCity.cost = 0;\n    currentCity.stops = 0;\n    priorityQueue.push(currentCity);\n\n    // Loop until the priority queue is empty\n    while (!priorityQueue.empty()) {\n        // Get the city with the minimum cost\n        currentCity = priorityQueue.top();\n        priorityQueue.pop();\n\n        // If the current city is the destination city, return the cost\n        if (currentCity.city == destinationCity) {\n            return currentCity.cost;\n        }\n\n        // If the number of stops exceeds the maximum, skip this city\n        if (currentCity.stops > maxStops) {\n            continue;\n        }\n\n        // Add the neighboring cities to the priority queue\n        for (int i = 0; i < cityCount; i++) {\n            if (graph[currentCity.city][i] != -1) {\n                City newCity;\n                newCity.city = i;\n                newCity.cost = currentCity.cost + graph[currentCity.city][i];\n                newCity.stops = currentCity.stops + 1;\n                priorityQueue.push(newCity);\n            }\n        }\n    }\n\n    // If no path is found, return -1\n    return -1;\n}\n\nint main() {\n    int T;\n    cin >> T;\n    for (int t = 0; t < T; t++) {\n        int cityCount;\n        cin >> cityCount;\n        vector<Flight> flightList;\n        for (int i = 0; i < cityCount; i++) {\n            for (int j = 0; j < cityCount; j++) {\n                int price;\n                cin >> price;\n                if (price != -1) {\n                    Flight flight;\n                    flight.sourceCity = i;\n                    flight.destinationCity = j;\n                    flight.price = price;\n                    flightList.push_back(flight);\n                }\n            }\n        }\n        int sourceCity, destinationCity, maxStops;\n        cin >> sourceCity >> destinationCity >> maxStops;\n        int lowestCost = findLowestFlightCost(cityCount, flightList, sourceCity, destinationCity, maxStops);\n        cout << lowestCost << endl;\n    }\n    return 0;\n}",
      "Java": "import java.util.PriorityQueue;\nimport java.util.Comparator;\nimport java.util.Scanner;\n\npublic class Main {\n    // Define the structure for a flight\n    static class Flight {\n        int sourceCity;\n        int destinationCity;\n        int price;\n\n        public Flight(int sourceCity, int destinationCity, int price) {\n            this.sourceCity = sourceCity;\n            this.destinationCity = destinationCity;\n            this.price = price;\n        }\n    }\n\n    // Define the structure for a city\n    static class City implements Comparable<City> {\n        int city;\n        int cost;\n        int stops;\n\n        public City(int city, int cost, int stops) {\n            this.city = city;\n            this.cost = cost;\n            this.stops = stops;\n        }\n\n        @Override\n        public int compareTo(City other) {\n            return Integer.compare(this.cost, other.cost);\n        }\n    }\n\n    // Function to find the lowest flight cost\n    public static int findLowestFlightCost(int cityCount, Flight[] flightList, int sourceCity, int destinationCity, int maxStops) {\n        // Create a graph from the flight list\n        int[][] graph = new int[cityCount][cityCount];\n        for (int i = 0; i < cityCount; i++) {\n            for (int j = 0; j < cityCount; j++) {\n                graph[i][j] = -1;\n            }\n        }\n        for (int i = 0; i < flightList.length; i++) {\n            graph[flightList[i].sourceCity][flightList[i].destinationCity] = flightList[i].price;\n        }\n\n        // Create a priority queue to store the cities to visit\n        PriorityQueue<City> priorityQueue = new PriorityQueue<>();\n\n        // Add the source city to the priority queue\n        City currentCity = new City(sourceCity, 0, 0);\n        priorityQueue.add(currentCity);\n\n        // Loop until the priority queue is empty\n        while (!priorityQueue.isEmpty()) {\n            // Get the city with the minimum cost\n            currentCity = priorityQueue.poll();\n\n            // If the current city is the destination city, return the cost\n            if (currentCity.city == destinationCity) {\n                return currentCity.cost;\n            }\n\n            // If the number of stops exceeds the maximum, skip this city\n            if (currentCity.stops > maxStops) {\n                continue;\n            }\n\n            // Add the neighboring cities to the priority queue\n            for (int i = 0; i < cityCount; i++) {\n                if (graph[currentCity.city][i] != -1) {\n                    City newCity = new City(i, currentCity.cost + graph[currentCity.city][i], currentCity.stops + 1);\n                    priorityQueue.add(newCity);\n                }\n            }\n        }\n\n        // If no path is found, return -1\n        return -1;\n    }\n\n    public static void main(String[] args) {\n        Scanner scanner = new Scanner(System.in);\n        int T = scanner.nextInt();\n        for (int t = 0; t < T; t++) {\n            int cityCount = scanner.nextInt();\n            Flight[] flightList = new Flight[cityCount * cityCount];\n            int flightCount = 0;\n            for (int i = 0; i < cityCount; i++) {\n                for (int j = 0; j < cityCount; j++) {\n                    int price = scanner.nextInt();\n                    if (price != -1) {\n                        flightList[flightCount] = new Flight(i, j, price);\n                        flightCount++;\n                    }\n                }\n            }\n            int sourceCity = scanner.nextInt();\n            int destinationCity = scanner.nextInt();\n            int maxStops = scanner.nextInt();\n            int lowestCost = findLowestFlightCost(cityCount, flightList, sourceCity, destinationCity, maxStops);\n            System.out.println(lowestCost);\n        }\n    }\n}",
      "Python3": "# COMPLETE, RUNNABLE Python3 code\nimport sys\nimport heapq\n\n# Define the structure for a flight\nclass Flight:\n    def __init__(self, sourceCity, destinationCity, price):\n        self.sourceCity = sourceCity\n        self.destinationCity = destinationCity\n        self.price = price\n\n# Define the structure for a city\nclass City:\n    def __init__(self, city, cost, stops):\n        self.city = city\n        self.cost = cost\n        self.stops = stops\n\n    def __lt__(self, other):\n        return self.cost < other.cost\n\n# Function to find the lowest flight cost\ndef findLowestFlightCost(cityCount, flightList, sourceCity, destinationCity, maxStops):\n    # Create a graph from the flight list\n    graph = [[-1 for _ in range(cityCount)] for _ in range(cityCount)]\n    for flight in flightList:\n        graph[flight.sourceCity][flight.destinationCity] = flight.price\n\n    # Create a priority queue to store the cities to visit\n    priorityQueue = []\n\n    # Add the source city to the priority queue\n    currentCity = City(sourceCity, 0, 0)\n    heapq.heappush(priorityQueue, currentCity)\n\n    # Loop until the priority queue is empty\n    while priorityQueue:\n        # Get the city with the minimum cost\n        currentCity = heapq.heappop(priorityQueue)\n\n        # If the current city is the destination city, return the cost\n        if currentCity.city == destinationCity:\n            return currentCity.cost\n\n        # If the number of stops exceeds the maximum, skip this city\n        if currentCity.stops > maxStops:\n            continue\n\n        # Add the neighboring cities to the priority queue\n        for i in range(cityCount):\n            if graph[currentCity.city][i] != -1:\n                newCity = City(i, currentCity.cost + graph[currentCity.city][i], currentCity.stops + 1)\n                heapq.heappush(priorityQueue, newCity)\n\n    # If no path is found, return -1\n    return -1\n\n# Read input from stdin\nT = int(sys.stdin.readline())\nfor t in range(T):\n    cityCount = int(sys.stdin.readline())\n    flightList = []\n    for i in range(cityCount):\n        for j in range(cityCount):\n            price = int(sys.stdin.readline())\n            if price != -1:\n                flight = Flight(i, j, price)\n                flightList.append(flight)\n    sourceCity = int(sys.stdin.readline())\n    destinationCity = int(sys.stdin.readline())\n    maxStops = int(sys.stdin.readline())\n    lowestCost = findLowestFlightCost(cityCount, flightList, sourceCity, destinationCity, maxStops)\n    print(lowestCost)",
      "JavaScript": ""
    }
  },
  "QBCategory": "CCD",
  "boilerPlates": {
    "C": "#include <stdio.h>\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "C++": "#include <iostream>\nusing namespace std;\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "Java": "import java.util.*;\nimport java.io.*;\n\npublic class Main {\n    public static void main(String[] args) throws IOException {\n        // your code goes here\n    }\n}",
    "Python3": "# your code goes here\n",
    "JavaScript": "// your code goes here\n"
  },
  "_internal": {
    "methodName": "",
    "params": []
  }
}