{
  "questionId": "Q4822",
  "title": "Cycle Detection in Directed Graph",
  "slug": "cycle-detection-in-directed-graph",
  "version": 1,
  "createdAt": "2026-07-01",
  "updatedAt": "2026-07-04",
  "metadata": {
    "category": "Graph",
    "difficulty": "Hard",
    "isPremium": true,
    "tags": [
      "DFS",
      "Cycle Detection",
      "Directed Graph",
      "Recursion Stack"
    ],
    "companyTags": [],
    "topicTags": [
      "Graphs"
    ],
    "QBCategory": "CCD"
  },
  "content": {
    "problemStatement": "You are given a directed graph with VVV vertices labeled from 000 to V−1V-1V−1. The graph is represented using an adjacency list, where adj[i]\\text{adj}[i]adj[i] contains all vertices that can be reached directly from vertex iii.\n\nFor each test case, determine whether the given graph contains at least one **directed cycle**. A directed cycle exists if it is possible to start from a vertex and return to the same vertex by following the direction of edges.\n\nFunction Declarations**Function Name**\n`hasDirectedCycle` – Determines whether the given directed graph contains at least one cycle. A cycle exists if there is a path that starts from a vertex and returns to that same vertex.\n\n\n**Parameters**\n`V` : An integer representing the total number of vertices in the graph, labeled from 000toV−1V-1V−1.\n`adj` : An adjacency list (or 2D array/list) representing the directed edges of the graph, where `adj[i]` contains all vertices that can be reached directly from vertex iii.\n**Return Value**\nReturns a boolean: `true` if the graph contains a directed cycle, otherwise returns `false`.\n\nInput Format\n\nThe first line contains a single integer TTT, denoting the number of test cases.\nFor each test case:\n\nThe first line contains two integers VVV and EEE, denoting the number of vertices and the number of edges.\nThe next EEE lines each contain two integers uuu and vvv, indicating a directed edge from vertex uuu to vertex vvv.\n\n\n\n\nOutput Format\n\nFor each test case, output a single line containing the string **`True`** if the graph contains a directed cycle, or **`False`** if it does not.\n\n**Note:** The output is case-sensitive. Ensure you print the strings exactly as shown.",
    "constraints": [
      "1 <= T <= 100",
      "1 <= V <= 100000",
      "0 <= E <= 100000",
      "0 <= u, v < V",
      "Sum of V + E over all test cases does not exceed 200000"
    ],
    "inputFormat": "The first line contains an integer T, the number of test cases. For each test case, the first line contains two integers V and E – the number of vertices (0 … V‑1) and the number of directed edges. The next E lines each contain two integers u and v denoting a directed edge from u to v.",
    "outputFormat": "For each test case output a single line containing \"True\" if the directed graph contains at least one directed cycle, otherwise output \"False\". The output is case‑sensitive.",
    "sampleTestCases": [
      {
        "id": "sample_1",
        "input": "2\n4 4\n0 1\n1 2\n2 3\n3 1\n4 3\n0 1\n0 2\n1 3",
        "output": "True\nFalse",
        "explanation": "Test case 1 forms a cycle 1→2→3→1, so output is True. Test case 2 is a DAG, so output is False."
      },
      {
        "id": "sample_2",
        "input": "1\n3 2\n0 1\n1 2",
        "output": "False",
        "explanation": "The graph 0→1→2 has no way to return to a previously visited vertex, thus no cycle."
      },
      {
        "id": "sample_3",
        "input": "1\n2 2\n0 0\n1 0",
        "output": "True",
        "explanation": "Vertex 0 has a self‑loop (0→0), which is a directed cycle."
      }
    ]
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "label": "minimum boundary",
        "input": "1\n1 0",
        "expectedOutput": "False",
        "weight": 2
      },
      {
        "id": "tc_2",
        "label": "maximum boundary",
        "input": "1\n5 5\n0 1\n1 2\n2 3\n3 4\n4 0",
        "expectedOutput": "True",
        "weight": 2
      },
      {
        "id": "tc_3",
        "label": "edge case",
        "input": "1\n6 4\n0 1\n1 2\n2 0\n3 4",
        "expectedOutput": "True",
        "weight": 2
      },
      {
        "id": "tc_4",
        "label": "general case 1",
        "input": "1\n5 5\n0 1\n0 2\n1 3\n2 3\n3 4",
        "expectedOutput": "False",
        "weight": 2
      },
      {
        "id": "tc_5",
        "label": "Case 5",
        "input": "1\n1 0",
        "expectedOutput": "False",
        "weight": 2
      }
    ],
    "totalTestCases": 5,
    "totalWeight": 10
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      2,
      2,
      2,
      2,
      2
    ],
    "maxScore": 10
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "For each test case we run a depth‑first search on the directed graph. While exploring a vertex we keep a recursion stack (a boolean array recStack) that marks vertices currently on the DFS path. If we encounter an edge to a vertex that is already in recStack, a directed cycle exists. If the vertex was visited earlier but is not in recStack, we ignore it. The algorithm visits every vertex and edge at most once, giving O(V+E) time per test case. Two boolean arrays visited and recStack of size V are used, so the memory consumption is O(V+E).",
    "timeComplexity": "O(V + E) per test case, where V is the number of vertices and E the number of edges of that test case.",
    "spaceComplexity": "O(V + E) for the adjacency list plus O(V) for the visited and recursion‑stack arrays.",
    "code": {
      "C": "// COMPLETE, COMPILABLE C code\n#include <stdio.h>\n#include <stdlib.h>\n#include <stdbool.h>\n\n// Simple dynamic array for adjacency list\ntypedef struct {\n    int *data;\n    int size;\n    int capacity;\n} DynArr;\n\nvoid initDynArr(DynArr *a, int cap) {\n    a->data = (int *)malloc(cap * sizeof(int));\n    a->size = 0;\n    a->capacity = cap;\n}\n\nvoid pushDynArr(DynArr *a, int val) {\n    if (a->size == a->capacity) {\n        a->capacity = a->capacity * 2 + 1;\n        a->data = (int *)realloc(a->data, a->capacity * sizeof(int));\n    }\n    a->data[a->size++] = val;\n}\n\nbool dfs(int v, DynArr *adj, bool *visited, bool *recStack) {\n    visited[v] = true;\n    recStack[v] = true;\n    for (int i = 0; i < adj[v].size; ++i) {\n        int nb = adj[v].data[i];\n        if (!visited[nb]) {\n            if (dfs(nb, adj, visited, recStack)) return true;\n        } else if (recStack[nb]) {\n            return true; // back edge -> cycle\n        }\n    }\n    recStack[v] = false;\n    return false;\n}\n\nbool hasDirectedCycle(int V, DynArr *adj) {\n    bool *visited = (bool *)calloc(V, sizeof(bool));\n    bool *recStack = (bool *)calloc(V, sizeof(bool));\n    for (int i = 0; i < V; ++i) {\n        if (!visited[i]) {\n            if (dfs(i, adj, visited, recStack)) {\n                free(visited);\n                free(recStack);\n                return true;\n            }\n        }\n    }\n    free(visited);\n    free(recStack);\n    return false;\n}\n\nint main() {\n    int T; scanf(\"%d\", &T);\n    while (T--) {\n        int V, E; scanf(\"%d %d\", &V, &E);\n        DynArr *adj = (DynArr *)malloc(V * sizeof(DynArr));\n        for (int i = 0; i < V; ++i) initDynArr(&adj[i], 2);\n        for (int i = 0; i < E; ++i) {\n            int u, v; scanf(\"%d %d\", &u, &v);\n            pushDynArr(&adj[u], v);\n        }\n        if (hasDirectedCycle(V, adj)) printf(\"True\\n\");\n        else printf(\"False\\n\");\n        for (int i = 0; i < V; ++i) free(adj[i].data);\n        free(adj);\n    }\n    return 0;\n}",
      "C++": "// COMPLETE, COMPILABLE C++ code\n#include <bits/stdc++.h>\nusing namespace std;\n\nbool dfs(int v, const vector<vector<int>>& adj, vector<char>& visited, vector<char>& recStack) {\n    visited[v] = 1;\n    recStack[v] = 1;\n    for (int nb : adj[v]) {\n        if (!visited[nb]) {\n            if (dfs(nb, adj, visited, recStack)) return true;\n        } else if (recStack[nb]) {\n            return true;\n        }\n    }\n    recStack[v] = 0;\n    return false;\n}\n\nbool hasDirectedCycle(int V, const vector<vector<int>>& adj) {\n    vector<char> visited(V, 0), recStack(V, 0);\n    for (int i = 0; i < V; ++i) {\n        if (!visited[i]) {\n            if (dfs(i, adj, visited, recStack)) return true;\n        }\n    }\n    return false;\n}\n\nint main() {\n    ios::sync_with_stdio(false);\n    cin.tie(nullptr);\n    int T; cin >> T;\n    while (T--) {\n        int V, E; cin >> V >> E;\n        vector<vector<int>> adj(V);\n        for (int i = 0; i < E; ++i) {\n            int u, v; cin >> u >> v;\n            adj[u].push_back(v);\n        }\n        cout << (hasDirectedCycle(V, adj) ? \"True\" : \"False\") << '\\n';\n    }\n    return 0;\n}",
      "Java": "// COMPLETE, COMPILABLE Java code\nimport java.io.*;\nimport java.util.*;\n\npublic class Main {\n    private static boolean dfs(int v, List<List<Integer>> adj, boolean[] visited, boolean[] recStack) {\n        visited[v] = true;\n        recStack[v] = true;\n        for (int nb : adj.get(v)) {\n            if (!visited[nb]) {\n                if (dfs(nb, adj, visited, recStack)) return true;\n            } else if (recStack[nb]) {\n                return true;\n            }\n        }\n        recStack[v] = false;\n        return false;\n    }\n\n    private static boolean hasDirectedCycle(int V, List<List<Integer>> adj) {\n        boolean[] visited = new boolean[V];\n        boolean[] recStack = new boolean[V];\n        for (int i = 0; i < V; ++i) {\n            if (!visited[i]) {\n                if (dfs(i, adj, visited, recStack)) return true;\n            }\n        }\n        return false;\n    }\n\n    public static void main(String[] args) throws Exception {\n        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));\n        StringTokenizer st = new StringTokenizer(br.readLine());\n        int T = Integer.parseInt(st.nextToken());\n        StringBuilder sb = new StringBuilder();\n        while (T-- > 0) {\n            st = new StringTokenizer(br.readLine());\n            int V = Integer.parseInt(st.nextToken());\n            int E = Integer.parseInt(st.nextToken());\n            List<List<Integer>> adj = new ArrayList<>(V);\n            for (int i = 0; i < V; ++i) adj.add(new ArrayList<>());\n            for (int i = 0; i < E; ++i) {\n                st = new StringTokenizer(br.readLine());\n                int u = Integer.parseInt(st.nextToken());\n                int v = Integer.parseInt(st.nextToken());\n                adj.get(u).add(v);\n            }\n            sb.append(hasDirectedCycle(V, adj) ? \"True\" : \"False\").append('\\n');\n        }\n        System.out.print(sb.toString());\n    }\n}",
      "Python3": "# COMPLETE, RUNNABLE Python3 code\nimport sys\n\ndef dfs(v, adj, visited, rec_stack):\n    visited[v] = True\n    rec_stack[v] = True\n    for nb in adj[v]:\n        if not visited[nb]:\n            if dfs(nb, adj, visited, rec_stack):\n                return True\n        elif rec_stack[nb]:\n            return True\n    rec_stack[v] = False\n    return False\n\ndef has_directed_cycle(V, adj):\n    visited = [False] * V\n    rec_stack = [False] * V\n    for i in range(V):\n        if not visited[i]:\n            if dfs(i, adj, visited, rec_stack):\n                return True\n    return False\n\ndef main():\n    data = sys.stdin.read().strip().split()\n    if not data:\n        return\n    it = iter(data)\n    T = int(next(it))\n    out_lines = []\n    for _ in range(T):\n        V = int(next(it)); E = int(next(it))\n        adj = [[] for _ in range(V)]\n        for _ in range(E):\n            u = int(next(it)); v = int(next(it))\n            adj[u].append(v)\n        out_lines.append(\"True\" if has_directed_cycle(V, adj) else \"False\")\n    sys.stdout.write(\"\\n\".join(out_lines))\n\nif __name__ == \"__main__\":\n    main()",
      "JavaScript": "// COMPLETE, RUNNABLE JavaScript code\nconst fs = require('fs');\nconst input = fs.readFileSync(0, 'utf-8').trim().split(/\\s+/);\nlet idx = 0;\nconst T = Number(input[idx++]);\nlet output = [];\nfunction dfs(v, adj, visited, recStack) {\n  visited[v] = true;\n  recStack[v] = true;\n  for (const nb of adj[v]) {\n    if (!visited[nb]) {\n      if (dfs(nb, adj, visited, recStack)) return true;\n    } else if (recStack[nb]) {\n      return true;\n    }\n  }\n  recStack[v] = false;\n  return false;\n}\nfunction hasDirectedCycle(V, adj) {\n  const visited = new Array(V).fill(false);\n  const recStack = new Array(V).fill(false);\n  for (let i = 0; i < V; ++i) {\n    if (!visited[i]) {\n      if (dfs(i, adj, visited, recStack)) return true;\n    }\n  }\n  return false;\n}\nfor (let t = 0; t < T; ++t) {\n  const V = Number(input[idx++]);\n  const E = Number(input[idx++]);\n  const adj = Array.from({ length: V }, () => []);\n  for (let i = 0; i < E; ++i) {\n    const u = Number(input[idx++]);\n    const v = Number(input[idx++]);\n    adj[u].push(v);\n  }\n  output.push(hasDirectedCycle(V, adj) ? 'True' : 'False');\n}\nconsole.log(output.join('\\n'));"
    }
  },
  "QBCategory": "CCD",
  "boilerPlates": {
    "C": "#include <stdio.h>\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "C++": "#include <iostream>\nusing namespace std;\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "Java": "import java.util.*;\nimport java.io.*;\n\npublic class Main {\n    public static void main(String[] args) throws IOException {\n        // your code goes here\n    }\n}",
    "Python3": "# your code goes here\n",
    "JavaScript": "// your code goes here\n"
  },
  "_internal": {
    "methodName": "",
    "params": []
  }
}