{
  "questionId": "Q1693",
  "title": "Check for BST",
  "slug": "check-for-bst",
  "version": 1,
  "createdAt": "2026-07-01",
  "updatedAt": "2026-07-04",
  "metadata": {
    "category": "Graphs",
    "difficulty": "Medium",
    "isPremium": false,
    "tags": [
      "Tree Traversal",
      "Graphs",
      "Binary Search Tree"
    ],
    "companyTags": [],
    "topicTags": [
      "Trees"
    ]
  },
  "content": {
    "problemStatement": "Check if the given binary tree is a valid Binary Search Tree (BST).\n\nA valid BST adheres to the following criteria:\n\n\nThe left subtree of a node comprises only nodes with keys less than the node's key.\nThe right subtree of a node comprises only nodes with keys greater than the node's key.\nBoth the left and right subtrees must also be valid Binary Search Trees.\nFor eg., the given binary tree is a valid BST.\n\n\n\n\nInput Format\n\nThe first line of the input contains s single integer NNN \u2014 the number of nodes in the binary search tree.\nNext N\u22121N - 1N\u22121 lines contain three space separated characters pi,ci,Rip_i, c_i, R_ipi\u200b,ci\u200b,Ri\u200b, describing the tree with edge informations - pip_ipi\u200b is an integer denoting the parent node of the iiith edge, cic_ici\u200b is child node, and the letter RiR_iRi\u200b denotes whether the child is left child or right child, it's value is `L` if cic_ici\u200b is left child of pip_ipi\u200b, else `R`.\n\n\nOutput Format\nOutput on a single line - `YES` if the given binary tree is a valid BST, else `NO`.",
    "constraints": [
      "1 <= T <= 10",
      "1 <= N <= 10000",
      "1 <= pi, ci <= 100000",
      "Ri \u2208 {'L','R'}",
      "All pi and ci are distinct within a test case"
    ],
    "inputFormat": "The first line contains an integer T, the number of test cases. For each test case: The first line contains an integer N, the number of nodes in the tree. The next N-1 lines each contain three space-separated values pi ci Ri, where pi is the parent node value, ci is the child node value, and Ri is either 'L' (ci is the left child of pi) or 'R' (ci is the right child of pi).",
    "outputFormat": "For each test case, output 'YES' if the described tree satisfies the Binary Search Tree properties, otherwise output 'NO'.",
    "sampleTestCases": [
      {
        "id": "sample_1",
        "input": "2\n7\n4 2 L\n4 6 R\n2 3 R\n2 1 L\n6 5 L\n6 7 R\n5\n1 2 L\n1 3 R\n2 4 L\n2 5 R",
        "output": "YES\nNO",
        "explanation": "First test case forms a perfect BST (root 4, left subtree {2,1,3}, right subtree {6,5,7}). Second test case violates BST property because node 5 is in the left subtree of node 2 but is greater than 2."
      },
      {
        "id": "sample_2",
        "input": "1\n1",
        "output": "YES",
        "explanation": "A single node tree trivially satisfies BST conditions."
      },
      {
        "id": "sample_3",
        "input": "1\n3\n10 15 L\n10 20 R",
        "output": "NO",
        "explanation": "Node 15 is a left child of 10 but is greater than 10, breaking the BST rule."
      }
    ]
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "label": "minimum boundary",
        "input": "1\n1",
        "expectedOutput": "YES",
        "weight": 2
      },
      {
        "id": "tc_2",
        "label": "maximum boundary",
        "input": "1\n10000\n1 2 L\n1 3 R\n2 4 L\n2 5 R\n3 6 L\n3 7 R",
        "expectedOutput": "YES",
        "weight": 2
      },
      {
        "id": "tc_3",
        "label": "uniform/edge case",
        "input": "1\n5\n1 2 L\n1 3 R\n2 4 L\n2 5 R",
        "expectedOutput": "NO",
        "weight": 2
      },
      {
        "id": "tc_4",
        "label": "tricky case",
        "input": "1\n5\n1 2 L\n1 3 R\n2 4 L\n3 5 R",
        "expectedOutput": "YES",
        "weight": 2
      },
      {
        "id": "tc_5",
        "label": "stress/general",
        "input": "2\n7\n4 2 L\n4 6 R\n2 3 R\n2 1 L\n6 5 L\n6 7 R\n5\n1 2 L\n1 3 R\n2 4 L\n2 5 R",
        "expectedOutput": "YES\nNO",
        "weight": 2
      }
    ],
    "totalTestCases": 5,
    "totalWeight": 10
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      2,
      2,
      2,
      2,
      2
    ],
    "maxScore": 10
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "To determine if a given binary tree is a valid Binary Search Tree (BST), we need to check if for every node, all elements in its left subtree are less than the node, and all elements in its right subtree are greater than the node. This can be achieved by performing a depth-first search (DFS) traversal of the tree and checking the BST property at each node.",
    "timeComplexity": "O(T * N) where T is the number of test cases and N is the number of nodes in the tree. The DFS traversal takes O(N) time for each test case.",
    "spaceComplexity": "O(N) for storing the tree nodes and their children.",
    "code": {
      "C": "#include <stdio.h>\n#include <stdlib.h>\n#include <limits.h>\n\n#define MAXV 100000\n\nstatic int leftChild[MAXV + 1];\nstatic int rightChild[MAXV + 1];\nstatic int isChild[MAXV + 1];\nstatic int existsNode[MAXV + 1];\nstatic int nodeList[10005];\nstatic int nodeCnt;\n\nint isBSTUtil(int val, long long minVal, long long maxVal) {\n    if (val == 0) return 1; // null represented by 0\n    if (val <= minVal || val >= maxVal) return 0;\n    int l = leftChild[val];\n    int r = rightChild[val];\n    return isBSTUtil(l, minVal, val) && isBSTUtil(r, val, maxVal);\n}\n\nint main() {\n    int T; if (scanf(\"%d\", &T)!=1) return 0;\n    while (T--) {\n        int N; scanf(\"%d\", &N);\n        // reset arrays for values that will be used\n        nodeCnt = 0;\n        for (int i = 0; i < N-1; ++i) {\n            int p, c; char dir; scanf(\"%d %d %c\", &p, &c, &dir);\n            leftChild[p] = rightChild[p] = 0; // ensure cleared\n            leftChild[c] = rightChild[c] = 0;\n            if (dir == 'L') leftChild[p] = c; else rightChild[p] = c;\n            isChild[c] = 1;\n            if (!existsNode[p]) { existsNode[p] = 1; nodeList[nodeCnt++] = p; }\n            if (!existsNode[c]) { existsNode[c] = 1; nodeList[nodeCnt++] = c; }\n        }\n        if (N == 1) {\n            // No edges, single node \u2013 always a BST\n            printf(\"YES\\n\");\n        } else {\n            int root = 0;\n            for (int i = 0; i < nodeCnt; ++i) {\n                int v = nodeList[i];\n                if (existsNode[v] && !isChild[v]) { root = v; break; }\n            }\n            int ok = isBSTUtil(root, LLONG_MIN, LLONG_MAX);\n            printf(ok ? \"YES\\n\" : \"NO\\n\");\n        }\n        // clean up for next test case\n        for (int i = 0; i < nodeCnt; ++i) {\n            int v = nodeList[i];\n            leftChild[v] = rightChild[v] = 0;\n            isChild[v] = 0;\n            existsNode[v] = 0;\n        }\n    }\n    return 0;\n}",
      "C++": "#include <bits/stdc++.h>\nusing namespace std;\n\nbool checkBST(int node, long long lo, long long hi,\n              const unordered_map<int,int>& L,\n              const unordered_map<int,int>& R) {\n    if (node == 0) return true;\n    if (node <= lo || node >= hi) return false;\n    int l = L.count(node) ? L.at(node) : 0;\n    int r = R.count(node) ? R.at(node) : 0;\n    return checkBST(l, lo, node, L, R) && checkBST(r, node, hi, L, R);\n}\n\nint main(){\n    ios::sync_with_stdio(false);\n    cin.tie(nullptr);\n    int T; if(!(cin>>T)) return 0;\n    while(T--){\n        int N; cin>>N;\n        unordered_map<int,int> left, right;\n        unordered_set<int> children, all;\n        for(int i=0;i<N-1;++i){\n            int p,c; char dir; cin>>p>>c>>dir;\n            all.insert(p); all.insert(c);\n            if(dir=='L') left[p]=c; else right[p]=c;\n            children.insert(c);\n        }\n        if(N==1){\n            cout<<\"YES\\n\";\n            continue;\n        }\n        int root=0;\n        for(int v: all) if(!children.count(v)){ root=v; break; }\n        bool ok = checkBST(root, LLONG_MIN, LLONG_MAX, left, right);\n        cout<<(ok?\"YES\":\"NO\")<<'\\n';\n    }\n    return 0;\n}",
      "Java": "import java.io.*;\nimport java.util.*;\n\npublic class Main {\n    static boolean dfs(int node, long lo, long hi,\n                       Map<Integer,Integer> left,\n                       Map<Integer,Integer> right) {\n        if (node == 0) return true;\n        if (node <= lo || node >= hi) return false;\n        int l = left.getOrDefault(node, 0);\n        int r = right.getOrDefault(node, 0);\n        return dfs(l, lo, node, left, right) && dfs(r, node, hi, left, right);\n    }\n    public static void main(String[] args) throws Exception {\n        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));\n        StringTokenizer st;\n        int T = Integer.parseInt(br.readLine().trim());\n        while (T-- > 0) {\n            int N = Integer.parseInt(br.readLine().trim());\n            Map<Integer,Integer> left = new HashMap<>();\n            Map<Integer,Integer> right = new HashMap<>();\n            Set<Integer> children = new HashSet<>();\n            Set<Integer> all = new HashSet<>();\n            for (int i = 0; i < N-1; ++i) {\n                st = new StringTokenizer(br.readLine());\n                int p = Integer.parseInt(st.nextToken());\n                int c = Integer.parseInt(st.nextToken());\n                char dir = st.nextToken().charAt(0);\n                all.add(p); all.add(c);\n                if (dir == 'L') left.put(p, c); else right.put(p, c);\n                children.add(c);\n            }\n            if (N == 1) {\n                System.out.println(\"YES\");\n                continue;\n            }\n            int root = 0;\n            for (int v : all) if (!children.contains(v)) { root = v; break; }\n            boolean ok = dfs(root, Long.MIN_VALUE, Long.MAX_VALUE, left, right);\n            System.out.println(ok ? \"YES\" : \"NO\");\n        }\n    }\n}",
      "Python3": "import sys\n\ndef is_bst(node, lo, hi, left, right):\n    if node == 0:\n        return True\n    if not (lo < node < hi):\n        return False\n    return is_bst(left.get(node, 0), lo, node, left, right) and is_bst(right.get(node, 0), node, hi, left, right)\n\ndef solve():\n    data = sys.stdin.read().strip().split()\n    if not data:\n        return\n    it = iter(data)\n    T = int(next(it))\n    out_lines = []\n    for _ in range(T):\n        N = int(next(it))\n        left = {}\n        right = {}\n        children = set()\n        all_nodes = set()\n        for __ in range(N-1):\n            p = int(next(it)); c = int(next(it)); dir = next(it)\n            all_nodes.add(p); all_nodes.add(c)\n            if dir == 'L':\n                left[p] = c\n            else:\n                right[p] = c\n            children.add(c)\n        if N == 1:\n            out_lines.append('YES')\n            continue\n        root = next(v for v in all_nodes if v not in children)\n        ok = is_bst(root, -10**18, 10**18, left, right)\n        out_lines.append('YES' if ok else 'NO')\n    sys.stdout.write('\\n'.join(out_lines))\n\nif __name__ == \"__main__\":\n    solve()\n",
      "JavaScript": "const fs = require('fs');\nconst data = fs.readFileSync(0, 'utf-8').trim().split(/\\s+/);\nlet idx = 0;\nfunction next(){return data[idx++];}\nconst T = parseInt(next()||'0');\nlet outputs = [];\nfor(let tc=0; tc<T; ++tc){\n  const N = parseInt(next());\n  const left = new Map();\n  const right = new Map();\n  const children = new Set();\n  const all = new Set();\n  for(let i=0;i<N-1;++i){\n    const p = parseInt(next());\n    const c = parseInt(next());\n    const dir = next();\n    all.add(p); all.add(c);\n    if(dir==='L') left.set(p,c); else right.set(p,c);\n    children.add(c);\n  }\n  if(N===1){\n    outputs.push('YES');\n    continue;\n  }\n  let root = 0;\n  for(const v of all){ if(!children.has(v)){ root=v; break; } }\n  function dfs(node, lo, hi){\n    if(node===0) return true;\n    if(!(lo < node && node < hi)) return false;\n    const l = left.get(node)||0;\n    const r = right.get(node)||0;\n    return dfs(l, lo, node) && dfs(r, node, hi);\n  }\n  const ok = dfs(root, -Infinity, Infinity);\n  outputs.push(ok?'YES':'NO');\n}\nconsole.log(outputs.join('\\n'));"
    }
  }
}