{
  "questionId": "Q4449",
  "title": "Nodes at Kth Level",
  "slug": "nodes-at-kth-level",
  "version": 1,
  "createdAt": "2026-07-01",
  "updatedAt": "2026-07-04",
  "metadata": {
    "category": "Graph",
    "difficulty": "Medium",
    "isPremium": true,
    "tags": [
      "BFS",
      "Tree Traversal"
    ],
    "companyTags": [],
    "topicTags": [
      "Trees"
    ],
    "QBCategory": "CCD"
  },
  "content": {
    "problemStatement": "Given an undirected connected tree with **N** nodes, numbered from **1** to **N**, rooted at node **1**, and a positive integer **K**. Print all the nodes at the level **K** from root node in sorted order.\n\nFor example, in the following tree, root node `1` is at level `0`. Similarly, nodes `2` and `4` are at level `1` from the root. Similarly, nodes 5, 3, 6 and 7 are at level 2 from the root node.\n\n\n\n\nInput Format\n\nThe first line of the input contains two space-separated integers NNN and KKK— the number of vertices and level at which we need to find the nodes.\nThe next N−1N - 1N−1 lines describe the edges. The iii-th of these N−1N - 1N−1 lines contains two space-separated integers uiu_iui​ and viv_ivi​, denoting a bidirectional edge between uiu_iui​ and viv_ivi​.\n\n\nOutput Format\nOutput on the single line, the space separated nodes (in sorted order) which are present at the **Kth** level from root node **1**,",
    "constraints": [
      "1 <= T <= 10",
      "1 <= N <= 100000",
      "1 <= u, v <= N",
      "0 <= K <= N-1",
      "The given graph is a connected tree."
    ],
    "inputFormat": "The first line contains an integer T, the number of test cases. For each test case: The first line contains two integers N and K, the number of nodes in the tree and the required level (root is at level 0). The next N-1 lines each contain two integers u and v, denoting an undirected edge between nodes u and v.",
    "outputFormat": "For each test case, output a single line containing the nodes that are exactly at level K from the root, sorted in increasing order and separated by a single space. If no node exists at that level, output an empty line.",
    "sampleTestCases": [
      {
        "id": "sample_1",
        "input": "1\n8 2\n1 2\n1 3\n3 4\n3 5\n3 6\n2 7\n2 8",
        "output": "4 5 6 7 8",
        "explanation": "The tree has root 1. Nodes at distance 2 from the root are 4,5,6 (through node 3) and 7,8 (through node 2). After sorting they are printed as \"4 5 6 7 8\"."
      },
      {
        "id": "sample_2",
        "input": "1\n5 1\n1 2\n1 3\n2 4\n3 5",
        "output": "2 3",
        "explanation": "Level 1 from the root consists of the direct children of node 1, which are nodes 2 and 3."
      },
      {
        "id": "sample_3",
        "input": "1\n3 0\n1 2\n1 3",
        "output": "1",
        "explanation": "Level 0 always contains only the root node itself."
      }
    ]
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "label": "minimum boundary",
        "input": "1\n1 0\n",
        "expectedOutput": "1",
        "weight": 2
      },
      {
        "id": "tc_2",
        "label": "maximum boundary",
        "input": "1\n100000 99999\n",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_3",
        "label": "uniform/edge case",
        "input": "1\n5 2\n1 2\n1 3\n1 4\n1 5",
        "expectedOutput": "2 3 4 5",
        "weight": 2
      },
      {
        "id": "tc_4",
        "label": "tricky case",
        "input": "1\n10 3\n1 2\n1 3\n2 4\n2 5\n3 6\n3 7\n4 8\n4 9\n5 10",
        "expectedOutput": "8 9 10",
        "weight": 2
      },
      {
        "id": "tc_5",
        "label": "stress/general",
        "input": "2\n10 2\n1 2\n1 3\n2 4\n2 5\n3 6\n3 7\n4 8\n4 9\n5 10\n5 1\n1 2\n1 3\n2 4\n2 5\n3 6\n3 7\n4 8\n4 9\n5 10",
        "expectedOutput": "4 5 6 7\n4 5 6 7",
        "weight": 2
      }
    ],
    "totalTestCases": 5,
    "totalWeight": 10
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      2,
      2,
      2,
      2,
      2
    ],
    "maxScore": 10
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "To solve this problem, we use a Breadth-First Search (BFS) algorithm to traverse the tree level by level. We start from the root node (level 0) and explore all its neighbors, then move on to the next level. We keep track of the current level and the nodes at each level. Once we reach the desired level K, we add all the nodes at that level to our result list.",
    "timeComplexity": "O(T * (N + M)) where T is the number of test cases, N is the number of nodes, and M is the number of edges. The BFS traversal takes O(N + M) time for each test case.",
    "spaceComplexity": "O(N) for storing the adjacency list representation of the tree and the queue for BFS traversal.",
    "code": {
      "C": "#include <stdio.h>\n#include <stdlib.h>\n\n// Define the structure for a queue node\ntypedef struct QueueNode {\n    int data;\n    struct QueueNode* next;\n} QueueNode;\n\n// Define the structure for a graph node\ntypedef struct GraphNode {\n    int data;\n    struct GraphNode* next;\n} GraphNode;\n\n// Function to create a new queue node\nQueueNode* createQueueNode(int data) {\n    QueueNode* newNode = (QueueNode*) malloc(sizeof(QueueNode));\n    if (!newNode) {\n        printf(\"Memory error\n\");\n        return NULL;\n    }\n    newNode->data = data;\n    newNode->next = NULL;\n    return newNode;\n}\n\n// Function to create a new graph node\nGraphNode* createGraphNode(int data) {\n    GraphNode* newNode = (GraphNode*) malloc(sizeof(GraphNode));\n    if (!newNode) {\n        printf(\"Memory error\n\");\n        return NULL;\n    }\n    newNode->data = data;\n    newNode->next = NULL;\n    return newNode;\n}\n\n// Function to add an edge to the graph\nvoid addEdge(GraphNode** adjList, int src, int dest, int n) {\n    GraphNode* newNode = createGraphNode(dest);\n    if (adjList[src] == NULL) {\n        adjList[src] = newNode;\n    } else {\n        GraphNode* lastNode = adjList[src];\n        while (lastNode->next) {\n            lastNode = lastNode->next;\n        }\n        lastNode->next = newNode;\n    }\n}\n\n// Function to perform BFS traversal\nvoid bfs(GraphNode** adjList, int* visited, int* level, int src, int n) {\n    QueueNode* queue = createQueueNode(src);\n    visited[src] = 1;\n    level[src] = 0;\n    while (queue) {\n        int currentNode = queue->data;\n        GraphNode* temp = adjList[currentNode];\n        while (temp) {\n            if (!visited[temp->data]) {\n                QueueNode* newNode = createQueueNode(temp->data);\n                newNode->next = queue;\n                queue = newNode;\n                visited[temp->data] = 1;\n                level[temp->data] = level[currentNode] + 1;\n            }\n            temp = temp->next;\n        }\n        QueueNode* tempQueue = queue;\n        queue = queue->next;\n        free(tempQueue);\n    }\n}\n\nint main() {\n    int t;\n    scanf(\"%d\", &t);\n    while (t--) {\n        int n, k;\n        scanf(\"%d %d\", &n, &k);\n        GraphNode** adjList = (GraphNode**) malloc(n * sizeof(GraphNode*));\n        for (int i = 0; i < n; i++) {\n            adjList[i] = NULL;\n        }\n        for (int i = 0; i < n - 1; i++) {\n            int u, v;\n            scanf(\"%d %d\", &u, &v);\n            addEdge(adjList, u - 1, v - 1, n);\n            addEdge(adjList, v - 1, u - 1, n);\n        }\n        int* visited = (int*) malloc(n * sizeof(int));\n        int* level = (int*) malloc(n * sizeof(int));\n        for (int i = 0; i < n; i++) {\n            visited[i] = 0;\n            level[i] = -1;\n        }\n        bfs(adjList, visited, level, 0, n);\n        for (int i = 0; i < n; i++) {\n            if (level[i] == k) {\n                printf(\"%d \", i + 1);\n            }\n        }\n        printf(\"\\n\");\n        for (int i = 0; i < n; i++) {\n            GraphNode* temp = adjList[i];\n            while (temp) {\n                GraphNode* next = temp->next;\n                free(temp);\n                temp = next;\n            }\n        }\n        free(adjList);\n        free(visited);\n        free(level);\n    }\n    return 0;\n}",
      "C++": "#include <bits/stdc++.h>\nusing namespace std;\n\n// Function to perform BFS traversal\nvoid bfs(vector<vector<int>>& adjList, vector<int>& visited, vector<int>& level, int src, int n) {\n    queue<int> q;\n    q.push(src);\n    visited[src] = 1;\n    level[src] = 0;\n    while (!q.empty()) {\n        int currentNode = q.front();\n        q.pop();\n        for (int neighbor : adjList[currentNode]) {\n            if (!visited[neighbor]) {\n                q.push(neighbor);\n                visited[neighbor] = 1;\n                level[neighbor] = level[currentNode] + 1;\n            }\n        }\n    }\n}\n\nint main() {\n    ios::sync_with_stdio(false);\n    cin.tie(NULL);\n    int t;\n    cin >> t;\n    while (t--) {\n        int n, k;\n        cin >> n >> k;\n        vector<vector<int>> adjList(n);\n        for (int i = 0; i < n - 1; i++) {\n            int u, v;\n            cin >> u >> v;\n            adjList[u - 1].push_back(v - 1);\n            adjList[v - 1].push_back(u - 1);\n        }\n        vector<int> visited(n, 0);\n        vector<int> level(n, -1);\n        bfs(adjList, visited, level, 0, n);\n        for (int i = 0; i < n; i++) {\n            if (level[i] == k) {\n                cout << i + 1 << \" \";\n            }\n        }\n        cout << \"\\n\";\n    }\n    return 0;\n}",
      "Java": "import java.io.*;\nimport java.util.*;\n\npublic class Main {\n    // Function to perform BFS traversal\n    public static void bfs(List<List<Integer>> adjList, int[] visited, int[] level, int src, int n) {\n        Queue<Integer> q = new LinkedList<>();\n        q.add(src);\n        visited[src] = 1;\n        level[src] = 0;\n        while (!q.isEmpty()) {\n            int currentNode = q.poll();\n            for (int neighbor : adjList.get(currentNode)) {\n                if (visited[neighbor] == 0) {\n                    q.add(neighbor);\n                    visited[neighbor] = 1;\n                    level[neighbor] = level[currentNode] + 1;\n                }\n            }\n        }\n    }\n\n    public static void main(String[] args) throws Exception {\n        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));\n        StringTokenizer st = new StringTokenizer(br.readLine());\n        int t = Integer.parseInt(st.nextToken());\n        StringBuilder out = new StringBuilder();\n        while (t-- > 0) {\n            st = new StringTokenizer(br.readLine());\n            int n = Integer.parseInt(st.nextToken());\n            int k = Integer.parseInt(st.nextToken());\n            List<List<Integer>> adjList = new ArrayList<>();\n            for (int i = 0; i < n; i++) {\n                adjList.add(new ArrayList<>());\n            }\n            for (int i = 0; i < n - 1; i++) {\n                st = new StringTokenizer(br.readLine());\n                int u = Integer.parseInt(st.nextToken());\n                int v = Integer.parseInt(st.nextToken());\n                adjList.get(u - 1).add(v - 1);\n                adjList.get(v - 1).add(u - 1);\n            }\n            int[] visited = new int[n];\n            int[] level = new int[n];\n            Arrays.fill(level, -1);\n            bfs(adjList, visited, level, 0, n);\n            for (int i = 0; i < n; i++) {\n                if (level[i] == k) {\n                    out.append(i + 1).append(\" \");\n                }\n            }\n            out.append(\"\\n\");\n        }\n        System.out.print(out.toString());\n    }\n}",
      "Python3": "import sys\nfrom collections import deque\n\n# Function to perform BFS traversal\ndef bfs(adj_list, visited, level, src, n):\n    q = deque([src])\n    visited[src] = 1\n    level[src] = 0\n    while q:\n        current_node = q.popleft()\n        for neighbor in adj_list[current_node]:\n            if not visited[neighbor]:\n                q.append(neighbor)\n                visited[neighbor] = 1\n                level[neighbor] = level[current_node] + 1\n\n# Read input\ninput_data = sys.stdin.read().strip().split()\ninput_index = 0\n\nt = int(input_data[input_index])\ninput_index += 1\n\nfor _ in range(t):\n    n = int(input_data[input_index])\n    input_index += 1\n    k = int(input_data[input_index])\n    input_index += 1\n    adj_list = [[] for _ in range(n)]\n    for _ in range(n - 1):\n        u = int(input_data[input_index]) - 1\n        input_index += 1\n        v = int(input_data[input_index]) - 1\n        input_index += 1\n        adj_list[u].append(v)\n        adj_list[v].append(u)\n    visited = [0] * n\n    level = [-1] * n\n    bfs(adj_list, visited, level, 0, n)\n    for i in range(n):\n        if level[i] == k:\n            print(i + 1, end=\" \")\n    print()",
      "JavaScript": "const fs = require('fs');\nconst data = fs.readFileSync(0, 'utf-8').trim().split(/\\s+/).map(Number);\nlet idx = 0;\nconst t = data[idx++];\nlet outputs = [];\nfor (let i = 0; i < t; i++) {\n  const n = data[idx++];\n  const k = data[idx++];\n  const adjList = Array.from({ length: n }, () => []);\n  for (let j = 0; j < n - 1; j++) {\n    const u = data[idx++] - 1;\n    const v = data[idx++] - 1;\n    adjList[u].push(v);\n    adjList[v].push(u);\n  }\n  const visited = new Array(n).fill(0);\n  const level = new Array(n).fill(-1);\n  const queue = [0];\n  visited[0] = 1;\n  level[0] = 0;\n  while (queue.length) {\n    const currentNode = queue.shift();\n    for (const neighbor of adjList[currentNode]) {\n      if (!visited[neighbor]) {\n        queue.push(neighbor);\n        visited[neighbor] = 1;\n        level[neighbor] = level[currentNode] + 1;\n      }\n    }\n  }\n  const result = [];\n  for (let j = 0; j < n; j++) {\n    if (level[j] === k) {\n      result.push(j + 1);\n    }\n  }\n  outputs.push(result.join(' '));\n}\nconsole.log(outputs.join('\\n'));"
    }
  },
  "QBCategory": "CCD",
  "boilerPlates": {
    "C": "#include <stdio.h>\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "C++": "#include <iostream>\nusing namespace std;\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "Java": "import java.util.*;\nimport java.io.*;\n\npublic class Main {\n    public static void main(String[] args) throws IOException {\n        // your code goes here\n    }\n}",
    "Python3": "# your code goes here\n",
    "JavaScript": "// your code goes here\n"
  },
  "_internal": {
    "methodName": "",
    "params": []
  }
}