{
  "questionId": "Q5023",
  "title": "Creating a Full Name from First and Last Name",
  "slug": "creating-a-full-name-from-first-and-last-name",
  "version": 1,
  "createdAt": "2026-07-01",
  "updatedAt": "2026-07-04",
  "metadata": {
    "category": "Strings",
    "difficulty": "Easy",
    "isPremium": true,
    "tags": [
      "String Manipulation",
      "Input Validation",
      "Lists"
    ],
    "companyTags": [],
    "topicTags": [
      "Strings in Python"
    ],
    "QBCategory": "CCD"
  },
  "content": {
    "problemStatement": "Imagine you are filling out an online form that requires your first name and last name separately. However, the system stores names as lists of individual characters for processing.\n\nTo handle this, you will need to write a program that does the following:\n\n**Steps to Complete**\n\n\n**Take User Input**: Use `input()` to ask for the first and last name.\n**Remove Extra Spaces**: Apply `.strip()` to clean the input.\n**Validate Input**: Use `.isalpha()` to ensure that both input contains only letters.\n**Convert Strings to Lists**: Use `list()` to break each name into a list of characters.\n**Concatenate the Lists**: Use the `+` operator to merge both lists.\n**Display the Final List**: Print the combined list of characters.\n**Handle Invalid Input**: If any input contains numbers, special characters, or is empty, display an error message.",
    "constraints": [
      "1 ≤ T ≤ 10",
      "1 ≤ length of each name after stripping ≤ 100",
      "Names contain only letters A‑Z or a‑z after stripping."
    ],
    "inputFormat": "The first line contains an integer T (1 ≤ T ≤ 10), the number of test cases. Each test case consists of two lines: the first line is the first name, the second line is the last name. Leading and trailing spaces may be present and should be ignored.",
    "outputFormat": "For each test case, if both names consist only of alphabetic characters after stripping spaces, output a line:\nFull name as a list of characters: [list]\nwhere [list] is a Python‑style list of single‑quoted characters representing the concatenation of the first and last name. If either name is invalid, output:\nError: Please enter valid first and last names (letters only).",
    "sampleTestCases": [
      {
        "id": "sample_1",
        "input": "1\\nAlice\\nBrown",
        "output": "Full name as a list of characters: ['A', 'l', 'i', 'c', 'e', 'B', 'r', 'o', 'w', 'n']",
        "explanation": "Both names contain only letters. After stripping (no spaces) they are converted to character lists and concatenated."
      },
      {
        "id": "sample_2",
        "input": "1\\nJohn123\\nDoe",
        "output": "Error: Please enter valid first and last names (letters only).",
        "explanation": "The first name contains digits, so validation fails and the error message is printed."
      },
      {
        "id": "sample_3",
        "input": "2\\nA\\nB\\n   Charlie   \\n   Delta   ",
        "output": "Full name as a list of characters: ['A', 'B']\\nFull name as a list of characters: ['C', 'h', 'a', 'r', 'l', 'i', 'e', 'D', 'e', 'l', 't', 'a']",
        "explanation": "First test case uses single‑letter names. Second test case has leading/trailing spaces which are removed before validation and concatenation."
      }
    ]
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "label": "minimum boundary",
        "input": "1\\nA\\nB",
        "expectedOutput": "Full name as a list of characters: ['A', 'B']",
        "weight": 2
      },
      {
        "id": "tc_2",
        "label": "maximum boundary",
        "input": "1\\nAAAAAAAAAA\\nZZZZZZZZZZ",
        "expectedOutput": "Full name as a list of characters: ['A', 'A', 'A', 'A', 'A', 'A', 'A', 'A', 'A', 'A', 'Z', 'Z', 'Z', 'Z', 'Z', 'Z', 'Z', 'Z', 'Z', 'Z']",
        "weight": 2
      },
      {
        "id": "tc_3",
        "label": "edge case – empty after strip",
        "input": "1\\n   \\nBob",
        "expectedOutput": "Error: Please enter valid first and last names (letters only).",
        "weight": 2
      },
      {
        "id": "tc_4",
        "label": "general case 1",
        "input": "1\\nMiKe\\nO'Neil",
        "expectedOutput": "Error: Please enter valid first and last names (letters only).",
        "weight": 2
      },
      {
        "id": "tc_5",
        "label": "general case 2",
        "input": "1\\nAnna\\nSmith",
        "expectedOutput": "Full name as a list of characters: ['A', 'n', 'n', 'a', 'S', 'm', 'i', 't', 'h']",
        "weight": 2
      }
    ],
    "totalTestCases": 5,
    "totalWeight": 10
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      2,
      2,
      2,
      2,
      2
    ],
    "maxScore": 10
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "For each test case we read the two lines, apply strip() to remove leading/trailing whitespace, and check with isalpha() that the remaining strings contain only letters. If the validation succeeds we convert each string to a list of its characters with list(), concatenate the two lists, and print them in the required format. If validation fails we print the error message. The algorithm is linear in the total number of characters.",
    "timeComplexity": "O(total length of all names) – each character is examined a constant number of times.",
    "spaceComplexity": "O(max length of a concatenated name) – the list that holds the characters of one test case.",
    "code": {
      "C": "#include <stdio.h>\n#include <string.h>\n#include <ctype.h>\n\nint is_alpha_str(const char *s) {\n    while (*s) {\n        if (!isalpha((unsigned char)*s)) return 0;\n        s++;\n    }\n    return 1;\n}\n\nvoid trim(char *s) {\n    // remove leading spaces\n    char *start = s;\n    while (isspace((unsigned char)*start)) start++;\n    // remove trailing spaces\n    char *end = start + strlen(start) - 1;\n    while (end >= start && isspace((unsigned char)*end)) {\n        *end = '\\0';\n        end--;\n    }\n    // shift trimmed string to beginning\n    if (start != s) memmove(s, start, strlen(start) + 1);\n}\n\nint main() {\n    int T;\n    if (scanf(\"%d\", &T) != 1) return 0;\n    getchar(); // consume newline after T\n    for (int tc = 0; tc < T; ++tc) {\n        char first[105], last[105];\n        if (!fgets(first, sizeof(first), stdin)) break;\n        if (!fgets(last, sizeof(last), stdin)) break;\n        // remove trailing newline\n        first[strcspn(first, \"\\n\")] = '\\0';\n        last[strcspn(last, \"\\n\")] = '\\0';\n        trim(first);\n        trim(last);\n        if (is_alpha_str(first) && is_alpha_str(last) && first[0] && last[0]) {\n            printf(\"Full name as a list of characters: [\");\n            int firstPrinted = 0;\n            for (char *p = first; *p; ++p) {\n                if (firstPrinted) printf(\", \");\n                printf(\"'%c'\", *p);\n                firstPrinted = 1;\n            }\n            for (char *p = last; *p; ++p) {\n                if (firstPrinted) printf(\", \");\n                printf(\"'%c'\", *p);\n                firstPrinted = 1;\n            }\n            printf(\"]\\n\");\n        } else {\n            printf(\"Error: Please enter valid first and last names (letters only).\\n\");\n        }\n    }\n    return 0;\n}",
      "C++": "#include <bits/stdc++.h>\nusing namespace std;\n\nbool isAlpha(const string &s) {\n    for (char c : s) if (!isalpha(static_cast<unsigned char>(c))) return false;\n    return true;\n}\n\nstring trim(const string &s) {\n    size_t start = s.find_first_not_of(\" \\t\\r\\n\");\n    if (start == string::npos) return \"\";\n    size_t end = s.find_last_not_of(\" \\t\\r\\n\");\n    return s.substr(start, end - start + 1);\n}\n\nint main() {\n    ios::sync_with_stdio(false);\n    cin.tie(nullptr);\n    int T;\n    if (!(cin >> T)) return 0;\n    string dummy; getline(cin, dummy); // consume endline after T\n    for (int tc = 0; tc < T; ++tc) {\n        string first, last;\n        getline(cin, first);\n        getline(cin, last);\n        first = trim(first);\n        last = trim(last);\n        if (!first.empty() && !last.empty() && isAlpha(first) && isAlpha(last)) {\n            cout << \"Full name as a list of characters: [\";\n            bool firstChar = true;\n            for (char c : first) {\n                if (!firstChar) cout << \", \";\n                cout << '\\'' << c << '\\'';\n                firstChar = false;\n            }\n            for (char c : last) {\n                if (!firstChar) cout << \", \";\n                cout << '\\'' << c << '\\'';\n                firstChar = false;\n            }\n            cout << \"]\\n\";\n        } else {\n            cout << \"Error: Please enter valid first and last names (letters only).\\n\";\n        }\n    }\n    return 0;\n}",
      "Java": "import java.io.*;\nimport java.util.*;\n\npublic class Main {\n    private static String trim(String s) {\n        return s == null ? \"\" : s.strip();\n    }\n    private static boolean isAlpha(String s) {\n        for (int i = 0; i < s.length(); i++) {\n            if (!Character.isLetter(s.charAt(i))) return false;\n        }\n        return true;\n    }\n    public static void main(String[] args) throws Exception {\n        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));\n        String line = br.readLine();\n        if (line == null) return;\n        int T = Integer.parseInt(line.trim());\n        for (int tc = 0; tc < T; tc++) {\n            String first = br.readLine();\n            String last = br.readLine();\n            if (first == null) first = \"\";\n            if (last == null) last = \"\";\n            first = trim(first);\n            last = trim(last);\n            if (!first.isEmpty() && !last.isEmpty() && isAlpha(first) && isAlpha(last)) {\n                StringBuilder sb = new StringBuilder();\n                sb.append(\"Full name as a list of characters: [\");\n                boolean firstChar = true;\n                for (char c : first.toCharArray()) {\n                    if (!firstChar) sb.append(\", \");\n                    sb.append('\\'').append(c).append('\\'');\n                    firstChar = false;\n                }\n                for (char c : last.toCharArray()) {\n                    if (!firstChar) sb.append(\", \");\n                    sb.append('\\'').append(c).append('\\'');\n                    firstChar = false;\n                }\n                sb.append(\"]\");\n                System.out.println(sb.toString());\n            } else {\n                System.out.println(\"Error: Please enter valid first and last names (letters only).\");\n            }\n        }\n    }\n}",
      "Python3": "#!/usr/bin/env python3\nimport sys\n\ndef main():\n    data = sys.stdin.read().splitlines()\n    if not data:\n        return\n    T = int(data[0].strip())\n    idx = 1\n    out = []\n    for _ in range(T):\n        first = data[idx].strip() if idx < len(data) else \"\"\n        idx += 1\n        last = data[idx].strip() if idx < len(data) else \"\"\n        idx += 1\n        if first and last and first.isalpha() and last.isalpha():\n            full = list(first) + list(last)\n            out.append(f\"Full name as a list of characters: {full}\")\n        else:\n            out.append(\"Error: Please enter valid first and last names (letters only).\")\n    sys.stdout.write(\"\\n\".join(out))\n\nif __name__ == \"__main__\":\n    main()\n",
      "JavaScript": "const fs = require('fs');\nconst input = fs.readFileSync(0, 'utf-8').split('\\n');\nlet idx = 0;\nconst T = parseInt((input[idx] || '').trim(), 10);\nidx++;\nlet out = [];\nfunction isAlpha(str) { return /^[A-Za-z]+$/.test(str); }\nfunction trim(str) { return str.replace(/^\\s+|\\s+$/g, ''); }\nfor (let t = 0; t < T; t++) {\n  const rawFirst = input[idx++] || '';\n  const rawLast = input[idx++] || '';\n  const first = trim(rawFirst);\n  const last = trim(rawLast);\n  if (first && last && isAlpha(first) && isAlpha(last)) {\n    const chars = [...first, ...last];\n    const listStr = '[' + chars.map(c => `'${c}'`).join(', ') + ']';\n    out.push(`Full name as a list of characters: ${listStr}`);\n  } else {\n    out.push('Error: Please enter valid first and last names (letters only).');\n  }\n}\nconsole.log(out.join('\\n'));\n"
    }
  },
  "QBCategory": "CCD",
  "boilerPlates": {
    "C": "#include <stdio.h>\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "C++": "#include <iostream>\nusing namespace std;\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "Java": "import java.util.*;\nimport java.io.*;\n\npublic class Main {\n    public static void main(String[] args) throws IOException {\n        // your code goes here\n    }\n}",
    "Python3": "# your code goes here\n",
    "JavaScript": "// your code goes here\n"
  },
  "_internal": {
    "methodName": "",
    "params": []
  }
}