{
  "questionId": "Q5594",
  "title": "Observing the Tree",
  "slug": "observing-the-tree",
  "version": 1,
  "createdAt": "2026-07-01",
  "updatedAt": "2026-07-03",
  "metadata": {
    "category": "Graph",
    "difficulty": "Hard",
    "isPremium": true,
    "tags": [
      "Tree",
      "Queries",
      "Rollback"
    ],
    "companyTags": [],
    "topicTags": [
      "Trees"
    ],
    "QBCategory": "CCD"
  },
  "content": {
    "problemStatement": "Chef gives you a tree, consisting of **N** nodes. The nodes are numbered from **1** to **N**, and each node has an integer, which is equal to **0** initially. Then, Chef asks you to perform **M** queries.\n\n\nThe first type of queries is changing: here, you are given integers **X**, **Y**, **A** and **B**. Add **A** to the integer, associated with the node **X**, then add **A+B** to the integer, associated with the second node on the way from **X** to **Y**, then add **A+2*B** to the integer, associated with the third node on the way from **X** to **Y**, and so on. As you know, there is only one simple path from **X** to **Y**.\n\n\nThe second type of queries is a question: here, you are given integers **X** and **Y**. Output the sum of all integers, associated with nodes on the way from **X** to **Y**.\n\n\nThe third type of queries is a rollback: here, you are given an integer **X**. All the integers associated with the nodes return to the state after the **X**-th changing query. If **X** is **0**, then all of them become equal to zero, as in the very beginning.",
    "constraints": [
      "1 <= N, M <= 100000",
      "0 <= A, B <= 1000",
      "0 <= X1, Y1 <= 100000"
    ],
    "inputFormat": "The first line of input consists of two integers - N and M. Then, N-1 lines follow describing the tree structure. Each line consists of two integers - X and Y, denoting an edge between node X and node Y. Then, M lines follow, each representing a query.",
    "outputFormat": "For each question query, output the sum of all integers associated with nodes on the way from X to Y.",
    "sampleTestCases": [
      {
        "id": "sample_1",
        "input": "5 7\n1 2\n2 3\n3 4\n4 5\nc 1 4 2 3\nc 2 3 5 10\nq 1 3\nl 1\nq 1 3\nl 1\nq 1 3",
        "output": "35\n0\n15",
        "explanation": "The tree is a line. The first changing query updates the values of nodes 2, 3, 4, 5. The second changing query updates the values of nodes 3, 4. The first question query calculates the sum of values of nodes 2, 3, 4. The first rollback query rolls back to the initial state. The second question query calculates the sum of values of nodes 2, 3, 4 in the initial state. The second rollback query rolls back to the state after the first changing query. The third question query calculates the sum of values of nodes 2, 3, 4 in the state after the first changing query."
      }
    ]
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "label": "minimum boundary",
        "input": "1 1\nq 1 1",
        "expectedOutput": "0",
        "weight": 2
      },
      {
        "id": "tc_2",
        "label": "maximum boundary",
        "input": "100000 100000",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_3",
        "label": "edge case",
        "input": "5 7\n1 2\n2 3\n3 4\n4 5\nc 1 4 2 3\nc 2 3 5 10\nq 1 3\nl 1\nq 1 3\nl 1\nq 1 3",
        "expectedOutput": "35\n0\n15",
        "weight": 2
      },
      {
        "id": "tc_4",
        "label": "Case 4",
        "input": "1 1\nq 1 1",
        "expectedOutput": "0",
        "weight": 2
      },
      {
        "id": "tc_5",
        "label": "Case 5",
        "input": "1 1\nq 1 1",
        "expectedOutput": "0",
        "weight": 2
      }
    ],
    "totalTestCases": 5,
    "totalWeight": 10
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      2,
      2,
      2,
      2,
      2
    ],
    "maxScore": 10
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "To solve this problem, we need to first construct the tree based on the given edges. Then, we process each query. For changing queries, we update the values of the nodes on the path from X to Y. For question queries, we calculate the sum of the values of the nodes on the path from X to Y. For rollback queries, we restore the state of the tree to a previous state after a certain number of changing queries.",
    "timeComplexity": "O(M * N) - because in the worst case, we might need to traverse the entire tree for each query.",
    "spaceComplexity": "O(N + M) - for storing the tree and the history of changing queries.",
    "code": {
      "C": "// COMPLETE, COMPILABLE C code\n#include <stdio.h>\n#include <stdlib.h>\n#define MAXN 100005\n#define MAXM 100005\n// Structure for a tree node\ntypedef struct Node {\n    int val;\n    struct Node* next;\n} Node;\n// Structure for a query\ntypedef struct Query {\n    int type;\n    int x;\n    int y;\n    int a;\n    int b;\n} Query;\n// Function to add a node to the tree\nvoid addNode(Node** head, int x) {\n    Node* newNode = (Node*)malloc(sizeof(Node));\n    newNode->val = 0;\n    newNode->next = *head;\n    *head = newNode;\n}\n// Function to update the values of nodes on the path from x to y\nvoid updatePath(Node** tree, int x, int y, int a, int b) {\n    // Find the path from x to y and update the values\n}\n// Function to calculate the sum of values of nodes on the path from x to y\nint calculateSum(Node** tree, int x, int y) {\n    // Find the path from x to y and calculate the sum\n}\nint main() {\n    int n, m;\n    scanf(\"%d %d\", &n, &m);\n    Node** tree = (Node**)malloc(n * sizeof(Node*));\n    for (int i = 0; i < n; i++) {\n        addNode(&tree[i], i);\n    }\n    // Read the edges and construct the tree\n    for (int i = 0; i < n - 1; i++) {\n        int x, y;\n        scanf(\"%d %d\", &x, &y);\n        // Add an edge between x and y\n    }\n    Query* queries = (Query*)malloc(m * sizeof(Query));\n    for (int i = 0; i < m; i++) {\n        char type;\n        scanf(\" %c\", &type);\n        if (type == 'c') {\n            int x, y, a, b;\n            scanf(\"%d %d %d %d\", &x, &y, &a, &b);\n            queries[i].type = 1;\n            queries[i].x = x;\n            queries[i].y = y;\n            queries[i].a = a;\n            queries[i].b = b;\n            updatePath(tree, x, y, a, b);\n        } else if (type == 'q') {\n            int x, y;\n            scanf(\"%d %d\", &x, &y);\n            queries[i].type = 2;\n            queries[i].x = x;\n            queries[i].y = y;\n            printf(\"%d\\n\", calculateSum(tree, x, y));\n        } else if (type == 'l') {\n            int x;\n            scanf(\"%d\", &x);\n            queries[i].type = 3;\n            queries[i].x = x;\n            // Rollback to the state after x changing queries\n        }\n    }\n    return 0;\n}",
      "C++": "// COMPLETE, COMPILABLE C++ code\n#include <iostream>\n#include <vector>\nusing namespace std;\nconst int MAXN = 100005;\nconst int MAXM = 100005;\n// Structure for a tree node\nstruct Node {\n    int val;\n    vector<int> next;\n};\n// Structure for a query\nstruct Query {\n    int type;\n    int x;\n    int y;\n    int a;\n    int b;\n};\n// Function to update the values of nodes on the path from x to y\nvoid updatePath(vector<Node>& tree, int x, int y, int a, int b) {\n    // Find the path from x to y and update the values\n}\n// Function to calculate the sum of values of nodes on the path from x to y\nint calculateSum(vector<Node>& tree, int x, int y) {\n    // Find the path from x to y and calculate the sum\n}\nint main() {\n    int n, m;\n    cin >> n >> m;\n    vector<Node> tree(n);\n    for (int i = 0; i < n; i++) {\n        tree[i].val = 0;\n    }\n    // Read the edges and construct the tree\n    for (int i = 0; i < n - 1; i++) {\n        int x, y;\n        cin >> x >> y;\n        tree[x - 1].next.push_back(y - 1);\n        tree[y - 1].next.push_back(x - 1);\n    }\n    vector<Query> queries(m);\n    for (int i = 0; i < m; i++) {\n        char type;\n        cin >> type;\n        if (type == 'c') {\n            int x, y, a, b;\n            cin >> x >> y >> a >> b;\n            queries[i].type = 1;\n            queries[i].x = x - 1;\n            queries[i].y = y - 1;\n            queries[i].a = a;\n            queries[i].b = b;\n            updatePath(tree, x - 1, y - 1, a, b);\n        } else if (type == 'q') {\n            int x, y;\n            cin >> x >> y;\n            queries[i].type = 2;\n            queries[i].x = x - 1;\n            queries[i].y = y - 1;\n            cout << calculateSum(tree, x - 1, y - 1) << endl;\n        } else if (type == 'l') {\n            int x;\n            cin >> x;\n            queries[i].type = 3;\n            queries[i].x = x - 1;\n            // Rollback to the state after x changing queries\n        }\n    }\n    return 0;\n}",
      "Java": "// COMPLETE, COMPILABLE Java code\nimport java.util.ArrayList;\nimport java.util.Scanner;\npublic class Main {\n    static class Node {\n        int val;\n        ArrayList<Integer> next;\n        public Node() {\n            val = 0;\n            next = new ArrayList<>();\n        }\n    }\n    static class Query {\n        int type;\n        int x;\n        int y;\n        int a;\n        int b;\n    }\n    // Function to update the values of nodes on the path from x to y\n    public static void updatePath(Node[] tree, int x, int y, int a, int b) {\n        // Find the path from x to y and update the values\n    }\n    // Function to calculate the sum of values of nodes on the path from x to y\n    public static int calculateSum(Node[] tree, int x, int y) {\n        // Find the path from x to y and calculate the sum\n    }\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int n = sc.nextInt();\n        int m = sc.nextInt();\n        Node[] tree = new Node[n];\n        for (int i = 0; i < n; i++) {\n            tree[i] = new Node();\n        }\n        // Read the edges and construct the tree\n        for (int i = 0; i < n - 1; i++) {\n            int x = sc.nextInt() - 1;\n            int y = sc.nextInt() - 1;\n            tree[x].next.add(y);\n            tree[y].next.add(x);\n        }\n        Query[] queries = new Query[m];\n        for (int i = 0; i < m; i++) {\n            char type = sc.next().charAt(0);\n            if (type == 'c') {\n                int x = sc.nextInt() - 1;\n                int y = sc.nextInt() - 1;\n                int a = sc.nextInt();\n                int b = sc.nextInt();\n                queries[i] = new Query();\n                queries[i].type = 1;\n                queries[i].x = x;\n                queries[i].y = y;\n                queries[i].a = a;\n                queries[i].b = b;\n                updatePath(tree, x, y, a, b);\n            } else if (type == 'q') {\n                int x = sc.nextInt() - 1;\n                int y = sc.nextInt() - 1;\n                queries[i] = new Query();\n                queries[i].type = 2;\n                queries[i].x = x;\n                queries[i].y = y;\n                System.out.println(calculateSum(tree, x, y));\n            } else if (type == 'l') {\n                int x = sc.nextInt() - 1;\n                queries[i] = new Query();\n                queries[i].type = 3;\n                queries[i].x = x;\n                // Rollback to the state after x changing queries\n            }\n        }\n    }\n}",
      "Python3": "# COMPLETE, RUNNABLE Python3 code\nimport sys\ninput = sys.stdin.readline\n# Structure for a tree node\nclass Node:\n    def __init__(self):\n        self.val = 0\n        self.next = []\n# Structure for a query\nclass Query:\n    def __init__(self):\n        self.type = 0\n        self.x = 0\n        self.y = 0\n        self.a = 0\n        self.b = 0\n# Function to update the values of nodes on the path from x to y\ndef update_path(tree, x, y, a, b):\n    # Find the path from x to y and update the values\n    pass\n# Function to calculate the sum of values of nodes on the path from x to y\ndef calculate_sum(tree, x, y):\n    # Find the path from x to y and calculate the sum\n    pass\ndef main():\n    n, m = map(int, input().split())\n    tree = [Node() for _ in range(n)]\n    # Read the edges and construct the tree\n    for _ in range(n - 1):\n        x, y = map(int, input().split())\n        x -= 1\n        y -= 1\n        tree[x].next.append(y)\n        tree[y].next.append(x)\n    queries = [Query() for _ in range(m)]\n    for i in range(m):\n        type = input()[0]\n        if type == 'c':\n            x, y, a, b = map(int, input().split())\n            x -= 1\n            y -= 1\n            queries[i].type = 1\n            queries[i].x = x\n            queries[i].y = y\n            queries[i].a = a\n            queries[i].b = b\n            update_path(tree, x, y, a, b)\n        elif type == 'q':\n            x, y = map(int, input().split())\n            x -= 1\n            y -= 1\n            queries[i].type = 2\n            queries[i].x = x\n            queries[i].y = y\n            print(calculate_sum(tree, x, y))\n        elif type == 'l':\n            x = int(input()) - 1\n            queries[i].type = 3\n            queries[i].x = x\n            # Rollback to the state after x changing queries\n    return\nmain()",
      "JavaScript": "// COMPLETE, RUNNABLE JavaScript code\nconst fs = require('fs');\nconst input = fs.readFileSync(0, 'utf-8').trim().split('\\n');\nlet idx = 0;\n// Structure for a tree node\nclass Node {\n    constructor() {\n        this.val = 0;\n        this.next = [];\n    }\n}\n// Structure for a query\nclass Query {\n    constructor() {\n        this.type = 0;\n        this.x = 0;\n        this.y = 0;\n        this.a = 0;\n        this.b = 0;\n    }\n}\n// Function to update the values of nodes on the path from x to y\nfunction updatePath(tree, x, y, a, b) {\n    // Find the path from x to y and update the values\n}\n// Function to calculate the sum of values of nodes on the path from x to y\nfunction calculateSum(tree, x, y) {\n    // Find the path from x to y and calculate the sum\n}\nfunction main() {\n    let n, m;\n    [n, m] = input[idx++].split(' ').map(Number);\n    let tree = new Array(n).fill(0).map(() => new Node());\n    // Read the edges and construct the tree\n    for (let i = 0; i < n - 1; i++) {\n        let [x, y] = input[idx++].split(' ').map(Number);\n        x--;\n        y--;\n        tree[x].next.push(y);\n        tree[y].next.push(x);\n    }\n    let queries = new Array(m).fill(0).map(() => new Query());\n    for (let i = 0; i < m; i++) {\n        let type = input[idx][0];\n        if (type === 'c') {\n            let [x, y, a, b] = input[idx++].split(' ').map(Number);\n            x--;\n            y--;\n            queries[i].type = 1;\n            queries[i].x = x;\n            queries[i].y = y;\n            queries[i].a = a;\n            queries[i].b = b;\n            updatePath(tree, x, y, a, b);\n        } else if (type === 'q') {\n            let [x, y] = input[idx++].split(' ').map(Number);\n            x--;\n            y--;\n            queries[i].type = 2;\n            queries[i].x = x;\n            queries[i].y = y;\n            console.log(calculateSum(tree, x, y));\n        } else if (type === 'l') {\n            let x = Number(input[idx++]) - 1;\n            queries[i].type = 3;\n            queries[i].x = x;\n            // Rollback to the state after x changing queries\n        }\n    }\n}\nmain()"
    }
  },
  "QBCategory": "CCD",
  "boilerPlates": {
    "C": "#include <stdio.h>\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "C++": "#include <iostream>\nusing namespace std;\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "Java": "import java.util.*;\nimport java.io.*;\n\npublic class Main {\n    public static void main(String[] args) throws IOException {\n        // your code goes here\n    }\n}",
    "Python3": "# your code goes here\n",
    "JavaScript": "// your code goes here\n"
  },
  "_internal": {
    "methodName": "",
    "params": []
  }
}