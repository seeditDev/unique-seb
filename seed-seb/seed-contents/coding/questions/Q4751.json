{
  "questionId": "Q4751",
  "title": "Prime factorisation of numbers",
  "slug": "prime-factorisation-of-numbers",
  "version": 1,
  "createdAt": "2026-07-01",
  "updatedAt": "2026-07-04",
  "metadata": {
    "category": "Math",
    "difficulty": "Medium",
    "isPremium": true,
    "tags": [
      "Prime Factorization",
      "Sieve of Eratosthenes"
    ],
    "companyTags": [],
    "topicTags": [
      "Combinatorics & Number Theory"
    ],
    "QBCategory": "CCD"
  },
  "content": {
    "problemStatement": "You are given an array of integers numbersnumbersnumbers of length nnn.\n\nYour task is to compute the **prime factorisation** of each number and return the result as a list of lists, where each inner list contains the prime factors in **ascending order**.\n\n**Function Declaration****Function Name**primeFactorizationprimeFactorizationprimeFactorization – This function computes the prime factors of each number in the given array.\n\n**Parameters**\nnumbersnumbersnumbers : A list of integers of length nnn, where each integer represents a number to factorize.\n**Return Value**\nReturns a list of lists.\nEach inner list contains the prime factors of the corresponding number in **ascending order**.\n`The input and output formats given below are only if you want to test using custom inputs.`\n\n**Constraints**\n1≤n≤1051 \\leq n \\leq 10^51≤n≤105\n2≤numbers[i]≤2×1052 \\leq numbers[i] \\leq 2 \\times 10^52≤numbers[i]≤2×105\n\nInput Format\n\nThe first line contains a single integer nnn — the number of elements in the array.\nThe second line contains nnn space-separated integers — the elements of the array numbersnumbersnumbers.\n\n\nOutput Format\n\nPrint nnn lines.\nEach line contains the **prime factors** of the corresponding number in ascending order, separated by spaces.",
    "constraints": [
      "1 <= T <= 10^5",
      "1 <= N <= 10^5",
      "2 <= numbers[i] <= 2 * 10^5"
    ],
    "inputFormat": "First line contains T, the number of test cases. Each test case contains a single integer N, followed by N space-separated integers.",
    "outputFormat": "For each test case, print the prime factors of each number in ascending order, separated by spaces.",
    "sampleTestCases": [
      {
        "id": "sample_1",
        "input": "3\n8 9 10",
        "output": "2 2 2\n3 3\n2 5",
        "explanation": "Explanation:\n8 = 2 × 2 × 2\n9 = 3 × 3\n10 = 2 × 5"
      },
      {
        "id": "sample_2",
        "input": "3\n14 21 25",
        "output": "2 7\n3 7\n5 5",
        "explanation": "Explanation:\n14 = 2 × 7\n21 = 3 × 7\n25 = 5 × 5"
      },
      {
        "id": "sample_3",
        "input": "2\n17 30",
        "output": "17\n2 3 5",
        "explanation": "Explanation:\n17 is a prime number.\n30 = 2 × 3 × 5"
      }
    ]
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "label": "minimum boundary",
        "input": "1\n2",
        "expectedOutput": "2",
        "weight": 2
      },
      {
        "id": "tc_2",
        "label": "max boundary",
        "input": "1\n200000",
        "expectedOutput": "2 2 2 2 2 2 2 2 2 2 2 2 2 2 2 2 2 2 2 2",
        "weight": 2
      },
      {
        "id": "tc_3",
        "label": "all-same",
        "input": "3\n2 2 2",
        "expectedOutput": "2\n2\n2",
        "weight": 2
      },
      {
        "id": "tc_4",
        "label": "tricky edge",
        "input": "1\n1",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_5",
        "label": "stress",
        "input": "100\n100000 100000 100000 100000 100000",
        "expectedOutput": "2 2 2 2 2 2 2 2 2 2 2 2 2 2 2 2 2 2 2 2\n2 2 2 2 2 2 2 2 2 2 2 2 2 2 2 2 2 2 2 2\n2 2 2 2 2 2 2 2 2 2 2 2 2 2 2 2 2 2 2 2\n2 2 2 2 2 2 2 2 2 2 2 2 2 2 2 2 2 2 2 2\n2 2 2 2 2 2 2 2 2 2 2 2 2 2 2 2 2 2 2 2",
        "weight": 2
      }
    ],
    "totalTestCases": 5,
    "totalWeight": 10
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      2,
      2,
      2,
      2,
      2
    ],
    "maxScore": 10
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "We use the Sieve of Eratosthenes algorithm to precompute the smallest prime factor for every number up to 2 * 10^5. Then, for each test case, we factorize each number using the precomputed smallest prime factors.",
    "timeComplexity": "O(T * N * log(max(numbers[i]))) per test case, O(T * N * log(max(numbers[i]))) total. Precomputation is done once in O(maxN log(log(maxN))) time.",
    "spaceComplexity": "O(maxN) for the precomputed smallest prime factors, where maxN = 2 * 10^5.",
    "code": {
      "C": "// COMPLETE, COMPILABLE C code\n// Uses correct I/O\n// Handles all T test cases\n#include <stdio.h>\n#include <stdlib.h>\n\nvoid primeFactorization(int *numbers, int n) {\n    if (n <= 0) return;\n\n    int maxVal = 0;\n    for (int i = 0; i < n; ++i) if (numbers[i] > maxVal) maxVal = numbers[i];\n    if (maxVal < 2) maxVal = 2;\n\n    // allocate SPF array of size maxVal+1\n    int *spf = (int*)malloc((maxVal + 1) * sizeof(int));\n    if (!spf) {\n        // allocation failed\n        return;\n    }\n\n    // initialize spf: spf[x] = x\n    spf[0] = 0;\n    spf[1] = 1;\n    for (int i = 2; i <= maxVal; i++) spf[i] = i;\n\n    // sieve to fill smallest prime factors\n    for (long long i = 2; i * i <= maxVal; ++i) {\n        if (spf[i] == i) {\n            for (long long j = i * i; j <= maxVal; j += i) {\n                if (spf[j] == j) spf[j] = (int)i;\n            }\n        }\n    }\n\n    // factorize each number using spf\n    for (int i = 0; i < n; ++i) {\n        int num = numbers[i];\n        // As per constraints num >= 2, but handle safely\n        if (num <= 1) {\n            printf(\"\\n\");\n            continue;\n        }\n\n        int firstPrinted = 0;\n        while (num > 1) {\n            int p = spf[num];\n            // print all p's\n            while (num % p == 0) {\n                if (firstPrinted) printf(\" \");\n                printf(\"%d\", p);\n                firstPrinted = 1;\n                num /= p;\n            }\n        }\n        printf(\"\\n\");\n    }\n\n    free(spf);\n}\n\nint main() {\n    int t;\n    scanf(\"%d\", &t);\n    while (t--) {\n        int n;\n        scanf(\"%d\", &n);\n        int *numbers = (int*)malloc(n * sizeof(int));\n        for (int i = 0; i < n; i++) {\n            scanf(\"%d\", &numbers[i]);\n        }\n        primeFactorization(numbers, n);\n        free(numbers);\n    }\n    return 0;\n}",
      "C++": "// COMPLETE, COMPILABLE C++ code\n// Uses correct I/O\n// Handles all T test cases\n#include <iostream>\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    vector<vector<int>> primeFactorization(vector<int> &numbers) {\n        int maxN = 200000; // since numbers[i] <= 2*10^5\n        vector<int> spf(maxN + 1);\n\n        // Precompute smallest prime factor for every number\n        for (int i = 2; i <= maxN; i++) {\n            spf[i] = (i % 2 == 0 ? 2 : i);\n        }\n\n        for (long long i = 3; i * i <= maxN; i += 2) {\n            if (spf[i] == i) {\n                for (long long j = i * i; j <= maxN; j += i) {\n                    if (spf[j] == j) spf[j] = i;\n                }\n            }\n        }\n\n        vector<vector<int>> result;\n\n        // Factorize each number using spf[]\n        for (int num : numbers) {\n            vector<int> factors;\n            while (num > 1) {\n                int p = spf[num];\n                while (num % p == 0) {\n                    factors.push_back(p);\n                    num /= p;\n                }\n            }\n            result.push_back(factors);\n        }\n\n        return result;\n    }\n};\n\nint main() {\n    int t;\n    cin >> t;\n    while (t--) {\n        int n;\n        cin >> n;\n        vector<int> numbers(n);\n        for (int i = 0; i < n; i++) {\n            cin >> numbers[i];\n        }\n        Solution solution;\n        vector<vector<int>> result = solution.primeFactorization(numbers);\n        for (const auto &factors : result) {\n            for (int factor : factors) {\n                cout << factor << \" \";\n            }\n            cout << endl;\n        }\n    }\n    return 0;\n}",
      "Java": "// COMPLETE, COMPILABLE Java code\n// Class MUST be named Main\n// Uses correct I/O\npublic class Main {\n\n    public static List<List<Integer>> primeFactorization(int[] numbers) {\n        int maxN = 200000;  // since numbers[i] ≤ 2×10^5\n        int[] spf = new int[maxN + 1];\n\n        // Initialize SPF array\n        for (int i = 2; i <= maxN; i++) {\n            if (i % 2 == 0) spf[i] = 2;\n            else spf[i] = i;\n        }\n\n        // Compute SPF using sieve\n        for (int i = 3; i * i <= maxN; i += 2) {\n            if (spf[i] == i) {\n                for (int j = i * i; j <= maxN; j += i) {\n                    if (spf[j] == j) spf[j] = i;\n                }\n            }\n        }\n\n        List<List<Integer>> result = new ArrayList<>();\n\n        // Factorize each number\n        for (int num : numbers) {\n            List<Integer> factors = new ArrayList<>();\n            while (num > 1) {\n                int p = spf[num];\n                while (num % p == 0) {\n                    factors.add(p);\n                    num /= p;\n                }\n            }\n            result.add(factors);\n        }\n\n        return result;\n    }\n\n    public static void main(String[] args) {\n        Scanner scanner = new Scanner(System.in);\n        int t = scanner.nextInt();\n        while (t-- > 0) {\n            int n = scanner.nextInt();\n            int[] numbers = new int[n];\n            for (int i = 0; i < n; i++) {\n                numbers[i] = scanner.nextInt();\n            }\n            List<List<Integer>> result = primeFactorization(numbers);\n            for (List<Integer> factors : result) {\n                for (Integer factor : factors) {\n                    System.out.print(factor + \" \");\n                }\n                System.out.println();\n            }\n        }\n    }\n}",
      "Python3": "# COMPLETE, RUNNABLE Python3 code\n# Uses input() or sys.stdin\n# Handles all T test cases\nimport sys\n\nclass Solution:\n    def primeFactorization(self, numbers):\n        maxN = 200000  # since numbers[i] ≤ 2×10^5\n        spf = [0] * (maxN + 1)\n\n        # Precompute smallest prime factor for every number\n        for i in range(2, maxN + 1):\n            if i % 2 == 0:\n                spf[i] = 2\n            else:\n                spf[i] = i\n\n        i = 3\n        while i * i <= maxN:\n            if spf[i] == i:\n                for j in range(i * i, maxN + 1, i):\n                    if spf[j] == j:\n                        spf[j] = i\n            i += 2\n\n        result = []\n\n        # Factorization using spf[]\n        for num in numbers:\n            factors = []\n            while num > 1:\n                p = spf[num]\n                while num % p == 0:\n                    factors.append(p)\n                    num //= p\n            result.append(factors)\n\n        return result\n\n# Read input from stdin\ninput_lines = sys.stdin.readlines()\ninput_index = 0\n\ndef read_int():\n    global input_index\n    result = int(input_lines[input_index].strip())\n    input_index += 1\n    return result\n\ndef read_ints():\n    global input_index\n    result = list(map(int, input_lines[input_index].strip().split()))\n    input_index += 1\n    return result\n\nt = read_int()\nwhile t > 0:\n    n = read_int()\n    numbers = read_ints()\n    solution = Solution()\n    result = solution.primeFactorization(numbers)\n    for factors in result:\n        print(' '.join(map(str, factors)))\n    t -= 1",
      "JavaScript": "// COMPLETE, RUNNABLE JavaScript code\n// Uses fs.readFileSync(0,'utf-8') — NEVER readline()\n// Handles all T test cases\nconst fs = require('fs');\nconst input = fs.readFileSync(0, 'utf-8').trim().split('\\n');\nlet idx = 0;\n\nfunction readLine() {\n    return input[idx++];\n}\n\nfunction readInt() {\n    return parseInt(readLine());\n}\n\nfunction readInts() {\n    return readLine().split(' ').map(x => parseInt(x));\n}\n\nclass Solution {\n    primeFactorization(numbers) {\n        const maxN = 200000; // since numbers[i] ≤ 2×10^5\n        const spf = new Array(maxN + 1).fill(0);\n\n        // Initialize SPF\n        for (let i = 2; i <= maxN; i++) {\n            if (i % 2 === 0) spf[i] = 2;\n            else spf[i] = i;\n        }\n\n        // Compute SPF using sieve\n        for (let i = 3; i * i <= maxN; i += 2) {\n            if (spf[i] === i) {\n                for (let j = i * i; j <= maxN; j += i) {\n                    if (spf[j] === j) spf[j] = i;\n                }\n            }\n        }\n\n        let result = [];\n\n        // Factorize each number\n        for (let num of numbers) {\n            let factors = [];\n            while (num > 1) {\n                let p = spf[num];\n                // print all p's\n                while (num % p === 0) {\n                    factors.push(p);\n                    num = Math.floor(num / p);\n                }\n            }\n            result.push(factors);\n        }\n\n        return result;\n    }\n}\n\nlet t = readInt();\nwhile (t-- > 0) {\n    let n = readInt();\n    let numbers = readInts();\n    let solution = new Solution();\n    let result = solution.primeFactorization(numbers);\n    for (let factors of result) {\n        console.log(factors.join(' '));\n    }\n}"
    }
  },
  "QBCategory": "CCD",
  "boilerPlates": {
    "C": "#include <stdio.h>\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "C++": "#include <iostream>\nusing namespace std;\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "Java": "import java.util.*;\nimport java.io.*;\n\npublic class Main {\n    public static void main(String[] args) throws IOException {\n        // your code goes here\n    }\n}",
    "Python3": "# your code goes here\n",
    "JavaScript": "// your code goes here\n"
  },
  "_internal": {
    "methodName": "",
    "params": []
  }
}