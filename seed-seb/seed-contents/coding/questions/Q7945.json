{
  "questionId": "Q7945",
  "title": "Maximum Value",
  "slug": "maximum-value",
  "version": 1,
  "createdAt": "2026-07-02",
  "updatedAt": "2026-07-04",
  "metadata": {
    "category": "Arrays | Math | Greedy",
    "difficulty": "Medium",
    "isPremium": true,
    "tags": [
      "Maximum Pair Value",
      "Sorting",
      "Absolute Difference",
      "Long Long"
    ],
    "companyTags": [],
    "topicTags": [
      "Sorting"
    ],
    "QBCategory": "CCD"
  },
  "content": {
    "problemStatement": "Given an array AAA of size NNN. Find the maximum value of the expression a∗b+a−ba * b + a - ba∗b+a−b where aaa and bbb are 222 distinct elements of the array.\n\n###Input:\n\n\nFirst line will contain TTT, number of testcases. Then the testcases follow.\nEach testcase contains of two lines of input.\nFirst line will contain NNN, number of elements of the array.\nSecond line contains NNN space separated integers, elements of the array AAA.\n###Output:\r\nFor each testcase, output in a single line answer to the problem.\n\n###Constraints\n\n\n1≤T≤201 \\leq T \\leq 201≤T≤20\n2≤N≤1052 \\leq N \\leq 10^52≤N≤105\n−109≤Ai≤109-10^9 \\leq A_i \\leq 10^9−109≤Ai​≤109",
    "constraints": [
      "1 <= T <= 20",
      "2 <= N <= 100000",
      "-10^9 <= Ai <= 10^9"
    ],
    "inputFormat": "The first line contains an integer T, the number of test cases. For each test case: the first line contains an integer N (the size of the array). The second line contains N space‑separated integers A1, A2, …, AN.",
    "outputFormat": "For each test case output a single line containing the maximum possible value of a·b + |a‑b| where a and b are values of two distinct elements of the array.",
    "sampleTestCases": [
      {
        "id": "sample_1",
        "input": "2\n2   \n2 2\n3\n5 3 2",
        "output": "4\n17",
        "explanation": "Explanation:\nCase 1: Only possible pair is [2,2][2, 2][2,2] for which answer = 2∗2+2−22 * 2 + 2 - 22∗2+2−2 = 444.\nCase 2: There are six possible pairs.\nFor pairs [2,3][2, 3][2,3] and [3,2][3, 2][3,2], answer = 2∗3+max(2−3,3−2)2 * 3 + max(2 - 3, 3 - 2)2∗3+max(2−3,3−2) = 777\nFor pairs [3,5][3, 5][3,5] and [5,3][5, 3][5,3] answer = 5∗3+max(3−5,5−3)5 * 3 + max(3 - 5, 5 - 3)5∗3+max(3−5,5−3) = 171717 and\nFor the pairs [2,5][2, 5][2,5] and [5,2][5, 2][5,2], answer = 2∗5+max(2−5,5−2)2 * 5 + max(2 - 5, 5 - 2)2∗5+max(2−5,5−2) = 131313.\nSo final answer is maximum of {7,17,13}\\{7, 17, 13\\}{7,17,13} = 171717."
      }
    ]
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "label": "Case 1",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_2",
        "label": "Case 2",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_3",
        "label": "Case 3",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_4",
        "label": "Case 4",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_5",
        "label": "Case 5",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      }
    ],
    "totalTestCases": 5,
    "totalWeight": 10
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      2,
      2,
      2,
      2,
      2
    ],
    "maxScore": 10
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "For a pair (a,b) the expression can be written as a*b + |a-b|. If we assume a >= b, the value becomes a*b + a - b = a*(b+1) - b. For a fixed a, the expression is monotonic in b: it increases with b when a > 1, is constant when a = 1, and decreases with b when a < 1. Consequently the optimum will be obtained by using extreme values of the array – the largest positives or the smallest (most negative) numbers. It is sufficient to examine all pairs formed by the three smallest and the three largest elements (after sorting). This set contains at most 6 numbers, so checking every unordered pair among them (at most 15 pairs) yields the answer. The algorithm per test case:\n1. Read N and the array.\n2. Sort the array.\n3. Collect the first three and the last three elements (if N < 6, just take all elements).\n4. Iterate over all unordered pairs of the collected elements, compute val = a*b + llabs(a-b) using 64‑bit integers, and keep the maximum.\n5. Output the maximum.\nThe sorting step dominates the complexity: O(N log N) time and O(N) memory.",
    "timeComplexity": "O(N log N) per test case (sorting). The pair checking part is O(1).",
    "spaceComplexity": "O(N) for storing the array (can be O(1) extra besides the input).",
    "code": {
      "C": "#include <stdio.h>\n#include <stdlib.h>\n#include <limits.h>\n#include <inttypes.h>\n\nint cmp_ll(const void *a, const void *b) {\n    long long x = *(const long long *)a;\n    long long y = *(const long long *)b;\n    if (x < y) return -1;\n    if (x > y) return 1;\n    return 0;\n}\n\nint main() {\n    int T; if (scanf(\"%d\", &T)!=1) return 0;\n    while (T--) {\n        int N; scanf(\"%d\", &N);\n        long long *arr = (long long*)malloc(N * sizeof(long long));\n        for (int i = 0; i < N; ++i) scanf(\"%lld\", &arr[i]);\n        qsort(arr, N, sizeof(long long), cmp_ll);\n        long long cand[6];\n        int cnt = 0;\n        for (int i = 0; i < 3 && i < N; ++i) cand[cnt++] = arr[i];\n        for (int i = N-3; i < N; ++i) if (i >= 0) cand[cnt++] = arr[i];\n        long long best = LLONG_MIN;\n        for (int i = 0; i < cnt; ++i) {\n            for (int j = i+1; j < cnt; ++j) {\n                long long a = cand[i], b = cand[j];\n                long long val = a * b + llabs(a - b);\n                if (val > best) best = val;\n            }\n        }\n        printf(\"%lld\\n\", best);\n        free(arr);\n    }\n    return 0;\n}",
      "C++": "#include <bits/stdc++.h>\nusing namespace std;\nint main(){\n    ios::sync_with_stdio(false);\n    cin.tie(nullptr);\n    int T; if(!(cin>>T)) return 0;\n    while(T--){\n        int N; cin>>N;\n        vector<long long> a(N);\n        for(int i=0;i<N;++i) cin>>a[i];\n        sort(a.begin(), a.end());\n        vector<long long> cand;\n        for(int i=0;i<3 && i<N;++i) cand.push_back(a[i]);\n        for(int i=max(0,N-3); i<N; ++i) cand.push_back(a[i]);\n        long long best = LLONG_MIN;\n        for(size_t i=0;i<cand.size();++i){\n            for(size_t j=i+1;j<cand.size();++j){\n                long long x=cand[i], y=cand[j];\n                long long val = x*y + llabs(x-y);\n                best = max(best, val);\n            }\n        }\n        cout<<best<<\"\\n\";\n    }\n    return 0;\n}",
      "Java": "import java.io.*;\nimport java.util.*;\n\npublic class Main {\n    public static void main(String[] args) throws Exception {\n        FastScanner fs = new FastScanner(System.in);\n        int T = fs.nextInt();\n        StringBuilder sb = new StringBuilder();\n        while (T-- > 0) {\n            int N = fs.nextInt();\n            long[] arr = new long[N];\n            for (int i = 0; i < N; i++) arr[i] = fs.nextLong();\n            Arrays.sort(arr);\n            List<Long> cand = new ArrayList<>();\n            for (int i = 0; i < 3 && i < N; i++) cand.add(arr[i]);\n            for (int i = Math.max(0, N - 3); i < N; i++) cand.add(arr[i]);\n            long best = Long.MIN_VALUE;\n            for (int i = 0; i < cand.size(); i++) {\n                for (int j = i + 1; j < cand.size(); j++) {\n                    long a = cand.get(i);\n                    long b = cand.get(j);\n                    long val = a * b + Math.abs(a - b);\n                    if (val > best) best = val;\n                }\n            }\n            sb.append(best).append('\\n');\n        }\n        System.out.print(sb.toString());\n    }\n\n    // Fast scanner for large input\n    static class FastScanner {\n        private final BufferedInputStream in;\n        private final byte[] buffer = new byte[1 << 16];\n        private int ptr = 0, len = 0;\n        FastScanner(InputStream stream) { in = new BufferedInputStream(stream); }\n        private int readByte() throws IOException {\n            if (ptr >= len) {\n                len = in.read(buffer);\n                ptr = 0;\n                if (len <= 0) return -1;\n            }\n            return buffer[ptr++];\n        }\n        long nextLong() throws IOException { int c; do { c = readByte(); } while (c <= ' ');\n            boolean neg = false; if (c == '-') { neg = true; c = readByte(); }\n            long val = 0; while (c > ' ') { val = val * 10 + (c - '0'); c = readByte(); }\n            return neg ? -val : val; }\n        int nextInt() throws IOException { return (int) nextLong(); }\n    }\n}",
      "Python3": "import sys\n\ndef solve():\n    data = sys.stdin.read().strip().split()\n    if not data:\n        return\n    it = iter(data)\n    T = int(next(it))\n    out_lines = []\n    for _ in range(T):\n        N = int(next(it))\n        arr = [int(next(it)) for _ in range(N)]\n        arr.sort()\n        cand = arr[:3] + arr[-3:]\n        best = -10**30\n        m = len(cand)\n        for i in range(m):\n            a = cand[i]\n            for j in range(i+1, m):\n                b = cand[j]\n                val = a * b + abs(a - b)\n                if val > best:\n                    best = val\n        out_lines.append(str(best))\n    sys.stdout.write('\\n'.join(out_lines))\n\nif __name__ == \"__main__\":\n    solve()",
      "JavaScript": "const fs = require('fs');\nconst input = fs.readFileSync(0, 'utf-8').trim().split(/\\s+/).map(Number);\nlet idx = 0;\nconst T = input[idx++];\nlet outputs = [];\nfor (let t = 0; t < T; t++) {\n  const N = input[idx++];\n  const arr = input.slice(idx, idx + N);\n  idx += N;\n  arr.sort((a, b) => a - b);\n  const cand = [];\n  for (let i = 0; i < 3 && i < N; i++) cand.push(arr[i]);\n  for (let i = Math.max(0, N - 3); i < N; i++) cand.push(arr[i]);\n  let best = -Infinity;\n  for (let i = 0; i < cand.length; i++) {\n    for (let j = i + 1; j < cand.length; j++) {\n      const a = cand[i];\n      const b = cand[j];\n      const val = a * b + Math.abs(a - b);\n      if (val > best) best = val;\n    }\n  }\n  outputs.push(String(best));\n}\nconsole.log(outputs.join('\\n'));"
    }
  },
  "QBCategory": "CCD",
  "boilerPlates": {
    "C": "#include <stdio.h>\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "C++": "#include <iostream>\nusing namespace std;\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "Java": "import java.util.*;\nimport java.io.*;\n\npublic class Main {\n    public static void main(String[] args) throws IOException {\n        // your code goes here\n    }\n}",
    "Python3": "# your code goes here\n",
    "JavaScript": "// your code goes here\n"
  },
  "_internal": {
    "methodName": "",
    "params": []
  }
}