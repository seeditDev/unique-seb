{
  "questionId": "Q1136",
  "title": "All Primes in Range",
  "slug": "all-primes-in-range",
  "version": 1,
  "createdAt": "2026-07-01",
  "updatedAt": "2026-07-06",
  "metadata": {
    "category": "Math",
    "difficulty": "Easy",
    "isPremium": false,
    "tags": [
      "Prime Numbers",
      "Math",
      "Sieve of Eratosthenes"
    ],
    "companyTags": [],
    "topicTags": [
      "Control Flow"
    ]
  },
  "content": {
    "problemStatement": "Write a program to print all prime numbers in the range [LLL, RRR].\n\n\nInput Format\n\nThe first and only line of input contains two space separated integers, LLL and RRR - The start and end of the range.\n\n\nOutput Format\n\nPrint on a single line, all prime numbers between LLL and RRR (inclusive) in increasing order with a space between them.",
    "constraints": [
      "1 <= L <= R <= 1000"
    ],
    "inputFormat": "The first and only line of input contains two space-separated integers, L and R, denoting the start and end of the range.",
    "outputFormat": "Print on a single line, all prime numbers between L and R (inclusive) in increasing order with a space between them.",
    "sampleTestCases": [
      {
        "id": "sample_1",
        "input": "2 11",
        "output": "2 3 5 7 11",
        "explanation": "There is a single test case. The primes between 2 and 11 inclusive are 2, 3, 5, 7 and 11."
      },
      {
        "id": "sample_2",
        "input": "11 13",
        "output": "11 13",
        "explanation": "Only the numbers 11 and 13 are prime in the given range, so they are printed separated by a space."
      },
      {
        "id": "sample_3",
        "input": "5 5",
        "output": "5",
        "explanation": "The range consists of a single number 5, which is prime, therefore the output is just \"5\"."
      }
    ]
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "label": "maximum boundary",
        "input": "1000 1000",
        "expectedOutput": "",
        "weight": 2
      }
    ],
    "totalTestCases": 5,
    "totalWeight": 10
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      2,
      2,
      2,
      2,
      2
    ],
    "maxScore": 10
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "The problem can be solved using the Sieve of Eratosthenes algorithm to generate all prime numbers up to 1000. Then, for each test case, we simply print the prime numbers within the given range.",
    "timeComplexity": "O(N log log N) for the Sieve of Eratosthenes, where N is 1000. Then, O(R - L) for printing the prime numbers in each test case.",
    "spaceComplexity": "O(N) for storing the prime numbers, where N is 1000.",
    "code": {
      "C": "#include <stdio.h>\n#include <stdbool.h>\n\nbool isPrime[1001];\n\nvoid sieve() {\n    for (int i = 0; i <= 1000; i++) isPrime[i] = true;\n    isPrime[0] = isPrime[1] = false;\n    for (int p = 2; p * p <= 1000; p++) if (isPrime[p]) for (int q = p * p; q <= 1000; q += p) isPrime[q] = false;\n}\n\nint main() {\n    sieve();\n    int L, R; scanf(\"%d %d\", &L, &R);\n    bool first = true;\n    for (int i = L; i <= R; i++) if (isPrime[i]) {\n        if (!first) printf(\" \");\n        printf(\"%d\", i);\n        first = false;\n    }\n    if (first) printf(\"\");\n    printf(\"\\n\");\n    return 0;\n}",
      "C++": "#include <bits/stdc++.h>\nusing namespace std;\n\nconst int MAX = 1000;\nbool prime[MAX + 1];\n\nvoid sieve() {\n    fill(prime, prime + MAX + 1, true);\n    prime[0] = prime[1] = false;\n    for (int p = 2; p * p <= MAX; p++) if (prime[p]) for (int q = p * p; q <= MAX; q += p) prime[q] = false;\n}\n\nint main() {\n    ios::sync_with_stdio(false);\n    cin.tie(NULL);\n    sieve();\n    int L, R; cin >> L >> R;\n    bool first = true;\n    for (int i = L; i <= R; i++) if (prime[i]) {\n        if (!first) cout << ' ';\n        cout << i;\n        first = false;\n    }\n    if (first) cout << \"\";\n    cout << \"\\n\";\n    return 0;\n}",
      "Java": "import java.io.*;\nimport java.util.*;\npublic class Main {\n    public static void main(String[] args) throws Exception {\n        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));\n        StringTokenizer st = new StringTokenizer(br.readLine());\n        int L = Integer.parseInt(st.nextToken());\n        int R = Integer.parseInt(st.nextToken());\n        final int MAX = 1000;\n        boolean[] prime = new boolean[MAX + 1];\n        Arrays.fill(prime, true);\n        prime[0] = prime[1] = false;\n        for (int p = 2; p * p <= MAX; p++) if (prime[p]) for (int q = p * p; q <= MAX; q += p) prime[q] = false;\n        StringBuilder out = new StringBuilder();\n        boolean first = true;\n        for (int i = L; i <= R; i++) if (prime[i]) {\n            if (!first) out.append(' ');\n            out.append(i);\n            first = false;\n        }\n        if (first) out.append(\"\");\n        System.out.print(out.toString());\n    }\n}",
      "Python3": "import sys\n\ndef sieve(limit):\n    prime = [True] * (limit + 1)\n    prime[0] = prime[1] = False\n    for p in range(2, int(limit ** 0.5) + 1):\n        if prime[p]:\n            for q in range(p * p, limit + 1, p):\n                prime[q] = False\n    return prime\n\nprime = sieve(1000)\n\nL, R = map(int, sys.stdin.readline().split())\nline = [str(i) for i in range(L, R + 1) if prime[i]]\nprint(' '.join(line) if line else '')",
      "JavaScript": "const fs = require('fs');\nconst data = fs.readFileSync(0, 'utf-8').trim().split(/\\s+/);\nif (data.length === 0) process.exit(0);\nlet idx = 0;\nconst L = parseInt(data[idx++], 10);\nconst R = parseInt(data[idx++], 10);\nconst MAX = 1000;\nconst prime = new Array(MAX + 1).fill(true);\nprime[0] = prime[1] = false;\nfor (let p = 2; p * p <= MAX; p++) if (prime[p]) for (let q = p * p; q <= MAX; q += p) prime[q] = false;\nlet output = [];\nfor (let i = L; i <= R; i++) if (prime[i]) output.push(i.toString());\nconsole.log(output.join(' ') || '')"
    }
  }
}