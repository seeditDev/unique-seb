{
  "questionId": "Q11794",
  "title": "Detect A cycle in Undirected Graph using BFS",
  "slug": "course-schedule",
  "version": 1,
  "createdAt": "2026-07-05",
  "updatedAt": "2026-07-05",
  "metadata": {
    "category": "Graph",
    "difficulty": "Hard",
    "isPremium": true,
    "tags": [
      "BFS",
      "Cycle Detection",
      "Undirected Graph"
    ],
    "companyTags": [],
    "topicTags": [],
    "QBCategory": "CCD"
  },
  "content": {
    "problemStatement": "Given an undirected graph with V vertices and E edges, check whether it contains any cycle or not.&nbsp;",
    "constraints": [
      "1 <= V <= 10^5",
      "1 <= E <= 10^5",
      "1 <= u, v <= V"
    ],
    "inputFormat": "The first line of input contains two integers V and E, where V is the number of vertices and E is the number of edges. The next E lines contain two integers u and v, representing an edge between vertices u and v.",
    "outputFormat": "The output will be 1 if the graph contains a cycle, otherwise 0.",
    "sampleTestCases": [
      {
        "id": "sample_1",
        "input": "V = 8, E = 7",
        "output": "0",
        "explanation": "No cycle in the given graph.\n\n\n\n\n**Example 2:**"
      },
      {
        "id": "sample_2",
        "input": "V = 8, E = 6",
        "output": "1",
        "explanation": "4-&gt;5-&gt;6-&gt;4 is a cycle.\n\n\n\n\n**Solution**\n\n\n\n**Disclaimer**: Don't jump directly to the solution, try it out yourself first."
      }
    ]
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "label": "min boundary",
        "input": "1 0",
        "expectedOutput": "0",
        "weight": 2
      },
      {
        "id": "tc_2",
        "label": "max boundary",
        "input": "100000 99999",
        "expectedOutput": "1",
        "weight": 2
      },
      {
        "id": "tc_3",
        "label": "uniform/edge cases",
        "input": "5 4",
        "expectedOutput": "0",
        "weight": 2
      },
      {
        "id": "tc_4",
        "label": "tricky case / empty-like / negative if allowed",
        "input": "0 0",
        "expectedOutput": "0",
        "weight": 2
      },
      {
        "id": "tc_5",
        "label": "general mixed case",
        "input": "8 7",
        "expectedOutput": "0",
        "weight": 2
      }
    ],
    "totalTestCases": 5,
    "totalWeight": 10
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      2,
      2,
      2,
      2,
      2
    ],
    "maxScore": 10
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "We will use BFS traversal to detect a cycle in the graph. We will keep track of the parent of each node and if we encounter a node that is already visited and is not the parent of the current node, then we have found a cycle.",
    "timeComplexity": "O(V + E)",
    "spaceComplexity": "O(V)",
    "code": {
      "C": "#include <stdio.h>\n#include <stdlib.h>\n#include <stdbool.h>\n\n// Structure for a queue node\ntypedef struct QueueNode {\n    int vertex;\n    struct QueueNode* next;\n} QueueNode;\n\n// Structure for a graph\ntypedef struct Graph {\n    int numVertices;\n    int* visited;\n    int** adjLists;\n} Graph;\n\n// Function to create a graph\nGraph* createGraph(int vertices) {\n    Graph* graph = (Graph*) malloc(sizeof(Graph));\n    graph->numVertices = vertices;\n    graph->visited = (int*) malloc(vertices * sizeof(int));\n    graph->adjLists = (int**) malloc(vertices * sizeof(int*));\n    for (int i = 0; i < vertices; i++) {\n        graph->adjLists[i] = (int*) malloc(2 * sizeof(int));\n        graph->adjLists[i][0] = -1;\n        graph->adjLists[i][1] = -1;\n    }\n    return graph;\n}\n\n// Function to add an edge to the graph\nvoid addEdge(Graph* graph, int src, int dest) {\n    int* newNode = (int*) malloc(2 * sizeof(int));\n    newNode[0] = dest;\n    newNode[1] = graph->adjLists[src][0];\n    graph->adjLists[src][0] = newNode - graph->adjLists[src];\n}\n\n// Function to detect a cycle using BFS\nbool detectCycle(Graph* graph, int vertex) {\n    int* visited = (int*) malloc(graph->numVertices * sizeof(int));\n    for (int i = 0; i < graph->numVertices; i++) {\n        visited[i] = 0;\n    }\n    QueueNode* queue = (QueueNode*) malloc(sizeof(QueueNode));\n    queue->vertex = vertex;\n    queue->next = NULL;\n    visited[vertex] = 1;\n    while (queue != NULL) {\n        int currentVertex = queue->vertex;\n        QueueNode* temp = queue;\n        queue = queue->next;\n        free(temp);\n        for (int i = 0; i < graph->numVertices; i++) {\n            if (graph->adjLists[currentVertex][i] != -1 && !visited[graph->adjLists[currentVertex][i]]) {\n                QueueNode* newNode = (QueueNode*) malloc(sizeof(QueueNode));\n                newNode->vertex = graph->adjLists[currentVertex][i];\n                newNode->next = queue;\n                queue = newNode;\n                visited[graph->adjLists[currentVertex][i]] = 1;\n            } else if (graph->adjLists[currentVertex][i] != -1 && visited[graph->adjLists[currentVertex][i]]) {\n                return true;\n            }\n        }\n    }\n    return false;\n}\n\nint main() {\n    int vertices, edges;\n    scanf(\"%d %d\", &vertices, &edges);\n    Graph* graph = createGraph(vertices);\n    for (int i = 0; i < edges; i++) {\n        int src, dest;\n        scanf(\"%d %d\", &src, &dest);\n        addEdge(graph, src, dest);\n        addEdge(graph, dest, src);\n    }\n    bool cycle = false;\n    for (int i = 0; i < vertices; i++) {\n        if (!graph->visited[i]) {\n            if (detectCycle(graph, i)) {\n                cycle = true;\n                break;\n            }\n        }\n    }\n    if (cycle) {\n        printf(\"1\\n\");\n    } else {\n        printf(\"0\\n\");\n    }\n    return 0;\n}",
      "C++": "#include <bits/stdc++.h>\nusing namespace std;\n\nclass Solution {\n  private: \n  bool detect(int src, vector<int> adj[], int vis[]) {\n      vis[src] = 1; \n      // store <source node, parent node>\n      queue<pair<int,int>> q; \n      q.push({src, -1}); \n      // traverse until queue is not empty\n      while(!q.empty()) {\n          int node = q.front().first; \n          int parent = q.front().second; \n          q.pop(); \n          \n          // go to all adjacent nodes\n          for(auto adjacentNode: adj[node]) {\n              // if adjacent node is unvisited\n              if(!vis[adjacentNode]) {\n                  vis[adjacentNode] = 1; \n                  q.push({adjacentNode, node}); \n              }\n              // if adjacent node is visited and is not it's own parent node\n              else if(parent != adjacentNode) {\n                  // yes it is a cycle\n                  return true; \n              }\n          }\n      }\n      // there's no cycle\n      return false; \n  }\n  public:\n    // Function to detect cycle in an undirected graph.\n    bool isCycle(int V, vector<int> adj[]) {\n        // initialise them as unvisited \n        int vis[V] = {0};\n        for(int i = 0;i<V;i++) {\n            if(!vis[i]) {\n                if(detect(i, adj, vis)) return true; \n            }\n        }\n        return false; \n    }\n};\n\nint main() {\n    \n    // V = 4, E = 2\n    vector<int> adj[4] = {{}, {2}, {1, 3}, {2}};\n    Solution obj;\n    bool ans = obj.isCycle(4, adj);\n    if (ans)\n        cout << \"1\\n\";\n    else\n        cout << \"0\\n\";\n    return 0;\n}",
      "Java": "import java.util.*;\n\nclass Solution\n{\n   static boolean checkForCycle(ArrayList<ArrayList<Integer>> adj, int s,\n            boolean vis[], int parent[])\n    {\n       Queue<Node> q =  new LinkedList<>(); //BFS\n       q.add(new Node(s, -1));\n       vis[s] =true;\n       \n       // until the queue is empty\n       while(!q.isEmpty())\n       {\n           // source node and its parent node\n           int node = q.peek().first;\n           int par = q.peek().second;\n           q.remove(); \n           \n           // go to all the adjacent nodes\n           for(Integer it: adj.get(node))\n           {\n               if(vis[it]==false)  \n               {\n                   q.add(new Node(it, node));\n                   vis[it] = true; \n               }\n        \n                // if adjacent node is visited and is not its own parent node\n               else if(par != it) return true;\n           }\n       }\n       \n       return false;\n    }\n    \n    // function to detect cycle in an undirected graph\n    public boolean isCycle(int V, ArrayList<ArrayList<Integer>> adj)\n    {\n        boolean vis[] = new boolean[V];\n        Arrays.fill(vis,false);\n        int parent[] = new int[V];\n        Arrays.fill(parent,-1);  \n        \n        for(int i=0;i<V;i++)\n            if(vis[i]==false) \n                if(checkForCycle(adj, i,vis, parent)) \n                    return true;\n    \n        return false;\n    }\n    \n    public static void main(String[] args)\n    {\n        ArrayList<ArrayList<Integer>> adj = new ArrayList<>();\n        for (int i = 0; i < 4; i++) {\n            adj.add(new ArrayList < > ());\n        }\n        adj.get(1).add(2);\n        adj.get(2).add(1);\n        adj.get(2).add(3);\n        adj.get(3).add(2);\n                \n        Solution obj = new Solution();\n        boolean ans = obj.isCycle(4, adj);\n        if (ans)\n            System.out.println(\"1\");    \n        else\n            System.out.println(\"0\");\n    }\n}\n\nclass Node {\n    int first;\n    int second;\n    public Node(int first, int second) {\n        this.first = first;\n        this.second = second; \n    }\n}",
      "Python3": "from collections import defaultdict, deque\n\nclass Solution:\n    def isCycle(self, V, adj):\n        visited = [False] * V\n        for i in range(V):\n            if not visited[i]:\n                if self.detect(i, adj, visited):\n                    return True\n        return False\n    \n    def detect(self, src, adj, visited):\n        visited[src] = True\n        queue = deque([(src, -1)])\n        while queue:\n            node, parent = queue.popleft()\n            for adjacentNode in adj[node]:\n                if not visited[adjacentNode]:\n                    visited[adjacentNode] = True\n                    queue.append((adjacentNode, node))\n                elif parent != adjacentNode:\n                    return True\n        return False\n\nif __name__ == '__main__':\n    V = 4\n    adj = [[] for _ in range(V)]\n    adj[1].append(2)\n    adj[2].append(1)\n    adj[2].append(3)\n    adj[3].append(2)\n    obj = Solution()\n    ans = obj.isCycle(V, adj)\n    if ans:\n        print(1)\n    else:\n        print(0)",
      "JavaScript": "class Solution {\n    isCycle(V, adj) {\n        let visited = new Array(V).fill(false);\n        for (let i = 0; i < V; i++) {\n            if (!visited[i]) {\n                if (this.detect(i, adj, visited)) return true;\n            }\n        }\n        return false;\n    }\n    \n    detect(src, adj, visited) {\n        visited[src] = true;\n        let queue = [[src, -1]];\n        while (queue.length > 0) {\n            let [node, parent] = queue.shift();\n            for (let adjacentNode of adj[node]) {\n                if (!visited[adjacentNode]) {\n                    visited[adjacentNode] = true;\n                    queue.push([adjacentNode, node]);\n                } else if (parent !== adjacentNode) {\n                    return true;\n                }\n            }\n        }\n        return false;\n    }\n}\n\nlet V = 4;\nlet adj = [[]];\nfor (let i = 1; i < V; i++) {\n    adj.push([]);\n}\nadj[1].push(2);\nadj[2].push(1);\nadj[2].push(3);\nadj[3].push(2);\nlet obj = new Solution();\nlet ans = obj.isCycle(V, adj);\nif (ans) {\n    console.log(1);\n} else {\n    console.log(0);\n}"
    }
  },
  "QBCategory": "CCD",
  "boilerPlates": {
    "C": "#include <stdio.h>\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "C++": "#include <iostream>\nusing namespace std;\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "Java": "import java.util.*;\nimport java.io.*;\n\npublic class Main {\n    public static void main(String[] args) throws IOException {\n        // your code goes here\n    }\n}",
    "Python3": "# your code goes here\n",
    "JavaScript": "// your code goes here\n"
  },
  "_internal": {
    "methodName": "",
    "params": []
  }
}