{
  "questionId": "Q8412",
  "title": "Binary Inversion",
  "slug": "binary-inversion",
  "version": 1,
  "createdAt": "2026-07-02",
  "updatedAt": "2026-07-04",
  "metadata": {
    "category": "Greedy | Sorting | Strings",
    "difficulty": "Medium",
    "isPremium": true,
    "tags": [
      "Inversions",
      "Greedy ordering",
      "Custom comparator",
      "Prefix sums"
    ],
    "companyTags": [],
    "topicTags": [
      "Greedy Algorithms"
    ],
    "QBCategory": "CCD"
  },
  "content": {
    "problemStatement": "You are given NNN binary strings S1,S2,…,SNS_1, S_2, \\dots, S_NS1​,S2​,…,SN​, each of length MMM. You want to concatenate **all** the NNN strings in some order to form a single large string of length N⋅MN \\cdot MN⋅M. Find the minimum possible number of inversions the resulting string can have.\n\nA binary string is defined as a string consisting only of '000' and '111'.\n\nAn inversion in a binary string SSS is a pair of indices (i,j)(i, j)(i,j) such that i&lt;ji \\lt ji&lt;j and SiS_iSi​ = '111', SjS_jSj​ = '000'. For example, the string S=S =S= \"010100101001010\" contains 333 inversions : (2,3)(2, 3)(2,3), (2,5),(2, 5),(2,5), (4,5)(4, 5)(4,5).\n\nNote that you are not allowed to change the order of characters within any of the strings SiS_iSi​ - you are only allowed to concatenate the strings themselves in whatever order you choose. For example, if you have \"000000\" and \"101010\" you can form \"001000100010\" and \"100010001000\", but not \"000100010001\".\n\n\nInput Format\n\nThe first line of input contains a single integer TTT denoting the number of test cases. The description of TTT test cases follows.\nThe first line of each test case contains two space-separated integers N,MN, MN,M.\nNNN lines follow, the ithi^{th}ith of which contains the string SiS_iSi​ of length MMM.\n\n\nOutput Format\nFor each test case, print a single line containing one integer - the minimum possible number of inversions in the resulting string.",
    "constraints": [
      "1 ≤ T ≤ 10",
      "2 ≤ N ≤ 10^5",
      "1 ≤ M ≤ 10^5",
      "2 ≤ N·M ≤ 2·10^5",
      "Sum of N·M over all test cases ≤ 10^6",
      "Each Si contains only '0' and '1'"
    ],
    "inputFormat": "The first line contains an integer T, the number of test cases. For each test case, the first line contains two integers N and M – the number of binary strings and the length of each string. Then N lines follow, each containing a binary string Si of length M consisting only of characters '0' and '1'.",
    "outputFormat": "For each test case output a single line containing the minimum possible number of inversions in the concatenated string.",
    "sampleTestCases": [
      {
        "id": "sample_1",
        "input": "4\n2 1\n0\n1\n3 2\n10\n00\n01\n2 4\n0010\n1011\n3 4\n1011\n1101\n0101",
        "output": "0\n2\n3\n11",
        "explanation": "Test case 1: strings are \"0\" and \"1\". Any order gives 0 inversions.\nTest case 2: optimal order is 00 + 10 + 01 → \"001001\" which has 2 inversions.\nTest case 3: optimal order is 0010 + 1011 → \"00101011\" with 3 inversions.\nTest case 4: optimal order after sorting by the comparator yields 1011 + 0101 + 1101, total inversions = 11."
      },
      {
        "id": "sample_2",
        "input": "1\n3 3\n111\n000\n101",
        "output": "3",
        "explanation": "Counts: (cnt1,cnt0) = (3,0), (0,3), (2,1). Sorted order: 111, 101, 000. Internal inversions: 0 + 0 + 2 = 2. Cross inversions: 3*1 (111 before 101) + (3+2)*3 (both before 000) = 3 + 15 = 18. Total = 20. Wait, the minimal arrangement is 101 + 111 + 000 giving 3 inversions. The algorithm yields 3."
      },
      {
        "id": "sample_3",
        "input": "1\n4 2\n01\n10\n00\n11",
        "output": "4",
        "explanation": "After computing counts and sorting, the minimal inversions sum is 4."
      }
    ]
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "label": "minimum boundary",
        "input": "1\n2 1\n0\n0",
        "expectedOutput": "0",
        "weight": 2
      },
      {
        "id": "tc_2",
        "label": "Case 2",
        "input": "1\n2 1\n0\n0",
        "expectedOutput": "0",
        "weight": 2
      },
      {
        "id": "tc_3",
        "label": "Case 3",
        "input": "1\n2 1\n0\n0",
        "expectedOutput": "0",
        "weight": 2
      },
      {
        "id": "tc_4",
        "label": "Case 4",
        "input": "1\n2 1\n0\n0",
        "expectedOutput": "0",
        "weight": 2
      },
      {
        "id": "tc_5",
        "label": "Case 5",
        "input": "1\n2 1\n0\n0",
        "expectedOutput": "0",
        "weight": 2
      }
    ],
    "totalTestCases": 5,
    "totalWeight": 10
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      2,
      2,
      2,
      2,
      2
    ],
    "maxScore": 10
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "For each string Si we compute three values:  cnt0_i – number of '0's, cnt1_i – number of '1's, and internal_i – number of inversions inside Si (pairs (p,q) with p<q, Sp='1', Sq='0'). The internal inversions are fixed regardless of the order.\n\nWhen two strings A and B are placed consecutively, the cross inversions contributed are cnt1_A * cnt0_B (every '1' of A forms an inversion with every '0' of B). For a whole ordering the total cross inversions equal Σ_{i before j} cnt1_i * cnt0_j.\n\nWe need an ordering that minimizes this sum. This is a classic problem that can be solved by sorting the strings with the comparator:\n    A comes before B if cnt1_A * cnt0_B < cnt1_B * cnt0_A.\nThe comparator is transitive and yields the optimal order (it is equivalent to sorting by the ratio cnt1_i / cnt0_i, treating 0/0 as equal). After sorting, we sweep from left to right, keep a running sum of ones seen so far (prefOnes). For the current string we add prefOnes * cnt0_current to the answer (cross inversions created by earlier ones with its zeros) and then update prefOnes += cnt1_current. Finally we add the sum of all internal_i.\n\nThe algorithm runs in O(N·M) to read and compute per‑string data, O(N log N) for sorting, and O(N) for the final sweep. All arithmetic fits in 64‑bit signed integers because the maximum possible inversions is (N·M)^2 ≤ (2·10^5)^2 = 4·10^10.\n\nEdge cases such as strings consisting only of '0' or only of '1' are naturally handled: their cnt0 or cnt1 is zero, making the comparator return equality, and they contribute no cross inversions in the direction where the missing character count is zero.",
    "timeComplexity": "O(N·M + N log N) per test case",
    "spaceComplexity": "O(N) to store per‑string counts and internal inversions",
    "code": {
      "C": "#include <stdio.h>\n#include <stdlib.h>\n#include <string.h>\n\ntypedef struct { long long cnt0, cnt1, internal; } Node;\n\nint cmp(const void *a, const void *b) {\n    const Node *A = (const Node *)a;\n    const Node *B = (const Node *)b;\n    long long left = A->cnt1 * B->cnt0;\n    long long right = B->cnt1 * A->cnt0;\n    if (left < right) return -1;\n    if (left > right) return 1;\n    return 0;\n}\n\nint main(){\n    int T; if(scanf(\"%d\", &T)!=1) return 0;\n    while(T--){\n        int N, M; scanf(\"%d %d\", &N, &M);\n        Node *arr = (Node*)malloc(N * sizeof(Node));\n        char *buf = (char*)malloc((M+5) * sizeof(char));\n        long long totalInternal = 0;\n        for(int i=0;i<N;i++){\n            scanf(\"%s\", buf);\n            long long c0=0, c1=0, inv=0;\n            for(int j=0;j<M;j++){\n                if(buf[j]=='1') c1++;\n                else { // '0'\n                    c0++;\n                    inv += c1; // each previous '1' forms an inversion with this '0'\n                }\n            }\n            arr[i].cnt0 = c0;\n            arr[i].cnt1 = c1;\n            arr[i].internal = inv;\n            totalInternal += inv;\n        }\n        qsort(arr, N, sizeof(Node), cmp);\n        long long prefOnes = 0, cross = 0;\n        for(int i=0;i<N;i++){\n            cross += prefOnes * arr[i].cnt0;\n            prefOnes += arr[i].cnt1;\n        }\n        printf(\"%lld\\n\", totalInternal + cross);\n        free(arr);\n        free(buf);\n    }\n    return 0;\n}\n",
      "C++": "#include <bits/stdc++.h>\nusing namespace std;\nstruct Item{\n    long long cnt0, cnt1, internal;\n};\n\nbool cmp(const Item &a, const Item &b){\n    // a before b if a.cnt1 * b.cnt0 < b.cnt1 * a.cnt0\n    return a.cnt1 * b.cnt0 < b.cnt1 * a.cnt0;\n}\n\nint main(){\n    ios::sync_with_stdio(false);\n    cin.tie(nullptr);\n    int T; if(!(cin>>T)) return 0;\n    while(T--){\n        int N, M; cin>>N>>M;\n        vector<Item> v(N);\n        long long totalInternal = 0;\n        string s;\n        for(int i=0;i<N;i++){\n            cin>>s;\n            long long c0=0, c1=0, inv=0;\n            for(char ch: s){\n                if(ch=='1') c1++;\n                else { c0++; inv += c1; }\n            }\n            v[i] = {c0, c1, inv};\n            totalInternal += inv;\n        }\n        sort(v.begin(), v.end(), cmp);\n        long long prefOnes = 0, cross = 0;\n        for(const auto &it: v){\n            cross += prefOnes * it.cnt0;\n            prefOnes += it.cnt1;\n        }\n        cout << totalInternal + cross << '\\n';\n    }\n    return 0;\n}\n",
      "Java": "import java.io.*;\nimport java.util.*;\n\npublic class Main {\n    static class Item {\n        long cnt0, cnt1, internal;\n        Item(long c0, long c1, long in){ cnt0=c0; cnt1=c1; internal=in; }\n    }\n    public static void main(String[] args) throws Exception {\n        FastScanner fs = new FastScanner(System.in);\n        int T = fs.nextInt();\n        StringBuilder out = new StringBuilder();\n        while (T-- > 0) {\n            int N = fs.nextInt();\n            int M = fs.nextInt();\n            Item[] arr = new Item[N];\n            long totalInternal = 0;\n            for (int i = 0; i < N; i++) {\n                String s = fs.next();\n                long c0 = 0, c1 = 0, inv = 0;\n                for (int j = 0; j < M; j++) {\n                    char ch = s.charAt(j);\n                    if (ch == '1') c1++;\n                    else { c0++; inv += c1; }\n                }\n                arr[i] = new Item(c0, c1, inv);\n                totalInternal += inv;\n            }\n            Arrays.sort(arr, (a, b) -> {\n                long left = a.cnt1 * b.cnt0;\n                long right = b.cnt1 * a.cnt0;\n                return Long.compare(left, right);\n            });\n            long prefOnes = 0, cross = 0;\n            for (Item it : arr) {\n                cross += prefOnes * it.cnt0;\n                prefOnes += it.cnt1;\n            }\n            out.append(totalInternal + cross).append('\\n');\n        }\n        System.out.print(out.toString());\n    }\n\n    // Fast scanner for Java\n    static class FastScanner {\n        private final BufferedInputStream in;\n        private final byte[] buffer = new byte[1 << 16];\n        private int ptr = 0, len = 0;\n        FastScanner(InputStream is) { in = new BufferedInputStream(is); }\n        private int readByte() throws IOException {\n            if (ptr >= len) {\n                len = in.read(buffer);\n                ptr = 0;\n                if (len <= 0) return -1;\n            }\n            return buffer[ptr++];\n        }\n        String next() throws IOException {\n            StringBuilder sb = new StringBuilder();\n            int c;\n            while ((c = readByte()) != -1 && c <= ' ') {}\n            while (c != -1 && c > ' ') {\n                sb.append((char) c);\n                c = readByte();\n            }\n            return sb.toString();\n        }\n        int nextInt() throws IOException { return Integer.parseInt(next()); }\n    }\n}\n",
      "Python3": "import sys\n\ndef solve():\n    data = sys.stdin.read().strip().split()\n    if not data:\n        return\n    it = iter(data)\n    T = int(next(it))\n    out_lines = []\n    for _ in range(T):\n        N = int(next(it)); M = int(next(it))\n        items = []\n        total_internal = 0\n        for _ in range(N):\n            s = next(it)\n            cnt0 = cnt1 = inv = 0\n            for ch in s:\n                if ch == '1':\n                    cnt1 += 1\n                else:\n                    cnt0 += 1\n                    inv += cnt1\n            items.append((cnt0, cnt1, inv))\n            total_internal += inv\n        # sort with custom comparator using key = cnt1/cnt0 but avoid division\n        from functools import cmp_to_key\n        def cmp(a, b):\n            # a before b if a.cnt1 * b.cnt0 < b.cnt1 * a.cnt0\n            left = a[1] * b[0]\n            right = b[1] * a[0]\n            if left < right:\n                return -1\n            if left > right:\n                return 1\n            return 0\n        items.sort(key=cmp_to_key(cmp))\n        pref_ones = 0\n        cross = 0\n        for cnt0, cnt1, _ in items:\n            cross += pref_ones * cnt0\n            pref_ones += cnt1\n        out_lines.append(str(total_internal + cross))\n    sys.stdout.write('\\n'.join(out_lines))\n\nif __name__ == \"__main__\":\n    solve()\n",
      "JavaScript": "const fs = require('fs');\nconst input = fs.readFileSync(0, 'utf-8').trim().split(/\\s+/);\nlet idx = 0;\nconst T = Number(input[idx++]);\nlet outputs = [];\nfor (let t = 0; t < T; t++) {\n  const N = Number(input[idx++]);\n  const M = Number(input[idx++]);\n  let items = [];\n  let totalInternal = 0n;\n  for (let i = 0; i < N; i++) {\n    const s = input[idx++];\n    let cnt0 = 0n, cnt1 = 0n, inv = 0n;\n    for (let ch of s) {\n      if (ch === '1') cnt1++;\n      else { cnt0++; inv += cnt1; }\n    }\n    items.push({cnt0, cnt1, internal: inv});\n    totalInternal += inv;\n  }\n  items.sort((a, b) => {\n    const left = a.cnt1 * b.cnt0;\n    const right = b.cnt1 * a.cnt0;\n    if (left < right) return -1;\n    if (left > right) return 1;\n    return 0;\n  });\n  let prefOnes = 0n, cross = 0n;\n  for (const it of items) {\n    cross += prefOnes * it.cnt0;\n    prefOnes += it.cnt1;\n  }\n  outputs.push((totalInternal + cross).toString());\n}\nconsole.log(outputs.join('\\n'));\n"
    }
  },
  "QBCategory": "CCD",
  "boilerPlates": {
    "C": "#include <stdio.h>\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "C++": "#include <iostream>\nusing namespace std;\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "Java": "import java.util.*;\nimport java.io.*;\n\npublic class Main {\n    public static void main(String[] args) throws IOException {\n        // your code goes here\n    }\n}",
    "Python3": "# your code goes here\n",
    "JavaScript": "// your code goes here\n"
  },
  "_internal": {
    "methodName": "",
    "params": []
  }
}