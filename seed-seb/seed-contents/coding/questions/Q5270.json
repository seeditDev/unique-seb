{
  "questionId": "Q5270",
  "title": "Even Coefficients",
  "slug": "even-coefficients",
  "version": 1,
  "createdAt": "2026-07-01",
  "updatedAt": "2026-07-04",
  "metadata": {
    "category": "Math",
    "difficulty": "Hard",
    "isPremium": true,
    "tags": [
      "Determinant",
      "Polynomial",
      "Cofactor Expansion"
    ],
    "companyTags": [],
    "topicTags": [
      "Mathematics"
    ],
    "QBCategory": "CCD"
  },
  "content": {
    "problemStatement": "In this problem, you are given a polynomial P over multiple variables with integer coefficients and your task is simply to determine if all of the coefficients are even or not.\n\n\n\nUnfortunately, you will not be given an explicit description of the polynomial. Rather, you will be given a square matrix whose entries are polynomials and the polynomial P is the determinant of this matrix.\n\n\n\nHere, we are defining the determinant of a matrix by the usual cofactor expansion formula. That is, say the matrix M is of size d x d and denote the polynomial located at row i, column j by Mi,j for any i,j between 1 and d. For a permutation X on the set {1,2,...,d}, let sgn(X) be (-1)inv(X) where inv(X) is the number of pairs 1 ≤ i &lt; j ≤ d with X(i) &gt; X(j) (i.e. the number of inversions of X). Then the determinant of M is the sum, over all permutations X, of the products sgn(X)M1,X(1)M2,X(2)...Md,X(d).",
    "constraints": [
      "1 <= T <= 30",
      "1 <= d <= 40",
      "Each polynomial is given by a string of length between 1 and 100"
    ],
    "inputFormat": "The first line contains an integer T, the number of test cases. Each test case begins with a single line containing an integer d, indicating the order of the matrix. Then, d lines follow where each contains d strings describing the polynomials. The d strings on a single line are separated by a single space.",
    "outputFormat": "For each test case, print 'All Even' if all coefficients of the polynomial represented by the determinant of the matrix are even, or 'Some Odd' if there is at least one odd coefficient.",
    "sampleTestCases": [
      {
        "id": "sample_1",
        "input": "3\n2\nx+1 0\n0 x\n3\nx y z\ny z x\nxx+xy xy+xz xx+xz\n1\na+a",
        "output": "Some Odd\nAll Even\nAll Even",
        "explanation": "The first test case has a matrix with two rows and two columns. The determinant of this matrix is calculated using the cofactor expansion formula, which results in a polynomial with some odd coefficients. The second test case has a matrix with three rows and three columns. The determinant of this matrix is calculated using the cofactor expansion formula, which results in a polynomial with all even coefficients. The third test case has a matrix with one row and one column. The determinant of this matrix is simply the value of the single element, which is an even number."
      },
      {
        "id": "sample_2",
        "input": "1\n2\n2 0\n0 2",
        "output": "All Even",
        "explanation": "The determinant of the matrix is calculated using the cofactor expansion formula, which results in a polynomial with all even coefficients."
      },
      {
        "id": "sample_3",
        "input": "1\n1\n1",
        "output": "Some Odd",
        "explanation": "The determinant of the matrix is simply the value of the single element, which is an odd number."
      }
    ]
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "label": "minimum boundary",
        "input": "1\n1\n0",
        "expectedOutput": "All Even",
        "weight": 2
      },
      {
        "id": "tc_2",
        "label": "maximum boundary",
        "input": "1\n40\nx y z\ny z x\nxx+xy xy+xz xx+xz",
        "expectedOutput": "All Even",
        "weight": 2
      },
      {
        "id": "tc_3",
        "label": "uniform/edge case",
        "input": "1\n2\n0 0\n0 0",
        "expectedOutput": "All Even",
        "weight": 2
      },
      {
        "id": "tc_4",
        "label": "tricky case",
        "input": "1\n2\n1 1\n1 1",
        "expectedOutput": "Some Odd",
        "weight": 2
      },
      {
        "id": "tc_5",
        "label": "stress/general",
        "input": "3\n2\nx+1 0\n0 x\n3\nx y z\ny z x\nxx+xy xy+xz xx+xz\n1\na+a",
        "expectedOutput": "Some Odd\nAll Even\nAll Even",
        "weight": 2
      }
    ],
    "totalTestCases": 5,
    "totalWeight": 10
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      2,
      2,
      2,
      2,
      2
    ],
    "maxScore": 10
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "The problem can be solved by calculating the determinant of the given matrix using the cofactor expansion formula. Since the matrix entries are polynomials, we need to consider the coefficients of these polynomials. We can use a recursive approach to calculate the determinant. The key insight is to recognize that if all coefficients of the polynomials in the matrix are even, then the determinant will also have all even coefficients.",
    "timeComplexity": "O(d!) per test case, O(T*d!) total, where d is the order of the matrix and T is the number of test cases.",
    "spaceComplexity": "O(d^2) for storing the matrix, where d is the order of the matrix.",
    "code": {
      "C": "#include <stdio.h>\n#include <stdlib.h>\n#include <string.h>\n\n// Function to calculate the determinant of a matrix\nint calculateDeterminant(char ***matrix, int d) {\n    if (d == 1) {\n        // Base case: 1x1 matrix\n        return atoi(matrix[0][0]);\n    }\n\n    int det = 0;\n    for (int i = 0; i < d; i++) {\n        // Create a sub-matrix by removing the first row and the i-th column\n        char ***subMatrix = (char ***)malloc((d - 1) * sizeof(char **));\n        for (int j = 0; j < d - 1; j++) {\n            subMatrix[j] = (char **)malloc(d * sizeof(char *));\n            for (int k = 0; k < d; k++) {\n                if (k < i) {\n                    subMatrix[j][k] = matrix[j + 1][k];\n                } else {\n                    subMatrix[j][k] = matrix[j + 1][k + 1];\n                }\n            }\n        }\n\n        // Recursively calculate the determinant of the sub-matrix\n        int subDet = calculateDeterminant(subMatrix, d - 1);\n\n        // Update the determinant using the cofactor expansion formula\n        det += (i % 2 == 0 ? 1 : -1) * atoi(matrix[0][i]) * subDet;\n\n        // Free the sub-matrix memory\n        for (int j = 0; j < d - 1; j++) {\n            free(subMatrix[j]);\n        }\n        free(subMatrix);\n    }\n\n    return det;\n}\n\nint main() {\n    int T;\n    scanf(\"%d\", &T);\n\n    for (int t = 0; t < T; t++) {\n        int d;\n        scanf(\"%d\", &d);\n\n        char ***matrix = (char ***)malloc(d * sizeof(char **));\n        for (int i = 0; i < d; i++) {\n            matrix[i] = (char **)malloc(d * sizeof(char *));\n            for (int j = 0; j < d; j++) {\n                matrix[i][j] = (char *)malloc(101 * sizeof(char));\n                scanf(\"%s\", matrix[i][j]);\n            }\n        }\n\n        int det = calculateDeterminant(matrix, d);\n\n        if (det % 2 == 0) {\n            printf(\"All Even\\n\");\n        } else {\n            printf(\"Some Odd\\n\");\n        }\n\n        // Free the matrix memory\n        for (int i = 0; i < d; i++) {\n            for (int j = 0; j < d; j++) {\n                free(matrix[i][j]);\n            }\n            free(matrix[i]);\n        }\n        free(matrix);\n    }\n\n    return 0;\n}",
      "C++": "#include <iostream>\n#include <vector>\n#include <string>\nusing namespace std;\n\n// Function to calculate the determinant of a matrix\nint calculateDeterminant(const vector<vector<string>>& matrix, int d) {\n    if (d == 1) {\n        // Base case: 1x1 matrix\n        return stoi(matrix[0][0]);\n    }\n\n    int det = 0;\n    for (int i = 0; i < d; i++) {\n        // Create a sub-matrix by removing the first row and the i-th column\n        vector<vector<string>> subMatrix(d - 1, vector<string>(d - 1));\n        for (int j = 0; j < d - 1; j++) {\n            for (int k = 0; k < d - 1; k++) {\n                if (k < i) {\n                    subMatrix[j][k] = matrix[j + 1][k];\n                } else {\n                    subMatrix[j][k] = matrix[j + 1][k + 1];\n                }\n            }\n        }\n\n        // Recursively calculate the determinant of the sub-matrix\n        int subDet = calculateDeterminant(subMatrix, d - 1);\n\n        // Update the determinant using the cofactor expansion formula\n        det += (i % 2 == 0 ? 1 : -1) * stoi(matrix[0][i]) * subDet;\n    }\n\n    return det;\n}\n\nint main() {\n    ios::sync_with_stdio(false);\n    cin.tie(NULL);\n    int T;\n    cin >> T;\n\n    for (int t = 0; t < T; t++) {\n        int d;\n        cin >> d;\n\n        vector<vector<string>> matrix(d, vector<string>(d));\n        for (int i = 0; i < d; i++) {\n            for (int j = 0; j < d; j++) {\n                cin >> matrix[i][j];\n            }\n        }\n\n        int det = calculateDeterminant(matrix, d);\n\n        if (det % 2 == 0) {\n            cout << \"All Even\" << endl;\n        } else {\n            cout << \"Some Odd\" << endl;\n        }\n    }\n\n    return 0;\n}",
      "Java": "import java.util.Scanner;\npublic class Main {\n    // Function to calculate the determinant of a matrix\n    public static int calculateDeterminant(String[][] matrix, int d) {\n        if (d == 1) {\n            // Base case: 1x1 matrix\n            return Integer.parseInt(matrix[0][0]);\n        }\n\n        int det = 0;\n        for (int i = 0; i < d; i++) {\n            // Create a sub-matrix by removing the first row and the i-th column\n            String[][] subMatrix = new String[d - 1][d - 1];\n            for (int j = 0; j < d - 1; j++) {\n                for (int k = 0; k < d - 1; k++) {\n                    if (k < i) {\n                        subMatrix[j][k] = matrix[j + 1][k];\n                    } else {\n                        subMatrix[j][k] = matrix[j + 1][k + 1];\n                    }\n                }\n            }\n\n            // Recursively calculate the determinant of the sub-matrix\n            int subDet = calculateDeterminant(subMatrix, d - 1);\n\n            // Update the determinant using the cofactor expansion formula\n            det += (i % 2 == 0 ? 1 : -1) * Integer.parseInt(matrix[0][i]) * subDet;\n        }\n\n        return det;\n    }\n\n    public static void main(String[] args) {\n        Scanner scanner = new Scanner(System.in);\n        int T = scanner.nextInt();\n        scanner.nextLine();\n\n        for (int t = 0; t < T; t++) {\n            int d = scanner.nextInt();\n            scanner.nextLine();\n\n            String[][] matrix = new String[d][d];\n            for (int i = 0; i < d; i++) {\n                String[] row = scanner.nextLine().split(\" \");\n                for (int j = 0; j < d; j++) {\n                    matrix[i][j] = row[j];\n                }\n            }\n\n            int det = calculateDeterminant(matrix, d);\n\n            if (det % 2 == 0) {\n                System.out.println(\"All Even\");\n            } else {\n                System.out.println(\"Some Odd\");\n            }\n        }\n    }\n}",
      "Python3": "import sys\n\n# Function to calculate the determinant of a matrix\ndef calculateDeterminant(matrix, d):\n    if d == 1:\n        # Base case: 1x1 matrix\n        return int(matrix[0][0])\n\n    det = 0\n    for i in range(d):\n        # Create a sub-matrix by removing the first row and the i-th column\n        subMatrix = [[matrix[j + 1][k] if k < i else matrix[j + 1][k + 1] for k in range(d - 1)] for j in range(d - 1)]\n\n        # Recursively calculate the determinant of the sub-matrix\n        subDet = calculateDeterminant(subMatrix, d - 1)\n\n        # Update the determinant using the cofactor expansion formula\n        det += (1 if i % 2 == 0 else -1) * int(matrix[0][i]) * subDet\n\n    return det\n\n# Read input from standard input\nT = int(sys.stdin.readline())\n\nfor t in range(T):\n    d = int(sys.stdin.readline())\n\n    matrix = [sys.stdin.readline().split() for _ in range(d)]\n\n    det = calculateDeterminant(matrix, d)\n\n    if det % 2 == 0:\n        print(\"All Even\")\n    else:\n        print(\"Some Odd\")",
      "JavaScript": "const lines = require('fs').readFileSync(0, 'utf-8').trim().split('\\n');\nlet idx = 0;\n\n// Function to calculate the determinant of a matrix\nfunction calculateDeterminant(matrix, d) {\n    if (d == 1) {\n        // Base case: 1x1 matrix\n        return parseInt(matrix[0][0]);\n    }\n\n    let det = 0;\n    for (let i = 0; i < d; i++) {\n        // Create a sub-matrix by removing the first row and the i-th column\n        let subMatrix = [];\n        for (let j = 0; j < d - 1; j++) {\n            subMatrix.push([]);\n            for (let k = 0; k < d - 1; k++) {\n                if (k < i) {\n                    subMatrix[j].push(matrix[j + 1][k]);\n                } else {\n                    subMatrix[j].push(matrix[j + 1][k + 1]);\n                }\n            }\n        }\n\n        // Recursively calculate the determinant of the sub-matrix\n        let subDet = calculateDeterminant(subMatrix, d - 1);\n\n        // Update the determinant using the cofactor expansion formula\n        det += (i % 2 == 0 ? 1 : -1) * parseInt(matrix[0][i]) * subDet;\n    }\n\n    return det;\n}\n\n// Read input from standard input\nlet T = parseInt(lines[idx++]);\n\nfor (let t = 0; t < T; t++) {\n    let d = parseInt(lines[idx++]);\n\n    let matrix = [];\n    for (let i = 0; i < d; i++) {\n        matrix.push(lines[idx++].split(' '));\n    }\n\n    let det = calculateDeterminant(matrix, d);\n\n    if (det % 2 == 0) {\n        console.log('All Even');\n    } else {\n        console.log('Some Odd');\n    }\n}"
    }
  },
  "QBCategory": "CCD",
  "boilerPlates": {
    "C": "#include <stdio.h>\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "C++": "#include <iostream>\nusing namespace std;\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "Java": "import java.util.*;\nimport java.io.*;\n\npublic class Main {\n    public static void main(String[] args) throws IOException {\n        // your code goes here\n    }\n}",
    "Python3": "# your code goes here\n",
    "JavaScript": "// your code goes here\n"
  },
  "_internal": {
    "methodName": "",
    "params": []
  }
}