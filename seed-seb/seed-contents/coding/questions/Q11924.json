{
  "questionId": "Q11924",
  "title": "Print root to leaf path in BT",
  "slug": "printroottoleafpathinbt",
  "version": 1,
  "createdAt": "2026-07-05",
  "updatedAt": "2026-07-05",
  "metadata": {
    "category": "Tree Traversal",
    "difficulty": "Medium",
    "isPremium": true,
    "tags": [
      "Binary Tree",
      "Tree Traversal",
      "Path Finding"
    ],
    "companyTags": [],
    "topicTags": [],
    "QBCategory": "CCD"
  },
  "content": {
    "problemStatement": "Given a Binary Tree and a reference to a root belonging to it. Return the path from the root node to the given leaf node.\n\n**Note**: No two nodes in the tree have the same data value and it is assured that the given node is present and a path always exists.",
    "constraints": [
      "1 <= Number of nodes in the binary tree <= 10^5",
      "1 <= Value of each node <= 10^9",
      "The binary tree is not empty",
      "The target leaf node exists in the binary tree"
    ],
    "inputFormat": "The input will contain a binary tree and a target leaf node value. The binary tree will be represented as a collection of nodes, where each node contains an integer value and references to its left and right children. The target leaf node value will be an integer.",
    "outputFormat": "The output will be a list of integers representing the path from the root node to the target leaf node.",
    "sampleTestCases": []
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "label": "min boundary",
        "input": {
          "root": {
            "val": 1,
            "left": null,
            "right": null
          },
          "target": 1
        },
        "expectedOutput": [
          1
        ],
        "weight": 2
      },
      {
        "id": "tc_2",
        "label": "max boundary",
        "input": {
          "root": {
            "val": 1,
            "left": {
              "val": 2,
              "left": null,
              "right": null
            },
            "right": {
              "val": 3,
              "left": null,
              "right": null
            }
          },
          "target": 3
        },
        "expectedOutput": [
          1,
          3
        ],
        "weight": 2
      },
      {
        "id": "tc_3",
        "label": "uniform/edge cases",
        "input": {
          "root": {
            "val": 1,
            "left": {
              "val": 1,
              "left": null,
              "right": null
            },
            "right": {
              "val": 1,
              "left": null,
              "right": null
            }
          },
          "target": 1
        },
        "expectedOutput": [
          1,
          1
        ],
        "weight": 2
      },
      {
        "id": "tc_4",
        "label": "tricky case / empty-like / negative if allowed",
        "input": {
          "root": {
            "val": 1,
            "left": null,
            "right": {
              "val": 2,
              "left": null,
              "right": null
            }
          },
          "target": 2
        },
        "expectedOutput": [
          1,
          2
        ],
        "weight": 2
      },
      {
        "id": "tc_5",
        "label": "general mixed case",
        "input": {
          "root": {
            "val": 3,
            "left": {
              "val": 5,
              "left": {
                "val": 6,
                "left": null,
                "right": null
              },
              "right": {
                "val": 2,
                "left": {
                  "val": 7,
                  "left": null,
                  "right": null
                },
                "right": {
                  "val": 4,
                  "left": null,
                  "right": null
                }
              }
            },
            "right": {
              "val": 1,
              "left": {
                "val": 0,
                "left": null,
                "right": null
              },
              "right": {
                "val": 8,
                "left": null,
                "right": null
              }
            }
          },
          "target": 7
        },
        "expectedOutput": [
          3,
          5,
          2,
          7
        ],
        "weight": 2
      }
    ],
    "totalTestCases": 5,
    "totalWeight": 10
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      2,
      2,
      2,
      2,
      2
    ],
    "maxScore": 10
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "Recursive or Iterative Tree Traversal",
    "timeComplexity": "O(N)",
    "spaceComplexity": "O(H)",
    "code": {
      "C": "#include <stdio.h>\n#include <stdlib.h>\n\n// Define the structure for a tree node\ntypedef struct TreeNode {\n    int val;\n    struct TreeNode* left;\n    struct TreeNode* right;\n} TreeNode;\n\n// Function to create a new tree node\nTreeNode* createTreeNode(int val) {\n    TreeNode* newNode = (TreeNode*) malloc(sizeof(TreeNode));\n    if (!newNode) {\n        printf(\"Memory error\n\");\n        return NULL;\n    }\n    newNode->val = val;\n    newNode->left = newNode->right = NULL;\n    return newNode;\n}\n\n// Function to find the path from root to a given node\nvoid findPath(TreeNode* root, int target, int* path, int* len) {\n    if (root == NULL) return;\n    path[*len] = root->val;\n    (*len)++;\n    if (root->val == target) return;\n    findPath(root->left, target, path, len);\n    if (*len == 0) findPath(root->right, target, path, len);\n    if (*len == 0) (*len)--;\n}\n\n// Function to print the path\nvoid printPath(int* path, int len) {\n    for (int i = 0; i < len; i++) {\n        printf(\"%d \", path[i]);\n    }\n    printf(\"\\n\");\n}\n\nint main() {\n    // Create the tree\n    TreeNode* root = createTreeNode(3);\n    root->left = createTreeNode(5);\n    root->right = createTreeNode(1);\n    root->left->left = createTreeNode(6);\n    root->left->right = createTreeNode(2);\n    root->right->left = createTreeNode(0);\n    root->right->right = createTreeNode(8);\n    root->left->right->left = createTreeNode(7);\n    root->left->right->right = createTreeNode(4);\n\n    int target = 7;\n    int path[100];\n    int len = 0;\n    findPath(root, target, path, &len);\n    printPath(path, len);\n    return 0;\n}",
      "C++": "#include <bits/stdc++.h>\nusing namespace std;\n\n// Structure for a binary tree node\nstruct TreeNode {\n    // Value of the node\n    int val;\n\n    // Pointers to left and right children\n    TreeNode* left;\n    TreeNode* right;\n\n    // Constructor to initialize the node\n    TreeNode(int x) : val(x), left(nullptr), right(nullptr) {}\n};\n\n// Class containing the solution\nclass Solution {\npublic:\n    // Function to find the path from root to node with value x\n    bool getPath(TreeNode* root, vector<int>& arr, int x) {\n        // If current node is NULL, return false\n        if (!root) {\n            return false;\n        }\n\n        // Add current node's value to the path\n        arr.push_back(root->val);\n\n        // If current node's value is equal to x, return true\n        if (root->val == x) {\n            return true;\n        }\n\n        // Recursively search in left or right subtree\n        if (getPath(root->left, arr, x) ||\n            getPath(root->right, arr, x)) {\n            return true;\n        }\n\n        // If not found, backtrack and remove current node\n        arr.pop_back();\n        return false;\n    }\n\n    // Function to get the path from root to node with value B\n    vector<int> solve(TreeNode* A, int B) {\n        // Vector to store the path\n        vector<int> arr;\n\n        // If root is NULL, return empty path\n        if (A == NULL) {\n            return arr;\n        }\n\n        // Call helper function to fill the path\n        getPath(A, arr, B);\n\n        // Return the resulting path\n        return arr;\n    }\n};\n\n// Main function to run the code\nint main() {\n    // Creating the binary tree\n    TreeNode* root = new TreeNode(3);\n    root->left = new TreeNode(5);\n    root->right = new TreeNode(1);\n    root->left->left = new TreeNode(6);\n    root->left->right = new TreeNode(2);\n    root->right->left = new TreeNode(0);\n    root->right->right = new TreeNode(8);\n    root->left->right->left = new TreeNode(7);\n    root->left->right->right = new TreeNode(4);\n\n    // Create an instance of the solution class\n    Solution sol;\n\n    // Value of the target node\n    int targetLeafValue = 7;\n\n    // Get the path from root to the target node\n    vector<int> path = sol.solve(root, targetLeafValue);\n\n    // Print the path\n    cout << \"Path from root to node with value \";\n    cout << targetLeafValue << \": \";\n\n    for (int i = 0; i < path.size(); ++i) {\n        cout << path[i];\n        if (i < path.size() - 1) {\n            cout << \" -> \";\n        }\n    }\n\n    return 0;\n}",
      "Java": "import java.util.*;\n\n// TreeNode class for Binary Tree\nclass TreeNode {\n    int val;\n    TreeNode left, right;\n\n    // Constructor to initialize the node\n    TreeNode(int x) {\n        val = x;\n        left = right = null;\n    }\n}\n\nclass Solution {\n    // Function to get path from root to node with value x\n    public boolean getPath(TreeNode root, List<Integer> arr, int x) {\n        // Base case: If node is null\n        if (root == null)\n            return false;\n\n        // Add current node to path\n        arr.add(root.val);\n\n        // If current node is the target\n        if (root.val == x)\n            return true;\n\n        // Recurse into left and right children\n        if (getPath(root.left, arr, x) || getPath(root.right, arr, x))\n            return true;\n\n        // Backtrack if not found\n        arr.remove(arr.size() - 1);\n        return false;\n    }\n\n    // Function to return the final path vector\n    public List<Integer> solve(TreeNode root, int x) {\n        // Initialize result list\n        List<Integer> arr = new ArrayList<>();\n\n        // If tree is empty\n        if (root == null)\n            return arr;\n\n        // Call helper function\n        getPath(root, arr, x);\n        return arr;\n    }\n}\n\npublic class Main {\n    public static void main(String[] args) {\n        // Construct the tree\n        TreeNode root = new TreeNode(3);\n        root.left = new TreeNode(5);\n        root.right = new TreeNode(1);\n        root.left.left = new TreeNode(6);\n        root.left.right = new TreeNode(2);\n        root.right.left = new TreeNode(0);\n        root.right.right = new TreeNode(8);\n        root.left.right.left = new TreeNode(7);\n        root.left.right.right = new TreeNode(4);\n\n        // Create solution object\n        Solution sol = new Solution();\n\n        // Target node value\n        int target = 7;\n\n        // Get path from root to target\n        List<Integer> path = sol.solve(root, target);\n\n        // Print the path\n        System.out.print(\"Path from root to node \"+ target + \": \");\n        for (int i = 0; i < path.size(); i++) {\n            System.out.print(path.get(i));\n            if (i < path.size() - 1)\n                System.out.print(\" -> \");\n        }\n    }\n}",
      "Python3": "# TreeNode class for Binary Tree\nclass TreeNode:\n    def __init__(self, val):\n        # Initialize node with value\n        self.val = val\n        self.left = None\n        self.right = None\n\nclass Solution:\n    # Function to find the path from root to a given node\n    def getPath(self, root, arr, x):\n        # Base case: If root is None\n        if root is None:\n            return False\n\n        # Add current node to path\n        arr.append(root.val)\n\n        # If current node is the target\n        if root.val == x:\n            return True\n\n        # Recurse on left and right\n        if self.getPath(root.left, arr, x) or self.getPath(root.right, arr, x):\n            return True\n\n        # Backtrack if not found\n        arr.pop()\n        return False\n\n    # Function to return the final path list\n    def solve(self, root, x):\n        # Initialize result path\n        arr = []\n\n        # If tree is empty\n        if root is None:\n            return arr\n\n        # Get path using helper\n        self.getPath(root, arr, x)\n        return arr\n\n# Main driver function\ndef main():\n    # Construct the tree\n    root = TreeNode(3)\n    root.left = TreeNode(5)\n    root.right = TreeNode(1)\n    root.left.left = TreeNode(6)\n    root.left.right = TreeNode(2)\n    root.right.left = TreeNode(0)\n    root.right.right = TreeNode(8)\n    root.left.right.left = TreeNode(7)\n    root.left.right.right = TreeNode(4)\n\n    # Create solution object\n    sol = Solution()\n\n    # Target node value\n    target = 7\n\n    # Get path\n    path = sol.solve(root, target)\n\n    # Print result\n    print(f\"Path from root to node {target}:\", end=\" \")\n    for i in range(len(path)):\n        print(path[i], end=\"\")\n        if i < len(path) - 1:\n            print(\" -> \", end=\"\")\n\nmain()",
      "JavaScript": "// TreeNode class for Binary Tree\nclass TreeNode {\n    constructor(val) {\n        // Initialize node with value\n        this.val = val;\n        this.left = null;\n        this.right = null;\n    }\n}\n\nclass Solution {\n    // Function to find path from root to node with value x\n    getPath(root, arr, x) {\n        // Base case: If root is null\n        if (!root) return false;\n\n        // Push current node to path\n        arr.push(root.val);\n\n        // If current node is the target\n        if (root.val === x) return true;\n\n        // Recurse on left and right subtrees\n        if (this.getPath(root.left, arr, x) || this.getPath(root.right, arr, x)) {\n            return true;\n        }\n\n        // Backtrack if not found\n        arr.pop();\n        return false;\n    }\n\n    // Function to return path from root to target node\n    solve(root, x) {\n        // Initialize result array\n        const arr = [];\n\n        // If tree is empty\n        if (!root) return arr;\n\n        // Get path using helper\n        this.getPath(root, arr, x);\n        return arr;\n    }\n}\n\n// Driver code\nfunction main() {\n    // Build the tree\n    const root = new TreeNode(3);\n    root.left = new TreeNode(5);\n    root.right = new TreeNode(1);\n    root.left.left = new TreeNode(6);\n    root.left.right = new TreeNode(2);\n    root.right.left = new TreeNode(0);\n    root.right.right = new TreeNode(8);\n    root.left.right.left = new TreeNode(7);\n    root.left.right.right = new TreeNode(4);\n\n    // Create solution object\n    const sol = new Solution();\n\n    // Target node value\n    const target = 7;\n\n    // Get the path\n    const path = sol.solve(root, target);\n\n    // Print the path\n    let output = `Path from root to node ${target}: `;\n    for (let i = 0; i < path.length; i++) {\n        output += path[i];\n        if (i < path.length - 1) output += \" -> \";\n    }\n    console.log(output);\n}\n\nmain();"
    }
  },
  "QBCategory": "CCD",
  "boilerPlates": {
    "C": "#include <stdio.h>\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "C++": "#include <iostream>\nusing namespace std;\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "Java": "import java.util.*;\nimport java.io.*;\n\npublic class Main {\n    public static void main(String[] args) throws IOException {\n        // your code goes here\n    }\n}",
    "Python3": "# your code goes here\n",
    "JavaScript": "// your code goes here\n"
  },
  "_internal": {
    "methodName": "",
    "params": []
  }
}