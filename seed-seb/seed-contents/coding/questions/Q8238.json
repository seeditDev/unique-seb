{
  "questionId": "Q8238",
  "title": "Three Points",
  "slug": "three-points",
  "version": 1,
  "createdAt": "2026-07-02",
  "updatedAt": "2026-07-04",
  "metadata": {
    "category": "Geometry",
    "difficulty": "Hard",
    "isPremium": true,
    "tags": [
      "Axis-aligned movement",
      "Manhattan path",
      "Greedy",
      "Geometry"
    ],
    "companyTags": [],
    "topicTags": [
      "Geometry"
    ],
    "QBCategory": "CCD"
  },
  "content": {
    "problemStatement": "There are three **distinct** points - A,B,CA, B, CA,B,C in the X−YX-YX−Y plane. Initially, you are located at point AAA. You want to reach the point CCC satisfying the following conditions:\n\n\nYou have to go through point B.\nYou can move in any of the four axis-parallel directions (+X+X+X, −X-X−X, +Y+Y+Y, −Y-Y−Y direction). However, you can make **at most** one turn in the path from AAA to CCC.\nDetermine if it is possible to reach the destination CCC satisfying the above conditions.\n\n**NOTE**:\n\n\nOne turn means 90∘90 ^ \\circ90∘  clockwise or anticlockwise rotation along the direction of movement. For example, if you are moving towards the +X+X+X direction, after one turn you can move either in the +Y+Y+Y direction or in the −Y-Y−Y direction. **Changing the direction by 180∘180 ^ \\circ180∘ is not allowed**.\n\nInput Format\n\nThe first line of input contains a single integer TTT denoting the number of test cases. The description of TTT test cases follows.\nThe first line of each test case contains two space-separated integers XA,YAX_A, Y_AXA​,YA​, denoting the coordinates of the starting point AAA.\nThe second line of each test case contains two space-separated integers XB,YBX_B, Y_BXB​,YB​, denoting the coordinates of the point BBB.\nThe third line of each test case contains two space-separated integers XC,YCX_C, Y_CXC​,YC​, denoting the coordinates of the finishing point CCC.\n\n\nOutput Format\nFor each test case, print a single line containing one string - `\"YES\"`(without quotes) if it is possible to reach the finishing point satisfying the above conditions, otherwise print `\"NO\"`(without quotes).",
    "constraints": [
      "1 <= T <= 200000",
      "-10^9 <= XA, YA, XB, YB, XC, YC <= 10^9",
      "The three points are distinct"
    ],
    "inputFormat": "The first line contains an integer T, the number of test cases. For each test case, three lines follow:\n- Line 1: two integers XA YA – coordinates of point A.\n- Line 2: two integers XB YB – coordinates of point B.\n- Line 3: two integers XC YC – coordinates of point C.\nAll coordinates are between -10^9 and 10^9 and the three points are pairwise distinct.",
    "outputFormat": "For each test case output a single line containing \"YES\" if it is possible to travel from A to C passing through B with at most one 90° turn using only axis‑parallel moves, otherwise output \"NO\".",
    "sampleTestCases": [
      {
        "id": "sample_1",
        "input": "4\n1 1\n1 3\n3 3\n0 0\n2 2\n3 4\n5 2\n3 2\n1 2\n1 1\n-1 1\n10000 10000",
        "output": "YES\nNO\nYES\nNO",
        "explanation": "Test case 1: A(1,1)->B(1,3) vertical, turn, B->C(3,3) horizontal – one turn, YES.\nTest case 2: No way with at most one turn – NO.\nTest case 3: A(5,2)->C(1,2) straight horizontal passing through B(3,2) – no turn, YES.\nTest case 4: Any path needs two turns – NO."
      },
      {
        "id": "sample_2",
        "input": "2\n0 0\n1 1\n2 2\n0 0\n0 5\n3 5",
        "output": "NO\nYES",
        "explanation": "First case: points are diagonal, cannot move without turning more than once – NO.\nSecond case: vertical A->B then horizontal B->C – one turn – YES."
      }
    ]
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "label": "Case 1",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_2",
        "label": "Case 2",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_3",
        "label": "Case 3",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_4",
        "label": "Case 4",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_5",
        "label": "Case 5",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      }
    ],
    "totalTestCases": 5,
    "totalWeight": 10
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      2,
      2,
      2,
      2,
      2
    ],
    "maxScore": 10
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "The whole path consists of at most two straight axis‑parallel segments: A→B and B→C. Therefore:\n1. A and B must share either the same x‑coordinate (vertical segment) or the same y‑coordinate (horizontal segment).\n2. B and C must also share either the same x‑coordinate or the same y‑coordinate.\n3. If both segments are vertical (A.x == B.x == C.x) or both are horizontal (A.y == B.y == C.y) we are moving along a straight line. This is allowed only when B lies between A and C, i.e. the coordinate of B is between the coordinates of A and C.\n4. If one segment is vertical and the other horizontal we make exactly one 90° turn, which is always allowed.\n5. The case where the direction would reverse (180° turn) is excluded by condition 3 because then B would not be between A and C.\nThe algorithm checks these conditions in O(1) time per test case.",
    "timeComplexity": "O(T) – constant work for each test case.",
    "spaceComplexity": "O(1) – only a few variables are used.",
    "code": {
      "C": "#include <stdio.h>\n#include <stdbool.h>\nint main(){\n    int T; if(scanf(\"%d\", &T)!=1) return 0;\n    while(T--){\n        long long xa, ya, xb, yb, xc, yc;\n        scanf(\"%lld %lld\", &xa, &ya);\n        scanf(\"%lld %lld\", &xb, &yb);\n        scanf(\"%lld %lld\", &xc, &yc);\n        bool ok = false;\n        if (xa == xb && xb == xc) {\n            if ((ya <= yb && yb <= yc) || (yc <= yb && yb <= ya)) ok = true;\n        } else if (ya == yb && yb == yc) {\n            if ((xa <= xb && xb <= xc) || (xc <= xb && xb <= xa)) ok = true;\n        } else if ((xa == xb && yb == yc) || (ya == yb && xb == xc)) {\n            ok = true;\n        }\n        printf(\"%s\\n\", ok ? \"YES\" : \"NO\");\n    }\n    return 0;\n}",
      "C++": "#include <bits/stdc++.h>\nusing namespace std;\nint main(){\n    ios::sync_with_stdio(false);\n    cin.tie(nullptr);\n    int T; if(!(cin>>T)) return 0;\n    while(T--){\n        long long xa, ya, xb, yb, xc, yc;\n        cin>>xa>>ya>>xb>>yb>>xc>>yc;\n        bool ok = false;\n        if (xa==xb && xb==xc) {\n            if ((ya<=yb && yb<=yc) || (yc<=yb && yb<=ya)) ok = true;\n        } else if (ya==yb && yb==yc) {\n            if ((xa<=xb && xb<=xc) || (xc<=xb && xb<=xa)) ok = true;\n        } else if ((xa==xb && yb==yc) || (ya==yb && xb==xc)) {\n            ok = true;\n        }\n        cout << (ok?\"YES\":\"NO\") << '\\n';\n    }\n    return 0;\n}",
      "Java": "import java.io.*;\nimport java.util.*;\npublic class Main {\n    public static void main(String[] args) throws Exception {\n        FastScanner fs = new FastScanner(System.in);\n        int T = fs.nextInt();\n        StringBuilder sb = new StringBuilder();\n        while (T-- > 0) {\n            long xa = fs.nextLong();\n            long ya = fs.nextLong();\n            long xb = fs.nextLong();\n            long yb = fs.nextLong();\n            long xc = fs.nextLong();\n            long yc = fs.nextLong();\n            boolean ok = false;\n            if (xa == xb && xb == xc) {\n                if ((ya <= yb && yb <= yc) || (yc <= yb && yb <= ya)) ok = true;\n            } else if (ya == yb && yb == yc) {\n                if ((xa <= xb && xb <= xc) || (xc <= xb && xb <= xa)) ok = true;\n            } else if ((xa == xb && yb == yc) || (ya == yb && xb == xc)) {\n                ok = true;\n            }\n            sb.append(ok ? \"YES\" : \"NO\").append('\\n');\n        }\n        System.out.print(sb.toString());\n    }\n    // Fast scanner for large input\n    static class FastScanner {\n        private final InputStream in;\n        private final byte[] buffer = new byte[1 << 16];\n        private int ptr = 0, len = 0;\n        FastScanner(InputStream in) { this.in = in; }\n        private int readByte() throws IOException {\n            if (ptr >= len) {\n                len = in.read(buffer);\n                ptr = 0;\n                if (len <= 0) return -1;\n            }\n            return buffer[ptr++];\n        }\n        long nextLong() throws IOException {\n            int c; do { c = readByte(); } while (c <= ' ');\n            boolean neg = false;\n            if (c == '-') { neg = true; c = readByte(); }\n            long val = 0;\n            while (c > ' ') {\n                val = val * 10 + (c - '0');\n                c = readByte();\n            }\n            return neg ? -val : val;\n        }\n        int nextInt() throws IOException { return (int) nextLong(); }\n    }\n}",
      "Python3": "#!/usr/bin/env python3\nimport sys\n\ndef solve():\n    data = sys.stdin.read().strip().split()\n    if not data:\n        return\n    it = iter(data)\n    T = int(next(it))\n    out = []\n    for _ in range(T):\n        xa = int(next(it)); ya = int(next(it))\n        xb = int(next(it)); yb = int(next(it))\n        xc = int(next(it)); yc = int(next(it))\n        ok = False\n        if xa == xb == xc:\n            if (ya <= yb <= yc) or (yc <= yb <= ya):\n                ok = True\n        elif ya == yb == yc:\n            if (xa <= xb <= xc) or (xc <= xb <= xa):\n                ok = True\n        elif (xa == xb and yb == yc) or (ya == yb and xb == xc):\n            ok = True\n        out.append('YES' if ok else 'NO')\n    sys.stdout.write('\\n'.join(out))\n\nif __name__ == \"__main__\":\n    solve()\n",
      "JavaScript": "const fs = require('fs');\nconst input = fs.readFileSync(0, 'utf-8').trim().split(/\\s+/);\nlet idx = 0;\nconst T = Number(input[idx++]);\nlet res = [];\nfor (let t = 0; t < T; t++) {\n  const xa = Number(input[idx++]);\n  const ya = Number(input[idx++]);\n  const xb = Number(input[idx++]);\n  const yb = Number(input[idx++]);\n  const xc = Number(input[idx++]);\n  const yc = Number(input[idx++]);\n  let ok = false;\n  if (xa === xb && xb === xc) {\n    if ((ya <= yb && yb <= yc) || (yc <= yb && yb <= ya)) ok = true;\n  } else if (ya === yb && yb === yc) {\n    if ((xa <= xb && xb <= xc) || (xc <= xb && xb <= xa)) ok = true;\n  } else if ((xa === xb && yb === yc) || (ya === yb && xb === xc)) {\n    ok = true;\n  }\n  res.push(ok ? 'YES' : 'NO');\n}\nconsole.log(res.join('\\n'));\n"
    }
  },
  "QBCategory": "CCD",
  "boilerPlates": {
    "C": "#include <stdio.h>\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "C++": "#include <iostream>\nusing namespace std;\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "Java": "import java.util.*;\nimport java.io.*;\n\npublic class Main {\n    public static void main(String[] args) throws IOException {\n        // your code goes here\n    }\n}",
    "Python3": "# your code goes here\n",
    "JavaScript": "// your code goes here\n"
  },
  "_internal": {
    "methodName": "",
    "params": []
  }
}