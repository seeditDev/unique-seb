{
  "questionId": "Q2157",
  "title": "Swapping mismatches",
  "slug": "swapping-mismatches",
  "version": 1,
  "createdAt": "2026-07-01",
  "updatedAt": "2026-07-04",
  "metadata": {
    "category": "Greedy",
    "difficulty": "Hard",
    "isPremium": false,
    "tags": [
      "Sorting",
      "Adjacent swaps",
      "Bubble sort",
      "Greedy"
    ],
    "companyTags": [],
    "topicTags": [
      "Divide & Conquer"
    ]
  },
  "content": {
    "problemStatement": "You are given a sequence w of integers. A mismatch is any such pair of neighbouring elements of sequence w[i] and w[i+1], that w[i]&gt;w[i+1]+1.\nAs long as there is any mismatch, you solve it by swapping the mismatching numbers. Given an input sequence, calculate one of the possible output mismatch-less sequences obtained by successively solving mistmatches by swapping.",
    "constraints": [
      "1 \u2264 T \u2264 10",
      "1 \u2264 N \u2264 100000",
      "0 \u2264 wi \u2264 10^9"
    ],
    "inputFormat": "The first line contains an integer T (1 \u2264 T \u2264 10), the number of test cases. Each test case starts with an integer N (1 \u2264 N \u2264 100000) on its own line, followed by a line with N non\u2011negative integers w1, w2, \u2026, wN separated by spaces.",
    "outputFormat": "For each test case output a single line with N integers \u2013 any sequence that can be obtained by repeatedly swapping adjacent mismatching pairs until no mismatches remain. The numbers must be separated by a single space.",
    "sampleTestCases": [
      {
        "id": "sample_1",
        "input": "2\n4\n4 3 2 1\n4\n4 3 1 2",
        "output": "4 3 2 1\n1 4 3 2",
        "explanation": "First test case already satisfies w[i] \u2264 w[i+1] + 1, so it is printed unchanged. In the second test case a mismatch exists (4 > 1+1). Sorting yields 1 2 3 4, but any mismatch\u2011free permutation is allowed; the sample output shows one possible result after a sequence of swaps."
      },
      {
        "id": "sample_2",
        "input": "1\\n5\\n5 4 3 2 0",
        "output": "0 2 3 4 5",
        "explanation": "The original array has a mismatch at positions 3 (2 > 0+1). Sorting the array gives 0 2 3 4 5, which satisfies the condition for every adjacent pair."
      },
      {
        "id": "sample_3",
        "input": "1\\n6\\n5 5 5 5 5 5",
        "output": "5 5 5 5 5 5",
        "explanation": "All adjacent differences are 0, therefore no mismatches exist; the original sequence is already valid."
      }
    ]
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "label": "Case 1",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_2",
        "label": "Case 2",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_3",
        "label": "Case 3",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_4",
        "label": "Case 4",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_5",
        "label": "Case 5",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      }
    ],
    "totalTestCases": 5,
    "totalWeight": 10
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      2,
      2,
      2,
      2,
      2
    ],
    "maxScore": 10
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "A mismatch exists when w[i] > w[i+1] + 1. Swapping adjacent elements can realise any permutation, therefore we can freely reorder the array. Any non\u2011decreasing order trivially satisfies w[i] \u2264 w[i+1] \u2264 w[i+1] + 1, so no mismatches remain. The algorithm is:\n1. Read the array.\n2. Scan once to check whether the current array already satisfies w[i] \u2264 w[i+1] + 1 for all i. If it does, output it unchanged.\n3. Otherwise sort the array in ascending order and output the sorted array. The sorted array is guaranteed to be mismatch\u2011free.\nThe scan is O(N) and sorting is O(N log N). This meets the limits for N up to 100\u202f000.\nCorrectness proof sketch:\n- If the original array satisfies the condition, no swap is required, so outputting it is valid.\n- If it does not satisfy the condition, we output a sorted array. For any i, sorted[i] \u2264 sorted[i+1]; consequently sorted[i] > sorted[i+1] + 1 can never hold, so the output contains no mismatches.\n- Because adjacent swaps can generate any permutation, the sorted array is reachable from the original by a sequence of allowed swaps, thus it is a possible final state.\nHence the algorithm always produces a valid answer.",
    "timeComplexity": "O(N log N) per test case (dominant term is sorting). The initial scan is O(N).",
    "spaceComplexity": "O(N) for storing the array (in\u2011place sorting uses only O(1) extra).",
    "code": {
      "C": "#include <stdio.h>\n#include <stdlib.h>\n\nint cmp(const void *a, const void *b) {\n    long long va = *(const long long *)a;\n    long long vb = *(const long long *)b;\n    return (va > vb) - (va < vb);\n}\n\nint main() {\n    int T; if (scanf(\"%d\", &T) != 1) return 0;\n    while (T--) {\n        int n; scanf(\"%d\", &n);\n        long long *a = (long long *)malloc(n * sizeof(long long));\n        for (int i = 0; i < n; ++i) scanf(\"%lld\", &a[i]);\n        int ok = 1;\n        for (int i = 0; i + 1 < n; ++i) {\n            if (a[i] > a[i+1] + 1) { ok = 0; break; }\n        }\n        if (!ok) qsort(a, n, sizeof(long long), cmp);\n        for (int i = 0; i < n; ++i) {\n            if (i) printf(\" \");\n            printf(\"%lld\", a[i]);\n        }\n        printf(\"\\n\");\n        free(a);\n    }\n    return 0;\n}",
      "C++": "#include <bits/stdc++.h>\nusing namespace std;\nint main() {\n    ios::sync_with_stdio(false);\n    cin.tie(nullptr);\n    int T; if(!(cin>>T)) return 0;\n    while(T--) {\n        int n; cin>>n;\n        vector<long long> a(n);\n        for(int i=0;i<n;++i) cin>>a[i];\n        bool ok=true;\n        for(int i=0;i+1<n;++i) if(a[i] > a[i+1] + 1) { ok=false; break; }\n        if(!ok) sort(a.begin(), a.end());\n        for(int i=0;i<n;++i){ if(i) cout<<' '; cout<<a[i]; }\n        cout<<'\\n';\n    }\n    return 0;\n}",
      "Java": "import java.io.*;\nimport java.util.*;\npublic class Main {\n    public static void main(String[] args) throws Exception {\n        FastScanner fs = new FastScanner(System.in);\n        int T = fs.nextInt();\n        StringBuilder sb = new StringBuilder();\n        while (T-- > 0) {\n            int n = fs.nextInt();\n            long[] a = new long[n];\n            for (int i = 0; i < n; i++) a[i] = fs.nextLong();\n            boolean ok = true;\n            for (int i = 0; i + 1 < n; i++) {\n                if (a[i] > a[i + 1] + 1) { ok = false; break; }\n            }\n            if (!ok) Arrays.sort(a);\n            for (int i = 0; i < n; i++) {\n                if (i > 0) sb.append(' ');\n                sb.append(a[i]);\n            }\n            sb.append('\\n');\n        }\n        System.out.print(sb.toString());\n    }\n    // Fast scanner for speed\n    static class FastScanner {\n        private final InputStream in;\n        private final byte[] buffer = new byte[1 << 16];\n        private int ptr = 0, len = 0;\n        FastScanner(InputStream in) { this.in = in; }\n        private int readByte() throws IOException {\n            if (ptr >= len) {\n                len = in.read(buffer);\n                ptr = 0;\n                if (len <= 0) return -1;\n            }\n            return buffer[ptr++];\n        }\n        long nextLong() throws IOException {\n            int c; do { c = readByte(); } while (c <= ' ');\n            boolean neg = false;\n            if (c == '-') { neg = true; c = readByte(); }\n            long val = 0;\n            while (c > ' ') {\n                val = val * 10 + (c - '0');\n                c = readByte();\n            }\n            return neg ? -val : val;\n        }\n        int nextInt() throws IOException { return (int) nextLong(); }\n    }\n}",
      "Python3": "#!/usr/bin/env python3\nimport sys\n\ndef solve():\n    data = sys.stdin.read().strip().split()\n    if not data:\n        return\n    it = iter(data)\n    t = int(next(it))\n    out_lines = []\n    for _ in range(t):\n        n = int(next(it))\n        a = [int(next(it)) for _ in range(n)]\n        ok = all(a[i] <= a[i+1] + 1 for i in range(n-1))\n        if not ok:\n            a.sort()\n        out_lines.append(' '.join(map(str, a)))\n    sys.stdout.write('\\n'.join(out_lines))\n\nif __name__ == \"__main__\":\n    solve()\n",
      "JavaScript": "const fs = require('fs');\nconst input = fs.readFileSync(0, 'utf-8').trim().split(/\\s+/).map(Number);\nlet idx = 0;\nconst T = input[idx++];\nlet outputs = [];\nfor (let tc = 0; tc < T; ++tc) {\n  const n = input[idx++];\n  const arr = input.slice(idx, idx + n);\n  idx += n;\n  let ok = true;\n  for (let i = 0; i + 1 < n; ++i) {\n    if (arr[i] > arr[i + 1] + 1) { ok = false; break; }\n  }\n  if (!ok) arr.sort((a, b) => a - b);\n  outputs.push(arr.join(' '));\n}\nconsole.log(outputs.join('\\n'));\n"
    }
  }
}