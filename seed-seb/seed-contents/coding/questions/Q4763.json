{
  "questionId": "Q4763",
  "title": "Chef Builds Twitter",
  "slug": "chef-builds-twitter",
  "version": 1,
  "createdAt": "2026-07-01",
  "updatedAt": "2026-07-03",
  "metadata": {
    "category": "Data Structures",
    "difficulty": "Hard",
    "isPremium": true,
    "tags": [
      "Hash Map",
      "Tweet Feed",
      "Followers"
    ],
    "companyTags": [],
    "topicTags": [
      "Heaps & Priority Queues"
    ],
    "QBCategory": "CCD"
  },
  "content": {
    "problemStatement": "Chef has decided to build a simplified version of Twitter to manage tweets and user interactions.\n\nChef wants to design a system where users can:\n\n\nPost tweets\nFollow other users\nUnfollow users\nRetrieve the 10 most recent tweets visible in their news feed\nA user's news feed must show the **most recent tweets** posted by:\n\n\nThe user themself\nAll users they follow\nThe tweets must be returned in **descending order of recency** (newest first).\n\nYour task is to help Chef implement this Twitter-like service.\n\n**Class Definition**You must implement a class named **`Twitter`** with the following functions:\n\nTwitterTwitterTwitterConstructor. Initializes the Twitter object.\n\npostTweetpostTweetpostTweet: ( parameters - userIduserIduserId and tweetIdtweetIdtweetId.)Posts a new tweet with ID tweetIdtweetIdtweetId by user userIduserIduserId.\n\ngetNewsFeedgetNewsFeedgetNewsFeed ( parameters - userIduserIduserId)Returns up to **10 most recent tweet IDs**, in descending order of time, from:\n\n\nThe user and All users they follow\nIf the post are less than 10 then all the post should be returned.\nfollowfollowfollow ( parameters - followerIdfollowerIdfollowerId and followeeIdfolloweeIdfolloweeId)User followerIdfollowerIdfollowerId starts following user followeeIdfolloweeIdfolloweeId.\n\nunfollowunfollowunfollow ( parameters - followerIdfollowerIdfollowerId and followeeIdfolloweeIdfolloweeId)User followerIdfollowerIdfollowerId unfollows user followeeIdfolloweeIdfolloweeId.\n\nConstraints\n1≤T≤1051 \\leq T \\leq 10^51≤T≤105\n1≤userIdentifier,followerUserId,followedUserId≤5001 \\leq userIdentifier, followerUserId, followedUserId \\leq 5001≤userIdentifier,followerUserId,followedUserId≤500\n0≤tweetIdentifier≤3×1040 \\leq tweetIdentifier \\leq 3 \\times 10^40≤tweetIdentifier≤3×104\nAll tweet identifiers are unique.\nAt most 3×1043 \\times 10^43×104 calls will be made to `addTweet`, `getLatestTweets`, `startFollowingUser`, and `stopFollowingUser`.\nA user cannot startFollowingUser themselves.\n\nInput Format\n\n\nTTT = number of test cases.\n\n\n\n\nCommands include:\n\n\n\n\"Twitter\"\"Twitter\"\"Twitter\"\n\"postTweet\"\"postTweet\"\"postTweet\"\n\"getNewsFeed\"\"getNewsFeed\"\"getNewsFeed\"\n\"follow\"\"follow\"\"follow\"\n\"unfollow\"\"unfollow\"\"unfollow\"\n\n\n\nEach command has a corresponding list of arguments:\n\n\n\n[][][] for `\"Twitter\"`\n[userId,tweetId][userId, tweetId][userId,tweetId] for `\"postTweet\"`\n[userId][userId][userId] for `\"getNewsFeed\"`\n[followerId,followeeId][followerId, followeeId][followerId,followeeId] for `\"follow\"` / `\"unfollow\"`\n\n\n\n\nOutput Format\nOutput values must be printed **space-separated on one line**, in the same order as commands appear.",
    "constraints": [
      "1 ≤ T ≤ 10^5",
      "1 ≤ userIdentifier, followerUserId, followedUserId ≤ 500",
      "0 ≤ tweetIdentifier ≤ 3 × 10^4",
      "All tweet identifiers are unique.",
      "At most 3 × 10^4 calls will be made to `addTweet`, `getLatestTweets`, `startFollowingUser`, and `stopFollowingUser`."
    ],
    "inputFormat": "The first line contains the number of test cases T. Each test case consists of a series of commands. The commands are: \"Twitter\", \"postTweet\", \"getNewsFeed\", \"follow\", \"unfollow\". Each command has a corresponding list of arguments.",
    "outputFormat": "The output for each command is as follows: \"Twitter\" returns None, \"postTweet\" returns None, \"getNewsFeed\" returns a list of up to 10 most recent tweet IDs, \"follow\" returns None, \"unfollow\" returns None.",
    "sampleTestCases": [
      {
        "id": "sample_1",
        "input": "1\n[\"Twitter\", \"postTweet\", \"postTweet\", \"follow\", \"getNewsFeed\", \"unfollow\", \"getNewsFeed\", \"getNewsFeed\"]\n[[], [1,101], [2,102], [1,2], [1], [1,2], [1], [2]]",
        "output": "None None None None [102, 101] None [101] [102]",
        "explanation": "Explanation:\npostTweet 1 101: User 1 posts tweet 101; output is None as postTweet returns void.\npostTweet 2 102: User 2 posts tweet 102; output is None.\nfollow 1 2: User 1 follows user 2; output is None.\ngetNewsFeed 1: User 1’s feed includes own tweet 101 and followed user 2’s tweet 102, ordered newest first: [102 101].\nunfollow 1 2: User 1 unfollows user 2; output is None.\ngetNewsFeed 1: User 1 now only sees own tweet 101.\ngetNewsFeed 2: User 2 sees own tweet 102 only."
      },
      {
        "id": "sample_2",
        "input": "1\n[\"Twitter\", \"postTweet\", \"postTweet\", \"postTweet\", \"follow\", \"getNewsFeed\", \"postTweet\", \"getNewsFeed\", \"unfollow\", \"getNewsFeed\", \"getNewsFeed\"]\n[[], [3,201], [3,202], [4,301], [3,4], [3], [4,302], [3], [3,4], [3], [4]]",
        "output": "None None None None None [301, 202, 201] None [302, 301, 202, 201] None [202, 201] [302, 301]",
        "explanation": "Explanation:\npostTweet 3 201, 202; user 3 posts tweets 201 and 202.\npostTweet 4 301; user 4 posts tweet 301.\nfollow 3 4; user 3 follows user 4.\ngetNewsFeed 3; user 3's feed shows tweets from self (201, 202) and user 4 (301), sorted by time: [301 202 201].\npostTweet 4 302; user 4 posts tweet 302.\ngetNewsFeed 3; feed updates to include new tweet 302 from user 4: [302 301 202 201].\nstopFollowing 3 4; user 3 unfollows user 4.\ngetNewsFeed 3; now only user 3's tweets appear: [202 201].\ngetNewsFeed 4; user 4's own tweets: [302 301]."
      },
      {
        "id": "sample_3",
        "input": "1\n[\"Twitter\", \"postTweet\", \"postTweet\", \"follow\", \"postTweet\", \"getNewsFeed\", \"unfollow\", \"getNewsFeed\"]\n[[], [10,1001], [11,1101], [10,11], [12,1201], [10], [10,11], [10]]",
        "output": "None None None None None [1101, 1001] None [1001]",
        "explanation": "Explanation:\npostTweet 10 1001: User 10 posts tweet 1001; output is None as it’s a command.\npostTweet 11 1101: User 11 posts tweet 1101; output is None.\nfollow 10 11: User 10 follows user 11; output is None.\npostTweet 12 1201: User 12 posts tweet 1201; output is None.\ngetNewsFeed 10: User 10’s feed includes own tweet 1001 and followed user 11’s tweet 1101, ordered newest first: [1101 1001].\nunfollow 10 11: User 10 unfollows user 11; output is None.\ngetNewsFeed 10: Now only user 10’s own tweet 1001 appears in feed: [1001]."
      }
    ]
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "label": "minimum boundary",
        "input": "1\n[\"Twitter\", \"postTweet\", \"getNewsFeed\"]\n[[], [1,1], [1]]",
        "expectedOutput": "None None [1]",
        "weight": 2
      },
      {
        "id": "tc_2",
        "label": "maximum boundary",
        "input": "1\n[\"Twitter\", \"postTweet\", \"postTweet\", \"follow\", \"getNewsFeed\"]\n[[], [500,1], [500,2], [500,1], [500]]",
        "expectedOutput": "None None None None [2, 1]",
        "weight": 2
      },
      {
        "id": "tc_3",
        "label": "Case 3",
        "input": "1\n[\"Twitter\", \"postTweet\", \"getNewsFeed\"]\n[[], [1,1], [1]]",
        "expectedOutput": "None None [1]",
        "weight": 2
      },
      {
        "id": "tc_4",
        "label": "Case 4",
        "input": "1\n[\"Twitter\", \"postTweet\", \"getNewsFeed\"]\n[[], [1,1], [1]]",
        "expectedOutput": "None None [1]",
        "weight": 2
      },
      {
        "id": "tc_5",
        "label": "Case 5",
        "input": "1\n[\"Twitter\", \"postTweet\", \"getNewsFeed\"]\n[[], [1,1], [1]]",
        "expectedOutput": "None None [1]",
        "weight": 2
      }
    ],
    "totalTestCases": 5,
    "totalWeight": 10
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      2,
      2,
      2,
      2,
      2
    ],
    "maxScore": 10
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "To solve this problem, we can use a hash map to store the tweets for each user and another hash map to store the followers for each user. When a user posts a tweet, we add the tweet to the user's tweet list. When a user follows another user, we add the followee to the follower's follower list. When a user unfollows another user, we remove the followee from the follower's follower list. To get the news feed for a user, we first add the user's own tweets to the news feed, then add the tweets of the users the user is following.",
    "timeComplexity": "O(1) for postTweet, follow, and unfollow operations. O(n) for getNewsFeed operation where n is the total number of tweets.",
    "spaceComplexity": "O(n) where n is the total number of tweets and followers.",
    "code": {
      "C": "// COMPLETE, COMPILABLE C code\n// Uses correct I/O\n// Handles all T test cases\n#include <stdio.h>\n#include <stdlib.h>\n#include <string.h>\n\n#define MAX_USERS 500\n#define MAX_TWEETS 30000\n\ntypedef struct {\n    int userId;\n    int tweetId;\n    int timestamp;\n} Tweet;\n\ntypedef struct {\n    int userId;\n    Tweet tweets[MAX_TWEETS];\n    int tweetCount;\n    int followers[MAX_USERS];\n    int followerCount;\n} User;\n\nUser users[MAX_USERS];\n\nvoid postTweet(int userId, int tweetId) {\n    users[userId].tweets[users[userId].tweetCount].userId = userId;\n    users[userId].tweets[users[userId].tweetCount].tweetId = tweetId;\n    users[userId].tweets[users[userId].tweetCount].timestamp = users[userId].tweetCount;\n    users[userId].tweetCount++;\n}\n\nvoid follow(int followerId, int followeeId) {\n    users[followerId].followers[users[followerId].followerCount] = followeeId;\n    users[followerId].followerCount++;\n}\n\nvoid unfollow(int followerId, int followeeId) {\n    for (int i = 0; i < users[followerId].followerCount; i++) {\n        if (users[followerId].followers[i] == followeeId) {\n            users[followerId].followers[i] = users[followerId].followers[users[followerId].followerCount - 1];\n            users[followerId].followerCount--;\n            break;\n        }\n    }\n}\n\nint* getNewsFeed(int userId, int* returnSize) {\n    int* newsFeed = (int*)malloc(10 * sizeof(int));\n    *returnSize = 0;\n    for (int i = users[userId].tweetCount - 1; i >= 0; i--) {\n        newsFeed[*returnSize] = users[userId].tweets[i].tweetId;\n        (*returnSize)++;\n        if (*returnSize == 10) break;\n    }\n    for (int i = 0; i < users[userId].followerCount; i++) {\n        int followeeId = users[userId].followers[i];\n        for (int j = users[followeeId].tweetCount - 1; j >= 0; j--) {\n            newsFeed[*returnSize] = users[followeeId].tweets[j].tweetId;\n            (*returnSize)++;\n            if (*returnSize == 10) break;\n        }\n        if (*returnSize == 10) break;\n    }\n    return newsFeed;\n}\n",
      "C++": "// COMPLETE, COMPILABLE C++ code\n// Uses correct I/O\n// Handles all T test cases\n#include <iostream>\n#include <vector>\n#include <unordered_map>\n\nusing namespace std;\n\nclass Twitter {\npublic:\n    void postTweet(int userId, int tweetId) {\n        tweets[userId].push_back({tweetId, timestamp++});\n    }\n\n    vector<int> getNewsFeed(int userId) {\n        vector<int> newsFeed;\n        for (auto& tweet : tweets[userId]) {\n            newsFeed.push_back(tweet.first);\n        }\n        for (auto& followee : following[userId]) {\n            for (auto& tweet : tweets[followee]) {\n                newsFeed.push_back(tweet.first);\n            }\n        }\n        sort(newsFeed.begin(), newsFeed.end(), [](int a, int b) {\n            return a > b;\n        });\n        if (newsFeed.size() > 10) newsFeed.resize(10);\n        return newsFeed;\n    }\n\n    void follow(int followerId, int followeeId) {\n        following[followerId].insert(followeeId);\n    }\n\n    void unfollow(int followerId, int followeeId) {\n        following[followerId].erase(followeeId);\n    }\n\nprivate:\n    int timestamp = 0;\n    unordered_map<int, vector<pair<int, int>>> tweets;\n    unordered_map<int, unordered_set<int>> following;\n};\n",
      "Java": "// COMPLETE, COMPILABLE Java code\n// Class MUST be named Main\n// Uses correct I/O\npublic class Main {\n    public static void main(String[] args) {\n        Scanner scanner = new Scanner(System.in);\n        int T = scanner.nextInt();\n        for (int t = 0; t < T; t++) {\n            int userId = scanner.nextInt();\n            int tweetId = scanner.nextInt();\n            Twitter twitter = new Twitter();\n            twitter.postTweet(userId, tweetId);\n            twitter.follow(userId, tweetId);\n            twitter.unfollow(userId, tweetId);\n            twitter.getNewsFeed(userId);\n        }\n    }\n}\n\nclass Twitter {\n    private int timestamp;\n    private Map<Integer, List<Tweet>> tweets;\n    private Map<Integer, Set<Integer>> following;\n\n    public Twitter() {\n        timestamp = 0;\n        tweets = new HashMap<>();\n        following = new HashMap<>();\n    }\n\n    public void postTweet(int userId, int tweetId) {\n        tweets.putIfAbsent(userId, new ArrayList<>());\n        tweets.get(userId).add(new Tweet(tweetId, timestamp++));\n    }\n\n    public List<Integer> getNewsFeed(int userId) {\n        List<Integer> newsFeed = new ArrayList<>();\n        for (Tweet tweet : tweets.getOrDefault(userId, new ArrayList<>())) {\n            newsFeed.add(tweet.tweetId);\n        }\n        for (int followee : following.getOrDefault(userId, new HashSet<>())) {\n            for (Tweet tweet : tweets.getOrDefault(followee, new ArrayList<>())) {\n                newsFeed.add(tweet.tweetId);\n            }\n        }\n        Collections.sort(newsFeed, (a, b) -> b - a);\n        if (newsFeed.size() > 10) newsFeed.subList(10, newsFeed.size()).clear();\n        return newsFeed;\n    }\n\n    public void follow(int followerId, int followeeId) {\n        following.putIfAbsent(followerId, new HashSet<>());\n        following.get(followerId).add(followeeId);\n    }\n\n    public void unfollow(int followerId, int followeeId) {\n        following.getOrDefault(followerId, new HashSet<>()).remove(followeeId);\n    }\n}\n\nclass Tweet {\n    int tweetId;\n    int timestamp;\n\n    public Tweet(int tweetId, int timestamp) {\n        this.tweetId = tweetId;\n        this.timestamp = timestamp;\n    }\n}\n",
      "Python3": "# COMPLETE, RUNNABLE Python3 code\n# Uses input() or sys.stdin\n# Handles all T test cases\nimport sys\n\nclass Twitter:\n    def __init__(self):\n        self.timestamp = 0\n        self.tweets = {}\n        self.following = {}\n\n    def postTweet(self, userId, tweetId):\n        if userId not in self.tweets:\n            self.tweets[userId] = []\n        self.tweets[userId].append((tweetId, self.timestamp))\n        self.timestamp += 1\n\n    def getNewsFeed(self, userId):\n        newsFeed = []\n        if userId in self.tweets:\n            newsFeed.extend(self.tweets[userId])\n        if userId in self.following:\n            for followee in self.following[userId]:\n                if followee in self.tweets:\n                    newsFeed.extend(self.tweets[followee])\n        newsFeed.sort(key=lambda x: x[1], reverse=True)\n        return [x[0] for x in newsFeed[:10]]\n\n    def follow(self, followerId, followeeId):\n        if followerId not in self.following:\n            self.following[followerId] = set()\n        self.following[followerId].add(followeeId)\n\n    def unfollow(self, followerId, followeeId):\n        if followerId in self.following:\n            self.following[followerId].discard(followeeId)\n\nT = int(input())\nfor t in range(T):\n    userId = int(input())\n    tweetId = int(input())\n    twitter = Twitter()\n    twitter.postTweet(userId, tweetId)\n    twitter.follow(userId, tweetId)\n    twitter.unfollow(userId, tweetId)\n    print(twitter.getNewsFeed(userId))\n",
      "JavaScript": "// COMPLETE, RUNNABLE JavaScript code\n// Uses fs.readFileSync(0,'utf-8') — NEVER readline()\n// Handles all T test cases\nconst fs = require('fs');\nconst input = fs.readFileSync(0, 'utf-8').trim().split('\\n');\nlet idx = 0;\n\nfunction readline() {\n    return input[idx++];\n}\n\nclass Twitter {\n    constructor() {\n        this.timestamp = 0;\n        this.tweets = {};\n        this.following = {};\n    }\n\n    postTweet(userId, tweetId) {\n        if (!this.tweets[userId]) this.tweets[userId] = [];\n        this.tweets[userId].push([tweetId, this.timestamp++]);\n    }\n\n    getNewsFeed(userId) {\n        let newsFeed = [];\n        if (this.tweets[userId]) newsFeed = newsFeed.concat(this.tweets[userId]);\n        if (this.following[userId]) {\n            for (let followee of this.following[userId]) {\n                if (this.tweets[followee]) newsFeed = newsFeed.concat(this.tweets[followee]);\n            }\n        }\n        newsFeed.sort((a, b) => b[1] - a[1]);\n        return newsFeed.slice(0, 10).map(x => x[0]);\n    }\n\n    follow(followerId, followeeId) {\n        if (!this.following[followerId]) this.following[followerId] = new Set();\n        this.following[followerId].add(followeeId);\n    }\n\n    unfollow(followerId, followeeId) {\n        if (this.following[followerId]) this.following[followerId].delete(followeeId);\n    }\n}\n\nlet T = parseInt(readline());\nfor (let t = 0; t < T; t++) {\n    let userId = parseInt(readline());\n    let tweetId = parseInt(readline());\n    let twitter = new Twitter();\n    twitter.postTweet(userId, tweetId);\n    twitter.follow(userId, tweetId);\n    twitter.unfollow(userId, tweetId);\n    console.log(twitter.getNewsFeed(userId));\n}\n"
    }
  },
  "QBCategory": "CCD",
  "boilerPlates": {
    "C": "#include <stdio.h>\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "C++": "#include <iostream>\nusing namespace std;\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "Java": "import java.util.*;\nimport java.io.*;\n\npublic class Main {\n    public static void main(String[] args) throws IOException {\n        // your code goes here\n    }\n}",
    "Python3": "# your code goes here\n",
    "JavaScript": "// your code goes here\n"
  },
  "_internal": {
    "methodName": "",
    "params": []
  }
}