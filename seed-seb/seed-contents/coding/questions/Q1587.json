{
  "questionId": "Q1587",
  "title": "Floor and Ceil in a Binary Search Tree",
  "slug": "floor-and-ceil-in-a-binary-search-tree",
  "version": 1,
  "createdAt": "2026-07-01",
  "updatedAt": "2026-07-04",
  "metadata": {
    "category": "Trees",
    "difficulty": "Medium",
    "isPremium": false,
    "tags": [
      "Trees",
      "Floor and Ceil Values",
      "Binary Search Tree"
    ],
    "companyTags": [],
    "topicTags": [
      "Trees"
    ]
  },
  "content": {
    "problemStatement": "You are given the root of a **Binary Search Tree (BST)** and an integer value **key**.\nYour task is to find:\n\n\n**Floor value:** The node value in the BST that is the **largest value less than or equal to** the given key.\n**Ceil value:** The node value in the BST that is the **smallest value greater than or equal to** the given key.\nIf either the floor or the ceil value does not exist in the tree, return **-1** for that value.\n\nFunction Declaration\n\n**Function Name**\n\n\n\nfloorAndCeilfloorAndCeilfloorAndCeil\n\n\n\n**Parameters**\n\n\n\nrootrootroot: The root node of the Binary Search Tree (BST).\nkeykeykey: An integer value for which the floor and ceil values are to be found.\n\n\n\n**Return Value**\n\n\n\nReturns a pair (or tuple) of integers:\n\nThe first value represents the **floor** of the key (largest value \u2264 key, or `-1` if it does not exist).\nThe second value represents the **ceil** of the key (smallest value \u2265 key, or `-1` if it does not exist).",
    "constraints": [
      "1 <= T <= 1000",
      "1 <= N <= 5000",
      "1 <= Node.val <= 10^7",
      "1 <= key <= 10^7"
    ],
    "inputFormat": "The first line contains a single integer T, denoting the number of test cases. Each test case consists of two lines of input: The first line contains two space-separated integers: N \u2014 the number of nodes in the BST, K \u2014 the key value for which the floor and ceil must be found. The second line contains N space-separated integers representing the level-order traversal of the BST. Use -1 to denote missing children.",
    "outputFormat": "For each test case, output on a new line two space-separated integers: The floor value of the key in the BST (or -1 if it does not exist), The ceil value of the key in the BST (or -1 if it does not exist)",
    "sampleTestCases": [
      {
        "id": "sample_1",
        "input": "3\n7 13\n10 5 15 2 7 12 20\n7 1\n10 5 15 2 7 12 20\n7 25\n10 5 15 2 7 12 20",
        "output": "12 15\n-1 2\n20 -1",
        "explanation": "Explanation:\nFor the first test case:\n\nThe largest value <= 13 is 12 -> floor.\nThe smallest value >= 13 is 15 -> ceil.\n\nFor the second test case:\n\nNo node has value <= 1, so floor is -1.\nThe smallest node >= 1 is 2, hence ceil is 2."
      },
      {
        "id": "sample_2",
        "input": "1\n5 10\n10 5 15 2 7\n",
        "output": "7 10",
        "explanation": "Explanation:\nFor the test case:\n\nThe largest value <= 10 is 7 -> floor.\nThe smallest value >= 10 is 10 -> ceil."
      },
      {
        "id": "sample_3",
        "input": "1\n3 5\n5 3 7\n",
        "output": "3 5",
        "explanation": "Explanation:\nFor the test case:\n\nThe largest value <= 5 is 3 -> floor.\nThe smallest value >= 5 is 5 -> ceil."
      }
    ]
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "label": "minimum boundary",
        "input": "1\n1 1\n1\n",
        "expectedOutput": "1 1",
        "weight": 2
      },
      {
        "id": "tc_2",
        "label": "maximum boundary",
        "input": "1\n10 10000000\n10000000 9999999 10000001 9999998 10000002 9999997 10000003 9999996 9999995\n",
        "expectedOutput": "9999999 10000000",
        "weight": 2
      },
      {
        "id": "tc_3",
        "label": "uniform/edge case",
        "input": "1\n5 5\n5 5 5 5 5\n",
        "expectedOutput": "5 5",
        "weight": 2
      },
      {
        "id": "tc_4",
        "label": "tricky case",
        "input": "1\n5 0\n5 5 5 5 5\n",
        "expectedOutput": "-1 5",
        "weight": 2
      },
      {
        "id": "tc_5",
        "label": "stress/general",
        "input": "5\n10 10\n10 9 11 8 12 7 13 6 14 5\n10 20\n10 9 11 8 12 7 13 6 14 5\n10 0\n10 9 11 8 12 7 13 6 14 5\n10 30\n10 9 11 8 12 7 13 6 14 5\n10 25\n10 9 11 8 12 7 13 6 14 5\n",
        "expectedOutput": "9 10\n-1 10\n-1 10\n9 11\n-1 10",
        "weight": 2
      }
    ],
    "totalTestCases": 5,
    "totalWeight": 10
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      2,
      2,
      2,
      2,
      2
    ],
    "maxScore": 10
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "To find the floor and ceil values in a Binary Search Tree (BST), we can utilize the properties of a BST. We start at the root and compare the node's value with the given key. If the node's value is equal to the key, both the floor and ceil values are the node's value. If the node's value is less than the key, we update the floor value and move to the right subtree. If the node's value is greater than the key, we update the ceil value and move to the left subtree. We continue this process until we reach a leaf node or find the exact match.",
    "timeComplexity": "O(N) per test case in the worst case when the tree is skewed, O(log N) on average for a balanced BST, O(T * N) total",
    "spaceComplexity": "O(N) for the recursive call stack in the worst case, O(log N) on average for a balanced BST",
    "code": {
      "C": "#include <stdio.h>\n#include <stdlib.h>\n// Define the structure for a BST node\ntypedef struct Node {\n    int val;\n    struct Node* left;\n    struct Node* right;\n} Node;\n// Function to create a new BST node\nNode* createNode(int val) {\n    Node* newNode = (Node*)malloc(sizeof(Node));\n    if (!newNode) {\n        printf(\"Memory error\n\");\n        return NULL;\n    }\n    newNode->val = val;\n    newNode->left = newNode->right = NULL;\n    return newNode;\n}\n// Function to insert a node into the BST\nNode* insertNode(Node* root, int val) {\n    if (root == NULL) {\n        root = createNode(val);\n    } else if (val < root->val) {\n        root->left = insertNode(root->left, val);\n    } else if (val > root->val) {\n        root->right = insertNode(root->right, val);\n    }\n    return root;\n}\n// Function to find the floor and ceil values in the BST\nvoid findFloorAndCeil(Node* root, int key, int* floor, int* ceil) {\n    *floor = -1;\n    *ceil = -1;\n    while (root) {\n        if (root->val == key) {\n            *floor = root->val;\n            *ceil = root->val;\n            break;\n        } else if (root->val < key) {\n            *floor = root->val;\n            root = root->right;\n        } else {\n            *ceil = root->val;\n            root = root->left;\n        }\n    }\n}\nint main() {\n    int T;\n    scanf(\"%d\", &T);\n    while (T--) {\n        int N, K;\n        scanf(\"%d %d\", &N, &K);\n        Node* root = NULL;\n        for (int i = 0; i < N; i++) {\n            int val;\n            scanf(\"%d\", &val);\n            if (val != -1) {\n                root = insertNode(root, val);\n            }\n        }\n        int floor, ceil;\n        findFloorAndCeil(root, K, &floor, &ceil);\n        printf(\"%d %d\\n\", floor, ceil);\n    }\n    return 0;\n}",
      "C++": "#include <iostream>\nusing namespace std;\n// Define the structure for a BST node\nstruct Node {\n    int val;\n    Node* left;\n    Node* right;\n};\n// Function to create a new BST node\nNode* createNode(int val) {\n    Node* newNode = new Node;\n    newNode->val = val;\n    newNode->left = newNode->right = NULL;\n    return newNode;\n}\n// Function to insert a node into the BST\nNode* insertNode(Node* root, int val) {\n    if (root == NULL) {\n        root = createNode(val);\n    } else if (val < root->val) {\n        root->left = insertNode(root->left, val);\n    } else if (val > root->val) {\n        root->right = insertNode(root->right, val);\n    }\n    return root;\n}\n// Function to find the floor and ceil values in the BST\nvoid findFloorAndCeil(Node* root, int key, int& floor, int& ceil) {\n    floor = -1;\n    ceil = -1;\n    while (root) {\n        if (root->val == key) {\n            floor = root->val;\n            ceil = root->val;\n            break;\n        } else if (root->val < key) {\n            floor = root->val;\n            root = root->right;\n        } else {\n            ceil = root->val;\n            root = root->left;\n        }\n    }\n}\nint main() {\n    int T;\n    cin >> T;\n    while (T--) {\n        int N, K;\n        cin >> N >> K;\n        Node* root = NULL;\n        for (int i = 0; i < N; i++) {\n            int val;\n            cin >> val;\n            if (val != -1) {\n                root = insertNode(root, val);\n            }\n        }\n        int floor, ceil;\n        findFloorAndCeil(root, K, floor, ceil);\n        cout << floor << \" \" << ceil << endl;\n    }\n    return 0;\n}",
      "Java": "import java.util.Scanner;\npublic class Main {\n    // Define the structure for a BST node\n    static class Node {\n        int val;\n        Node left;\n        Node right;\n        Node(int val) {\n            this.val = val;\n            this.left = null;\n            this.right = null;\n        }\n    }\n    // Function to insert a node into the BST\n    static Node insertNode(Node root, int val) {\n        if (root == null) {\n            root = new Node(val);\n        } else if (val < root.val) {\n            root.left = insertNode(root.left, val);\n        } else if (val > root.val) {\n            root.right = insertNode(root.right, val);\n        }\n        return root;\n    }\n    // Function to find the floor and ceil values in the BST\n    static void findFloorAndCeil(Node root, int key, int[] floor, int[] ceil) {\n        floor[0] = -1;\n        ceil[0] = -1;\n        while (root != null) {\n            if (root.val == key) {\n                floor[0] = root.val;\n                ceil[0] = root.val;\n                break;\n            } else if (root.val < key) {\n                floor[0] = root.val;\n                root = root.right;\n            } else {\n                ceil[0] = root.val;\n                root = root.left;\n            }\n        }\n    }\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int T = sc.nextInt();\n        while (T-- > 0) {\n            int N = sc.nextInt();\n            int K = sc.nextInt();\n            Node root = null;\n            for (int i = 0; i < N; i++) {\n                int val = sc.nextInt();\n                if (val != -1) {\n                    root = insertNode(root, val);\n                }\n            }\n            int[] floor = new int[1];\n            int[] ceil = new int[1];\n            findFloorAndCeil(root, K, floor, ceil);\n            System.out.println(floor[0] + \" \" + ceil[0]);\n        }\n    }\n}",
      "Python3": "import sys\n# Define the structure for a BST node\nclass Node:\n    def __init__(self, val):\n        self.val = val\n        self.left = None\n        self.right = None\n# Function to insert a node into the BST\ndef insert_node(root, val):\n    if root is None:\n        root = Node(val)\n    elif val < root.val:\n        root.left = insert_node(root.left, val)\n    elif val > root.val:\n        root.right = insert_node(root.right, val)\n    return root\n# Function to find the floor and ceil values in the BST\ndef find_floor_and_ceil(root, key):\n    floor = -1\n    ceil = -1\n    while root:\n        if root.val == key:\n            floor = root.val\n            ceil = root.val\n            break\n        elif root.val < key:\n            floor = root.val\n            root = root.right\n        else:\n            ceil = root.val\n            root = root.left\n    return floor, ceil\n# Read input from stdin\nt = int(input())\nfor _ in range(t):\n    n, k = map(int, input().split())\n    root = None\n    values = list(map(int, input().split()))\n    for val in values:\n        if val != -1:\n            root = insert_node(root, val)\n    floor, ceil = find_floor_and_ceil(root, k)\n    print(floor, ceil)",
      "JavaScript": "const fs = require('fs');\nconst lines = fs.readFileSync(0, 'utf-8').trim().split('\\n');\nlet idx = 0;\n// Define the structure for a BST node\nfunction Node(val) {\n    this.val = val;\n    this.left = null;\n    this.right = null;\n}\n// Function to insert a node into the BST\nfunction insertNode(root, val) {\n    if (root === null) {\n        root = new Node(val);\n    } else if (val < root.val) {\n        root.left = insertNode(root.left, val);\n    } else if (val > root.val) {\n        root.right = insertNode(root.right, val);\n    }\n    return root;\n}\n// Function to find the floor and ceil values in the BST\nfunction findFloorAndCeil(root, key) {\n    let floor = -1;\n    let ceil = -1;\n    while (root) {\n        if (root.val === key) {\n            floor = root.val;\n            ceil = root.val;\n            break;\n        } else if (root.val < key) {\n            floor = root.val;\n            root = root.right;\n        } else {\n            ceil = root.val;\n            root = root.left;\n        }\n    }\n    return [floor, ceil];\n}\n// Read input from stdin\nlet t = +lines[idx++];\nwhile (t--) {\n    let [n, k] = lines[idx++].trim().split(' ').map(Number);\n    let root = null;\n    let values = lines[idx++].trim().split(' ').map(Number);\n    for (let val of values) {\n        if (val !== -1) {\n            root = insertNode(root, val);\n        }\n    }\n    let [floor, ceil] = findFloorAndCeil(root, k);\n    console.log(floor, ceil);\n}"
    }
  }
}