{
  "questionId": "Q9947",
  "title": "Maximise Sum",
  "slug": "maximise-sum",
  "version": 1,
  "createdAt": "2026-07-02",
  "updatedAt": "2026-07-04",
  "metadata": {
    "category": "Arrays | Greedy",
    "difficulty": "Medium",
    "isPremium": true,
    "tags": [
      "Prefix Maximum",
      "Suffix Maximum",
      "Greedy",
      "Array Transformation"
    ],
    "companyTags": [],
    "topicTags": [
      "Data Structures"
    ],
    "QBCategory": "CCD"
  },
  "content": {
    "problemStatement": "You are given an array AAA consisting of NNN positive integers.\n\nYou are allowed to do the following operation any number of times:\n\n\nSelect two integers lll and rrr such that 1≤l&lt;r≤N1 \\leq l &lt; r \\leq N1≤l&lt;r≤N;\nChange AiA_iAi​ to min⁡(Al,Ar)\\min(A_l, A_r)min(Al​,Ar​) for all l&lt;i&lt;rl &lt; i &lt; rl&lt;i&lt;r.\nFind the **maximum** value of ∑i=1NAi\\sum_{i=1}^{N} A_i∑i=1N​Ai​ that you can achieve after any (possibly zero) number of operations.\n\n\nInput Format\n\nThe first line of input contains a single integer TTT, denoting the number of test cases.\nEach test case consists of two lines of input.\n\nThe first line of each test case contains a single integer NNN — the length of the array AAA.\nThe second line of each test case contains NNN space-separated integers A1,A2,…,ANA_1,A_2,\\ldots,A_NA1​,A2​,…,AN​ representing the array AAA.\n\n\n\n\nOutput Format\nFor each test case, output on a new line, the maximum sum that you can achieve after any (possibly zero) number of operations.",
    "constraints": [
      "1 ≤ T ≤ 10^5",
      "1 ≤ N ≤ 10^5",
      "1 ≤ Ai ≤ 10^9",
      "The sum of N over all test cases does not exceed 5·10^5."
    ],
    "inputFormat": "The first line contains an integer T, the number of test cases. Each test case consists of two lines: the first line contains an integer N, the length of the array. The second line contains N space‑separated positive integers A1, A2, …, AN.",
    "outputFormat": "For each test case output a single line containing the maximum possible sum of the array after applying any number of allowed operations.",
    "sampleTestCases": [
      {
        "id": "sample_1",
        "input": "3\n2\n2 3\n3\n5 1 7\n4\n5 5 5 5",
        "output": "5\n17\n20",
        "explanation": "Test case 1: No interior element exists, sum stays 2+3=5.\\nTest case 2: Using l=1, r=3 we set A2 to min(5,7)=5, final array [5,5,7] sum=17.\\nTest case 3: All elements equal, any operation keeps them unchanged, sum=20."
      }
    ]
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "label": "Case 1",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_2",
        "label": "Case 2",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_3",
        "label": "Case 3",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_4",
        "label": "Case 4",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_5",
        "label": "Case 5",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      }
    ],
    "totalTestCases": 5,
    "totalWeight": 10
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      2,
      2,
      2,
      2,
      2
    ],
    "maxScore": 10
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "For a position i we can increase its value using any pair (l, r) with l ≤ i ≤ r. The best we can do is to use the largest element on the left of i as the left boundary and the largest element on the right of i as the right boundary. The operation sets A_i to min(A_l, A_r), therefore the maximal achievable value for i is min( max_{j ≤ i} A_j , max_{k ≥ i} A_k ). Computing the prefix maximums and suffix maximums gives these two values for every i in O(N). The answer is the sum of min(prefixMax[i], suffixMax[i]) over all i. The algorithm is linear per test case and uses O(N) additional memory.\nProof sketch: Let L_i = max_{j ≤ i} A_j and R_i = max_{j ≥ i} A_j. Choose positions p and q where A_p = L_i (p ≤ i) and A_q = R_i (q ≥ i). By first raising all elements between p and q to min(L_i, R_i) using the operation on (p, q) we can set A_i to that value. No operation can give a value larger than min(L_i, R_i) because any operation affecting i uses two boundary values, each ≤ the respective side maximum, so the resulting value is bounded by the minimum of those maxima. Hence the formula is optimal.",
    "timeComplexity": "O(N) per test case, overall O( Σ N ) ≤ O(5·10^5 ).",
    "spaceComplexity": "O(N) for the prefix and suffix arrays (can be reduced to O(1) with two passes, but O(N) fits the limits).",
    "code": {
      "C": "#include <stdio.h>\n#include <stdlib.h>\n\nint main() {\n    int T; if (scanf(\"%d\", &T)!=1) return 0;\n    while (T--) {\n        int N; scanf(\"%d\", &N);\n        long long *a = (long long*)malloc(N * sizeof(long long));\n        for (int i = 0; i < N; ++i) scanf(\"%lld\", &a[i]);\n        long long *pref = (long long*)malloc(N * sizeof(long long));\n        long long *suff = (long long*)malloc(N * sizeof(long long));\n        long long cur = 0;\n        for (int i = 0; i < N; ++i) {\n            if (a[i] > cur) cur = a[i];\n            pref[i] = cur;\n        }\n        cur = 0;\n        for (int i = N-1; i >= 0; --i) {\n            if (a[i] > cur) cur = a[i];\n            suff[i] = cur;\n        }\n        long long ans = 0;\n        for (int i = 0; i < N; ++i) {\n            long long v = pref[i] < suff[i] ? pref[i] : suff[i];\n            ans += v;\n        }\n        printf(\"%lld\\n\", ans);\n        free(a); free(pref); free(suff);\n    }\n    return 0;\n}",
      "C++": "#include <bits/stdc++.h>\nusing namespace std;\nint main(){\n    ios::sync_with_stdio(false);\n    cin.tie(nullptr);\n    int T; if(!(cin>>T)) return 0;\n    while(T--){\n        int N; cin>>N;\n        vector<long long> a(N);\n        for(int i=0;i<N;++i) cin>>a[i];\n        vector<long long> pref(N), suff(N);\n        long long cur=0;\n        for(int i=0;i<N;++i){\n            cur = max(cur, a[i]);\n            pref[i]=cur;\n        }\n        cur=0;\n        for(int i=N-1;i>=0;--i){\n            cur = max(cur, a[i]);\n            suff[i]=cur;\n        }\n        long long ans=0;\n        for(int i=0;i<N;++i) ans += min(pref[i], suff[i]);\n        cout<<ans<<\"\\n\";\n    }\n    return 0;\n}",
      "Java": "import java.io.*;\nimport java.util.*;\n\npublic class Main {\n    public static void main(String[] args) throws Exception {\n        FastScanner fs = new FastScanner(System.in);\n        int T = fs.nextInt();\n        StringBuilder sb = new StringBuilder();\n        while (T-- > 0) {\n            int N = fs.nextInt();\n            long[] a = new long[N];\n            for (int i = 0; i < N; i++) a[i] = fs.nextLong();\n            long[] pref = new long[N];\n            long[] suff = new long[N];\n            long cur = 0;\n            for (int i = 0; i < N; i++) {\n                cur = Math.max(cur, a[i]);\n                pref[i] = cur;\n            }\n            cur = 0;\n            for (int i = N - 1; i >= 0; i--) {\n                cur = Math.max(cur, a[i]);\n                suff[i] = cur;\n            }\n            long ans = 0;\n            for (int i = 0; i < N; i++) ans += Math.min(pref[i], suff[i]);\n            sb.append(ans).append('\\n');\n        }\n        System.out.print(sb.toString());\n    }\n\n    // Fast scanner for large input\n    static class FastScanner {\n        private final InputStream in;\n        private final byte[] buffer = new byte[1 << 16];\n        private int ptr = 0, len = 0;\n        FastScanner(InputStream in) { this.in = in; }\n        private int readByte() throws IOException {\n            if (ptr >= len) {\n                len = in.read(buffer);\n                ptr = 0;\n                if (len <= 0) return -1;\n            }\n            return buffer[ptr++];\n        }\n        long nextLong() throws IOException {\n            int c; do { c = readByte(); } while (c <= ' ');\n            boolean neg = false;\n            if (c == '-') { neg = true; c = readByte(); }\n            long val = 0;\n            while (c > ' ') {\n                val = val * 10 + (c - '0');\n                c = readByte();\n            }\n            return neg ? -val : val;\n        }\n        int nextInt() throws IOException { return (int) nextLong(); }\n    }\n}",
      "Python3": "#!/usr/bin/env python3\nimport sys\n\ndef solve():\n    data = sys.stdin.buffer.read().split()\n    it = iter(data)\n    t = int(next(it))\n    out_lines = []\n    for _ in range(t):\n        n = int(next(it))\n        a = [int(next(it)) for _ in range(n)]\n        pref = [0]*n\n        suff = [0]*n\n        cur = 0\n        for i in range(n):\n            cur = max(cur, a[i])\n            pref[i] = cur\n        cur = 0\n        for i in range(n-1, -1, -1):\n            cur = max(cur, a[i])\n            suff[i] = cur\n        ans = sum(min(pref[i], suff[i]) for i in range(n))\n        out_lines.append(str(ans))\n    sys.stdout.write('\\n'.join(out_lines))\n\nif __name__ == \"__main__\":\n    solve()\n",
      "JavaScript": "const fs = require('fs');\nconst input = fs.readFileSync(0, 'utf-8').trim().split(/\\s+/).map(Number);\nlet idx = 0;\nconst T = input[idx++];\nlet outputs = [];\nfor (let tc = 0; tc < T; ++tc) {\n  const N = input[idx++];\n  const a = input.slice(idx, idx + N);\n  idx += N;\n  const pref = new Array(N);\n  const suff = new Array(N);\n  let cur = 0;\n  for (let i = 0; i < N; ++i) {\n    if (a[i] > cur) cur = a[i];\n    pref[i] = cur;\n  }\n  cur = 0;\n  for (let i = N - 1; i >= 0; --i) {\n    if (a[i] > cur) cur = a[i];\n    suff[i] = cur;\n  }\n  let ans = 0n;\n  for (let i = 0; i < N; ++i) {\n    const v = pref[i] < suff[i] ? pref[i] : suff[i];\n    ans += BigInt(v);\n  }\n  outputs.push(ans.toString());\n}\nconsole.log(outputs.join('\\n'));\n"
    }
  },
  "QBCategory": "CCD",
  "boilerPlates": {
    "C": "#include <stdio.h>\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "C++": "#include <iostream>\nusing namespace std;\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "Java": "import java.util.*;\nimport java.io.*;\n\npublic class Main {\n    public static void main(String[] args) throws IOException {\n        // your code goes here\n    }\n}",
    "Python3": "# your code goes here\n",
    "JavaScript": "// your code goes here\n"
  },
  "_internal": {
    "methodName": "",
    "params": []
  }
}