{
  "questionId": "Q9439",
  "title": "Existence Of X",
  "slug": "existence-of-x",
  "version": 1,
  "createdAt": "2026-07-02",
  "updatedAt": "2026-07-04",
  "metadata": {
    "category": "Bit Manipulation",
    "difficulty": "Hard",
    "isPremium": true,
    "tags": [
      "XOR",
      "Dynamic Programming",
      "Bitwise DP"
    ],
    "companyTags": [],
    "topicTags": [
      "Dynamic Programming"
    ],
    "QBCategory": "CCD"
  },
  "content": {
    "problemStatement": "You are given non-negative integers AAA, BBB and CCC.\n\nDoes there exist a non-negative integer XXX such that A⊕X+B⊕X=C⊕XA \\oplus X+ B \\oplus X = C \\oplus XA⊕X+B⊕X=C⊕X?\n\nAs a reminder, ⊕\\oplus⊕ denotes the bitwise XOR operation.\n\n\nInput Format\n\nThe first line of input will contain a single integer TTT, denoting the number of test cases.\nThe only line of each test case contains three space-separated non-negative integers AAA, BBB and CCC.\n\n\nOutput Format\nFor each test case, print on a new line the answer: `YES` if valid XXX exists, and `NO` otherwise.\n\n\nEach character of the output may be printed in either uppercase or lowercase, i.e, the strings `Yes`, `YES, `yes`, `yEs` will all be treated as identical.",
    "constraints": [
      "1 <= T <= 100000",
      "0 <= A, B, C < 2^27"
    ],
    "inputFormat": "The first line contains an integer T, the number of test cases. Each of the next T lines contains three space‑separated non‑negative integers A, B and C.",
    "outputFormat": "For each test case output a single line containing \"YES\" if there exists a non‑negative integer X such that (A⊕X)+(B⊕X)=C⊕X, otherwise output \"NO\".",
    "sampleTestCases": [
      {
        "id": "sample_1",
        "input": "5\\n2 5 7\\n2 3 13\\n7 0 7\\n2 7 6\\n1 6 6",
        "output": "YES\\nNO\\nYES\\nYES\\nYES",
        "explanation": "Test case 1: X = 0 gives (2⊕0)+(5⊕0)=2+5=7=7⊕0.\\nTest case 2: No X satisfies the equation (can be verified by the DP).\\nTest case 3: X = 0 works because 7+0=7.\\nTest case 4: X = 3 works: (2⊕3)=1, (7⊕3)=4, 1+4=5 and (6⊕3)=5.\\nTest case 5: X = 1 works: (1⊕1)=0, (6⊕1)=7, 0+7=7 and (6⊕1)=7."
      },
      {
        "id": "sample_2",
        "input": "3\\n0 0 0\\n0 1 1\\n5 9 12",
        "output": "YES\\nYES\\nNO",
        "explanation": "First case: X = 0 gives 0+0=0.\\nSecond case: X = 0 gives 0+1=1.\\nThird case: Exhaustive check of X in [0,2^27) shows no solution; the DP also returns false."
      }
    ]
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "label": "Case 1",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_2",
        "label": "Case 2",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_3",
        "label": "Case 3",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_4",
        "label": "Case 4",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_5",
        "label": "Case 5",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      }
    ],
    "totalTestCases": 5,
    "totalWeight": 10
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      2,
      2,
      2,
      2,
      2
    ],
    "maxScore": 10
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "We treat each bit independently but keep track of the carry generated by the addition. For every bit position i (0‑based) we know a_i, b_i, c_i (the i‑th bits of A, B, C). The unknown is x_i (0 or 1). After choosing x_i we obtain a'_i = a_i xor x_i, b'_i = b_i xor x_i and c'_i = c_i xor x_i. The addition condition for this bit is:\n    a'_i + b'_i + carry_in = c'_i + 2 * carry_out\nwhere carry_in is the carry from the previous (lower) bit and carry_out is the carry to the next (higher) bit. We perform a DP over the bits with two states: possible carry = 0 or 1 after processing the current bit. Starting with carry = 0 at bit 0, for each bit we try both possibilities for x_i and keep the resulting carry_out if the equality holds. After processing all bits (up to 30 bits, enough for the given limits and a possible final carry) the answer is YES iff a state with carry = 0 remains reachable. The DP runs in O(number_of_bits * 2 * 2) = O(30) per test case and uses O(1) extra memory.",
    "timeComplexity": "O(T * 30) ≈ O(T)",
    "spaceComplexity": "O(1)",
    "code": {
      "C": "#include <stdio.h>\n#include <stdbool.h>\nint main(){\n    int T; if(scanf(\"%d\", &T)!=1) return 0;\n    while(T--){\n        unsigned int A,B,C; scanf(\"%u %u %u\", &A, &B, &C);\n        bool dp[2] = {true,false}, ndp[2];\n        for(int bit=0; bit<31; ++bit){\n            ndp[0]=ndp[1]=false;\n            int a = (A>>bit)&1;\n            int b = (B>>bit)&1;\n            int c = (C>>bit)&1;\n            for(int carry=0; carry<2; ++carry) if(dp[carry]){\n                for(int x=0; x<2; ++x){\n                    int a1 = a ^ x;\n                    int b1 = b ^ x;\n                    int c1 = c ^ x;\n                    int sum = a1 + b1 + carry;\n                    int bitRes = sum & 1;\n                    int ncarry = sum >> 1;\n                    if(bitRes == c1) ndp[ncarry] = true;\n                }\n            }\n            dp[0]=ndp[0]; dp[1]=ndp[1];\n        }\n        puts(dp[0] ? \"YES\" : \"NO\");\n    }\n    return 0;\n}\n",
      "C++": "#include <bits/stdc++.h>\nusing namespace std;\nint main(){\n    ios::sync_with_stdio(false);\n    cin.tie(nullptr);\n    int T; if(!(cin>>T)) return 0;\n    while(T--){\n        unsigned int A,B,C; cin>>A>>B>>C;\n        bool dp[2] = {true,false}, ndp[2];\n        for(int bit=0; bit<31; ++bit){\n            ndp[0]=ndp[1]=false;\n            int a = (A>>bit)&1;\n            int b = (B>>bit)&1;\n            int c = (C>>bit)&1;\n            for(int carry=0; carry<2; ++carry) if(dp[carry]){\n                for(int x=0; x<2; ++x){\n                    int a1 = a ^ x;\n                    int b1 = b ^ x;\n                    int c1 = c ^ x;\n                    int sum = a1 + b1 + carry;\n                    int bitRes = sum & 1;\n                    int ncarry = sum >> 1;\n                    if(bitRes == c1) ndp[ncarry] = true;\n                }\n            }\n            dp[0]=ndp[0]; dp[1]=ndp[1];\n        }\n        cout << (dp[0] ? \"YES\" : \"NO\") << '\\n';\n    }\n    return 0;\n}\n",
      "Java": "import java.io.*;\nimport java.util.*;\npublic class Main {\n    public static void main(String[] args) throws Exception {\n        FastScanner fs = new FastScanner(System.in);\n        int T = fs.nextInt();\n        StringBuilder sb = new StringBuilder();\n        while (T-- > 0) {\n            int A = fs.nextInt();\n            int B = fs.nextInt();\n            int C = fs.nextInt();\n            boolean[] dp = {true, false};\n            boolean[] ndp = new boolean[2];\n            for (int bit = 0; bit < 31; ++bit) {\n                ndp[0] = ndp[1] = false;\n                int a = (A >> bit) & 1;\n                int b = (B >> bit) & 1;\n                int c = (C >> bit) & 1;\n                for (int carry = 0; carry < 2; ++carry) if (dp[carry]) {\n                    for (int x = 0; x < 2; ++x) {\n                        int a1 = a ^ x;\n                        int b1 = b ^ x;\n                        int c1 = c ^ x;\n                        int sum = a1 + b1 + carry;\n                        int bitRes = sum & 1;\n                        int ncarry = sum >> 1;\n                        if (bitRes == c1) ndp[ncarry] = true;\n                    }\n                }\n                dp[0] = ndp[0];\n                dp[1] = ndp[1];\n            }\n            sb.append(dp[0] ? \"YES\" : \"NO\").append('\\n');\n        }\n        System.out.print(sb.toString());\n    }\n    // Fast scanner for speed\n    static class FastScanner {\n        private final InputStream in;\n        private final byte[] buffer = new byte[1 << 16];\n        private int ptr = 0, len = 0;\n        FastScanner(InputStream in) { this.in = in; }\n        private int readByte() throws IOException {\n            if (ptr >= len) {\n                len = in.read(buffer);\n                ptr = 0;\n                if (len <= 0) return -1;\n            }\n            return buffer[ptr++];\n        }\n        int nextInt() throws IOException {\n            int c, sign = 1, val = 0;\n            do { c = readByte(); } while (c <= ' ');\n            if (c == '-') { sign = -1; c = readByte(); }\n            while (c > ' ') {\n                val = val * 10 + (c - '0');\n                c = readByte();\n            }\n            return val * sign;\n        }\n    }\n}\n",
      "Python3": "import sys\n\ndef possible(A, B, C):\n    dp = [True, False]  # carry 0,1\n    for bit in range(31):\n        ndp = [False, False]\n        a = (A >> bit) & 1\n        b = (B >> bit) & 1\n        c = (C >> bit) & 1\n        for carry in (0, 1):\n            if not dp[carry]:\n                continue\n            for x in (0, 1):\n                a1 = a ^ x\n                b1 = b ^ x\n                c1 = c ^ x\n                s = a1 + b1 + carry\n                bit_res = s & 1\n                ncarry = s >> 1\n                if bit_res == c1:\n                    ndp[ncarry] = True\n        dp = ndp\n    return dp[0]\n\ndef main():\n    data = sys.stdin.read().strip().split()\n    if not data:\n        return\n    it = iter(data)\n    T = int(next(it))\n    out_lines = []\n    for _ in range(T):\n        A = int(next(it)); B = int(next(it)); C = int(next(it))\n        out_lines.append('YES' if possible(A, B, C) else 'NO')\n    sys.stdout.write('\\n'.join(out_lines))\n\nif __name__ == \"__main__\":\n    main()\n",
      "JavaScript": "const fs = require('fs');\nconst input = fs.readFileSync(0, 'utf-8').trim().split(/\\s+/);\nlet idx = 0;\nconst T = Number(input[idx++]);\nlet out = [];\nfunction possible(A, B, C) {\n  let dp = [true, false]; // carry 0,1\n  for (let bit = 0; bit < 31; ++bit) {\n    const ndp = [false, false];\n    const a = (A >> bit) & 1;\n    const b = (B >> bit) & 1;\n    const c = (C >> bit) & 1;\n    for (let carry = 0; carry < 2; ++carry) if (dp[carry]) {\n      for (let x = 0; x < 2; ++x) {\n        const a1 = a ^ x;\n        const b1 = b ^ x;\n        const c1 = c ^ x;\n        const sum = a1 + b1 + carry;\n        const bitRes = sum & 1;\n        const ncarry = sum >> 1;\n        if (bitRes === c1) ndp[ncarry] = true;\n      }\n    }\n    dp = ndp;\n  }\n  return dp[0];\n}\nfor (let t = 0; t < T; ++t) {\n  const A = Number(input[idx++]);\n  const B = Number(input[idx++]);\n  const C = Number(input[idx++]);\n  out.push(possible(A, B, C) ? 'YES' : 'NO');\n}\nconsole.log(out.join('\\n'));\n"
    }
  },
  "QBCategory": "CCD",
  "boilerPlates": {
    "C": "#include <stdio.h>\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "C++": "#include <iostream>\nusing namespace std;\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "Java": "import java.util.*;\nimport java.io.*;\n\npublic class Main {\n    public static void main(String[] args) throws IOException {\n        // your code goes here\n    }\n}",
    "Python3": "# your code goes here\n",
    "JavaScript": "// your code goes here\n"
  },
  "_internal": {
    "methodName": "",
    "params": []
  }
}