{
  "questionId": "Q4671",
  "title": "Short Answer type",
  "slug": "short-answer-type",
  "version": 1,
  "createdAt": "2026-07-01",
  "updatedAt": "2026-07-04",
  "metadata": {
    "category": "Implementation",
    "difficulty": "Medium",
    "isPremium": true,
    "tags": [
      "Explanation",
      "String Handling",
      "Input/Output"
    ],
    "companyTags": [],
    "topicTags": [
      "Arrays"
    ],
    "QBCategory": "CCD"
  },
  "content": {
    "problemStatement": "Q3) A. Explain the differences between mutable and immutable data types in Python with suitable\nexamples. ---3 Marks",
    "constraints": [
      "1 <= T <= 10",
      "Each prompt line length <= 100 characters"
    ],
    "inputFormat": "The first line contains an integer T (1 ≤ T ≤ 10), the number of test cases. Each of the next T lines contains a prompt string (length ≤ 100) which can be ignored by the program.",
    "outputFormat": "For each test case, output a single line containing a clear explanation of the differences between mutable and immutable data types in Python, including at least one mutable example and one immutable example.",
    "sampleTestCases": [
      {
        "id": "sample_1",
        "input": "1\nExplain mutable vs immutable data types in Python.",
        "output": "In Python, mutable data types can be changed after creation (e.g., list, dict, set). Immutable data types cannot be changed after creation (e.g., int, float, str, tuple). Example of mutable: lst = [1, 2]; lst.append(3) modifies lst. Example of immutable: s = \"abc\"; trying to assign s[0] = \"d\" raises TypeError.",
        "explanation": "T=1, the single prompt is ignored and the fixed explanation is printed once."
      },
      {
        "id": "sample_2",
        "input": "2\nWhat are mutable types?\nGive examples of immutable types.",
        "output": "In Python, mutable data types can be changed after creation (e.g., list, dict, set). Immutable data types cannot be changed after creation (e.g., int, float, str, tuple). Example of mutable: lst = [1, 2]; lst.append(3) modifies lst. Example of immutable: s = \"abc\"; trying to assign s[0] = \"d\" raises TypeError.\nIn Python, mutable data types can be changed after creation (e.g., list, dict, set). Immutable data types cannot be changed after creation (e.g., int, float, str, tuple). Example of mutable: lst = [1, 2]; lst.append(3) modifies lst. Example of immutable: s = \"abc\"; trying to assign s[0] = \"d\" raises TypeError.",
        "explanation": "T=2, each of the two prompts is ignored and the same explanation is printed on separate lines."
      },
      {
        "id": "sample_3",
        "input": "3\\nPrompt1\\nPrompt2\\nPrompt3",
        "output": "In Python, mutable data types can be changed after creation (e.g., list, dict, set). Immutable data types cannot be changed after creation (e.g., int, float, str, tuple). Example of mutable: lst = [1, 2]; lst.append(3) modifies lst. Example of immutable: s = \"abc\"; trying to assign s[0] = \"d\" raises TypeError.\nIn Python, mutable data types can be changed after creation (e.g., list, dict, set). Immutable data types cannot be changed after creation (e.g., int, float, str, tuple). Example of mutable: lst = [1, 2]; lst.append(3) modifies lst. Example of immutable: s = \"abc\"; trying to assign s[0] = \"d\" raises TypeError.\nIn Python, mutable data types can be changed after creation (e.g., list, dict, set). Immutable data types cannot be changed after creation (e.g., int, float, str, tuple). Example of mutable: lst = [1, 2]; lst.append(3) modifies lst. Example of immutable: s = \"abc\"; trying to assign s[0] = \"d\" raises TypeError.",
        "explanation": "T=3, three prompts are read and ignored; the explanation is printed three times."
      }
    ]
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "label": "minimum boundary",
        "input": "1\nx",
        "expectedOutput": "In Python, mutable data types can be changed after creation (e.g., list, dict, set). Immutable data types cannot be changed after creation (e.g., int, float, str, tuple). Example of mutable: lst = [1, 2]; lst.append(3) modifies lst. Example of immutable: s = \"abc\"; trying to assign s[0] = \"d\" raises TypeError.",
        "weight": 2
      },
      {
        "id": "tc_2",
        "label": "maximum boundary",
        "input": "10\np1\np2\np3\np4\np5\np6\np7\np8\np9\np10",
        "expectedOutput": "In Python, mutable data types can be changed after creation (e.g., list, dict, set). Immutable data types cannot be changed after creation (e.g., int, float, str, tuple). Example of mutable: lst = [1, 2]; lst.append(3) modifies lst. Example of immutable: s = \"abc\"; trying to assign s[0] = \"d\" raises TypeError.\nIn Python, mutable data types can be changed after creation (e.g., list, dict, set). Immutable data types cannot be changed after creation (e.g., int, float, str, tuple). Example of mutable: lst = [1, 2]; lst.append(3) modifies lst. Example of immutable: s = \"abc\"; trying to assign s[0] = \"d\" raises TypeError.\nIn Python, mutable data types can be changed after creation (e.g., list, dict, set). Immutable data types cannot be changed after creation (e.g., int, float, str, tuple). Example of mutable: lst = [1, 2]; lst.append(3) modifies lst. Example of immutable: s = \"abc\"; trying to assign s[0] = \"d\" raises TypeError.\nIn Python, mutable data types can be changed after creation (e.g., list, dict, set). Immutable data types cannot be changed after creation (e.g., int, float, str, tuple). Example of mutable: lst = [1, 2]; lst.append(3) modifies lst. Example of immutable: s = \"abc\"; trying to assign s[0] = \"d\" raises TypeError.\nIn Python, mutable data types can be changed after creation (e.g., list, dict, set). Immutable data types cannot be changed after creation (e.g., int, float, str, tuple). Example of mutable: lst = [1, 2]; lst.append(3) modifies lst. Example of immutable: s = \"abc\"; trying to assign s[0] = \"d\" raises TypeError.\nIn Python, mutable data types can be changed after creation (e.g., list, dict, set). Immutable data types cannot be changed after creation (e.g., int, float, str, tuple). Example of mutable: lst = [1, 2]; lst.append(3) modifies lst. Example of immutable: s = \"abc\"; trying to assign s[0] = \"d\" raises TypeError.\nIn Python, mutable data types can be changed after creation (e.g., list, dict, set). Immutable data types cannot be changed after creation (e.g., int, float, str, tuple). Example of mutable: lst = [1, 2]; lst.append(3) modifies lst. Example of immutable: s = \"abc\"; trying to assign s[0] = \"d\" raises TypeError.\nIn Python, mutable data types can be changed after creation (e.g., list, dict, set). Immutable data types cannot be changed after creation (e.g., int, float, str, tuple). Example of mutable: lst = [1, 2]; lst.append(3) modifies lst. Example of immutable: s = \"abc\"; trying to assign s[0] = \"d\" raises TypeError.\nIn Python, mutable data types can be changed after creation (e.g., list, dict, set). Immutable data types cannot be changed after creation (e.g., int, float, str, tuple). Example of mutable: lst = [1, 2]; lst.append(3) modifies lst. Example of immutable: s = \"abc\"; trying to assign s[0] = \"d\" raises TypeError.",
        "weight": 2
      },
      {
        "id": "tc_3",
        "label": "Case 3",
        "input": "1\nx",
        "expectedOutput": "In Python, mutable data types can be changed after creation (e.g., list, dict, set). Immutable data types cannot be changed after creation (e.g., int, float, str, tuple). Example of mutable: lst = [1, 2]; lst.append(3) modifies lst. Example of immutable: s = \"abc\"; trying to assign s[0] = \"d\" raises TypeError.",
        "weight": 2
      },
      {
        "id": "tc_4",
        "label": "Case 4",
        "input": "1\nx",
        "expectedOutput": "In Python, mutable data types can be changed after creation (e.g., list, dict, set). Immutable data types cannot be changed after creation (e.g., int, float, str, tuple). Example of mutable: lst = [1, 2]; lst.append(3) modifies lst. Example of immutable: s = \"abc\"; trying to assign s[0] = \"d\" raises TypeError.",
        "weight": 2
      },
      {
        "id": "tc_5",
        "label": "Case 5",
        "input": "1\nx",
        "expectedOutput": "In Python, mutable data types can be changed after creation (e.g., list, dict, set). Immutable data types cannot be changed after creation (e.g., int, float, str, tuple). Example of mutable: lst = [1, 2]; lst.append(3) modifies lst. Example of immutable: s = \"abc\"; trying to assign s[0] = \"d\" raises TypeError.",
        "weight": 2
      }
    ],
    "totalTestCases": 5,
    "totalWeight": 10
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      2,
      2,
      2,
      2,
      2
    ],
    "maxScore": 10
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "Read the integer T. Loop T times, reading and discarding each prompt line. For every iteration print a predefined explanatory sentence about mutable and immutable data types in Python. The algorithm is O(T) time and O(1) extra space.",
    "timeComplexity": "O(T) – one constant‑time operation per test case.",
    "spaceComplexity": "O(1) – only a few scalar variables are used.",
    "code": {
      "C": "#include <stdio.h>\n#include <string.h>\nint main(){\n    int T;\n    if(scanf(\"%d\", &T)!=1) return 0;\n    char buffer[256];\n    // consume the newline after T\n    fgets(buffer, sizeof(buffer), stdin);\n    const char *explanation = \"In Python, mutable data types can be changed after creation (e.g., list, dict, set). Immutable data types cannot be changed after creation (e.g., int, float, str, tuple). Example of mutable: lst = [1, 2]; lst.append(3) modifies lst. Example of immutable: s = \\\"abc\\\"; trying to assign s[0] = \\\"d\\\" raises TypeError.\";\n    for(int i=0;i<T;i++){\n        fgets(buffer, sizeof(buffer), stdin); // read and ignore prompt\n        printf(\"%s\", explanation);\n        if(i!=T-1) printf(\"\\n\");\n    }\n    return 0;\n}",
      "C++": "#include <bits/stdc++.h>\nusing namespace std;\nint main(){\n    ios::sync_with_stdio(false);\n    cin.tie(nullptr);\n    int T;\n    if(!(cin>>T)) return 0;\n    string line;\n    getline(cin, line); // consume endline after T\n    const string explanation = \"In Python, mutable data types can be changed after creation (e.g., list, dict, set). Immutable data types cannot be changed after creation (e.g., int, float, str, tuple). Example of mutable: lst = [1, 2]; lst.append(3) modifies lst. Example of immutable: s = \\\"abc\\\"; trying to assign s[0] = \\\"d\\\" raises TypeError.\";\n    for(int i=0;i<T;i++){\n        getline(cin, line); // read prompt, ignore\n        cout << explanation;\n        if(i!=T-1) cout << '\\n';\n    }\n    return 0;\n}",
      "Java": "import java.io.*;\npublic class Main {\n    public static void main(String[] args) throws IOException {\n        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));\n        String first = br.readLine();\n        if(first == null) return;\n        int T = Integer.parseInt(first.trim());\n        String explanation = \"In Python, mutable data types can be changed after creation (e.g., list, dict, set). Immutable data types cannot be changed after creation (e.g., int, float, str, tuple). Example of mutable: lst = [1, 2]; lst.append(3) modifies lst. Example of immutable: s = \\\"abc\\\"; trying to assign s[0] = \\\"d\\\" raises TypeError.\";\n        StringBuilder sb = new StringBuilder();\n        for(int i=0;i<T;i++){\n            br.readLine(); // ignore prompt line\n            sb.append(explanation);\n            if(i!=T-1) sb.append('\\n');\n        }\n        System.out.print(sb.toString());\n    }\n}",
      "Python3": "import sys\n\ndef main():\n    data = sys.stdin.read().splitlines()\n    if not data:\n        return\n    T = int(data[0].strip())\n    explanation = (\n        \"In Python, mutable data types can be changed after creation (e.g., list, dict, set). \"\n        \"Immutable data types cannot be changed after creation (e.g., int, float, str, tuple). \"\n        \"Example of mutable: lst = [1, 2]; lst.append(3) modifies lst. \"\n        \"Example of immutable: s = \\\"abc\\\"; trying to assign s[0] = \\\"d\\\" raises TypeError.\"\n    )\n    out = []\n    for i in range(1, T+1):\n        # each line data[i] is the prompt, ignored\n        out.append(explanation)\n    sys.stdout.write(\"\\n\".join(out))\n\nif __name__ == \"__main__\":\n    main()",
      "JavaScript": "const fs = require('fs');\nconst input = fs.readFileSync(0, 'utf-8').trim().split('\\n');\nif (input.length === 0) process.exit(0);\nconst T = parseInt(input[0].trim(), 10);\nconst explanation = \"In Python, mutable data types can be changed after creation (e.g., list, dict, set). Immutable data types cannot be changed after creation (e.g., int, float, str, tuple). Example of mutable: lst = [1, 2]; lst.append(3) modifies lst. Example of immutable: s = \\\"abc\\\"; trying to assign s[0] = \\\"d\\\" raises TypeError.\";\nlet out = [];\nfor (let i = 1; i <= T; i++) {\n  // input[i] is the prompt, ignored\n  out.push(explanation);\n}\nprocess.stdout.write(out.join('\\n'));\n"
    }
  },
  "QBCategory": "CCD",
  "boilerPlates": {
    "C": "#include <stdio.h>\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "C++": "#include <iostream>\nusing namespace std;\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "Java": "import java.util.*;\nimport java.io.*;\n\npublic class Main {\n    public static void main(String[] args) throws IOException {\n        // your code goes here\n    }\n}",
    "Python3": "# your code goes here\n",
    "JavaScript": "// your code goes here\n"
  },
  "_internal": {
    "methodName": "",
    "params": []
  }
}