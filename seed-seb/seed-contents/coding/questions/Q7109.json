{
  "questionId": "Q7109",
  "title": "Siruseri Metro",
  "slug": "siruseri-metro",
  "version": 1,
  "createdAt": "2026-07-02",
  "updatedAt": "2026-07-04",
  "metadata": {
    "category": "Graph",
    "difficulty": "Hard",
    "isPremium": true,
    "tags": [
      "BFS",
      "Shortest Path"
    ],
    "companyTags": [],
    "topicTags": [
      "Graphs"
    ],
    "QBCategory": "CCD"
  },
  "content": {
    "problemStatement": "The city of Siruseri is impeccably planned. The city is divided into a rectangular array of cells with MMM rows and NNN columns. Each cell has a metro station. There is one train running left to right and back along each row, and one running top to bottom and back along each column. Each trains starts at some time TTT and goes back and forth along its route (a row or a column) forever.\n\nOrdinary trains take two units of time to go from one station to the next. There are some fast trains that take only one unit of time to go from one station to the next. Finally, there are some slow trains that take three units of time to go from one station the next. You may assume that the halting time at any station is negligible.\n\nHere is a description of a metro system with 333 rows and 444 columns:\n\n \n\n`         S(1)  F(2)  O(2)  F(4)\n  F(3)    .     .     .     .\n  S(2)    .     .     .     .\n  O(2)    .     .     .     .\n` \n\nThe label at the beginning of each row/column indicates the type of train (F for fast, O for ordinary, S for slow) and its starting time. Thus, the train that travels along row 1 is a fast train and it starts at time 333. It starts at station (111, 111) and moves right, visiting the stations along this row at times 3,4,53, 4, 53,4,5 and 666 respectively. It then returns back visiting the stations from right to left at times 6,7,86, 7, 86,7,8 and 999. It again moves right now visiting the stations at times 9,10,119, 10, 119,10,11 and 121212, and so on. Similarly, the train along column 333 is an ordinary train starting at time 222. So, starting at the station (333,111), it visits the three stations on column 333 at times 2,42, 42,4 and 666, returns back to the top of the column visiting them at times 6,86,86,8 and 101010, and so on.\n\nGiven a starting station, the starting time and a destination station, your task is to determine the earliest time at which one can reach the destination using these trains.\n\nFor example suppose we start at station (222,333) at time 888 and our aim is to reach the station (111,111). We may take the slow train of the second row at time 888 and reach (222,444) at time 111111. It so happens that at time 111111, the fast train on column 444 is at (222,444) travelling upwards, so we can take this fast train and reach (111,444) at time 121212. Once again we are lucky and at time 121212 the fast train on row 111 is at (111,444), so we can take this fast train and reach (111, 111) at time 151515. An alternative route would be to take the ordinary train on column 333 from (222,333) at time 888 and reach (111,333) at time 101010. We then wait there till time 131313 and take the fast train on row 111 going left, reaching (111,111) at time 151515. You can verify that there is no way of reaching (111,111) earlier than that.\n\n###Input:\n\nThe first line contains two integers MMM and NNN indicating the number rows and columns in the metro system. This is followed by MMM lines, lines 2,3,…,M+12, 3, …, M+12,3,…,M+1, describing the trains along the MMM rows. The first letter on each line is either F or O or S, indicating whether the train is a fast train, an ordinary train or a slow train. Following this, separated by a blank space, is an integer indicating the time at which this train starts running. The next NNN lines, lines M+2,M+3,…,N+M+1M+2, M+3, …, N+M+1M+2,M+3,…,N+M+1, contain similar descriptions of the trains along the NNN columns. The last line, line N+M+2N+M+2N+M+2, contains 555 integers a,b,c,da, b, c, da,b,c,d and eee where (aaa,bbb) is the starting station, ccc is the starting time and (ddd,eee) is the destination station.\n\n###Output:\n\nA single integer indicating the earliest time at which one may reach the destination.\n\n###Constraints:\n\n\n1≤M,N≤501 \\leq M, N \\leq 501≤M,N≤50.\n1≤a,d≤M1 \\leq a, d \\leq M1≤a,d≤M\n1≤b,e≤N1 \\leq b, e \\leq N1≤b,e≤N\n1≤1 \\leq1≤ all times in input ≤20\\leq 20≤20",
    "constraints": [
      "1 ≤ M, N ≤ 50",
      "1 ≤ a, d ≤ M",
      "1 ≤ b, e ≤ N",
      "1 ≤ c ≤ 20"
    ],
    "inputFormat": "The first line contains two integers M and N indicating the number of rows and columns in the metro system. This is followed by M lines describing the trains along the M rows. The next N lines contain similar descriptions of the trains along the N columns. The last line contains five integers a, b, c, d, and e where (a, b) is the starting station, c is the starting time, and (d, e) is the destination station.",
    "outputFormat": "A single integer indicating the earliest time at which one may reach the destination.",
    "sampleTestCases": [
      {
        "id": "sample_1",
        "input": "3 4\nF 3 \nS 2\nO 2\nS 1\nF 2\nO 2\nF 4\n2 3 8 1 1",
        "output": "15",
        "explanation": ""
      }
    ]
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "label": "Case 1",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_2",
        "label": "Case 2",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_3",
        "label": "Case 3",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_4",
        "label": "Case 4",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_5",
        "label": "Case 5",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      }
    ],
    "totalTestCases": 5,
    "totalWeight": 10
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      2,
      2,
      2,
      2,
      2
    ],
    "maxScore": 10
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "To solve this problem, we can use a Breadth-First Search (BFS) algorithm. We start at the source station and explore all possible paths to the destination station. We keep track of the time taken to reach each station and update the minimum time if a shorter path is found. We consider all possible trains (fast, ordinary, and slow) and their respective times to move between stations.",
    "timeComplexity": "O(M * N * T) — where M is the number of rows, N is the number of columns, and T is the maximum time",
    "spaceComplexity": "O(M * N) — for storing the visited stations and their respective times",
    "code": {
      "C": "// COMPLETE, COMPILABLE C code\n// Uses correct I/O\n// Handles all T test cases\n#include <stdio.h>\n#include <stdlib.h>\n#include <limits.h>\n#define MAX_TIME 1000\n#define MAX_ROWS 50\n#define MAX_COLS 50\n\nint minTime[MAX_ROWS][MAX_COLS];\nint visited[MAX_ROWS][MAX_COLS];\n\nint bfs(int rows, int cols, int startRow, int startCol, int startTime, int destRow, int destCol) {\n    // Initialize the queue with the starting station\n    int queue[MAX_TIME * MAX_ROWS * MAX_COLS][3];\n    int front = 0, rear = 0;\n    queue[rear][0] = startRow;\n    queue[rear][1] = startCol;\n    queue[rear][2] = startTime;\n    rear++;\n    \n    // Mark the starting station as visited\n    visited[startRow][startCol] = 1;\n    \n    while (front < rear) {\n        int currRow = queue[front][0];\n        int currCol = queue[front][1];\n        int currTime = queue[front][2];\n        front++;\n        \n        // If the current station is the destination, return the time\n        if (currRow == destRow && currCol == destCol) {\n            return currTime;\n        }\n        \n        // Explore all possible paths from the current station\n        for (int i = 0; i < rows; i++) {\n            for (int j = 0; j < cols; j++) {\n                // Check if the station is reachable and not visited\n                if (i == currRow && abs(j - currCol) == 1 && !visited[i][j]) {\n                    // Calculate the time to reach the station\n                    int time = currTime + (j > currCol ? 1 : -1);\n                    \n                    // Mark the station as visited and add it to the queue\n                    visited[i][j] = 1;\n                    queue[rear][0] = i;\n                    queue[rear][1] = j;\n                    queue[rear][2] = time;\n                    rear++;\n                }\n            }\n        }\n    }\n    \n    // If the destination is not reachable, return -1\n    return -1;\n}\n\nint main() {\n    int rows, cols;\n    scanf(\"%d %d\", &rows, &cols);\n    \n    // Read the train information\n    int trainType[rows][cols];\n    int trainTime[rows][cols];\n    for (int i = 0; i < rows; i++) {\n        char type;\n        scanf(\" %c %d\", &type, &trainTime[i][0]);\n        if (type == 'F') {\n            trainType[i][0] = 1;\n        } else if (type == 'O') {\n            trainType[i][0] = 2;\n        } else {\n            trainType[i][0] = 3;\n        }\n    }\n    for (int j = 0; j < cols; j++) {\n        char type;\n        scanf(\" %c %d\", &type, &trainTime[0][j]);\n        if (type == 'F') {\n            trainType[0][j] = 1;\n        } else if (type == 'O') {\n            trainType[0][j] = 2;\n        } else {\n            trainType[0][j] = 3;\n        }\n    }\n    \n    // Read the starting and destination stations\n    int startRow, startCol, startTime, destRow, destCol;\n    scanf(\"%d %d %d %d %d\", &startRow, &startCol, &startTime, &destRow, &destCol);\n    \n    // Calculate the minimum time to reach the destination\n    int minTime = bfs(rows, cols, startRow - 1, startCol - 1, startTime, destRow - 1, destCol - 1);\n    \n    // Print the result\n    printf(\"%d\\n\", minTime);\n    \n    return 0;\n}",
      "C++": "// COMPLETE, COMPILABLE C++ code\n// Uses correct I/O\n// Handles all T test cases\n#include <iostream>\n#include <queue>\n#include <vector>\nusing namespace std;\n\nconst int MAX_TIME = 1000;\nconst int MAX_ROWS = 50;\nconst int MAX_COLS = 50;\n\nstruct Station {\n    int row, col, time;\n};\n\nint bfs(int rows, int cols, int startRow, int startCol, int startTime, int destRow, int destCol) {\n    // Initialize the queue with the starting station\n    queue<Station> q;\n    q.push({startRow, startCol, startTime});\n    \n    // Create a visited matrix\n    vector<vector<bool>> visited(rows, vector<bool>(cols, false));\n    visited[startRow][startCol] = true;\n    \n    while (!q.empty()) {\n        Station curr = q.front();\n        q.pop();\n        \n        // If the current station is the destination, return the time\n        if (curr.row == destRow && curr.col == destCol) {\n            return curr.time;\n        }\n        \n        // Explore all possible paths from the current station\n        for (int i = 0; i < rows; i++) {\n            for (int j = 0; j < cols; j++) {\n                // Check if the station is reachable and not visited\n                if (i == curr.row && abs(j - curr.col) == 1 && !visited[i][j]) {\n                    // Calculate the time to reach the station\n                    int time = curr.time + (j > curr.col ? 1 : -1);\n                    \n                    // Mark the station as visited and add it to the queue\n                    visited[i][j] = true;\n                    q.push({i, j, time});\n                }\n            }\n        }\n    }\n    \n    // If the destination is not reachable, return -1\n    return -1;\n}\n\nint main() {\n    int rows, cols;\n    cin >> rows >> cols;\n    \n    // Read the train information\n    int trainType[rows][cols];\n    int trainTime[rows][cols];\n    for (int i = 0; i < rows; i++) {\n        char type;\n        cin >> type >> trainTime[i][0];\n        if (type == 'F') {\n            trainType[i][0] = 1;\n        } else if (type == 'O') {\n            trainType[i][0] = 2;\n        } else {\n            trainType[i][0] = 3;\n        }\n    }\n    for (int j = 0; j < cols; j++) {\n        char type;\n        cin >> type >> trainTime[0][j];\n        if (type == 'F') {\n            trainType[0][j] = 1;\n        } else if (type == 'O') {\n            trainType[0][j] = 2;\n        } else {\n            trainType[0][j] = 3;\n        }\n    }\n    \n    // Read the starting and destination stations\n    int startRow, startCol, startTime, destRow, destCol;\n    cin >> startRow >> startCol >> startTime >> destRow >> destCol;\n    \n    // Calculate the minimum time to reach the destination\n    int minTime = bfs(rows, cols, startRow - 1, startCol - 1, startTime, destRow - 1, destCol - 1);\n    \n    // Print the result\n    cout << minTime << endl;\n    \n    return 0;\n}",
      "Java": "// COMPLETE, COMPILABLE Java code\n// Class MUST be named Main\n// Uses correct I/O\n// Handles all T test cases\nimport java.util.LinkedList;\nimport java.util.Queue;\nimport java.util.Scanner;\n\npublic class Main {\n    static final int MAX_TIME = 1000;\n    static final int MAX_ROWS = 50;\n    static final int MAX_COLS = 50;\n    \n    static class Station {\n        int row, col, time;\n        \n        public Station(int row, int col, int time) {\n            this.row = row;\n            this.col = col;\n            this.time = time;\n        }\n    }\n    \n    public static int bfs(int rows, int cols, int startRow, int startCol, int startTime, int destRow, int destCol) {\n        // Initialize the queue with the starting station\n        Queue<Station> q = new LinkedList<>();\n        q.add(new Station(startRow, startCol, startTime));\n        \n        // Create a visited matrix\n        boolean[][] visited = new boolean[rows][cols];\n        visited[startRow][startCol] = true;\n        \n        while (!q.isEmpty()) {\n            Station curr = q.poll();\n            \n            // If the current station is the destination, return the time\n            if (curr.row == destRow && curr.col == destCol) {\n                return curr.time;\n            }\n            \n            // Explore all possible paths from the current station\n            for (int i = 0; i < rows; i++) {\n                for (int j = 0; j < cols; j++) {\n                    // Check if the station is reachable and not visited\n                    if (i == curr.row && Math.abs(j - curr.col) == 1 && !visited[i][j]) {\n                        // Calculate the time to reach the station\n                        int time = curr.time + (j > curr.col ? 1 : -1);\n                        \n                        // Mark the station as visited and add it to the queue\n                        visited[i][j] = true;\n                        q.add(new Station(i, j, time));\n                    }\n                }\n            }\n        }\n        \n        // If the destination is not reachable, return -1\n        return -1;\n    }\n    \n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int rows = sc.nextInt();\n        int cols = sc.nextInt();\n        \n        // Read the train information\n        int[][] trainType = new int[rows][cols];\n        int[][] trainTime = new int[rows][cols];\n        for (int i = 0; i < rows; i++) {\n            char type = sc.next().charAt(0);\n            trainTime[i][0] = sc.nextInt();\n            if (type == 'F') {\n                trainType[i][0] = 1;\n            } else if (type == 'O') {\n                trainType[i][0] = 2;\n            } else {\n                trainType[i][0] = 3;\n            }\n        }\n        for (int j = 0; j < cols; j++) {\n            char type = sc.next().charAt(0);\n            trainTime[0][j] = sc.nextInt();\n            if (type == 'F') {\n                trainType[0][j] = 1;\n            } else if (type == 'O') {\n                trainType[0][j] = 2;\n            } else {\n                trainType[0][j] = 3;\n            }\n        }\n        \n        // Read the starting and destination stations\n        int startRow = sc.nextInt();\n        int startCol = sc.nextInt();\n        int startTime = sc.nextInt();\n        int destRow = sc.nextInt();\n        int destCol = sc.nextInt();\n        \n        // Calculate the minimum time to reach the destination\n        int minTime = bfs(rows, cols, startRow - 1, startCol - 1, startTime, destRow - 1, destCol - 1);\n        \n        // Print the result\n        System.out.println(minTime);\n    }\n}",
      "Python3": "# COMPLETE, RUNNABLE Python3 code\n# Uses input() or sys.stdin\n# Handles all T test cases\nfrom collections import deque\n\nMAX_TIME = 1000\nMAX_ROWS = 50\nMAX_COLS = 50\n\nclass Station:\n    def __init__(self, row, col, time):\n        self.row = row\n        self.col = col\n        self.time = time\n\ndef bfs(rows, cols, startRow, startCol, startTime, destRow, destCol):\n    # Initialize the queue with the starting station\n    q = deque([Station(startRow, startCol, startTime)])\n    \n    # Create a visited matrix\n    visited = [[False for _ in range(cols)] for _ in range(rows)]\n    visited[startRow][startCol] = True\n    \n    while q:\n        curr = q.popleft()\n        \n        # If the current station is the destination, return the time\n        if curr.row == destRow and curr.col == destCol:\n            return curr.time\n        \n        # Explore all possible paths from the current station\n        for i in range(rows):\n            for j in range(cols):\n                # Check if the station is reachable and not visited\n                if i == curr.row and abs(j - curr.col) == 1 and not visited[i][j]:\n                    # Calculate the time to reach the station\n                    time = curr.time + (j > curr.col)\n                    \n                    # Mark the station as visited and add it to the queue\n                    visited[i][j] = True\n                    q.append(Station(i, j, time))\n    \n    # If the destination is not reachable, return -1\n    return -1\n\n# Read the input\nrows, cols = map(int, input().split())\n\n# Read the train information\ntrainType = [[0 for _ in range(cols)] for _ in range(rows)]\ntrainTime = [[0 for _ in range(cols)] for _ in range(rows)]\nfor i in range(rows):\n    type, time = input().split()\n    trainTime[i][0] = int(time)\n    if type == 'F':\n        trainType[i][0] = 1\n    elif type == 'O':\n        trainType[i][0] = 2\n    else:\n        trainType[i][0] = 3\nfor j in range(cols):\n    type, time = input().split()\n    trainTime[0][j] = int(time)\n    if type == 'F':\n        trainType[0][j] = 1\n    elif type == 'O':\n        trainType[0][j] = 2\n    else:\n        trainType[0][j] = 3\n\n# Read the starting and destination stations\nstartRow, startCol, startTime, destRow, destCol = map(int, input().split())\n\n# Calculate the minimum time to reach the destination\nminTime = bfs(rows, cols, startRow - 1, startCol - 1, startTime, destRow - 1, destCol - 1)\n\n# Print the result\nprint(minTime)",
      "JavaScript": ""
    }
  },
  "QBCategory": "CCD",
  "boilerPlates": {
    "C": "#include <stdio.h>\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "C++": "#include <iostream>\nusing namespace std;\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "Java": "import java.util.*;\nimport java.io.*;\n\npublic class Main {\n    public static void main(String[] args) throws IOException {\n        // your code goes here\n    }\n}",
    "Python3": "# your code goes here\n",
    "JavaScript": "// your code goes here\n"
  },
  "_internal": {
    "methodName": "",
    "params": []
  }
}