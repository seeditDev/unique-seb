{
  "questionId": "Q11832",
  "title": "Articulation point in graph",
  "slug": "articulationpointingraph",
  "version": 1,
  "createdAt": "2026-07-05",
  "updatedAt": "2026-07-05",
  "metadata": {
    "category": "Graph Theory",
    "difficulty": "Hard",
    "isPremium": true,
    "tags": [
      "Articulation Points",
      "DFS",
      "Graph Traversal"
    ],
    "companyTags": [],
    "topicTags": [],
    "QBCategory": "CCD"
  },
  "content": {
    "problemStatement": "Find all articulation points (cut vertices) in an undirected graph whose removal increases the number of connected components.",
    "constraints": [
      "1 <= n <= 10^5",
      "0 <= adj[i].size() <= n-1",
      "0 <= adj[i][j] <= n-1"
    ],
    "inputFormat": "The input consists of two parameters: n (the number of nodes in the graph) and adj (an adjacency list representing the graph).",
    "outputFormat": "The output is a list of articulation points in the graph. If no articulation points exist, the output is [-1].",
    "sampleTestCases": []
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "label": "min boundary",
        "input": {
          "n": 1,
          "adj": [
            []
          ]
        },
        "expectedOutput": [
          -1
        ],
        "weight": 2
      },
      {
        "id": "tc_2",
        "label": "max boundary",
        "input": {
          "n": 10,
          "adj": [
            [
              1,
              2
            ],
            [
              0,
              3
            ],
            [
              0,
              4
            ],
            [
              1
            ],
            [
              2
            ]
          ]
        },
        "expectedOutput": [
          0,
          1,
          2
        ],
        "weight": 2
      },
      {
        "id": "tc_3",
        "label": "uniform/edge cases",
        "input": {
          "n": 5,
          "adj": [
            [
              1,
              2
            ],
            [
              0,
              3
            ],
            [
              0,
              4
            ],
            [
              1
            ],
            [
              2
            ]
          ]
        },
        "expectedOutput": [
          0,
          1,
          2
        ],
        "weight": 2
      },
      {
        "id": "tc_4",
        "label": "tricky case / empty-like",
        "input": {
          "n": 5,
          "adj": [
            [],
            [
              2
            ],
            [
              1,
              3
            ],
            [
              2,
              4
            ],
            [
              3
            ]
          ]
        },
        "expectedOutput": [
          2,
          3
        ],
        "weight": 2
      },
      {
        "id": "tc_5",
        "label": "general mixed case",
        "input": {
          "n": 7,
          "adj": [
            [
              1,
              2
            ],
            [
              0,
              3,
              4
            ],
            [
              0,
              5
            ],
            [
              1
            ],
            [
              1,
              6
            ],
            [
              2
            ],
            [
              4
            ]
          ]
        },
        "expectedOutput": [
          1,
          2,
          4
        ],
        "weight": 2
      }
    ],
    "totalTestCases": 5,
    "totalWeight": 10
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      2,
      2,
      2,
      2,
      2
    ],
    "maxScore": 10
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "Depth-First Search (DFS) with low and tin values to identify articulation points.",
    "timeComplexity": "O(n + m)",
    "spaceComplexity": "O(n + m)",
    "code": {
      "C": "#include <stdio.h>\n#include <stdlib.h>\n\n// Define the structure for the graph\ntypedef struct {\n    int numVertices;\n    int** adj;\n} Graph;\n\n// Function to create a graph\nGraph* createGraph(int numVertices) {\n    Graph* graph = (Graph*) malloc(sizeof(Graph));\n    graph->numVertices = numVertices;\n    graph->adj = (int**) malloc(numVertices * sizeof(int*));\n    for (int i = 0; i < numVertices; i++) {\n        graph->adj[i] = (int*) malloc(numVertices * sizeof(int));\n        for (int j = 0; j < numVertices; j++) {\n            graph->adj[i][j] = 0;\n        }\n    }\n    return graph;\n}\n\n// Function to add an edge to the graph\nvoid addEdge(Graph* graph, int src, int dest) {\n    graph->adj[src][dest] = 1;\n    graph->adj[dest][src] = 1;\n}\n\n// Function to find articulation points\nvoid findArticulationPoints(Graph* graph) {\n    int* visited = (int*) malloc(graph->numVertices * sizeof(int));\n    int* tin = (int*) malloc(graph->numVertices * sizeof(int));\n    int* low = (int*) malloc(graph->numVertices * sizeof(int));\n    int* mark = (int*) malloc(graph->numVertices * sizeof(int));\n    int timer = 0;\n\n    for (int i = 0; i < graph->numVertices; i++) {\n        visited[i] = 0;\n        tin[i] = -1;\n        low[i] = -1;\n        mark[i] = 0;\n    }\n\n    for (int i = 0; i < graph->numVertices; i++) {\n        if (!visited[i]) {\n            dfs(graph, i, -1, visited, tin, low, mark, &timer);\n        }\n    }\n\n    for (int i = 0; i < graph->numVertices; i++) {\n        if (mark[i]) {\n            printf(\"%d \", i);\n        }\n    }\n    printf(\"\\n\");\n}\n\n// Function to perform DFS\nvoid dfs(Graph* graph, int node, int parent, int* visited, int* tin, int* low, int* mark, int* timer) {\n    visited[node] = 1;\n    tin[node] = *timer;\n    low[node] = *timer;\n    (*timer)++;\n    int child = 0;\n\n    for (int i = 0; i < graph->numVertices; i++) {\n        if (graph->adj[node][i]) {\n            if (i == parent) continue;\n            if (!visited[i]) {\n                dfs(graph, i, node, visited, tin, low, mark, timer);\n                low[node] = min(low[node], low[i]);\n                if (low[i] >= tin[node] && parent != -1) {\n                    mark[node] = 1;\n                }\n                child++;\n            } else if (visited[i]) {\n                low[node] = min(low[node], tin[i]);\n            }\n        }\n    }\n\n    if (parent == -1 && child > 1) {\n        mark[node] = 1;\n    }\n}\n\n// Function to find the minimum of two numbers\nint min(int a, int b) {\n    return (a < b) ? a : b;\n}\n\nint main() {\n    int numVertices = 5;\n    Graph* graph = createGraph(numVertices);\n    addEdge(graph, 0, 1);\n    addEdge(graph, 1, 2);\n    addEdge(graph, 2, 0);\n    addEdge(graph, 1, 3);\n    addEdge(graph, 1, 4);\n    findArticulationPoints(graph);\n    return 0;\n}",
      "C++": "#include <bits/stdc++.h>\nusing namespace std;\n\nclass Solution {\nprivate:\n    int timer = 1;\n\n    // DFS to find articulation points\n    void dfs(int node, int parent, vector<int> &vis, int tin[], int low[],\n             vector<int> &mark, vector<int> adj[]) {\n\n        vis[node] = 1;\n        tin[node] = low[node] = timer++; // set discovery and low time\n        int child = 0;\n\n        for (auto it : adj[node]) {\n            if (it == parent) continue; // skip the edge to parent\n\n            if (!vis[it]) {\n                dfs(it, node, vis, tin, low, mark, adj); // recursive DFS\n                low[node] = min(low[node], low[it]);     // update low time\n\n                // Check articulation condition (excluding root)\n                if (low[it] >= tin[node] && parent != -1) {\n                    mark[node] = 1;\n                }\n                child++;\n            }\n            else {\n                // back edge case\n                low[node] = min(low[node], tin[it]);\n            }\n        }\n\n        // If root node has more than one child\n        if (parent == -1 && child > 1) {\n            mark[node] = 1;\n        }\n    }\n\npublic:\n    vector<int> articulationPoints(int n, vector<int> adj[]) {\n        vector<int> vis(n, 0), mark(n, 0);\n        int tin[n], low[n];\n\n        for (int i = 0; i < n; i++) {\n            if (!vis[i]) {\n                dfs(i, -1, vis, tin, low, mark, adj);\n            }\n        }\n\n        vector<int> ans;\n        for (int i = 0; i < n; i++) {\n            if (mark[i]) ans.push_back(i);\n        }\n        return ans.empty() ? vector<int>{-1} : ans;\n    }\n};\n\nint main() {\n    int n = 5;\n    vector<vector<int>> edges = {\n        {0, 1}, {1, 4}, {2, 4}, {2, 3}, {3, 4}\n    };\n\n    vector<int> adj[n];\n    for (auto e : edges) {\n        adj[e[0]].push_back(e[1]);\n        adj[e[1]].push_back(e[0]);\n    }\n\n    Solution sol;\n    vector<int> res = sol.articulationPoints(n, adj);\n    for (int v : res) cout << v << \" \";\n    cout << endl;\n    return 0;\n}",
      "Java": "import java.util.*;\n\nclass Solution {\n    private int timer = 1;\n\n    // DFS function to identify articulation points\n    private void dfs(int node, int parent, int[] vis, int[] tin, int[] low, int[] mark, List<List<Integer>> adj) {\n        vis[node] = 1; //Make visited node as 1\n        tin[node] = low[node] = timer++;\n        int child = 0;\n\n        for (int neighbor : adj.get(node)) {\n            if (neighbor == parent) continue;  //If neighbour==parent then continue\n\n            if (vis[neighbor] == 0) {\n                dfs(neighbor, node, vis, tin, low, mark, adj); //if vis[neighbour]==0 then call dfs function\n                low[node] = Math.min(low[node], low[neighbor]);\n\n                // Articulation condition\n                if (low[neighbor] >= tin[node] && parent != -1) {\n                    mark[node] = 1;\n                }\n\n                child++;//Increment child by 1\n            } else {\n                // back edge\n                low[node] = Math.min(low[node], tin[neighbor]);\n            }\n        }\n\n        if (parent == -1 && child > 1) {\n            mark[node] = 1;\n        }\n    }\n\n    public List<Integer> articulationPoints(int n, List<List<Integer>> adj) {\n        int[] vis = new int[n], tin = new int[n], low = new int[n], mark = new int[n];\n        List<Integer> ans = new ArrayList<>();\n\n        for (int i = 0; i < n; i++) {\n            if (vis[i] == 0) {\n                dfs(i, -1, vis, tin, low, mark, adj);\n            }\n        }\n\n        for (int i = 0; i < n; i++) {\n            if (mark[i] == 1) ans.add(i);\n        }\n\n        if (ans.isEmpty()) ans.add(-1);\n        return ans;  //if ans is empty add -1 else return ans\n    }\n\n    public static void main(String[] args) {\n        int n = 5;\n        int[][] edges = {\n            {0, 1}, {1, 4}, {2, 4}, {2, 3}, {3, 4}\n        };\n\n        List<List<Integer>> adj = new ArrayList<>();\n        for (int i = 0; i < n; i++) adj.add(new ArrayList<>());\n        for (int[] e : edges) {\n            adj.get(e[0]).add(e[1]);\n            adj.get(e[1]).add(e[0]);\n        }\n\n        Solution sol = new Solution();\n        List<Integer> res = sol.articulationPoints(n, adj);\n        for (int x : res) System.out.print(x + \" \");\n    }\n}",
      "Python3": "class Solution:\n    def __init__(self):\n        self.timer = 1\n\n    # DFS to find articulation points\n    def dfs(self, node, parent, vis, tin, low, mark, adj):\n        vis[node] = True  #make vis[node] as true\n        tin[node] = low[node] = self.timer\n        self.timer += 1\n        child = 0  #make child 0\n\n        for neighbor in adj[node]:\n            if neighbor == parent:\n                continue  #if neighbour==parent then continue;\n\n            if not vis[neighbor]:\n                self.dfs(neighbor, node, vis, tin, low, mark, adj) #else call the dfs function\n                low[node] = min(low[node], low[neighbor])\n\n                # Condition for articulation point (non-root)\n                if low[neighbor] >= tin[node] and parent != -1:\n                    mark[node] = 1\n\n                child += 1  #increment child by 1\n            else:\n                # back edge\n                low[node] = min(low[node], tin[neighbor])\n\n        if parent == -1 and child > 1:\n            mark[node] = 1\n\n    def articulationPoints(self, n, adj):\n        vis = [False] * n\n        tin = [0] * n\n        low = [0] * n\n        mark = [0] * n\n\n        for i in range(n):\n            if not vis[i]:\n                self.dfs(i, -1, vis, tin, low, mark, adj)\n\n        res = [i for i in range(n) if mark[i]]\n        return res if res else [-1]\n\n\nif __name__ == \"__main__\":\n    n = 5\n    edges = [\n        [0, 1], [1, 4], [2, 4], [2, 3], [3, 4]\n    ]\n\n    adj = [[] for _ in range(n)]\n    for u, v in edges:\n        adj[u].append(v)\n        adj[v].append(u)\n\n    sol = Solution()\n    res = sol.articulationPoints(n, adj)\n    print(\" \".join(map(str, res)))",
      "JavaScript": "class Solution {\n    constructor() {\n        this.timer = 1;\n    }\n\n    // DFS to find articulation points\n    dfs(node, parent, vis, tin, low, mark, adj) {\n        vis[node] = true;  //make vis[node]=true;\n        tin[node] = low[node] = this.timer++;\n        let child = 0;  //make child equal to 0\n\n        for (let neighbor of adj[node]) {\n            if (neighbor === parent) continue;  //if neighbour==parent then continue\n\n            if (!vis[neighbor]) {\n                this.dfs(neighbor, node, vis, tin, low, mark, adj);  //if not visited then call the dfs fucntion\n                low[node] = Math.min(low[node], low[neighbor]);\n\n                // Articulation condition (non-root)\n                if (low[neighbor] >= tin[node] && parent !== -1) {\n                    mark[node] = 1; //mark the node as 1\n                }\n\n                child++;\n            } else {\n                // back edge\n                low[node] = Math.min(low[node], tin[neighbor]);\n            }\n        }\n\n        if (parent === -1 && child > 1) {\n            mark[node] = 1;\n        }\n    }\n\n    articulationPoints(n, adj) {\n        const vis = new Array(n).fill(false);\n        const tin = new Array(n).fill(0);\n        const low = new Array(n).fill(0);\n        const mark = new Array(n).fill(0);\n\n        for (let i = 0; i < n; i++) {\n            if (!vis[i]) {\n                this.dfs(i, -1, vis, tin, low, mark, adj);\n            }\n        }\n\n        const res = [];\n        for (let i = 0; i < n; i++) {\n            if (mark[i] === 1) res.push(i);\n        }\n\n        return res.length ? res : [-1];\n    }\n}\n\n// Example usage\nconst n = 5;\nconst edges = [\n    [0, 1], [1, 4], [2, 4], [2, 3], [3, 4]\n];\n\nconst adj = Array.from({ length: n }, () => []);\nfor (const [u, v] of edges) {\n    adj[u].push(v);\n    adj[v].push(u);\n}\n\nconst sol = new Solution();\nconst res = sol.articulationPoints(n, adj);\nconsole.log(res.join(\" \"));"
    }
  },
  "QBCategory": "CCD",
  "boilerPlates": {
    "C": "#include <stdio.h>\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "C++": "#include <iostream>\nusing namespace std;\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "Java": "import java.util.*;\nimport java.io.*;\n\npublic class Main {\n    public static void main(String[] args) throws IOException {\n        // your code goes here\n    }\n}",
    "Python3": "# your code goes here\n",
    "JavaScript": "// your code goes here\n"
  },
  "_internal": {
    "methodName": "",
    "params": []
  }
}