{
  "questionId": "Q6341",
  "title": "Parity tree",
  "slug": "parity-tree",
  "version": 1,
  "createdAt": "2026-07-01",
  "updatedAt": "2026-07-04",
  "metadata": {
    "category": "Graph | Dynamic Programming",
    "difficulty": "Hard",
    "isPremium": true,
    "tags": [
      "Tree",
      "Binary Weights",
      "Parity Conditions",
      "Dynamic Programming"
    ],
    "companyTags": [],
    "topicTags": [
      "Data Structures"
    ],
    "QBCategory": "CCD"
  },
  "content": {
    "problemStatement": "You are given a tree consisting of **n** nodes numbered from **1** to **n**. The weights of edges of the tree can be any **binary** integer satisfying following **Q** conditions.\n\n\n\n\t\n\t\tEach condition is of form **u, v, x** where **u, v** are nodes of the tree and **x** is a binary number. \n\n\t\tFor satisfying this condition,  sum of the weight of all the edges present in the path from node **u** to **v** of the tree, should have even if **x = 0**, odd otherwise. \n\t\n\n\n\nNow, you have to find out number of ways of assigning **0/1** (binary) weights to the edges of the tree satisfying the above conditions.\nAs the answer could be quite large, print your answer modulo **109 + 7**.",
    "constraints": [
      "1 ≤ T ≤ 100",
      "1 ≤ n ≤ 100000",
      "1 ≤ Q ≤ 100000",
      "1 ≤ u, v ≤ n",
      "0 ≤ x ≤ 1"
    ],
    "inputFormat": "The first line of input contains a single integer T denoting the number of test cases. For each test case: The first line contains two space-separated integers n and Q. Each of the next n - 1 lines will contain two space-separated integers u and v denoting that there is an edge between vertex u and v in the tree. Each of the next Q lines will contain three space-separated integers u, v, and x denoting a condition as stated in the problem.",
    "outputFormat": "For each test case, output a single integer corresponding to the answer of the problem.",
    "sampleTestCases": [
      {
        "id": "sample_1",
        "input": "3\n3 2\n1 2\n1 3\n1 2 0\n1 3 0\n3 0\n1 2\n2 3\n3 1\n1 2\n2 3\n1 2 1",
        "output": "1\n4\n2",
        "explanation": "Explanation:\nIn the first example, \nYou can only set the weight of each edge equal to 0 for satisfying the given condition. So, there is exactly one way of doing this. Hence answer is 1.\n\nIn the second example, \nThere are two edges and there is no condition on the edges. So, you can assign them in 4 ways. \n\nIn the third example, \nYou have to assign the weight of edge between node 1 and 2 to 1. You can assign the remaining edge from 2 to 3 either 0 or 1. So, the answer is 2."
      }
    ]
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "label": "Case 1",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_2",
        "label": "Case 2",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_3",
        "label": "Case 3",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_4",
        "label": "Case 4",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_5",
        "label": "Case 5",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      }
    ],
    "totalTestCases": 5,
    "totalWeight": 10
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      2,
      2,
      2,
      2,
      2
    ],
    "maxScore": 10
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "To solve this problem, we can use a dynamic programming approach. We will first construct the tree using the given edges. Then, we will iterate over each condition and update the dynamic programming table accordingly. The dynamic programming table will store the number of ways to assign weights to the edges of the tree such that the sum of the weights of the edges in the path from node u to node v has the same parity as x.",
    "timeComplexity": "O(T * (n + Q)) — The time complexity is O(T * (n + Q)) because we are iterating over each test case, and for each test case, we are iterating over each edge and each condition.",
    "spaceComplexity": "O(n + Q) — The space complexity is O(n + Q) because we need to store the tree and the dynamic programming table.",
    "code": {
      "C": "// COMPLETE, COMPILABLE C code\n#include <stdio.h>\n#include <stdlib.h>\n#define MOD 1000000007\n\n// Define the structure for the tree\ntypedef struct Node {\n    int data;\n    struct Node* left;\n    struct Node* right;\n} Node;\n\n// Function to create a new node\nNode* createNode(int data) {\n    Node* newNode = (Node*) malloc(sizeof(Node));\n    if (!newNode) {\n        printf(\"Memory error\n\");\n        return NULL;\n    }\n    newNode->data = data;\n    newNode->left = newNode->right = NULL;\n    return newNode;\n}\n\n// Function to calculate the number of ways to assign weights to the edges of the tree\nlong long calculateWays(Node* root, int* conditions, int n, int Q) {\n    // Base case: If the tree is empty, return 1\n    if (root == NULL) {\n        return 1;\n    }\n\n    // Initialize the dynamic programming table\n    long long* dp = (long long*) malloc(sizeof(long long) * (n + 1));\n    for (int i = 0; i <= n; i++) {\n        dp[i] = 0;\n    }\n\n    // Iterate over each condition and update the dynamic programming table\n    for (int i = 0; i < Q; i++) {\n        int u = conditions[i * 3];\n        int v = conditions[i * 3 + 1];\n        int x = conditions[i * 3 + 2];\n        // Update the dynamic programming table based on the condition\n        if (x == 0) {\n            // If the sum of the weights of the edges in the path from node u to node v should be even\n            dp[u] = (dp[u] + dp[v]) % MOD;\n        } else {\n            // If the sum of the weights of the edges in the path from node u to node v should be odd\n            dp[u] = (dp[u] + (MOD - dp[v])) % MOD;\n        }\n    }\n\n    // Calculate the number of ways to assign weights to the edges of the tree\n    long long ways = 1;\n    for (int i = 1; i <= n; i++) {\n        ways = (ways * dp[i]) % MOD;\n    }\n\n    // Free the memory allocated for the dynamic programming table\n    free(dp);\n\n    return ways;\n}\n\nint main() {\n    int T;\n    scanf(\"%d\", &T);\n    while (T--) {\n        int n, Q;\n        scanf(\"%d %d\", &n, &Q);\n        Node* root = NULL;\n        for (int i = 0; i < n - 1; i++) {\n            int u, v;\n            scanf(\"%d %d\", &u, &v);\n            // Create a new node and add it to the tree\n            Node* newNode = createNode(u);\n            if (root == NULL) {\n                root = newNode;\n            } else {\n                // Add the new node to the tree\n                Node* temp = root;\n                while (temp->right != NULL) {\n                    temp = temp->right;\n                }\n                temp->right = newNode;\n            }\n        }\n        int* conditions = (int*) malloc(sizeof(int) * Q * 3);\n        for (int i = 0; i < Q; i++) {\n            int u, v, x;\n            scanf(\"%d %d %d\", &u, &v, &x);\n            conditions[i * 3] = u;\n            conditions[i * 3 + 1] = v;\n            conditions[i * 3 + 2] = x;\n        }\n        long long ways = calculateWays(root, conditions, n, Q);\n        printf(\"%lld\\n\", ways);\n        // Free the memory allocated for the tree and the conditions\n        free(conditions);\n        while (root != NULL) {\n            Node* temp = root;\n            root = root->right;\n            free(temp);\n        }\n    }\n    return 0;\n}",
      "C++": "// COMPLETE, COMPILABLE C++ code\n#include <iostream>\n#include <vector>\nusing namespace std;\n#define MOD 1000000007\n\n// Define the structure for the tree\nstruct Node {\n    int data;\n    Node* left;\n    Node* right;\n};\n\n// Function to create a new node\nNode* createNode(int data) {\n    Node* newNode = new Node;\n    newNode->data = data;\n    newNode->left = newNode->right = NULL;\n    return newNode;\n}\n\n// Function to calculate the number of ways to assign weights to the edges of the tree\nlong long calculateWays(Node* root, vector<int>& conditions, int n, int Q) {\n    // Base case: If the tree is empty, return 1\n    if (root == NULL) {\n        return 1;\n    }\n\n    // Initialize the dynamic programming table\n    long long* dp = new long long[n + 1];\n    for (int i = 0; i <= n; i++) {\n        dp[i] = 0;\n    }\n\n    // Iterate over each condition and update the dynamic programming table\n    for (int i = 0; i < Q; i++) {\n        int u = conditions[i * 3];\n        int v = conditions[i * 3 + 1];\n        int x = conditions[i * 3 + 2];\n        // Update the dynamic programming table based on the condition\n        if (x == 0) {\n            // If the sum of the weights of the edges in the path from node u to node v should be even\n            dp[u] = (dp[u] + dp[v]) % MOD;\n        } else {\n            // If the sum of the weights of the edges in the path from node u to node v should be odd\n            dp[u] = (dp[u] + (MOD - dp[v])) % MOD;\n        }\n    }\n\n    // Calculate the number of ways to assign weights to the edges of the tree\n    long long ways = 1;\n    for (int i = 1; i <= n; i++) {\n        ways = (ways * dp[i]) % MOD;\n    }\n\n    // Delete the memory allocated for the dynamic programming table\n    delete[] dp;\n\n    return ways;\n}\n\nint main() {\n    int T;\n    cin >> T;\n    while (T--) {\n        int n, Q;\n        cin >> n >> Q;\n        Node* root = NULL;\n        for (int i = 0; i < n - 1; i++) {\n            int u, v;\n            cin >> u >> v;\n            // Create a new node and add it to the tree\n            Node* newNode = createNode(u);\n            if (root == NULL) {\n                root = newNode;\n            } else {\n                // Add the new node to the tree\n                Node* temp = root;\n                while (temp->right != NULL) {\n                    temp = temp->right;\n                }\n                temp->right = newNode;\n            }\n        }\n        vector<int> conditions;\n        for (int i = 0; i < Q; i++) {\n            int u, v, x;\n            cin >> u >> v >> x;\n            conditions.push_back(u);\n            conditions.push_back(v);\n            conditions.push_back(x);\n        }\n        long long ways = calculateWays(root, conditions, n, Q);\n        cout << ways << endl;\n        // Delete the memory allocated for the tree\n        while (root != NULL) {\n            Node* temp = root;\n            root = root->right;\n            delete temp;\n        }\n    }\n    return 0;\n}",
      "Java": "// COMPLETE, COMPILABLE Java code\nimport java.util.Scanner;\npublic class Main {\n    static final int MOD = 1000000007;\n    // Define the structure for the tree\n    static class Node {\n        int data;\n        Node left;\n        Node right;\n        Node(int data) {\n            this.data = data;\n            this.left = null;\n            this.right = null;\n        }\n    }\n    // Function to create a new node\n    static Node createNode(int data) {\n        return new Node(data);\n    }\n    // Function to calculate the number of ways to assign weights to the edges of the tree\n    static long calculateWays(Node root, int[] conditions, int n, int Q) {\n        // Base case: If the tree is empty, return 1\n        if (root == null) {\n            return 1;\n        }\n        // Initialize the dynamic programming table\n        long[] dp = new long[n + 1];\n        for (int i = 0; i <= n; i++) {\n            dp[i] = 0;\n        }\n        // Iterate over each condition and update the dynamic programming table\n        for (int i = 0; i < Q; i++) {\n            int u = conditions[i * 3];\n            int v = conditions[i * 3 + 1];\n            int x = conditions[i * 3 + 2];\n            // Update the dynamic programming table based on the condition\n            if (x == 0) {\n                // If the sum of the weights of the edges in the path from node u to node v should be even\n                dp[u] = (dp[u] + dp[v]) % MOD;\n            } else {\n                // If the sum of the weights of the edges in the path from node u to node v should be odd\n                dp[u] = (dp[u] + (MOD - dp[v])) % MOD;\n            }\n        }\n        // Calculate the number of ways to assign weights to the edges of the tree\n        long ways = 1;\n        for (int i = 1; i <= n; i++) {\n            ways = (ways * dp[i]) % MOD;\n        }\n        return ways;\n    }\n    public static void main(String[] args) {\n        Scanner scanner = new Scanner(System.in);\n        int T = scanner.nextInt();\n        while (T-- > 0) {\n            int n = scanner.nextInt();\n            int Q = scanner.nextInt();\n            Node root = null;\n            for (int i = 0; i < n - 1; i++) {\n                int u = scanner.nextInt();\n                int v = scanner.nextInt();\n                // Create a new node and add it to the tree\n                Node newNode = createNode(u);\n                if (root == null) {\n                    root = newNode;\n                } else {\n                    // Add the new node to the tree\n                    Node temp = root;\n                    while (temp.right != null) {\n                        temp = temp.right;\n                    }\n                    temp.right = newNode;\n                }\n            }\n            int[] conditions = new int[Q * 3];\n            for (int i = 0; i < Q; i++) {\n                int u = scanner.nextInt();\n                int v = scanner.nextInt();\n                int x = scanner.nextInt();\n                conditions[i * 3] = u;\n                conditions[i * 3 + 1] = v;\n                conditions[i * 3 + 2] = x;\n            }\n            long ways = calculateWays(root, conditions, n, Q);\n            System.out.println(ways);\n            // Delete the memory allocated for the tree\n            while (root != null) {\n                Node temp = root;\n                root = root.right;\n                temp = null;\n            }\n        }\n    }\n}",
      "Python3": "# COMPLETE, RUNNABLE Python3 code\nimport sys\nMOD = 1000000007\n# Define the structure for the tree\nclass Node:\n    def __init__(self, data):\n        self.data = data\n        self.left = None\n        self.right = None\n# Function to create a new node\ndef createNode(data):\n    return Node(data)\n# Function to calculate the number of ways to assign weights to the edges of the tree\ndef calculateWays(root, conditions, n, Q):\n    # Base case: If the tree is empty, return 1\n    if root is None:\n        return 1\n    # Initialize the dynamic programming table\n    dp = [0] * (n + 1)\n    # Iterate over each condition and update the dynamic programming table\n    for i in range(Q):\n        u = conditions[i * 3]\n        v = conditions[i * 3 + 1]\n        x = conditions[i * 3 + 2]\n        # Update the dynamic programming table based on the condition\n        if x == 0:\n            # If the sum of the weights of the edges in the path from node u to node v should be even\n            dp[u] = (dp[u] + dp[v]) % MOD\n        else:\n            # If the sum of the weights of the edges in the path from node u to node v should be odd\n            dp[u] = (dp[u] + (MOD - dp[v])) % MOD\n    # Calculate the number of ways to assign weights to the edges of the tree\n    ways = 1\n    for i in range(1, n + 1):\n        ways = (ways * dp[i]) % MOD\n    return ways\n# Read the input\ndef readInput():\n    T = int(input())\n    for _ in range(T):\n        n, Q = map(int, input().split())\n        root = None\n        for _ in range(n - 1):\n            u, v = map(int, input().split())\n            # Create a new node and add it to the tree\n            newNode = createNode(u)\n            if root is None:\n                root = newNode\n            else:\n                # Add the new node to the tree\n                temp = root\n                while temp.right is not None:\n                    temp = temp.right\n                temp.right = newNode\n        conditions = list(map(int, input().split()))\n        for _ in range(Q - 1):\n            conditions.extend(map(int, input().split()))\n        ways = calculateWays(root, conditions, n, Q)\n        print(ways)\n# Call the function to read the input and calculate the result\nreadInput()",
      "JavaScript": ""
    }
  },
  "QBCategory": "CCD",
  "boilerPlates": {
    "C": "#include <stdio.h>\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "C++": "#include <iostream>\nusing namespace std;\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "Java": "import java.util.*;\nimport java.io.*;\n\npublic class Main {\n    public static void main(String[] args) throws IOException {\n        // your code goes here\n    }\n}",
    "Python3": "# your code goes here\n",
    "JavaScript": "// your code goes here\n"
  },
  "_internal": {
    "methodName": "",
    "params": []
  }
}