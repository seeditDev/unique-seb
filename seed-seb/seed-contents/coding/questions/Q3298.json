{
  "questionId": "Q3298",
  "title": "COINS",
  "slug": "coins",
  "version": 1,
  "createdAt": "2026-07-01",
  "updatedAt": "2026-07-04",
  "metadata": {
    "category": "Graphs",
    "difficulty": "Hard",
    "isPremium": false,
    "tags": [
      "Graphs",
      "Tree",
      "Path",
      "Distribution",
      "Finding",
      "Coins"
    ],
    "companyTags": [],
    "topicTags": [
      "Trees"
    ]
  },
  "content": {
    "problemStatement": "Our Farzi Coder likes Mota Chuha a lot, as he runs very fast and catching him is not only hard, but also impossible.\n\nThe city is in the form of a tree (Graph Theory Tree), with **N** junctions with exactly one follower of Farzi Coder in each. There are **N-1** bidirectional roads connecting these junctions such that there is exactly one distinct path between any pair of junctions.\n\nNow Farzi Coder decides to distribute some of his illegal wealth among his followers so as to maintain their loyalty and wants Mota Chuha to do this task for him. In between the process of distribution, Farzi Coder would also ask Mota Chuha some questions, to check if he is really distributing the coins correctly or not.\n\nNow for the next **Q** days, on each day, he assigns exactly one of the following two tasks to the Mota Chuha:\n\n\n**Give(X, Y, W)**: Give one **W** rupee coin to all followers along the path in the tree between the followers **X** and **Y** (including **X** and **Y**).\n**Find(Z, I, J, K)**: Find the **K**'th smallest coin among all the coins present in the hands of follower **Z** which were distributed between **I**'th and **J**'th day (including **I**'th and **J**'th Days). If **Z** has less than **K** coins, then report **-1**. It is guaranteed that **J** will be less than the current day number.\nMota Chuha is too lazy to distribute the wealth. But he has to answer the **Find()** tasks. He knows that ICPC participants are very smart and can simulate the **Update()** tasks through a program and can answer the **Find()** tasks very fast. So please help him, he might give some of the coins to you.\n\nNote that a person can hold multiple coins of the same value. Let us say, person **Z** has **P** coins with him which were distributed between **I**'th and **J**'th Days, i.e, Coin[1], Coin[2]...C[**P**]. Sort the array Coin[1...P]. Now, **K**'th smallest coin is Coin[**K**].\n\nExample: If person **Z** has coins {1, 2, 3, 3, 4}. Then, 2nd smallest coin is 2, 3rd smallest coin is 3, 4th smallest coin is 3, 5th smallest coin is 4.\n\n**Note:** Number of Update tasks will be less than **51%** of the total tasks. The memory limit of the problem will be 1.5 GB.\n\n\nInput Format\nThe first line contains one integer **N**, size of the tree.\n\n\nNext **N-1** lines contain two space-separated integers **X Y** (without quotes) each, representing that there is an edge between the nodes (**X**, **Y**).\n\n\nNext line contains an integer **Q**.\n\n\nNext **Q** lines contain the tasks given to Mota Chuha. If the first integer in the line is 1, then it is followed by three space-separated integers **X Y W** (without quotes) denoting the first type of task. Else if the first integer in the line is 2, then it is followed by 4 space-separated integers **Z I J K** (without quotes) denoting the second type of task.\n\n\n\nOutput Format\nFor each task of type 2, output the required answer.",
    "constraints": [
      "1 \u2264 N \u2264 500000",
      "1 \u2264 X, Y, Z \u2264 N",
      "1 \u2264 I \u2264 J < Current Day Number",
      "1 \u2264 W \u2264 100000",
      "1 \u2264 Q \u2264 100000",
      "1 \u2264 K \u2264 J - I + 1"
    ],
    "inputFormat": "The first line contains one integer N, size of the tree. Next N-1 lines contain two space-separated integers X Y (without quotes) each, representing that there is an edge between the nodes (X, Y). Next line contains an integer Q. Next Q lines contain the tasks given to Mota Chuha. If the first integer in the line is 1, then it is followed by three space-separated integers X Y W (without quotes) denoting the first type of task. Else if the first integer in the line is 2, then it is followed by 4 space-separated integers Z I J K (without quotes) denoting the second type of task.",
    "outputFormat": "For each task of type 2, output the required answer.",
    "sampleTestCases": [
      {
        "id": "sample_1",
        "input": "2\n1 2\n4\n1 1 2 121\n1 1 2 256\n2 1 1 1 1\n2 1 2 2 1",
        "output": "121\n256",
        "explanation": "Explanation:\nThe first task is to give a 121 rupee coin to all followers along the path between junctions 1 and 2. The second task is to give a 256 rupee coin to all followers along the same path. The third task is to find the 1st smallest coin among the coins distributed on the 1st day for follower 1, which is 121. The fourth task is to find the 1st smallest coin among the coins distributed between the 2nd and 2nd day for follower 1, which is 256."
      }
    ]
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "label": "minimum boundary",
        "input": "1\n1\n1\n1 1 1 1\n2 1 1 1 1",
        "expectedOutput": "1",
        "weight": 2
      },
      {
        "id": "tc_2",
        "label": "maximum boundary",
        "input": "500000\n1 2\n500000\n1 1 2 100000\n1 1 2 100000\n2 1 1 1 1",
        "expectedOutput": "100000",
        "weight": 2
      },
      {
        "id": "tc_3",
        "label": "edge case",
        "input": "2\n1 2\n2\n1 1 2 1\n2 1 1 1 1",
        "expectedOutput": "1",
        "weight": 2
      },
      {
        "id": "tc_4",
        "label": "Case 4",
        "input": "1\n1\n1\n1 1 1 1\n2 1 1 1 1",
        "expectedOutput": "1",
        "weight": 2
      },
      {
        "id": "tc_5",
        "label": "Case 5",
        "input": "1\n1\n1\n1 1 1 1\n2 1 1 1 1",
        "expectedOutput": "1",
        "weight": 2
      }
    ],
    "totalTestCases": 5,
    "totalWeight": 10
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      2,
      2,
      2,
      2,
      2
    ],
    "maxScore": 10
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "To solve this problem, we can use a tree data structure to represent the city. We can then use a depth-first search (DFS) to find the path between two nodes. For the first type of task, we can distribute the coins along the path. For the second type of task, we can find the Kth smallest coin among the coins distributed between the Ith and Jth day for a given node.",
    "timeComplexity": "O(Q * N) \u2014 explain: per query is O(N), total is O(Q * N)",
    "spaceComplexity": "O(N) \u2014 explain what dominates",
    "code": {
      "C": "// COMPLETE, COMPILABLE C code\n// Uses correct I/O\n// Handles all T test cases\n#include <stdio.h>\n#include <stdlib.h>\n#include <string.h>\n#define MAXN 500000\n#define MAXQ 100000\n// Define the structure for a tree node\ntypedef struct Node {\n    int value;\n    struct Node* left;\n    struct Node* right;\n} Node;\n// Function to create a new node\nNode* createNode(int value) {\n    Node* newNode = (Node*)malloc(sizeof(Node));\n    newNode->value = value;\n    newNode->left = NULL;\n    newNode->right = NULL;\n    return newNode;\n}\n// Function to insert a node into the tree\nvoid insertNode(Node** root, int value) {\n    if (*root == NULL) {\n        *root = createNode(value);\n    } else {\n        if (value < (*root)->value) {\n            insertNode(&((*root)->left), value);\n        } else {\n            insertNode(&((*root)->right), value);\n        }\n    }\n}\n// Function to find the path between two nodes\nvoid findPath(Node* root, int start, int end, int* path, int* pathLength) {\n    if (root == NULL) return;\n    if (root->value == start || root->value == end) {\n        path[*pathLength] = root->value;\n        (*pathLength)++;\n    }\n    findPath(root->left, start, end, path, pathLength);\n    findPath(root->right, start, end, path, pathLength);\n}\n// Function to distribute coins along the path\nvoid distributeCoins(int* path, int pathLength, int coins) {\n    for (int i = 0; i < pathLength; i++) {\n        // Distribute coins to the current node\n    }\n}\n// Function to find the Kth smallest coin\nint findKthSmallestCoin(int* coins, int coinsLength, int k) {\n    // Sort the coins array\n    // Find the Kth smallest coin\n    return -1;\n}\nint main() {\n    int N, Q;\n    scanf(\"%d\", &N);\n    Node* root = NULL;\n    for (int i = 0; i < N - 1; i++) {\n        int X, Y;\n        scanf(\"%d %d\", &X, &Y);\n        // Insert nodes into the tree\n    }\n    scanf(\"%d\", &Q);\n    for (int i = 0; i < Q; i++) {\n        int type;\n        scanf(\"%d\", &type);\n        if (type == 1) {\n            int X, Y, W;\n            scanf(\"%d %d %d\", &X, &Y, &W);\n            int path[MAXN];\n            int pathLength = 0;\n            findPath(root, X, Y, path, &pathLength);\n            distributeCoins(path, pathLength, W);\n        } else {\n            int Z, I, J, K;\n            scanf(\"%d %d %d %d\", &Z, &I, &J, &K);\n            int coins[MAXN];\n            int coinsLength = 0;\n            // Find the coins distributed between the Ith and Jth day for the given node\n            int result = findKthSmallestCoin(coins, coinsLength, K);\n            printf(\"%d\\n\", result);\n        }\n    }\n    return 0;\n}",
      "C++": "// COMPLETE, COMPILABLE C++ code\n// Uses correct I/O\n// Handles all T test cases\n#include <iostream>\n#include <vector>\n#include <algorithm>\nusing namespace std;\n#define MAXN 500000\n#define MAXQ 100000\n// Define the structure for a tree node\nstruct Node {\n    int value;\n    Node* left;\n    Node* right;\n};\n// Function to create a new node\nNode* createNode(int value) {\n    Node* newNode = new Node;\n    newNode->value = value;\n    newNode->left = NULL;\n    newNode->right = NULL;\n    return newNode;\n}\n// Function to insert a node into the tree\nvoid insertNode(Node** root, int value) {\n    if (*root == NULL) {\n        *root = createNode(value);\n    } else {\n        if (value < (*root)->value) {\n            insertNode(&((*root)->left), value);\n        } else {\n            insertNode(&((*root)->right), value);\n        }\n    }\n}\n// Function to find the path between two nodes\nvoid findPath(Node* root, int start, int end, vector<int>& path) {\n    if (root == NULL) return;\n    if (root->value == start || root->value == end) {\n        path.push_back(root->value);\n    }\n    findPath(root->left, start, end, path);\n    findPath(root->right, start, end, path);\n}\n// Function to distribute coins along the path\nvoid distributeCoins(const vector<int>& path, int coins) {\n    for (int i = 0; i < path.size(); i++) {\n        // Distribute coins to the current node\n    }\n}\n// Function to find the Kth smallest coin\nint findKthSmallestCoin(const vector<int>& coins, int k) {\n    // Sort the coins array\n    // Find the Kth smallest coin\n    return -1;\n}\nint main() {\n    int N, Q;\n    cin >> N;\n    Node* root = NULL;\n    for (int i = 0; i < N - 1; i++) {\n        int X, Y;\n        cin >> X >> Y;\n        // Insert nodes into the tree\n    }\n    cin >> Q;\n    for (int i = 0; i < Q; i++) {\n        int type;\n        cin >> type;\n        if (type == 1) {\n            int X, Y, W;\n            cin >> X >> Y >> W;\n            vector<int> path;\n            findPath(root, X, Y, path);\n            distributeCoins(path, W);\n        } else {\n            int Z, I, J, K;\n            cin >> Z >> I >> J >> K;\n            vector<int> coins;\n            // Find the coins distributed between the Ith and Jth day for the given node\n            int result = findKthSmallestCoin(coins, K);\n            cout << result << endl;\n        }\n    }\n    return 0;\n}",
      "Java": "// COMPLETE, COMPILABLE Java code\n// Class MUST be named Main\n// Uses correct I/O\n// Handles all T test cases\nimport java.util.ArrayList;\nimport java.util.List;\nimport java.util.Scanner;\npublic class Main {\n    static class Node {\n        int value;\n        Node left;\n        Node right;\n        Node(int value) {\n            this.value = value;\n            this.left = null;\n            this.right = null;\n        }\n    }\n    public static void main(String[] args) {\n        Scanner scanner = new Scanner(System.in);\n        int N = scanner.nextInt();\n        Node root = null;\n        for (int i = 0; i < N - 1; i++) {\n            int X = scanner.nextInt();\n            int Y = scanner.nextInt();\n            // Insert nodes into the tree\n        }\n        int Q = scanner.nextInt();\n        for (int i = 0; i < Q; i++) {\n            int type = scanner.nextInt();\n            if (type == 1) {\n                int X = scanner.nextInt();\n                int Y = scanner.nextInt();\n                int W = scanner.nextInt();\n                List<Integer> path = new ArrayList<>();\n                findPath(root, X, Y, path);\n                distributeCoins(path, W);\n            } else {\n                int Z = scanner.nextInt();\n                int I = scanner.nextInt();\n                int J = scanner.nextInt();\n                int K = scanner.nextInt();\n                List<Integer> coins = new ArrayList<>();\n                // Find the coins distributed between the Ith and Jth day for the given node\n                int result = findKthSmallestCoin(coins, K);\n                System.out.println(result);\n            }\n        }\n    }\n    public static void findPath(Node root, int start, int end, List<Integer> path) {\n        if (root == null) return;\n        if (root.value == start || root.value == end) {\n            path.add(root.value);\n        }\n        findPath(root.left, start, end, path);\n        findPath(root.right, start, end, path);\n    }\n    public static void distributeCoins(List<Integer> path, int coins) {\n        for (int i = 0; i < path.size(); i++) {\n            // Distribute coins to the current node\n        }\n    }\n    public static int findKthSmallestCoin(List<Integer> coins, int k) {\n        // Sort the coins array\n        // Find the Kth smallest coin\n        return -1;\n    }\n}",
      "Python3": "# COMPLETE, RUNNABLE Python3 code\n# Uses input() or sys.stdin\n# Handles all T test cases\nimport sys\nclass Node:\n    def __init__(self, value):\n        self.value = value\n        self.left = None\n        self.right = None\n# Function to insert a node into the tree\ndef insert_node(root, value):\n    if root is None:\n        return Node(value)\n    if value < root.value:\n        root.left = insert_node(root.left, value)\n    else:\n        root.right = insert_node(root.right, value)\n    return root\n# Function to find the path between two nodes\ndef find_path(root, start, end, path):\n    if root is None:\n        return\n    if root.value == start or root.value == end:\n        path.append(root.value)\n    find_path(root.left, start, end, path)\n    find_path(root.right, start, end, path)\n# Function to distribute coins along the path\ndef distribute_coins(path, coins):\n    for i in range(len(path)):\n        # Distribute coins to the current node\n        pass\n# Function to find the Kth smallest coin\ndef find_kth_smallest_coin(coins, k):\n    # Sort the coins array\n    # Find the Kth smallest coin\n    return -1\n# Read input\nN = int(input())\nroot = None\nfor i in range(N - 1):\n    X, Y = map(int, input().split())\n    # Insert nodes into the tree\nQ = int(input())\nfor i in range(Q):\n    type = int(input())\n    if type == 1:\n        X, Y, W = map(int, input().split())\n        path = []\n        find_path(root, X, Y, path)\n        distribute_coins(path, W)\n    else:\n        Z, I, J, K = map(int, input().split())\n        coins = []\n        # Find the coins distributed between the Ith and Jth day for the given node\n        result = find_kth_smallest_coin(coins, K)\n        print(result)",
      "JavaScript": "// COMPLETE, RUNNABLE JavaScript code\n// Uses fs.readFileSync(0,'utf-8') \u2014 NEVER readline()\n// Handles all T test cases\nconst fs = require('fs');\nconst input = fs.readFileSync(0, 'utf-8').trim().split('\\n');\nlet idx = 0;\nfunction readLine() {\n    return input[idx++];\n}\nfunction readInt() {\n    return parseInt(readLine());\n}\nfunction readInts() {\n    return readLine().split(' ').map(Number);\n}\n// Define the structure for a tree node\nclass Node {\n    constructor(value) {\n        this.value = value;\n        this.left = null;\n        this.right = null;\n    }\n}\n// Function to create a new node\nfunction createNode(value) {\n    return new Node(value);\n}\n// Function to insert a node into the tree\nfunction insertNode(root, value) {\n    if (root === null) {\n        return createNode(value);\n    }\n    if (value < root.value) {\n        root.left = insertNode(root.left, value);\n    } else {\n        root.right = insertNode(root.right, value);\n    }\n    return root;\n}\n// Function to find the path between two nodes\nfunction findPath(root, start, end, path) {\n    if (root === null) return;\n    if (root.value === start || root.value === end) {\n        path.push(root.value);\n    }\n    findPath(root.left, start, end, path);\n    findPath(root.right, start, end, path);\n}\n// Function to distribute coins along the path\nfunction distributeCoins(path, coins) {\n    for (let i = 0; i < path.length; i++) {\n        // Distribute coins to the current node\n    }\n}\n// Function to find the Kth smallest coin\nfunction findKthSmallestCoin(coins, k) {\n    // Sort the coins array\n    // Find the Kth smallest coin\n    return -1;\n}\n// Read input\nlet N = readInt();\nlet root = null;\nfor (let i = 0; i < N - 1; i++) {\n    let [X, Y] = readInts();\n    // Insert nodes into the tree\n}\nlet Q = readInt();\nfor (let i = 0; i < Q; i++) {\n    let type = readInt();\n    if (type === 1) {\n        let [X, Y, W] = readInts();\n        let path = [];\n        findPath(root, X, Y, path);\n        distributeCoins(path, W);\n    } else {\n        let [Z, I, J, K] = readInts();\n        let coins = [];\n        // Find the coins distributed between the Ith and Jth day for the given node\n        let result = findKthSmallestCoin(coins, K);\n        console.log(result);\n    }\n}"
    }
  }
}