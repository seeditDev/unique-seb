{
  "questionId": "Q5801",
  "title": "Chef and the Orders",
  "slug": "chef-and-the-orders",
  "version": 1,
  "createdAt": "2026-07-01",
  "updatedAt": "2026-07-04",
  "metadata": {
    "category": "Greedy",
    "difficulty": "Hard",
    "isPremium": true,
    "tags": [
      "Scheduling",
      "Deadline",
      "Penalty"
    ],
    "companyTags": [],
    "topicTags": [
      "Flow & Matching"
    ],
    "QBCategory": "CCD"
  },
  "content": {
    "problemStatement": "Chef gets **N** orders. The orders are numbered from **1** to **N**. He gets order **i** at **Si** time, and this order contains **Xi** number of items. Chef needs to serve each of these **Xi** items before **Di** time and for each unit of items he cannot cook before this deadline he needs to pay **Pi** unit of money as penalty. Given all of the orders, help the Chef to minimize the penalty he will have to pay. \n\n **Important Note**: Chef can cook at most one item at a unit time and for each item he needs exactly one unit of time to cook. Also Chef can serve an item instantly, when the item is cooked. If Chef wants to serve an item at time **t**, then the latest he can cook that item is at time time **t**. In another words, for order **i** Chef can cook the items at time units **Si**, **Si+1**, **Si+2**, ..., **Di-1**. Please note, that Chef can not cook items from order **i** exactly at time unit **Di**.",
    "constraints": [
      "1 <= T <= 50",
      "1 <= N <= 200",
      "1 <= Si <= 10^8",
      "1 <= Xi <= 10^8",
      "1 <= Di <= 10^8",
      "1 <= Pi <= 10^8",
      "Si + Xi <= Di"
    ],
    "inputFormat": "The first line contains an integer T, the number of test cases. For each test case, the first line contains an integer N, the number of orders. Then, N lines follow, each containing four integers: Si, Xi, Di, Pi, representing the start time, number of items, deadline, and penalty per item for each order.",
    "outputFormat": "For each test case, print the minimum total penalty that the chef will have to pay.",
    "sampleTestCases": [
      {
        "id": "sample_1",
        "input": "5\n1\n1 5 6 10\n2\n1 5 6 10\n1 5 6 10\n2\n1 5 6 1\n1 5 6 10\n2\n1 5 6 10\n6 5 11 10\n4\n5 8 15 20\n11 8 20 21\n16 8 25 22\n21 8 30 23",
        "output": "0\n50\n5\n0\n147",
        "explanation": "The first test case has only one order, and all items can be cooked before the deadline, so the penalty is 0. The second test case has two orders, and 5 items cannot be cooked before the deadline, so the penalty is 50. The third test case has two orders, and 5 items cannot be cooked before the deadline, but it is better not to serve these 5 items from the first order, so the penalty is 5. The fourth test case has two orders, and all items can be cooked before the deadline, so the penalty is 0. The fifth test case has four orders, and some items cannot be cooked before the deadline, so the penalty is 147."
      },
      {
        "id": "sample_2",
        "input": "1\n2\n1 5 6 10\n1 5 6 10",
        "output": "50",
        "explanation": "There are two orders, and 5 items cannot be cooked before the deadline, so the penalty is 50."
      },
      {
        "id": "sample_3",
        "input": "1\n1\n1 5 6 10",
        "output": "0",
        "explanation": "There is only one order, and all items can be cooked before the deadline, so the penalty is 0."
      }
    ]
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "label": "minimum boundary",
        "input": "1\n1\n1 1 2 1",
        "expectedOutput": "0",
        "weight": 2
      },
      {
        "id": "tc_2",
        "label": "maximum boundary",
        "input": "1\n1\n100000000 100000000 200000000 100000000",
        "expectedOutput": "0",
        "weight": 2
      },
      {
        "id": "tc_3",
        "label": "uniform/edge case",
        "input": "1\n2\n1 5 6 10\n1 5 6 10",
        "expectedOutput": "50",
        "weight": 2
      },
      {
        "id": "tc_4",
        "label": "tricky case",
        "input": "1\n2\n1 5 6 1\n1 5 6 10",
        "expectedOutput": "5",
        "weight": 2
      },
      {
        "id": "tc_5",
        "label": "stress/general",
        "input": "5\n1\n1 5 6 10\n2\n1 5 6 10\n1 5 6 10\n2\n1 5 6 1\n1 5 6 10\n2\n1 5 6 10\n6 5 11 10\n4\n5 8 15 20\n11 8 20 21\n16 8 25 22\n21 8 30 23",
        "expectedOutput": "0\n50\n5\n0\n147",
        "weight": 2
      }
    ],
    "totalTestCases": 5,
    "totalWeight": 10
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      2,
      2,
      2,
      2,
      2
    ],
    "maxScore": 10
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "The problem can be solved using a greedy approach. We sort the orders based on their deadlines and penalties. Then, we iterate over the sorted orders and try to cook as many items as possible before the deadline. If we cannot cook all items before the deadline, we calculate the penalty and add it to the total penalty.",
    "timeComplexity": "O(T * N log N) per test case, O(T * N log N) total. The sorting operation takes O(N log N) time, and we do this for each test case.",
    "spaceComplexity": "O(N) — we need to store the orders for each test case.",
    "code": {
      "C": "#include <stdio.h>\n#include <stdlib.h>\n#include <string.h>\n#include <math.h>\n#include <time.h>\n#include <stdint.h>\n\n#define MAX_ORDERS 200\n\n// Structure to represent an order\ntypedef struct {\n    int start_time;\n    int num_items;\n    int deadline;\n    int penalty;\n} Order;\n\n// Comparison function for sorting orders\nint compare_orders(const void *a, const void *b) {\n    Order *order1 = (Order *)a;\n    Order *order2 = (Order *)b;\n    if (order1->deadline < order2->deadline) return -1;\n    if (order1->deadline > order2->deadline) return 1;\n    if (order1->penalty < order2->penalty) return -1;\n    if (order1->penalty > order2->penalty) return 1;\n    return 0;\n}\n\nint main() {\n    int T;\n    scanf(\"%d\", &T);\n    while (T--) {\n        int N;\n        scanf(\"%d\", &N);\n        Order orders[N];\n        for (int i = 0; i < N; i++) {\n            scanf(\"%d %d %d %d\", &orders[i].start_time, &orders[i].num_items, &orders[i].deadline, &orders[i].penalty);\n        }\n        qsort(orders, N, sizeof(Order), compare_orders);\n        int current_time = 0;\n        int total_penalty = 0;\n        for (int i = 0; i < N; i++) {\n            int items_cooked = 0;\n            while (current_time < orders[i].deadline && items_cooked < orders[i].num_items) {\n                current_time++;\n                items_cooked++;\n            }\n            if (items_cooked < orders[i].num_items) {\n                total_penalty += (orders[i].num_items - items_cooked) * orders[i].penalty;\n            }\n        }\n        printf(\"%d\\n\", total_penalty);\n    }\n    return 0;\n}",
      "C++": "#include <iostream>\n#include <vector>\n#include <algorithm>\nusing namespace std;\n\nstruct Order {\n    int start_time;\n    int num_items;\n    int deadline;\n    int penalty;\n};\n\nbool compare_orders(const Order &a, const Order &b) {\n    if (a.deadline < b.deadline) return true;\n    if (a.deadline > b.deadline) return false;\n    if (a.penalty < b.penalty) return true;\n    if (a.penalty > b.penalty) return false;\n    return false;\n}\n\nint main() {\n    int T;\n    cin >> T;\n    while (T--) {\n        int N;\n        cin >> N;\n        vector<Order> orders(N);\n        for (int i = 0; i < N; i++) {\n            cin >> orders[i].start_time >> orders[i].num_items >> orders[i].deadline >> orders[i].penalty;\n        }\n        sort(orders.begin(), orders.end(), compare_orders);\n        int current_time = 0;\n        int total_penalty = 0;\n        for (int i = 0; i < N; i++) {\n            int items_cooked = 0;\n            while (current_time < orders[i].deadline && items_cooked < orders[i].num_items) {\n                current_time++;\n                items_cooked++;\n            }\n            if (items_cooked < orders[i].num_items) {\n                total_penalty += (orders[i].num_items - items_cooked) * orders[i].penalty;\n            }\n        }\n        cout << total_penalty << endl;\n    }\n    return 0;\n}",
      "Java": "import java.util.Arrays;\nimport java.util.Scanner;\n\npublic class Main {\n    static class Order implements Comparable<Order> {\n        int start_time;\n        int num_items;\n        int deadline;\n        int penalty;\n\n        public Order(int start_time, int num_items, int deadline, int penalty) {\n            this.start_time = start_time;\n            this.num_items = num_items;\n            this.deadline = deadline;\n            this.penalty = penalty;\n        }\n\n        @Override\n        public int compareTo(Order o) {\n            if (this.deadline < o.deadline) return -1;\n            if (this.deadline > o.deadline) return 1;\n            if (this.penalty < o.penalty) return -1;\n            if (this.penalty > o.penalty) return 1;\n            return 0;\n        }\n    }\n\n    public static void main(String[] args) {\n        Scanner scanner = new Scanner(System.in);\n        int T = scanner.nextInt();\n        while (T-- > 0) {\n            int N = scanner.nextInt();\n            Order[] orders = new Order[N];\n            for (int i = 0; i < N; i++) {\n                orders[i] = new Order(scanner.nextInt(), scanner.nextInt(), scanner.nextInt(), scanner.nextInt());\n            }\n            Arrays.sort(orders);\n            int current_time = 0;\n            int total_penalty = 0;\n            for (int i = 0; i < N; i++) {\n                int items_cooked = 0;\n                while (current_time < orders[i].deadline && items_cooked < orders[i].num_items) {\n                    current_time++;\n                    items_cooked++;\n                }\n                if (items_cooked < orders[i].num_items) {\n                    total_penalty += (orders[i].num_items - items_cooked) * orders[i].penalty;\n                }\n            }\n            System.out.println(total_penalty);\n        }\n    }\n}",
      "Python3": "import sys\n\nclass Order:\n    def __init__(self, start_time, num_items, deadline, penalty):\n        self.start_time = start_time\n        self.num_items = num_items\n        self.deadline = deadline\n        self.penalty = penalty\n\n    def __lt__(self, other):\n        if self.deadline < other.deadline:\n            return True\n        if self.deadline > other.deadline:\n            return False\n        if self.penalty < other.penalty:\n            return True\n        if self.penalty > other.penalty:\n            return False\n        return False\n\nT = int(input())\nfor _ in range(T):\n    N = int(input())\n    orders = []\n    for _ in range(N):\n        start_time, num_items, deadline, penalty = map(int, input().split())\n        orders.append(Order(start_time, num_items, deadline, penalty))\n    orders.sort()\n    current_time = 0\n    total_penalty = 0\n    for order in orders:\n        items_cooked = 0\n        while current_time < order.deadline and items_cooked < order.num_items:\n            current_time += 1\n            items_cooked += 1\n        if items_cooked < order.num_items:\n            total_penalty += (order.num_items - items_cooked) * order.penalty\n    print(total_penalty)",
      "JavaScript": "const fs = require('fs');\nconst lines = fs.readFileSync(0, 'utf-8').trim().split('\\n');\nlet idx = 0;\n\nfunction nextLine() {\n    return lines[idx++];\n}\n\nfunction nextInt() {\n    return parseInt(nextLine());\n}\n\nfunction nextIntArray() {\n    return nextLine().split(' ').map(Number);\n}\n\nclass Order {\n    constructor(start_time, num_items, deadline, penalty) {\n        this.start_time = start_time;\n        this.num_items = num_items;\n        this.deadline = deadline;\n        this.penalty = penalty;\n    }\n}\n\nfunction compareOrders(a, b) {\n    if (a.deadline < b.deadline) return -1;\n    if (a.deadline > b.deadline) return 1;\n    if (a.penalty < b.penalty) return -1;\n    if (a.penalty > b.penalty) return 1;\n    return 0;\n}\n\nlet T = nextInt();\nwhile (T-- > 0) {\n    let N = nextInt();\n    let orders = [];\n    for (let i = 0; i < N; i++) {\n        let [start_time, num_items, deadline, penalty] = nextIntArray();\n        orders.push(new Order(start_time, num_items, deadline, penalty));\n    }\n    orders.sort(compareOrders);\n    let current_time = 0;\n    let total_penalty = 0;\n    for (let order of orders) {\n        let items_cooked = 0;\n        while (current_time < order.deadline && items_cooked < order.num_items) {\n            current_time++;\n            items_cooked++;\n        }\n        if (items_cooked < order.num_items) {\n            total_penalty += (order.num_items - items_cooked) * order.penalty;\n        }\n    }\n    console.log(total_penalty);\n}"
    }
  },
  "QBCategory": "CCD",
  "boilerPlates": {
    "C": "#include <stdio.h>\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "C++": "#include <iostream>\nusing namespace std;\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "Java": "import java.util.*;\nimport java.io.*;\n\npublic class Main {\n    public static void main(String[] args) throws IOException {\n        // your code goes here\n    }\n}",
    "Python3": "# your code goes here\n",
    "JavaScript": "// your code goes here\n"
  },
  "_internal": {
    "methodName": "",
    "params": []
  }
}