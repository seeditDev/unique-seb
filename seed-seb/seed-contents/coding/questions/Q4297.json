{
  "questionId": "Q4297",
  "title": "PostOrder Traversal",
  "slug": "postorder-traversal",
  "version": 1,
  "createdAt": "2026-07-01",
  "updatedAt": "2026-07-04",
  "metadata": {
    "category": "Recursion | Trees",
    "difficulty": "Easy",
    "isPremium": true,
    "tags": [
      "Binary Tree",
      "Post‑order Traversal",
      "DFS",
      "Recursive Traversal"
    ],
    "companyTags": [],
    "topicTags": [
      "Trees"
    ],
    "QBCategory": "CCD"
  },
  "content": {
    "problemStatement": "Post-order traversal involves visiting the left subtree first, then the right subtree, and finally the root node. This method is useful for deleting the tree and freeing up the space, as it ensures that child nodes are processed before their respective parent nodes.\n\n**Algorithm**:\n\n\nRecursively traverse the left subtree.\nRecursively traverse the right subtree.\nVisit the root node and print it.\nVideo Explanation\nFor example, check the postorder traversal of the following binary tree.\n\n\n\nTaskGiven a binary tree, print its nodes in postorder traversal.\n\n\nInput Format\n\nThe first line of the input contains a single integer NNN — the number of nodes in the binary tree.\nNext N−1N - 1N−1 lines contain three space separated characters pi,ci,Rip_i, c_i, R_ipi​,ci​,Ri​, describing the tree with edge informations - pip_ipi​ is an integer denoting the parent node of the iiith edge, cic_ici​ is child node, and the letter RiR_iRi​ denotes whether the child is left child or right child, it's value is `L` if cic_ici​ is left child of pip_ipi​, else `R`.\n\n\nOutput Format\nOutput on the single line, NNN space separated integers - its nodes in the postorder traversal of the given tree.",
    "constraints": [
      "1 <= T <= 10",
      "1 <= N <= 15",
      "1 <= pi, ci <= N",
      "Ri is either 'L' or 'R'",
      "The given edges always form a valid binary tree."
    ],
    "inputFormat": "The first line contains an integer T — the number of test cases. For each test case:\n- The first line contains an integer N — the number of nodes in the binary tree (nodes are numbered from 1 to N).\n- The next N‑1 lines each contain three space‑separated values: pi ci Ri, where pi is the parent node, ci is the child node, and Ri is either 'L' (ci is the left child of pi) or 'R' (ci is the right child of pi).",
    "outputFormat": "For each test case output a single line containing N space‑separated integers — the nodes of the tree in post‑order traversal.",
    "sampleTestCases": [
      {
        "id": "sample_1",
        "input": "1\n5\n1 2 L\n1 3 R\n3 4 L\n3 5 R",
        "output": "2 4 5 3 1",
        "explanation": "The tree is:\n      1\n     / \\\\\n    2   3\n       / \\\\\n      4   5\nPost‑order visits left subtree (2), then right subtree (4,5,3), then root (1)."
      },
      {
        "id": "sample_2",
        "input": "1\n1",
        "output": "1",
        "explanation": "A single node tree; post‑order is just that node."
      },
      {
        "id": "sample_3",
        "input": "1\n7\n1 2 L\n1 3 R\n2 4 L\n2 5 R\n3 6 L\n3 7 R",
        "output": "4 5 2 6 7 3 1",
        "explanation": "A perfect binary tree of height 2. Post‑order yields left subtree (4 5 2), then right subtree (6 7 3), then root (1)."
      }
    ]
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "label": "minimum boundary",
        "input": "1\n1",
        "expectedOutput": "1",
        "weight": 2
      },
      {
        "id": "tc_2",
        "label": "maximum boundary",
        "input": "1\n15\n1 2 L\n1 3 R\n2 4 L\n2 5 R\n3 6 L\n3 7 R\n4 8 L\n4 9 R\n5 10 L\n5 11 R\n6 12 L\n6 13 R\n7 14 L\n7 15 R",
        "expectedOutput": "8 9 4 10 11 5 2 12 13 6 14 15 7 3 1",
        "weight": 2
      },
      {
        "id": "tc_3",
        "label": "edge case – right‑only chain",
        "input": "1\n4\n1 2 R\n2 3 R\n3 4 R",
        "expectedOutput": "4 3 2 1",
        "weight": 2
      },
      {
        "id": "tc_4",
        "label": "general case 1",
        "input": "1\n6\n5 3 L\n5 6 R\n3 1 L\n3 4 R\n6 2 L",
        "expectedOutput": "1 4 3 2 6 5",
        "weight": 2
      },
      {
        "id": "tc_5",
        "label": "general case 2",
        "input": "1\n7\n4 2 L\n4 6 R\n2 1 L\n2 3 R\n6 5 L\n6 7 R",
        "expectedOutput": "1 3 2 5 7 6 4",
        "weight": 2
      }
    ],
    "totalTestCases": 5,
    "totalWeight": 10
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      2,
      2,
      2,
      2,
      2
    ],
    "maxScore": 10
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "For each test case we build two arrays left[1..N] and right[1..N] storing the left and right child of every node (0 if absent). While reading the edges we also compute indegree of each node; the node with indegree 0 is the root. Then we perform a standard recursive post‑order traversal: visit left subtree, then right subtree, then output the current node. The recursion depth never exceeds N (≤15), so it is safe. All operations are O(N) per test case.",
    "timeComplexity": "O(N) per test case, total O(T·N).",
    "spaceComplexity": "O(N) for the child arrays and recursion stack.",
    "code": {
      "C": "#include <stdio.h>\n#include <stdlib.h>\n\nint *leftChild, *rightChild, *indeg;\n\nvoid postOrder(int node) {\n    if (node == 0) return;\n    postOrder(leftChild[node]);\n    postOrder(rightChild[node]);\n    printf(\"%d \", node);\n}\n\nint main() {\n    int T; if (scanf(\"%d\", &T) != 1) return 0;\n    while (T--) {\n        int N; scanf(\"%d\", &N);\n        leftChild = (int*)calloc(N + 1, sizeof(int));\n        rightChild = (int*)calloc(N + 1, sizeof(int));\n        indeg = (int*)calloc(N + 1, sizeof(int));\n        for (int i = 0; i < N - 1; ++i) {\n            int p, c; char dir; scanf(\"%d %d %c\", &p, &c, &dir);\n            if (dir == 'L') leftChild[p] = c; else rightChild[p] = c;\n            indeg[c]++;\n        }\n        int root = 1;\n        for (int i = 1; i <= N; ++i) if (indeg[i] == 0) { root = i; break; }\n        postOrder(root);\n        printf(\"\\n\");\n        free(leftChild); free(rightChild); free(indeg);\n    }\n    return 0;\n}",
      "C++": "#include <bits/stdc++.h>\nusing namespace std;\n\nvector<int> leftChild, rightChild, indeg;\n\nvoid postOrder(int node) {\n    if (node == 0) return;\n    postOrder(leftChild[node]);\n    postOrder(rightChild[node]);\n    cout << node << ' ';\n}\n\nint main() {\n    ios::sync_with_stdio(false);\n    cin.tie(nullptr);\n    int T; if(!(cin >> T)) return 0;\n    while (T--) {\n        int N; cin >> N;\n        leftChild.assign(N + 1, 0);\n        rightChild.assign(N + 1, 0);\n        indeg.assign(N + 1, 0);\n        for (int i = 0; i < N - 1; ++i) {\n            int p, c; char dir; cin >> p >> c >> dir;\n            if (dir == 'L') leftChild[p] = c; else rightChild[p] = c;\n            indeg[c]++;\n        }\n        int root = 1;\n        for (int i = 1; i <= N; ++i) if (indeg[i] == 0) { root = i; break; }\n        postOrder(root);\n        cout << '\\n';\n    }\n    return 0;\n}",
      "Java": "import java.io.*;\nimport java.util.*;\n\npublic class Main {\n    static int[] left, right, indeg;\n    static StringBuilder sb = new StringBuilder();\n\n    static void postOrder(int node) {\n        if (node == 0) return;\n        postOrder(left[node]);\n        postOrder(right[node]);\n        sb.append(node).append(' ');\n    }\n\n    public static void main(String[] args) throws Exception {\n        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));\n        StringTokenizer st = new StringTokenizer(br.readLine());\n        int T = Integer.parseInt(st.nextToken());\n        while (T-- > 0) {\n            int N = Integer.parseInt(br.readLine().trim());\n            left = new int[N + 1];\n            right = new int[N + 1];\n            indeg = new int[N + 1];\n            for (int i = 0; i < N - 1; i++) {\n                st = new StringTokenizer(br.readLine());\n                int p = Integer.parseInt(st.nextToken());\n                int c = Integer.parseInt(st.nextToken());\n                char dir = st.nextToken().charAt(0);\n                if (dir == 'L') left[p] = c; else right[p] = c;\n                indeg[c]++;\n            }\n            int root = 1;\n            for (int i = 1; i <= N; i++) if (indeg[i] == 0) { root = i; break; }\n            postOrder(root);\n            sb.append('\\n');\n        }\n        System.out.print(sb.toString());\n    }\n}",
      "Python3": "import sys\n\ndef post_order(node, left, right, out):\n    if node == 0:\n        return\n    post_order(left[node], left, right, out)\n    post_order(right[node], left, right, out)\n    out.append(str(node))\n\ndef solve():\n    data = sys.stdin.read().strip().split()\n    if not data:\n        return\n    it = iter(data)\n    T = int(next(it))\n    out_lines = []\n    for _ in range(T):\n        N = int(next(it))\n        left = [0] * (N + 1)\n        right = [0] * (N + 1)\n        indeg = [0] * (N + 1)\n        for _ in range(N - 1):\n            p = int(next(it)); c = int(next(it)); dir = next(it)\n            if dir == 'L':\n                left[p] = c\n            else:\n                right[p] = c\n            indeg[c] += 1\n        root = 1\n        for i in range(1, N + 1):\n            if indeg[i] == 0:\n                root = i\n                break\n        res = []\n        post_order(root, left, right, res)\n        out_lines.append(' '.join(res))\n    sys.stdout.write('\\n'.join(out_lines))\n\nif __name__ == \"__main__\":\n    solve()",
      "JavaScript": "const fs = require('fs');\nconst data = fs.readFileSync(0, 'utf-8').trim().split(/\\s+/);\nlet idx = 0;\nif (data.length === 0) process.exit(0);\nconst T = parseInt(data[idx++]);\nlet outputs = [];\nfor (let t = 0; t < T; t++) {\n  const N = parseInt(data[idx++]);\n  const left = new Array(N + 1).fill(0);\n  const right = new Array(N + 1).fill(0);\n  const indeg = new Array(N + 1).fill(0);\n  for (let i = 0; i < N - 1; i++) {\n    const p = parseInt(data[idx++]);\n    const c = parseInt(data[idx++]);\n    const dir = data[idx++];\n    if (dir === 'L') left[p] = c; else right[p] = c;\n    indeg[c]++;\n  }\n  let root = 1;\n  for (let i = 1; i <= N; i++) if (indeg[i] === 0) { root = i; break; }\n  const result = [];\n  function postOrder(node) {\n    if (node === 0) return;\n    postOrder(left[node]);\n    postOrder(right[node]);\n    result.push(node);\n  }\n  postOrder(root);\n  outputs.push(result.join(' '));\n}\nconsole.log(outputs.join('\\n'));"
    }
  },
  "QBCategory": "CCD",
  "boilerPlates": {
    "C": "#include <stdio.h>\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "C++": "#include <iostream>\nusing namespace std;\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "Java": "import java.util.*;\nimport java.io.*;\n\npublic class Main {\n    public static void main(String[] args) throws IOException {\n        // your code goes here\n    }\n}",
    "Python3": "# your code goes here\n",
    "JavaScript": "// your code goes here\n"
  },
  "_internal": {
    "methodName": "",
    "params": []
  }
}