{
  "questionId": "Q8351",
  "title": "Maximizing Minimum Segment Sum",
  "slug": "maximizing-minimum-segment-sum",
  "version": 1,
  "createdAt": "2026-07-02",
  "updatedAt": "2026-07-04",
  "metadata": {
    "category": "Dynamic Programming | Greedy | Binary Search",
    "difficulty": "Hard",
    "isPremium": true,
    "tags": [
      "Binary Search",
      "Greedy",
      "Prefix Sum"
    ],
    "companyTags": [],
    "topicTags": [
      "Binary Search"
    ],
    "QBCategory": "CCD"
  },
  "content": {
    "problemStatement": "You are given a positive integer kkk and an array aaa consisting of nnn integers. You will partition the array into kkk non-empty segments such that each number belongs to exactly one segment.\n\nLet's denote sis_isi​ as the sum of all numbers belonging to iii-th segment after partitioning. Your goal is to maximize the value of min(s1,…,sk)min(s_1, \\ldots, s_k)min(s1​,…,sk​). Calculate and print this value.\n\n\nInput Format\n\nThe first line of the input contains a single integer TTT denoting the number of test cases. The description of TTT test cases follows.\nEach test case contains two lines of input.\nThe first line of each test case contains two space-separated integers nnn and kkk.\nThe second line of each test case contains nnn space-separated integers a1,…,ana_1, \\ldots, a_na1​,…,an​.\n\n\nOutput Format\nFor each test case, print a single line containing a single integer --- the largest value of min(s1,…,sk)min(s_1, \\ldots, s_k)min(s1​,…,sk​) you can obtain.",
    "constraints": [
      "1 ≤ T ≤ 5·10^4",
      "2 ≤ n ≤ 10^5",
      "1 ≤ k ≤ n",
      "−10^9 ≤ ai ≤ 10^9",
      "Sum of n over all test cases ≤ 10^5"
    ],
    "inputFormat": "The first line contains an integer T, the number of test cases. Each test case consists of two lines. The first line contains two integers n and k (1 ≤ k ≤ n). The second line contains n space‑separated integers a1,…,an (|ai| ≤ 10^9).",
    "outputFormat": "For each test case output a single line with one integer – the maximum possible value of min(s1,…,sk) after partitioning the array into exactly k non‑empty contiguous segments.",
    "sampleTestCases": [
      {
        "id": "sample_1",
        "input": "3\n5 2\n1 2 3 3 3\n8 5\n7 -6 1 -9 -4 -7 -6 7\n6 3\n-2 6 -2 -3 2 -3",
        "output": "6\n-7\n-2",
        "explanation": "Test case 1: split after the third element → sums 6 and 6, min = 6.\\nTest case 2: optimal partition gives minimum segment sum -7.\\nTest case 3: best we can do is min segment sum -2."
      }
    ]
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "label": "minimum boundary",
        "input": "1\n2 2\n-5 -3",
        "expectedOutput": "-5",
        "weight": 2
      },
      {
        "id": "tc_2",
        "label": "maximum boundary",
        "input": "1\\n5 1\\n1000000000 1000000000 1000000000 1000000000 1000000000",
        "expectedOutput": "5000000000",
        "weight": 2
      },
      {
        "id": "tc_3",
        "label": "edge case with zeros",
        "input": "1\\n4 3\\n0 0 0 0",
        "expectedOutput": "0",
        "weight": 2
      },
      {
        "id": "tc_4",
        "label": "Case 4",
        "input": "1\n2 2\n-5 -3",
        "expectedOutput": "-5",
        "weight": 2
      },
      {
        "id": "tc_5",
        "label": "Case 5",
        "input": "1\n2 2\n-5 -3",
        "expectedOutput": "-5",
        "weight": 2
      }
    ],
    "totalTestCases": 5,
    "totalWeight": 10
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      2,
      2,
      2,
      2,
      2
    ],
    "maxScore": 10
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "The answer is monotone: if we can obtain a partition where every segment sum is at least X, then we can also obtain a partition for any Y ≤ X. Therefore we binary‑search the answer. For a fixed candidate X we greedily scan the array, accumulating a running sum. Whenever the sum becomes ≥ X we close the current segment, reset the sum to 0 and increase the segment count. After the whole array we check whether we created at least k segments. If we can create ≥ k segments, we can always merge some of them (which only increases segment sums) to end with exactly k segments, so X is feasible. The binary search runs over the range [−10^14, 10^14] (the possible total sum limits) and uses the usual upper‑mid trick to keep the invariant that lo is feasible and hi is not. The final answer is lo.",
    "timeComplexity": "O(n · log M) per test case, where M ≈ 2·10^14 (≈ 48 iterations). The total over all test cases is O( (sum n) · log M ).",
    "spaceComplexity": "O(1) besides the input array.",
    "code": {
      "C": "#include <stdio.h>\n#include <stdlib.h>\n#include <stdbool.h>\n\nbool feasible(long long *a, int n, int k, long long x) {\n    long long cur = 0;\n    int cnt = 0;\n    for (int i = 0; i < n; ++i) {\n        cur += a[i];\n        if (cur >= x) {\n            ++cnt;\n            cur = 0;\n        }\n    }\n    return cnt >= k;\n}\n\nint main() {\n    int T; if (scanf(\"%d\", &T)!=1) return 0;\n    while (T--) {\n        int n, k; scanf(\"%d %d\", &n, &k);\n        long long *a = (long long*)malloc(n * sizeof(long long));\n        for (int i = 0; i < n; ++i) scanf(\"%lld\", &a[i]);\n        long long lo = -100000000000000LL, hi = 100000000000000LL;\n        while (lo < hi) {\n            long long mid = lo + (hi - lo + 1) / 2;\n            if (feasible(a, n, k, mid)) lo = mid; else hi = mid - 1;\n        }\n        printf(\"%lld\\n\", lo);\n        free(a);\n    }\n    return 0;\n}",
      "C++": "#include <bits/stdc++.h>\nusing namespace std;\n\nbool feasible(const vector<long long>& a, int k, long long x) {\n    long long cur = 0;\n    int cnt = 0;\n    for (long long v : a) {\n        cur += v;\n        if (cur >= x) {\n            ++cnt;\n            cur = 0;\n        }\n    }\n    return cnt >= k;\n}\n\nint main() {\n    ios::sync_with_stdio(false);\n    cin.tie(nullptr);\n    int T; if(!(cin>>T)) return 0;\n    while (T--) {\n        int n, k; cin >> n >> k;\n        vector<long long> a(n);\n        for (int i = 0; i < n; ++i) cin >> a[i];\n        long long lo = -100000000000000LL, hi = 100000000000000LL;\n        while (lo < hi) {\n            long long mid = lo + (hi - lo + 1) / 2;\n            if (feasible(a, k, mid)) lo = mid; else hi = mid - 1;\n        }\n        cout << lo << '\\n';\n    }\n    return 0;\n}",
      "Java": "import java.io.*;\nimport java.util.*;\n\npublic class Main {\n    static boolean feasible(long[] a, int k, long x) {\n        long cur = 0;\n        int cnt = 0;\n        for (long v : a) {\n            cur += v;\n            if (cur >= x) {\n                cnt++;\n                cur = 0;\n            }\n        }\n        return cnt >= k;\n    }\n    public static void main(String[] args) throws Exception {\n        FastScanner fs = new FastScanner(System.in);\n        int T = fs.nextInt();\n        StringBuilder sb = new StringBuilder();\n        while (T-- > 0) {\n            int n = fs.nextInt();\n            int k = fs.nextInt();\n            long[] a = new long[n];\n            for (int i = 0; i < n; ++i) a[i] = fs.nextLong();\n            long lo = -100000000000000L, hi = 100000000000000L;\n            while (lo < hi) {\n                long mid = lo + (hi - lo + 1) / 2;\n                if (feasible(a, k, mid)) lo = mid; else hi = mid - 1;\n            }\n            sb.append(lo).append('\\n');\n        }\n        System.out.print(sb.toString());\n    }\n    // Fast scanner for large input\n    static class FastScanner {\n        private final InputStream in;\n        private final byte[] buffer = new byte[1 << 16];\n        private int ptr = 0, len = 0;\n        FastScanner(InputStream in) { this.in = in; }\n        private int readByte() throws IOException {\n            if (ptr >= len) {\n                len = in.read(buffer);\n                ptr = 0;\n                if (len <= 0) return -1;\n            }\n            return buffer[ptr++];\n        }\n        long nextLong() throws IOException {\n            int c; do { c = readByte(); } while (c <= ' ');\n            boolean neg = false;\n            if (c == '-') { neg = true; c = readByte(); }\n            long val = 0;\n            while (c > ' ') {\n                val = val * 10 + (c - '0');\n                c = readByte();\n            }\n            return neg ? -val : val;\n        }\n        int nextInt() throws IOException { return (int) nextLong(); }\n    }\n}",
      "Python3": "import sys\n\ndef feasible(arr, k, x):\n    cur = 0\n    cnt = 0\n    for v in arr:\n        cur += v\n        if cur >= x:\n            cnt += 1\n            cur = 0\n    return cnt >= k\n\ndef solve():\n    data = sys.stdin.read().strip().split()\n    if not data:\n        return\n    it = iter(data)\n    T = int(next(it))\n    out_lines = []\n    for _ in range(T):\n        n = int(next(it)); k = int(next(it))\n        arr = [int(next(it)) for _ in range(n)]\n        lo, hi = -10**14, 10**14\n        while lo < hi:\n            mid = lo + (hi - lo + 1)//2\n            if feasible(arr, k, mid):\n                lo = mid\n            else:\n                hi = mid - 1\n        out_lines.append(str(lo))\n    sys.stdout.write('\\n'.join(out_lines))\n\nif __name__ == \"__main__\":\n    solve()\n",
      "JavaScript": "const fs = require('fs');\nconst input = fs.readFileSync(0, 'utf-8').trim().split(/\\s+/);\nlet idx = 0;\nfunction next(){return input[idx++];}\nif (input.length===0) process.exit(0);\nconst T = Number(next());\nlet out = [];\nfunction feasible(arr, k, x){\n  let cur = 0n, cnt = 0;\n  const X = BigInt(x);\n  for (let v of arr){\n    cur += v;\n    if (cur >= X){\n      cnt++;\n      cur = 0n;\n    }\n  }\n  return cnt >= k;\n}\nfor(let t=0;t<T;++t){\n  const n = Number(next());\n  const k = Number(next());\n  const arr = new Array(n);\n  for(let i=0;i<n;++i) arr[i] = BigInt(next());\n  let lo = -100000000000000n, hi = 100000000000000n;\n  while(lo < hi){\n    const mid = lo + (hi - lo + 1n)/2n;\n    if (feasible(arr, k, mid)) lo = mid; else hi = mid - 1n;\n  }\n  out.push(lo.toString());\n}\nconsole.log(out.join('\\n'));\n"
    }
  },
  "QBCategory": "CCD",
  "boilerPlates": {
    "C": "#include <stdio.h>\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "C++": "#include <iostream>\nusing namespace std;\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "Java": "import java.util.*;\nimport java.io.*;\n\npublic class Main {\n    public static void main(String[] args) throws IOException {\n        // your code goes here\n    }\n}",
    "Python3": "# your code goes here\n",
    "JavaScript": "// your code goes here\n"
  },
  "_internal": {
    "methodName": "",
    "params": []
  }
}