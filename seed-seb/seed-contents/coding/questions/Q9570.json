{
  "questionId": "Q9570",
  "title": "Pasha and Good Ones",
  "slug": "pasha-and-good-ones",
  "version": 1,
  "createdAt": "2026-07-02",
  "updatedAt": "2026-07-04",
  "metadata": {
    "category": "Strings | Math | Prefix Sum | Hash Map",
    "difficulty": "Hard",
    "isPremium": true,
    "tags": [
      "Frequency",
      "Prefix sums",
      "Hash map counting",
      "Difference of prefix values",
      "Alphabet size 26"
    ],
    "companyTags": [],
    "topicTags": [
      "Data Structures"
    ],
    "QBCategory": "CCD"
  },
  "content": {
    "problemStatement": "Pasha has a string AAA, of length NNN, and an integer KKK.\n\nConsider a string BBB of length MMM. A character ccc in string BBB is said to be good, if the difference between the total occurrences of ccc and the total remaining characters in BBB (other than ccc), is equal to KKK.\n\nFormally, if freq(c,&nbsp;B)\\texttt{freq(c, B)}freq(c,&nbsp;B) denotes the frequency of character ccc in string BBB of length MMM, then, the following condition should hold true for ccc to be good:\n\n\nfreq(c,&nbsp;B)−(M−freq(c,&nbsp;B))=2⋅\\texttt{freq(c, B)} - (M - \\texttt{freq(c, B)}) = 2\\cdotfreq(c,&nbsp;B)−(M−freq(c,&nbsp;B))=2⋅ freq(c,&nbsp;B)−M=K\\texttt{freq(c, B)} - M = Kfreq(c,&nbsp;B)−M=K\nPasha ordered you to find the **sum** of count of good characters across all substrings of string AAA.\n\nNote that a substring is obtained by deleting some (possibly zero) characters from the beginning of the string and some (possibly zero) characters from the end of the string.\n\n\nInput Format\n\nThe first line of input will contain a single integer TTT, denoting the number of test cases.\nEach test case consists of multiple lines of input.\n\nThe first line of each test case contains two space-separated integers NNN and KKK — the length of string AAA and an integer as defined in statement, respectively.\nThe second line contains a string AAA consisting of NNN&nbsp;lowercase english alphabets.\n\n\n\n\nOutput Format\nFor each test case, output the **sum** of count of good characters across all substrings of string AAA.",
    "constraints": [
      "1 ≤ T ≤ 100",
      "1 ≤ N ≤ 100000",
      "−1000 ≤ K ≤ 1000",
      "A contains only lowercase English letters",
      "The sum of N over all test cases does not exceed 200000"
    ],
    "inputFormat": "The first line contains an integer T, the number of test cases. Each test case consists of two lines. The first line contains two integers N and K — the length of the string and the required value K. The second line contains a string A of length N consisting of lowercase English letters.",
    "outputFormat": "For each test case output a single line containing the sum of the number of good characters over all substrings of A.",
    "sampleTestCases": [
      {
        "id": "sample_1",
        "input": "3\n4 0\nabcd\n4 -1\nabac\n3 1\naab",
        "output": "6\n4\n4",
        "explanation": "Explanation:\nTest case 111:  Consider all the substrings of AAA:\n\nFor substrings of length 111, for each character, the value of 2⋅2\\cdot2⋅ freq(c, B)−M=2⋅1−1=1≠K\\texttt{freq(c, B)} - M = 2\\cdot 1 - 1 = 1 \\neq Kfreq(c, B)−M=2⋅1−1=1=K.\nFor substrings of length 333, for each character, the value of 2⋅2\\cdot2⋅ freq(c, B)−M=2⋅1−3=−1≠K\\texttt{freq(c, B)} - M = 2\\cdot 1 - 3 = -1 \\neq Kfreq(c, B)−M=2⋅1−3=−1=K.\nFor substring of length 444, for each character, the value of 2⋅2\\cdot2⋅ freq(c, B)−M=2⋅1−4=−2≠K\\texttt{freq(c, B)} - M = 2\\cdot 1 - 4 = -2 \\neq Kfreq(c, B)−M=2⋅1−4=−2=K.\nIn the substring ababab, for both characters aaa and bbb, the frequency is 111 and 2⋅1−2=02\\cdot 1 - 2 = 02⋅1−2=0. The same satisfies for both characters of substring bcbcbc and substring cdcdcd.\n\nThus, each substring of length 222 has 222 good characters each. The sum of count of good characters is 2+2+2=62+2+2 = 62+2+2=6.\nTest case 222:  In this case, only the substrings of length 333 have good characters:\n\nConsider BBB as abaabaaba. Here bbb is a good character, as 2⋅2\\cdot2⋅ freq(b, B)−M=2⋅1−3=−1\\texttt{freq(b, B)} - M = 2\\cdot 1 - 3 = -1freq(b, B)−M=2⋅1−3=−1.\nConsider BBB as bacbacbac. Here, all characters are good characters since the frequency is 111 for all characters and 2⋅1−3=−12\\cdot 1 - 3 = -12⋅1−3=−1.\n\nThe sum of count of good characters is 1+3=41+3 = 41+3=4.\nTest case 333: In this case,\n\nAll the substrings of length 111 have good characters.\nConsider BBB as aabaabaab. Here aaa is a good character, as 2⋅2\\cdot2⋅ freq(a, B)−M=2⋅2−3=1\\texttt{freq(a, B)} - M = 2\\cdot 2 - 3 = 1freq(a, B)−M=2⋅2−3=1.\n\nThe sum of count of good characters is 3+1=43+1 = 43+1=4."
      }
    ]
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "label": "Case 1",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_2",
        "label": "Case 2",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_3",
        "label": "Case 3",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_4",
        "label": "Case 4",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_5",
        "label": "Case 5",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      }
    ],
    "totalTestCases": 5,
    "totalWeight": 10
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      2,
      2,
      2,
      2,
      2
    ],
    "maxScore": 10
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "For a fixed character c, let pref[i] be the number of occurrences of c in the prefix A[1‥i] (pref[0]=0). For a substring B = A[l‥r] (length M = r‑l+1) the frequency of c is f = pref[r]‑pref[l‑1]. The character is good iff 2·f‑M = K. Substituting f gives:\n2·(pref[r]‑pref[l‑1]) ‑ (r‑l+1) = K\n⇔ (2·pref[r]‑r) ‑ (2·pref[l‑1]‑(l‑1)) = K.\nDefine value[i] = 2·pref[i] ‑ i. Then the condition becomes value[r] ‑ value[l‑1] = K.\nThus for each character we need the number of pairs (p, q) with p < q such that value[q] ‑ value[p] = K. This can be counted in linear time using a hash map: iterate i from 0 to N, keep a map cnt of how many times each value has appeared so far. For the current value v = value[i], the number of valid previous positions is cnt[v‑K]; add this to the answer, then increment cnt[v].\nWe repeat the process for all 26 letters and sum the contributions. The overall complexity is O(26·N) time and O(N) extra space per test case (the map is cleared for each character).",
    "timeComplexity": "O(26 · N) per test case, i.e., O(N) because the alphabet size is constant.",
    "spaceComplexity": "O(N) for the hash map (at most N+1 distinct values).",
    "code": {
      "C": "#include <stdio.h>\n#include <string.h>\n#include <stdlib.h>\n#include <stdint.h>\n\ntypedef struct Node {\n    long long key;\n    long long val;\n    struct Node* next;\n} Node;\n\n#define MOD 1000003 // size of hash table, a prime > N\n\nstatic Node* table[MOD];\n\nstatic unsigned int hash_idx(long long x) {\n    unsigned long long y = (unsigned long long)(x ^ (x >> 32));\n    return (unsigned int)(y % MOD);\n}\n\nstatic long long get(Node* head, long long key) {\n    for (Node* p = head; p; p = p->next) if (p->key == key) return p->val;\n    return 0;\n}\n\nstatic void inc(Node** head, long long key) {\n    for (Node* p = *head; p; p = p->next) if (p->key == key) { p->val++; return; }\n    Node* nd = (Node*)malloc(sizeof(Node));\n    nd->key = key; nd->val = 1; nd->next = *head; *head = nd;\n}\n\nint main(){\n    int T; if(scanf(\"%d\", &T)!=1) return 0;\n    while(T--){\n        int N; long long K; scanf(\"%d %lld\", &N, &K);\n        char s[100005]; scanf(\"%s\", s);\n        long long total = 0;\n        for(char ch='a'; ch<='z'; ++ch){\n            // clear hash table\n            for(int i=0;i<MOD;++i){\n                Node* p = table[i];\n                while(p){ Node* nxt = p->next; free(p); p = nxt; }\n                table[i]=NULL;\n            }\n            long long pref = 0;\n            // i = 0 (empty prefix)\n            inc(table + hash_idx(0), 0); // value[0] = 0\n            for(int i=1;i<=N;++i){\n                if(s[i-1]==ch) pref++;\n                long long v = 2*pref - i;\n                long long need = v - K;\n                total += get(table[hash_idx(need)], need);\n                inc(table + hash_idx(v), v);\n            }\n        }\n        printf(\"%lld\\n\", total);\n    }\n    return 0;\n}\n",
      "C++": "#include <bits/stdc++.h>\nusing namespace std;\n\nint main(){\n    ios::sync_with_stdio(false);\n    cin.tie(nullptr);\n    int T; if(!(cin>>T)) return 0;\n    while(T--){\n        int N; long long K; cin>>N>>K;\n        string s; cin>>s;\n        long long answer = 0;\n        for(char ch='a'; ch<='z'; ++ch){\n            unordered_map<long long,long long> cnt;\n            cnt.reserve(N*2);\n            long long pref = 0;\n            cnt[0] = 1; // value[0]\n            for(int i=1;i<=N;++i){\n                if(s[i-1]==ch) ++pref;\n                long long v = 2*pref - i;\n                answer += cnt[v - K];\n                ++cnt[v];\n            }\n        }\n        cout<<answer<<\"\\n\";\n    }\n    return 0;\n}\n",
      "Java": "import java.io.*;\nimport java.util.*;\n\npublic class Main {\n    public static void main(String[] args) throws Exception {\n        FastScanner fs = new FastScanner(System.in);\n        int T = fs.nextInt();\n        StringBuilder out = new StringBuilder();\n        while (T-- > 0) {\n            int N = fs.nextInt();\n            long K = fs.nextLong();\n            char[] s = fs.next().toCharArray();\n            long ans = 0;\n            for (char ch = 'a'; ch <= 'z'; ++ch) {\n                Long2LongMap cnt = new Long2LongHashMap();\n                cnt.add(0L, 1L); // value[0]\n                long pref = 0;\n                for (int i = 1; i <= N; ++i) {\n                    if (s[i - 1] == ch) pref++;\n                    long v = 2L * pref - i;\n                    ans += cnt.get(v - K);\n                    cnt.add(v, 1L);\n                }\n            }\n            out.append(ans).append('\\n');\n        }\n        System.out.print(out.toString());\n    }\n\n    // Simple long->long hash map with open addressing (no deletions needed)\n    static class Long2LongHashMap {\n        private static final int INIT_CAP = 1 << 20; // enough for N<=1e5\n        private long[] keys;\n        private long[] vals;\n        private boolean[] used;\n        private int mask;\n        Long2LongHashMap() {\n            int cap = 1;\n            while (cap < INIT_CAP) cap <<= 1;\n            keys = new long[cap];\n            vals = new long[cap];\n            used = new boolean[cap];\n            mask = cap - 1;\n        }\n        private int idx(long k) {\n            long h = k ^ (k >>> 32);\n            return (int)h & mask;\n        }\n        long get(long k) {\n            int i = idx(k);\n            while (used[i]) {\n                if (keys[i] == k) return vals[i];\n                i = (i + 1) & mask;\n            }\n            return 0L;\n        }\n        void add(long k, long delta) {\n            int i = idx(k);\n            while (used[i]) {\n                if (keys[i] == k) { vals[i] += delta; return; }\n                i = (i + 1) & mask;\n            }\n            used[i] = true;\n            keys[i] = k;\n            vals[i] = delta;\n        }\n    }\n\n    // Fast scanner for Java\n    static class FastScanner {\n        private final InputStream in;\n        private final byte[] buffer = new byte[1 << 16];\n        private int ptr = 0, len = 0;\n        FastScanner(InputStream in) { this.in = in; }\n        private int readByte() throws IOException {\n            if (ptr >= len) {\n                len = in.read(buffer);\n                ptr = 0;\n                if (len <= 0) return -1;\n            }\n            return buffer[ptr++];\n        }\n        String next() throws IOException {\n            StringBuilder sb = new StringBuilder();\n            int c;\n            while ((c = readByte()) != -1 && c <= ' ') {}\n            while (c != -1 && c > ' ') {\n                sb.append((char)c);\n                c = readByte();\n            }\n            return sb.toString();\n        }\n        int nextInt() throws IOException { return Integer.parseInt(next()); }\n        long nextLong() throws IOException { return Long.parseLong(next()); }\n    }\n}\n",
      "Python3": "import sys\nfrom collections import defaultdict\n\ndef solve():\n    data = sys.stdin.read().strip().split()\n    if not data:\n        return\n    it = iter(data)\n    T = int(next(it))\n    out_lines = []\n    for _ in range(T):\n        N = int(next(it)); K = int(next(it))\n        s = next(it)\n        ans = 0\n        for ch in map(chr, range(ord('a'), ord('z')+1)):\n            cnt = defaultdict(int)\n            cnt[0] = 1  # value[0]\n            pref = 0\n            for i, ch_i in enumerate(s, 1):\n                if ch_i == ch:\n                    pref += 1\n                v = 2*pref - i\n                ans += cnt[v - K]\n                cnt[v] += 1\n        out_lines.append(str(ans))\n    sys.stdout.write('\\n'.join(out_lines))\n\nif __name__ == \"__main__\":\n    solve()\n",
      "JavaScript": "const fs = require('fs');\nconst input = fs.readFileSync(0, 'utf-8').trim().split(/\\s+/);\nlet idx = 0;\nconst T = Number(input[idx++]);\nlet outputs = [];\nfor (let t = 0; t < T; ++t) {\n  const N = Number(input[idx++]);\n  const K = Number(input[idx++]);\n  const s = input[idx++];\n  let ans = 0n;\n  for (let code = 97; code <= 122; ++code) { // 'a'..'z'\n    const ch = String.fromCharCode(code);\n    const cnt = new Map();\n    cnt.set(0, 1n); // value[0]\n    let pref = 0;\n    for (let i = 1; i <= N; ++i) {\n      if (s[i-1] === ch) pref++;\n      const v = 2*pref - i;\n      const need = v - K;\n      const prev = cnt.get(need) || 0n;\n      ans += prev;\n      cnt.set(v, (cnt.get(v) || 0n) + 1n);\n    }\n  }\n  outputs.push(ans.toString());\n}\nconsole.log(outputs.join('\\n'));\n"
    }
  },
  "QBCategory": "CCD",
  "boilerPlates": {
    "C": "#include <stdio.h>\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "C++": "#include <iostream>\nusing namespace std;\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "Java": "import java.util.*;\nimport java.io.*;\n\npublic class Main {\n    public static void main(String[] args) throws IOException {\n        // your code goes here\n    }\n}",
    "Python3": "# your code goes here\n",
    "JavaScript": "// your code goes here\n"
  },
  "_internal": {
    "methodName": "",
    "params": []
  }
}