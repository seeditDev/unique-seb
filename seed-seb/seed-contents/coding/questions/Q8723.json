{
  "questionId": "Q8723",
  "title": "Flatten a Linked List",
  "slug": "flatten-a-linked-list",
  "version": 1,
  "createdAt": "2026-07-02",
  "updatedAt": "2026-07-04",
  "metadata": {
    "category": "Data Structures",
    "difficulty": "Medium",
    "isPremium": true,
    "tags": [
      "Linked List",
      "Depth‑First Search",
      "Iterative Stack",
      "Flattening"
    ],
    "companyTags": [],
    "topicTags": [
      "Linked Lists"
    ],
    "QBCategory": "CCD"
  },
  "content": {
    "problemStatement": "Alice has a **strange singly-linked list**. A usual singly linked-list or a not-strange linked list consist of nodes pointing to exactly one node ( except for the the tail node which points to null ). However, in case of a **strange singly-linked list**, instead of pointing towards a single linked-list node, some nodes of the linked-list are pointing towards two nodes! Each node in the strange linked-list has two pointers named :\n\n\n**next**\n**child**\nThe “child” pointer of a node may again point towards a strange singly linked list.\n\nAlice wants to restructure this linked-list into a not-strange linked list. While doing so, she must follow the following rules.\n\nLet **cur** be a node in restructured linked-list, then\n\n\n**cur.next** must occur after cur in the restructured linked-list.\nAll the nodes ( if any ) which are part of **cur.child** linked list must occur after the node cur and before the node **cur.next** in the restructured linked list.\nLet **cur** be a node in the restructured linked-list, then **cur.child** must be “null”.",
    "constraints": [
      "1 <= T <= 100",
      "1 <= N <= 200000",
      "Sum of N over all test cases <= 300000",
      "Node values are the integers 1 … N and are all distinct."
    ],
    "inputFormat": "The first line contains an integer T, the number of test cases. For each test case:\n- The first line contains an integer N, the number of nodes (nodes are numbered uniquely from 1 to N).\n- The next N‑1 lines each contain three integers u, v and type.\n  * If type = 0, the next pointer of node u points to node v.\n  * If type = 1, the child pointer of node u points to node v.\nIt is guaranteed that each node has at most one next pointer and at most one child pointer, and the whole structure forms a single “strange” singly‑linked list as described.",
    "outputFormat": "For each test case output a single line containing the values of the nodes after flattening, in order, separated by a single space.",
    "sampleTestCases": [
      {
        "id": "sample_1",
        "input": "1\n1\n",
        "output": "1",
        "explanation": "Only one node exists, so the flattened list is just that node."
      },
      {
        "id": "sample_2",
        "input": "1\n9\n4 7 1\n1 2 0\n2 3 0\n6 9 0\n3 6 0\n2 4 1\n4 8 0\n6 5 1\n",
        "output": "1 2 4 7 8 3 6 5 9",
        "explanation": "The main list is 1→2→3→6→9. Node 2 has a child list 4→8, and node 1 has a child list 7. Node 6 has a child list 5. Flattening visits each node, then its child list, then continues with the next pointer, giving the order shown."
      }
    ]
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "label": "Case 1",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_2",
        "label": "Case 2",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_3",
        "label": "Case 3",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_4",
        "label": "Case 4",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_5",
        "label": "Case 5",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      }
    ],
    "totalTestCases": 5,
    "totalWeight": 10
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      2,
      2,
      2,
      2,
      2
    ],
    "maxScore": 10
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "The structure can be seen as a directed graph where each node has at most one outgoing 'next' edge and at most one outgoing 'child' edge. The required order is a depth‑first traversal that always visits the child list before the next node. The algorithm:\n1. Build two arrays next[1..N] and child[1..N] from the input. Also compute indegree for every node (incoming from next or child).\n2. The head of the whole list is the unique node with indegree 0.\n3. Perform an iterative DFS starting from the head. Use a stack. When a node is popped, append its value to the answer. Then push its next pointer (if any) **first**, and push its child pointer (if any) **second**. Because the stack is LIFO, the child subtree will be processed before the next node, exactly matching the required order.\n4. After the traversal the answer vector contains the flattened list.\nThe algorithm runs in O(N) time per test case and uses O(N) additional memory for the adjacency arrays and the stack.",
    "timeComplexity": "O(N) per test case, where N is the number of nodes in that test case.",
    "spaceComplexity": "O(N) for the next, child, indegree arrays and the explicit stack.",
    "code": {
      "C": "#include <stdio.h>\n#include <stdlib.h>\n\nint main() {\n    int T; if (scanf(\"%d\", &T)!=1) return 0;\n    while (T--) {\n        int N; scanf(\"%d\", &N);\n        int *next = (int*)calloc(N+1, sizeof(int));\n        int *child = (int*)calloc(N+1, sizeof(int));\n        int *indeg = (int*)calloc(N+1, sizeof(int));\n        for (int i=0;i<N-1;i++) {\n            int u,v,type; scanf(\"%d %d %d\", &u, &v, &type);\n            if (type==0) { next[u]=v; indeg[v]++; }\n            else { child[u]=v; indeg[v]++; }\n        }\n        int head=1; for (int i=1;i<=N;i++) if (indeg[i]==0) { head=i; break; }\n        // iterative DFS using a manual stack\n        int *stack = (int*)malloc((N+5)*sizeof(int));\n        int top=0; stack[top++] = head;\n        int first=1;\n        while (top) {\n            int u = stack[--top];\n            if (!first) printf(\" \");\n            printf(\"%d\", u);\n            first=0;\n            if (next[u]) stack[top++] = next[u];\n            if (child[u]) stack[top++] = child[u];\n        }\n        printf(\"\\n\");\n        free(next); free(child); free(indeg); free(stack);\n    }\n    return 0;\n}",
      "C++": "#include <bits/stdc++.h>\nusing namespace std;\nint main(){\n    ios::sync_with_stdio(false);\n    cin.tie(nullptr);\n    int T; if(!(cin>>T)) return 0;\n    while(T--){\n        int N; cin>>N;\n        vector<int> nxt(N+1,0), ch(N+1,0), indeg(N+1,0);\n        for(int i=0;i<N-1;i++){\n            int u,v,type; cin>>u>>v>>type;\n            if(type==0){ nxt[u]=v; indeg[v]++; }\n            else{ ch[u]=v; indeg[v]++; }\n        }\n        int head=1; for(int i=1;i<=N;i++) if(indeg[i]==0){ head=i; break; }\n        vector<int> st; st.reserve(N);\n        st.push_back(head);\n        bool first=true;\n        while(!st.empty()){\n            int u=st.back(); st.pop_back();\n            if(!first) cout<<' ';\n            cout<<u; first=false;\n            if(nxt[u]) st.push_back(nxt[u]);\n            if(ch[u]) st.push_back(ch[u]);\n        }\n        cout<<'\\n';\n    }\n    return 0;\n}",
      "Java": "import java.io.*;\nimport java.util.*;\n\npublic class Main {\n    public static void main(String[] args) throws Exception {\n        FastScanner fs = new FastScanner(System.in);\n        int T = fs.nextInt();\n        StringBuilder out = new StringBuilder();\n        while (T-- > 0) {\n            int N = fs.nextInt();\n            int[] nxt = new int[N + 1];\n            int[] child = new int[N + 1];\n            int[] indeg = new int[N + 1];\n            for (int i = 0; i < N - 1; i++) {\n                int u = fs.nextInt();\n                int v = fs.nextInt();\n                int type = fs.nextInt();\n                if (type == 0) { nxt[u] = v; indeg[v]++; }\n                else { child[u] = v; indeg[v]++; }\n            }\n            int head = 1;\n            for (int i = 1; i <= N; i++) if (indeg[i] == 0) { head = i; break; }\n            Deque<Integer> stack = new ArrayDeque<>();\n            stack.push(head);\n            boolean first = true;\n            while (!stack.isEmpty()) {\n                int u = stack.pop();\n                if (!first) out.append(' ');\n                out.append(u);\n                first = false;\n                if (nxt[u] != 0) stack.push(nxt[u]);\n                if (child[u] != 0) stack.push(child[u]);\n            }\n            out.append('\\n');\n        }\n        System.out.print(out.toString());\n    }\n\n    // Fast scanner for competitive programming\n    static class FastScanner {\n        private final InputStream in;\n        private final byte[] buffer = new byte[1 << 16];\n        private int ptr = 0, len = 0;\n        FastScanner(InputStream in) { this.in = in; }\n        private int readByte() throws IOException {\n            if (ptr >= len) {\n                len = in.read(buffer);\n                ptr = 0;\n                if (len <= 0) return -1;\n            }\n            return buffer[ptr++];\n        }\n        int nextInt() throws IOException {\n            int c, sign = 1, val = 0;\n            do { c = readByte(); } while (c <= ' ');\n            if (c == '-') { sign = -1; c = readByte(); }\n            while (c > ' ') {\n                val = val * 10 + (c - '0');\n                c = readByte();\n            }\n            return val * sign;\n        }\n    }\n}",
      "Python3": "import sys\n\ndef solve():\n    data = sys.stdin.read().strip().split()\n    if not data:\n        return\n    it = iter(data)\n    T = int(next(it))\n    out_lines = []\n    for _ in range(T):\n        N = int(next(it))\n        nxt = [0] * (N + 1)\n        child = [0] * (N + 1)\n        indeg = [0] * (N + 1)\n        for __ in range(N - 1):\n            u = int(next(it)); v = int(next(it)); typ = int(next(it))\n            if typ == 0:\n                nxt[u] = v\n                indeg[v] += 1\n            else:\n                child[u] = v\n                indeg[v] += 1\n        head = 1\n        for i in range(1, N + 1):\n            if indeg[i] == 0:\n                head = i\n                break\n        stack = [head]\n        res = []\n        while stack:\n            u = stack.pop()\n            res.append(str(u))\n            if nxt[u]:\n                stack.append(nxt[u])\n            if child[u]:\n                stack.append(child[u])\n        out_lines.append(' '.join(res))\n    sys.stdout.write('\\n'.join(out_lines))\n\nif __name__ == \"__main__\":\n    solve()",
      "JavaScript": "const fs = require('fs');\nconst input = fs.readFileSync(0, 'utf-8').trim().split(/\\s+/).map(Number);\nlet idx = 0;\nconst T = input[idx++] || 0;\nlet outputs = [];\nfor (let t = 0; t < T; t++) {\n  const N = input[idx++];\n  const nxt = new Array(N + 1).fill(0);\n  const child = new Array(N + 1).fill(0);\n  const indeg = new Array(N + 1).fill(0);\n  for (let i = 0; i < N - 1; i++) {\n    const u = input[idx++];\n    const v = input[idx++];\n    const type = input[idx++];\n    if (type === 0) { nxt[u] = v; indeg[v]++; }\n    else { child[u] = v; indeg[v]++; }\n  }\n  let head = 1;\n  for (let i = 1; i <= N; i++) if (indeg[i] === 0) { head = i; break; }\n  const stack = [head];\n  const res = [];\n  while (stack.length) {\n    const u = stack.pop();\n    res.push(u);\n    if (nxt[u]) stack.push(nxt[u]);\n    if (child[u]) stack.push(child[u]);\n  }\n  outputs.push(res.join(' '));\n}\nconsole.log(outputs.join('\\n'));\n"
    }
  },
  "QBCategory": "CCD",
  "boilerPlates": {
    "C": "#include <stdio.h>\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "C++": "#include <iostream>\nusing namespace std;\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "Java": "import java.util.*;\nimport java.io.*;\n\npublic class Main {\n    public static void main(String[] args) throws IOException {\n        // your code goes here\n    }\n}",
    "Python3": "# your code goes here\n",
    "JavaScript": "// your code goes here\n"
  },
  "_internal": {
    "methodName": "",
    "params": []
  }
}