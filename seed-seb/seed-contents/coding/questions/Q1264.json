{
  "questionId": "Q1264",
  "title": "Insertion and deletion together in a Heap",
  "slug": "insertion-and-deletion-together-in-a-heap",
  "version": 1,
  "createdAt": "2026-07-01",
  "updatedAt": "2026-07-05",
  "metadata": {
    "category": "Data Structures",
    "difficulty": "Easy",
    "isPremium": false,
    "tags": [
      "Multiset",
      "Insertion",
      "Heap",
      "Deletion",
      "Maximum",
      "Priority Queue",
      "Data Structures"
    ],
    "companyTags": [],
    "topicTags": [
      "Heaps & Priority Queues"
    ]
  },
  "content": {
    "problemStatement": "Write a program to perform the following operations:\n\n\nInitialize an empty array\nGiven an integer N, perform N queries over this array\nGiven a query \"+ x\", insert x in your integer array at the end.\nGiven a query \"-\", remove the (Single occurrence of)maximum element of the array.\nAfter each query print the maximum element left in the array.\nNote:\nFor the given test cases the array will never be empty.\n\nInput Format\n\nThe first line of each test case contains one integer N, the number of queries.\nThere will be N more lines, each line giving one query.\n\n\nOutput Format\n\nPrint the maximum element left in the array, after each query.",
    "constraints": [
      "1 <= T <= 10",
      "1 <= N <= 100000 per test case",
      "-10^9 <= x <= 10^9"
    ],
    "inputFormat": "First line contains an integer T, the number of test cases. For each test case, the first line contains an integer N, the number of queries. Each of the next N lines contains a query: either \"+ x\" to insert integer x at the end of the array, or \"-\" to delete one occurrence of the current maximum element. It is guaranteed that after every query the array is non\u2011empty.",
    "outputFormat": "After each query, output the maximum element present in the array on its own line.",
    "sampleTestCases": [
      {
        "id": "sample_1",
        "input": "1\n7\n+ 4\n+ 6\n+ 6\n+ 5\n-\n-\n-",
        "output": "4\n6\n6\n6\n6\n5\n4",
        "explanation": "The sequence of operations follows the statement. After each operation the current maximum is printed, yielding the shown output."
      },
      {
        "id": "sample_2",
        "input": "1\n6\n+ -5\n+ -2\n+ -2\n-\n-\n+ -1",
        "output": "-5\n-2\n-2\n-2\n-5\n-1",
        "explanation": "All numbers are negative. The heap always keeps the largest (least negative) value at the top."
      },
      {
        "id": "sample_3",
        "input": "1\n8\n+ 3\n+ 1\n-\n+ 4\n+ 2\n-\n-\n+ 5",
        "output": "3\n3\n1\n4\n4\n2\n1\n5",
        "explanation": "Mixed insertions and deletions demonstrate that after each deletion the next maximum is correctly reported."
      }
    ]
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "label": "Case 1",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_2",
        "label": "Case 2",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_3",
        "label": "Case 3",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_4",
        "label": "Case 4",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_5",
        "label": "Case 5",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      }
    ],
    "totalTestCases": 5,
    "totalWeight": 10
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      2,
      2,
      2,
      2,
      2
    ],
    "maxScore": 10
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "Maintain a max\u2011heap (priority queue) that stores all current elements. For a \"+ x\" query, push x into the heap. For a \"-\" query, pop the top element (the current maximum). After each operation, the element at the top of the heap is the required maximum and is printed. Because each heap operation costs O(log\u202fM) where M is the current size, the total complexity per test case is O(N\u202flog\u202fN). The heap size never exceeds N, so the memory usage is O(N).",
    "timeComplexity": "O(N log N) per test case, O(total N log N) over all test cases.",
    "spaceComplexity": "O(N) \u2013 the heap stores at most all inserted elements.",
    "code": {
      "C": "#include <stdio.h>\n#include <stdlib.h>\n\ntypedef struct {\n    int *data;\n    int size;\n    int capacity;\n} MaxHeap;\n\nMaxHeap* createHeap(int cap) {\n    MaxHeap *h = (MaxHeap*)malloc(sizeof(MaxHeap));\n    h->data = (int*)malloc(sizeof(int) * cap);\n    h->size = 0;\n    h->capacity = cap;\n    return h;\n}\n\nvoid swap(int *a, int *b) { int t=*a; *a=*b; *b=t; }\n\nvoid heapifyUp(MaxHeap *h, int idx) {\n    while (idx > 0) {\n        int p = (idx - 1) / 2;\n        if (h->data[idx] > h->data[p]) {\n            swap(&h->data[idx], &h->data[p]);\n            idx = p;\n        } else break;\n    }\n}\n\nvoid heapifyDown(MaxHeap *h, int idx) {\n    while (1) {\n        int l = 2*idx + 1, r = 2*idx + 2, largest = idx;\n        if (l < h->size && h->data[l] > h->data[largest]) largest = l;\n        if (r < h->size && h->data[r] > h->data[largest]) largest = r;\n        if (largest != idx) {\n            swap(&h->data[idx], &h->data[largest]);\n            idx = largest;\n        } else break;\n    }\n}\n\nvoid push(MaxHeap *h, int val) {\n    h->data[h->size++] = val;\n    heapifyUp(h, h->size-1);\n}\n\nvoid pop(MaxHeap *h) {\n    if (h->size == 0) return;\n    h->data[0] = h->data[--h->size];\n    heapifyDown(h, 0);\n}\n\nint top(MaxHeap *h) { return h->size ? h->data[0] : -1; }\n\nint main() {\n    int T; if (scanf(\"%d\", &T)!=1) return 0;\n    while (T--) {\n        int N; scanf(\"%d\", &N);\n        MaxHeap *heap = createHeap(N);\n        for (int i=0;i<N;i++) {\n            char op; scanf(\" %c\", &op);\n            if (op == '+') {\n                int x; scanf(\"%d\", &x);\n                push(heap, x);\n            } else {\n                pop(heap);\n            }\n            printf(\"%d\\n\", top(heap));\n        }\n        free(heap->data);\n        free(heap);\n    }\n    return 0;\n}",
      "C++": "#include <bits/stdc++.h>\nusing namespace std;\nint main(){\n    ios::sync_with_stdio(false);\n    cin.tie(nullptr);\n    int T; if(!(cin>>T)) return 0;\n    while(T--){\n        int N; cin>>N;\n        priority_queue<int> pq;\n        for(int i=0;i<N;++i){\n            char op; cin>>op;\n            if(op=='+'){\n                int x; cin>>x; pq.push(x);\n            }else{\n                pq.pop();\n            }\n            cout<<pq.top()<<'\\n';\n        }\n    }\n    return 0;\n}",
      "Java": "import java.io.*;\nimport java.util.*;\npublic class Main {\n    public static void main(String[] args) throws Exception {\n        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));\n        StringBuilder out = new StringBuilder();\n        int T = Integer.parseInt(br.readLine().trim());\n        while (T-- > 0) {\n            int N = Integer.parseInt(br.readLine().trim());\n            PriorityQueue<Integer> pq = new PriorityQueue<>(Collections.reverseOrder());\n            for (int i = 0; i < N; i++) {\n                String line = br.readLine().trim();\n                if (line.charAt(0) == '+') {\n                    int x = Integer.parseInt(line.substring(2).trim());\n                    pq.add(x);\n                } else {\n                    pq.poll();\n                }\n                out.append(pq.peek()).append('\\n');\n            }\n        }\n        System.out.print(out.toString());\n    }\n}",
      "Python3": "import sys, heapq\ndef solve():\n    data = sys.stdin.read().strip().splitlines()\n    if not data:\n        return\n    idx = 0\n    T = int(data[idx]); idx += 1\n    out = []\n    for _ in range(T):\n        N = int(data[idx]); idx += 1\n        heap = []  # max\u2011heap using negatives\n        for __ in range(N):\n            parts = data[idx].split(); idx += 1\n            if parts[0] == '+':\n                x = int(parts[1])\n                heapq.heappush(heap, -x)\n            else:\n                heapq.heappop(heap)\n            out.append(str(-heap[0]))\n    sys.stdout.write('\\n'.join(out))\nif __name__ == \"__main__\":\n    solve()",
      "JavaScript": "const fs = require('fs');\nconst lines = fs.readFileSync(0, 'utf-8').trim().split('\\n');\nlet idx = 0;\nconst T = parseInt(lines[idx++], 10);\nlet output = [];\nfor (let t = 0; t < T; ++t) {\n  const N = parseInt(lines[idx++], 10);\n  const heap = [];\n  const push = (val) => {\n    heap.push(-val);\n    let i = heap.length - 1;\n    while (i > 0) {\n      const p = (i - 1) >> 1;\n      if (heap[i] < heap[p]) {\n        [heap[i], heap[p]] = [heap[p], heap[i]];\n        i = p;\n      } else break;\n    }\n  };\n  const pop = () => {\n    if (heap.length === 0) return;\n    heap[0] = heap[heap.length - 1];\n    heap.pop();\n    let i = 0;\n    while (true) {\n      let l = i * 2 + 1, r = i * 2 + 2, smallest = i;\n      if (l < heap.length && heap[l] < heap[smallest]) smallest = l;\n      if (r < heap.length && heap[r] < heap[smallest]) smallest = r;\n      if (smallest !== i) {\n        [heap[i], heap[smallest]] = [heap[smallest], heap[i]];\n        i = smallest;\n      } else break;\n    }\n  };\n  const top = () => -heap[0];\n  for (let i = 0; i < N; ++i) {\n    const parts = lines[idx++].trim().split(' ');\n    if (parts[0] === '+') {\n      push(parseInt(parts[1], 10));\n    } else {\n      pop();\n    }\n    output.push(String(top()));\n  }\n}\nconsole.log(output.join('\\n'));"
    }
  }
}