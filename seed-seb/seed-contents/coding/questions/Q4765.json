{
  "questionId": "Q4765",
  "title": "Chef Solves LRU Cache",
  "slug": "chef-solves-lru-cache",
  "version": 1,
  "createdAt": "2026-07-01",
  "updatedAt": "2026-07-04",
  "metadata": {
    "category": "Data Structures",
    "difficulty": "Hard",
    "isPremium": true,
    "tags": [
      "LRU Cache",
      "Hash Map",
      "Doubly Linked List"
    ],
    "companyTags": [],
    "topicTags": [
      "Hashing"
    ],
    "QBCategory": "CCD"
  },
  "content": {
    "problemStatement": "Chef wants to design a special data structure called **leastRecentlyUsedCacheleastRecentlyUsedCacheleastRecentlyUsedCache** that stores key-value pairs with a limited capacity.\n\nChef needs this cache to support the following operations efficiently:\n\n\n**getValuegetValuegetValue**: Retrieve the value of a given key.\n**putValueputValueputValue**: Insert or update a key-value pair.\nIf the cache reaches its maximum capacity and Chef inserts a new key, the **least recently used** key must be removed to make space.\n\nChef wants both **getValuegetValuegetValue** and **putValueputValueputValue** to run in constant average time complexity **O(1)O(1)O(1)**.\n\nYour task is to help Chef implement this cache system.\n\nFunction Declaration\n\n**Function Name:**\n\n\n\n**createCachecreateCachecreateCache**\n\n\n\n**Parameters:**\n\n\n\n**cacheCapacitycacheCapacitycacheCapacity** (`int`)\nThe maximum number of key-value pairs the cache can store. It is guaranteed to be a positive integer.\n\n\n\n**Return Value:**\n\n\n\nReturns a pointer/reference to the created **leastRecentlyUsedCacheleastRecentlyUsedCacheleastRecentlyUsedCache** instance.\nThe returned object supports further operations **getValuegetValuegetValue** and **putValueputValueputValue**.\n\n\nInput FormatThe first line contains a single integer **TTT** — the number of test cases.\n\nEach test case consists of operations on the cache:\n\n\n\nThe first line contains an integer **cacheCapacitycacheCapacitycacheCapacity** — the capacity of the cache.\n\n\n\n\nThe following lines contain operations, where each line starts with:\n\n\n\n**`putValue`** followed by two integers **lookupKeylookupKeylookupKey** and **storedValuestoredValuestoredValue**\n**`getValue`** followed by one integer **lookupKeylookupKeylookupKey**\n\n\nOutput FormatFor each **`getValue`** operation, print a single integer:\n\n\nPrint the **storedValuestoredValuestoredValue** associated with **lookupKeylookupKeylookupKey** if the key exists in the cache.\nOtherwise, print **−1-1−1**.\nNo output is required for **`putValue`** operations.",
    "constraints": [
      "1 <= T <= 1000",
      "1 <= cacheCapacity <= 1000",
      "1 <= lookupKey <= 1000",
      "1 <= storedValue <= 1000"
    ],
    "inputFormat": "The first line contains a single integer T — the number of test cases. Each test case consists of operations on the cache: The first line contains an integer cacheCapacity — the capacity of the cache. The following lines contain operations, where each line starts with: `putValue` followed by two integers lookupKey and storedValue, or `getValue` followed by one integer lookupKey.",
    "outputFormat": "For each `getValue` operation, print a single integer: the storedValue associated with lookupKey if the key exists in the cache, otherwise print -1. No output is required for `putValue` operations.",
    "sampleTestCases": [
      {
        "id": "sample_1",
        "input": "1\n10\nputValue 2 1\nputValue 1 1\ngetValue 2\nputValue 3 3\nputValue 4 4\ngetValue 1\nputValue 5 5\ngetValue 2\ngetValue 3",
        "output": "1\n-1\n3",
        "explanation": "Explanation:\nInitialize LRU cache with capacity 10.\nputValue(2,1) and putValue(1,1) store keys 2 and 1.\ngetValue(2) returns 1 and marks key 2 as recently used.\nputValue(3,3) inserts key 3.\nputValue(4,4) inserts key 4.\ngetValue(1) returns 1 since key 1 is in the cache.\nputValue(5,5) inserts key 5.\ngetValue(2) returns 1 since key 2 is in the cache.\ngetValue(3) returns 3 as key 3 is in the cache."
      }
    ]
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "label": "Case 1",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_2",
        "label": "Case 2",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_3",
        "label": "Case 3",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_4",
        "label": "Case 4",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_5",
        "label": "Case 5",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      }
    ],
    "totalTestCases": 5,
    "totalWeight": 10
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      2,
      2,
      2,
      2,
      2
    ],
    "maxScore": 10
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "To implement the LRU Cache, we use a combination of a hash map and a doubly linked list. The hash map allows for O(1) lookups, while the linked list allows for efficient insertion and removal of nodes. When a key is accessed, we move the corresponding node to the front of the linked list. If the cache is full and a new key is added, we remove the least recently used key (the node at the end of the linked list).",
    "timeComplexity": "O(1) per operation, O(T * Q) total, where T is the number of test cases and Q is the number of operations per test case.",
    "spaceComplexity": "O(capacity) — the space used by the hash map and linked list.",
    "code": {
      "C": "#include <stdio.h>\n#include <stdlib.h>\ntypedef struct Node {\n    int key;\n    int value;\n    struct Node* prev;\n    struct Node* next;\n} Node;\ntypedef struct LRUCache {\n    int capacity;\n    Node** cache;\n    Node* head;\n    Node* tail;\n} LRUCache;\nNode* createNode(int key, int value) {\n    Node* node = (Node*)malloc(sizeof(Node));\n    node->key = key;\n    node->value = value;\n    node->prev = NULL;\n    node->next = NULL;\n    return node;\n}\nLRUCache* createLRUCache(int capacity) {\n    LRUCache* cache = (LRUCache*)malloc(sizeof(LRUCache));\n    cache->capacity = capacity;\n    cache->cache = (Node**)calloc(capacity, sizeof(Node*));\n    cache->head = createNode(0, 0);\n    cache->tail = createNode(0, 0);\n    cache->head->next = cache->tail;\n    cache->tail->prev = cache->head;\n    return cache;\n}\nint getValue(LRUCache* cache, int key) {\n    if (cache->cache[key] != NULL) {\n        Node* node = cache->cache[key];\n        node->prev->next = node->next;\n        node->next->prev = node->prev;\n        node->next = cache->head->next;\n        node->prev = cache->head;\n        cache->head->next->prev = node;\n        cache->head->next = node;\n        return node->value;\n    } else {\n        return -1;\n    }\n}\nvoid putValue(LRUCache* cache, int key, int value) {\n    if (cache->cache[key] != NULL) {\n        cache->cache[key]->value = value;\n        Node* node = cache->cache[key];\n        node->prev->next = node->next;\n        node->next->prev = node->prev;\n        node->next = cache->head->next;\n        node->prev = cache->head;\n        cache->head->next->prev = node;\n        cache->head->next = node;\n    } else {\n        Node* node = createNode(key, value);\n        cache->cache[key] = node;\n        node->next = cache->head->next;\n        node->prev = cache->head;\n        cache->head->next->prev = node;\n        cache->head->next = node;\n        if (cache->capacity == 0) {\n            Node* lru = cache->tail->prev;\n            cache->cache[lru->key] = NULL;\n            lru->prev->next = lru->next;\n            lru->next->prev = lru->prev;\n            free(lru);\n        } else {\n            cache->capacity--;\n        }\n    }\n}\nint main() {\n    int T;\n    scanf(\"%d\", &T);\n    while (T--) {\n        int cacheCapacity;\n        scanf(\"%d\", &cacheCapacity);\n        LRUCache* cache = createLRUCache(cacheCapacity);\n        while (1) {\n            char op[10];\n            scanf(\"%s\", op);\n            if (strcmp(op, \"putValue\") == 0) {\n                int key, value;\n                scanf(\"%d %d\", &key, &value);\n                putValue(cache, key, value);\n            } else if (strcmp(op, \"getValue\") == 0) {\n                int key;\n                scanf(\"%d\", &key);\n                printf(\"%d\\n\", getValue(cache, key));\n            } else {\n                break;\n            }\n        }\n    }\n    return 0;\n}",
      "C++": "#include <iostream>\n#include <unordered_map>\nusing namespace std;\nclass Node {\npublic:\n    int key;\n    int value;\n    Node* prev;\n    Node* next;\n    Node(int k, int v) : key(k), value(v), prev(NULL), next(NULL) {}\n};\nclass LRUCache {\nprivate:\n    int capacity;\n    unordered_map<int, Node*> cache;\n    Node* head;\n    Node* tail;\npublic:\n    LRUCache(int cap) : capacity(cap) {\n        head = new Node(0, 0);\n        tail = new Node(0, 0);\n        head->next = tail;\n        tail->prev = head;\n    }\n    ~LRUCache() {\n        while (head->next != tail) {\n            Node* node = head->next;\n            head->next = node->next;\n            node->next->prev = head;\n            delete node;\n        }\n        delete head;\n        delete tail;\n    }\n    int get(int key) {\n        if (cache.find(key) != cache.end()) {\n            Node* node = cache[key];\n            node->prev->next = node->next;\n            node->next->prev = node->prev;\n            node->next = head->next;\n            node->prev = head;\n            head->next->prev = node;\n            head->next = node;\n            return node->value;\n        } else {\n            return -1;\n        }\n    }\n    void put(int key, int value) {\n        if (cache.find(key) != cache.end()) {\n            cache[key]->value = value;\n            Node* node = cache[key];\n            node->prev->next = node->next;\n            node->next->prev = node->prev;\n            node->next = head->next;\n            node->prev = head;\n            head->next->prev = node;\n            head->next = node;\n        } else {\n            Node* node = new Node(key, value);\n            cache[key] = node;\n            node->next = head->next;\n            node->prev = head;\n            head->next->prev = node;\n            head->next = node;\n            if (cache.size() > capacity) {\n                Node* lru = tail->prev;\n                cache.erase(lru->key);\n                lru->prev->next = lru->next;\n                lru->next->prev = lru->prev;\n                delete lru;\n            }\n        }\n    }\n};\nint main() {\n    int T;\n    cin >> T;\n    while (T--) {\n        int cacheCapacity;\n        cin >> cacheCapacity;\n        LRUCache cache(cacheCapacity);\n        while (1) {\n            string op;\n            cin >> op;\n            if (op == \"putValue\") {\n                int key, value;\n                cin >> key >> value;\n                cache.put(key, value);\n            } else if (op == \"getValue\") {\n                int key;\n                cin >> key;\n                cout << cache.get(key) << endl;\n            } else {\n                break;\n            }\n        }\n    }\n    return 0;\n}",
      "Java": "import java.util.HashMap;\nimport java.util.Map;\nimport java.util.Scanner;\n\nclass Node {\n    int key;\n    int value;\n    Node prev;\n    Node next;\n    Node(int k, int v) {\n        key = k;\n        value = v;\n        prev = null;\n        next = null;\n    }\n}\n\nclass Main {\n    public static void main(String[] args) {\n        Scanner scanner = new Scanner(System.in);\n        int T = scanner.nextInt();\n        while (T-- > 0) {\n            int cacheCapacity = scanner.nextInt();\n            LRUCache cache = new LRUCache(cacheCapacity);\n            while (scanner.hasNext()) {\n                String op = scanner.next();\n                if (op.equals(\"putValue\")) {\n                    int key = scanner.nextInt();\n                    int value = scanner.nextInt();\n                    cache.put(key, value);\n                } else if (op.equals(\"getValue\")) {\n                    int key = scanner.nextInt();\n                    System.out.println(cache.get(key));\n                } else {\n                    break;\n                }\n            }\n        }\n    }\n}\n\nclass LRUCache {\n    private int capacity;\n    private Map<Integer, Node> cache;\n    private Node head;\n    private Node tail;\n    public LRUCache(int cap) {\n        capacity = cap;\n        cache = new HashMap<>();\n        head = new Node(0, 0);\n        tail = new Node(0, 0);\n        head.next = tail;\n        tail.prev = head;\n    }\n    public int get(int key) {\n        if (cache.containsKey(key)) {\n            Node node = cache.get(key);\n            node.prev.next = node.next;\n            node.next.prev = node.prev;\n            node.next = head.next;\n            node.prev = head;\n            head.next.prev = node;\n            head.next = node;\n            return node.value;\n        } else {\n            return -1;\n        }\n    }\n    public void put(int key, int value) {\n        if (cache.containsKey(key)) {\n            cache.get(key).value = value;\n            Node node = cache.get(key);\n            node.prev.next = node.next;\n            node.next.prev = node.prev;\n            node.next = head.next;\n            node.prev = head;\n            head.next.prev = node;\n            head.next = node;\n        } else {\n            Node node = new Node(key, value);\n            cache.put(key, node);\n            node.next = head.next;\n            node.prev = head;\n            head.next.prev = node;\n            head.next = node;\n            if (cache.size() > capacity) {\n                Node lru = tail.prev;\n                cache.remove(lru.key);\n                lru.prev.next = lru.next;\n                lru.next.prev = lru.prev;\n            }\n        }\n    }\n}",
      "Python3": "from collections import OrderedDict\n\nclass LRUCache:\n    def __init__(self, capacity: int):\n        self.capacity = capacity\n        self.cache = OrderedDict()\n\n    def get(self, key: int) -> int:\n        if key in self.cache:\n            value = self.cache.pop(key)\n            self.cache[key] = value\n            return value\n        else:\n            return -1\n\n    def put(self, key: int, value: int) -> None:\n        if key in self.cache:\n            self.cache.pop(key)\n        elif len(self.cache) >= self.capacity:\n            self.cache.popitem(last=False)\n        self.cache[key] = value\n\nif __name__ == '__main__':\n    T = int(input())\n    for _ in range(T):\n        cacheCapacity = int(input())\n        cache = LRUCache(cacheCapacity)\n        while True:\n            op = input().split()\n            if op[0] == 'putValue':\n                key, value = int(op[1]), int(op[2])\n                cache.put(key, value)\n            elif op[0] == 'getValue':\n                key = int(op[1])\n                print(cache.get(key))\n            else:\n                break",
      "JavaScript": "const fs = require('fs');\nconst input = fs.readFileSync(0, 'utf-8').trim().split('\\n');\nlet idx = 0;\n\nclass Node {\n    constructor(key, value) {\n        this.key = key;\n        this.value = value;\n        this.prev = null;\n        this.next = null;\n    }\n}\n\nclass LRUCache {\n    constructor(capacity) {\n        this.capacity = capacity;\n        this.cache = new Map();\n        this.head = new Node(0, 0);\n        this.tail = new Node(0, 0);\n        this.head.next = this.tail;\n        this.tail.prev = this.head;\n    }\n    get(key) {\n        if (this.cache.has(key)) {\n            const node = this.cache.get(key);\n            node.prev.next = node.next;\n            node.next.prev = node.prev;\n            node.next = this.head.next;\n            node.prev = this.head;\n            this.head.next.prev = node;\n            this.head.next = node;\n            return node.value;\n        } else {\n            return -1;\n        }\n    }\n    put(key, value) {\n        if (this.cache.has(key)) {\n            this.cache.get(key).value = value;\n            const node = this.cache.get(key);\n            node.prev.next = node.next;\n            node.next.prev = node.prev;\n            node.next = this.head.next;\n            node.prev = this.head;\n            this.head.next.prev = node;\n            this.head.next = node;\n        } else {\n            const node = new Node(key, value);\n            this.cache.set(key, node);\n            node.next = this.head.next;\n            node.prev = this.head;\n            this.head.next.prev = node;\n            this.head.next = node;\n            if (this.cache.size > this.capacity) {\n                const lru = this.tail.prev;\n                this.cache.delete(lru.key);\n                lru.prev.next = lru.next;\n                lru.next.prev = lru.prev;\n            }\n        }\n    }\n}\n\nconst T = parseInt(input[idx++]);\nfor (let i = 0; i < T; i++) {\n    const cacheCapacity = parseInt(input[idx++]);\n    const cache = new LRUCache(cacheCapacity);\n    while (idx < input.length) {\n        const op = input[idx++].split(' ');\n        if (op[0] === 'putValue') {\n            const key = parseInt(op[1]);\n            const value = parseInt(op[2]);\n            cache.put(key, value);\n        } else if (op[0] === 'getValue') {\n            const key = parseInt(op[1]);\n            console.log(cache.get(key));\n        } else {\n            break;\n        }\n    }\n}"
    }
  },
  "QBCategory": "CCD",
  "boilerPlates": {
    "C": "#include <stdio.h>\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "C++": "#include <iostream>\nusing namespace std;\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "Java": "import java.util.*;\nimport java.io.*;\n\npublic class Main {\n    public static void main(String[] args) throws IOException {\n        // your code goes here\n    }\n}",
    "Python3": "# your code goes here\n",
    "JavaScript": "// your code goes here\n"
  },
  "_internal": {
    "methodName": "",
    "params": []
  }
}