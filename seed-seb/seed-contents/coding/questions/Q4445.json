{
  "questionId": "Q4445",
  "title": "Reverse a Linked list",
  "slug": "reverse-a-linked-list",
  "version": 1,
  "createdAt": "2026-07-01",
  "updatedAt": "2026-07-04",
  "metadata": {
    "category": "Data Structures",
    "difficulty": "Medium",
    "isPremium": true,
    "tags": [
      "Linked List",
      "Reversal"
    ],
    "companyTags": [],
    "topicTags": [
      "Linked Lists"
    ],
    "QBCategory": "CCD"
  },
  "content": {
    "problemStatement": "Write a program to reverse a linked list iteratively.\n\n\nInput Format\nThe first input line will contain a single integer n, which denotes the number of nodes in the linked list.\n\nThe second line will contain n space-separated integers, where each integer represents the data of a node in the linked list.\n\n\n\nOutput Format\nThe output should be the reversed linked list printed as a space-separated sequence of node data.",
    "constraints": [
      "1 <= n <= 10^2",
      "-10^3 <= data <= 10^3"
    ],
    "inputFormat": "The first line contains a single integer n, which denotes the number of nodes in the linked list. The second line will contain n space-separated integers, where each integer represents the data of a node in the linked list.",
    "outputFormat": "The output should be the reversed linked list printed as a space-separated sequence of node data.",
    "sampleTestCases": [
      {
        "id": "sample_1",
        "input": "4\n1 2 3 4",
        "output": "4 3 2 1",
        "explanation": "The input linked list is 1 -> 2 -> 3 -> 4. After reversing, the output linked list is 4 -> 3 -> 2 -> 1."
      },
      {
        "id": "sample_2",
        "input": "4\n-1 0 1 -10",
        "output": "-10 1 0 -1",
        "explanation": "The input linked list is -1 -> 0 -> 1 -> -10. After reversing, the output linked list is -10 -> 1 -> 0 -> -1."
      },
      {
        "id": "sample_3",
        "input": "1\n5",
        "output": "5",
        "explanation": "The input linked list is 5. After reversing, the output linked list is 5."
      }
    ]
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "label": "minimum boundary",
        "input": "1\n1",
        "expectedOutput": "1",
        "weight": 2
      },
      {
        "id": "tc_2",
        "label": "maximum boundary",
        "input": "10\n-1000 -999 -998 -997 -996 -995 -994 -993 -992 -991",
        "expectedOutput": "-991 -992 -993 -994 -995 -996 -997 -998 -999 -1000",
        "weight": 2
      },
      {
        "id": "tc_3",
        "label": "uniform/edge case",
        "input": "5\n0 0 0 0 0",
        "expectedOutput": "0 0 0 0 0",
        "weight": 2
      },
      {
        "id": "tc_4",
        "label": "tricky case",
        "input": "5\n1 2 3 4 5",
        "expectedOutput": "5 4 3 2 1",
        "weight": 2
      },
      {
        "id": "tc_5",
        "label": "stress/general",
        "input": "4\n-1 0 1 -10",
        "expectedOutput": "-10 1 0 -1",
        "weight": 2
      }
    ],
    "totalTestCases": 5,
    "totalWeight": 10
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      2,
      2,
      2,
      2,
      2
    ],
    "maxScore": 10
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "To reverse a linked list iteratively, we initialize three pointers: previous, current, and next. We traverse the list, and for each node, we do the following: store the next node in next, reverse the current node's pointer by setting current->next = previous, and then move previous and current one step forward. At the end of the traversal, previous will be pointing to the new head of the reversed list.",
    "timeComplexity": "O(n) per test case, where n is the number of nodes in the linked list.",
    "spaceComplexity": "O(1) — we only use a constant amount of space to store the pointers.",
    "code": {
      "C": "#include <stdio.h>\n#include <stdlib.h>\n\n// Structure for linked list node\nstruct Node {\n    int data;\n    struct Node* next;\n};\n\n// Function to create a new node\nstruct Node* newNode(int data) {\n    struct Node* node = (struct Node*)malloc(sizeof(struct Node));\n    node->data = data;\n    node->next = NULL;\n    return node;\n}\n\n// Function to print the linked list\nvoid printList(struct Node* head) {\n    struct Node* temp = head;\n    while (temp != NULL) {\n        if (temp->next != NULL) {\n            printf(\"%d \", temp->data);\n        } else {\n            printf(\"%d\", temp->data);\n        }\n        temp = temp->next;\n    }\n    printf(\"\\n\");\n}\n\n// Iterative function to reverse the linked list\nstruct Node* reverseIterative(struct Node* head) {\n    struct Node* prev = NULL;\n    struct Node* current = head;\n    struct Node* next = NULL;\n\n    while (current != NULL) {\n        next = current->next;\n        current->next = prev;\n        prev = current;\n        current = next;\n    }\n    return prev;  // New head of the reversed list\n}\n\nint main() {\n    int n;\n    scanf(\"%d\", &n);\n    struct Node* head = NULL;\n    struct Node* temp = NULL;\n\n    // Creating the linked list\n    for (int i = 0; i < n; i++) {\n        int data;\n        scanf(\"%d\", &data);\n\n        struct Node* new_node = newNode(data);\n        if (head == NULL) {\n            head = new_node;\n        } else {\n            temp->next = new_node;\n        }\n        temp = new_node;\n    }\n\n    // Reversing the list iteratively\n    head = reverseIterative(head);\n    printList(head);\n    return 0;\n}",
      "C++": "#include <iostream>\nusing namespace std;\n\n// Structure for linked list node\nstruct Node {\n    int data;\n    Node* next;\n    Node(int data) {\n        this->data = data;\n        this->next = nullptr;\n    }\n};\n\n// Function to print the linked list\nvoid printList(Node* head) {\n    Node* temp = head;\n    while (temp != nullptr) {\n        if (temp->next != nullptr) {\n            cout << temp->data << \" \";\n        } else {\n            cout << temp->data;\n        }\n        temp = temp->next;\n    }\n    cout << endl;\n}\n\n// Iterative method to reverse the linked list\nNode* reverseIterative(Node* head) {\n    Node* prev = nullptr;\n    Node* current = head;\n    Node* next = nullptr;\n\n    while (current != nullptr) {\n        next = current->next;  // Store next node\n        current->next = prev;  // Reverse the current node's pointer\n        prev = current;        // Move prev and current one step forward\n        current = next;\n    }\n\n    return prev;  // prev is the new head of the reversed list\n}\n\nint main() {\n    ios::sync_with_stdio(false);\n    cin.tie(NULL);\n    int n;\n    cin >> n;\n    Node* head = nullptr;\n    Node* temp = nullptr;\n\n    for (int i = 0; i < n; i++) {\n        int data;\n        cin >> data;\n\n        Node* newNode = new Node(data);\n\n        if (head == nullptr) {\n            head = newNode;  // First node becomes the head\n        } else {\n            temp->next = newNode;  // Link the new node to the previous one\n        }\n        temp = newNode;  // Move temp to the new node\n    }\n\n    // Reversing the list iteratively\n    head = reverseIterative(head);\n    printList(head);\n    return 0;\n}",
      "Java": "import java.util.*;\nimport java.lang.*;\nimport java.io.*;\nimport java.util.Scanner;\n\nclass Main {\n\n    // Definition of Node structure\n    static class Node {\n        int data;\n        Node next;\n\n        Node(int data) {\n            this.data = data;\n            this.next = null;\n        }\n    }\n\n    // Function to print the linked list\n    public static void printList(Node head) {\n        Node temp = head;\n        while (temp != null) {\n            if (temp.next != null) {\n                System.out.print(temp.data + \" \");\n            } else {\n                System.out.print(temp.data);\n            }\n            temp = temp.next;\n        }\n        System.out.println();\n    }\n\n    // Iterative function to reverse the linked list\n    public static Node reverseIterative(Node head) {\n        Node prev = null;\n        Node current = head;\n        Node next = null;\n\n        while (current != null) {\n            next = current.next;\n            current.next = prev;\n            prev = current;\n            current = next;\n        }\n        return prev;  // New head of the reversed list\n    }\n\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int n = sc.nextInt();\n        Node head = null;\n        Node temp = null;\n\n        // Creating the linked list\n        for (int i = 0; i < n; i++) {\n            int data = sc.nextInt();\n            Node newNode = new Node(data);\n            if (head == null) {\n                head = newNode;\n            } else {\n                temp.next = newNode;\n            }\n            temp = newNode;\n        }\n\n        // Reversing the list iteratively\n        head = reverseIterative(head);\n        printList(head);\n        sc.close();\n    }\n}",
      "Python3": "import sys\n\nclass Node:\n    def __init__(self, data):\n        self.data = data\n        self.next = None\n\ndef print_list(head):\n    temp = head\n    while temp:\n        if temp.next:\n            print(temp.data, end=\" \")\n        else:\n            print(temp.data, end=\"\")\n        temp = temp.next\n    print()\n\ndef reverse_iterative(head):\n    prev = None\n    current = head\n    while current:\n        next_node = current.next\n        current.next = prev\n        prev = current\n        current = next_node\n    return prev  # New head of the reversed list\n\ndef main():\n    input = sys.stdin.readline\n    n = int(input())\n    head = None\n    temp = None\n\n    # Reading the data for each node\n    data_list = list(map(int, input().split()))  # This reads space-separated integers\n\n    # Creating the linked list\n    for data in data_list:\n        new_node = Node(data)\n        if head is None:\n            head = new_node\n        else:\n            temp.next = new_node\n        temp = new_node\n\n    # Reversing the list iteratively\n    head = reverse_iterative(head)\n\n    # Printing the reversed list\n    print_list(head)\n\nif __name__ == \"__main__\":\n    main()",
      "JavaScript": "const readline = require('readline');\nconst rl = readline.createInterface({\n    input: process.stdin,\n    output: process.stdout\n});\n\n// Structure for linked list node\nclass Node {\n    constructor(data) {\n        this.data = data;\n        this.next = null;\n    }\n}\n\n// Function to create a new node\nfunction newNode(data) {\n    return new Node(data);\n}\n\n// Function to print the linked list\nfunction printList(head) {\n    let temp = head;\n    let result = '';\n    while (temp) {\n        if (temp.next) {\n            result += temp.data + ' ';\n        } else {\n            result += temp.data;\n        }\n        temp = temp.next;\n    }\n    console.log(result);\n}\n\n// Iterative function to reverse the linked list\nfunction reverseIterative(head) {\n    let prev = null;\n    let current = head;\n    let next = null;\n\n    while (current) {\n        next = current.next;\n        current.next = prev;\n        prev = current;\n        current = next;\n    }\n    return prev;  // New head of the reversed list\n}\n\n// Main function\nfunction main() {\n    let input = '';\n    rl.on('line', (line) => {\n        input += line + '\\n';\n    });\n    rl.on('close', () => {\n        let lines = input.trim().split('\\n');\n        let n = parseInt(lines[0]);\n        let head = null;\n        let temp = null;\n\n        // Creating the linked list\n        let data = lines[1].split(' ').map(Number);\n        for (let j = 0; j < n; j++) {\n            let newNode = new Node(data[j]);\n            if (head === null) {\n                head = newNode;\n            } else {\n                temp.next = newNode;\n            }\n            temp = newNode;\n        }\n\n        // Reversing the list iteratively\n        head = reverseIterative(head);\n        printList(head);\n    });\n}\n\nmain();"
    }
  },
  "QBCategory": "CCD",
  "boilerPlates": {
    "C": "#include <stdio.h>\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "C++": "#include <iostream>\nusing namespace std;\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "Java": "import java.util.*;\nimport java.io.*;\n\npublic class Main {\n    public static void main(String[] args) throws IOException {\n        // your code goes here\n    }\n}",
    "Python3": "# your code goes here\n",
    "JavaScript": "// your code goes here\n"
  },
  "_internal": {
    "methodName": "",
    "params": []
  }
}