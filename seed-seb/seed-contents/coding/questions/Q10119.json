{
  "questionId": "Q10119",
  "title": "Transformation Cost",
  "slug": "transformation-cost",
  "version": 1,
  "createdAt": "2026-07-02",
  "updatedAt": "2026-07-04",
  "metadata": {
    "category": "Bit Manipulation | Greedy | Constructive Algorithms",
    "difficulty": "Medium",
    "isPremium": true,
    "tags": [
      "binary representation",
      "subsequence avoidance",
      "construct minimal number",
      "enumeration of patterns"
    ],
    "companyTags": [],
    "topicTags": [
      "Dynamic Programming"
    ],
    "QBCategory": "CCD"
  },
  "content": {
    "problemStatement": "Ayush is being interviewed for an executive role in the finance department of Aparoksha, IIIT Allahabad's annual tech fest.\n\nHimanshu Shekhar, the interviewer, decided to test Ayush's mathematical skills with the following problem.\n\nAn integer is called good if, when written in binary, it doesn't contain 101\\texttt{101}101 as a subsequence.\n\nFor example, 444 and 151515 are good (4=10024 = \\texttt{100}_24=1002​ and 15=1111215 = \\texttt{1111}_215=11112​, both of which don't contain 101\\texttt{101}101 as a subsequence), while 999 is not good (9=100129 = \\texttt{1001}_29=10012​, which contains 101\\texttt{101}101 as a subsequence).\n\nNote that a subsequence need not be contiguous.\n\nAyush is given an integer NNN. He can do the following operation on it:\n\n\nChoose an integer x≥0x \\geq 0x≥0, and add 2x2^x2x to NNN.\n\nThe cost of this operation is 2x2^x2x.\nAyush's task is to find the **minimum total cost** needed to transform NNN into a good integer. Can you help him find this minimum cost?\n\nThe total cost of a sequence of moves is the sum of their individual costs.\n\nIt can be shown that it's always possible to transform NNN into a good integer.\n\n\nInput Format\n\nThe first line of input will contain a single integer TTT, denoting the number of test cases.\nThe first and only line of each test case contains a single integer NNN.\n\n\nOutput Format\nFor each test case, print a single line containing one integer: the minimum transformation cost.",
    "constraints": [
      "1 ≤ T ≤ 10^5",
      "0 ≤ N < 2^30"
    ],
    "inputFormat": "The first line contains an integer T, the number of test cases. Each of the next T lines contains a single integer N (0 ≤ N < 2^30).",
    "outputFormat": "For each test case output a single line containing the minimum total cost, i.e., the smallest non‑negative integer D such that N + D is a good integer.",
    "sampleTestCases": [
      {
        "id": "sample_1",
        "input": "3\n6\n10\n43223",
        "output": "0\n2\n5929",
        "explanation": "Explanation:\nTest case 111: 6=11026 = \\texttt{110}_26=1102​ in binary, which already doesn't have 101\\texttt{101}101 as a subsequence. So, no operations need to be performed, and the answer is 000.\nTest case 222: 101010 (in decimal) is 10102\\texttt{1010}_210102​ in binary.\nAyush can choose x=1x = 1x=1 and add 2x=21=22^x = 2^1 = 22x=21=2 to NNN, obtaining 10+2=1210+2 = 1210+2=12.\nThe binary representation of 121212 is 11002\\texttt{1100}_211002​, which doesn't have 101\\texttt{101}101 as a subsequence; so it is good."
      }
    ]
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "label": "Case 1",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_2",
        "label": "Case 2",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_3",
        "label": "Case 3",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_4",
        "label": "Case 4",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_5",
        "label": "Case 5",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      }
    ],
    "totalTestCases": 5,
    "totalWeight": 10
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      2,
      2,
      2,
      2,
      2
    ],
    "maxScore": 10
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "A binary number is *good* iff it does not contain the subsequence 101. This is equivalent to the binary string being of the form 0*1*0* – after the first occurrence of a 1 followed later by a 0, no more 1s may appear. Therefore a good number is completely defined by two indices: the first position where the block of 1s starts (l) and the first position after that where the block of 0s starts (r). All bits before l are 0, bits l … r‑1 are 1, and bits r … end are 0. For a fixed length of 31 bits (enough for N < 2^30) there are only O(31²) possible (l, r) pairs. For each pair we can compute the corresponding integer directly:  value = (1 << (31‑l)) – (1 << (31‑r)) (the second term is 0 when r = 31). We keep the smallest value that is ≥ N; the answer is that value minus N. The enumeration is performed for every test case, giving a worst‑case of about 900 operations per case, easily fast enough for T = 10⁵ in compiled languages and acceptable in Python.",
    "timeComplexity": "O(L²) per test case, where L = 31 (constant). Effectively O(T).",
    "spaceComplexity": "O(1) – only a few integer variables are used.",
    "code": {
      "C": "#include <stdio.h>\n#include <stdint.h>\n\nstatic inline uint32_t minCost(uint32_t n) {\n    const int L = 31; // we work with 31 bits (0 … 30)\n    uint32_t best = UINT32_MAX; // large sentinel\n    for (int l = 0; l <= L; ++l) {\n        for (int r = l; r <= L; ++r) {\n            uint32_t val;\n            if (l == L) {\n                val = 0; // all zeros\n            } else {\n                uint32_t high = 1u << (L - l);\n                uint32_t low = (r == L) ? 0u : (1u << (L - r));\n                val = high - low;\n            }\n            if (val >= n) {\n                uint32_t diff = val - n;\n                if (diff < best) best = diff;\n            }\n        }\n    }\n    return best;\n}\n\nint main() {\n    int T; if (scanf(\"%d\", &T)!=1) return 0;\n    while (T--) {\n        uint32_t N; scanf(\"%u\", &N);\n        printf(\"%u\\n\", minCost(N));\n    }\n    return 0;\n}\n",
      "C++": "#include <bits/stdc++.h>\nusing namespace std;\n\nstatic inline uint32_t minCost(uint32_t n) {\n    const int L = 31; // enough for N < 2^30\n    uint32_t best = UINT32_MAX;\n    for (int l = 0; l <= L; ++l) {\n        for (int r = l; r <= L; ++r) {\n            uint32_t val;\n            if (l == L) {\n                val = 0;\n            } else {\n                uint32_t high = 1u << (L - l);\n                uint32_t low = (r == L) ? 0u : (1u << (L - r));\n                val = high - low;\n            }\n            if (val >= n) {\n                best = min(best, val - n);\n            }\n        }\n    }\n    return best;\n}\n\nint main() {\n    ios::sync_with_stdio(false);\n    cin.tie(nullptr);\n    int T; if(!(cin>>T)) return 0;\n    while (T--) {\n        uint32_t N; cin >> N;\n        cout << minCost(N) << '\\n';\n    }\n    return 0;\n}\n",
      "Java": "import java.io.*;\nimport java.util.*;\n\npublic class Main {\n    private static final int L = 31; // bits needed\n    private static long minCost(int n) {\n        long best = Long.MAX_VALUE;\n        for (int l = 0; l <= L; ++l) {\n            for (int r = l; r <= L; ++r) {\n                long val;\n                if (l == L) {\n                    val = 0L;\n                } else {\n                    long high = 1L << (L - l);\n                    long low = (r == L) ? 0L : (1L << (L - r));\n                    val = high - low;\n                }\n                if (val >= n) {\n                    long diff = val - n;\n                    if (diff < best) best = diff;\n                }\n            }\n        }\n        return best;\n    }\n    public static void main(String[] args) throws Exception {\n        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));\n        StringBuilder sb = new StringBuilder();\n        int T = Integer.parseInt(br.readLine().trim());\n        for (int t = 0; t < T; ++t) {\n            int N = Integer.parseInt(br.readLine().trim());\n            sb.append(minCost(N)).append('\\n');\n        }\n        System.out.print(sb.toString());\n    }\n}\n",
      "Python3": "import sys\n\ndef min_cost(n: int) -> int:\n    L = 31  # enough for N < 2**30\n    best = 1 << 60\n    for l in range(L + 1):\n        for r in range(l, L + 1):\n            if l == L:\n                val = 0\n            else:\n                high = 1 << (L - l)\n                low = 0 if r == L else 1 << (L - r)\n                val = high - low\n            if val >= n:\n                diff = val - n\n                if diff < best:\n                    best = diff\n    return best\n\ndef solve():\n    data = sys.stdin.read().strip().split()\n    if not data:\n        return\n    it = iter(data)\n    T = int(next(it))\n    out = []\n    for _ in range(T):\n        N = int(next(it))\n        out.append(str(min_cost(N)))\n    sys.stdout.write('\\n'.join(out))\n\nif __name__ == \"__main__\":\n    solve()\n",
      "JavaScript": "const fs = require('fs');\nconst input = fs.readFileSync(0, 'utf-8').trim().split(/\\s+/);\nlet idx = 0;\nconst T = Number(input[idx++]);\nconst L = 31;\nfunction minCost(n) {\n  let best = Number.MAX_SAFE_INTEGER;\n  for (let l = 0; l <= L; ++l) {\n    for (let r = l; r <= L; ++r) {\n      let val;\n      if (l === L) {\n        val = 0;\n      } else {\n        const high = 1 << (L - l);\n        const low = (r === L) ? 0 : (1 << (L - r));\n        val = high - low;\n      }\n      if (val >= n) {\n        const diff = val - n;\n        if (diff < best) best = diff;\n      }\n    }\n  }\n  return best;\n}\nlet out = [];\nfor (let t = 0; t < T; ++t) {\n  const N = Number(input[idx++]);\n  out.push(String(minCost(N)));\n}\nconsole.log(out.join('\\n'));\n"
    }
  },
  "QBCategory": "CCD",
  "boilerPlates": {
    "C": "#include <stdio.h>\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "C++": "#include <iostream>\nusing namespace std;\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "Java": "import java.util.*;\nimport java.io.*;\n\npublic class Main {\n    public static void main(String[] args) throws IOException {\n        // your code goes here\n    }\n}",
    "Python3": "# your code goes here\n",
    "JavaScript": "// your code goes here\n"
  },
  "_internal": {
    "methodName": "",
    "params": []
  }
}