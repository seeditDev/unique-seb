{
  "questionId": "Q3213",
  "title": "Palindrome Pairs",
  "slug": "palindrome-pairs",
  "version": 1,
  "createdAt": "2026-07-01",
  "updatedAt": "2026-07-05",
  "metadata": {
    "category": "Strings",
    "difficulty": "Hard",
    "isPremium": false,
    "tags": [
      "Palindrome",
      "Hashing",
      "Strings",
      "String Splitting",
      "Hash Map",
      "Two Pointers"
    ],
    "companyTags": [],
    "topicTags": [
      "Strings"
    ]
  },
  "content": {
    "problemStatement": "Given a list of strings **S**[&nbsp;1..**N**&nbsp;], you need to count how many ordered pairs of strings are awesome. Given two integers i and j, such that 1 \u2264 i, j \u2264 n and i \u2260 j, an ordered pair (&nbsp;**S**[&nbsp;**i**&nbsp;], **S**[&nbsp;**j**&nbsp;]&nbsp;) is called awesome if and only if concatenating **S**[&nbsp;**i**&nbsp;] and **S**[&nbsp;**j**&nbsp;] gives a palindrome.",
    "constraints": [
      "1<=T<=5",
      "1<=N<=10^6",
      "Sum of lengths of all strings in a test case \u226410^6",
      "Each string length \u2264100",
      "Strings contain only lowercase English letters; the empty string is allowed."
    ],
    "inputFormat": "First line contains an integer T, the number of test cases. For each test case, the first line contains an integer N, the number of strings. Then N lines follow, each containing a string of lowercase English letters; a line may be empty representing the empty string.",
    "outputFormat": "For each test case output a single line containing the number of ordered pairs (i, j) with i\u2260j such that the concatenation S[i]+S[j] is a palindrome.",
    "sampleTestCases": [
      {
        "id": "sample_1",
        "input": "1\n3\na\nab\nabb",
        "output": "2",
        "explanation": "Explanation: ab|a and abb|a"
      },
      {
        "id": "sample_2",
        "input": "1\n2\nab\nba",
        "output": "2",
        "explanation": "Explanation: ab|ba and ba|ab"
      },
      {
        "id": "sample_3",
        "input": "1\n1\na",
        "output": "0",
        "explanation": "Explanation: No pairs"
      }
    ]
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "label": "Case 1",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_2",
        "label": "Case 2",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_3",
        "label": "Case 3",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_4",
        "label": "Case 4",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_5",
        "label": "Case 5",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      }
    ],
    "totalTestCases": 5,
    "totalWeight": 10
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      2,
      2,
      2,
      2,
      2
    ],
    "maxScore": 10
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "Store the frequency of every distinct string in an unordered_map<string,long long>. For each distinct string s we pre\u2011compute two boolean arrays: prefPal[i] is true if the prefix s[0..i] is a palindrome, and suffPal[i] is true if the suffix s[i..len-1] is a palindrome. For every split position pos (0\u2026len) we have prefix = s[0..pos-1] and suffix = s[pos..len-1]. If prefPal[pos-1] is true (or pos==0) then any string t whose reverse equals suffix will form a palindrome when placed before s, because reverse(suffix)+s = reverse(suffix)+prefix+suffix is a palindrome. Similarly, if suffPal[pos] is true (or pos==len) then any string t whose reverse equals prefix will form a palindrome when placed after s. Using the map we obtain the count of such t in O(1). While adding contributions we subtract the case where t is the same occurrence of s (i=j). Summing over all distinct strings and multiplying by their frequencies yields the total number of ordered pairs. The algorithm processes each string in O(L) time where L is its length, so the total time is O(total length\u200a+\u200aU\u00b7M) \u2264 O(10^6\u00b7100) which fits the limits. Memory usage is O(U\u00b7average length) for storing the strings and O(U) for the map.",
    "timeComplexity": "O(totalLength\u200a+\u200aU\u00b7M) \u2264 O(10^8) per test case, where totalLength is the sum of lengths of all strings, U is the number of distinct strings and M\u2264100 is the maximum string length.",
    "spaceComplexity": "O(totalLength) for storing the input strings plus O(U) for the hash map of frequencies.",
    "code": {
      "C": "#include <stdio.h>\\n#include <stdlib.h>\\n#include <string.h>\\n#include <stdbool.h>\\n#include <stdint.h>\\n\\ntypedef struct Node {\\n    char *key;\\n    long long cnt;\\n    struct Node *next;\\n} Node;\\n\\n#define TABLE_SIZE 2000003 /* a prime > 2*10^6 */\\n\\nstatic Node *table[TABLE_SIZE];\\n\\nstatic unsigned long long hash_str(const char *s){\\n    unsigned long long h = 0;\\n    while(*s){ h = h*131 + (unsigned char)(*s); s++; }\\n    return h;\\n}\\n\\nstatic Node* find_node(const char *s, bool create){\\n    unsigned long long h = hash_str(s);\\n    size_t idx = h % TABLE_SIZE;\\n    Node *cur = table[idx];\\n    while(cur){\\n        if(strcmp(cur->key, s) == 0) return cur;\\n        cur = cur->next;\\n    }\\n    if(!create) return NULL;\\n    Node *node = (Node*)malloc(sizeof(Node));\\n    node->key = strdup(s);\\n    node->cnt = 0;\\n    node->next = table[idx];\\n    table[idx] = node;\\n    return node;\\n}\\n\\nstatic bool is_pal(const char *s, int l, int r){ /* inclusive */\\n    while(l < r){ if(s[l] != s[r]) return false; l++; r--; }\\n    return true;\\n}\\n\\nstatic void clear_table(){\\n    for(size_t i = 0; i < TABLE_SIZE; i++) {\\n        Node *cur = table[i];\\n        while(cur){\\n            Node *tmp = cur;\\n            cur = cur->next;\\n            free(tmp->key);\\n            free(tmp);\\n        }\\n        table[i] = NULL;\\n    }\\n}\\n\\nint main(){\\n    int T; if(scanf(\"%d\", &T) != 1) return 0;\\n    while(T--){\\n        int N; scanf(\"%d\", &N);\\n        getchar(); // consume newline after N\\n        char **arr = (char**)malloc(N * sizeof(char*));\\n        for(int i = 0; i < N; i++) {\\n            char buf[105];\\n            if(!fgets(buf, sizeof(buf), stdin)) buf[0] = '\\0';\\n            size_t len = strlen(buf);\\n            if(len && buf[len-1] == '\\n') buf[--len] = '\\0';\\n            arr[i] = strdup(buf);\\n            Node *node = find_node(arr[i], true);\\n            node->cnt++;\\n        }\\n        long long answer = 0;\\n        for(size_t idx = 0; idx < TABLE_SIZE; ++idx){\\n            for(Node *node = table[idx]; node; node = node->next){\\n                const char *s = node->key;\\n                int L = (int)strlen(s);\\n                bool pref[101];\\n                bool suff[101];\\n                for(int i = 0; i < L; i++) pref[i] = is_pal(s, 0, i);\\n                for(int i = 0; i < L; i++) suff[i] = is_pal(s, i, L-1);\\n                for(int pos = 0; pos <= L; ++pos){\\n                    /* t before s */\\n                    if(pos == 0 || pref[pos-1]){\\n                        int sufLen = L - pos;\\n                        char rev[101];\\n                        for(int k = 0; k < sufLen; k++) rev[k] = s[L-1 - k];\\n                        rev[sufLen] = '\\0';\\n                        Node *partner = find_node(rev, false);\\n                        if(partner){\\n                            long long add = partner->cnt;\\n                            if(strcmp(s, rev) == 0) add -= node->cnt;\\n                            answer += add * node->cnt;\\n                        }\\n                    }\\n                    /* t after s */\\n                    if(pos == L || suff[pos]){\\n                        int preLen = pos;\\n                        char rev[101];\\n                        for(int k = 0; k < preLen; k++) rev[k] = s[preLen-1 - k];\\n                        rev[preLen] = '\\0';\\n                        Node *partner = find_node(rev, false);\\n                        if(partner){\\n                            long long add = partner->cnt;\\n                            if(strcmp(s, rev) == 0) add -= node->cnt;\\n                            answer += add * node->cnt;\\n                        }\\n                    }\\n                }\\n            }\\n        }\\n        printf(\"%lld\\n\", answer);\\n        for(int i = 0; i < N; i++) free(arr[i]);\\n        free(arr);\\n        clear_table();\\n    }\\n    return 0;\\n}"
    }
  }
}