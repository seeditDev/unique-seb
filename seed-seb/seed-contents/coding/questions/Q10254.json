{
  "questionId": "Q10254",
  "title": "Non-Zero XOR",
  "slug": "non-zero-xor",
  "version": 1,
  "createdAt": "2026-07-02",
  "updatedAt": "2026-07-04",
  "metadata": {
    "category": "Arrays | Greedy | Bit Manipulation",
    "difficulty": "Hard",
    "isPremium": true,
    "tags": [
      "Prefix XOR",
      "Greedy ordering",
      "Sorting"
    ],
    "companyTags": [],
    "topicTags": [
      "Constructive Algorithms"
    ],
    "QBCategory": "CCD"
  },
  "content": {
    "problemStatement": "You are given an array AAA of size NNN.\n\nYou can rearrange the elements of AAA as you want. After that, construct an array BBB of size NNN as:\n\n\nB1=A1B_1 = A_1B1​=A1​;\nBi=Bi−1⊕AiB_i = B_{i - 1} \\oplus A_iBi​=Bi−1​⊕Ai​ for all 2≤i≤N2 \\le i \\le N2≤i≤N, where ⊕\\oplus⊕ denotes the bitwise XOR operator.\nFind a rearrangement of AAA such that Bi≠0B_i \\ne 0Bi​=0 for all 1≤i≤N1 \\le i \\le N1≤i≤N.\n\n\nInput Format\n\nThe first line of input will contain a single integer TTT, denoting the number of test cases.\nEach test case consists of multiple lines of input.\n\nThe first line of each test case contains NNN, the size of the array AAA.\nThe second line of each test case contains NNN space-separated integers : A1,A2,…,ANA_1, A_2, \\ldots, A_NA1​,A2​,…,AN​.\n\n\n\n\nOutput Format\nFor each test case, output on a new line, NNN space-separated integers denoting the rearrangement of AAA satisfying the conditions.\n\nIn case no such rearrangement exists, print −1-1−1 instead.",
    "constraints": [
      "1 ≤ T ≤ 2·10^4",
      "1 ≤ N ≤ 2·10^5 (sum of N over all test cases ≤ 2·10^5)",
      "1 ≤ Ai ≤ 10^9"
    ],
    "inputFormat": "The first line contains an integer T, the number of test cases. Each test case consists of two lines: the first line contains an integer N, the size of the array. The second line contains N space‑separated integers A1, A2, …, AN.",
    "outputFormat": "For each test case, output a single line. If a rearrangement of the given array exists such that every prefix XOR is non‑zero, print N space‑separated integers – the required permutation. If no such permutation exists, print -1.",
    "sampleTestCases": [
      {
        "id": "sample_1",
        "input": "4\n3\n1 2 3\n5\n1 2 3 4 5\n3\n1 2 2\n1\n1",
        "output": "-1\n1 2 4 3 5\n1 2 2\n1",
        "explanation": "Test case 1: total XOR = 1⊕2⊕3 = 0, so impossible.\\nTest case 2: after sorting and fixing the zero prefix we obtain [1,2,4,3,5]; all prefix XORs are non‑zero.\\nTest case 3: total XOR = 1⊕2⊕2 = 1 ≠ 0, the sorted order already works.\\nTest case 4: single element 1 is non‑zero, so the answer is [1]."
      }
    ]
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "label": "minimum boundary",
        "input": "1\n1\n5",
        "expectedOutput": "5",
        "weight": 2
      },
      {
        "id": "tc_2",
        "label": "Case 2",
        "input": "1\n1\n5",
        "expectedOutput": "5",
        "weight": 2
      },
      {
        "id": "tc_3",
        "label": "Case 3",
        "input": "1\n1\n5",
        "expectedOutput": "5",
        "weight": 2
      },
      {
        "id": "tc_4",
        "label": "Case 4",
        "input": "1\n1\n5",
        "expectedOutput": "5",
        "weight": 2
      },
      {
        "id": "tc_5",
        "label": "Case 5",
        "input": "1\n1\n5",
        "expectedOutput": "5",
        "weight": 2
      }
    ],
    "totalTestCases": 5,
    "totalWeight": 10
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      2,
      2,
      2,
      2,
      2
    ],
    "maxScore": 10
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "Let pref_i be the XOR of the first i elements of the chosen order. We need pref_i ≠ 0 for every i. The XOR of the whole array (total) equals pref_N. If total = 0, the last prefix would be zero for any order, therefore the answer is impossible. If total ≠ 0 we can always construct a valid order.\n\nGreedy construction:\n1. Sort the array in non‑decreasing order.\n2. Scan the array from left to right while maintaining the current prefix XOR.\n3. If after adding A[i] the prefix becomes zero, swap A[i] with A[i+1] (i < N). Because total ≠ 0, there is at least one element to the right that differs in the most significant set bit, guaranteeing the swap makes the prefix non‑zero.\n4. Continue the scan. When the scan finishes, no prefix is zero.\n\nThe algorithm works because a zero prefix can only appear when the current prefix XOR equals the next element. Swapping with a later element changes the most significant bit of the prefix, preventing zero. The final element cannot make the prefix zero unless total = 0, which we have already excluded.\n\nEdge cases handled:\n- N = 1: possible only if the single element is non‑zero.\n- Presence of zeros in the array: a zero can be placed after any non‑zero prefix without creating a zero prefix.\n- Total XOR = 0: answer is -1.\n\nComplexities: sorting O(N log N) per test case, scanning O(N). Overall O(Σ N log N) ≤ O(2·10^5 log 2·10^5). Memory O(N).",
    "timeComplexity": "O(N log N) per test case (dominated by sorting)",
    "spaceComplexity": "O(N) for storing the array",
    "code": {
      "C": "#include <stdio.h>\n#include <stdlib.h>\n\nint cmp(const void *a, const void *b) {\n    long long x = *(const long long *)a;\n    long long y = *(const long long *)b;\n    return (x > y) - (x < y);\n}\n\nint main() {\n    int T; if (scanf(\"%d\", &T)!=1) return 0;\n    while (T--) {\n        int N; scanf(\"%d\", &N);\n        long long *a = (long long*)malloc(N * sizeof(long long));\n        long long total = 0;\n        for (int i = 0; i < N; ++i) {\n            scanf(\"%lld\", &a[i]);\n            total ^= a[i];\n        }\n        if (total == 0) {\n            printf(\"-1\\n\");\n            free(a);\n            continue;\n        }\n        qsort(a, N, sizeof(long long), cmp);\n        long long pref = 0;\n        for (int i = 0; i < N; ++i) {\n            pref ^= a[i];\n            if (pref == 0 && i + 1 < N) {\n                long long tmp = a[i];\n                a[i] = a[i+1];\n                a[i+1] = tmp;\n                pref ^= a[i]; // recompute prefix for current position after swap\n                pref ^= tmp;   // remove old contribution\n                pref ^= a[i]; // add new contribution (same as first line after swap)\n                // simpler: recompute pref from scratch up to i\n                pref = 0;\n                for (int j = 0; j <= i; ++j) pref ^= a[j];\n            }\n        }\n        for (int i = 0; i < N; ++i) {\n            if (i) printf(\" \");\n            printf(\"%lld\", a[i]);\n        }\n        printf(\"\\n\");\n        free(a);\n    }\n    return 0;\n}\n",
      "C++": "#include <bits/stdc++.h>\nusing namespace std;\n\nint main() {\n    ios::sync_with_stdio(false);\n    cin.tie(nullptr);\n    int T; if(!(cin>>T)) return 0;\n    while(T--) {\n        int N; cin>>N;\n        vector<long long> a(N);\n        long long total=0;\n        for(int i=0;i<N;++i){cin>>a[i]; total ^= a[i];}\n        if(total==0){\n            cout<<-1<<\"\\n\";\n            continue;\n        }\n        sort(a.begin(), a.end());\n        long long pref=0;\n        for(int i=0;i<N;++i){\n            pref ^= a[i];\n            if(pref==0 && i+1<N){\n                swap(a[i], a[i+1]);\n                pref = 0;\n                for(int j=0;j<=i;++j) pref ^= a[j];\n            }\n        }\n        for(int i=0;i<N;++i){\n            if(i) cout<<' ';\n            cout<<a[i];\n        }\n        cout<<\"\\n\";\n    }\n    return 0;\n}\n",
      "Java": "import java.io.*;\nimport java.util.*;\n\npublic class Main {\n    public static void main(String[] args) throws Exception {\n        FastScanner fs = new FastScanner(System.in);\n        int T = fs.nextInt();\n        StringBuilder out = new StringBuilder();\n        while (T-- > 0) {\n            int N = fs.nextInt();\n            long[] a = new long[N];\n            long total = 0;\n            for (int i = 0; i < N; i++) {\n                a[i] = fs.nextLong();\n                total ^= a[i];\n            }\n            if (total == 0) {\n                out.append(\"-1\\n\");\n                continue;\n            }\n            Arrays.sort(a);\n            long pref = 0;\n            for (int i = 0; i < N; i++) {\n                pref ^= a[i];\n                if (pref == 0 && i + 1 < N) {\n                    long tmp = a[i];\n                    a[i] = a[i + 1];\n                    a[i + 1] = tmp;\n                    pref = 0;\n                    for (int j = 0; j <= i; j++) pref ^= a[j];\n                }\n            }\n            for (int i = 0; i < N; i++) {\n                if (i > 0) out.append(' ');\n                out.append(a[i]);\n            }\n            out.append('\\n');\n        }\n        System.out.print(out.toString());\n    }\n\n    // Fast scanner for large input\n    static class FastScanner {\n        private final InputStream in;\n        private final byte[] buffer = new byte[1 << 16];\n        private int ptr = 0, len = 0;\n        FastScanner(InputStream in) { this.in = in; }\n        private int readByte() throws IOException {\n            if (ptr >= len) {\n                len = in.read(buffer);\n                ptr = 0;\n                if (len <= 0) return -1;\n            }\n            return buffer[ptr++];\n        }\n        long nextLong() throws IOException { return Long.parseLong(next()); }\n        int nextInt() throws IOException { return Integer.parseInt(next()); }\n        String next() throws IOException {\n            StringBuilder sb = new StringBuilder();\n            int c;\n            while ((c = readByte()) != -1 && c <= ' ') {}\n            while (c != -1 && c > ' ') {\n                sb.append((char) c);\n                c = readByte();\n            }\n            return sb.toString();\n        }\n    }\n}\n",
      "Python3": "#!/usr/bin/env python3\nimport sys\n\ndef solve():\n    data = sys.stdin.read().strip().split()\n    if not data:\n        return\n    it = iter(data)\n    T = int(next(it))\n    out_lines = []\n    for _ in range(T):\n        N = int(next(it))\n        a = [int(next(it)) for _ in range(N)]\n        total = 0\n        for v in a:\n            total ^= v\n        if total == 0:\n            out_lines.append('-1')\n            continue\n        a.sort()\n        pref = 0\n        i = 0\n        while i < N:\n            pref ^= a[i]\n            if pref == 0 and i + 1 < N:\n                a[i], a[i+1] = a[i+1], a[i]\n                pref = 0\n                for j in range(i+1):\n                    pref ^= a[j]\n            i += 1\n        out_lines.append(' '.join(map(str, a)))\n    sys.stdout.write('\\n'.join(out_lines))\n\nif __name__ == \"__main__\":\n    solve()\n",
      "JavaScript": "const fs = require('fs');\nconst input = fs.readFileSync(0, 'utf-8').trim().split(/\\s+/);\nlet idx = 0;\nconst T = Number(input[idx++]);\nlet output = [];\nfor (let t = 0; t < T; ++t) {\n  const N = Number(input[idx++]);\n  let a = new Array(N);\n  let total = 0;\n  for (let i = 0; i < N; ++i) {\n    a[i] = Number(input[idx++]);\n    total ^= a[i];\n  }\n  if (total === 0) {\n    output.push('-1');\n    continue;\n  }\n  a.sort((x, y) => x - y);\n  let pref = 0;\n  for (let i = 0; i < N; ++i) {\n    pref ^= a[i];\n    if (pref === 0 && i + 1 < N) {\n      // swap with next element\n      const tmp = a[i];\n      a[i] = a[i + 1];\n      a[i + 1] = tmp;\n      pref = 0;\n      for (let j = 0; j <= i; ++j) pref ^= a[j];\n    }\n  }\n  output.push(a.join(' '));\n}\nconsole.log(output.join('\\n'));\n"
    }
  },
  "QBCategory": "CCD",
  "boilerPlates": {
    "C": "#include <stdio.h>\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "C++": "#include <iostream>\nusing namespace std;\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "Java": "import java.util.*;\nimport java.io.*;\n\npublic class Main {\n    public static void main(String[] args) throws IOException {\n        // your code goes here\n    }\n}",
    "Python3": "# your code goes here\n",
    "JavaScript": "// your code goes here\n"
  },
  "_internal": {
    "methodName": "",
    "params": []
  }
}