{
  "questionId": "Q4575",
  "title": "Check if Linked List is Palindrome",
  "slug": "check-if-linked-list-is-palindrome",
  "version": 1,
  "createdAt": "2026-07-01",
  "updatedAt": "2026-07-04",
  "metadata": {
    "category": "Data Structures",
    "difficulty": "Hard",
    "isPremium": true,
    "tags": [
      "Linked List",
      "Two Pointers",
      "Palindrome",
      "O(N) Time",
      "O(1) Extra Space"
    ],
    "companyTags": [],
    "topicTags": [
      "Linked Lists"
    ],
    "QBCategory": "CCD"
  },
  "content": {
    "problemStatement": "Chef has been given a singly linked list containing integers.\n\nHe wants to check whether the given linked list forms a **palindrome** — i.e., it reads the same forwards and backwards.\n\nYour task is to help Chef determine if the linked list is a palindrome.\n\nFunction DeclarationFunction NameisPalindromeisPalindromeisPalindrome – This function checks whether a singly linked list is a palindrome.\n\nParameters\nheadheadhead : A pointer to the head node of the singly linked list.\nReturn Value\nReturns truetruetrue if the linked list is a palindrome.\nReturns falsefalsefalse otherwise.\nConstraints\n1≤N≤1051 \\leq N \\leq 10^51≤N≤105, where NNN is the number of nodes in the linked list.\n0≤0 \\leq0≤ Value stored in each node ≤9\\leq 9≤9\nThe linked list is singly linked\nThe input and output formats provided below are only for testing with custom inputs. You only need to complete the core logic function.\n\n\nInput Format\n\nThe first line contains a single integer NNN — the number of nodes in the linked list.\nThe second line contains NNN space-separated integers representing the values of the linked list nodes in order.\n\n\nOutput Format\n\nPrint YESYESYES if the linked list is a palindrome.\nOtherwise, print NONONO.",
    "constraints": [
      "1 <= T <= 10",
      "1 <= N <= 100000",
      "0 <= node value <= 9"
    ],
    "inputFormat": "The first line contains an integer T, the number of test cases. For each test case, the first line contains an integer N, the number of nodes in the linked list. The second line contains N space‑separated integers (each between 0 and 9) representing the node values in order.",
    "outputFormat": "For each test case, output a single line containing \"YES\" if the linked list is a palindrome, otherwise output \"NO\".",
    "sampleTestCases": [
      {
        "id": "sample_1",
        "input": "3\n5\n1 2 3 2 1\n4\n1 2 3 4\n1\n7",
        "output": "YES\nNO\nYES",
        "explanation": "First list [1,2,3,2,1] reads the same forward and backward → YES. Second list [1,2,3,4] is not a palindrome → NO. A single node list [7] is always a palindrome → YES."
      },
      {
        "id": "sample_2",
        "input": "2\n6\n9 8 7 7 8 9\n4\n1 2 2 3",
        "output": "YES\nNO",
        "explanation": "The first list is symmetric around the centre (9,8,7,7,8,9) → YES. The second list differs at the last element (1,2,2,3) → NO."
      },
      {
        "id": "sample_3",
        "input": "1\n15\n1 2 3 4 5 6 7 8 7 6 5 4 3 2 1",
        "output": "YES",
        "explanation": "Odd length palindrome with central element 8; the sequence reads identically from both ends."
      }
    ]
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "label": "minimum boundary",
        "input": "1\n1\n0",
        "expectedOutput": "YES",
        "weight": 2
      },
      {
        "id": "tc_2",
        "label": "maximum boundary (15 elements palindrome)",
        "input": "1\n15\n1 2 3 4 5 6 7 8 7 6 5 4 3 2 1",
        "expectedOutput": "YES",
        "weight": 2
      },
      {
        "id": "tc_3",
        "label": "edge case even non‑palindrome",
        "input": "1\n2\n1 2",
        "expectedOutput": "NO",
        "weight": 2
      },
      {
        "id": "tc_4",
        "label": "general case even palindrome",
        "input": "1\n8\n1 2 3 4 4 3 2 1",
        "expectedOutput": "YES",
        "weight": 2
      },
      {
        "id": "tc_5",
        "label": "general case odd non‑palindrome",
        "input": "1\n5\n1 2 3 4 5",
        "expectedOutput": "NO",
        "weight": 2
      }
    ],
    "totalTestCases": 5,
    "totalWeight": 10
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      2,
      2,
      2,
      2,
      2
    ],
    "maxScore": 10
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "For each test case we traverse the list with two pointers: 'slow' moves one step, 'fast' moves two steps. When 'fast' reaches the end, 'slow' is at the middle. We reverse the second half of the list starting from slow->next. Then we compare the first half and the reversed second half node by node. If all corresponding values match, the list is a palindrome. The reversal is done in‑place, so the algorithm uses O(1) extra memory. Optionally we could restore the original order, but it is not required for the answer.",
    "timeComplexity": "O(N) per test case – each node is visited a constant number of times (finding middle, reversing, comparing).",
    "spaceComplexity": "O(1) extra space – only a few pointer variables are used.",
    "code": {
      "C": "#include <stdio.h>\n#include <stdlib.h>\n\nstruct Node {\n    int val;\n    struct Node* next;\n};\n\nstruct Node* newNode(int v) {\n    struct Node* n = (struct Node*)malloc(sizeof(struct Node));\n    n->val = v;\n    n->next = NULL;\n    return n;\n}\n\nstruct Node* reverseList(struct Node* head) {\n    struct Node* prev = NULL;\n    while (head) {\n        struct Node* nxt = head->next;\n        head->next = prev;\n        prev = head;\n        head = nxt;\n    }\n    return prev;\n}\n\nint isPalindrome(struct Node* head) {\n    if (!head || !head->next) return 1;\n    struct Node *slow = head, *fast = head;\n    while (fast->next && fast->next->next) {\n        slow = slow->next;\n        fast = fast->next->next;\n    }\n    struct Node* second = reverseList(slow->next);\n    struct Node* first = head;\n    int ok = 1;\n    while (second) {\n        if (first->val != second->val) { ok = 0; break; }\n        first = first->next;\n        second = second->next;\n    }\n    // optional: restore list (not required)\n    return ok;\n}\n\nint main() {\n    int T; if (scanf(\"%d\", &T) != 1) return 0;\n    while (T--) {\n        int N; scanf(\"%d\", &N);\n        struct Node *head = NULL, *tail = NULL;\n        for (int i = 0; i < N; ++i) {\n            int x; scanf(\"%d\", &x);\n            struct Node* nd = newNode(x);\n            if (!head) head = tail = nd; else { tail->next = nd; tail = nd; }\n        }\n        printf(isPalindrome(head) ? \"YES\\n\" : \"NO\\n\");\n        while (head) { struct Node* tmp = head; head = head->next; free(tmp); }\n    }\n    return 0;\n}",
      "C++": "#include <bits/stdc++.h>\nusing namespace std;\n\nstruct Node {\n    int val;\n    Node* next;\n    Node(int v) : val(v), next(nullptr) {}\n};\n\nNode* reverseList(Node* head) {\n    Node* prev = nullptr;\n    while (head) {\n        Node* nxt = head->next;\n        head->next = prev;\n        prev = head;\n        head = nxt;\n    }\n    return prev;\n}\n\nbool isPalindrome(Node* head) {\n    if (!head || !head->next) return true;\n    Node *slow = head, *fast = head;\n    while (fast->next && fast->next->next) {\n        slow = slow->next;\n        fast = fast->next->next;\n    }\n    Node* second = reverseList(slow->next);\n    Node* first = head;\n    bool ok = true;\n    while (second) {\n        if (first->val != second->val) { ok = false; break; }\n        first = first->next;\n        second = second->next;\n    }\n    return ok;\n}\n\nint main() {\n    ios::sync_with_stdio(false);\n    cin.tie(nullptr);\n    int T; if(!(cin >> T)) return 0;\n    while (T--) {\n        int N; cin >> N;\n        Node *head = nullptr, *tail = nullptr;\n        for (int i = 0; i < N; ++i) {\n            int x; cin >> x;\n            Node* nd = new Node(x);\n            if (!head) head = tail = nd; else { tail->next = nd; tail = nd; }\n        }\n        cout << (isPalindrome(head) ? \"YES\" : \"NO\") << '\\n';\n        while (head) { Node* tmp = head; head = head->next; delete tmp; }\n    }\n    return 0;\n}",
      "Java": "import java.io.*;\nimport java.util.*;\n\npublic class Main {\n    static class Node {\n        int val;\n        Node next;\n        Node(int v) { val = v; }\n    }\n\n    static Node reverse(Node head) {\n        Node prev = null;\n        while (head != null) {\n            Node nxt = head.next;\n            head.next = prev;\n            prev = head;\n            head = nxt;\n        }\n        return prev;\n    }\n\n    static boolean isPalindrome(Node head) {\n        if (head == null || head.next == null) return true;\n        Node slow = head, fast = head;\n        while (fast.next != null && fast.next.next != null) {\n            slow = slow.next;\n            fast = fast.next.next;\n        }\n        Node second = reverse(slow.next);\n        Node first = head;\n        boolean ok = true;\n        while (second != null) {\n            if (first.val != second.val) { ok = false; break; }\n            first = first.next;\n            second = second.next;\n        }\n        return ok;\n    }\n\n    public static void main(String[] args) throws Exception {\n        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));\n        StringTokenizer st = new StringTokenizer(br.readLine());\n        int T = Integer.parseInt(st.nextToken());\n        StringBuilder sb = new StringBuilder();\n        while (T-- > 0) {\n            int N = Integer.parseInt(br.readLine().trim());\n            st = new StringTokenizer(br.readLine());\n            Node head = null, tail = null;\n            for (int i = 0; i < N; i++) {\n                int x = Integer.parseInt(st.nextToken());\n                Node nd = new Node(x);\n                if (head == null) head = tail = nd; else { tail.next = nd; tail = nd; }\n            }\n            sb.append(isPalindrome(head) ? \"YES\" : \"NO\").append('\\n');\n        }\n        System.out.print(sb.toString());\n    }\n}",
      "Python3": "#!/usr/bin/env python3\nimport sys\n\ndef is_palindrome(vals):\n    i, j = 0, len(vals) - 1\n    while i < j:\n        if vals[i] != vals[j]:\n            return False\n        i += 1\n        j -= 1\n    return True\n\ndef main():\n    data = sys.stdin.read().strip().split()\n    if not data:\n        return\n    it = iter(data)\n    T = int(next(it))\n    out = []\n    for _ in range(T):\n        N = int(next(it))\n        arr = [int(next(it)) for _ in range(N)]\n        out.append('YES' if is_palindrome(arr) else 'NO')\n    sys.stdout.write('\\n'.join(out))\n\nif __name__ == \"__main__\":\n    main()",
      "JavaScript": "const fs = require('fs');\nconst data = fs.readFileSync(0, 'utf-8').trim().split(/\\s+/).map(Number);\nlet idx = 0;\nconst T = data[idx++];\nlet results = [];\nfor (let t = 0; t < T; t++) {\n  const N = data[idx++];\n  const arr = data.slice(idx, idx + N);\n  idx += N;\n  let i = 0, j = N - 1, ok = true;\n  while (i < j) {\n    if (arr[i] !== arr[j]) { ok = false; break; }\n    i++; j--;\n  }\n  results.push(ok ? 'YES' : 'NO');\n}\nconsole.log(results.join('\\n'));"
    }
  },
  "QBCategory": "CCD",
  "boilerPlates": {
    "C": "#include <stdio.h>\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "C++": "#include <iostream>\nusing namespace std;\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "Java": "import java.util.*;\nimport java.io.*;\n\npublic class Main {\n    public static void main(String[] args) throws IOException {\n        // your code goes here\n    }\n}",
    "Python3": "# your code goes here\n",
    "JavaScript": "// your code goes here\n"
  },
  "_internal": {
    "methodName": "",
    "params": []
  }
}