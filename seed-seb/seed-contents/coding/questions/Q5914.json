{
  "questionId": "Q5914",
  "title": "Earthquake",
  "slug": "earthquake",
  "version": 1,
  "createdAt": "2026-07-01",
  "updatedAt": "2026-07-04",
  "metadata": {
    "category": "Arrays",
    "difficulty": "Hard",
    "isPremium": true,
    "tags": [
      "Segment Tree",
      "Rotation"
    ],
    "companyTags": [],
    "topicTags": [
      "Data Structures"
    ],
    "QBCategory": "CCD"
  },
  "content": {
    "problemStatement": "Earthquake in Bytetown! Situation is getting out of control!\n\nAll buildings in Bytetown stand on straight line. The buildings are numbered **0, 1, ..., N−1** from left to right.\n\n\n\n\nEvery hour one shake occurs. Each shake has **3** parameters: the leftmost building that was damaged during this shake, the corresponding rightmost building, and the force of the shake. Each time all the buildings in the disaster area are damaged equally. Let's consider this process in details.\n\nAt any moment every building has the number that indicates its height (may have leading zeroes). This number corresponds to some string consisting of digits. When some shake affects to the building its string circularly rotates to the left by the value of the force of the shake and its height corresponds to the value of new string. Pay attention that after rotation string may have leading zeroes. For instance: a building with height **950** got in disaster area with force **2**, Then its string become **095**, so height in reality is **95**. If it was one more shake with force **1**, then its height would become **950** again.\n\nMajor of Bytetown got some ideas how to protect residents, so sometimes he needs such kind of stats: find height of the highest building on some interval. You are given information about all the heights before the very first shake and then you get queries of two types: \n\n\nType 0, described by **0 Li Ri Fi**: Shake occurred on interval **[Li, Ri]** with force **Fi**. \nType 1, described by **1 Li Ri**: The major wants to know the biggest height on interval **[Li, Ri]**. \nHere, of course, the interval **[L, R]** contains all the building **k** such that **L ≤ k ≤ R**.\n\nYou want to get a promotion and promised to complete this task. Now it's time to do it!",
    "constraints": [
      "1 ≤ N ≤ 800000",
      "1 ≤ M ≤ 200000",
      "0 ≤ Ai < 10000",
      "0 ≤ Li ≤ Ri < N",
      "0 ≤ Fi ≤ 60"
    ],
    "inputFormat": "The first line contains an integer N, the number of buildings in Bytetown. The second line contains N space-separated integers A0, A1, ..., AN−1 denoting the initial height of the buildings. The third line contains an integer M denoting the number of queries. Each of the next M lines starts with 0 or 1, where 0 denotes a query Type 0 and 1 denotes a Type 1 query. If it's a Type 0 query then 3 integers follow Li, Ri, Fi, denoting the number of the leftmost building, the number of the rightmost building, and the force of this shake. If it's a Type 1 query then 2 integers follow Li, Ri, denoting the numbers of the leftmost building and the rightmost building of the necessary segment.",
    "outputFormat": "For each Type 1 query, output an integer denoting the answer for the query without leading zeroes.",
    "sampleTestCases": [
      {
        "id": "sample_1",
        "input": "3\n17 3140 832\n8\n1 0 2\n0 0 2 1\n1 1 2\n1 0 0\n0 0 2 2\n1 0 2\n0 1 1 1\n1 0 2",
        "output": "3140\n1403\n71\n832\n3140",
        "explanation": "The initial array is [17, 3140, 832]. The first query is a Type 1 query with numbers of buildings 0 and 2, so the answer is the maximum of the array: 3140. The second query is a Type 0 query with numbers of buildings 0 and 2, and force 1, so the array turns to [71, 1403, 328]. The third query is a Type 1 query again with numbers of buildings 1 and 2, so the answer is the maximum of 1403 and 328: 1403."
      }
    ]
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "label": "Case 1",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_2",
        "label": "Case 2",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_3",
        "label": "Case 3",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_4",
        "label": "Case 4",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_5",
        "label": "Case 5",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      }
    ],
    "totalTestCases": 5,
    "totalWeight": 10
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      2,
      2,
      2,
      2,
      2
    ],
    "maxScore": 10
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "To solve this problem, we can use a segment tree to store the heights of the buildings and the forces applied to them. When a query of Type 0 is made, we update the segment tree by applying the force to the corresponding buildings. When a query of Type 1 is made, we query the segment tree to find the maximum height of the buildings in the given range.",
    "timeComplexity": "O(M log N) per test case, O(T M log N) total. The time complexity is dominated by the queries and the updates to the segment tree.",
    "spaceComplexity": "O(N) — the dominant structure is the segment tree.",
    "code": {
      "C": "#include <stdio.h>\n#include <stdlib.h>\n#include <string.h>\n#define MAX_N 800000\n#define MAX_M 200000\n#define MAX_HEIGHT 10000\n\n// Segment tree node structure\ntypedef struct {\n    int height;\n    int force;\n} Node;\n\n// Function to create a new segment tree node\nNode* createNode(int height) {\n    Node* node = (Node*) malloc(sizeof(Node));\n    node->height = height;\n    node->force = 0;\n    return node;\n}\n\n// Function to update the segment tree\nvoid update(Node** tree, int node, int start, int end, int left, int right, int force) {\n    if (start > right || end < left) return;\n    if (start >= left && end <= right) {\n        tree[node]->force += force;\n        return;\n    }\n    int mid = (start + end) / 2;\n    update(tree, 2 * node, start, mid, left, right, force);\n    update(tree, 2 * node + 1, mid + 1, end, left, right, force);\n}\n\n// Function to query the segment tree\nint query(Node** tree, int node, int start, int end, int left, int right) {\n    if (start > right || end < left) return 0;\n    if (start >= left && end <= right) {\n        int height = tree[node]->height;\n        int force = tree[node]->force;\n        char str[5];\n        sprintf(str, \"%04d\", height);\n        char newStr[5];\n        for (int i = 0; i < 4; i++) {\n            newStr[(i + force) % 4] = str[i];\n        }\n        return atoi(newStr);\n    }\n    int mid = (start + end) / 2;\n    int leftMax = query(tree, 2 * node, start, mid, left, right);\n    int rightMax = query(tree, 2 * node + 1, mid + 1, end, left, right);\n    return (leftMax > rightMax) ? leftMax : rightMax;\n}\n\nint main() {\n    int T;\n    scanf(\"%d\", &T);\n    while (T--) {\n        int N;\n        scanf(\"%d\", &N);\n        Node** tree = (Node**) malloc(4 * N * sizeof(Node*));\n        for (int i = 0; i < N; i++) {\n            int height;\n            scanf(\"%d\", &height);\n            tree[i] = createNode(height);\n        }\n        int M;\n        scanf(\"%d\", &M);\n        for (int i = 0; i < M; i++) {\n            int type;\n            scanf(\"%d\", &type);\n            if (type == 0) {\n                int left, right, force;\n                scanf(\"%d %d %d\", &left, &right, &force);\n                update(tree, 1, 0, N - 1, left, right, force);\n            } else {\n                int left, right;\n                scanf(\"%d %d\", &left, &right);\n                int max = query(tree, 1, 0, N - 1, left, right);\n                printf(\"%d\\n\", max);\n            }\n        }\n    }\n    return 0;\n}",
      "C++": "#include <iostream>\n#include <vector>\nusing namespace std;\n#define MAX_N 800000\n#define MAX_M 200000\n#define MAX_HEIGHT 10000\n\n// Segment tree node structure\nstruct Node {\n    int height;\n    int force;\n};\n\n// Function to create a new segment tree node\nNode createNode(int height) {\n    Node node;\n    node.height = height;\n    node.force = 0;\n    return node;\n}\n\n// Function to update the segment tree\nvoid update(vector<Node>& tree, int node, int start, int end, int left, int right, int force) {\n    if (start > right || end < left) return;\n    if (start >= left && end <= right) {\n        tree[node].force += force;\n        return;\n    }\n    int mid = (start + end) / 2;\n    update(tree, 2 * node, start, mid, left, right, force);\n    update(tree, 2 * node + 1, mid + 1, end, left, right, force);\n}\n\n// Function to query the segment tree\nint query(const vector<Node>& tree, int node, int start, int end, int left, int right) {\n    if (start > right || end < left) return 0;\n    if (start >= left && end <= right) {\n        int height = tree[node].height;\n        int force = tree[node].force;\n        char str[5];\n        sprintf(str, \"%04d\", height);\n        char newStr[5];\n        for (int i = 0; i < 4; i++) {\n            newStr[(i + force) % 4] = str[i];\n        }\n        return atoi(newStr);\n    }\n    int mid = (start + end) / 2;\n    int leftMax = query(tree, 2 * node, start, mid, left, right);\n    int rightMax = query(tree, 2 * node + 1, mid + 1, end, left, right);\n    return (leftMax > rightMax) ? leftMax : rightMax;\n}\n\nint main() {\n    ios::sync_with_stdio(false);\n    cin.tie(NULL);\n    int T;\n    cin >> T;\n    while (T--) {\n        int N;\n        cin >> N;\n        vector<Node> tree(4 * N);\n        for (int i = 0; i < N; i++) {\n            int height;\n            cin >> height;\n            tree[i] = createNode(height);\n        }\n        int M;\n        cin >> M;\n        for (int i = 0; i < M; i++) {\n            int type;\n            cin >> type;\n            if (type == 0) {\n                int left, right, force;\n                cin >> left >> right >> force;\n                update(tree, 1, 0, N - 1, left, right, force);\n            } else {\n                int left, right;\n                cin >> left >> right;\n                int max = query(tree, 1, 0, N - 1, left, right);\n                cout << max << endl;\n            }\n        }\n    }\n    return 0;\n}",
      "Java": "import java.util.Scanner;\npublic class Main {\n    static class Node {\n        int height;\n        int force;\n        Node(int height) {\n            this.height = height;\n            this.force = 0;\n        }\n    }\n    public static void main(String[] args) {\n        Scanner scanner = new Scanner(System.in);\n        int T = scanner.nextInt();\n        while (T-- > 0) {\n            int N = scanner.nextInt();\n            Node[] tree = new Node[4 * N];\n            for (int i = 0; i < N; i++) {\n                int height = scanner.nextInt();\n                tree[i] = new Node(height);\n            }\n            int M = scanner.nextInt();\n            for (int i = 0; i < M; i++) {\n                int type = scanner.nextInt();\n                if (type == 0) {\n                    int left = scanner.nextInt();\n                    int right = scanner.nextInt();\n                    int force = scanner.nextInt();\n                    update(tree, 1, 0, N - 1, left, right, force);\n                } else {\n                    int left = scanner.nextInt();\n                    int right = scanner.nextInt();\n                    int max = query(tree, 1, 0, N - 1, left, right);\n                    System.out.println(max);\n                }\n            }\n        }\n    }\n    public static void update(Node[] tree, int node, int start, int end, int left, int right, int force) {\n        if (start > right || end < left) return;\n        if (start >= left && end <= right) {\n            tree[node].force += force;\n            return;\n        }\n        int mid = (start + end) / 2;\n        update(tree, 2 * node, start, mid, left, right, force);\n        update(tree, 2 * node + 1, mid + 1, end, left, right, force);\n    }\n    public static int query(Node[] tree, int node, int start, int end, int left, int right) {\n        if (start > right || end < left) return 0;\n        if (start >= left && end <= right) {\n            int height = tree[node].height;\n            int force = tree[node].force;\n            char[] str = new char[5];\n            String s = String.format(\"%04d\", height);\n            for (int i = 0; i < 4; i++) {\n                str[i] = s.charAt(i);\n            }\n            char[] newStr = new char[5];\n            for (int i = 0; i < 4; i++) {\n                newStr[(i + force) % 4] = str[i];\n            }\n            return Integer.parseInt(new String(newStr));\n        }\n        int mid = (start + end) / 2;\n        int leftMax = query(tree, 2 * node, start, mid, left, right);\n        int rightMax = query(tree, 2 * node + 1, mid + 1, end, left, right);\n        return (leftMax > rightMax) ? leftMax : rightMax;\n    }\n}",
      "Python3": "import sys\ninput = sys.stdin.readline\n\nclass Node:\n    def __init__(self, height):\n        self.height = height\n        self.force = 0\n\ndef update(tree, node, start, end, left, right, force):\n    if start > right or end < left:\n        return\n    if start >= left and end <= right:\n        tree[node].force += force\n        return\n    mid = (start + end) // 2\n    update(tree, 2 * node, start, mid, left, right, force)\n    update(tree, 2 * node + 1, mid + 1, end, left, right, force)\n\ndef query(tree, node, start, end, left, right):\n    if start > right or end < left:\n        return 0\n    if start >= left and end <= right:\n        height = tree[node].height\n        force = tree[node].force\n        str = '{:04d}'.format(height)\n        newStr = [''] * 4\n        for i in range(4):\n            newStr[(i + force) % 4] = str[i]\n        return int(''.join(newStr))\n    mid = (start + end) // 2\n    leftMax = query(tree, 2 * node, start, mid, left, right)\n    rightMax = query(tree, 2 * node + 1, mid + 1, end, left, right)\n    return max(leftMax, rightMax)\n\ntest_cases = int(input())\nfor _ in range(test_cases):\n    N = int(input())\n    tree = [Node(0) for _ in range(4 * N)]\n    heights = list(map(int, input().split()))\n    for i in range(N):\n        tree[i] = Node(heights[i])\n    M = int(input())\n    for _ in range(M):\n        type = int(input())\n        if type == 0:\n            left, right, force = map(int, input().split())\n            update(tree, 1, 0, N - 1, left, right, force)\n        else:\n            left, right = map(int, input().split())\n            max_height = query(tree, 1, 0, N - 1, left, right)\n            print(max_height)",
      "JavaScript": "const fs = require('fs');\nconst input = fs.readFileSync(0, 'utf-8').trim().split('\\n');\nlet idx = 0;\n\nclass Node {\n    constructor(height) {\n        this.height = height;\n        this.force = 0;\n    }\n}\n\nfunction update(tree, node, start, end, left, right, force) {\n    if (start > right || end < left) return;\n    if (start >= left && end <= right) {\n        tree[node].force += force;\n        return;\n    }\n    const mid = Math.floor((start + end) / 2);\n    update(tree, 2 * node, start, mid, left, right, force);\n    update(tree, 2 * node + 1, mid + 1, end, left, right, force);\n}\n\nfunction query(tree, node, start, end, left, right) {\n    if (start > right || end < left) return 0;\n    if (start >= left && end <= right) {\n        let height = tree[node].height;\n        let force = tree[node].force;\n        let str = height.toString().padStart(4, '0');\n        let newStr = '';\n        for (let i = 0; i < 4; i++) {\n            newStr += str[(i + force) % 4];\n        }\n        return parseInt(newStr);\n    }\n    const mid = Math.floor((start + end) / 2);\n    const leftMax = query(tree, 2 * node, start, mid, left, right);\n    const rightMax = query(tree, 2 * node + 1, mid + 1, end, left, right);\n    return Math.max(leftMax, rightMax);\n}\n\nconst T = parseInt(input[idx++]);\nfor (let i = 0; i < T; i++) {\n    const N = parseInt(input[idx++]);\n    const tree = new Array(4 * N).fill(0).map(() => new Node(0));\n    const heights = input[idx++].split(' ').map(Number);\n    for (let j = 0; j < N; j++) {\n        tree[j] = new Node(heights[j]);\n    }\n    const M = parseInt(input[idx++]);\n    for (let j = 0; j < M; j++) {\n        const type = parseInt(input[idx++]);\n        if (type === 0) {\n            const [left, right, force] = input[idx++].split(' ').map(Number);\n            update(tree, 1, 0, N - 1, left, right, force);\n        } else {\n            const [left, right] = input[idx++].split(' ').map(Number);\n            const max = query(tree, 1, 0, N - 1, left, right);\n            console.log(max);\n        }\n    }\n}"
    }
  },
  "QBCategory": "CCD",
  "boilerPlates": {
    "C": "#include <stdio.h>\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "C++": "#include <iostream>\nusing namespace std;\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "Java": "import java.util.*;\nimport java.io.*;\n\npublic class Main {\n    public static void main(String[] args) throws IOException {\n        // your code goes here\n    }\n}",
    "Python3": "# your code goes here\n",
    "JavaScript": "// your code goes here\n"
  },
  "_internal": {
    "methodName": "",
    "params": []
  }
}