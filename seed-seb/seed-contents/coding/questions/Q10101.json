{
  "questionId": "Q10101",
  "title": "Frequal",
  "slug": "frequal",
  "version": 1,
  "createdAt": "2026-07-02",
  "updatedAt": "2026-07-04",
  "metadata": {
    "category": "Number Theory | Greedy",
    "difficulty": "Medium",
    "isPremium": true,
    "tags": [
      "Divisor function",
      "Prime numbers",
      "Sieve of Eratosthenes",
      "Greedy construction"
    ],
    "companyTags": [],
    "topicTags": [
      "Constructive Algorithms"
    ],
    "QBCategory": "CCD"
  },
  "content": {
    "problemStatement": "An array AAA of length NNN is called frequal if, for each iii (1≤i≤N1 \\leq i \\leq N1≤i≤N), the frequency of AiA_iAi​ equals the number of its positive divisors.\n\nFor example, [2,8,1,8,8,8,2][2, 8, 1, 8, 8, 8, 2][2,8,1,8,8,8,2] is a frequal array (111 has one divisor, 222 has two divisors, and 888 has four divisors), while [10,4,10,4,4,10][10, 4, 10, 4, 4, 10][10,4,10,4,4,10] is not (101010 has four divisors but appears only three times in the array).\n\nLet f(A)f(A)f(A) denote the number of **distinct elements** in AAA.\n\nFor instance, f([1,1,1,1])=1f([1, 1, 1, 1]) = 1f([1,1,1,1])=1, and f([1,3,2,9,2,9,1])=4f([1, 3, 2, 9, 2, 9, 1]) = 4f([1,3,2,9,2,9,1])=4.\n\nYou are given an integer NNN.\n\nFind any frequal array AAA of length NNN such that f(A)f(A)f(A) is as large as possible.\n\nThe array should satisfy 1≤Ai≤1061 \\leq A_i \\leq 10^61≤Ai​≤106.\n\nIt can be shown that such a frequal array always exists.\n\n\nInput Format\n\nThe first line of input will contain a single integer TTT, denoting the number of test cases.\nEach test case consists of a single line of input, containing one integer NNN.\n\n\nOutput Format\nFor each test case, output on a new line NNN space-separated integers representing a frequal array of length NNN such that f(A)f(A)f(A) is maximal.\n\nEach integer in the output must lie between 111 and 10610^6106.\n\n\nIf there are multiple possible arrays satisfying this condition, you may print **any of them**.",
    "constraints": [
      "1 ≤ T ≤ 500",
      "1 ≤ N ≤ 2·10^4",
      "Sum of N over all test cases ≤ 2·10^5",
      "1 ≤ Ai ≤ 10^6"
    ],
    "inputFormat": "The first line contains an integer T, the number of test cases. Each of the next T lines contains a single integer N (1 ≤ N ≤ 2·10^4).",
    "outputFormat": "For each test case output a line with N space‑separated integers A1 … AN (1 ≤ Ai ≤ 10^6) forming a frequal array with the maximum possible number of distinct values.",
    "sampleTestCases": [
      {
        "id": "sample_1",
        "input": "2\n2\n8",
        "output": "2 2\n2 2 3 3 5 5 7 7",
        "explanation": "Test case 1: N=2 (even). Use one prime (2) twice → array [2,2]. Distinct count = 1 = ceil(2/2).\nTest case 2: N=8 (even). Use the first four primes each twice → [2,2,3,3,5,5,7,7]. Distinct count = 4 = ceil(8/2). Both arrays satisfy the frequal condition because each prime appears exactly twice, which equals its number of divisors."
      },
      {
        "id": "sample_2",
        "input": "3\n1\n3\n5",
        "output": "1\n1 2 2\n1 2 2 3 3",
        "explanation": "N=1 → only number 1 (τ(1)=1).\nN=3 (odd) → 1 once and one prime (2) twice.\nN=5 (odd) → 1 once and two primes (2,3) each twice."
      }
    ]
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "label": "minimum boundary",
        "input": "1\n1",
        "expectedOutput": "1",
        "weight": 2
      },
      {
        "id": "tc_2",
        "label": "Case 2",
        "input": "1\n1",
        "expectedOutput": "1",
        "weight": 2
      },
      {
        "id": "tc_3",
        "label": "Case 3",
        "input": "1\n1",
        "expectedOutput": "1",
        "weight": 2
      },
      {
        "id": "tc_4",
        "label": "Case 4",
        "input": "1\n1",
        "expectedOutput": "1",
        "weight": 2
      },
      {
        "id": "tc_5",
        "label": "Case 5",
        "input": "1\n1",
        "expectedOutput": "1",
        "weight": 2
      }
    ],
    "totalTestCases": 5,
    "totalWeight": 10
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      2,
      2,
      2,
      2,
      2
    ],
    "maxScore": 10
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "For a value x the required frequency is τ(x) – the number of its positive divisors. To maximise the number of distinct values we want the smallest possible τ(x). The smallest τ is 1 (only for x = 1) and the next smallest is 2 (for every prime). Using a number with τ = 2 adds two positions to the array and contributes one new distinct value, which is the best ratio possible after using 1. Therefore:\n* If N is even we can fill the whole array with N/2 distinct primes, each appearing exactly twice.\n* If N is odd we must also use the number 1 (τ(1)=1) once, and the remaining N‑1 positions are filled with (N‑1)/2 distinct primes, each twice.\nAll primes ≤ 10^6 are sufficient because we need at most 10^4 of them (N ≤ 2·10^4). A simple sieve up to 10^6 pre‑computes a list of primes. For each test case we output the required numbers in any order (the implementation prints 1 first when needed, then each prime twice). The construction is always frequal and uses the maximal possible distinct count, which equals ceil(N/2).",
    "timeComplexity": "Sieve: O(10^6 log log 10^6). For each test case O(N) to output the array. Overall O(10^6 + total N).",
    "spaceComplexity": "O(10^6) for the boolean sieve and the list of primes (≈78 498 primes).",
    "code": {
      "C": "#include <stdio.h>\n#include <stdlib.h>\n#define MAXV 1000000\nint main(){\n    int T; if(scanf(\"%d\",&T)!=1) return 0;\n    // sieve\n    char *isComp = calloc(MAXV+1, sizeof(char));\n    int *primes = malloc((MAXV/10)*sizeof(int)); // enough space\n    int pcnt = 0;\n    for(int i=2;i<=MAXV;i++) if(!isComp[i]){\n        primes[pcnt++] = i;\n        if((long long)i*i<=MAXV) for(int j=i*i;j<=MAXV;j+=i) isComp[j]=1;\n    }\n    while(T--){\n        int N; scanf(\"%d\",&N);\n        int idx=0;\n        if(N%2==1){\n            printf(\"1\");\n            idx=1;\n        }\n        int need = N/2; // number of primes needed\n        for(int i=0;i<need;i++){\n            if(idx) printf(\" \");\n            printf(\"%d %d\",primes[i],primes[i]);\n            idx=1;\n        }\n        printf(\"\\n\");\n    }\n    free(isComp); free(primes);\n    return 0;\n}",
      "C++": "#include <bits/stdc++.h>\nusing namespace std;\nint main(){\n    ios::sync_with_stdio(false);\n    cin.tie(nullptr);\n    const int MAXV = 1000000;\n    vector<bool> comp(MAXV+1,false);\n    vector<int> primes;\n    for(int i=2;i<=MAXV;i++) if(!comp[i]){\n        primes.push_back(i);\n        if(1LL*i*i<=MAXV) for(int j=i*i;j<=MAXV;j+=i) comp[j]=true;\n    }\n    int T; if(!(cin>>T)) return 0;\n    while(T--){\n        int N; cin>>N;\n        bool first=true;\n        if(N%2==1){\n            cout<<1; first=false;\n        }\n        int need=N/2;\n        for(int i=0;i<need;i++){\n            if(!first) cout<<' ';\n            cout<<primes[i]<<' '<<primes[i];\n            first=false;\n        }\n        cout<<'\\n';\n    }\n    return 0;\n}",
      "Java": "import java.io.*;\nimport java.util.*;\npublic class Main {\n    static final int MAXV = 1_000_000;\n    public static void main(String[] args) throws Exception {\n        FastScanner fs = new FastScanner(System.in);\n        int T = fs.nextInt();\n        // sieve\n        boolean[] comp = new boolean[MAXV + 1];\n        List<Integer> primes = new ArrayList<>();\n        for (int i = 2; i <= MAXV; i++) if (!comp[i]) {\n            primes.add(i);\n            if ((long) i * i <= MAXV) {\n                for (int j = i * i; j <= MAXV; j += i) comp[j] = true;\n            }\n        }\n        StringBuilder out = new StringBuilder();\n        while (T-- > 0) {\n            int N = fs.nextInt();\n            boolean first = true;\n            if (N % 2 == 1) {\n                out.append(1);\n                first = false;\n            }\n            int need = N / 2;\n            for (int i = 0; i < need; i++) {\n                if (!first) out.append(' ');\n                out.append(primes.get(i)).append(' ').append(primes.get(i));\n                first = false;\n            }\n            out.append('\\n');\n        }\n        System.out.print(out.toString());\n    }\n    // fast scanner\n    static class FastScanner {\n        private final InputStream in;\n        private final byte[] buffer = new byte[1 << 16];\n        private int ptr = 0, len = 0;\n        FastScanner(InputStream in) { this.in = in; }\n        private int readByte() throws IOException {\n            if (ptr >= len) { len = in.read(buffer); ptr = 0; if (len <= 0) return -1; }\n            return buffer[ptr++];\n        }\n        int nextInt() throws IOException {\n            int c, sign = 1, val = 0;\n            do { c = readByte(); } while (c <= ' ');\n            if (c == '-') { sign = -1; c = readByte(); }\n            while (c > ' ') { val = val * 10 + (c - '0'); c = readByte(); }\n            return val * sign;\n        }\n    }\n}",
      "Python3": "import sys\nMAXV = 10**6\nis_comp = bytearray(MAXV + 1)\nprimes = []\nfor i in range(2, MAXV + 1):\n    if not is_comp[i]:\n        primes.append(i)\n        if i * i <= MAXV:\n            step = i\n            start = i * i\n            is_comp[start:MAXV+1:step] = b'\\x01' * ((MAXV - start)//step + 1)\n\ndef solve():\n    data = sys.stdin.read().strip().split()\n    if not data:\n        return\n    it = iter(data)\n    T = int(next(it))\n    out_lines = []\n    for _ in range(T):\n        N = int(next(it))\n        parts = []\n        if N % 2 == 1:\n            parts.append('1')\n        need = N // 2\n        for i in range(need):\n            p = str(primes[i])\n            parts.append(p)\n            parts.append(p)\n        out_lines.append(' '.join(parts))\n    sys.stdout.write('\\n'.join(out_lines))\nif __name__ == \"__main__\":\n    solve()",
      "JavaScript": "const fs = require('fs');\nconst input = fs.readFileSync(0, 'utf-8').trim().split(/\\s+/).map(Number);\nlet idx = 0;\nconst T = input[idx++];\nconst MAXV = 1000000;\nconst isComp = new Uint8Array(MAXV + 1);\nconst primes = [];\nfor (let i = 2; i <= MAXV; i++) {\n  if (!isComp[i]) {\n    primes.push(i);\n    if (i * i <= MAXV) {\n      for (let j = i * i; j <= MAXV; j += i) isComp[j] = 1;\n    }\n  }\n}\nlet out = [];\nfor (let t = 0; t < T; t++) {\n  const N = input[idx++];\n  const parts = [];\n  if (N % 2 === 1) parts.push('1');\n  const need = Math.floor(N / 2);\n  for (let i = 0; i < need; i++) {\n    const p = primes[i].toString();\n    parts.push(p, p);\n  }\n  out.push(parts.join(' '));\n}\nconsole.log(out.join('\\n'));"
    }
  },
  "QBCategory": "CCD",
  "boilerPlates": {
    "C": "#include <stdio.h>\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "C++": "#include <iostream>\nusing namespace std;\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "Java": "import java.util.*;\nimport java.io.*;\n\npublic class Main {\n    public static void main(String[] args) throws IOException {\n        // your code goes here\n    }\n}",
    "Python3": "# your code goes here\n",
    "JavaScript": "// your code goes here\n"
  },
  "_internal": {
    "methodName": "",
    "params": []
  }
}