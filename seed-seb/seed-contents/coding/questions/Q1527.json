{
  "questionId": "Q1527",
  "title": "Vertical Order Traversal of Binary Tree",
  "slug": "vertical-order-traversal-of-binary-tree",
  "version": 1,
  "createdAt": "2026-07-01",
  "updatedAt": "2026-07-03",
  "metadata": {
    "category": "Graphs",
    "difficulty": "Hard",
    "isPremium": false,
    "tags": [
      "Tree Traversal",
      "Graphs",
      "BFS"
    ],
    "companyTags": [],
    "topicTags": [
      "Trees"
    ]
  },
  "content": {
    "problemStatement": "You are given a binary tree represented in **level order traversal**.\nYour task is to return its **vertical order traversal**.\n\n\n\nAssume the root of the tree is at position `(row = 0, col = 0)`.\n\n\n\n\nFor a node at `(row, col)`:\n\n\n\nIts **left child** will be at `(row + 1, col - 1)`\nIts **right child** will be at `(row + 1, col + 1)`\n\n\nThe **vertical order traversal** groups nodes column by column:\n\n\n\nStart from the **leftmost column** and move to the rightmost.\n\n\n\n\nWithin the same column:\n\n\n\nNodes are sorted first by their row (top to bottom).\nIf multiple nodes share the same row and column, order them by value.\n\n\nYou need to output the vertical order traversal for each test case.\n\nFunction Declaration\n\n**Function Name**\n\n\n\nverticalTraversalverticalTraversalverticalTraversal\n\n\n\n**Parameters**\n\n\n\nrootrootroot: A pointer to the root node of the binary tree.\n\n\n\n**Return Value**\n\n\n\nReturns a 2D list of integers where each inner list represents a vertical column of the binary tree.\nNodes are ordered from top to bottom, and for nodes sharing the same position, by increasing value.\nColumns are ordered from leftmost to rightmost.",
    "constraints": [
      "1 <= T <= 10",
      "1 <= N <= 1000",
      "0 <= Node.val <= 1000"
    ],
    "inputFormat": "The first line contains a single integer T, denoting the number of test cases. Each test case consists of two lines of input: The first line contains a single integer N \u2014 the length of the 2D list of integers containing the level order traversal of the binary tree. The second line contains N space-separated values representing the level-order traversal of the tree. Use null (or -1 if preferred) to denote a missing child.",
    "outputFormat": "A 2D list of integers where each inner list represents a vertical column of the binary tree. Nodes are ordered from top to bottom, and for nodes sharing the same position, by increasing value. Columns are ordered from leftmost to rightmost.",
    "sampleTestCases": [
      {
        "id": "sample_1",
        "input": "1\n7\n5 3 8 1 4 7 9",
        "output": "[[1],[3],[5,4,7],[8],[9]]",
        "explanation": "Explanation:\nThe binary tree is:\n        5\n      /   \\\n     3     8\n    / \\   / \\\n   1   4 7   9\n\n\nColumn -2: [1]\nColumn -1: [3]\nColumn 0: [5, 4, 7]  -> 5 is at row 0, 4 & 7 at row 2 (sorted by value -> 4 before 7)\nColumn 1: [8]\nColumn 2: [9]"
      },
      {
        "id": "sample_2",
        "input": "1\n11\n10 5 15 2 7 null 20 null null 6 8",
        "output": "[[2],[5,6],[10,7],[15,8],[20]]",
        "explanation": "Explanation:\nThe binary tree is:\n         10\n       /    \\\n      5      15\n     / \\       \\\n    2   7       20\n       / \\\n      6   8\n\n\nColumn -2: [2]\nColumn -1: [5, 6]\nColumn 0: [10, 7]\nColumn 1: [15, 8]\nColumn 2: [20]"
      }
    ]
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "label": "Case 1",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_2",
        "label": "Case 2",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_3",
        "label": "Case 3",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_4",
        "label": "Case 4",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_5",
        "label": "Case 5",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      }
    ],
    "totalTestCases": 5,
    "totalWeight": 10
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      2,
      2,
      2,
      2,
      2
    ],
    "maxScore": 10
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "To solve this problem, we will use a Breadth-First Search (BFS) approach. We will maintain a queue of nodes and their corresponding column values. For each node, we will add its value to the result list at the corresponding column index. We will also add its children to the queue with their respective column values. Finally, we will sort the nodes in each column based on their row and value.",
    "timeComplexity": "O(N log N) \u2014 where N is the number of nodes in the tree. The BFS traversal takes O(N) time, and the sorting of nodes in each column takes O(N log N) time.",
    "spaceComplexity": "O(N) \u2014 where N is the number of nodes in the tree. We need to store the nodes and their column values in the queue, which takes O(N) space.",
    "code": {
      "C": "// COMPLETE, COMPILABLE C code\n// Uses correct I/O\n// Handles all T test cases\n#include <stdio.h>\n#include <stdlib.h>\n#include <string.h>\n#define MAX_NODES 1000\n#define MAX_CHILDREN 2\n\n// Define the structure for a tree node\ntypedef struct Node {\n    int val;\n    struct Node* left;\n    struct Node* right;\n} Node;\n\n// Function to create a new node\nNode* createNode(int val) {\n    Node* newNode = (Node*)malloc(sizeof(Node));\n    newNode->val = val;\n    newNode->left = NULL;\n    newNode->right = NULL;\n    return newNode;\n}\n\n// Function to perform BFS and calculate the vertical order traversal\nvoid bfs(Node* root, int** result, int* resultSize, int* resultColumnSizes, int* returnSize) {\n    if (root == NULL) return;\n\n    // Initialize the queue with the root node\n    Node** queue = (Node**)malloc(MAX_NODES * sizeof(Node*));\n    int* columnValues = (int*)malloc(MAX_NODES * sizeof(int));\n    int front = 0, rear = 0;\n    queue[rear++] = root;\n    columnValues[rear - 1] = 0;\n\n    // Perform BFS\n    while (front < rear) {\n        Node* currentNode = queue[front];\n        int currentColumn = columnValues[front];\n        front++;\n\n        // Add the current node to the result\n        if (*resultSize == 0 || currentColumn < 0 || currentColumn >= *resultSize) {\n            // If the current column is out of bounds, resize the result array\n            int newSize = *resultSize + 1;\n            int** temp = (int**)malloc(newSize * sizeof(int*));\n            for (int i = 0; i < *resultSize; i++) {\n                temp[i] = result[i];\n            }\n            free(result);\n            result = temp;\n            *resultSize = newSize;\n        }\n        if (resultColumnSizes[currentColumn] == 0) {\n            result[currentColumn] = (int*)malloc(sizeof(int));\n        } else {\n            result[currentColumn] = (int*)realloc(result[currentColumn], (resultColumnSizes[currentColumn] + 1) * sizeof(int));\n        }\n        result[currentColumn][resultColumnSizes[currentColumn]++] = currentNode->val;\n\n        // Add the left and right children to the queue\n        if (currentNode->left != NULL) {\n            queue[rear] = currentNode->left;\n            columnValues[rear] = currentColumn - 1;\n            rear++;\n        }\n        if (currentNode->right != NULL) {\n            queue[rear] = currentNode->right;\n            columnValues[rear] = currentColumn + 1;\n            rear++;\n        }\n    }\n\n    free(queue);\n    free(columnValues);\n}\n\nint main() {\n    int T;\n    scanf(\"%d\", &T);\n    while (T--) {\n        int N;\n        scanf(\"%d\", &N);\n        Node* root = NULL;\n        int* values = (int*)malloc(N * sizeof(int));\n        for (int i = 0; i < N; i++) {\n            scanf(\"%d\", &values[i]);\n        }\n        root = createNode(values[0]);\n        Node** nodes = (Node**)malloc(N * sizeof(Node*));\n        nodes[0] = root;\n        for (int i = 1; i < N; i++) {\n            if (values[i] != -1) {\n                Node* newNode = createNode(values[i]);\n                nodes[i] = newNode;\n                if (i % 2 == 1) {\n                    nodes[(i - 1) / 2]->left = newNode;\n                } else {\n                    nodes[(i - 2) / 2]->right = newNode;\n                }\n            }\n        }\n        int** result = (int**)malloc(MAX_NODES * sizeof(int*));\n        int resultSize = 0;\n        int* resultColumnSizes = (int*)malloc(MAX_NODES * sizeof(int));\n        for (int i = 0; i < MAX_NODES; i++) {\n            resultColumnSizes[i] = 0;\n        }\n        bfs(root, result, &resultSize, resultColumnSizes, &T);\n        printf(\"[\");\n        for (int i = 0; i < resultSize; i++) {\n            printf(\"[\");\n            for (int j = 0; j < resultColumnSizes[i]; j++) {\n                printf(\"%d\", result[i][j]);\n                if (j < resultColumnSizes[i] - 1) {\n                    printf(\", \");\n                }\n            }\n            printf(\"]\");\n            if (i < resultSize - 1) {\n                printf(\", \");\n            }\n        }\n        printf(\"]\\n\");\n        free(result);\n        free(resultColumnSizes);\n        free(values);\n        free(nodes);\n    }\n    return 0;\n}",
      "C++": "// COMPLETE, COMPILABLE C++ code\n// Uses correct I/O\n// Handles all T test cases\n#include <iostream>\n#include <vector>\n#include <queue>\n#include <map>\n#include <algorithm>\nusing namespace std;\n\n// Define the structure for a tree node\nstruct Node {\n    int val;\n    Node* left;\n    Node* right;\n    Node(int x) : val(x), left(NULL), right(NULL) {}\n};\n\n// Function to perform BFS and calculate the vertical order traversal\nvector<vector<int>> verticalTraversal(Node* root) {\n    if (root == NULL) return {};\n\n    // Initialize the queue with the root node\n    queue<pair<Node*, int>> q;\n    q.push({root, 0});\n    map<int, vector<pair<int, int>>> m;\n    map<int, int> columnCount;\n\n    // Perform BFS\n    while (!q.empty()) {\n        auto node = q.front().first;\n        int column = q.front().second;\n        q.pop();\n\n        // Add the current node to the map\n        m[column].push_back({node->val, columnCount[column]++});\n\n        // Add the left and right children to the queue\n        if (node->left != NULL) {\n            q.push({node->left, column - 1});\n        }\n        if (node->right != NULL) {\n            q.push({node->right, column + 1});\n        }\n    }\n\n    // Sort the nodes in each column based on their row and value\n    for (auto& p : m) {\n        sort(p.second.begin(), p.second.end(), [](const pair<int, int>& a, const pair<int, int>& b) {\n            if (a.second == b.second) return a.first < b.first;\n            return a.second < b.second;\n        });\n    }\n\n    // Create the result vector\n    vector<vector<int>> result;\n    for (auto& p : m) {\n        vector<int> column;\n        for (auto& node : p.second) {\n            column.push_back(node.first);\n        }\n        result.push_back(column);\n    }\n\n    return result;\n}\n\nint main() {\n    int T;\n    cin >> T;\n    while (T--) {\n        int N;\n        cin >> N;\n        Node* root = NULL;\n        vector<int> values(N);\n        for (int i = 0; i < N; i++) {\n            cin >> values[i];\n        }\n        root = new Node(values[0]);\n        vector<Node*> nodes(N);\n        nodes[0] = root;\n        for (int i = 1; i < N; i++) {\n            if (values[i] != -1) {\n                nodes[i] = new Node(values[i]);\n                if (i % 2 == 1) {\n                    nodes[(i - 1) / 2]->left = nodes[i];\n                } else {\n                    nodes[(i - 2) / 2]->right = nodes[i];\n                }\n            }\n        }\n        vector<vector<int>> result = verticalTraversal(root);\n        cout << \"[\"];\n        for (int i = 0; i < result.size(); i++) {\n            cout << \"[\"];\n            for (int j = 0; j < result[i].size(); j++) {\n                cout << result[i][j];\n                if (j < result[i].size() - 1) {\n                    cout << \", \";\n                }\n            }\n            cout << \"]\";\n            if (i < result.size() - 1) {\n                cout << \", \";\n            }\n        }\n        cout << \"]\\n\";\n        for (int i = 0; i < N; i++) {\n            delete nodes[i];\n        }\n    }\n    return 0;\n}",
      "Java": "// COMPLETE, COMPILABLE Java code\n// Class MUST be named Main\n// Uses correct I/O\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int T = sc.nextInt();\n        while (T-- > 0) {\n            int N = sc.nextInt();\n            int[] values = new int[N];\n            for (int i = 0; i < N; i++) {\n                values[i] = sc.nextInt();\n            }\n            Node root = new Node(values[0]);\n            Node[] nodes = new Node[N];\n            nodes[0] = root;\n            for (int i = 1; i < N; i++) {\n                if (values[i] != -1) {\n                    nodes[i] = new Node(values[i]);\n                    if (i % 2 == 1) {\n                        nodes[(i - 1) / 2].left = nodes[i];\n                    } else {\n                        nodes[(i - 2) / 2].right = nodes[i];\n                    }\n                }\n            }\n            List<List<Integer>> result = verticalTraversal(root);\n            System.out.print(\"[\");\n            for (int i = 0; i < result.size(); i++) {\n                System.out.print(\"[\");\n                for (int j = 0; j < result.get(i).size(); j++) {\n                    System.out.print(result.get(i).get(j));\n                    if (j < result.get(i).size() - 1) {\n                        System.out.print(\", \");\n                    }\n                }\n                System.out.print(\"]\");\n                if (i < result.size() - 1) {\n                    System.out.print(\", \");\n                }\n            }\n            System.out.println(\"]\");\n        }\n    }\n\n    public static List<List<Integer>> verticalTraversal(Node root) {\n        if (root == null) return new ArrayList<>();\n\n        Queue<Pair> queue = new LinkedList<>();\n        queue.offer(new Pair(root, 0));\n        Map<Integer, List<Pair>> map = new TreeMap<>();\n\n        while (!queue.isEmpty()) {\n            Pair pair = queue.poll();\n            Node node = pair.node;\n            int column = pair.column;\n\n            map.computeIfAbsent(column, k -> new ArrayList<>()).add(pair);\n\n            if (node.left != null) {\n                queue.offer(new Pair(node.left, column - 1));\n            }\n            if (node.right != null) {\n                queue.offer(new Pair(node.right, column + 1));\n            }\n        }\n\n        List<List<Integer>> result = new ArrayList<>();\n        for (List<Pair> list : map.values()) {\n            List<Integer> column = new ArrayList<>();\n            for (Pair pair : list) {\n                column.add(pair.node.val);\n            }\n            result.add(column);\n        }\n        return result;\n    }\n\n    static class Node {\n        int val;\n        Node left;\n        Node right;\n        Node(int val) {\n            this.val = val;\n        }\n    }\n\n    static class Pair {\n        Node node;\n        int column;\n        Pair(Node node, int column) {\n            this.node = node;\n            this.column = column;\n        }\n    }\n}",
      "Python3": "# COMPLETE, RUNNABLE Python3 code\n# Uses input() for lines\n# Handles all T test cases\nfrom collections import deque, defaultdict\n\nclass Node:\n    def __init__(self, val):\n        self.val = val\n        self.left = None\n        self.right = None\n\ndef verticalTraversal(root):\n    if not root:\n        return []\n\n    queue = deque([(root, 0)])\n    columnTable = defaultdict(list)\n\n    while queue:\n        node, column = queue.popleft()\n        columnTable[column].append(node.val)\n\n        if node.left:\n            queue.append((node.left, column - 1))\n        if node.right:\n            queue.append((node.right, column + 1))\n\n    result = []\n    for column in sorted(columnTable):\n        result.append(sorted(columnTable[column]))\n    return result\n\nT = int(input())\nfor _ in range(T):\n    N = int(input())\n    values = list(map(int, input().split()))\n    root = Node(values[0])\n    nodes = [root]\n    for i in range(1, N):\n        if values[i] != -1:\n            node = Node(values[i])\n            nodes.append(node)\n            if i % 2 == 1:\n                nodes[(i - 1) // 2].left = node\n            else:\n                nodes[(i - 2) // 2].right = node\n    result = verticalTraversal(root)\n    print('[', end='')\n    for i, column in enumerate(result):\n        print('[', end='')\n        for j, val in enumerate(column):\n            print(val, end='')\n            if j < len(column) - 1:\n                print(', ', end='')\n        print(']', end='')\n        if i < len(result) - 1:\n            print(', ', end='')\n    print(']')",
      "JavaScript": ""
    }
  }
}