{
  "questionId": "Q8262",
  "title": "Javelin Qualification",
  "slug": "javelin-qualification",
  "version": 1,
  "createdAt": "2026-07-02",
  "updatedAt": "2026-07-04",
  "metadata": {
    "category": "Greedy | Sorting",
    "difficulty": "Medium",
    "isPremium": true,
    "tags": [
      "Greedy",
      "Sorting"
    ],
    "companyTags": [],
    "topicTags": [
      "Ad-hoc & Implementation"
    ],
    "QBCategory": "CCD"
  },
  "content": {
    "problemStatement": "There are NNN players with IDs from 111 to NNN, who are participating in the Javelin throw competition which has two rounds. The first is the qualification round, followed by the final round. The qualification round has gotten over, and you are given the longest distance that each of the NNN players has thrown as A1,A2,…,ANA_1, A_2, \\ldots, A_NA1​,A2​,…,AN​. Now, the selection process for the final round happens in the following two steps:\n\n\n\nIf the longest throw of a player in the qualification round is greater than or equal to the qualification mark of MMM cm, they qualify for the final round.\n\n\n\n\nIf after step 111, less than XXX players have qualified for the finals, the remaining spots are filled by players who have thrown the maximum distance among the players who have not qualified yet.\n\n\n\nYou are given the best throws of the NNN players in the qualification round A1,A2,…,ANA_1, A_2, \\ldots, A_NA1​,A2​,…,AN​ and the integers MMM and XXX. Print the list of the players who will qualify for the finals in increasing order of their IDs.\n\n\nInput Format\n\nThe first line of input contains a single integer TTT denoting the number of test cases. The description of TTT test cases follows.\nThe first line of each test case contains three space-separated integers N,M,XN, M, XN,M,X.\nThe second line of each test case contains NNN space-separated integers A1,A2,…,ANA_1, A_2, \\dots, A_NA1​,A2​,…,AN​.\n\n\nOutput Format\n\nFor each test case, print a single line containing K+1K + 1K+1 space-separated integers, where KKK is the number of players qualified.\nFirst, print the integer KKK, then followed by a space, print KKK space-separated integers ID1,ID2,…,IDKID_1, ID_2, \\dots, ID_KID1​,ID2​,…,IDK​ where IDiID_iIDi​ denotes the players' ID who qualified for the finals.\nYou have to print the IDs of the qualified players in increasing order.",
    "constraints": [
      "1 ≤ T ≤ 1000",
      "2 ≤ X ≤ N ≤ 30",
      "5000 ≤ Ai ≤ 8000",
      "7000 ≤ M ≤ 8000",
      "All Ai are distinct"
    ],
    "inputFormat": "The first line contains an integer T, the number of test cases. Each test case consists of two lines. The first line contains three space‑separated integers N, M and X — the number of players, the qualification mark (in cm) and the minimum number of finalists required. The second line contains N distinct integers A1 … AN, where Ai is the longest throw of the player with ID i (players are numbered from 1 to N).",
    "outputFormat": "For each test case output one line. First print K, the total number of players who qualify for the final, followed by K space‑separated player IDs in increasing order.",
    "sampleTestCases": [
      {
        "id": "sample_1",
        "input": "3\n3 8000 2\n5000 5001 5002\n3 5000 2\n7999 7998 8000\n4 6000 3\n5999 5998 6000 6001",
        "output": "2 2 3\n3 1 2 3\n3 1 3 4",
        "explanation": "Explanation:\nTest Case 111: Since no player crosses the qualification mark, they are chosen based on the distance thrown. So player 333 who has thrown the maximum distance gets selected followed by the player 222. Now since we have got the required number of players, player 111 isn't selected.\nTest Case 222: Since all the players cross the qualification mark, they all are selected.\nTest Case 333: The player 333 and player 444 cross the qualification mark. So for the third and final spot in the final, the player 111 is selected since he has the maximum distance thrown amongst the remaining two players."
      }
    ]
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "label": "Case 1",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_2",
        "label": "Case 2",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_3",
        "label": "Case 3",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_4",
        "label": "Case 4",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_5",
        "label": "Case 5",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      }
    ],
    "totalTestCases": 5,
    "totalWeight": 10
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      2,
      2,
      2,
      2,
      2
    ],
    "maxScore": 10
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "For each test case we first select all players whose throw is at least M. If their count is already ≥ X we are done. Otherwise we need (X‑count) more players. Among the remaining players we pick those with the largest throws – this can be done by sorting the remaining players in descending order of distance and taking the required number. Finally we sort the selected IDs in ascending order for output. The algorithm is O(N log N) per test case because of the sorting step, and uses O(N) extra memory.",
    "timeComplexity": "O(N log N) per test case",
    "spaceComplexity": "O(N) per test case",
    "code": {
      "C": "#include <stdio.h>\n#include <stdlib.h>\n\nint cmp_desc(const void *a, const void *b) {\n    int da = ((int*)a)[0];\n    int db = ((int*)b)[0];\n    return db - da; // descending by distance\n}\n\nint main(){\n    int T; if(scanf(\"%d\", &T)!=1) return 0;\n    while(T--){\n        int N, M, X; scanf(\"%d %d %d\", &N, &M, &X);\n        int *dist = (int*)malloc((N+1)*sizeof(int));\n        for(int i=1;i<=N;i++) scanf(\"%d\", &dist[i]);\n        int *qual = (int*)malloc(N*sizeof(int));\n        int qcnt = 0;\n        int (*rem)[2] = malloc(N * sizeof *rem);\n        int rcnt = 0;\n        for(int i=1;i<=N;i++){\n            if(dist[i] >= M) qual[qcnt++] = i;\n            else { rem[rcnt][0] = dist[i]; rem[rcnt][1] = i; rcnt++; }\n        }\n        if(qcnt < X){\n            qsort(rem, rcnt, sizeof *rem, cmp_desc);\n            int need = X - qcnt;\n            for(int i=0;i<need;i++) qual[qcnt++] = rem[i][1];\n        }\n        // sort IDs ascending (simple insertion sort, N ≤ 30)\n        for(int i=1;i<qcnt;i++){\n            int key = qual[i];\n            int j=i-1;\n            while(j>=0 && qual[j]>key){qual[j+1]=qual[j];j--;}\n            qual[j+1]=key;\n        }\n        printf(\"%d\", qcnt);\n        for(int i=0;i<qcnt;i++) printf(\" %d\", qual[i]);\n        if(T) printf(\"\\n\");\n        free(dist); free(qual); free(rem);\n    }\n    return 0;\n}",
      "C++": "#include <bits/stdc++.h>\nusing namespace std;\nint main(){\n    ios::sync_with_stdio(false);\n    cin.tie(nullptr);\n    int T; if(!(cin>>T)) return 0;\n    while(T--){\n        int N,M,X; cin>>N>>M>>X;\n        vector<int> A(N+1);\n        for(int i=1;i<=N;i++) cin>>A[i];\n        vector<int> qualified;\n        vector<pair<int,int>> others;\n        for(int i=1;i<=N;i++){\n            if(A[i]>=M) qualified.push_back(i);\n            else others.push_back({A[i],i});\n        }\n        if((int)qualified.size()<X){\n            sort(others.begin(), others.end(), [&](auto &p1, auto &p2){return p1.first>p2.first;});\n            int need = X - qualified.size();\n            for(int i=0;i<need;i++) qualified.push_back(others[i].second);\n        }\n        sort(qualified.begin(), qualified.end());\n        cout<<qualified.size();\n        for(int id:qualified) cout<<' '<<id;\n        if(T) cout<<'\\n';\n    }\n    return 0;\n}",
      "Java": "import java.io.*;\nimport java.util.*;\n\npublic class Main {\n    public static void main(String[] args) throws Exception {\n        FastScanner fs = new FastScanner(System.in);\n        int T = fs.nextInt();\n        StringBuilder sb = new StringBuilder();\n        while (T-- > 0) {\n            int N = fs.nextInt();\n            int M = fs.nextInt();\n            int X = fs.nextInt();\n            int[] A = new int[N+1];\n            for (int i = 1; i <= N; i++) A[i] = fs.nextInt();\n            List<Integer> qualified = new ArrayList<>();\n            List<int[]> others = new ArrayList<>();\n            for (int i = 1; i <= N; i++) {\n                if (A[i] >= M) qualified.add(i);\n                else others.add(new int[]{A[i], i});\n            }\n            if (qualified.size() < X) {\n                others.sort((a,b) -> Integer.compare(b[0], a[0])); // descending distance\n                int need = X - qualified.size();\n                for (int i = 0; i < need; i++) qualified.add(others.get(i)[1]);\n            }\n            Collections.sort(qualified);\n            sb.append(qualified.size());\n            for (int id : qualified) sb.append(' ').append(id);\n            if (T > 0) sb.append('\\n');\n        }\n        System.out.print(sb.toString());\n    }\n\n    // Fast scanner for competitive programming\n    static class FastScanner {\n        private final InputStream in;\n        private final byte[] buffer = new byte[1 << 16];\n        private int ptr = 0, len = 0;\n        FastScanner(InputStream in) { this.in = in; }\n        private int readByte() throws IOException {\n            if (ptr >= len) {\n                len = in.read(buffer);\n                ptr = 0;\n                if (len <= 0) return -1;\n            }\n            return buffer[ptr++];\n        }\n        int nextInt() throws IOException {\n            int c, sign = 1, val = 0;\n            do { c = readByte(); } while (c <= ' ');\n            if (c == '-') { sign = -1; c = readByte(); }\n            while (c > ' ') { val = val * 10 + (c - '0'); c = readByte(); }\n            return val * sign;\n        }\n    }\n}",
      "Python3": "#!/usr/bin/env python3\nimport sys\n\ndef solve():\n    data = sys.stdin.read().strip().split()\n    if not data:\n        return\n    it = iter(data)\n    T = int(next(it))\n    out_lines = []\n    for _ in range(T):\n        N = int(next(it)); M = int(next(it)); X = int(next(it))\n        A = [0] + [int(next(it)) for _ in range(N)]\n        qualified = []\n        others = []\n        for i in range(1, N+1):\n            if A[i] >= M:\n                qualified.append(i)\n            else:\n                others.append((A[i], i))\n        if len(qualified) < X:\n            others.sort(key=lambda x: -x[0])\n            need = X - len(qualified)\n            qualified.extend(i for _, i in others[:need])\n        qualified.sort()\n        out_lines.append(str(len(qualified)) + ' ' + ' '.join(map(str, qualified)))\n    sys.stdout.write('\\n'.join(out_lines))\n\nif __name__ == \"__main__\":\n    solve()\n",
      "JavaScript": "const fs = require('fs');\nconst input = fs.readFileSync(0, 'utf-8').trim().split(/\\s+/).map(Number);\nlet idx = 0;\nconst T = input[idx++];\nlet outputs = [];\nfor (let t = 0; t < T; t++) {\n  const N = input[idx++];\n  const M = input[idx++];\n  const X = input[idx++];\n  const A = new Array(N + 1);\n  for (let i = 1; i <= N; i++) A[i] = input[idx++];\n  let qualified = [];\n  let others = [];\n  for (let i = 1; i <= N; i++) {\n    if (A[i] >= M) qualified.push(i);\n    else others.push([A[i], i]);\n  }\n  if (qualified.length < X) {\n    others.sort((a, b) => b[0] - a[0]); // descending distance\n    const need = X - qualified.length;\n    for (let i = 0; i < need; i++) qualified.push(others[i][1]);\n  }\n  qualified.sort((a, b) => a - b);\n  outputs.push(qualified.length + ' ' + qualified.join(' '));\n}\nconsole.log(outputs.join('\\n'));\n"
    }
  },
  "QBCategory": "CCD",
  "boilerPlates": {
    "C": "#include <stdio.h>\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "C++": "#include <iostream>\nusing namespace std;\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "Java": "import java.util.*;\nimport java.io.*;\n\npublic class Main {\n    public static void main(String[] args) throws IOException {\n        // your code goes here\n    }\n}",
    "Python3": "# your code goes here\n",
    "JavaScript": "// your code goes here\n"
  },
  "_internal": {
    "methodName": "",
    "params": []
  }
}