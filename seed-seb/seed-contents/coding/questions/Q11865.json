{
  "questionId": "Q11865",
  "title": "Letter Combinations of a Phone Number",
  "slug": "letter-combinations-of-a-phone-number",
  "version": 1,
  "createdAt": "2026-07-05",
  "updatedAt": "2026-07-05",
  "metadata": {
    "category": "String Manipulation",
    "difficulty": "Hard",
    "isPremium": true,
    "tags": [
      "Backtracking",
      "Recursion"
    ],
    "companyTags": [],
    "topicTags": [],
    "QBCategory": "CCD"
  },
  "content": {
    "problemStatement": "Given a string consisting of digits from 2 to 9 (inclusive). Return all possible letter combinations that the number can represent.",
    "constraints": [
      "1 <= digits.length <= 4",
      "digits consists only of digits from 2 to 9 (inclusive)"
    ],
    "inputFormat": "A string of digits from 2 to 9 (inclusive)",
    "outputFormat": "A list of all possible letter combinations that the number can represent",
    "sampleTestCases": [
      {
        "id": "sample_1",
        "input": "digits = \"34\"",
        "output": "[ \"dg\", \"dh\", \"di\", \"eg\", \"eh\", \"ei\", \"fg\", \"fh\", \"fi\" ]",
        "explanation": "The 3 is mapped with \"def\" and 4 is mapped with \"ghi\".  \nSo all possible combinations by replacing the digits with characters are shown in the output.\n\n**Example 2:**"
      },
      {
        "id": "sample_2",
        "input": "digits = \"3\"",
        "output": "[ \"d\", \"e\", \"f\" ]",
        "explanation": "The 3 is mapped with \"def\".\n        \n    \n\n\n**Disclaimer**: Here is the practice link to help you assess your knowledge better. It's highly recommend trying to solve it before looking at the solution."
      }
    ]
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "label": "min boundary",
        "input": "2",
        "expectedOutput": [
          "a",
          "b",
          "c"
        ],
        "weight": 2
      },
      {
        "id": "tc_2",
        "label": "max boundary",
        "input": "234",
        "expectedOutput": [
          "adg",
          "adh",
          "adi",
          "aeg",
          "aeh",
          "aei",
          "afg",
          "afh",
          "afi",
          "bdg",
          "bdh",
          "bdi",
          "beg",
          "beh",
          "bei",
          "bfg",
          "bfh",
          "bfi",
          "cdg",
          "cdh",
          "cdi",
          "ceg",
          "ceh",
          "cei",
          "cfg",
          "cfh",
          "cfi"
        ],
        "weight": 2
      },
      {
        "id": "tc_3",
        "label": "uniform/edge cases",
        "input": "222",
        "expectedOutput": [
          "aaa",
          "aab",
          "aac",
          "aba",
          "abb",
          "abc",
          "aca",
          "acb",
          "acc",
          "baa",
          "bab",
          "bac",
          "bba",
          "bbb",
          "bbc",
          "bca",
          "bcb",
          "bcc",
          "caa",
          "cab",
          "cac",
          "cba",
          "cbb",
          "cbc",
          "cca",
          "ccb",
          "ccc"
        ],
        "weight": 2
      },
      {
        "id": "tc_4",
        "label": "tricky case / empty-like",
        "input": "",
        "expectedOutput": [],
        "weight": 2
      },
      {
        "id": "tc_5",
        "label": "general mixed case",
        "input": "23",
        "expectedOutput": [
          "ad",
          "ae",
          "af",
          "bd",
          "be",
          "bf",
          "cd",
          "ce",
          "cf"
        ],
        "weight": 2
      }
    ],
    "totalTestCases": 5,
    "totalWeight": 10
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      2,
      2,
      2,
      2,
      2
    ],
    "maxScore": 10
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "Use a recursive function to generate all possible combinations of letters for a given string of digits",
    "timeComplexity": "O(4^n) where n is the length of the input string",
    "spaceComplexity": "O(n) for the recursive call stack",
    "code": {
      "C": "#include <stdio.h>\n#include <stdlib.h>\n#include <string.h>\n\nvoid helper(char* digits, int index, char* current, char*** ans, int* returnSize, char* combos[]) {\n    if (index == strlen(digits)) {\n        (*returnSize)++;\n        (*ans) = realloc((*ans), (*returnSize) * sizeof(char*));\n        (*ans)[(*returnSize) - 1] = malloc(strlen(current) + 1);\n        strcpy((*ans)[(*returnSize) - 1], current);\n        return;\n    }\n    char* s = combos[digits[index] - '0'];\n    for (int i = 0; i < strlen(s); i++) {\n        current[index] = s[i];\n        helper(digits, index + 1, current, ans, returnSize, combos);\n    }\n}\n\nchar** letterCombinations(char* digits, int* returnSize) {\n    if (strlen(digits) == 0) {\n        *returnSize = 0;\n        return NULL;\n    }\n    char* combos[] = {\"\", \"\", \"abc\", \"def\", \"ghi\", \"jkl\", \"mno\", \"pqrs\", \"tuv\", \"wxyz\"};\n    char* current = malloc(strlen(digits) + 1);\n    char** ans = NULL;\n    *returnSize = 0;\n    helper(digits, 0, current, &ans, returnSize, combos);\n    free(current);\n    return ans;\n}",
      "C++": "#include<bits/stdc++.h>\nusing namespace std;\n\nclass Solution {\nprivate:\n    // Recursive function to generate letter combinations\n    void func(int ind, string digits, string s, vector<string> &ans, string combos[]) {\n        // Base case: if index reaches the end of digits\n        if(ind == digits.size()) {\n            // Add the current combination to the answer\n            ans.push_back(s);\n            return;\n        }\n        // Convert the current character to an integer\n        int digit = digits[ind] - '0';\n        // Loop through the corresponding characters\n        for(int i = 0; i < combos[digit].size(); i++) {\n            // Recursively call function with next index\n            // Add current character to the string\n            func(ind + 1, digits, s + combos[digit][i], ans, combos);\n        }\n    }\n\npublic:\n    // Function to get all letter combinations for a given digit string\n    vector<string> letterCombinations(string digits) {\n        // Mapping digits to corresponding characters\n        string combos[] = {\"\", \"\", \"abc\", \"def\", \"ghi\", \"jkl\", \"mno\", \"pqrs\", \"tuv\", \"wxyz\"};\n        vector<string> ans; // Vector to store results\n        string s = \"\"; // Temporary string to build combinations\n        // Initiate recursive function\n        func(0, digits, s, ans, combos);\n        return ans; // Return the result\n    }\n};",
      "Java": "import java.util.ArrayList;\nimport java.util.List;\n\nclass Solution {\n    private final String[] map;\n\n    // Constructor to initialize the mapping of digits to characters\n    public Solution() {\n        map = new String[]{\"\", \"\", \"abc\", \"def\", \"ghi\", \"jkl\", \"mno\", \"pqrs\", \"tuv\", \"wxyz\"};\n    }\n\n    // Recursive helper function to generate combinations\n    private void helper(String digits, List<String> ans, int index, String current) {\n        // Base case: if index reaches the end of digits\n        if (index == digits.length()) {\n            // Add the current combination to the answer\n            ans.add(current);\n            return;\n        }\n\n        // Get characters corresponding to the current digit\n        String s = map[digits.charAt(index) - '0'];\n\n        // Loop through the corresponding characters\n        for (int i = 0; i < s.length(); i++) {\n            // Recursively call function with the next index\n            // Add current character to the string\n            helper(digits, ans, index + 1, current + s.charAt(i));\n        }\n    }\n\n    // Function to get all letter combinations for a given digit string\n    public List<String> letterCombinations(String digits) {\n        List<String> ans = new ArrayList<>(); // List to store results\n        \n        // Return empty list if digits string is empty\n        if (digits.length() == 0) return ans;\n        \n        // Initiate recursive function\n        helper(digits, ans, 0, \"\");\n        return ans; // Return the result\n    }\n}",
      "Python3": "class Solution:\n    def __init__(self):\n        # Mapping digits to corresponding characters\n        self.map = [\"\", \"\", \"abc\", \"def\", \"ghi\", \"jkl\", \"mno\", \"pqrs\", \"tuv\", \"wxyz\"]\n\n    # Recursive helper function to generate combinations\n    def helper(self, digits, ans, index, current):\n        # Base case: if index reaches the end of digits\n        if index == len(digits):\n            # Add the current combination to the answer\n            ans.append(current)\n            return\n        # Get characters corresponding to the current digit\n        s = self.map[int(digits[index])]\n        # Loop through the corresponding characters\n        for char in s:\n            # Recursively call function with next index\n            # Add current character to the string\n            self.helper(digits, ans, index + 1, current + char)\n\n    # Function to get all letter combinations for a given digit string\n    def letterCombinations(self, digits):\n        ans = []  # List to store results\n        # Return empty list if digits string is empty\n        if not digits:\n            return ans\n        # Initiate recursive function\n        self.helper(digits, ans, 0, \"\")\n        return ans  # Return the result",
      "JavaScript": "class Solution {\n    constructor() {\n        // Mapping digits to corresponding characters\n        this.map = [\"\", \"\", \"abc\", \"def\", \"ghi\", \"jkl\", \"mno\", \"pqrs\", \"tuv\", \"wxyz\"];\n    }\n\n    // Recursive helper function to generate combinations\n    helper(digits, ans, index, current) {\n        // Base case: if index reaches the end of digits\n        if (index === digits.length) {\n            // Add the current combination to the answer\n            ans.push(current);\n            return;\n        }\n        // Get characters corresponding to the current digit\n        let s = this.map[digits[index] - '0'];\n        // Loop through the corresponding characters\n        for (let i = 0; i < s.length; i++) {\n            // Recursively call function with the next index\n            // Add current character to the string\n            this.helper(digits, ans, index + 1, current + s[i]);\n        }\n    }\n\n    // Function to get all letter combinations for a given digit string\n    letterCombinations(digits) {\n        let ans = []; // Array to store results\n        // Return empty array if digits string is empty\n        if (digits.length === 0) return ans;\n        // Initiate recursive function\n        this.helper(digits, ans, 0, \"\");\n        return ans; // Return the result\n    }\n}"
    }
  },
  "QBCategory": "CCD",
  "boilerPlates": {
    "C": "#include <stdio.h>\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "C++": "#include <iostream>\nusing namespace std;\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "Java": "import java.util.*;\nimport java.io.*;\n\npublic class Main {\n    public static void main(String[] args) throws IOException {\n        // your code goes here\n    }\n}",
    "Python3": "# your code goes here\n",
    "JavaScript": "// your code goes here\n"
  },
  "_internal": {
    "methodName": "",
    "params": []
  }
}