{
  "questionId": "Q7871",
  "title": "Yet Another Crossover Episode",
  "slug": "yet-another-crossover-episode",
  "version": 1,
  "createdAt": "2026-07-02",
  "updatedAt": "2026-07-04",
  "metadata": {
    "category": "Math | Construction",
    "difficulty": "Hard",
    "isPremium": true,
    "tags": [
      "Number Theory",
      "GCD",
      "Constructive Algorithms",
      "Greedy"
    ],
    "companyTags": [],
    "topicTags": [
      "Constructive Algorithms"
    ],
    "QBCategory": "CCD"
  },
  "content": {
    "problemStatement": "You are given an integer DDD. Find an integer sequence A1,A2,…,ANA_1, A_2, \\ldots, A_NA1​,A2​,…,AN​ such that the following conditions are satisfied:\n\n\n1≤N≤1051 \\le N \\le 10^51≤N≤105\n1≤Ai≤1051 \\le A_i \\le 10^51≤Ai​≤105 for each valid iii\n∑i=1N∑j=iN(min(Ai,Ai+1,…,Aj)−GCD(Ai,Ai+1,…,Aj))=D\\sum_{i=1}^N \\sum_{j=i}^N \\left( \\mathrm{min}(A_i, A_{i+1}, \\ldots, A_j) - \\mathrm{GCD}(A_i, A_{i+1}, \\ldots, A_j) \\right) = D∑i=1N​∑j=iN​(min(Ai​,Ai+1​,…,Aj​)−GCD(Ai​,Ai+1​,…,Aj​))=D\nIt can be proved that a solution always exists under the given constraints.\n\nNote: GCD(B1,B2,…,BM)\\mathrm{GCD}(B_1, B_2, \\ldots, B_M)GCD(B1​,B2​,…,BM​) is the greatest integer which divides all the integers B1,B2,…,BMB_1, B_2, \\ldots, B_MB1​,B2​,…,BM​.",
    "constraints": [
      "1 ≤ T ≤ 10",
      "0 ≤ D ≤ 10^9",
      "1 ≤ N ≤ 10^5",
      "1 ≤ Ai ≤ 10^5"
    ],
    "inputFormat": "The first line contains an integer T (1 ≤ T ≤ 10), the number of test cases. Each of the next T lines contains a single integer D (0 ≤ D ≤ 10^9).",
    "outputFormat": "For each test case output two lines. The first line contains an integer N (1 ≤ N ≤ 100000) – the length of the constructed sequence. The second line contains N space‑separated integers A1 … AN (1 ≤ Ai ≤ 100000) such that the required sum equals D.",
    "sampleTestCases": [
      {
        "id": "sample_1",
        "input": "",
        "output": "4\r\n2\r\n5\r\n200\r\n13",
        "explanation": ""
      },
      {
        "id": "sample_2",
        "input": "",
        "output": "3\r\n3 3 2 \r\n5\r\n2 8 5 1 10 \r\n7\r\n12 10 15 11 19 13 15\r\n4\r\n5 4 4 10",
        "explanation": ""
      }
    ]
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "label": "Case 1",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_2",
        "label": "Case 2",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_3",
        "label": "Case 3",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_4",
        "label": "Case 4",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_5",
        "label": "Case 5",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      }
    ],
    "totalTestCases": 5,
    "totalWeight": 10
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      2,
      2,
      2,
      2,
      2
    ],
    "maxScore": 10
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "The contribution of a subarray of length 1 is always zero because min = gcd = Ai. To obtain a positive contribution we need a subarray of length at least two where min > gcd. Consider a pair (x, y) with x > y and gcd(x, y) = 1. Then for the subarray consisting of these two elements we have min = y and gcd = 1, so the contribution is y‑1.\n\nIf we place a value 1 after the pair, any subarray that contains this 1 has min = 1 and gcd = 1, therefore its contribution is zero. Consequently each block \"x y 1\" contributes exactly y‑1 to the total sum and does not interfere with other blocks.\n\nTo realise any D we decompose it into chunks of size at most 99 998 (the maximal y‑1 we can obtain while keeping all numbers ≤ 100 000). For each chunk c we create a block:\n  y = c + 1 ( ≤ 99 999 )\n  x = y + 1 ( ≤ 100 000 )\n  block = [x, y, 1]\nThe block contributes c = y‑1. Repeating blocks for all chunks yields a total contribution equal to D. If D = 0 we can output a single element sequence [1].\n\nThe number of blocks needed is at most ceil(D / 99 998) ≤ 10 001, therefore N ≤ 3·10 001 < 10^5, satisfying the limits.\n\nThe algorithm is linear in the size of the output.\n\n**Algorithm**\n1. Read T.\n2. For each test case read D.\n3. If D = 0 → print \"1\" and \"1\".\n4. Otherwise while D > 0:\n   * c = min(D, 99 998)\n   * y = c + 1, x = y + 1\n   * append x, y, 1 to answer list\n   * D -= c\n5. Print the length of the list and the list itself.",
    "timeComplexity": "O(N) per test case, where N is the length of the produced sequence (≤ 3·10^4).",
    "spaceComplexity": "O(N) to store the answer.",
    "code": {
      "C": "#include <stdio.h>\n#include <stdlib.h>\nint main(){\n    int T; if(scanf(\"%d\", &T)!=1) return 0;\n    while(T--){\n        long long D; scanf(\"%lld\", &D);\n        if(D==0){\n            printf(\"1\\n1\\n\");\n            continue;\n        }\n        const int MAX_CHUNK = 99998;\n        int *arr = (int*)malloc(sizeof(int)*30000); // enough for all cases\n        int idx=0;\n        while(D>0){\n            int c = (int)(D>MAX_CHUNK?MAX_CHUNK:D);\n            int y = c+1; // ≤ 99999\n            int x = y+1; // ≤ 100000\n            arr[idx++] = x;\n            arr[idx++] = y;\n            arr[idx++] = 1;\n            D -= c;\n        }\n        printf(\"%d\\n\", idx);\n        for(int i=0;i<idx;i++){\n            if(i) printf(\" \");\n            printf(\"%d\", arr[i]);\n        }\n        printf(\"\\n\");\n        free(arr);\n    }\n    return 0;\n}",
      "C++": "#include <bits/stdc++.h>\nusing namespace std;\nint main(){\n    ios::sync_with_stdio(false);\n    cin.tie(nullptr);\n    int T; if(!(cin>>T)) return 0;\n    const int MAX_CHUNK = 99998;\n    while(T--){\n        long long D; cin>>D;\n        if(D==0){\n            cout<<1<<\"\\n\"<<1<<\"\\n\";\n            continue;\n        }\n        vector<int> ans;\n        while(D>0){\n            int c = (int)min<long long>(D, MAX_CHUNK);\n            int y = c+1; // ≤ 99999\n            int x = y+1; // ≤ 100000\n            ans.push_back(x);\n            ans.push_back(y);\n            ans.push_back(1);\n            D -= c;\n        }\n        cout<<ans.size()<<\"\\n\";\n        for(size_t i=0;i<ans.size();++i){\n            if(i) cout<<' ';\n            cout<<ans[i];\n        }\n        cout<<\"\\n\";\n    }\n    return 0;\n}",
      "Java": "import java.io.*;\nimport java.util.*;\npublic class Main {\n    public static void main(String[] args) throws Exception {\n        FastScanner fs = new FastScanner(System.in);\n        int T = fs.nextInt();\n        final int MAX_CHUNK = 99998;\n        StringBuilder out = new StringBuilder();\n        while (T-- > 0) {\n            long D = fs.nextLong();\n            if (D == 0) {\n                out.append(\"1\\n1\\n\");\n                continue;\n            }\n            List<Integer> ans = new ArrayList<>();\n            while (D > 0) {\n                int c = (int) Math.min(D, MAX_CHUNK);\n                int y = c + 1; // ≤ 99999\n                int x = y + 1; // ≤ 100000\n                ans.add(x);\n                ans.add(y);\n                ans.add(1);\n                D -= c;\n            }\n            out.append(ans.size()).append('\\n');\n            for (int i = 0; i < ans.size(); i++) {\n                if (i > 0) out.append(' ');\n                out.append(ans.get(i));\n            }\n            out.append('\\n');\n        }\n        System.out.print(out.toString());\n    }\n    // Fast scanner for competitive programming\n    static class FastScanner {\n        private final InputStream in;\n        private final byte[] buffer = new byte[1 << 16];\n        private int ptr = 0, len = 0;\n        FastScanner(InputStream in) { this.in = in; }\n        private int readByte() throws IOException {\n            if (ptr >= len) {\n                len = in.read(buffer);\n                ptr = 0;\n                if (len <= 0) return -1;\n            }\n            return buffer[ptr++];\n        }\n        long nextLong() throws IOException {\n            int c; do { c = readByte(); } while (c <= ' ');\n            boolean neg = false;\n            if (c == '-') { neg = true; c = readByte(); }\n            long val = 0;\n            while (c > ' ') {\n                val = val * 10 + (c - '0');\n                c = readByte();\n            }\n            return neg ? -val : val;\n        }\n        int nextInt() throws IOException { return (int) nextLong(); }\n    }\n}",
      "Python3": "import sys\n\ndef solve():\n    data = sys.stdin.read().strip().split()\n    if not data:\n        return\n    it = iter(data)\n    T = int(next(it))\n    MAX_CHUNK = 99998\n    out_lines = []\n    for _ in range(T):\n        D = int(next(it))\n        if D == 0:\n            out_lines.append('1')\n            out_lines.append('1')\n            continue\n        ans = []\n        while D > 0:\n            c = min(D, MAX_CHUNK)\n            y = c + 1          # <= 99999\n            x = y + 1          # <= 100000\n            ans.extend([x, y, 1])\n            D -= c\n        out_lines.append(str(len(ans)))\n        out_lines.append(' '.join(map(str, ans)))\n    sys.stdout.write('\\n'.join(out_lines))\n\nif __name__ == \"__main__\":\n    solve()\n",
      "JavaScript": "const fs = require('fs');\nconst input = fs.readFileSync(0, 'utf-8').trim().split(/\\s+/);\nlet idx = 0;\nconst T = Number(input[idx++]);\nconst MAX_CHUNK = 99998;\nlet output = [];\nfor (let t = 0; t < T; ++t) {\n  let D = BigInt(input[idx++]);\n  if (D === 0n) {\n    output.push('1');\n    output.push('1');\n    continue;\n  }\n  let ans = [];\n  while (D > 0n) {\n    const c = Number(D > BigInt(MAX_CHUNK) ? BigInt(MAX_CHUNK) : D);\n    const y = c + 1; // <= 99999\n    const x = y + 1; // <= 100000\n    ans.push(x, y, 1);\n    D -= BigInt(c);\n  }\n  output.push(String(ans.length));\n  output.push(ans.join(' '));\n}\nconsole.log(output.join('\\n'));\n"
    }
  },
  "QBCategory": "CCD",
  "boilerPlates": {
    "C": "#include <stdio.h>\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "C++": "#include <iostream>\nusing namespace std;\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "Java": "import java.util.*;\nimport java.io.*;\n\npublic class Main {\n    public static void main(String[] args) throws IOException {\n        // your code goes here\n    }\n}",
    "Python3": "# your code goes here\n",
    "JavaScript": "// your code goes here\n"
  },
  "_internal": {
    "methodName": "",
    "params": []
  }
}