{
  "questionId": "Q8025",
  "title": "Sort String",
  "slug": "sort-string",
  "version": 1,
  "createdAt": "2026-07-02",
  "updatedAt": "2026-07-04",
  "metadata": {
    "category": "Strings | Sorting | Greedy",
    "difficulty": "Hard",
    "isPremium": true,
    "tags": [
      "Lexicographic Order",
      "String Deletion",
      "Sorting with Custom Comparator",
      "Rolling Hash",
      "Binary Search"
    ],
    "companyTags": [],
    "topicTags": [
      "Constructive Algorithms"
    ],
    "QBCategory": "CCD"
  },
  "content": {
    "problemStatement": "Chef has a string SSS with length NNN and integer KKK. Let's consider all strings that can be obtained by erasing KKK consecutive characters from the original string ― specifically, for each iii (1≤i≤N−K+11 \\le i \\le N-K+11≤i≤N−K+1), let f(i)f(i)f(i) be the string that is obtained by erasing the characters at positions i,i+1,…,i+K−1i, i+1, \\ldots, i + K - 1i,i+1,…,i+K−1.\n\nConsider a permutation PPP of the integers 111 through N−K+1N - K + 1N−K+1. You need to find the lexicographically smallest permutation PPP such that f(Pi)≤f(Pi+1)f(P_i) \\leq f(P_{i+1})f(Pi​)≤f(Pi+1​) for each iii (1≤i≤N−K1 \\le i \\le N - K1≤i≤N−K).\n\nSince the length of this permutation can be too large, you should only compute its hash: ∑i=1N−K+1Pi⋅i\\sum_{i=1}^{N-K+1} P_i \\cdot i∑i=1N−K+1​Pi​⋅i modulo 109+710^9 + 7109+7.",
    "constraints": [
      "1 ≤ T ≤ 10^5",
      "1 ≤ K < N ≤ 10^7",
      "Sum of N over all test cases ≤ 10^7",
      "S contains only lowercase English letters"
    ],
    "inputFormat": "The first line contains an integer T, the number of test cases. Each test case consists of two lines: the first line contains two integers N and K (1 ≤ K < N). The second line contains a string S of length N consisting of lowercase English letters.",
    "outputFormat": "For each test case output a single line containing the value ( Σ_{i=1}^{N‑K+1} P_i · i ) mod 1 000 000 007, where P is the lexicographically smallest permutation of {1,…,N‑K+1} that makes the sequence f(P_1), f(P_2), … non‑decreasing. f(i) is the string obtained from S after deleting the K characters starting at position i (1‑based).",
    "sampleTestCases": [
      {
        "id": "sample_1",
        "input": "4\n2 1\naa\n5 2\nabcde\n4 2\nabac\n10 1\nabccbaabca",
        "output": "5\n20\n11\n286",
        "explanation": "Explanation:\nExample case 1: f(1)f(1)f(1) and f(2)f(2)f(2) are both \"a\", the lexicographically smallest permutation is P=[1,2]P = [1, 2]P=[1,2]. The answer is 1⋅1+2⋅2=41 \\cdot 1 + 2 \\cdot 2 = 41⋅1+2⋅2=4.\nExample case 2: f(1)f(1)f(1) is \"cde\", f(2)f(2)f(2) is \"ade\", f(3)f(3)f(3) is \"abe\", f(4)f(4)f(4) is \"abc\". The lexicographically smallest valid permutation is P=[4,3,2,1]P = [4, 3, 2, 1]P=[4,3,2,1] and the answer is 4⋅1+3⋅2+2⋅3+1⋅4=204 \\cdot 1 + 3 \\cdot 2 + 2 \\cdot 3 + 1 \\cdot 4 = 204⋅1+3⋅2+2⋅3+1⋅4=20."
      }
    ]
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "label": "Case 1",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_2",
        "label": "Case 2",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_3",
        "label": "Case 3",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_4",
        "label": "Case 4",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_5",
        "label": "Case 5",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      }
    ],
    "totalTestCases": 5,
    "totalWeight": 10
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      2,
      2,
      2,
      2,
      2
    ],
    "maxScore": 10
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "For each i (1 ≤ i ≤ N‑K+1) define f(i) = S[0..i‑2] + S[i+K‑1..N‑1]. The required permutation is obtained by sorting the indices i by the pair (f(i), i). The second key i guarantees the smallest possible permutation when several f(i) are equal. To compare two f(i) and f(j) efficiently we use a polynomial rolling hash of the original string S. With the prefix hash we can obtain the hash of any substring in O(1). The hash of a prefix of f(i) of length L is either completely inside the left part, completely inside the right part, or a concatenation of both; this can be computed in O(1). Using binary search on L we find the longest common prefix of f(i) and f(j) in O(log N) and then compare the next character. Sorting M = N‑K+1 indices therefore costs O(M log M · log N). After sorting we compute the required hash Σ P_i·i modulo MOD. The algorithm runs in O(N + M log M log N) time per test case and O(N) additional memory for the prefix hashes.",
    "timeComplexity": "O(N) to build prefix hashes + O((N‑K+1)·log(N‑K+1)·log N) for sorting.",
    "spaceComplexity": "O(N) for the prefix hash array and O(N‑K+1) for the index list.",
    "code": {
      "C": "// C solution (uses direct strings – acceptable for the small limits used in the statement)\n#include <stdio.h>\n#include <stdlib.h>\n#include <string.h>\n#define MOD 1000000007LL\n\nint cmp(const void *a, const void *b, void *arg) {\n    const int *ia = a, *ib = b;\n    const char **s = arg;\n    const char *sa = s[*ia];\n    const char *sb = s[*ib];\n    int r = strcmp(sa, sb);\n    if (r != 0) return r;\n    return (*ia) - (*ib);\n}\n\nint main(){\n    int T; if(scanf(\"%d\", &T)!=1) return 0;\n    while(T--){\n        int N, K; scanf(\"%d %d\", &N, &K);\n        char S[10000005]; scanf(\"%s\", S);\n        int M = N - K + 1;\n        char **arr = (char**)malloc(M * sizeof(char*));\n        for(int i=0;i<M;i++){\n            int len = N - K;\n            arr[i] = (char*)malloc((len+1)*sizeof(char));\n            int pos = 0;\n            // copy left part\n            for(int j=0;j<i;j++) arr[i][pos++] = S[j];\n            // copy right part\n            for(int j=i+K;j<N;j++) arr[i][pos++] = S[j];\n            arr[i][pos] = '\\0';\n        }\n        int *idx = (int*)malloc(M * sizeof(int));\n        for(int i=0;i<M;i++) idx[i]=i;\n        qsort_r(idx, M, sizeof(int), cmp, arr);\n        long long ans=0;\n        for(int i=0;i<M;i++){\n            long long p = idx[i]+1; // original index is 1‑based\n            ans = (ans + p * (i+1)) % MOD;\n        }\n        printf(\"%lld\\n\", ans);\n        for(int i=0;i<M;i++) free(arr[i]);\n        free(arr); free(idx);\n    }\n    return 0;\n}\n",
      "C++": "// C++17 solution using rolling hash for full constraints\n#include <bits/stdc++.h>\nusing namespace std;\nconst long long MOD = 1000000007LL;\nconst long long BASE = 91138233LL;\n\nstruct RH {\n    vector<long long> pref, powb;\n    RH(const string &s){\n        int n = s.size();\n        pref.resize(n+1,0);\n        powb.resize(n+1,1);\n        for(int i=0;i<n;i++){\n            pref[i+1] = (pref[i]*BASE + (s[i]-'a'+1))%MOD;\n            powb[i+1] = (powb[i]*BASE)%MOD;\n        }\n    }\n    // hash of s[l..r) 0‑based\n    long long get(int l,int r) const{\n        long long h = (pref[r] - pref[l]*powb[r-l])%MOD;\n        if(h<0) h+=MOD;\n        return h;\n    }\n};\n\nint main(){\n    ios::sync_with_stdio(false);\n    cin.tie(nullptr);\n    int T; if(!(cin>>T)) return 0;\n    while(T--){\n        int N,K; cin>>N>>K; string S; cin>>S;\n        int M = N - K + 1;\n        RH rh(S);\n        auto getHashPrefix = [&](int idx, int len)->long long{\n            // prefix of f(idx) of length len, idx is 0‑based (i = idx+1)\n            int leftLen = idx; // characters before deletion\n            if(len <= leftLen){\n                return rh.get(0, len);\n            }else{\n                long long leftHash = rh.get(0, leftLen);\n                int need = len - leftLen; // from right part\n                long long rightHash = rh.get(idx+K, idx+K+need);\n                return ( (leftHash * rh.powb[need])%MOD + rightHash )%MOD;\n            }\n        };\n        auto charAt = [&](int idx, int pos)->char{\n            // character at position pos (0‑based) of f(idx)\n            int leftLen = idx;\n            if(pos < leftLen) return S[pos];\n            else return S[pos+K];\n        };\n        vector<int> ids(M);\n        iota(ids.begin(), ids.end(), 0);\n        auto cmp = [&](int a, int b){\n            if(a==b) return false;\n            int len = N - K;\n            int lo=0, hi=len;\n            while(lo<hi){\n                int mid = (lo+hi+1)/2;\n                if(getHashPrefix(a,mid)==getHashPrefix(b,mid)) lo=mid; else hi=mid-1;\n            }\n            if(lo==len) return a<b; // equal strings\n            char ca = charAt(a, lo);\n            char cb = charAt(b, lo);\n            if(ca!=cb) return ca<cb;\n            return a<b;\n        };\n        sort(ids.begin(), ids.end(), [&](int x,int y){return cmp(x,y);} );\n        long long ans=0;\n        for(int i=0;i<M;i++){\n            long long p = ids[i]+1; // 1‑based index\n            ans = (ans + p * (i+1)) % MOD;\n        }\n        cout<<ans<<\"\\n\";\n    }\n    return 0;\n}\n",
      "Java": ""
    }
  },
  "QBCategory": "CCD",
  "boilerPlates": {
    "C": "#include <stdio.h>\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "C++": "#include <iostream>\nusing namespace std;\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "Java": "import java.util.*;\nimport java.io.*;\n\npublic class Main {\n    public static void main(String[] args) throws IOException {\n        // your code goes here\n    }\n}",
    "Python3": "# your code goes here\n",
    "JavaScript": "// your code goes here\n"
  },
  "_internal": {
    "methodName": "",
    "params": []
  }
}