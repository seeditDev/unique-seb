{
  "questionId": "Q6625",
  "title": "Bear and Clique Distances",
  "slug": "bear-and-clique-distances",
  "version": 1,
  "createdAt": "2026-07-02",
  "updatedAt": "2026-07-04",
  "metadata": {
    "category": "Graph",
    "difficulty": "Medium",
    "isPremium": true,
    "tags": [
      "Dijkstra's Algorithm",
      "Shortest Path"
    ],
    "companyTags": [],
    "topicTags": [
      "Graphs"
    ],
    "QBCategory": "CCD"
  },
  "content": {
    "problemStatement": "Bearland consists of **N** cities, numbered 1 through **N**.\nCities are connected with bidirectional roads.\n\nCities 1 through **K** were built a long time ago, when citizens liked order and regularity.\nEach pair of those old cities is connected with a road of length **X**.\nNote that this description generates **K***(**K**-1)/2 roads.\n\nThere are **M** newer roads, not necessarily of the same lengths.\nThe i-th of them connects cities **a**i and **b**i and has length **c**i.\n\nThere is no road that connects a city to itself.\nAll **M**+**K***(**K**-1)/2 roads are distinct (ie. no two of them connects the same pair of cities).\nIt's guaranteed that it's possible to get from every city to every other city, using one or more roads.\n\nLimak, a bear, lives in the city **S**.\nIn order to plan a trip to some other city, for every city he wants to know how quickly he can get there.\nCan you help him and find the distance from **S** to every city?\n\nThe distance between two cities is the minimum total length of a path (sequence of roads) between the two cities.",
    "constraints": [
      "1 ≤ T ≤ 3",
      "2 ≤ K ≤ N ≤ 10^5",
      "0 ≤ M ≤ 10^5",
      "1 ≤ S, a, b ≤ N",
      "1 ≤ X, c ≤ 10^9"
    ],
    "inputFormat": "The first line of the input contains an integer T denoting the number of test cases. The description of T test cases follows. The first line of each test case contains five integers N, K, X, M, and S, denoting the number of cities, the number of old cities, the length of a road between every two old cities, the number of newer roads, and the city where Limak lives, respectively. Each of the following M lines contains three integers a, b, and c, denoting a road of length c between cities a and b.",
    "outputFormat": "For each test case, output a single line containing N integers. The i-th of them should denote the distance from the city S to the city i.",
    "sampleTestCases": [
      {
        "id": "sample_1",
        "input": "3\n5 4 100 2 3\n1 5 50\n5 3 160\n5 4 100 2 3\n1 5 50\n5 3 140\n8 3 15 7 5\n3 4 10\n4 5 20\n5 6 10\n6 1 7\n3 7 1000\n7 8 50\n3 5 1000000000",
        "output": "100 100 0 100 150 \n100 100 0 100 140 \n17 32 30 20 0 10 1030 1080",
        "explanation": "Explanation:\nTest case 1. There are N = 5 cities. The first K = 4 of them are all connected with each other with roads of length X = 100. There are M = 2 extra roads:\n\nCities 1 and 5 are connected with a road of length 50.\nCities 5 and 3 are connected with a road of length 160.\n\nWe are asked to compute distances to all cities from the city S = 3.\nNote that the distance from a city to itself is 0, and this is why the third number in the output is 0."
      }
    ]
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "label": "minimum boundary",
        "input": "1\n2 2 1 0 1",
        "expectedOutput": "0 1",
        "weight": 2
      },
      {
        "id": "tc_2",
        "label": "Case 2",
        "input": "1\n2 2 1 0 1",
        "expectedOutput": "0 1",
        "weight": 2
      },
      {
        "id": "tc_3",
        "label": "Case 3",
        "input": "1\n2 2 1 0 1",
        "expectedOutput": "0 1",
        "weight": 2
      },
      {
        "id": "tc_4",
        "label": "Case 4",
        "input": "1\n2 2 1 0 1",
        "expectedOutput": "0 1",
        "weight": 2
      },
      {
        "id": "tc_5",
        "label": "Case 5",
        "input": "1\n2 2 1 0 1",
        "expectedOutput": "0 1",
        "weight": 2
      }
    ],
    "totalTestCases": 5,
    "totalWeight": 10
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      2,
      2,
      2,
      2,
      2
    ],
    "maxScore": 10
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "To solve this problem, we can use Dijkstra's algorithm to find the shortest path from the city S to all other cities. We will first create a graph with N nodes and add edges between the old cities with length X. Then, we will add the newer roads to the graph. After that, we will use Dijkstra's algorithm to find the shortest distance from the city S to all other cities.",
    "timeComplexity": "O(T * (N + M) * logN) — because we are using Dijkstra's algorithm for each test case, and the time complexity of Dijkstra's algorithm is O((N + M) * logN)",
    "spaceComplexity": "O(N + M) — because we need to store the graph and the distances",
    "code": {
      "C": "#include <stdio.h>\n#include <stdlib.h>\n#include <limits.h>\n#define INF INT_MAX\n#define MIN(a, b) ((a) < (b) ? (a) : (b))\ntypedef struct Edge {\n    int to;\n    int weight;\n    struct Edge* next;\n} Edge;\ntypedef struct Graph {\n    int numVertices;\n    Edge** adjLists;\n} Graph;\nGraph* createGraph(int numVertices) {\n    Graph* graph = (Graph*) malloc(sizeof(Graph));\n    graph->numVertices = numVertices;\n    graph->adjLists = (Edge**) malloc(numVertices * sizeof(Edge*));\n    for (int i = 0; i < numVertices; i++) {\n        graph->adjLists[i] = NULL;\n    }\n    return graph;\n}\nvoid addEdge(Graph* graph, int src, int dest, int weight) {\n    Edge* newNode = (Edge*) malloc(sizeof(Edge));\n    newNode->to = dest;\n    newNode->weight = weight;\n    newNode->next = graph->adjLists[src];\n    graph->adjLists[src] = newNode;\n}\nvoid dijkstra(Graph* graph, int src) {\n    int* distances = (int*) malloc(graph->numVertices * sizeof(int));\n    for (int i = 0; i < graph->numVertices; i++) {\n        distances[i] = INF;\n    }\n    distances[src] = 0;\n    int* visited = (int*) malloc(graph->numVertices * sizeof(int));\n    for (int i = 0; i < graph->numVertices; i++) {\n        visited[i] = 0;\n    }\n    for (int i = 0; i < graph->numVertices - 1; i++) {\n        int minDistance = INF;\n        int minIndex = -1;\n        for (int j = 0; j < graph->numVertices; j++) {\n            if (!visited[j] && distances[j] < minDistance) {\n                minDistance = distances[j];\n                minIndex = j;\n            }\n        }\n        visited[minIndex] = 1;\n        Edge* temp = graph->adjLists[minIndex];\n        while (temp) {\n            if (!visited[temp->to] && distances[minIndex] + temp->weight < distances[temp->to]) {\n                distances[temp->to] = distances[minIndex] + temp->weight;\n            }\n            temp = temp->next;\n        }\n    }\n    for (int i = 0; i < graph->numVertices; i++) {\n        printf(\"%d \", distances[i]);\n    }\n    printf(\"\\n\");\n    free(distances);\n    free(visited);\n}\nint main() {\n    int T;\n    scanf(\"%d\", &T);\n    while (T--) {\n        int N, K, X, M, S;\n        scanf(\"%d %d %d %d %d\", &N, &K, &X, &M, &S);\n        Graph* graph = createGraph(N);\n        for (int i = 0; i < K; i++) {\n            for (int j = i + 1; j < K; j++) {\n                addEdge(graph, i, j, X);\n                addEdge(graph, j, i, X);\n            }\n        }\n        for (int i = 0; i < M; i++) {\n            int a, b, c;\n            scanf(\"%d %d %d\", &a, &b, &c);\n            addEdge(graph, a - 1, b - 1, c);\n            addEdge(graph, b - 1, a - 1, c);\n        }\n        dijkstra(graph, S - 1);\n        for (int i = 0; i < N; i++) {\n            Edge* temp = graph->adjLists[i];\n            while (temp) {\n                Edge* next = temp->next;\n                free(temp);\n                temp = next;\n            }\n        }\n        free(graph->adjLists);\n        free(graph);\n    }\n    return 0;\n}",
      "C++": "#include <iostream>\n#include <vector>\n#include <queue>\n#include <limits>\nusing namespace std;\ntypedef long long ll;\nconst ll INF = 1e18;\nvoid dijkstra(vector<vector<pair<int, int>>> &graph, int src, vector<ll> &distances) {\n    priority_queue<pair<ll, int>, vector<pair<ll, int>>, greater<pair<ll, int>>> pq;\n    pq.push({0, src});\n    while (!pq.empty()) {\n        ll currDist = pq.top().first;\n        int currNode = pq.top().second;\n        pq.pop();\n        if (currDist > distances[currNode]) continue;\n        for (auto &neighbor : graph[currNode]) {\n            ll newDist = currDist + neighbor.second;\n            if (newDist < distances[neighbor.first]) {\n                distances[neighbor.first] = newDist;\n                pq.push({newDist, neighbor.first});\n            }\n        }\n    }\n}\nint main() {\n    ios_base::sync_with_stdio(false);\n    cin.tie(NULL);\n    int T;\n    cin >> T;\n    while (T--) {\n        int N, K, X, M, S;\n        cin >> N >> K >> X >> M >> S;\n        vector<vector<pair<int, int>>> graph(N);\n        for (int i = 0; i < K; i++) {\n            for (int j = i + 1; j < K; j++) {\n                graph[i].push_back({j, X});\n                graph[j].push_back({i, X});\n            }\n        }\n        for (int i = 0; i < M; i++) {\n            int a, b, c;\n            cin >> a >> b >> c;\n            graph[a - 1].push_back({b - 1, c});\n            graph[b - 1].push_back({a - 1, c});\n        }\n        vector<ll> distances(N, INF);\n        distances[S - 1] = 0;\n        dijkstra(graph, S - 1, distances);\n        for (int i = 0; i < N; i++) {\n            cout << distances[i] << \" \";\n        }\n        cout << \"\\n\";\n    }\n    return 0;\n}",
      "Java": "import java.util.PriorityQueue;\nimport java.util.Scanner;\nimport java.util.ArrayList;\nimport java.util.List;\npublic class Main {\n    static class Pair implements Comparable<Pair> {\n        int node;\n        long distance;\n        Pair(int node, long distance) {\n            this.node = node;\n            this.distance = distance;\n        }\n        @Override\n        public int compareTo(Pair other) {\n            return Long.compare(this.distance, other.distance);\n        }\n    }\n    public static void main(String[] args) {\n        Scanner scanner = new Scanner(System.in);\n        int T = scanner.nextInt();\n        while (T-- > 0) {\n            int N = scanner.nextInt();\n            int K = scanner.nextInt();\n            int X = scanner.nextInt();\n            int M = scanner.nextInt();\n            int S = scanner.nextInt();\n            List<List<Pair>> graph = new ArrayList<>();\n            for (int i = 0; i < N; i++) {\n                graph.add(new ArrayList<>());\n            }\n            for (int i = 0; i < K; i++) {\n                for (int j = i + 1; j < K; j++) {\n                    graph.get(i).add(new Pair(j, X));\n                    graph.get(j).add(new Pair(i, X));\n                }\n            }\n            for (int i = 0; i < M; i++) {\n                int a = scanner.nextInt() - 1;\n                int b = scanner.nextInt() - 1;\n                int c = scanner.nextInt();\n                graph.get(a).add(new Pair(b, c));\n                graph.get(b).add(new Pair(a, c));\n            }\n            long[] distances = new long[N];\n            for (int i = 0; i < N; i++) {\n                distances[i] = Long.MAX_VALUE;\n            }\n            distances[S - 1] = 0;\n            PriorityQueue<Pair> pq = new PriorityQueue<>();\n            pq.add(new Pair(S - 1, 0));\n            while (!pq.isEmpty()) {\n                Pair curr = pq.poll();\n                if (curr.distance > distances[curr.node]) continue;\n                for (Pair neighbor : graph.get(curr.node)) {\n                    long newDistance = curr.distance + neighbor.distance;\n                    if (newDistance < distances[neighbor.node]) {\n                        distances[neighbor.node] = newDistance;\n                        pq.add(new Pair(neighbor.node, newDistance));\n                    }\n                }\n            }\n            for (int i = 0; i < N; i++) {\n                System.out.print(distances[i] + \" \");\n            }\n            System.out.println();\n        }\n    }\n}",
      "Python3": "import sys\nimport heapq\ninput = sys.stdin.readline\ndef dijkstra(graph, src, distances):\n    pq = [(0, src)]\n    while pq:\n        curr_dist, curr_node = heapq.heappop(pq)\n        if curr_dist > distances[curr_node]:\n            continue\n        for neighbor, weight in graph[curr_node]:\n            new_dist = curr_dist + weight\n            if new_dist < distances[neighbor]:\n                distances[neighbor] = new_dist\n                heapq.heappush(pq, (new_dist, neighbor))\nT = int(input())\nfor _ in range(T):\n    N, K, X, M, S = map(int, input().split())\n    graph = [[] for _ in range(N)]\n    for i in range(K):\n        for j in range(i + 1, K):\n            graph[i].append((j, X))\n            graph[j].append((i, X))\n    for _ in range(M):\n        a, b, c = map(int, input().split())\n        graph[a - 1].append((b - 1, c))\n        graph[b - 1].append((a - 1, c))\n    distances = [float('inf')] * N\n    distances[S - 1] = 0\n    dijkstra(graph, S - 1, distances)\n    print(' '.join(map(str, distances)))",
      "JavaScript": "const fs = require('fs');\nconst input = fs.readFileSync(0, 'utf-8').trim().split('\\n');\nlet idx = 0;\nfunction readline() {\n    return input[idx++];\n}\nfunction dijkstra(graph, src, distances) {\n    const pq = [];\n    pq.push([0, src]);\n    while (pq.length > 0) {\n        pq.sort((a, b) => a[0] - b[0]);\n        const [currDist, currNode] = pq.shift();\n        if (currDist > distances[currNode]) continue;\n        for (const [neighbor, weight] of graph[currNode]) {\n            const newDist = currDist + weight;\n            if (newDist < distances[neighbor]) {\n                distances[neighbor] = newDist;\n                pq.push([newDist, neighbor]);\n            }\n        }\n    }\n}\nconst T = parseInt(readline());\nfor (let i = 0; i < T; i++) {\n    const [N, K, X, M, S] = readline().split(' ').map(Number);\n    const graph = Array(N).fill(0).map(() => []);\n    for (let j = 0; j < K; j++) {\n        for (let k = j + 1; k < K; k++) {\n            graph[j].push([k, X]);\n            graph[k].push([j, X]);\n        }\n    }\n    for (let j = 0; j < M; j++) {\n        const [a, b, c] = readline().split(' ').map(Number);\n        graph[a - 1].push([b - 1, c]);\n        graph[b - 1].push([a - 1, c]);\n    }\n    const distances = Array(N).fill(Infinity);\n    distances[S - 1] = 0;\n    dijkstra(graph, S - 1, distances);\n    console.log(distances.join(' '));\n}"
    }
  },
  "QBCategory": "CCD",
  "boilerPlates": {
    "C": "#include <stdio.h>\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "C++": "#include <iostream>\nusing namespace std;\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "Java": "import java.util.*;\nimport java.io.*;\n\npublic class Main {\n    public static void main(String[] args) throws IOException {\n        // your code goes here\n    }\n}",
    "Python3": "# your code goes here\n",
    "JavaScript": "// your code goes here\n"
  },
  "_internal": {
    "methodName": "",
    "params": []
  }
}