{
  "questionId": "Q8688",
  "title": "Increasing-Decreasing Sequence",
  "slug": "increasing-decreasing-sequence",
  "version": 1,
  "createdAt": "2026-07-02",
  "updatedAt": "2026-07-04",
  "metadata": {
    "category": "Dynamic Programming",
    "difficulty": "Medium",
    "isPremium": true,
    "tags": [
      "Longest Bitonic Subsequence",
      "LIS",
      "Patience Sorting",
      "Binary Search"
    ],
    "companyTags": [],
    "topicTags": [
      "Dynamic Programming"
    ],
    "QBCategory": "CCD"
  },
  "content": {
    "problemStatement": "You are given a sequence A1,A2,…,ANA_1, A_2, \\ldots, A_NA1​,A2​,…,AN​. You need to find the maximum length of a subsequence of AAA whose elements first strictly increase and then strictly decrease in value. Note that subsequences that are only strictly increasing and subsequences that are only strictly decreasing are considered as well.\n\nA subsequence of a given sequence is a sequence that can be derived from the given sequence by deleting some or no elements without changing the order of the remaining elements.\n\n\nInput Format\n\n\nThe first line of the input contains a single integer TTT denoting the number of test cases. The description of TTT test cases follows.\n\n\n\n\nThe first line of each test case contains an integer NNN.\n\n\n\n\nThe second line contains NNN space-separated integers A1,A2,…,ANA_1, A_2, \\ldots, A_NA1​,A2​,…,AN​.\n\n\n\n\n\nOutput Format\n\nFor each test case, print a single line containing one integer ― the maximum length of a subsequence of the sequence AAA which is first strictly increasing and then strictly increasing.",
    "constraints": [
      "1 <= T <= 100",
      "1 <= N <= 200000",
      "Sum of N over all test cases does not exceed 200000",
      "1 <= Ai <= 10^9"
    ],
    "inputFormat": "The first line contains an integer T, the number of test cases. For each test case, the first line contains an integer N, the length of the sequence. The second line contains N space‑separated integers A1 … AN.",
    "outputFormat": "For each test case output a single line with one integer – the maximum possible length of a subsequence that first strictly increases and then strictly decreases (purely increasing or purely decreasing subsequences are also allowed).",
    "sampleTestCases": [
      {
        "id": "sample_1",
        "input": "4\n9\n1 2 3 1 4 1 3 2 1\n5\n2 4 3 6 3\n4\n1 2 3 4\n5\n9 9 9 9 9",
        "output": "7\n4\n4\n1",
        "explanation": "For each test case the longest subsequence that first strictly increases then strictly decreases (or only increases/decreases) has lengths 7, 4, 4 and 1 respectively, as described in the statement."
      }
    ]
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "label": "Case 1",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_2",
        "label": "Case 2",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_3",
        "label": "Case 3",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_4",
        "label": "Case 4",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_5",
        "label": "Case 5",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      }
    ],
    "totalTestCases": 5,
    "totalWeight": 10
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      2,
      2,
      2,
      2,
      2
    ],
    "maxScore": 10
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "For every position i compute L[i] = length of the longest strictly increasing subsequence that ends at i. This can be done in O(N log N) using the classic patience‑sorting method with a tail array and binary search. Similarly compute R[i] = length of the longest strictly decreasing subsequence that starts at i. R[i] is obtained by running the same LIS algorithm on the reversed array (which gives the longest increasing subsequence in reverse order, i.e. decreasing in the original order) and then mapping the results back. The answer for the test case is max_i (L[i] + R[i] - 1). The subtraction removes the double‑counted element at the peak. The algorithm runs in O(N log N) time and O(N) memory per test case.",
    "timeComplexity": "O(N log N) per test case",
    "spaceComplexity": "O(N)",
    "code": {
      "C": "#include <stdio.h>\n#include <stdlib.h>\n\nint lower_bound(int *arr, int size, int val){\n    int l=0, r=size;\n    while(l<r){\n        int m=(l+r)/2;\n        if(arr[m]>=val) r=m; else l=m+1;\n    }\n    return l;\n}\n\nint main(){\n    int T; if(scanf(\"%d\",&T)!=1) return 0;\n    while(T--){\n        int N; scanf(\"%d\",&N);\n        int *a=malloc(N*sizeof(int));\n        for(int i=0;i<N;i++) scanf(\"%d\",&a[i]);\n        int *L=malloc(N*sizeof(int));\n        int *tail=malloc(N*sizeof(int));\n        int sz=0;\n        for(int i=0;i<N;i++){\n            int pos=lower_bound(tail,sz,a[i]);\n            tail[pos]=a[i];\n            if(pos==sz) sz++;\n            L[i]=pos+1;\n        }\n        // compute decreasing lengths using reversed array\n        int *R=malloc(N*sizeof(int));\n        sz=0;\n        for(int i=N-1;i>=0;i--){\n            int pos=lower_bound(tail,sz,a[i]);\n            tail[pos]=a[i];\n            if(pos==sz) sz++;\n            R[i]=pos+1;\n        }\n        int ans=0;\n        for(int i=0;i<N;i++){\n            int cur=L[i]+R[i]-1;\n            if(cur>ans) ans=cur;\n        }\n        printf(\"%d\\n\",ans);\n        free(a); free(L); free(R); free(tail);\n    }\n    return 0;\n}",
      "C++": "#include <bits/stdc++.h>\nusing namespace std;\nint main(){\n    ios::sync_with_stdio(false);\n    cin.tie(nullptr);\n    int T; if(!(cin>>T)) return 0;\n    while(T--){\n        int N; cin>>N;\n        vector<int> a(N);\n        for(int &x:a) cin>>x;\n        vector<int> L(N), tail;\n        for(int i=0;i<N;i++){\n            auto it=lower_bound(tail.begin(),tail.end(),a[i]);\n            int pos=it-tail.begin();\n            if(it==tail.end()) tail.push_back(a[i]); else *it=a[i];\n            L[i]=pos+1;\n        }\n        vector<int> R(N);\n        tail.clear();\n        for(int i=N-1;i>=0;i--){\n            auto it=lower_bound(tail.begin(),tail.end(),a[i]);\n            int pos=it-tail.begin();\n            if(it==tail.end()) tail.push_back(a[i]); else *it=a[i];\n            R[i]=pos+1;\n        }\n        int ans=0;\n        for(int i=0;i<N;i++) ans=max(ans, L[i]+R[i]-1);\n        cout<<ans<<\"\\n\";\n    }\n    return 0;\n}",
      "Java": "import java.io.*;\nimport java.util.*;\npublic class Main {\n    static int lowerBound(int[] arr, int size, int val){\n        int l=0, r=size;\n        while(l<r){\n            int m=(l+r)>>>1;\n            if(arr[m]>=val) r=m; else l=m+1;\n        }\n        return l;\n    }\n    public static void main(String[] args) throws Exception{\n        FastScanner fs=new FastScanner(System.in);\n        int T=fs.nextInt();\n        StringBuilder sb=new StringBuilder();\n        while(T-- >0){\n            int N=fs.nextInt();\n            int[] a=new int[N];\n            for(int i=0;i<N;i++) a[i]=fs.nextInt();\n            int[] L=new int[N];\n            int[] tail=new int[N];\n            int sz=0;\n            for(int i=0;i<N;i++){\n                int pos=lowerBound(tail,sz,a[i]);\n                tail[pos]=a[i];\n                if(pos==sz) sz++;\n                L[i]=pos+1;\n            }\n            int[] R=new int[N];\n            sz=0;\n            for(int i=N-1;i>=0;i--){\n                int pos=lowerBound(tail,sz,a[i]);\n                tail[pos]=a[i];\n                if(pos==sz) sz++;\n                R[i]=pos+1;\n            }\n            int ans=0;\n            for(int i=0;i<N;i++) ans=Math.max(ans, L[i]+R[i]-1);\n            sb.append(ans).append('\\n');\n        }\n        System.out.print(sb.toString());\n    }\n    static class FastScanner{\n        private final InputStream in;\n        private final byte[] buffer=new byte[1<<16];\n        private int ptr=0, len=0;\n        FastScanner(InputStream in){this.in=in;}\n        private int read() throws IOException{\n            if(ptr>=len){\n                len=in.read(buffer);\n                ptr=0;\n                if(len<=0) return -1;\n            }\n            return buffer[ptr++];\n        }\n        int nextInt() throws IOException{\n            int c, sign=1, val=0;\n            do{c=read();}while(c<=32 && c!=-1);\n            if(c=='-'){sign=-1; c=read();}\n            while(c>32 && c!=-1){val=val*10+(c-'0'); c=read();}\n            return val*sign;\n        }\n    }\n}",
      "Python3": "import sys\n\ndef lis_lengths(arr):\n    import bisect\n    tail=[]\n    res=[0]*len(arr)\n    for i,x in enumerate(arr):\n        pos=bisect.bisect_left(tail,x)\n        if pos==len(tail):\n            tail.append(x)\n        else:\n            tail[pos]=x\n        res[i]=pos+1\n    return res\n\ndef solve():\n    data=sys.stdin.read().strip().split()\n    if not data: return\n    it=iter(data)\n    T=int(next(it))\n    out=[]\n    for _ in range(T):\n        N=int(next(it))\n        a=[int(next(it)) for _ in range(N)]\n        L=lis_lengths(a)\n        R=lis_lengths(a[::-1])[::-1]\n        ans=0\n        for l,r in zip(L,R):\n            ans=max(ans,l+r-1)\n        out.append(str(ans))\n    sys.stdout.write('\\n'.join(out))\n\nif __name__=='__main__':\n    solve()\n",
      "JavaScript": "const fs=require('fs');\\nconst input=fs.readFileSync(0,'utf-8').trim().split(/\\\\s+/).map(Number);\\nlet idx=0;\\nconst T=input[idx++]||0;\\nlet outputs=[];\\nfunction lisLengths(arr){\\n  const tail=[];\\n  const res=new Array(arr.length);\\n  for(let i=0;i<arr.length;i++){\\n    const x=arr[i];\\n    // binary search lower_bound\\n    let l=0, r=tail.length;\\n    while(l<r){\\n      const m=(l+r)>>1;\\n      if(tail[m]>=x) r=m; else l=m+1;\\n    }\\n    if(l===tail.length) tail.push(x); else tail[l]=x;\\n    res[i]=l+1;\\n  }\\n  return res;\\n}\\nfor(let t=0;t<T;t++){\\n  const N=input[idx++];\\n  const a=input.slice(idx, idx+N); idx+=N;\\n  const L=lisLengths(a);\\n  const R=lisLengths(a.slice().reverse()).reverse();\\n  let ans=0;\\n  for(let i=0;i<N;i++) ans=Math.max(ans, L[i]+R[i]-1);\\n  outputs.push(String(ans));\\n}\\nconsole.log(outputs.join('\\\\n'));\\n"
    }
  },
  "QBCategory": "CCD",
  "boilerPlates": {
    "C": "#include <stdio.h>\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "C++": "#include <iostream>\nusing namespace std;\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "Java": "import java.util.*;\nimport java.io.*;\n\npublic class Main {\n    public static void main(String[] args) throws IOException {\n        // your code goes here\n    }\n}",
    "Python3": "# your code goes here\n",
    "JavaScript": "// your code goes here\n"
  },
  "_internal": {
    "methodName": "",
    "params": []
  }
}