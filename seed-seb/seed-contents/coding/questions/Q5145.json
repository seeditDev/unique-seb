{
  "questionId": "Q5145",
  "title": "The Rise and Fall of Power",
  "slug": "the-rise-and-fall-of-power",
  "version": 1,
  "createdAt": "2026-07-01",
  "updatedAt": "2026-07-04",
  "metadata": {
    "category": "Math",
    "difficulty": "Hard",
    "isPremium": true,
    "tags": [
      "Number Theory",
      "Modular Exponentiation",
      "Floating Point",
      "Logarithms"
    ],
    "companyTags": [],
    "topicTags": [
      "Mathematics"
    ],
    "QBCategory": "CCD"
  },
  "content": {
    "problemStatement": "Johnny was asked by his math teacher to compute nn (n to the power of n, where n is an integer), and has to read his answer out loud. This is a bit of a tiring task, since the result is probably an extremely large number, and would certainly keep Johnny occupied for a while if he were to do it honestly. But Johnny knows that the teacher will certainly get bored when listening to his answer, and will sleep through most of it! So, Johnny feels he will get away with reading only the first k digits of the result before the teacher falls asleep, and then the last k digits when the teacher wakes up.\n\n\nWrite a program to help Johnny to compute the digits he will need to read out.",
    "constraints": [
      "1 ≤ t ≤ 30000",
      "1 ≤ n ≤ 10^9",
      "1 ≤ k ≤ 9",
      "k ≤ number of digits of n^n"
    ],
    "inputFormat": "The first line contains an integer t (1 ≤ t ≤ 30000), the number of test cases. Each of the next t lines contains two integers n and k (1 ≤ n ≤ 10^9, 1 ≤ k ≤ 9). It is guaranteed that k does not exceed the number of decimal digits of n^n.",
    "outputFormat": "For each test case output a line with two numbers separated by a single space: the first k digits of n^n and the last k digits of n^n. The last k digits must be printed with leading zeros if necessary.",
    "sampleTestCases": [
      {
        "id": "sample_1",
        "input": "2\n4 2\n9 3",
        "output": "25 56\n387 489",
        "explanation": "4^4 = 256 → first 2 digits 25, last 2 digits 56. 9^9 = 387420489 → first 3 digits 387, last 3 digits 489."
      },
      {
        "id": "sample_2",
        "input": "1\n12 4",
        "output": "8916 8256",
        "explanation": "12^12 = 8916100448256. First 4 digits are 8916, last 4 digits are 8256."
      },
      {
        "id": "sample_3",
        "input": "1\n7 3",
        "output": "823 543",
        "explanation": "7^7 = 823543. First 3 digits 823, last 3 digits 543."
      }
    ]
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "label": "minimum boundary",
        "input": "1\n1 1",
        "expectedOutput": "1 1",
        "weight": 2
      },
      {
        "id": "tc_2",
        "label": "maximum boundary",
        "input": "1\n1000000000 9",
        "expectedOutput": "100000000 000000000",
        "weight": 2
      },
      {
        "id": "tc_3",
        "label": "edge case",
        "input": "1\n2 1",
        "expectedOutput": "4 4",
        "weight": 2
      },
      {
        "id": "tc_4",
        "label": "general case 1",
        "input": "1\n15 3",
        "expectedOutput": "437 375",
        "weight": 2
      },
      {
        "id": "tc_5",
        "label": "general case 2",
        "input": "1\n20 2",
        "expectedOutput": "10 00",
        "weight": 2
      }
    ],
    "totalTestCases": 5,
    "totalWeight": 10
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      2,
      2,
      2,
      2,
      2
    ],
    "maxScore": 10
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "For each test case we need the first k decimal digits and the last k decimal digits of n^n. The last k digits are obtained by modular exponentiation: compute n^n mod 10^k using binary exponentiation with 128‑bit intermediate values (or BigInteger in Java). The first k digits are derived from the fractional part of log10(n^n) = n·log10(n). Let x = n·log10(n). Write x = ⌊x⌋ + f where 0 ≤ f < 1. Then n^n = 10^{⌊x⌋}·10^{f}. The leading k digits are floor(10^{f + k - 1}). Because k ≤ 9, double/long double precision is sufficient. Edge cases such as n = 1 are handled naturally (log10(1)=0). The algorithm runs in O(log n) per test case for the modular exponentiation and O(1) for the leading part.",
    "timeComplexity": "O(t·log n) – binary exponentiation dominates, the logarithm computation is O(1).",
    "spaceComplexity": "O(1) additional space per test case.",
    "code": {
      "C": "#include <stdio.h>\n#include <math.h>\n#include <stdint.h>\n\nstatic uint64_t modPow(uint64_t base, uint64_t exp, uint64_t mod) {\n    __uint128_t result = 1;\n    __uint128_t b = base % mod;\n    while (exp) {\n        if (exp & 1) result = (result * b) % mod;\n        b = (b * b) % mod;\n        exp >>= 1;\n    }\n    return (uint64_t)result;\n}\n\nint main() {\n    int T;\n    if (scanf(\"%d\", &T) != 1) return 0;\n    while (T--) {\n        uint64_t n; int k;\n        scanf(\"%llu %d\", &n, &k);\n        // leading digits\n        long double x = (long double)n * log10l((long double)n);\n        long double intpart;\n        long double frac = modfl(x, &intpart);\n        long double leading_f = powl(10.0L, frac + k - 1);\n        uint64_t leading = (uint64_t)floorl(leading_f + 1e-12L);\n        // trailing digits\n        uint64_t mod = 1;\n        for (int i = 0; i < k; ++i) mod *= 10ULL;\n        uint64_t trailing = modPow(n, n, mod);\n        // print with leading zeros for trailing part\n        char fmt[32];\n        sprintf(fmt, \"%%llu %%0%dlu\\n\", k);\n        printf(fmt, leading, trailing);\n    }\n    return 0;\n}",
      "C++": "#include <bits/stdc++.h>\nusing namespace std;\nusing int64 = long long;\nusing i128 = __int128_t;\n\nint64 modPow(int64 base, int64 exp, int64 mod) {\n    i128 res = 1;\n    i128 b = base % mod;\n    while (exp) {\n        if (exp & 1) res = (res * b) % mod;\n        b = (b * b) % mod;\n        exp >>= 1;\n    }\n    return (int64)res;\n}\n\nint main() {\n    ios::sync_with_stdio(false);\n    cin.tie(nullptr);\n    int T; if(!(cin>>T)) return 0;\n    while (T--) {\n        long long n; int k; cin>>n>>k;\n        long double x = (long double)n * log10l((long double)n);\n        long double intpart;\n        long double frac = modfl(x, &intpart);\n        long double leading_f = powl(10.0L, frac + k - 1);\n        long long leading = (long long)floorl(leading_f + 1e-12L);\n        long long mod = 1;\n        for (int i=0;i<k;++i) mod*=10LL;\n        long long trailing = modPow(n, n, mod);\n        cout << leading << ' ' << setw(k) << setfill('0') << trailing << \"\\n\";\n    }\n    return 0;\n}",
      "Java": "import java.io.*;\nimport java.util.*;\nimport java.math.*;\n\npublic class Main {\n    public static void main(String[] args) throws Exception {\n        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));\n        StringBuilder out = new StringBuilder();\n        int T = Integer.parseInt(br.readLine().trim());\n        for (int tc = 0; tc < T; ++tc) {\n            StringTokenizer st = new StringTokenizer(br.readLine());\n            long n = Long.parseLong(st.nextToken());\n            int k = Integer.parseInt(st.nextToken());\n            // leading digits\n            double x = n * Math.log10(n);\n            double intPart = Math.floor(x);\n            double frac = x - intPart;\n            double leadingDouble = Math.pow(10.0, frac + k - 1);\n            long leading = (long) Math.floor(leadingDouble + 1e-12);\n            // trailing digits\n            BigInteger MOD = BigInteger.TEN.pow(k);\n            BigInteger base = BigInteger.valueOf(n);\n            BigInteger trailingBI = base.modPow(BigInteger.valueOf(n), MOD);\n            String trailingStr = trailingBI.toString();\n            while (trailingStr.length() < k) trailingStr = \"0\" + trailingStr;\n            out.append(leading).append(' ').append(trailingStr).append('\\n');\n        }\n        System.out.print(out.toString());\n    }\n}",
      "Python3": "#!/usr/bin/env python3\nimport sys, math\n\ndef mod_pow(base, exp, mod):\n    result = 1\n    b = base % mod\n    while exp:\n        if exp & 1:\n            result = (result * b) % mod\n        b = (b * b) % mod\n        exp >>= 1\n    return result\n\ndef solve():\n    data = sys.stdin.read().strip().split()\n    if not data:\n        return\n    it = iter(data)\n    t = int(next(it))\n    out_lines = []\n    for _ in range(t):\n        n = int(next(it)); k = int(next(it))\n        # leading k digits\n        x = n * math.log10(n)\n        frac = x - math.floor(x)\n        leading = int(10 ** (frac + k - 1) + 1e-12)\n        # trailing k digits\n        mod = 10 ** k\n        trailing = mod_pow(n, n, mod)\n        out_lines.append(f\"{leading} {trailing:0{k}d}\")\n    sys.stdout.write('\\n'.join(out_lines))\n\nif __name__ == \"__main__\":\n    solve()\n",
      "JavaScript": "const fs = require('fs');\nconst input = fs.readFileSync(0, 'utf-8').trim().split(/\\s+/);\nlet idx = 0;\nconst T = Number(input[idx++]);\nlet result = [];\nfunction modPow(base, exp, mod) {\n  let b = BigInt(base) % BigInt(mod);\n  let e = BigInt(exp);\n  let m = BigInt(mod);\n  let res = 1n;\n  while (e > 0n) {\n    if (e & 1n) res = (res * b) % m;\n    b = (b * b) % m;\n    e >>= 1n;\n  }\n  return Number(res);\n}\nfor (let t = 0; t < T; ++t) {\n  const n = Number(input[idx++]);\n  const k = Number(input[idx++]);\n  // leading digits\n  const x = n * Math.log10(n);\n  const frac = x - Math.floor(x);\n  const leading = Math.floor(Math.pow(10, frac + k - 1) + 1e-12);\n  // trailing digits\n  const mod = 10 ** k;\n  const trailingNum = modPow(n, n, mod);\n  const trailingStr = trailingNum.toString().padStart(k, '0');\n  result.push(`${leading} ${trailingStr}`);\n}\nconsole.log(result.join('\\n'));\n"
    }
  },
  "QBCategory": "CCD",
  "boilerPlates": {
    "C": "#include <stdio.h>\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "C++": "#include <iostream>\nusing namespace std;\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "Java": "import java.util.*;\nimport java.io.*;\n\npublic class Main {\n    public static void main(String[] args) throws IOException {\n        // your code goes here\n    }\n}",
    "Python3": "# your code goes here\n",
    "JavaScript": "// your code goes here\n"
  },
  "_internal": {
    "methodName": "",
    "params": []
  }
}