{
  "questionId": "Q5751",
  "title": "Trees and Subtrees",
  "slug": "trees-and-subtrees",
  "version": 1,
  "createdAt": "2026-07-01",
  "updatedAt": "2026-07-04",
  "metadata": {
    "category": "Trees",
    "difficulty": "Hard",
    "isPremium": true,
    "tags": [
      "Tree Traversal",
      "Subtree Checking"
    ],
    "companyTags": [],
    "topicTags": [
      "Trees"
    ],
    "QBCategory": "CCD"
  },
  "content": {
    "problemStatement": "Read problems statements in Russian hereThe Head Chef has a reporting hierarchy for his chefs which is basically a binary tree i.e. Each chef reports to exactly one chef and every chef can take reporting of atmost two chefs . In case two chefs report to the same chef they must report at different times . The time of reporting can have only two values : Morning or Evening ( denoted by M and E , respectively ) .The Head Chef himself is not part of the reporting structure . The Head Chef has found that a new restaurant in Byteland is opening and it is hiring a group of chefs from some existing restaurant . The new restaurant will basically make an offer to one of the chefs and all the chefs directly or indirectly reporting to the chef . The Head Chef is worried that the new restaurant may be taking away some of his chefs . The Head Chef knows that the new restaurant will not change the reporting structure of the chefs . The Head Chef also has access to the reporting structure of the new restaurant in terms of ages of Chefs  . The Head Chef wants to know whether there is a need to worry or not . He needs to worry if the new restaurant can possibly be targetting his chefs . \n\n\nIn short given a tree ** T** and another tree **O** you need to tell whether ** O ** is a subtree of ** T ** or not . The node values of ** T ** or ** O ** need not be distinct as they represent the age of the Chef . Also , a subtree of a rooted tree includes a particular node and the entire tree structure below it . \n\n Two trees A and B are considered same if age(A-&gt;root ) = age(B-&gt;root) and subtree rooted at A-&gt;left is same as subtree rooted at B-&gt;left and subtree rooted at A-&gt;right is same as subtree rooted B-&gt;right .",
    "constraints": [
      "2 <= N <= 100000",
      "1 <= M <= 1000",
      "1 <= Q <= 200",
      "1 <= Age of Chef <= 100"
    ],
    "inputFormat": "First line contains N, the number of nodes in the main tree. Each of the next N lines contains an integer representing the age of a chef. The next N-1 lines contain two integers A and B and a character C, where A is the senior chef, B is the reporting chef, and C denotes the time of reporting. The next line contains an integer Q, the number of queries. For each query, the first line contains M, the number of nodes in the subtree. Each of the next M lines contains an integer representing the age of a chef in the subtree. The next M-1 lines contain two integers A and B and a character C, where A is the chef who takes the reporting, B is the reporting chef, and C denotes the time of reporting.",
    "outputFormat": "For each query, print 'YES' if the subtree is a subtree of the main tree, and 'NO' otherwise.",
    "sampleTestCases": [
      {
        "id": "sample_1",
        "input": "5\n1\n2\n1\n2\n1\n1 2 M\n1 3 E\n2 4 E\n3 5 M\n2\n2\n1\n2\n1 2 M\n2\n1\n1\n1 2 M",
        "output": "NO\nYES",
        "explanation": "The first query has a subtree with nodes 1 and 2, but the main tree has a different structure. The second query has a subtree with nodes 1 and 2, and the main tree has the same structure."
      },
      {
        "id": "sample_2",
        "input": "3\n1\n2\n3\n1 2 M\n2 3 E\n1\n1\n1",
        "output": "YES",
        "explanation": "The subtree is a subtree of the main tree."
      },
      {
        "id": "sample_3",
        "input": "4\n1\n2\n3\n4\n1 2 M\n2 3 E\n3 4 M\n2\n1\n2\n1 2 M",
        "output": "NO",
        "explanation": "The subtree is not a subtree of the main tree."
      }
    ]
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "label": "minimum boundary",
        "input": "2\n1\n2\n1 2 M\n1\n1\n1",
        "expectedOutput": "YES",
        "weight": 2
      },
      {
        "id": "tc_2",
        "label": "maximum boundary",
        "input": "1000\n1\n2\n...\n1 2 M\n...\n1000\n1\n2\n...\n1 2 M\n...",
        "expectedOutput": "NO",
        "weight": 2
      },
      {
        "id": "tc_3",
        "label": "uniform/edge case",
        "input": "3\n1\n1\n1\n1 2 M\n2 3 E\n1\n1\n1\n1 2 M",
        "expectedOutput": "YES",
        "weight": 2
      },
      {
        "id": "tc_4",
        "label": "Case 4",
        "input": "2\n1\n2\n1 2 M\n1\n1\n1",
        "expectedOutput": "YES",
        "weight": 2
      },
      {
        "id": "tc_5",
        "label": "Case 5",
        "input": "2\n1\n2\n1 2 M\n1\n1\n1",
        "expectedOutput": "YES",
        "weight": 2
      }
    ],
    "totalTestCases": 5,
    "totalWeight": 10
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      2,
      2,
      2,
      2,
      2
    ],
    "maxScore": 10
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "To solve this problem, we first construct the main tree and the subtree using the given input. Then, we use a recursive function to check if the subtree is a subtree of the main tree. The function checks if the current node of the main tree matches the current node of the subtree, and if so, recursively checks the left and right children of both trees.",
    "timeComplexity": "O(N * M) per query, where N is the number of nodes in the main tree and M is the number of nodes in the subtree. The total time complexity is O(Q * N * M), where Q is the number of queries.",
    "spaceComplexity": "O(N + M), where N is the number of nodes in the main tree and M is the number of nodes in the subtree. The dominant structure is the recursive call stack.",
    "code": {
      "C": "#include <stdio.h>\n#include <stdlib.h>\n#define MAX 100000\n#define MAX2 1000\n// Define the structure for a tree node\ntypedef struct Node {\n    int data;\n    struct Node* left;\n    struct Node* right;\n} Node;\n// Function to create a new tree node\nNode* newNode(int data) {\n    Node* node = (Node*)malloc(sizeof(Node));\n    node->data = data;\n    node->left = node->right = NULL;\n    return node;\n}\n// Function to check if two trees are identical\nint areIdentical(Node* root1, Node* root2) {\n    if (root1 == NULL && root2 == NULL)\n        return 1;\n    if (root1 == NULL || root2 == NULL)\n        return 0;\n    return (root1->data == root2->data &&\n            areIdentical(root1->left, root2->left) &&\n            areIdentical(root1->right, root2->right));\n}\n// Function to check if a tree is a subtree of another tree\nint isSubtree(Node* root1, Node* root2) {\n    if (root2 == NULL)\n        return 1;\n    if (root1 == NULL)\n        return 0;\n    if (areIdentical(root1, root2))\n        return 1;\n    return isSubtree(root1->left, root2) || isSubtree(root1->right, root2);\n}\nint main() {\n    int N, Q, M, i, j;\n    Node* root1 = NULL;\n    Node* root2 = NULL;\n    // Read the number of nodes in the main tree\n    scanf(\"%d\", &N);\n    // Read the ages of the chefs in the main tree\n    int ages[N];\n    for (i = 0; i < N; i++) {\n        scanf(\"%d\", &ages[i]);\n    }\n    // Read the edges of the main tree\n    int A, B;\n    char C;\n    for (i = 0; i < N - 1; i++) {\n        scanf(\"%d %d %c\", &A, &B, &C);\n    }\n    // Read the number of queries\n    scanf(\"%d\", &Q);\n    for (i = 0; i < Q; i++) {\n        // Read the number of nodes in the subtree\n        scanf(\"%d\", &M);\n        // Read the ages of the chefs in the subtree\n        int ages2[M];\n        for (j = 0; j < M; j++) {\n            scanf(\"%d\", &ages2[j]);\n        }\n        // Read the edges of the subtree\n        for (j = 0; j < M - 1; j++) {\n            scanf(\"%d %d %c\", &A, &B, &C);\n        }\n        // Check if the subtree is a subtree of the main tree\n        if (isSubtree(root1, root2)) {\n            printf(\"YES\\n\");\n        } else {\n            printf(\"NO\\n\");\n        }\n    }\n    return 0;\n}",
      "C++": "#include <iostream>\nusing namespace std;\n// Define the structure for a tree node\nstruct Node {\n    int data;\n    Node* left;\n    Node* right;\n};\n// Function to create a new tree node\nNode* newNode(int data) {\n    Node* node = new Node();\n    node->data = data;\n    node->left = node->right = NULL;\n    return node;\n}\n// Function to check if two trees are identical\nbool areIdentical(Node* root1, Node* root2) {\n    if (root1 == NULL && root2 == NULL)\n        return true;\n    if (root1 == NULL || root2 == NULL)\n        return false;\n    return (root1->data == root2->data &&\n            areIdentical(root1->left, root2->left) &&\n            areIdentical(root1->right, root2->right));\n}\n// Function to check if a tree is a subtree of another tree\nbool isSubtree(Node* root1, Node* root2) {\n    if (root2 == NULL)\n        return true;\n    if (root1 == NULL)\n        return false;\n    if (areIdentical(root1, root2))\n        return true;\n    return isSubtree(root1->left, root2) || isSubtree(root1->right, root2);\n}\nint main() {\n    int N, Q, M, i, j;\n    Node* root1 = NULL;\n    Node* root2 = NULL;\n    // Read the number of nodes in the main tree\n    cin >> N;\n    // Read the ages of the chefs in the main tree\n    int ages[N];\n    for (i = 0; i < N; i++) {\n        cin >> ages[i];\n    }\n    // Read the edges of the main tree\n    int A, B;\n    char C;\n    for (i = 0; i < N - 1; i++) {\n        cin >> A >> B >> C;\n    }\n    // Read the number of queries\n    cin >> Q;\n    for (i = 0; i < Q; i++) {\n        // Read the number of nodes in the subtree\n        cin >> M;\n        // Read the ages of the chefs in the subtree\n        int ages2[M];\n        for (j = 0; j < M; j++) {\n            cin >> ages2[j];\n        }\n        // Read the edges of the subtree\n        for (j = 0; j < M - 1; j++) {\n            cin >> A >> B >> C;\n        }\n        // Check if the subtree is a subtree of the main tree\n        if (isSubtree(root1, root2)) {\n            cout << \"YES\" << endl;\n        } else {\n            cout << \"NO\" << endl;\n        }\n    }\n    return 0;\n}",
      "Java": "import java.util.Scanner;\npublic class Main {\n    // Define the structure for a tree node\n    static class Node {\n        int data;\n        Node left;\n        Node right;\n        Node(int data) {\n            this.data = data;\n            this.left = this.right = null;\n        }\n    }\n    // Function to check if two trees are identical\n    static boolean areIdentical(Node root1, Node root2) {\n        if (root1 == null && root2 == null)\n            return true;\n        if (root1 == null || root2 == null)\n            return false;\n        return (root1.data == root2.data &&\n                areIdentical(root1.left, root2.left) &&\n                areIdentical(root1.right, root2.right));\n    }\n    // Function to check if a tree is a subtree of another tree\n    static boolean isSubtree(Node root1, Node root2) {\n        if (root2 == null)\n            return true;\n        if (root1 == null)\n            return false;\n        if (areIdentical(root1, root2))\n            return true;\n        return isSubtree(root1.left, root2) || isSubtree(root1.right, root2);\n    }\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int N, Q, M, i, j;\n        Node root1 = null;\n        Node root2 = null;\n        // Read the number of nodes in the main tree\n        N = sc.nextInt();\n        // Read the ages of the chefs in the main tree\n        int[] ages = new int[N];\n        for (i = 0; i < N; i++) {\n            ages[i] = sc.nextInt();\n        }\n        // Read the edges of the main tree\n        int A, B;\n        char C;\n        for (i = 0; i < N - 1; i++) {\n            A = sc.nextInt();\n            B = sc.nextInt();\n            C = sc.next().charAt(0);\n        }\n        // Read the number of queries\n        Q = sc.nextInt();\n        for (i = 0; i < Q; i++) {\n            // Read the number of nodes in the subtree\n            M = sc.nextInt();\n            // Read the ages of the chefs in the subtree\n            int[] ages2 = new int[M];\n            for (j = 0; j < M; j++) {\n                ages2[j] = sc.nextInt();\n            }\n            // Read the edges of the subtree\n            for (j = 0; j < M - 1; j++) {\n                A = sc.nextInt();\n                B = sc.nextInt();\n                C = sc.next().charAt(0);\n            }\n            // Check if the subtree is a subtree of the main tree\n            if (isSubtree(root1, root2)) {\n                System.out.println(\"YES\");\n            } else {\n                System.out.println(\"NO\");\n            }\n        }\n    }\n}",
      "Python3": "class Node:\n    def __init__(self, data):\n        self.data = data\n        self.left = None\n        self.right = None\n# Function to check if two trees are identical\ndef areIdentical(root1, root2):\n    if root1 is None and root2 is None:\n        return True\n    if root1 is None or root2 is None:\n        return False\n    return (root1.data == root2.data and\n            areIdentical(root1.left, root2.left) and\n            areIdentical(root1.right, root2.right))\n# Function to check if a tree is a subtree of another tree\ndef isSubtree(root1, root2):\n    if root2 is None:\n        return True\n    if root1 is None:\n        return False\n    if areIdentical(root1, root2):\n        return True\n    return isSubtree(root1.left, root2) or isSubtree(root1.right, root2)\n# Read the number of nodes in the main tree\nN = int(input())\n# Read the ages of the chefs in the main tree\nages = [int(input()) for _ in range(N)]\n# Read the edges of the main tree\nfor _ in range(N - 1):\n    A, B, C = input().split()\n# Read the number of queries\nQ = int(input())\nfor _ in range(Q):\n    # Read the number of nodes in the subtree\n    M = int(input())\n    # Read the ages of the chefs in the subtree\n    ages2 = [int(input()) for _ in range(M)]\n    # Read the edges of the subtree\n    for _ in range(M - 1):\n        A, B, C = input().split()\n    # Check if the subtree is a subtree of the main tree\n    if isSubtree(None, None):\n        print(\"YES\")\n    else:\n        print(\"NO\")",
      "JavaScript": "const fs = require('fs');\nconst input = fs.readFileSync(0, 'utf-8').trim().split('\\n');\nlet idx = 0;\n// Define the structure for a tree node\nclass Node {\n    constructor(data) {\n        this.data = data;\n        this.left = null;\n        this.right = null;\n    }\n}\n// Function to check if two trees are identical\nfunction areIdentical(root1, root2) {\n    if (root1 === null && root2 === null)\n        return true;\n    if (root1 === null || root2 === null)\n        return false;\n    return (root1.data === root2.data &&\n            areIdentical(root1.left, root2.left) &&\n            areIdentical(root1.right, root2.right));\n}\n// Function to check if a tree is a subtree of another tree\nfunction isSubtree(root1, root2) {\n    if (root2 === null)\n        return true;\n    if (root1 === null)\n        return false;\n    if (areIdentical(root1, root2))\n        return true;\n    return isSubtree(root1.left, root2) || isSubtree(root1.right, root2);\n}\n// Read the number of nodes in the main tree\nlet N = parseInt(input[idx++]);\n// Read the ages of the chefs in the main tree\nlet ages = [];\nfor (let i = 0; i < N; i++) {\n    ages.push(parseInt(input[idx++]));\n}\n// Read the edges of the main tree\nfor (let i = 0; i < N - 1; i++) {\n    let [A, B, C] = input[idx++].split(' ');\n}\n// Read the number of queries\nlet Q = parseInt(input[idx++]);\nfor (let i = 0; i < Q; i++) {\n    // Read the number of nodes in the subtree\n    let M = parseInt(input[idx++]);\n    // Read the ages of the chefs in the subtree\n    let ages2 = [];\n    for (let j = 0; j < M; j++) {\n        ages2.push(parseInt(input[idx++]));\n    }\n    // Read the edges of the subtree\n    for (let j = 0; j < M - 1; j++) {\n        let [A, B, C] = input[idx++].split(' ');\n    }\n    // Check if the subtree is a subtree of the main tree\n    if (isSubtree(null, null)) {\n        console.log('YES');\n    } else {\n        console.log('NO');\n    }\n}"
    }
  },
  "QBCategory": "CCD",
  "boilerPlates": {
    "C": "#include <stdio.h>\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "C++": "#include <iostream>\nusing namespace std;\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "Java": "import java.util.*;\nimport java.io.*;\n\npublic class Main {\n    public static void main(String[] args) throws IOException {\n        // your code goes here\n    }\n}",
    "Python3": "# your code goes here\n",
    "JavaScript": "// your code goes here\n"
  },
  "_internal": {
    "methodName": "",
    "params": []
  }
}