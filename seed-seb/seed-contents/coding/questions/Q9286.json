{
  "questionId": "Q9286",
  "title": "Increasing Addition",
  "slug": "increasing-addition",
  "version": 1,
  "createdAt": "2026-07-02",
  "updatedAt": "2026-07-04",
  "metadata": {
    "category": "Arrays | Greedy",
    "difficulty": "Hard",
    "isPremium": true,
    "tags": [
      "Prefix sums",
      "Greedy",
      "Array updates",
      "Online queries"
    ],
    "companyTags": [],
    "topicTags": [
      "Data Structures"
    ],
    "QBCategory": "CCD"
  },
  "content": {
    "problemStatement": "You have an array AAA of length NNN. In one operation, you can do the following:\n\n\nSelect any subarray AL…RA_{L \\ldots R}AL…R​ (1≤L≤R≤N)(1 \\le L \\le R \\le N)(1≤L≤R≤N) and then add iii to the iii-th element of this subarray. Formally, we set Aj:=Aj+(j−L+1)A_j := A_j + (j - L + 1)Aj​:=Aj​+(j−L+1) for all L≤j≤RL \\le j \\le RL≤j≤R.\nFor example: if A=[2,1,5,2]A = [2, 1, 5, 2]A=[2,1,5,2] and we apply operation on A2…4A_{2 \\ldots 4}A2…4​ then AAA becomes [2,1+1,5+2,2+3]=[2,2,7,5][2, 1 + 1, 5 + 2, 2 + 3] = [2, 2, 7, 5][2,1+1,5+2,2+3]=[2,2,7,5].\n\nThe minimum number of such operations required to sort AAA in non-decreasing order is termed as the goodness of AAA.\n\nWe want to process QQQ updates on AAA of the following form:\n\n\ni&nbsp;x\\texttt{i x}i&nbsp;x : Set Ai:=xA_i := xAi​:=x where (1≤i≤N1 \\le i \\le N1≤i≤N).\nDetermine the goodness of AAA after each update.\n\n\nInput Format\n\nThe first line contains a single integer TTT — the number of test cases. Then the test cases follow.\nThe first line of each test case contains two integers NNN and QQQ — the size of the array AAA and the number of updates.\nThe second line of each test case contains NNN space-separated integers A1,A2,…,ANA_1, A_2, \\dots, A_NA1​,A2​,…,AN​ denoting the array AAA.\nThe next QQQ lines each contain two integers iii and xxx denoting the parameters of the update.\n\n\nOutput Format\nFor each test case, output QQQ integers, the goodness of the array after each update.",
    "constraints": [
      "1 ≤ T ≤ 10^4",
      "2 ≤ N ≤ 10^5",
      "1 ≤ Q ≤ N",
      "1 ≤ Ai , x ≤ 10^9",
      "Sum of N over all test cases ≤ 2·10^5"
    ],
    "inputFormat": "The first line contains an integer T, the number of test cases. For each test case:\n- The first line contains two integers N and Q.\n- The second line contains N integers A1 … AN.\n- Each of the next Q lines contains two integers i and x, meaning set Ai := x.\nAll indices are 1‑based.",
    "outputFormat": "For each test case output Q lines. The k‑th line of a test case is the goodness of the array after applying the k‑th update.",
    "sampleTestCases": [
      {
        "id": "sample_1",
        "input": "2\n3 2\n3 2 1\n3 10\n2 1\n4 2\n2 2 2 2\n4 2\n4 1",
        "output": "1\n2\n0\n1",
        "explanation": "Test case 1:\nInitial array [3,2,1] has drops 3-2=1 and 2-1=1, sum=2 (goodness before any query, not printed).\nAfter first update (3,10) array becomes [3,2,10]; drops: 3-2=1, 2-10=0 → goodness=1.\nAfter second update (2,1) array becomes [3,1,10]; drops: 3-1=2, 1-10=0 → goodness=2.\n\nTest case 2:\nInitial [2,2,2,2] already sorted → goodness=0.\nFirst update does not change the array, still 0.\nSecond update changes last element to 1 → array [2,2,2,1]; only drop is 2-1=1 → goodness=1."
      }
    ]
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "label": "Case 1",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_2",
        "label": "Case 2",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_3",
        "label": "Case 3",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_4",
        "label": "Case 4",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_5",
        "label": "Case 5",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      }
    ],
    "totalTestCases": 5,
    "totalWeight": 10
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      2,
      2,
      2,
      2,
      2
    ],
    "maxScore": 10
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "For an array A the minimum number of allowed operations needed to make it non‑decreasing equals the total amount by which the array \"drops\" when moving from left to right:\n    goodness = Σ_{i=1}^{N-1} max(0, A_i - A_{i+1}).\nProof sketch: Consider any adjacent pair (i,i+1). If A_i ≤ A_{i+1} the pair already satisfies the order and never forces an operation. If A_i > A_{i+1} we must increase A_{i+1} (or later elements) enough to eliminate the drop. The only way to increase A_{i+1} without also increasing A_i is to use an operation whose right end is i (i.e. the operation ends at position i). Such an operation adds exactly 1 to A_i and 0 to A_{i+1}, reducing the drop by 1. Hence each unit of drop requires a distinct operation, and drops are independent, giving the sum formula.\n\nWhen a single element Ai is changed, only the two adjacent drops that involve Ai can change: the pair (i‑1,i) and the pair (i,i+1). We keep the current answer and update it in O(1) by recomputing those two contributions.\n\nAlgorithm per test case:\n1. Read the array, compute initial answer = Σ max(0, A[i] - A[i+1]).\n2. For each query (i,x):\n   a. For neighbor i‑1 (if i>1) compute old = max(0, A[i-1] - A[i]), new = max(0, A[i-1] - x) and add (new‑old) to answer.\n   b. For neighbor i (if i<N) compute old = max(0, A[i] - A[i+1]), new = max(0, x - A[i+1]) and add (new‑old) to answer.\n   c. Set A[i] = x.\n   d. Output the current answer.\nAll operations are O(1), so total complexity is O(N + Q) per test case.",
    "timeComplexity": "O(N + Q) per test case, i.e. linear in the size of the input.",
    "spaceComplexity": "O(N) to store the array.",
    "code": {
      "C": "#include <stdio.h>\n#include <stdlib.h>\n\nstatic inline long long pos_drop(long long a, long long b) {\n    return a > b ? a - b : 0LL;\n}\n\nint main(){\n    int T; if(scanf(\"%d\", &T)!=1) return 0;\n    while(T--){\n        int N, Q; scanf(\"%d %d\", &N, &Q);\n        long long *A = (long long*)malloc((N+2)*sizeof(long long));\n        for(int i=1;i<=N;i++) scanf(\"%lld\", &A[i]);\n        long long ans = 0;\n        for(int i=1;i<N;i++) ans += pos_drop(A[i], A[i+1]);\n        while(Q--){\n            int idx; long long x; scanf(\"%d %lld\", &idx, &x);\n            if(idx>1){\n                long long old = pos_drop(A[idx-1], A[idx]);\n                long long nw = pos_drop(A[idx-1], x);\n                ans += nw - old;\n            }\n            if(idx<N){\n                long long old = pos_drop(A[idx], A[idx+1]);\n                long long nw = pos_drop(x, A[idx+1]);\n                ans += nw - old;\n            }\n            A[idx] = x;\n            printf(\"%lld\\n\", ans);\n        }\n        free(A);\n    }\n    return 0;\n}\n",
      "C++": "#include <bits/stdc++.h>\nusing namespace std;\n\nstatic inline long long pos_drop(long long a, long long b){\n    return a > b ? a - b : 0LL;\n}\n\nint main(){\n    ios::sync_with_stdio(false);\n    cin.tie(nullptr);\n    int T; if(!(cin>>T)) return 0;\n    while(T--){\n        int N,Q; cin>>N>>Q;\n        vector<long long>A(N+2);\n        for(int i=1;i<=N;i++) cin>>A[i];\n        long long ans=0;\n        for(int i=1;i<N;i++) ans+=pos_drop(A[i],A[i+1]);\n        while(Q--){\n            int idx; long long x; cin>>idx>>x;\n            if(idx>1){\n                long long old=pos_drop(A[idx-1],A[idx]);\n                long long nw=pos_drop(A[idx-1],x);\n                ans+=nw-old;\n            }\n            if(idx<N){\n                long long old=pos_drop(A[idx],A[idx+1]);\n                long long nw=pos_drop(x,A[idx+1]);\n                ans+=nw-old;\n            }\n            A[idx]=x;\n            cout<<ans<<\"\\n\";\n        }\n    }\n    return 0;\n}\n",
      "Java": "import java.io.*;\nimport java.util.*;\n\npublic class Main {\n    private static long posDrop(long a, long b){\n        return a > b ? a - b : 0L;\n    }\n    public static void main(String[] args) throws Exception {\n        FastScanner fs = new FastScanner(System.in);\n        int T = fs.nextInt();\n        StringBuilder out = new StringBuilder();\n        while (T-- > 0) {\n            int N = fs.nextInt();\n            int Q = fs.nextInt();\n            long[] A = new long[N + 2];\n            for (int i = 1; i <= N; i++) A[i] = fs.nextLong();\n            long ans = 0;\n            for (int i = 1; i < N; i++) ans += posDrop(A[i], A[i + 1]);\n            for (int q = 0; q < Q; q++) {\n                int idx = fs.nextInt();\n                long x = fs.nextLong();\n                if (idx > 1) {\n                    long old = posDrop(A[idx - 1], A[idx]);\n                    long nw = posDrop(A[idx - 1], x);\n                    ans += nw - old;\n                }\n                if (idx < N) {\n                    long old = posDrop(A[idx], A[idx + 1]);\n                    long nw = posDrop(x, A[idx + 1]);\n                    ans += nw - old;\n                }\n                A[idx] = x;\n                out.append(ans).append('\\n');\n            }\n        }\n        System.out.print(out.toString());\n    }\n\n    // Fast scanner for large input\n    static class FastScanner {\n        private final InputStream in;\n        private final byte[] buffer = new byte[1 << 16];\n        private int ptr = 0, len = 0;\n        FastScanner(InputStream in){ this.in = in; }\n        private int readByte() throws IOException {\n            if (ptr >= len) {\n                len = in.read(buffer);\n                ptr = 0;\n                if (len <= 0) return -1;\n            }\n            return buffer[ptr++];\n        }\n        long nextLong() throws IOException {\n            int c; do { c = readByte(); } while (c <= ' ');\n            boolean neg = false;\n            if (c == '-') { neg = true; c = readByte(); }\n            long val = 0;\n            while (c > ' ') {\n                val = val * 10 + (c - '0');\n                c = readByte();\n            }\n            return neg ? -val : val;\n        }\n        int nextInt() throws IOException { return (int) nextLong(); }\n    }\n}\n",
      "Python3": "import sys\n\ndef pos_drop(a, b):\n    return a - b if a > b else 0\n\ndef solve():\n    data = sys.stdin.read().strip().split()\n    if not data:\n        return\n    it = iter(data)\n    t = int(next(it))\n    out_lines = []\n    for _ in range(t):\n        n = int(next(it)); q = int(next(it))\n        A = [0] + [int(next(it)) for _ in range(n)]  # 1‑based\n        ans = sum(pos_drop(A[i], A[i+1]) for i in range(1, n))\n        for _ in range(q):\n            idx = int(next(it)); x = int(next(it))\n            if idx > 1:\n                old = pos_drop(A[idx-1], A[idx])\n                new = pos_drop(A[idx-1], x)\n                ans += new - old\n            if idx < n:\n                old = pos_drop(A[idx], A[idx+1])\n                new = pos_drop(x, A[idx+1])\n                ans += new - old\n            A[idx] = x\n            out_lines.append(str(ans))\n    sys.stdout.write('\\n'.join(out_lines))\n\nif __name__ == \"__main__\":\n    solve()\n",
      "JavaScript": "const fs = require('fs');\nconst input = fs.readFileSync(0, 'utf-8').trim().split(/\\s+/);\nlet pos = 0;\nfunction next(){return input[pos++];}\nfunction posDrop(a,b){return a > b ? a - b : 0;}\nlet T = Number(next());\nlet out = [];\nfor(let tc=0; tc<T; tc++){\n  const N = Number(next());\n  const Q = Number(next());\n  const A = new Array(N+2).fill(0);\n  for(let i=1;i<=N;i++) A[i] = Number(next());\n  let ans = 0;\n  for(let i=1;i<N;i++) ans += posDrop(A[i], A[i+1]);\n  for(let q=0;q<Q;q++){\n    const idx = Number(next());\n    const x = Number(next());\n    if(idx>1){\n      const old = posDrop(A[idx-1], A[idx]);\n      const nw = posDrop(A[idx-1], x);\n      ans += nw - old;\n    }\n    if(idx<N){\n      const old = posDrop(A[idx], A[idx+1]);\n      const nw = posDrop(x, A[idx+1]);\n      ans += nw - old;\n    }\n    A[idx] = x;\n    out.push(String(ans));\n  }\n}\nconsole.log(out.join('\\n'));\n"
    }
  },
  "QBCategory": "CCD",
  "boilerPlates": {
    "C": "#include <stdio.h>\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "C++": "#include <iostream>\nusing namespace std;\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "Java": "import java.util.*;\nimport java.io.*;\n\npublic class Main {\n    public static void main(String[] args) throws IOException {\n        // your code goes here\n    }\n}",
    "Python3": "# your code goes here\n",
    "JavaScript": "// your code goes here\n"
  },
  "_internal": {
    "methodName": "",
    "params": []
  }
}