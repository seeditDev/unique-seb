{
  "questionId": "Q8176",
  "title": "HTML Tags",
  "slug": "html-tags",
  "version": 1,
  "createdAt": "2026-07-02",
  "updatedAt": "2026-07-04",
  "metadata": {
    "category": "Strings",
    "difficulty": "Medium",
    "isPremium": true,
    "tags": [
      "String parsing",
      "Validation"
    ],
    "companyTags": [],
    "topicTags": [
      "Strings"
    ],
    "QBCategory": "CCD"
  },
  "content": {
    "problemStatement": "In addition to Competitive Programming, Chef recently developed an interest in Web Dev and started learning HTML. Now he wants to create his own HTML Code Editor. As a subproblem, he wants to check if a typed HTML closing tag has correct syntax or not.\n\nA closing HTML tag **must**:\n\n\nStart with `\"&lt;/\"`\nEnd with `\"&gt;\"`\nHave only lower-case alpha-numeric characters as its body (between `\"&lt;/\"` and `\"&gt;\"`). That is, each character of the body should either be a digit or a lower-case English letter.\nHave a non-empty body.\nHelp Chef by printing `\"Success\"` if the tag is fine. If not, print `\"Error\"`.\n\n\nInput Format\n\nThe first line contains an integer TTT, the number of test cases. Then TTT test cases follow.\nEach test case is a single line of input, a string describing the tag.\n\n\nOutput Format\nFor each test case, output in a single line, `\"Success\"` if it is a valid closing tag and `\"Error\"` otherwise (without quotes).\n\n\nYou may print each character of the string in uppercase or lowercase (for example, the strings `\"SuccEss\"`, `\"success\"`, `\"Success\"`, `\"SUCCESS\"` etc. will all be treated as identical).",
    "constraints": [
      "1 <= T <= 1000",
      "1 <= length(tag) <= 1000",
      "All characters of the tag are in the ASCII range [33,126]"
    ],
    "inputFormat": "The first line contains an integer T, the number of test cases. Each of the next T lines contains a non‑empty string representing a tag. The string consists of visible ASCII characters (code 33 to 126) and does not contain spaces.",
    "outputFormat": "For each test case output a single line containing \"Success\" if the tag is a valid closing HTML tag, otherwise output \"Error\".",
    "sampleTestCases": [
      {
        "id": "sample_1",
        "input": "5\n</h1>\nClearly_Invalid\n</singlabharat>\n</5>\n<//aA>",
        "output": "Success\nError\nSuccess\nSuccess\nError",
        "explanation": "Test case 1: </h1> starts with </, ends with > and body \"h1\" contains only lower‑case letters and digits → Success.\nTest case 2: \"Clearly_Invalid\" does not start with </ and contains an underscore and upper‑case letters → Error.\nTest case 3: </singlabharat> has body \"singlabharat\" which is all lower‑case letters → Success.\nTest case 4: </5> has body \"5\" which is a digit → Success.\nTest case 5: <//aA> body is \"/aA\" containing '/' and upper‑case 'A' → Error."
      }
    ]
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "label": "minimum boundary",
        "input": "1\n</a>",
        "expectedOutput": "Success",
        "weight": 2
      },
      {
        "id": "tc_2",
        "label": "empty body",
        "input": "1\n</>",
        "expectedOutput": "Error",
        "weight": 2
      },
      {
        "id": "tc_3",
        "label": "invalid characters",
        "input": "1\n</Abc123>",
        "expectedOutput": "Error",
        "weight": 2
      },
      {
        "id": "tc_4",
        "label": "long valid tag",
        "input": "1\n</abcdefghijklmnopqrstuvwxyz0123456789>",
        "expectedOutput": "Success",
        "weight": 2
      },
      {
        "id": "tc_5",
        "label": "missing closing angle bracket",
        "input": "1\n</valid",
        "expectedOutput": "Error",
        "weight": 2
      }
    ],
    "totalTestCases": 5,
    "totalWeight": 10
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      2,
      2,
      2,
      2,
      2
    ],
    "maxScore": 10
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "For each tag we perform a constant‑time series of checks:\n1. The tag must be at least 4 characters long (\"</>\" would have an empty body).\n2. The first two characters must be '<' and '/' and the last character must be '>'.\n3. Extract the substring between positions 2 and length‑2 (the body). It must be non‑empty.\n4. Every character of the body must be either a digit ('0'‑'9') or a lower‑case letter ('a'‑'z').\nIf all checks pass we print \"Success\", otherwise \"Error\". The algorithm runs in O(L) per test case where L is the tag length, which is at most 1000, well within limits.",
    "timeComplexity": "O(T * L) where L is the length of each tag (≤ 1000).",
    "spaceComplexity": "O(1) additional space besides the input string.",
    "code": {
      "C": "#include <stdio.h>\n#include <string.h>\n#include <ctype.h>\n\nint main() {\n    int T;\n    if (scanf(\"%d\", &T) != 1) return 0;\n    char buf[1010];\n    // consume the newline after T\n    getchar();\n    while (T--) {\n        if (!fgets(buf, sizeof(buf), stdin)) break;\n        size_t n = strlen(buf);\n        // remove trailing newline if present\n        if (n && buf[n-1] == '\\n') buf[--n] = '\\0';\n        int ok = 1;\n        if (n < 4) ok = 0; // need at least </a>\n        else if (buf[0] != '<' || buf[1] != '/' || buf[n-1] != '>') ok = 0;\n        else {\n            // body is from index 2 to n-2 inclusive\n            if (n == 4) ok = 0; // empty body\n            else {\n                for (size_t i = 2; i < n-1; ++i) {\n                    if (!isdigit((unsigned char)buf[i]) && !(buf[i] >= 'a' && buf[i] <= 'z')) {\n                        ok = 0;\n                        break;\n                    }\n                }\n            }\n        }\n        puts(ok ? \"Success\" : \"Error\");\n    }\n    return 0;\n}",
      "C++": "#include <bits/stdc++.h>\nusing namespace std;\n\nint main() {\n    ios::sync_with_stdio(false);\n    cin.tie(nullptr);\n    int T;\n    if (!(cin >> T)) return 0;\n    string s;\n    getline(cin, s); // consume end of line after T\n    while (T--) {\n        getline(cin, s);\n        bool ok = true;\n        if (s.size() < 4) ok = false;\n        else if (s[0] != '<' || s[1] != '/' || s.back() != '>') ok = false;\n        else {\n            string body = s.substr(2, s.size() - 3);\n            if (body.empty()) ok = false;\n            else {\n                for (char c : body) {\n                    if (!isdigit(c) && !(c >= 'a' && c <= 'z')) { ok = false; break; }\n                }\n            }\n        }\n        cout << (ok ? \"Success\" : \"Error\") << '\\n';\n    }\n    return 0;\n}",
      "Java": "import java.io.*;\nimport java.util.*;\n\npublic class Main {\n    public static void main(String[] args) throws Exception {\n        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));\n        int T = Integer.parseInt(br.readLine().trim());\n        StringBuilder out = new StringBuilder();\n        for (int t = 0; t < T; t++) {\n            String s = br.readLine();\n            boolean ok = true;\n            if (s.length() < 4) ok = false;\n            else if (s.charAt(0) != '<' || s.charAt(1) != '/' || s.charAt(s.length() - 1) != '>') ok = false;\n            else {\n                String body = s.substring(2, s.length() - 1);\n                if (body.isEmpty()) ok = false;\n                else {\n                    for (char c : body.toCharArray()) {\n                        if (!Character.isDigit(c) && !(c >= 'a' && c <= 'z')) { ok = false; break; }\n                    }\n                }\n            }\n            out.append(ok ? \"Success\" : \"Error\").append('\\n');\n        }\n        System.out.print(out.toString());\n    }\n}",
      "Python3": "#!/usr/bin/env python3\nimport sys\n\ndef is_valid(tag: str) -> bool:\n    if len(tag) < 4:\n        return False\n    if not (tag.startswith(\"</\") and tag.endswith(\">\")):\n        return False\n    body = tag[2:-1]\n    if not body:\n        return False\n    for ch in body:\n        if not (ch.isdigit() or ('a' <= ch <= 'z')):\n            return False\n    return True\n\ndef main():\n    data = sys.stdin.read().splitlines()\n    if not data:\n        return\n    T = int(data[0].strip())\n    out = []\n    for i in range(1, T + 1):\n        tag = data[i].strip()\n        out.append(\"Success\" if is_valid(tag) else \"Error\")\n    sys.stdout.write(\"\\n\".join(out))\n\nif __name__ == \"__main__\":\n    main()",
      "JavaScript": "const fs = require('fs');\nconst lines = fs.readFileSync(0, 'utf-8').trim().split('\\n');\nlet idx = 0;\nconst T = parseInt(lines[idx++].trim(), 10);\nlet result = [];\nfor (let t = 0; t < T; t++) {\n  const s = lines[idx++]?.trim() ?? '';\n  let ok = true;\n  if (s.length < 4) ok = false;\n  else if (s[0] !== '<' || s[1] !== '/' || s[s.length - 1] !== '>') ok = false;\n  else {\n    const body = s.slice(2, -1);\n    if (body.length === 0) ok = false;\n    else {\n      for (let i = 0; i < body.length; i++) {\n        const ch = body[i];\n        if (!(/[0-9]/.test(ch) || /[a-z]/.test(ch))) { ok = false; break; }\n      }\n    }\n  }\n  result.push(ok ? 'Success' : 'Error');\n}\nconsole.log(result.join('\\n'));"
    }
  },
  "QBCategory": "CCD",
  "boilerPlates": {
    "C": "#include <stdio.h>\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "C++": "#include <iostream>\nusing namespace std;\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "Java": "import java.util.*;\nimport java.io.*;\n\npublic class Main {\n    public static void main(String[] args) throws IOException {\n        // your code goes here\n    }\n}",
    "Python3": "# your code goes here\n",
    "JavaScript": "// your code goes here\n"
  },
  "_internal": {
    "methodName": "",
    "params": []
  }
}