{
  "questionId": "Q5795",
  "title": "Nomad Camp",
  "slug": "nomad-camp",
  "version": 1,
  "createdAt": "2026-07-01",
  "updatedAt": "2026-07-04",
  "metadata": {
    "category": "Math",
    "difficulty": "Hard",
    "isPremium": true,
    "tags": [
      "Geometry",
      "Circumcircle",
      "Triangle Area"
    ],
    "companyTags": [],
    "topicTags": [
      "Geometry"
    ],
    "QBCategory": "CCD"
  },
  "content": {
    "problemStatement": "A nomadic group wants to make camp inside a large forest having thousands of trees - all very tall, slick, and upright, with leaves and branches much above man-height. They will pitch a single tent, with the canvas of the tent conical in shape, and the tent has three long ropes fitted with the canvas-base that needs to be tied up with the bases of three trees so that the tree-bases form an acute-angled triangle (T). The circumcircle (C) of the points in T form the camp-base within which the nomads will set up all their other activities, so they naturally want to make their camp-base as large as possible and free of any other trees, although there may be some trees standing on the circumference of C. However, the nomads are all deadly against cutting any tree. Imagine that you are the nomad leader and have got the map of the forest. From this map, you find that the forest belongs to a planar land surface, and you also get the coordinates of all the tree bases. How can you compute the area of the largest camp-base that can be made? (Use π=3.14\\pi=3.14π=3.14 exactly)\n\n\nInput Format\nThe first line contains the number of test cases **N**.\n\n\nFor each test case, the first line contains the number of trees **X** followed by **X** lines, each containing the **x** and **y**-coordinate (in that order) of one tree (both positive integers).\n\n\n\nOutput Format\nFor each test case, print the case number, followed by a colon, followed by a single space, followed by a single integer indicating the area of the largest camp base. The integer should be the floor of the area computed (for example, if the area computed is 123.7, output 123). If no camp can be made satisfying the given conditions, print -1 for the area.",
    "constraints": [
      "1 <= T <= 30",
      "1 <= X <= 5000",
      "1 <= x, y <= 10000"
    ],
    "inputFormat": "The first line contains the number of test cases T. Each test case contains a single integer X, the number of trees, followed by X lines, each containing the x and y-coordinates of a tree.",
    "outputFormat": "For each test case, print the case number, followed by a colon, followed by a single space, followed by a single integer indicating the area of the largest camp base. The integer should be the floor of the computed area.",
    "sampleTestCases": [
      {
        "id": "sample_1",
        "input": "1\n4\n1 1\n10 1\n10 5\n5 4",
        "output": "Case 1: 27",
        "explanation": "The largest camp base that can be made has an area of 27, which is the floor of the computed area."
      },
      {
        "id": "sample_2",
        "input": "1\n3\n0 0\n3 0\n0 4",
        "output": "Case 1: 12",
        "explanation": "The largest camp base that can be made has an area of 12, which is the floor of the computed area."
      },
      {
        "id": "sample_3",
        "input": "1\n2\n1 1\n2 2",
        "output": "Case 1: -1",
        "explanation": "No camp base can be made with only two trees."
      }
    ]
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "label": "minimum boundary",
        "input": "1\n3\n0 0\n0 1\n1 0",
        "expectedOutput": "Case 1: 0",
        "weight": 2
      },
      {
        "id": "tc_2",
        "label": "maximum boundary",
        "input": "1\n3\n10000 10000\n10000 0\n0 10000",
        "expectedOutput": "Case 1: 785398163",
        "weight": 2
      },
      {
        "id": "tc_3",
        "label": "uniform/edge case",
        "input": "1\n3\n0 0\n0 0\n0 0",
        "expectedOutput": "Case 1: -1",
        "weight": 2
      },
      {
        "id": "tc_4",
        "label": "tricky case",
        "input": "1\n4\n0 0\n3 0\n0 4\n3 4",
        "expectedOutput": "Case 1: 12",
        "weight": 2
      },
      {
        "id": "tc_5",
        "label": "stress/general",
        "input": "2\n3\n0 0\n3 0\n0 4\n3\n0 0\n3 0\n0 4",
        "expectedOutput": "Case 1: 12\nCase 2: 12",
        "weight": 2
      }
    ],
    "totalTestCases": 5,
    "totalWeight": 10
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      2,
      2,
      2,
      2,
      2
    ],
    "maxScore": 10
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "To find the area of the largest camp base, we need to find the largest circumcircle of a triangle formed by three trees. We can do this by iterating over all possible combinations of three trees and calculating the area of the circumcircle for each combination. The area of the circumcircle can be calculated using the formula A = π * R^2, where R is the radius of the circumcircle. The radius of the circumcircle can be calculated using the formula R = (a * b * c) / (4 * area), where a, b, and c are the side lengths of the triangle and area is the area of the triangle.",
    "timeComplexity": "O(T * X^3) per test case, O(T * X^3) total. The time complexity is cubic because we are iterating over all possible combinations of three trees.",
    "spaceComplexity": "O(X) — the dominant structure is the array of tree coordinates.",
    "code": {
      "C": "#include <stdio.h>\n#include <math.h>\n#define PI 3.14\n\nint main() {\n    int T;\n    scanf(\"%d\", &T);\n    for (int i = 0; i < T; i++) {\n        int X;\n        scanf(\"%d\", &X);\n        int x[X], y[X];\n        for (int j = 0; j < X; j++) {\n            scanf(\"%d %d\", &x[j], &y[j]);\n        }\n        double maxArea = 0;\n        for (int j = 0; j < X; j++) {\n            for (int k = j + 1; k < X; k++) {\n                for (int l = k + 1; l < X; l++) {\n                    double a = sqrt(pow(x[j] - x[k], 2) + pow(y[j] - y[k], 2));\n                    double b = sqrt(pow(x[k] - x[l], 2) + pow(y[k] - y[l], 2));\n                    double c = sqrt(pow(x[l] - x[j], 2) + pow(y[l] - y[j], 2));\n                    double s = (a + b + c) / 2;\n                    double area = sqrt(s * (s - a) * (s - b) * (s - c));\n                    if (area > 0) {\n                        double R = (a * b * c) / (4 * area);\n                        double circumArea = PI * pow(R, 2);\n                        if (circumArea > maxArea) {\n                            maxArea = circumArea;\n                        }\n                    }\n                }\n            }\n        }\n        if(maxArea == 0) printf(\"Case %d: -1\\n\", i + 1);\n        else printf(\"Case %d: %d\\n\", i + 1, (int)floor(maxArea));\n    }\n    return 0;\n}",
      "C++": "#include <iostream>\n#include <cmath>\nusing namespace std;\n#define PI 3.14\n\nint main() {\n    int T;\n    cin >> T;\n    for (int i = 0; i < T; i++) {\n        int X;\n        cin >> X;\n        int x[X], y[X];\n        for (int j = 0; j < X; j++) {\n            cin >> x[j] >> y[j];\n        }\n        double maxArea = 0;\n        for (int j = 0; j < X; j++) {\n            for (int k = j + 1; k < X; k++) {\n                for (int l = k + 1; l < X; l++) {\n                    double a = sqrt(pow(x[j] - x[k], 2) + pow(y[j] - y[k], 2));\n                    double b = sqrt(pow(x[k] - x[l], 2) + pow(y[k] - y[l], 2));\n                    double c = sqrt(pow(x[l] - x[j], 2) + pow(y[l] - y[j], 2));\n                    double s = (a + b + c) / 2;\n                    double area = sqrt(s * (s - a) * (s - b) * (s - c));\n                    if (area > 0) {\n                        double R = (a * b * c) / (4 * area);\n                        double circumArea = PI * pow(R, 2);\n                        if (circumArea > maxArea) {\n                            maxArea = circumArea;\n                        }\n                    }\n                }\n            }\n        }\n        if(maxArea == 0) cout << \"Case \" << i + 1 << \": -1\" << endl;\n        else cout << \"Case \" << i + 1 << \": \" << (int)floor(maxArea) << endl;\n    }\n    return 0;\n}",
      "Java": "import java.util.Scanner;\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int T = sc.nextInt();\n        for (int i = 0; i < T; i++) {\n            int X = sc.nextInt();\n            int[] x = new int[X];\n            int[] y = new int[X];\n            for (int j = 0; j < X; j++) {\n                x[j] = sc.nextInt();\n                y[j] = sc.nextInt();\n            }\n            double maxArea = 0;\n            for (int j = 0; j < X; j++) {\n                for (int k = j + 1; k < X; k++) {\n                    for (int l = k + 1; l < X; l++) {\n                        double a = Math.sqrt(Math.pow(x[j] - x[k], 2) + Math.pow(y[j] - y[k], 2));\n                        double b = Math.sqrt(Math.pow(x[k] - x[l], 2) + Math.pow(y[k] - y[l], 2));\n                        double c = Math.sqrt(Math.pow(x[l] - x[j], 2) + Math.pow(y[l] - y[j], 2));\n                        double s = (a + b + c) / 2;\n                        double area = Math.sqrt(s * (s - a) * (s - b) * (s - c));\n                        if (area > 0) {\n                            double R = (a * b * c) / (4 * area);\n                            double circumArea = 3.14 * Math.pow(R, 2);\n                            if (circumArea > maxArea) {\n                                maxArea = circumArea;\n                            }\n                        }\n                    }\n                }\n            }\n            if(maxArea == 0) System.out.println(\"Case \" + (i + 1) + \": -1\");\n            else System.out.println(\"Case \" + (i + 1) + \": \" + (int)Math.floor(maxArea));\n        }\n    }\n}",
      "Python3": "import math\nimport sys\nT = int(sys.stdin.readline())\nfor i in range(T):\n    X = int(sys.stdin.readline())\n    x = []\n    y = []\n    for j in range(X):\n        a, b = map(int, sys.stdin.readline().split())\n        x.append(a)\n        y.append(b)\n    maxArea = 0\n    for j in range(X):\n        for k in range(j + 1, X):\n            for l in range(k + 1, X):\n                a = math.sqrt((x[j] - x[k])**2 + (y[j] - y[k])**2)\n                b = math.sqrt((x[k] - x[l])**2 + (y[k] - y[l])**2)\n                c = math.sqrt((x[l] - x[j])**2 + (y[l] - y[j])**2)\n                s = (a + b + c) / 2\n                area = math.sqrt(s * (s - a) * (s - b) * (s - c))\n                if area > 0:\n                    R = (a * b * c) / (4 * area)\n                    circumArea = 3.14 * R**2\n                    if circumArea > maxArea:\n                        maxArea = circumArea\n    if maxArea == 0: print(f\"Case {i + 1}: -1\")\n    else: print(f\"Case {i + 1}: {math.floor(maxArea)}\")",
      "JavaScript": "const lines = require('fs').readFileSync(0, 'utf-8').trim().split('\\n');\nlet i = 0;\nconst T = parseInt(lines[i++]);\nfor (let t = 0; t < T; t++) {\n    const X = parseInt(lines[i++]);\n    const x = [];\n    const y = [];\n    for (let j = 0; j < X; j++) {\n        const [a, b] = lines[i++].split(' ').map(Number);\n        x.push(a);\n        y.push(b);\n    }\n    let maxArea = 0;\n    for (let j = 0; j < X; j++) {\n        for (let k = j + 1; k < X; k++) {\n            for (let l = k + 1; l < X; l++) {\n                const a = Math.sqrt((x[j] - x[k])**2 + (y[j] - y[k])**2);\n                const b = Math.sqrt((x[k] - x[l])**2 + (y[k] - y[l])**2);\n                const c = Math.sqrt((x[l] - x[j])**2 + (y[l] - y[j])**2);\n                const s = (a + b + c) / 2;\n                const area = Math.sqrt(s * (s - a) * (s - b) * (s - c));\n                if (area > 0) {\n                    const R = (a * b * c) / (4 * area);\n                    const circumArea = 3.14 * R**2;\n                    if (circumArea > maxArea) {\n                        maxArea = circumArea;\n                    }\n                }\n            }\n        }\n    }\n    if(maxArea == 0) console.log(`Case ${t + 1}: -1`);\n    else console.log(`Case ${t + 1}: ${Math.floor(maxArea)}`);\n}"
    }
  },
  "QBCategory": "CCD",
  "boilerPlates": {
    "C": "#include <stdio.h>\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "C++": "#include <iostream>\nusing namespace std;\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "Java": "import java.util.*;\nimport java.io.*;\n\npublic class Main {\n    public static void main(String[] args) throws IOException {\n        // your code goes here\n    }\n}",
    "Python3": "# your code goes here\n",
    "JavaScript": "// your code goes here\n"
  },
  "_internal": {
    "methodName": "",
    "params": []
  }
}