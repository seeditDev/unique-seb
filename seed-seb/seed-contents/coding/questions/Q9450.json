{
  "questionId": "Q9450",
  "title": "Interesting Array",
  "slug": "interesting-array",
  "version": 1,
  "createdAt": "2026-07-02",
  "updatedAt": "2026-07-04",
  "metadata": {
    "category": "Arrays | Greedy | Sorting",
    "difficulty": "Hard",
    "isPremium": true,
    "tags": [
      "Frequency check",
      "Interleaving",
      "Sorting",
      "Greedy construction"
    ],
    "companyTags": [],
    "topicTags": [
      "Constructive Algorithms"
    ],
    "QBCategory": "CCD"
  },
  "content": {
    "problemStatement": "An array is called interesting if no subarray of length **greater than** 222 is non-increasing or non-decreasing.\n\nChef has an array AAA of length NNN. He wants to make the array **interesting** by rearranging the elements in any order.\n\nIf there exist multiple such arrays, output any one.\n\nIf no such array exists, print −1-1−1 instead.\n\n\nInput Format\n\nThe first line of input will contain a single integer TTT, denoting the number of test cases.\nEach test case consists of two lines of input.\n\nThe first line of each test case contains an integer NNN denoting the length of array AAA.\nThe next line contains NNN space separated integers, A1,A2,…,ANA_1, A_2, \\ldots, A_NA1​,A2​,…,AN​\n\n\n\n\nOutput Format\nFor each test case, output on a single line, any possible **interesting** array.\nIf no such array is possible, output −1-1−1.",
    "constraints": [
      "1 ≤ T ≤ 100",
      "1 ≤ N ≤ 10^5",
      "1 ≤ Ai ≤ 10^9",
      "Sum of N over all test cases ≤ 2·10^5"
    ],
    "inputFormat": "The first line contains an integer T, the number of test cases. Each test case consists of two lines. The first line contains an integer N, the length of the array. The second line contains N space‑separated integers A1 … AN.",
    "outputFormat": "For each test case output a single line. If it is possible to reorder the given numbers into an interesting array, print any such ordering as N space‑separated integers. If it is impossible, print -1.",
    "sampleTestCases": [
      {
        "id": "sample_1",
        "input": "3\n3\n2 2 2\n4\n2 6 5 2\n5\n5 5 2 4 2",
        "output": "-1\n5 2 6 2\n4 2 5 2 5",
        "explanation": "Test case 1: All elements are equal, frequency 3 > floor(3/2)=1, so impossible.\\nTest case 2: Sorted array is [2,2,5,6]; interleaving gives [5,2,6,2] which has no monotone subarray of length ≥3.\\nTest case 3: Sorted array is [2,2,4,5,5]; interleaving gives [4,2,5,2,5] which satisfies the condition."
      }
    ]
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "label": "minimum boundary",
        "input": "1\n1\n42",
        "expectedOutput": "42",
        "weight": 2
      },
      {
        "id": "tc_2",
        "label": "maximum boundary",
        "input": "1\n6\n1 2 3 4 5 6",
        "expectedOutput": "4 1 5 2 6 3",
        "weight": 2
      },
      {
        "id": "tc_3",
        "label": "edge case equal frequencies",
        "input": "1\n4\n7 7 8 8",
        "expectedOutput": "8 7 8 7",
        "weight": 2
      },
      {
        "id": "tc_4",
        "label": "general case 1",
        "input": "1\n7\n10 20 20 30 40 50 60",
        "expectedOutput": "40 10 50 20 60 20 30",
        "weight": 2
      },
      {
        "id": "tc_5",
        "label": "general case 2",
        "input": "1\n5\n9 9 9 1 2",
        "expectedOutput": "-1",
        "weight": 2
      }
    ],
    "totalTestCases": 5,
    "totalWeight": 10
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      2,
      2,
      2,
      2,
      2
    ],
    "maxScore": 10
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "An array is interesting iff no subarray of length ≥ 3 is monotone (non‑decreasing or non‑increasing). This is equivalent to requiring that every three consecutive elements are not monotone. If a value appears more than ⌊N/2⌋ times, any arrangement will contain three equal (or monotone) elements, so the answer is impossible. Otherwise we can always build a valid ordering: sort the array, split it into a lower half L (first ⌊N/2⌋ elements) and an upper half R (the remaining elements). Then output elements by alternating R[i] followed by L[i] for i = 0 … |L|‑1, and finally the last element of R if N is odd. This interleaving guarantees that any three consecutive elements consist of two from R and one from L or vice‑versa, which cannot be monotone because every element of R is ≥ every element of L and the strict inequality holds for at least one pair (otherwise the frequency condition would be violated). The construction runs in O(N log N) per test case due to sorting.",
    "timeComplexity": "O(N log N) per test case (sorting dominates).",
    "spaceComplexity": "O(N) for storing the sorted array and the result.",
    "code": {
      "C": "#include <stdio.h>\n#include <stdlib.h>\nint cmp(const void *a, const void *b){return (*(int*)a)-(*(int*)b);} \nint main(){\n    int T; if(scanf(\"%d\",&T)!=1) return 0;\n    while(T--){\n        int N; scanf(\"%d\",&N);\n        int *a = (int*)malloc(N*sizeof(int));\n        for(int i=0;i<N;i++) scanf(\"%d\",&a[i]);\n        if(N<=2){\n            for(int i=0;i<N;i++) printf(\"%d%s\",a[i], i+1==N?\"\\n\":\" \");\n            free(a); continue;\n        }\n        qsort(a,N,sizeof(int),cmp);\n        int maxFreq=1, cur=1;\n        for(int i=1;i<N;i++){\n            if(a[i]==a[i-1]) cur++; else{ if(cur>maxFreq) maxFreq=cur; cur=1; }\n        }\n        if(cur>maxFreq) maxFreq=cur;\n        if(maxFreq > N/2){ printf(\"-1\\n\"); free(a); continue; }\n        int k = N/2;\n        int *res = (int*)malloc(N*sizeof(int));\n        int idx=0;\n        for(int i=0;i<k;i++){\n            res[idx++] = a[k+i];\n            res[idx++] = a[i];\n        }\n        if(N%2) res[idx++] = a[N-1];\n        for(int i=0;i<N;i++) printf(\"%d%s\",res[i], i+1==N?\"\\n\":\" \");\n        free(a); free(res);\n    }\n    return 0;\n}",
      "C++": "#include <bits/stdc++.h>\nusing namespace std;\nint main(){\n    ios::sync_with_stdio(false);\n    cin.tie(nullptr);\n    int T; if(!(cin>>T)) return 0;\n    while(T--){\n        int N; cin>>N;\n        vector<long long> a(N);\n        for(int i=0;i<N;i++) cin>>a[i];\n        if(N<=2){\n            for(int i=0;i<N;i++) cout<<a[i]<<(i+1==N?'\\n':' ');\n            continue;\n        }\n        sort(a.begin(), a.end());\n        int maxFreq=1, cur=1;\n        for(int i=1;i<N;i++){\n            if(a[i]==a[i-1]) cur++; else{ maxFreq=max(maxFreq,cur); cur=1; }\n        }\n        maxFreq=max(maxFreq,cur);\n        if(maxFreq > N/2){ cout<<-1<<'\\n'; continue; }\n        int k=N/2;\n        vector<long long> res; res.reserve(N);\n        for(int i=0;i<k;i++){\n            res.push_back(a[k+i]);\n            res.push_back(a[i]);\n        }\n        if(N%2) res.push_back(a.back());\n        for(int i=0;i<N;i++) cout<<res[i]<<(i+1==N?'\\n':' ');\n    }\n    return 0;\n}",
      "Java": "import java.io.*;\nimport java.util.*;\npublic class Main {\n    public static void main(String[] args) throws Exception {\n        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));\n        StringBuilder out = new StringBuilder();\n        int T = Integer.parseInt(br.readLine().trim());\n        while (T-- > 0) {\n            int N = Integer.parseInt(br.readLine().trim());\n            long[] a = new long[N];\n            StringTokenizer st = new StringTokenizer(br.readLine());\n            for (int i = 0; i < N; i++) a[i] = Long.parseLong(st.nextToken());\n            if (N <= 2) {\n                for (int i = 0; i < N; i++) out.append(a[i]).append(i + 1 == N ? '\\n' : ' ');\n                continue;\n            }\n            Arrays.sort(a);\n            int maxFreq = 1, cur = 1;\n            for (int i = 1; i < N; i++) {\n                if (a[i] == a[i - 1]) cur++; else { maxFreq = Math.max(maxFreq, cur); cur = 1; }\n            }\n            maxFreq = Math.max(maxFreq, cur);\n            if (maxFreq > N / 2) {\n                out.append(-1).append('\\n');\n                continue;\n            }\n            int k = N / 2;\n            List<Long> res = new ArrayList<>(N);\n            for (int i = 0; i < k; i++) {\n                res.add(a[k + i]);\n                res.add(a[i]);\n            }\n            if (N % 2 == 1) res.add(a[N - 1]);\n            for (int i = 0; i < N; i++) out.append(res.get(i)).append(i + 1 == N ? '\\n' : ' ');\n        }\n        System.out.print(out.toString());\n    }\n}",
      "Python3": "#!/usr/bin/env python3\nimport sys\n\ndef solve():\n    data = sys.stdin.read().strip().split()\n    if not data:\n        return\n    it = iter(data)\n    T = int(next(it))\n    out_lines = []\n    for _ in range(T):\n        N = int(next(it))\n        a = [int(next(it)) for _ in range(N)]\n        if N <= 2:\n            out_lines.append(' '.join(map(str, a)))\n            continue\n        a.sort()\n        max_freq = 1\n        cur = 1\n        for i in range(1, N):\n            if a[i] == a[i-1]:\n                cur += 1\n            else:\n                max_freq = max(max_freq, cur)\n                cur = 1\n        max_freq = max(max_freq, cur)\n        if max_freq > N // 2:\n            out_lines.append('-1')\n            continue\n        k = N // 2\n        res = []\n        for i in range(k):\n            res.append(a[k + i])\n            res.append(a[i])\n        if N % 2:\n            res.append(a[-1])\n        out_lines.append(' '.join(map(str, res)))\n    sys.stdout.write('\\n'.join(out_lines))\n\nif __name__ == \"__main__\":\n    solve()\n",
      "JavaScript": "const fs = require('fs');\nconst input = fs.readFileSync(0, 'utf-8').trim().split(/\\s+/);\nlet idx = 0;\nconst T = Number(input[idx++]);\nlet out = [];\nfor (let t = 0; t < T; t++) {\n  const N = Number(input[idx++]);\n  let a = new Array(N);\n  for (let i = 0; i < N; i++) a[i] = Number(input[idx++]);\n  if (N <= 2) {\n    out.push(a.join(' '));\n    continue;\n  }\n  a.sort((x, y) => x - y);\n  let maxFreq = 1, cur = 1;\n  for (let i = 1; i < N; i++) {\n    if (a[i] === a[i - 1]) cur++; else { maxFreq = Math.max(maxFreq, cur); cur = 1; }\n  }\n  maxFreq = Math.max(maxFreq, cur);\n  if (maxFreq > Math.floor(N / 2)) {\n    out.push('-1');\n    continue;\n  }\n  const k = Math.floor(N / 2);\n  let res = [];\n  for (let i = 0; i < k; i++) {\n    res.push(a[k + i]);\n    res.push(a[i]);\n  }\n  if (N % 2 === 1) res.push(a[N - 1]);\n  out.push(res.join(' '));\n}\nconsole.log(out.join('\\n'));\n"
    }
  },
  "QBCategory": "CCD",
  "boilerPlates": {
    "C": "#include <stdio.h>\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "C++": "#include <iostream>\nusing namespace std;\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "Java": "import java.util.*;\nimport java.io.*;\n\npublic class Main {\n    public static void main(String[] args) throws IOException {\n        // your code goes here\n    }\n}",
    "Python3": "# your code goes here\n",
    "JavaScript": "// your code goes here\n"
  },
  "_internal": {
    "methodName": "",
    "params": []
  }
}