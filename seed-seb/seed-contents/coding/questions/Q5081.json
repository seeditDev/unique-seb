{
  "questionId": "Q5081",
  "title": "Delete a Node in BST",
  "slug": "delete-a-node-in-bst",
  "version": 1,
  "createdAt": "2026-07-01",
  "updatedAt": "2026-07-04",
  "metadata": {
    "category": "Data Structures",
    "difficulty": "Medium",
    "isPremium": true,
    "tags": [
      "Binary Search Tree",
      "Deletion",
      "Tree",
      "Recursion"
    ],
    "companyTags": [],
    "topicTags": [
      "Trees"
    ],
    "QBCategory": "CCD"
  },
  "content": {
    "problemStatement": "Given a Binary Search Tree (BST) and a target node XXX, delete the node from the BST. The goal is to return the reference to the root node, which may be modified after the deletion process.\nThe solution can be achieved into two main steps:\n\n\nSearch for the given node.\nIf the node is found, delete it and return the updated reference to the root node of the modified BST.\nVideo Explanation\nFor eg., in the following BST, node 2 is deleted:\n\n\n\n\nInput Format\n\nThe first line of the input contains two space separated integers NNN and XXX — the number of nodes in the binary search tree, and the node to be deleted.\nNext N−1N - 1N−1 lines contain three space separated characters pi,ci,Rip_i, c_i, R_ipi​,ci​,Ri​, describing the tree with edge informations - pip_ipi​ is an integer denoting the parent node of the iiith edge, cic_ici​ is child node, and the letter RiR_iRi​ denotes whether the child is left child or right child, it's value is `L` if cic_ici​ is left child of pip_ipi​, else `R`.\n\n\nOutput Format\nComplete the given function and return the root of modified BST after node XXX's deletion.",
    "constraints": [
      "1 <= T <= 10",
      "1 <= N <= 15 (the judge uses a small limit, but the algorithm works for up to 10^5)",
      "1 <= node values, X <= 100000",
      "All node values are distinct",
      "R is either 'L' or 'R'"
    ],
    "inputFormat": "The first line contains an integer T, the number of test cases. For each test case:\n- The first line contains two integers N and X, where N is the number of nodes in the BST and X is the value to be deleted.\n- The next N‑1 lines each contain two integers p and c and a character R separated by spaces. p is the parent node value, c is the child node value, and R is either 'L' (c is the left child of p) or 'R' (c is the right child of p). The given edges always form a valid binary search tree. If N = 1 there are no edge lines; the single node’s value is X (the only possible value).",
    "outputFormat": "For each test case output a single line:\n- \"Successfully deleted the node!\" if a node with value X existed and was removed.\n- \"Node not found\" if the tree does not contain a node with value X.",
    "sampleTestCases": [
      {
        "id": "sample_1",
        "input": "10 8\n3 1 L\n8 3 L\n8 11 R\n11 9 L\n13 8 L\n13 19 R\n18 16 L\n19 18 L\n19 20 R",
        "output": "Successfully deleted the node!",
        "explanation": ""
      }
    ]
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "label": "Case 1",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_2",
        "label": "Case 2",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_3",
        "label": "Case 3",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_4",
        "label": "Case 4",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_5",
        "label": "Case 5",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      }
    ],
    "totalTestCases": 5,
    "totalWeight": 10
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      2,
      2,
      2,
      2,
      2
    ],
    "maxScore": 10
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "For each test case we first reconstruct the BST from the given edges. While reading the edges we store every created node in a hash‑map (or an array indexed by value) and remember which values appear as children. After all edges are processed the node that never appeared as a child is the root.\n\nTo answer the query we first check whether X exists in the tree using the standard BST search (O(height)). If it does not exist we output \"Node not found\".\n\nIf the node exists we delete it using the classic BST deletion algorithm:\n1. Recurse down the tree until the node with value X is found.\n2. If the node is a leaf, simply free it and return NULL.\n3. If it has only one child, replace the node with that child.\n4. If it has two children, find its in‑order successor (the smallest node in its right subtree), copy the successor’s value into the current node, and recursively delete the successor.\nThe recursion returns the possibly new subtree root, which is attached back to the parent. The overall complexity per test case is O(N) in the worst case (when the tree is completely skewed) and O(log N) on average for balanced trees. The extra space is O(H) for the recursion stack, where H is the tree height.\n\nFinally we print the required message.",
    "timeComplexity": "O(N) per test case (search + deletion).",
    "spaceComplexity": "O(H) recursion stack, H <= N.",
    "code": {
      "C": "// COMPLETE, COMPILABLE C code\n#include <stdio.h>\n#include <stdlib.h>\n\ntypedef struct Node {\n    int val;\n    struct Node* left;\n    struct Node* right;\n} Node;\n\nNode* newNode(int v) {\n    Node* n = (Node*)malloc(sizeof(Node));\n    n->val = v; n->left = n->right = NULL; return n;\n}\n\nNode* deleteNode(Node* root, int x) {\n    if (!root) return NULL;\n    if (x < root->val) root->left = deleteNode(root->left, x);\n    else if (x > root->val) root->right = deleteNode(root->right, x);\n    else {\n        if (!root->left && !root->right) { free(root); return NULL; }\n        if (!root->left) { Node* r = root->right; free(root); return r; }\n        if (!root->right) { Node* l = root->left; free(root); return l; }\n        // two children: find inorder successor\n        Node* succ = root->right;\n        while (succ->left) succ = succ->left;\n        root->val = succ->val;\n        root->right = deleteNode(root->right, succ->val);\n    }\n    return root;\n}\n\nint exists(Node* root, int x) {\n    while (root) {\n        if (x == root->val) return 1;\n        root = (x < root->val) ? root->left : root->right;\n    }\n    return 0;\n}\n\nint main() {\n    int T; if (scanf(\"%d\", &T) != 1) return 0;\n    while (T--) {\n        int N, X; scanf(\"%d %d\", &N, &X);\n        Node* nodes[100005] = {0};\n        int isChild[100005] = {0};\n        for (int i = 0; i < N-1; ++i) {\n            int p, c; char dir; scanf(\"%d %d %c\", &p, &c, &dir);\n            if (!nodes[p]) nodes[p] = newNode(p);\n            if (!nodes[c]) nodes[c] = newNode(c);\n            if (dir == 'L') nodes[p]->left = nodes[c]; else nodes[p]->right = nodes[c];\n            isChild[c] = 1;\n        }\n        Node* root = NULL;\n        if (N == 1) {\n            root = newNode(X);\n        } else {\n            for (int i = 0; i < 100005; ++i) if (nodes[i] && !isChild[i]) { root = nodes[i]; break; }\n        }\n        if (exists(root, X)) {\n            root = deleteNode(root, X);\n            printf(\"Successfully deleted the node!\");\n        } else {\n            printf(\"Node not found\");\n        }\n        if (T) printf(\"\\n\");\n    }\n    return 0;\n}\n",
      "C++": "// COMPLETE, COMPILABLE C++ code\n#include <bits/stdc++.h>\nusing namespace std;\nstruct Node {\n    int val;\n    Node* left;\n    Node* right;\n    Node(int v): val(v), left(nullptr), right(nullptr) {}\n};\n\nNode* deleteNode(Node* root, int x) {\n    if (!root) return nullptr;\n    if (x < root->val) root->left = deleteNode(root->left, x);\n    else if (x > root->val) root->right = deleteNode(root->right, x);\n    else {\n        if (!root->left && !root->right) { delete root; return nullptr; }\n        if (!root->left) { Node* r = root->right; delete root; return r; }\n        if (!root->right) { Node* l = root->left; delete root; return l; }\n        // two children: inorder successor\n        Node* succ = root->right;\n        while (succ->left) succ = succ->left;\n        root->val = succ->val;\n        root->right = deleteNode(root->right, succ->val);\n    }\n    return root;\n}\n\nbool exists(Node* root, int x) {\n    while (root) {\n        if (x == root->val) return true;\n        root = (x < root->val) ? root->left : root->right;\n    }\n    return false;\n}\n\nint main() {\n    ios::sync_with_stdio(false);\n    cin.tie(nullptr);\n    int T; if (!(cin >> T)) return 0;\n    while (T--) {\n        int N, X; cin >> N >> X;\n        unordered_map<int, Node*> mp;\n        unordered_set<int> childSet;\n        for (int i = 0; i < N-1; ++i) {\n            int p, c; char dir; cin >> p >> c >> dir;\n            if (!mp.count(p)) mp[p] = new Node(p);\n            if (!mp.count(c)) mp[c] = new Node(c);\n            if (dir == 'L') mp[p]->left = mp[c]; else mp[p]->right = mp[c];\n            childSet.insert(c);\n        }\n        Node* root = nullptr;\n        if (N == 1) {\n            root = new Node(X);\n        } else {\n            for (auto &kv : mp) if (!childSet.count(kv.first)) { root = kv.second; break; }\n        }\n        if (exists(root, X)) {\n            root = deleteNode(root, X);\n            cout << \"Successfully deleted the node!\";\n        } else {\n            cout << \"Node not found\";\n        }\n        if (T) cout << '\\n';\n    }\n    return 0;\n}\n",
      "Java": ""
    }
  },
  "QBCategory": "CCD",
  "boilerPlates": {
    "C": "#include <stdio.h>\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "C++": "#include <iostream>\nusing namespace std;\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "Java": "import java.util.*;\nimport java.io.*;\n\npublic class Main {\n    public static void main(String[] args) throws IOException {\n        // your code goes here\n    }\n}",
    "Python3": "# your code goes here\n",
    "JavaScript": "// your code goes here\n"
  },
  "_internal": {
    "methodName": "",
    "params": []
  }
}