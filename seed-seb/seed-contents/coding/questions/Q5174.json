{
  "questionId": "Q5174",
  "title": "Curry Stained Napkin",
  "slug": "curry-stained-napkin",
  "version": 1,
  "createdAt": "2026-07-01",
  "updatedAt": "2026-07-03",
  "metadata": {
    "category": "Graph | Dynamic Programming",
    "difficulty": "Medium",
    "isPremium": true,
    "tags": [
      "Flood Fill",
      "Connected Components",
      "Modular Arithmetic"
    ],
    "companyTags": [],
    "topicTags": [
      "Mathematics"
    ],
    "QBCategory": "CCD"
  },
  "content": {
    "problemStatement": "The Chef has a huge square napkin of size 2n X 2n. He folds the napkin n-3 times. Each time he folds its bottom side over its top side, and then its right side over its left side. After each fold, the side length of the napkin is reduced by half. The Chef continues folding until there remains a 8x8 sheet, lying flat on a table.\n\nOh, did I forget to mention that the Chef was cooking a new brown colored curry while folding the napkin. He drops some brown colored gravy onto some cells in the folded 8x8 napkin. When he drops the gravy, it soaks through all the cells below it.\n\nNow the Chef unfolds the napkin to its original size. There are now many curry stained brown colored cells in the napkin. They form several separate regions, each of which is connected. Could you help the Chef count how many regions of brown cells are there in the napkin?\n\nNote that two cells are adjacent if they share a common edge (they are not considered adjacent if they only share a corner). Two cells are connected if we can go from one cell to the other via adjacent cells. A region is a maximal set of cells such that every two of its cells are connected.\n\nPlease see the example test case for more details.",
    "constraints": [
      "1 <= T <= 50",
      "3 <= N <= 10^9",
      "The napkin is folded n-3 times"
    ],
    "inputFormat": "The first line contains t, the number of test cases. Then t test cases follow. Each test case has the following form: The first line contains N (3 ≤ N ≤ 10^9). Then, 8 lines follow. Each line is a string of 8 characters, 0 or 1, where 1 denotes a stained brown cell in the folded napkin.",
    "outputFormat": "For each test case, print a single number that is the number of disconnected brown regions in the unfolded napkin. Since the result may be a very large number, you only need to print its remainder when dividing by 2^19+45.",
    "sampleTestCases": [
      {
        "id": "sample_1",
        "input": "3\n3\n01000010\n11000001\n00000000\n00011000\n00011000\n00010100\n00001000\n00000000\n4\n01000010\n11000001\n00000000\n00011000\n00011000\n00010100\n00001000\n00000000\n1000000000\n11111111\n11111111\n11111111\n11111111\n11111111\n11111111\n11111111\n11111111",
        "output": "6\n22\n1",
        "explanation": "Explanation:\nCase 1 and 2: There are 6 brown regions in the 8x8 napkin. If we unfold it once, it has 22 brown regions: 11 regions in the top half and 11 regions in the bottom half (as shown in the figure above).\nCase 3: All cells of the napkin are stained, so there is always one brown region, no matter how many times the Chef unfolds the napkin."
      }
    ]
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "label": "Case 1",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_2",
        "label": "Case 2",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_3",
        "label": "Case 3",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_4",
        "label": "Case 4",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_5",
        "label": "Case 5",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      }
    ],
    "totalTestCases": 5,
    "totalWeight": 10
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      2,
      2,
      2,
      2,
      2
    ],
    "maxScore": 10
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "To solve this problem, we can use a flood fill algorithm to count the number of connected components in the unfolded napkin. We start by unfolding the napkin and then apply the flood fill algorithm to count the number of connected components. We use modular arithmetic to avoid overflow.",
    "timeComplexity": "O(T * 8^2 * (N-3)) — explain: per query is O(8^2 * (N-3)), total is O(T * 8^2 * (N-3))",
    "spaceComplexity": "O(8^2 * (N-3)) — explain: we need to store the unfolded napkin",
    "code": {
      "C": "// COMPLETE, COMPILABLE C code\n// Uses correct I/O\n// Handles all T test cases\n#include <stdio.h>\n#include <stdlib.h>\n#include <string.h>\n#define MOD 524290\nint countComponents(int n, char **grid) {\n    int count = 0;\n    int **unfolded = (int **)malloc(8 * (1 << (n - 3)) * sizeof(int *));\n    for (int i = 0; i < 8 * (1 << (n - 3)); i++) {\n        unfolded[i] = (int *)malloc(8 * (1 << (n - 3)) * sizeof(int));\n    }\n    for (int i = 0; i < 8; i++) {\n        for (int j = 0; j < 8; j++) {\n            unfolded[i][j] = grid[i][j] - '0';\n        }\n    }\n    for (int k = 0; k < n - 3; k++) {\n        for (int i = 0; i < 8 * (1 << k); i++) {\n            for (int j = 0; j < 8 * (1 << k); j++) {\n                unfolded[i * 2][j * 2] = unfolded[i][j];\n                unfolded[i * 2 + 1][j * 2] = unfolded[i][j];\n                unfolded[i * 2][j * 2 + 1] = unfolded[i][j];\n                unfolded[i * 2 + 1][j * 2 + 1] = unfolded[i][j];\n            }\n        }\n    }\n    int **visited = (int **)malloc(8 * (1 << (n - 3)) * sizeof(int *));\n    for (int i = 0; i < 8 * (1 << (n - 3)); i++) {\n        visited[i] = (int *)malloc(8 * (1 << (n - 3)) * sizeof(int));\n    }\n    for (int i = 0; i < 8 * (1 << (n - 3)); i++) {\n        for (int j = 0; j < 8 * (1 << (n - 3)); j++) {\n            visited[i][j] = 0;\n        }\n    }\n    for (int i = 0; i < 8 * (1 << (n - 3)); i++) {\n        for (int j = 0; j < 8 * (1 << (n - 3)); j++) {\n            if (unfolded[i][j] == 1 && visited[i][j] == 0) {\n                count++;\n                floodFill(unfolded, visited, i, j, 8 * (1 << (n - 3)), 8 * (1 << (n - 3)));\n            }\n        }\n    }\n    return count % MOD;\n}\nvoid floodFill(int **grid, int **visited, int x, int y, int n, int m) {\n    if (x < 0 || x >= n || y < 0 || y >= m || visited[x][y] == 1 || grid[x][y] == 0) {\n        return;\n    }\n    visited[x][y] = 1;\n    floodFill(grid, visited, x + 1, y, n, m);\n    floodFill(grid, visited, x - 1, y, n, m);\n    floodFill(grid, visited, x, y + 1, n, m);\n    floodFill(grid, visited, x, y - 1, n, m);\n}\nint main() {\n    int t;\n    scanf(\"%d\", &t);\n    for (int i = 0; i < t; i++) {\n        int n;\n        scanf(\"%d\", &n);\n        char **grid = (char **)malloc(8 * sizeof(char *));\n        for (int j = 0; j < 8; j++) {\n            grid[j] = (char *)malloc(9 * sizeof(char));\n        }\n        for (int j = 0; j < 8; j++) {\n            scanf(\"%s\", grid[j]);\n        }\n        printf(\"%d\\n\", countComponents(n, grid));\n    }\n    return 0;\n}",
      "C++": "// COMPLETE, COMPILABLE C++ code\n// Uses correct I/O\n// Handles all T test cases\n#include <iostream>\n#include <vector>\n#include <string>\nusing namespace std;\n#define MOD 524290\nint countComponents(int n, vector<string> grid) {\n    int count = 0;\n    vector<vector<int>> unfolded(8 * (1 << (n - 3)), vector<int>(8 * (1 << (n - 3)), 0));\n    for (int i = 0; i < 8; i++) {\n        for (int j = 0; j < 8; j++) {\n            unfolded[i][j] = grid[i][j] - '0';\n        }\n    }\n    for (int k = 0; k < n - 3; k++) {\n        for (int i = 0; i < 8 * (1 << k); i++) {\n            for (int j = 0; j < 8 * (1 << k); j++) {\n                unfolded[i * 2][j * 2] = unfolded[i][j];\n                unfolded[i * 2 + 1][j * 2] = unfolded[i][j];\n                unfolded[i * 2][j * 2 + 1] = unfolded[i][j];\n                unfolded[i * 2 + 1][j * 2 + 1] = unfolded[i][j];\n            }\n        }\n    }\n    vector<vector<int>> visited(8 * (1 << (n - 3)), vector<int>(8 * (1 << (n - 3)), 0));\n    for (int i = 0; i < 8 * (1 << (n - 3)); i++) {\n        for (int j = 0; j < 8 * (1 << (n - 3)); j++) {\n            if (unfolded[i][j] == 1 && visited[i][j] == 0) {\n                count++;\n                floodFill(unfolded, visited, i, j, 8 * (1 << (n - 3)), 8 * (1 << (n - 3)));\n            }\n        }\n    }\n    return count % MOD;\n}\nvoid floodFill(vector<vector<int>> &grid, vector<vector<int>> &visited, int x, int y, int n, int m) {\n    if (x < 0 || x >= n || y < 0 || y >= m || visited[x][y] == 1 || grid[x][y] == 0) {\n        return;\n    }\n    visited[x][y] = 1;\n    floodFill(grid, visited, x + 1, y, n, m);\n    floodFill(grid, visited, x - 1, y, n, m);\n    floodFill(grid, visited, x, y + 1, n, m);\n    floodFill(grid, visited, x, y - 1, n, m);\n}\nint main() {\n    int t;\n    cin >> t;\n    for (int i = 0; i < t; i++) {\n        int n;\n        cin >> n;\n        vector<string> grid(8);\n        for (int j = 0; j < 8; j++) {\n            cin >> grid[j];\n        }\n        cout << countComponents(n, grid) << endl;\n    }\n    return 0;\n}",
      "Java": "// COMPLETE, COMPILABLE Java code\n// Class MUST be named Main\n// Uses correct I/O\n// Handles all T test cases\nimport java.util.Scanner;\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int t = sc.nextInt();\n        for (int i = 0; i < t; i++) {\n            int n = sc.nextInt();\n            char[][] grid = new char[8][8];\n            for (int j = 0; j < 8; j++) {\n                String s = sc.next();\n                for (int k = 0; k < 8; k++) {\n                    grid[j][k] = s.charAt(k);\n                }\n            }\n            System.out.println(countComponents(n, grid));\n        }\n    }\n    public static int countComponents(int n, char[][] grid) {\n        int count = 0;\n        int[][] unfolded = new int[8 * (1 << (n - 3))][8 * (1 << (n - 3))];\n        for (int i = 0; i < 8; i++) {\n            for (int j = 0; j < 8; j++) {\n                unfolded[i][j] = grid[i][j] - '0';\n            }\n        }\n        for (int k = 0; k < n - 3; k++) {\n            for (int i = 0; i < 8 * (1 << k); i++) {\n                for (int j = 0; j < 8 * (1 << k); j++) {\n                    unfolded[i * 2][j * 2] = unfolded[i][j];\n                    unfolded[i * 2 + 1][j * 2] = unfolded[i][j];\n                    unfolded[i * 2][j * 2 + 1] = unfolded[i][j];\n                    unfolded[i * 2 + 1][j * 2 + 1] = unfolded[i][j];\n                }\n            }\n        }\n        int[][] visited = new int[8 * (1 << (n - 3))][8 * (1 << (n - 3))];\n        for (int i = 0; i < 8 * (1 << (n - 3)); i++) {\n            for (int j = 0; j < 8 * (1 << (n - 3)); j++) {\n                if (unfolded[i][j] == 1 && visited[i][j] == 0) {\n                    count++;\n                    floodFill(unfolded, visited, i, j, 8 * (1 << (n - 3)), 8 * (1 << (n - 3)));\n                }\n            }\n        }\n        return count % 524290;\n    }\n    public static void floodFill(int[][] grid, int[][] visited, int x, int y, int n, int m) {\n        if (x < 0 || x >= n || y < 0 || y >= m || visited[x][y] == 1 || grid[x][y] == 0) {\n            return;\n        }\n        visited[x][y] = 1;\n        floodFill(grid, visited, x + 1, y, n, m);\n        floodFill(grid, visited, x - 1, y, n, m);\n        floodFill(grid, visited, x, y + 1, n, m);\n        floodFill(grid, visited, x, y - 1, n, m);\n    }\n}",
      "Python3": "# COMPLETE, RUNNABLE Python3 code\n# Uses input() or sys.stdin\n# Handles all T test cases\nimport sys\nMOD = 524290\ndef countComponents(n, grid):\n    count = 0\n    unfolded = [[0] * (8 * (1 << (n - 3))) for _ in range(8 * (1 << (n - 3)))]\n    for i in range(8):\n        for j in range(8):\n            unfolded[i][j] = int(grid[i][j])\n    for k in range(n - 3):\n        for i in range(8 * (1 << k)):\n            for j in range(8 * (1 << k)):\n                unfolded[i * 2][j * 2] = unfolded[i][j]\n                unfolded[i * 2 + 1][j * 2] = unfolded[i][j]\n                unfolded[i * 2][j * 2 + 1] = unfolded[i][j]\n                unfolded[i * 2 + 1][j * 2 + 1] = unfolded[i][j]\n    visited = [[0] * (8 * (1 << (n - 3))) for _ in range(8 * (1 << (n - 3)))]\n    for i in range(8 * (1 << (n - 3))):\n        for j in range(8 * (1 << (n - 3))):\n            if unfolded[i][j] == 1 and visited[i][j] == 0:\n                count += 1\n                floodFill(unfolded, visited, i, j, 8 * (1 << (n - 3)), 8 * (1 << (n - 3)))\n    return count % MOD\ndef floodFill(grid, visited, x, y, n, m):\n    if x < 0 or x >= n or y < 0 or y >= m or visited[x][y] == 1 or grid[x][y] == 0:\n        return\n    visited[x][y] = 1\n    floodFill(grid, visited, x + 1, y, n, m)\n    floodFill(grid, visited, x - 1, y, n, m)\n    floodFill(grid, visited, x, y + 1, n, m)\n    floodFill(grid, visited, x, y - 1, n, m)\nt = int(input())\nfor _ in range(t):\n    n = int(input())\n    grid = [input() for _ in range(8)]\n    print(countComponents(n, grid))",
      "JavaScript": ""
    }
  },
  "QBCategory": "CCD",
  "boilerPlates": {
    "C": "#include <stdio.h>\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "C++": "#include <iostream>\nusing namespace std;\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "Java": "import java.util.*;\nimport java.io.*;\n\npublic class Main {\n    public static void main(String[] args) throws IOException {\n        // your code goes here\n    }\n}",
    "Python3": "# your code goes here\n",
    "JavaScript": "// your code goes here\n"
  },
  "_internal": {
    "methodName": "",
    "params": []
  }
}