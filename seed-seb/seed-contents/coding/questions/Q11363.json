{
  "questionId": "Q11363",
  "title": "Maximum sum of non adjacent elements",
  "slug": "house-robber",
  "version": 1,
  "createdAt": "2026-07-05",
  "updatedAt": "2026-07-05",
  "metadata": {
    "category": "Dynamic Programming",
    "difficulty": "Medium",
    "isPremium": true,
    "tags": [
      "Memoization",
      "Recursion"
    ],
    "companyTags": [],
    "topicTags": [],
    "QBCategory": "CCD"
  },
  "content": {
    "problemStatement": "Given an array of N positive integers, we need to return the maximum sum of the subsequence such that no two elements of the subsequence are adjacent elements in the array.\n\n\nNote: A subsequence of an array is a list with elements of the array where some elements are deleted (or not deleted at all) and the elements should be in the same order in the subsequence as in the array.",
    "constraints": [
      "1 <= N <= 10^5",
      "1 <= arr[i] <= 10^9"
    ],
    "inputFormat": "An array of N positive integers",
    "outputFormat": "The maximum sum of the subsequence such that no two elements of the subsequence are adjacent elements in the array",
    "sampleTestCases": []
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "label": "min boundary",
        "input": "[1]",
        "expectedOutput": "1",
        "weight": 2
      },
      {
        "id": "tc_2",
        "label": "max boundary",
        "input": "[1, 2, 3, 4, 5, 6, 7, 8, 9]",
        "expectedOutput": "25",
        "weight": 2
      },
      {
        "id": "tc_3",
        "label": "uniform case",
        "input": "[5, 5, 5, 5, 5]",
        "expectedOutput": "15",
        "weight": 2
      },
      {
        "id": "tc_4",
        "label": "tricky case",
        "input": "[1, 100, 1, 100, 1]",
        "expectedOutput": "202",
        "weight": 2
      },
      {
        "id": "tc_5",
        "label": "mixed case",
        "input": "[2, 1, 4, 9, 7, 2, 9]",
        "expectedOutput": "24",
        "weight": 2
      }
    ],
    "totalTestCases": 5,
    "totalWeight": 10
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      2,
      2,
      2,
      2,
      2
    ],
    "maxScore": 10
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "Use dynamic programming with memoization to store the maximum sum of subsequence for each index",
    "timeComplexity": "O(N)",
    "spaceComplexity": "O(N)",
    "code": {
      "C": "#include <stdio.h>\nint maxNonAdjacentSum(int arr[], int n) {\n    if (n == 0) return 0;\n    if (n == 1) return arr[0];\n    int dp[n];\n    dp[0] = arr[0];\n    dp[1] = (arr[0] > arr[1]) ? arr[0] : arr[1];\n    for (int i = 2; i < n; i++) {\n        dp[i] = (dp[i-1] > (dp[i-2] + arr[i])) ? dp[i-1] : (dp[i-2] + arr[i]);\n    }\n    return dp[n-1];\n}\nint main() {\n    int arr[] = {2, 1, 4, 9};\n    int n = sizeof(arr)/sizeof(arr[0]);\n    printf(\"%d\", maxNonAdjacentSum(arr, n));\n    return 0;\n}",
      "C++": "#include <bits/stdc++.h>\nusing namespace std;\nclass Solution {\npublic:\n    // Recursive function with memoization\n    int solve(vector<int>& arr, int i, vector<int>& dp) {\n        // If index is negative, no element to pick\n        if (i < 0) return 0;\n\n        // If at first element, return its value\n        if (i == 0) return arr[0];\n\n        // Return already computed value\n        if (dp[i] != -1) return dp[i];\n\n        // Include current and move 2 back\n        int pick = arr[i] + solve(arr, i - 2, dp);\n\n        // Exclude current and move 1 back\n        int notPick = solve(arr, i - 1, dp);\n\n        // Store and return max of both choices\n        return dp[i] = max(pick, notPick);\n    }\n\n    // Main function to be called externally\n    int maximumNonAdjacentSum(vector<int>& arr) {\n        int n = arr.size();\n\n        // DP array initialized with -1\n        vector<int> dp(n, -1);\n\n        // Start solving from last index\n        return solve(arr, n - 1, dp);\n    }\n};\n\nint main() {\n    vector<int> arr = {2, 1, 4, 9};\n    Solution obj;\n\n    // Output the result\n    cout << obj.maximumNonAdjacentSum(arr);\n    return 0;\n}",
      "Java": "class Solution {\n    // Recursive helper function with memoization\n    public int solve(int[] arr, int i, int[] dp) {\n        // No element to pick\n        if (i < 0) return 0;\n\n        // Only one element\n        if (i == 0) return arr[0];\n\n        // Return memoized result\n        if (dp[i] != -1) return dp[i];\n\n        // Pick current and move 2 back\n        int pick = arr[i] + solve(arr, i - 2, dp);\n\n        // Don't pick current, move 1 back\n        int notPick = solve(arr, i - 1, dp);\n\n        // Memoize and return result\n        return dp[i] = Math.max(pick, notPick);\n    }\n\n    // Main function to compute result\n    public int maximumNonAdjacentSum(int[] arr) {\n        int n = arr.length;\n\n        // Initialize DP array\n        int[] dp = new int[n];\n        Arrays.fill(dp, -1);\n\n        // Start recursion\n        return solve(arr, n - 1, dp);\n    }\n}\n\npublic class Main {\n    public static void main(String[] args) {\n        int[] arr = {2, 1, 4, 9};\n        Solution obj = new Solution();\n\n        // Print result\n        System.out.println(obj.maximumNonAdjacentSum(arr));\n    }\n}",
      "Python3": "class Solution:\n    # Recursive helper with memoization\n    def solve(self, arr, i, dp):\n        # No elements to pick\n        if i < 0:\n            return 0\n\n        # One element case\n        if i == 0:\n            return arr[0]\n\n        # Return memoized result\n        if dp[i] != -1:\n            return dp[i]\n\n        # Pick current and go 2 steps back\n        pick = arr[i] + self.solve(arr, i - 2, dp)\n\n        # Skip current and go 1 step back\n        notPick = self.solve(arr, i - 1, dp)\n\n        # Store and return max\n        dp[i] = max(pick, notPick)\n        return dp[i]\n\n    # Main function to call\n    def maximumNonAdjacentSum(self, arr):\n        n = len(arr)\n\n        # Initialize DP array\n        dp = [-1] * n\n\n        # Start recursion\n        return self.solve(arr, n - 1, dp)\n\n# Driver code\narr = [2, 1, 4, 9]\nobj = Solution()\nprint(obj.maximumNonAdjacentSum(arr))",
      "JavaScript": "class Solution {\n    // Recursive helper with memoization\n    solve(arr, i, dp) {\n        // No element to pick\n        if (i < 0) return 0;\n\n        // Only one element\n        if (i === 0) return arr[0];\n\n        // Return memoized value\n        if (dp[i] !== -1) return dp[i];\n\n        // Pick current and go 2 back\n        let pick = arr[i] + this.solve(arr, i - 2, dp);\n\n        // Skip current and go 1 back\n        let notPick = this.solve(arr, i - 1, dp);\n\n        // Memoize and return\n        dp[i] = Math.max(pick, notPick);\n        return dp[i];\n    }\n\n    // Main function to start computation\n    maximumNonAdjacentSum(arr) {\n        let n = arr.length;\n\n        // Initialize DP array\n        let dp = new Array(n).fill(-1);\n\n        // Start solving\n        return this.solve(arr, n - 1, dp);\n    }\n}\n\n// Driver code\nlet arr = [2, 1, 4, 9];\nlet obj = new Solution();\nconsole.log(obj.maximumNonAdjacentSum(arr));"
    }
  },
  "QBCategory": "CCD",
  "boilerPlates": {
    "C": "#include <stdio.h>\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "C++": "#include <iostream>\nusing namespace std;\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "Java": "import java.util.*;\nimport java.io.*;\n\npublic class Main {\n    public static void main(String[] args) throws IOException {\n        // your code goes here\n    }\n}",
    "Python3": "# your code goes here\n",
    "JavaScript": "// your code goes here\n"
  },
  "_internal": {
    "methodName": "",
    "params": []
  }
}