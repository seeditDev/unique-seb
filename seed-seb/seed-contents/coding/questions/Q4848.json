{
  "questionId": "Q4848",
  "title": "Search in BST",
  "slug": "search-in-bst",
  "version": 1,
  "createdAt": "2026-07-01",
  "updatedAt": "2026-07-04",
  "metadata": {
    "category": "Searching",
    "difficulty": "Easy",
    "isPremium": true,
    "tags": [
      "Binary Search Tree",
      "Tree Traversal"
    ],
    "companyTags": [],
    "topicTags": [
      "Trees"
    ],
    "QBCategory": "CCD"
  },
  "content": {
    "problemStatement": "Given a binary search tree and an integer **`X`**, search if **`X`** exists in the BST or not.\n\n(**Note:** A binary search tree is a rooted binary tree data structure with the key of each internal node being greater than all the keys in the respective node's left subtree and less than the ones in its right subtree.)\n\nFor eg., the given tree is a binary search tree and we can easily search whether node 666 exists in it or not.\n\n\n\n\nInput Format\n\nThe first line of the input contains two space separated integer NNN and XXX — the number of nodes in the binary search tree and the node which we need to search.\nNext N−1N - 1N−1 lines contain three space separated characters pi,ci,Rip_i, c_i, R_ipi​,ci​,Ri​, describing the tree with edge informations - pip_ipi​ is an integer denoting the parent node of the iiith edge, cic_ici​ is child node, and the letter RiR_iRi​ denotes whether the child is left child or right child, it's value is `L` if cic_ici​ is left child of pip_ipi​, else `R`.\n\n\nOutput Format\nOutput on a single line `YES` if XXX is present in the given BST, otherwise `NO`.",
    "constraints": [
      "1 <= N <= 10000",
      "1 <= pi, ci <= 100000",
      "Ri = 'L' or 'R'",
      "All pi's and ci's are distinct.",
      "1 <= X <= 100000"
    ],
    "inputFormat": "First line contains two space-separated integers N and X, where N is the number of nodes in the binary search tree and X is the node to be searched. Next N-1 lines contain three space-separated characters pi, ci, and Ri, describing the tree with edge information - pi is an integer denoting the parent node of the ith edge, ci is the child node, and the letter Ri denotes whether the child is left child or right child, it's value is 'L' if ci is left child of pi, else 'R'.",
    "outputFormat": "Output 'YES' if X is present in the given BST, otherwise 'NO'.",
    "sampleTestCases": [
      {
        "id": "sample_1",
        "input": "7 6\\n4 2 L\\n4 6 R\\n2 1 L\\n2 3 R\\n6 5 L\\n6 7 R",
        "output": "YES",
        "explanation": "The given tree is a binary search tree and we can easily search whether node 6 exists in it or not."
      },
      {
        "id": "sample_2",
        "input": "5 4\\n3 1 L\\n3 5 R\\n1 0 L\\n1 2 R",
        "output": "NO",
        "explanation": "The given tree is a binary search tree and we can easily search whether node 4 exists in it or not."
      },
      {
        "id": "sample_3",
        "input": "3 5\\n2 1 L\\n2 3 R",
        "output": "NO",
        "explanation": "The given tree is a binary search tree and we can easily search whether node 5 exists in it or not."
      }
    ]
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "label": "minimum boundary",
        "input": "1 1\\n",
        "expectedOutput": "YES",
        "weight": 2
      },
      {
        "id": "tc_2",
        "label": "maximum boundary",
        "input": "10000 10000\\n",
        "expectedOutput": "YES",
        "weight": 2
      },
      {
        "id": "tc_3",
        "label": "uniform/edge case",
        "input": "5 5\\n5 5 L\\n5 5 R\\n5 5 L\\n5 5 R",
        "expectedOutput": "YES",
        "weight": 2
      },
      {
        "id": "tc_4",
        "label": "tricky case",
        "input": "5 4\\n3 1 L\\n3 5 R\\n1 0 L\\n1 2 R",
        "expectedOutput": "NO",
        "weight": 2
      },
      {
        "id": "tc_5",
        "label": "stress/general",
        "input": "10 10\\n10 1 L\\n10 2 R\\n1 3 L\\n1 4 R\\n2 5 L\\n2 6 R\\n3 7 L\\n3 8 R\\n4 9 L\\n4 10 R",
        "expectedOutput": "YES",
        "weight": 2
      }
    ],
    "totalTestCases": 5,
    "totalWeight": 10
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      2,
      2,
      2,
      2,
      2
    ],
    "maxScore": 10
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "The problem can be solved by constructing the binary search tree from the given edge information and then searching for the node X in the constructed tree. The tree construction can be done by iterating over the edge information and assigning the child nodes to their respective parent nodes. The search operation can be performed using a recursive or iterative approach.",
    "timeComplexity": "O(N) for constructing the tree and O(h) for searching, where h is the height of the tree. In the worst case, the tree can be skewed, making the time complexity O(N) for searching.",
    "spaceComplexity": "O(N) for storing the tree nodes.",
    "code": {
      "C": "#include <stdio.h>\n#include <stdlib.h>\n\n// Define the structure for a tree node\ntypedef struct Node {\n    int val;\n    struct Node* left;\n    struct Node* right;\n} Node;\n\n// Function to create a new node\nNode* createNode(int val) {\n    Node* newNode = (Node*) malloc(sizeof(Node));\n    if (!newNode) {\n        printf(\"Memory error\n\");\n        return NULL;\n    }\n    newNode->val = val;\n    newNode->left = newNode->right = NULL;\n    return newNode;\n}\n\n// Function to search for a value in the BST\nint searchInBST(Node* root, int x) {\n    if (root == NULL) {\n        return 0;\n    }\n    if (root->val == x) {\n        return 1;\n    }\n    if (root->val < x) {\n        return searchInBST(root->right, x);\n    } else {\n        return searchInBST(root->left, x);\n    }\n}\n\nint main() {\n    int N, X;\n    scanf(\"%d %d\", &N, &X);\n    Node* root = NULL;\n    for (int i = 0; i < N - 1; i++) {\n        int pi, ci;\n        char Ri;\n        scanf(\"%d %d %c\", &pi, &ci, &Ri);\n        // Create nodes and assign them to the tree\n        Node* parentNode = NULL;\n        Node* childNode = createNode(ci);\n        if (root == NULL) {\n            root = createNode(pi);\n            parentNode = root;\n        } else {\n            Node* temp = root;\n            while (temp != NULL) {\n                if (temp->val == pi) {\n                    parentNode = temp;\n                    break;\n                }\n                if (temp->val < pi) {\n                    if (temp->right == NULL) {\n                        temp->right = createNode(pi);\n                        parentNode = temp->right;\n                        break;\n                    }\n                    temp = temp->right;\n                } else {\n                    if (temp->left == NULL) {\n                        temp->left = createNode(pi);\n                        parentNode = temp->left;\n                        break;\n                    }\n                    temp = temp->left;\n                }\n            }\n        }\n        if (Ri == 'L') {\n            parentNode->left = childNode;\n        } else {\n            parentNode->right = childNode;\n        }\n    }\n    if (searchInBST(root, X)) {\n        printf(\"YES\\n\");\n    } else {\n        printf(\"NO\\n\");\n    }\n    return 0;\n}",
      "C++": "#include <iostream>\nusing namespace std;\n\n// Define the structure for a tree node\nstruct Node {\n    int val;\n    Node* left;\n    Node* right;\n};\n\n// Function to create a new node\nNode* createNode(int val) {\n    Node* newNode = new Node();\n    newNode->val = val;\n    newNode->left = newNode->right = NULL;\n    return newNode;\n}\n\n// Function to search for a value in the BST\nbool searchInBST(Node* root, int x) {\n    if (root == NULL) {\n        return false;\n    }\n    if (root->val == x) {\n        return true;\n    }\n    if (root->val < x) {\n        return searchInBST(root->right, x);\n    } else {\n        return searchInBST(root->left, x);\n    }\n}\n\nint main() {\n    int N, X;\n    cin >> N >> X;\n    Node* root = NULL;\n    for (int i = 0; i < N - 1; i++) {\n        int pi, ci;\n        char Ri;\n        cin >> pi >> ci >> Ri;\n        // Create nodes and assign them to the tree\n        Node* parentNode = NULL;\n        Node* childNode = createNode(ci);\n        if (root == NULL) {\n            root = createNode(pi);\n            parentNode = root;\n        } else {\n            Node* temp = root;\n            while (temp != NULL) {\n                if (temp->val == pi) {\n                    parentNode = temp;\n                    break;\n                }\n                if (temp->val < pi) {\n                    if (temp->right == NULL) {\n                        temp->right = createNode(pi);\n                        parentNode = temp->right;\n                        break;\n                    }\n                    temp = temp->right;\n                } else {\n                    if (temp->left == NULL) {\n                        temp->left = createNode(pi);\n                        parentNode = temp->left;\n                        break;\n                    }\n                    temp = temp->left;\n                }\n            }\n        }\n        if (Ri == 'L') {\n            parentNode->left = childNode;\n        } else {\n            parentNode->right = childNode;\n        }\n    }\n    if (searchInBST(root, X)) {\n        cout << \"YES\" << endl;\n    } else {\n        cout << \"NO\" << endl;\n    }\n    return 0;\n}",
      "Java": "import java.util.Scanner;\n\n// Define the class for a tree node\nclass Node {\n    int val;\n    Node left;\n    Node right;\n    Node(int val) {\n        this.val = val;\n        left = right = null;\n    }\n}\n\npublic class Main {\n    // Function to search for a value in the BST\n    public static boolean searchInBST(Node root, int x) {\n        if (root == null) {\n            return false;\n        }\n        if (root.val == x) {\n            return true;\n        }\n        if (root.val < x) {\n            return searchInBST(root.right, x);\n        } else {\n            return searchInBST(root.left, x);\n        }\n    }\n\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int N = sc.nextInt();\n        int X = sc.nextInt();\n        Node root = null;\n        for (int i = 0; i < N - 1; i++) {\n            int pi = sc.nextInt();\n            int ci = sc.nextInt();\n            char Ri = sc.next().charAt(0);\n            // Create nodes and assign them to the tree\n            Node parentNode = null;\n            Node childNode = new Node(ci);\n            if (root == null) {\n                root = new Node(pi);\n                parentNode = root;\n            } else {\n                Node temp = root;\n                while (temp != null) {\n                    if (temp.val == pi) {\n                        parentNode = temp;\n                        break;\n                    }\n                    if (temp.val < pi) {\n                        if (temp.right == null) {\n                            temp.right = new Node(pi);\n                            parentNode = temp.right;\n                            break;\n                        }\n                        temp = temp.right;\n                    } else {\n                        if (temp.left == null) {\n                            temp.left = new Node(pi);\n                            parentNode = temp.left;\n                            break;\n                        }\n                        temp = temp.left;\n                    }\n                }\n            }\n            if (Ri == 'L') {\n                parentNode.left = childNode;\n            } else {\n                parentNode.right = childNode;\n            }\n        }\n        if (searchInBST(root, X)) {\n            System.out.println(\"YES\");\n        } else {\n            System.out.println(\"NO\");\n        }\n    }\n}",
      "Python3": "class Node:\n    def __init__(self, val):\n        self.val = val\n        self.left = None\n        self.right = None\n\n# Function to search for a value in the BST\ndef search_in_bst(root, x):\n    if root is None:\n        return False\n    if root.val == x:\n        return True\n    if root.val < x:\n        return search_in_bst(root.right, x)\n    else:\n        return search_in_bst(root.left, x)\n\n# Read input\nN, X = map(int, input().split())\nroot = None\nfor i in range(N - 1):\n    pi, ci, Ri = input().split()\n    pi, ci = int(pi), int(ci)\n    # Create nodes and assign them to the tree\n    child_node = Node(ci)\n    if root is None:\n        root = Node(pi)\n        parent_node = root\n    else:\n        temp = root\n        while temp is not None:\n            if temp.val == pi:\n                parent_node = temp\n                break\n            if temp.val < pi:\n                if temp.right is None:\n                    temp.right = Node(pi)\n                    parent_node = temp.right\n                    break\n                temp = temp.right\n            else:\n                if temp.left is None:\n                    temp.left = Node(pi)\n                    parent_node = temp.left\n                    break\n                temp = temp.left\n    if Ri == 'L':\n        parent_node.left = child_node\n    else:\n        parent_node.right = child_node\nif search_in_bst(root, X):\n    print('YES')\nelse:\n    print('NO')",
      "JavaScript": "const lines = require('fs').readFileSync(0, 'utf-8').trim().split('\\n');\nlet idx = 0;\n\n// Define the class for a tree node\nclass Node {\n    constructor(val) {\n        this.val = val;\n        this.left = null;\n        this.right = null;\n    }\n}\n\n// Function to search for a value in the BST\nfunction searchInBST(root, x) {\n    if (root === null) {\n        return false;\n    }\n    if (root.val === x) {\n        return true;\n    }\n    if (root.val < x) {\n        return searchInBST(root.right, x);\n    } else {\n        return searchInBST(root.left, x);\n    }\n}\n\n// Read input\nlet N, X;\n[N, X] = lines[idx++].trim().split(' ').map(Number);\nlet root = null;\nfor (let i = 0; i < N - 1; i++) {\n    let [pi, ci, Ri] = lines[idx++].trim().split(' ');\n    pi = Number(pi);\n    ci = Number(ci);\n    // Create nodes and assign them to the tree\n    let childNode = new Node(ci);\n    if (root === null) {\n        root = new Node(pi);\n        let parentNode = root;\n    } else {\n        let temp = root;\n        while (temp !== null) {\n            if (temp.val === pi) {\n                let parentNode = temp;\n                break;\n            }\n            if (temp.val < pi) {\n                if (temp.right === null) {\n                    temp.right = new Node(pi);\n                    let parentNode = temp.right;\n                    break;\n                }\n                temp = temp.right;\n            } else {\n                if (temp.left === null) {\n                    temp.left = new Node(pi);\n                    let parentNode = temp.left;\n                    break;\n                }\n                temp = temp.left;\n            }\n        }\n    }\n    if (Ri === 'L') {\n        parentNode.left = childNode;\n    } else {\n        parentNode.right = childNode;\n    }\n}\nif (searchInBST(root, X)) {\n    console.log('YES');\n} else {\n    console.log('NO');\n}"
    }
  },
  "QBCategory": "CCD",
  "boilerPlates": {
    "C": "#include <stdio.h>\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "C++": "#include <iostream>\nusing namespace std;\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "Java": "import java.util.*;\nimport java.io.*;\n\npublic class Main {\n    public static void main(String[] args) throws IOException {\n        // your code goes here\n    }\n}",
    "Python3": "# your code goes here\n",
    "JavaScript": "// your code goes here\n"
  },
  "_internal": {
    "methodName": "",
    "params": []
  }
}