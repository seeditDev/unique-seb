{
  "questionId": "Q3412",
  "title": "Chef Land",
  "slug": "chef-land",
  "version": 1,
  "createdAt": "2026-07-01",
  "updatedAt": "2026-07-04",
  "metadata": {
    "category": "Graphs",
    "difficulty": "Hard",
    "isPremium": false,
    "tags": [
      "Graphs",
      "Bridges",
      "Biconnected Components",
      "Connected Components",
      "Graph Theory"
    ],
    "companyTags": [],
    "topicTags": [
      "Graphs"
    ]
  },
  "content": {
    "problemStatement": "You are given an undirected unweighted graph consisting of **N** nodes and M edges. A pair of nodes **u, v** is called good if and only if you can go from node **u** to **v** and then return back from **v** to **u** without using any edge more than once.\n\nCan you add at most one edge to the graph to make all the pairs of nodes good? Note that the added edge can be between two vertices which are already connected by some edges. However you are not allowed to add self loops.",
    "constraints": [
      "1 \u2264 N \u2264 10^5",
      "0 \u2264 M \u2264 2 * 10^5",
      "1 \u2264 x, y \u2264 N",
      "The graph is connected.",
      "The graph may contain multi-edges but does not contain any self-loops."
    ],
    "inputFormat": "First line contains two space-separated integers - N (number of nodes) and M (number of edges). Each of the next M lines contains two space-separated integers x and y, denoting that there is an edge between node x and y.",
    "outputFormat": "Output 'YES' if it is possible to make all pairs of nodes good by adding at most one edge. Output 'NO' otherwise.",
    "sampleTestCases": [
      {
        "id": "sample_1",
        "input": "",
        "output": "Input:\n4 4\n3 2\n3 1\n2 1\n1 4\nOutput:\nYES",
        "explanation": ""
      }
    ]
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "label": "Case 1",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_2",
        "label": "Case 2",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_3",
        "label": "Case 3",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_4",
        "label": "Case 4",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_5",
        "label": "Case 5",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      }
    ],
    "totalTestCases": 5,
    "totalWeight": 10
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      2,
      2,
      2,
      2,
      2
    ],
    "maxScore": 10
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "To solve this problem, we can use the concept of biconnected components and bridges in a graph. A biconnected component is a maximal subgraph that remains connected even after removing any single edge. A bridge is an edge that, when removed, increases the number of connected components in the graph. We can use a depth-first search (DFS) to find all biconnected components and bridges in the graph. If the graph has more than one biconnected component or more than one bridge, it is not possible to make all pairs of nodes good by adding at most one edge. Otherwise, we can add an edge between two nodes from different biconnected components or between two nodes that are not connected by a bridge.",
    "timeComplexity": "O(N + M) - The time complexity is linear with respect to the number of nodes and edges in the graph, as we perform a constant amount of work for each node and edge during the DFS.",
    "spaceComplexity": "O(N + M) - The space complexity is also linear, as in the worst case, we may need to store all nodes and edges in the graph.",
    "code": {
      "C": "// COMPLETE, COMPILABLE C code\n// Uses correct I/O\n// Handles all T test cases\n#include <stdio.h>\n#include <stdlib.h>\n#define MAXN 100005\n#define MAXM 200005\n\n// Define the structure for an edge\ntypedef struct Edge {\n    int u, v;\n} Edge;\n\n// Define the structure for a graph\ntypedef struct Graph {\n    int n, m;\n    Edge *edges;\n} Graph;\n\n// Function to read the input graph\nGraph readGraph() {\n    Graph graph;\n    scanf(\"%d %d\", &graph.n, &graph.m);\n    graph.edges = (Edge *)malloc(graph.m * sizeof(Edge));\n    for (int i = 0; i < graph.m; i++) {\n        scanf(\"%d %d\", &graph.edges[i].u, &graph.edges[i].v);\n    }\n    return graph;\n}\n\n// Function to check if it is possible to make all pairs of nodes good by adding at most one edge\nint check(Graph graph) {\n    // Perform DFS to find all biconnected components and bridges\n    int *low = (int *)malloc(graph.n * sizeof(int));\n    int *disc = (int *)malloc(graph.n * sizeof(int));\n    int *parent = (int *)malloc(graph.n * sizeof(int));\n    int *visited = (int *)malloc(graph.n * sizeof(int));\n    int time = 0;\n    int bridgeCount = 0;\n    for (int i = 0; i < graph.n; i++) {\n        visited[i] = 0;\n    }\n    for (int i = 0; i < graph.n; i++) {\n        if (!visited[i]) {\n            bridgeCount += dfs(graph, i, -1, low, disc, parent, visited, &time);\n        }\n    }\n    free(low);\n    free(disc);\n    free(parent);\n    free(visited);\n    return bridgeCount <= 1;\n}\n\n// Function to perform DFS\nint dfs(Graph graph, int u, int parent, int *low, int *disc, int *parentArr, int *visited, int *time) {\n    visited[u] = 1;\n    disc[u] = *time;\n    low[u] = *time;\n    (*time)++;\n    int bridgeCount = 0;\n    for (int i = 0; i < graph.m; i++) {\n        int v = graph.edges[i].v;\n        if (graph.edges[i].u == u) {\n            if (!visited[v]) {\n                parentArr[v] = u;\n                bridgeCount += dfs(graph, v, u, low, disc, parentArr, visited, time);\n                low[u] = min(low[u], low[v]);\n                if (low[v] > disc[u]) {\n                    bridgeCount++;\n                }\n            } else if (v != parent) {\n                low[u] = min(low[u], disc[v]);\n            }\n        }\n    }\n    return bridgeCount;\n}\n\n// Function to find the minimum of two integers\nint min(int a, int b) {\n    return a < b ? a : b;\n}\n\nint main() {\n    int t;\n    scanf(\"%d\", &t);\n    while (t--) {\n        Graph graph = readGraph();\n        if (check(graph)) {\n            printf(\"YES\\n\");\n        } else {\n            printf(\"NO\\n\");\n        }\n    }\n    return 0;\n}",
      "C++": "// COMPLETE, COMPILABLE C++ code\n// Uses correct I/O\n// Handles all T test cases\n#include <iostream>\n#include <vector>\n#include <queue>\n#include <stack>\n#include <algorithm>\nusing namespace std;\n\n// Define the structure for an edge\nstruct Edge {\n    int u, v;\n};\n\n// Define the structure for a graph\nstruct Graph {\n    int n, m;\n    vector<Edge> edges;\n};\n\n// Function to read the input graph\nGraph readGraph() {\n    Graph graph;\n    cin >> graph.n >> graph.m;\n    graph.edges.resize(graph.m);\n    for (int i = 0; i < graph.m; i++) {\n        cin >> graph.edges[i].u >> graph.edges[i].v;\n    }\n    return graph;\n}\n\n// Function to check if it is possible to make all pairs of nodes good by adding at most one edge\nbool check(Graph graph) {\n    // Perform DFS to find all biconnected components and bridges\n    vector<int> low(graph.n, 0);\n    vector<int> disc(graph.n, 0);\n    vector<int> parent(graph.n, -1);\n    vector<bool> visited(graph.n, false);\n    int time = 0;\n    int bridgeCount = 0;\n    for (int i = 0; i < graph.n; i++) {\n        if (!visited[i]) {\n            bridgeCount += dfs(graph, i, -1, low, disc, parent, visited, &time);\n        }\n    }\n    return bridgeCount <= 1;\n}\n\n// Function to perform DFS\nint dfs(Graph graph, int u, int parent, vector<int>& low, vector<int>& disc, vector<int>& parentArr, vector<bool>& visited, int* time) {\n    visited[u] = true;\n    disc[u] = *time;\n    low[u] = *time;\n    (*time)++;\n    int bridgeCount = 0;\n    for (int i = 0; i < graph.m; i++) {\n        int v = graph.edges[i].v;\n        if (graph.edges[i].u == u) {\n            if (!visited[v]) {\n                parentArr[v] = u;\n                bridgeCount += dfs(graph, v, u, low, disc, parentArr, visited, time);\n                low[u] = min(low[u], low[v]);\n                if (low[v] > disc[u]) {\n                    bridgeCount++;\n                }\n            } else if (v != parent) {\n                low[u] = min(low[u], disc[v]);\n            }\n        }\n    }\n    return bridgeCount;\n}\n\nint main() {\n    int t;\n    cin >> t;\n    while (t--) {\n        Graph graph = readGraph();\n        if (check(graph)) {\n            cout << \"YES\" << endl;\n        } else {\n            cout << \"NO\" << endl;\n        }\n    }\n    return 0;\n}",
      "Java": "// COMPLETE, COMPILABLE Java code\n// Class MUST be named Main\n// Uses correct I/O\n// Handles all T test cases\nimport java.util.Scanner;\nimport java.util.ArrayList;\nimport java.util.List;\npublic class Main {\n    // Define the structure for an edge\n    static class Edge {\n        int u, v;\n        public Edge(int u, int v) {\n            this.u = u;\n            this.v = v;\n        }\n    }\n\n    // Define the structure for a graph\n    static class Graph {\n        int n, m;\n        List<Edge> edges;\n        public Graph(int n, int m) {\n            this.n = n;\n            this.m = m;\n            this.edges = new ArrayList<>();\n        }\n    }\n\n    // Function to read the input graph\n    public static Graph readGraph(Scanner scanner) {\n        Graph graph = new Graph(scanner.nextInt(), scanner.nextInt());\n        for (int i = 0; i < graph.m; i++) {\n            int u = scanner.nextInt() - 1;\n            int v = scanner.nextInt() - 1;\n            graph.edges.add(new Edge(u, v));\n        }\n        return graph;\n    }\n\n    // Function to check if it is possible to make all pairs of nodes good by adding at most one edge\n    public static boolean check(Graph graph) {\n        // Perform DFS to find all biconnected components and bridges\n        int[] low = new int[graph.n];\n        int[] disc = new int[graph.n];\n        int[] parent = new int[graph.n];\n        boolean[] visited = new boolean[graph.n];\n        int time = 0;\n        int bridgeCount = 0;\n        for (int i = 0; i < graph.n; i++) {\n            if (!visited[i]) {\n                bridgeCount += dfs(graph, i, -1, low, disc, parent, visited, time);\n            }\n        }\n        return bridgeCount <= 1;\n    }\n\n    // Function to perform DFS\n    public static int dfs(Graph graph, int u, int parent, int[] low, int[] disc, int[] parentArr, boolean[] visited, int time) {\n        visited[u] = true;\n        disc[u] = time;\n        low[u] = time;\n        time++;\n        int bridgeCount = 0;\n        for (Edge edge : graph.edges) {\n            int v = edge.v;\n            if (edge.u == u) {\n                if (!visited[v]) {\n                    parentArr[v] = u;\n                    bridgeCount += dfs(graph, v, u, low, disc, parentArr, visited, time);\n                    low[u] = Math.min(low[u], low[v]);\n                    if (low[v] > disc[u]) {\n                        bridgeCount++;\n                    }\n                } else if (v != parent) {\n                    low[u] = Math.min(low[u], disc[v]);\n                }\n            }\n        }\n        return bridgeCount;\n    }\n\n    public static void main(String[] args) {\n        Scanner scanner = new Scanner(System.in);\n        int t = scanner.nextInt();\n        while (t-- > 0) {\n            Graph graph = readGraph(scanner);\n            if (check(graph)) {\n                System.out.println(\"YES\");\n            } else {\n                System.out.println(\"NO\");\n            }\n        }\n    }\n}",
      "Python3": "# COMPLETE, RUNNABLE Python3 code\n# Uses input() or sys.stdin\n# Handles all T test cases\nimport sys\ninput = sys.stdin.readline\n\n# Define the structure for an edge\nclass Edge:\n    def __init__(self, u, v):\n        self.u = u\n        self.v = v\n\n# Define the structure for a graph\nclass Graph:\n    def __init__(self, n, m):\n        self.n = n\n        self.m = m\n        self.edges = []\n\n# Function to read the input graph\ndef readGraph():\n    n, m = map(int, input().split())\n    graph = Graph(n, m)\n    for _ in range(m):\n        u, v = map(int, input().split())\n        graph.edges.append(Edge(u - 1, v - 1))\n    return graph\n\n# Function to check if it is possible to make all pairs of nodes good by adding at most one edge\ndef check(graph):\n    # Perform DFS to find all biconnected components and bridges\n    low = [0] * graph.n\n    disc = [0] * graph.n\n    parent = [-1] * graph.n\n    visited = [False] * graph.n\n    time = [0]\n    bridgeCount = [0]\n    for i in range(graph.n):\n        if not visited[i]:\n            bridgeCount[0] += dfs(graph, i, -1, low, disc, parent, visited, time)\n    return bridgeCount[0] <= 1\n\n# Function to perform DFS\ndef dfs(graph, u, parent, low, disc, parentArr, visited, time):\n    visited[u] = True\n    disc[u] = time[0]\n    low[u] = time[0]\n    time[0] += 1\n    bridgeCount = 0\n    for edge in graph.edges:\n        v = edge.v\n        if edge.u == u:\n            if not visited[v]:\n                parentArr[v] = u\n                bridgeCount += dfs(graph, v, u, low, disc, parentArr, visited, time)\n                low[u] = min(low[u], low[v])\n                if low[v] > disc[u]:\n                    bridgeCount += 1\n            elif v != parent:\n                low[u] = min(low[u], disc[v])\n    return bridgeCount\n\n# Main function\nif __name__ == '__main__':\n    t = int(input())\n    for _ in range(t):\n        graph = readGraph()\n        if check(graph):\n            print('YES')\n        else:\n            print('NO')",
      "JavaScript": ""
    }
  }
}