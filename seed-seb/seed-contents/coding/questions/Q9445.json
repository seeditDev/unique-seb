{
  "questionId": "Q9445",
  "title": "Boring String",
  "slug": "boring-string",
  "version": 1,
  "createdAt": "2026-07-02",
  "updatedAt": "2026-07-04",
  "metadata": {
    "category": "Strings",
    "difficulty": "Medium",
    "isPremium": true,
    "tags": [
      "Run Length Encoding",
      "Sliding Window",
      "Greedy"
    ],
    "companyTags": [],
    "topicTags": [
      "Strings"
    ],
    "QBCategory": "CCD"
  },
  "content": {
    "problemStatement": "A string is called boring if all the characters of the string are **same**.\n\nYou are given a string SSS of length NNN, consisting of lowercase english alphabets. Find the length of the longest boring substring of SSS which occurs **more than once**.\n\nNote that if there is no boring substring which occurs more than once in SSS, the answer will be 000.\n\nA substring is obtained by deleting some (possibly zero) elements from the beginning of the string and some (possibly zero) elements from the end of the string.\n\n\nInput Format\n\nThe first line of input will contain a single integer TTT, denoting the number of test cases.\nEach test case consists of two lines of input.\n\nThe first line of each test case contains an integer NNN, denoting the length of string SSS.\nThe next contains string SSS.\n\n\n\n\nOutput Format\nFor each test case, output on a new line, the length of the longest boring substring of SSS which occurs **more than once**.",
    "constraints": [
      "1 <= T <= 1000",
      "1 <= N <= 200000",
      "Sum of N over all test cases <= 500000",
      "S consists only of lowercase English letters"
    ],
    "inputFormat": "The first line contains an integer T, the number of test cases. Each test case consists of two lines: the first line contains an integer N, the length of the string. The second line contains the string S of length N consisting of lowercase English letters.",
    "outputFormat": "For each test case output a single integer on its own line – the length of the longest boring substring (all characters equal) that occurs more than once in S. If no such substring exists output 0.",
    "sampleTestCases": [
      {
        "id": "sample_1",
        "input": "4\n3\naaa\n3\nabc\n5\nbcaca\n6\ncaabaa",
        "output": "2\n0\n1\n2",
        "explanation": "Test case 1: longest boring substring occurring more than once is \"aa\" of length 2.\\nTest case 2: no character repeats, answer 0.\\nTest case 3: characters \"c\" and \"a\" each appear as single‑character boring substrings more than once, so answer 1.\\nTest case 4: substring \"aa\" appears twice, giving answer 2."
      }
    ]
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "label": "minimum boundary",
        "input": "1\n1\na",
        "expectedOutput": "0",
        "weight": 2
      },
      {
        "id": "tc_2",
        "label": "maximum boundary (single character)",
        "input": "1\n100\naaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa",
        "expectedOutput": "99",
        "weight": 2
      },
      {
        "id": "tc_3",
        "label": "two equal runs",
        "input": "1\n6\naabbaa",
        "expectedOutput": "2",
        "weight": 2
      },
      {
        "id": "tc_4",
        "label": "different run lengths",
        "input": "1\n13\nabcccdecccccfg",
        "expectedOutput": "4",
        "weight": 2
      },
      {
        "id": "tc_5",
        "label": "no repeats",
        "input": "1\n5\nabcde",
        "expectedOutput": "0",
        "weight": 2
      }
    ],
    "totalTestCases": 5,
    "totalWeight": 10
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      2,
      2,
      2,
      2,
      2
    ],
    "maxScore": 10
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "For each character we only need the two longest contiguous runs of that character. While scanning the string we keep the current run length and when the run ends we update the first and second maximum for that character. For a character with longest run L1 and second longest L2 the longest boring substring that can appear more than once using this character is max(L1-1, L2) – L1-1 accounts for two overlapping occurrences inside a single long run, L2 accounts for using two separate runs. The answer for the test case is the maximum of this value over all 26 characters. The scan is O(N) per test case and uses O(1) extra memory.",
    "timeComplexity": "O(N) per test case, where N is the length of the string.",
    "spaceComplexity": "O(1) – only a few integer arrays of size 26 are stored.",
    "code": {
      "C": "#include <stdio.h>\n#include <string.h>\nint main(){\n    int T; if(scanf(\"%d\",&T)!=1) return 0;\n    while(T--){\n        int N; scanf(\"%d\",&N);\n        char s[200005]; scanf(\"%s\",s);\n        int best[26][2]; memset(best,0,sizeof(best));\n        int curLen=0; char curChar=0;\n        for(int i=0;i<N;i++){\n            if(i==0 || s[i]!=s[i-1]){\n                if(i!=0){\n                    int idx=curChar-'a';\n                    if(curLen>best[idx][0]){best[idx][1]=best[idx][0];best[idx][0]=curLen;}\n                    else if(curLen>best[idx][1]) best[idx][1]=curLen;\n                }\n                curChar=s[i]; curLen=1;\n            }else curLen++;\n        }\n        // last run\n        int idx=curChar-'a';\n        if(curLen>best[idx][0]){best[idx][1]=best[idx][0];best[idx][0]=curLen;}\n        else if(curLen>best[idx][1]) best[idx][1]=curLen;\n        int ans=0;\n        for(int c=0;c<26;c++){\n            int L1=best[c][0];\n            int L2=best[c][1];\n            int cand = L1>0 ? (L1-1> L2 ? L1-1 : L2) : 0;\n            if(cand>ans) ans=cand;\n        }\n        printf(\"%d\\n\",ans);\n    }\n    return 0;\n}",
      "C++": "#include <bits/stdc++.h>\nusing namespace std;\nint main(){\n    ios::sync_with_stdio(false);\n    cin.tie(nullptr);\n    int T; if(!(cin>>T)) return 0;\n    while(T--){\n        int N; string s; cin>>N>>s;\n        int best[26][2]={{0}}; // [][0]=max, [][1]=second max\n        int curLen=0; char curChar=0;\n        for(int i=0;i<N;i++){\n            if(i==0 || s[i]!=s[i-1]){\n                if(i!=0){\n                    int idx=curChar-'a';\n                    if(curLen>best[idx][0]){best[idx][1]=best[idx][0];best[idx][0]=curLen;}\n                    else if(curLen>best[idx][1]) best[idx][1]=curLen;\n                }\n                curChar=s[i]; curLen=1;\n            }else curLen++;\n        }\n        // last run\n        int idx=curChar-'a';\n        if(curLen>best[idx][0]){best[idx][1]=best[idx][0];best[idx][0]=curLen;}\n        else if(curLen>best[idx][1]) best[idx][1]=curLen;\n        int ans=0;\n        for(int c=0;c<26;c++){\n            int L1=best[c][0];\n            int L2=best[c][1];\n            int cand = max(L1>0?L1-1:0, L2);\n            ans=max(ans,cand);\n        }\n        cout<<ans<<\"\\n\";\n    }\n    return 0;\n}",
      "Java": "import java.io.*;\nimport java.util.*;\npublic class Main {\n    public static void main(String[] args) throws Exception {\n        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));\n        StringBuilder out = new StringBuilder();\n        int T = Integer.parseInt(br.readLine().trim());\n        while (T-- > 0) {\n            int N = Integer.parseInt(br.readLine().trim());\n            String s = br.readLine().trim();\n            int[][] best = new int[26][2];\n            int curLen = 0; char curChar = 0;\n            for (int i = 0; i < N; i++) {\n                char ch = s.charAt(i);\n                if (i == 0 || ch != s.charAt(i - 1)) {\n                    if (i != 0) {\n                        int idx = curChar - 'a';\n                        if (curLen > best[idx][0]) { best[idx][1] = best[idx][0]; best[idx][0] = curLen; }\n                        else if (curLen > best[idx][1]) best[idx][1] = curLen;\n                    }\n                    curChar = ch; curLen = 1;\n                } else {\n                    curLen++;\n                }\n            }\n            int idx = curChar - 'a';\n            if (curLen > best[idx][0]) { best[idx][1] = best[idx][0]; best[idx][0] = curLen; }\n            else if (curLen > best[idx][1]) best[idx][1] = curLen;\n            int ans = 0;\n            for (int c = 0; c < 26; c++) {\n                int L1 = best[c][0];\n                int L2 = best[c][1];\n                int cand = Math.max(L1 > 0 ? L1 - 1 : 0, L2);\n                ans = Math.max(ans, cand);\n            }\n            out.append(ans).append('\\n');\n        }\n        System.out.print(out.toString());\n    }\n}",
      "Python3": "import sys\n\ndef solve():\n    data = sys.stdin.read().split()\n    it = iter(data)\n    T = int(next(it))\n    out = []\n    for _ in range(T):\n        N = int(next(it))\n        s = next(it)\n        best = [[0,0] for _ in range(26)]\n        cur_len = 0\n        cur_char = ''\n        for i,ch in enumerate(s):\n            if i==0 or ch!=s[i-1]:\n                if i!=0:\n                    idx = ord(cur_char)-97\n                    if cur_len>best[idx][0]:\n                        best[idx][1]=best[idx][0]\n                        best[idx][0]=cur_len\n                    elif cur_len>best[idx][1]:\n                        best[idx][1]=cur_len\n                cur_char = ch\n                cur_len = 1\n            else:\n                cur_len+=1\n        idx = ord(cur_char)-97\n        if cur_len>best[idx][0]:\n            best[idx][1]=best[idx][0]\n            best[idx][0]=cur_len\n        elif cur_len>best[idx][1]:\n            best[idx][1]=cur_len\n        ans = 0\n        for L1,L2 in best:\n            cand = max(L1-1 if L1>0 else 0, L2)\n            if cand>ans:\n                ans=cand\n        out.append(str(ans))\n    sys.stdout.write('\\n'.join(out))\n\nif __name__ == \"__main__\":\n    solve()\n",
      "JavaScript": "const fs = require('fs');\nconst input = fs.readFileSync(0,'utf-8').trim().split(/\\s+/);\nlet idx = 0;\nconst T = Number(input[idx++]);\nlet results = [];\nfor(let t=0;t<T;t++){\n  const N = Number(input[idx++]);\n  const s = input[idx++];\n  const best = Array.from({length:26},()=>[0,0]);\n  let curLen=0; let curChar='';\n  for(let i=0;i<N;i++){\n    const ch = s[i];\n    if(i===0 || ch!==s[i-1]){\n      if(i!==0){\n        const id = curChar.charCodeAt(0)-97;\n        if(curLen>best[id][0]){best[id][1]=best[id][0];best[id][0]=curLen;}\n        else if(curLen>best[id][1]) best[id][1]=curLen;\n      }\n      curChar=ch; curLen=1;\n    }else curLen++;\n  }\n  const id = curChar.charCodeAt(0)-97;\n  if(curLen>best[id][0]){best[id][1]=best[id][0];best[id][0]=curLen;}\n  else if(curLen>best[id][1]) best[id][1]=curLen;\n  let ans=0;\n  for(let c=0;c<26;c++){\n    const L1=best[c][0]; const L2=best[c][1];\n    const cand = Math.max(L1>0?L1-1:0, L2);\n    if(cand>ans) ans=cand;\n  }\n  results.push(String(ans));\n}\nconsole.log(results.join('\\n'));\n"
    }
  },
  "QBCategory": "CCD",
  "boilerPlates": {
    "C": "#include <stdio.h>\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "C++": "#include <iostream>\nusing namespace std;\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "Java": "import java.util.*;\nimport java.io.*;\n\npublic class Main {\n    public static void main(String[] args) throws IOException {\n        // your code goes here\n    }\n}",
    "Python3": "# your code goes here\n",
    "JavaScript": "// your code goes here\n"
  },
  "_internal": {
    "methodName": "",
    "params": []
  }
}