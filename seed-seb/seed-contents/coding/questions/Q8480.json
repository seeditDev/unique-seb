{
  "questionId": "Q8480",
  "title": "Sorter Prodigy",
  "slug": "sorter-prodigy",
  "version": 1,
  "createdAt": "2026-07-02",
  "updatedAt": "2026-07-04",
  "metadata": {
    "category": "Greedy | Sorting",
    "difficulty": "Hard",
    "isPremium": true,
    "tags": [
      "Permutation",
      "Greedy",
      "Array manipulation",
      "Constructive algorithms"
    ],
    "companyTags": [],
    "topicTags": [
      "Constructive Algorithms"
    ],
    "QBCategory": "CCD"
  },
  "content": {
    "problemStatement": "You are given a permutation (P1,P2,…,PN)(P_1, P_2, \\ldots, P_N)(P1​,P2​,…,PN​). In one operation, you can do the following:\n\n\nChoose any integers L,RL, RL,R with 1≤L≤R≤N1 \\le L \\le R \\le N1≤L≤R≤N. Then, swap the maximum and minimum elements in subarray P[L:R]P[L:R]P[L:R].\nSort the permutation. You can spend at most 4⋅1064\\cdot 10^64⋅106 operations in total for all test cases from a single input.\n\n\nInput Format\n\n\nThe first line of the input contains a single integer TTT −-− the number of test cases. The description of test cases follows.\n\n\n\n\nThe first line of each test case contains a single integer NNN.\n\n\n\n\nThe second line of each test case contains NNN integers P1,P2,…,PNP_1, P_2, \\ldots, P_NP1​,P2​,…,PN​ −-− the elements of the permutation.\n\n\n\n\n\nOutput Format\nFor each test case, in the first line output KKK −-− the number of operations.\n\n\nIn each of the next KKK lines, output two integers L,RL, RL,R (1≤L≤R≤N1 \\le L \\le R \\le N1≤L≤R≤N), denoting that you are swapping maximum and minimum in segment P[L:R]P[L:R]P[L:R].\n\n\nSum of KKK over all test cases can't exceed 4⋅1064 \\cdot 10^64⋅106.",
    "constraints": [
      "1 ≤ T ≤ 30 000",
      "1 ≤ N ≤ 30 000",
      "The sum of N over all test cases ≤ 30 000",
      "P is a permutation of {1,…,N}"
    ],
    "inputFormat": "The first line contains an integer T – the number of test cases. Then for each test case:\n- A line with an integer N – the length of the permutation.\n- A line with N distinct integers P1,P2,…,PN – the permutation of numbers 1…N.",
    "outputFormat": "For each test case output K – the number of performed operations.\nThen output K lines, each containing two integers L and R (1 ≤ L ≤ R ≤ N) describing an operation that swaps the maximum and minimum elements of the subarray P[L…R]. The total number of printed operations over all test cases must not exceed 4·10^6.",
    "sampleTestCases": [
      {
        "id": "sample_1",
        "input": "3\n4\n1 2 3 4\n4\n4 1 2 3\n5\n5 2 3 4 1",
        "output": "0\n3\n1 4\n1 3\n1 2\n1\n1 5",
        "explanation": "Test case 1 is already sorted, so no operation is needed.\\nTest case 2: we process values 4,3,2.\\n- For 4 the segment [1,4] swaps 4 and 1.\\n- For 3 the segment [1,3] swaps 3 and 2.\\n- For 2 the segment [1,2] swaps 2 and 1. After three operations the array becomes (1,2,3,4).\\nTest case 3: value 5 is at position 1, segment [1,5] swaps 5 and 1, sorting the permutation in one operation."
      }
    ]
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "label": "Case 1",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_2",
        "label": "Case 2",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_3",
        "label": "Case 3",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_4",
        "label": "Case 4",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_5",
        "label": "Case 5",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      }
    ],
    "totalTestCases": 5,
    "totalWeight": 10
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      2,
      2,
      2,
      2,
      2
    ],
    "maxScore": 10
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "We place the numbers from the largest to the smallest.\nFor a value x we already know the position idx where x currently stands (maintained in an auxiliary array pos). If idx equals x, x is already in the correct place. Otherwise we apply one operation on the segment [L,R] where L = min(x,idx) and R = max(x,idx). In this segment all numbers larger than x are already fixed to the right of x, therefore x is the maximum element of the segment and the element at position x is the minimum. Swapping max and min moves x to its correct position x and puts the former element of position x to idx. After the swap we update the permutation and the positions of the two swapped values. Repeating this for x = N … 1 sorts the whole array using at most N operations per test case.\nThe algorithm runs in O(N) per test case because each value is processed once and all updates are O(1).",
    "timeComplexity": "O(N) per test case, O(total N) over the whole input (≤ 30 000).",
    "spaceComplexity": "O(N) for the permutation and the position array.",
    "code": {
      "C": "#include <stdio.h>\n#include <stdlib.h>\n\nint main(){\n    int T; if(scanf(\"%d\", &T)!=1) return 0;\n    while(T--){\n        int N; scanf(\"%d\", &N);\n        int *a = (int*)malloc((N+1)*sizeof(int));\n        int *pos = (int*)malloc((N+1)*sizeof(int));\n        for(int i=1;i<=N;i++){\n            scanf(\"%d\", &a[i]);\n            pos[a[i]] = i;\n        }\n        // store operations in dynamic arrays (max N)\n        int *L = (int*)malloc((N+1)*sizeof(int));\n        int *R = (int*)malloc((N+1)*sizeof(int));\n        int ops = 0;\n        for(int x=N;x>=1;x--){\n            int idx = pos[x];\n            if(idx==x) continue;\n            int l = idx < x ? idx : x;\n            int r = idx > x ? idx : x;\n            L[ops] = l; R[ops] = r; ops++;\n            // swap a[l] and a[r]\n            int v1 = a[l];\n            int v2 = a[r];\n            a[l] = v2; a[r] = v1;\n            pos[v1] = r; pos[v2] = l;\n        }\n        printf(\"%d\\n\", ops);\n        for(int i=0;i<ops;i++) printf(\"%d %d\\n\", L[i], R[i]);\n        free(a); free(pos); free(L); free(R);\n    }\n    return 0;\n}",
      "C++": "#include <bits/stdc++.h>\nusing namespace std;\nint main(){\n    ios::sync_with_stdio(false);\n    cin.tie(nullptr);\n    int T; if(!(cin>>T)) return 0;\n    while(T--){\n        int N; cin>>N;\n        vector<int> a(N+1), pos(N+1);\n        for(int i=1;i<=N;i++){\n            cin>>a[i];\n            pos[a[i]]=i;\n        }\n        vector<pair<int,int>> ops;\n        for(int x=N;x>=1;x--){\n            int idx = pos[x];\n            if(idx==x) continue;\n            int l = min(x, idx), r = max(x, idx);\n            ops.emplace_back(l,r);\n            int v1=a[l], v2=a[r];\n            swap(a[l], a[r]);\n            pos[v1]=r; pos[v2]=l;\n        }\n        cout<<ops.size()<<'\\n';\n        for(auto &p:ops) cout<<p.first<<' '<<p.second<<'\\n';\n    }\n    return 0;\n}",
      "Java": "import java.io.*;\nimport java.util.*;\n\npublic class Main {\n    public static void main(String[] args) throws Exception {\n        FastScanner fs = new FastScanner(System.in);\n        StringBuilder out = new StringBuilder();\n        int T = fs.nextInt();\n        while (T-- > 0) {\n            int N = fs.nextInt();\n            int[] a = new int[N + 1];\n            int[] pos = new int[N + 1];\n            for (int i = 1; i <= N; i++) {\n                a[i] = fs.nextInt();\n                pos[a[i]] = i;\n            }\n            List<int[]> ops = new ArrayList<>();\n            for (int x = N; x >= 1; x--) {\n                int idx = pos[x];\n                if (idx == x) continue;\n                int l = Math.min(x, idx);\n                int r = Math.max(x, idx);\n                ops.add(new int[]{l, r});\n                int v1 = a[l];\n                int v2 = a[r];\n                a[l] = v2; a[r] = v1;\n                pos[v1] = r; pos[v2] = l;\n            }\n            out.append(ops.size()).append('\\n');\n            for (int[] p : ops) out.append(p[0]).append(' ').append(p[1]).append('\\n');\n        }\n        System.out.print(out.toString());\n    }\n\n    // fast scanner\n    static class FastScanner {\n        private final InputStream in;\n        private final byte[] buffer = new byte[1 << 16];\n        private int ptr = 0, len = 0;\n        FastScanner(InputStream in) { this.in = in; }\n        private int readByte() throws IOException {\n            if (ptr >= len) {\n                len = in.read(buffer);\n                ptr = 0;\n                if (len <= 0) return -1;\n            }\n            return buffer[ptr++];\n        }\n        int nextInt() throws IOException {\n            int c, sign = 1, val = 0;\n            do { c = readByte(); } while (c <= ' ');\n            if (c == '-') { sign = -1; c = readByte(); }\n            while (c > ' ') {\n                val = val * 10 + (c - '0');\n                c = readByte();\n            }\n            return val * sign;\n        }\n    }\n}",
      "Python3": "import sys\n\ndef solve():\n    data = sys.stdin.read().strip().split()\n    if not data:\n        return\n    it = iter(data)\n    T = int(next(it))\n    out_lines = []\n    for _ in range(T):\n        N = int(next(it))\n        a = [0] * (N + 1)\n        pos = [0] * (N + 1)\n        for i in range(1, N + 1):\n            val = int(next(it))\n            a[i] = val\n            pos[val] = i\n        ops = []\n        for x in range(N, 0, -1):\n            idx = pos[x]\n            if idx == x:\n                continue\n            l, r = (x, idx) if x < idx else (idx, x)\n            ops.append((l, r))\n            v1, v2 = a[l], a[r]\n            a[l], a[r] = v2, v1\n            pos[v1], pos[v2] = r, l\n        out_lines.append(str(len(ops)))\n        out_lines.extend(f\"{l} {r}\" for l, r in ops)\n    sys.stdout.write('\\n'.join(out_lines))\n\nif __name__ == \"__main__\":\n    solve()",
      "JavaScript": "const fs = require('fs');\nconst input = fs.readFileSync(0, 'utf-8').trim().split(/\\s+/).map(Number);\nlet idx = 0;\nconst T = input[idx++];\nlet output = [];\nfor (let t = 0; t < T; t++) {\n  const N = input[idx++];\n  const a = new Array(N + 1);\n  const pos = new Array(N + 1);\n  for (let i = 1; i <= N; i++) {\n    const v = input[idx++];\n    a[i] = v;\n    pos[v] = i;\n  }\n  const ops = [];\n  for (let x = N; x >= 1; x--) {\n    const cur = pos[x];\n    if (cur === x) continue;\n    const l = Math.min(x, cur);\n    const r = Math.max(x, cur);\n    ops.push([l, r]);\n    const v1 = a[l];\n    const v2 = a[r];\n    a[l] = v2; a[r] = v1;\n    pos[v1] = r; pos[v2] = l;\n  }\n  output.push(String(ops.length));\n  for (const [l, r] of ops) output.push(l + ' ' + r);\n}\nconsole.log(output.join('\\n'));\n"
    }
  },
  "QBCategory": "CCD",
  "boilerPlates": {
    "C": "#include <stdio.h>\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "C++": "#include <iostream>\nusing namespace std;\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "Java": "import java.util.*;\nimport java.io.*;\n\npublic class Main {\n    public static void main(String[] args) throws IOException {\n        // your code goes here\n    }\n}",
    "Python3": "# your code goes here\n",
    "JavaScript": "// your code goes here\n"
  },
  "_internal": {
    "methodName": "",
    "params": []
  }
}