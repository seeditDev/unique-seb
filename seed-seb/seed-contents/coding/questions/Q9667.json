{
  "questionId": "Q9667",
  "title": "Unstable Subarray",
  "slug": "unstable-subarray",
  "version": 1,
  "createdAt": "2026-07-02",
  "updatedAt": "2026-07-04",
  "metadata": {
    "category": "Arrays | Math | Counting",
    "difficulty": "Medium",
    "isPremium": true,
    "tags": [
      "Hash Map",
      "Telescoping Sum",
      "Frequency Counting"
    ],
    "companyTags": [],
    "topicTags": [
      "Data Structures"
    ],
    "QBCategory": "CCD"
  },
  "content": {
    "problemStatement": "You are given an array AAA of size NNN.\n\nWe define the function f(l,r)=∑i=lr−1(Ai−Ai+1)f(l, r) = \\sum_{i=l}^{r-1}(A_i-A_{i+1})f(l,r)=∑i=lr−1​(Ai​−Ai+1​), where 1≤l≤r≤N1\\le l \\le r \\le N1≤l≤r≤N.\n\nNote that f(i,i)f(i, i)f(i,i) is defined as 000.\n\nA subarray A[l,r]A[l, r]A[l,r] is considered unstable if f(l,r)≠(Ar−Al)f(l,r) \\neq (A_r-A_l)f(l,r)=(Ar​−Al​).\n\nCount the number of unstable subarrays in the array.\n\nNote that the subarray A[l,r]A[l, r]A[l,r] consists of Al,A(l+1),…,A(r−1),ArA_l, A_{(l+1)}, \\ldots, A_{(r-1)}, A_rAl​,A(l+1)​,…,A(r−1)​,Ar​.\n\n\nInput Format\n\nThe first line of input will contain a single integer TTT, denoting the number of test cases.\nEach test case consists of two lines of input.\n\nThe first line of each test case contains a single integer NNN — the number of elements in the array.\nThe second line of each test case contains NNN space-separated integers A1,A2,…,ANA_1,A_2,\\ldots ,A_NA1​,A2​,…,AN​ — the elements of the array.\n\n\n\n\nOutput Format\nFor each test case, output on a new line, the count of unstable subarrays.",
    "constraints": [
      "1 ≤ T ≤ 10^5",
      "1 ≤ N ≤ 10^5",
      "0 ≤ Ai ≤ 10^9",
      "The sum of N over all test cases does not exceed 5·10^5"
    ],
    "inputFormat": "The first line contains an integer T, the number of test cases. Each test case consists of two lines: the first line contains an integer N, the size of the array. The second line contains N space‑separated integers A1, A2, …, AN.",
    "outputFormat": "For each test case output a single line containing the number of unstable subarrays of the given array.",
    "sampleTestCases": [
      {
        "id": "sample_1",
        "input": "3\n3\n10 20 30\n4\n1 2 1 2\n5\n1 2 3 4 5",
        "output": "3\n4\n10",
        "explanation": "For each test case we count subarrays whose first and last elements differ.\\nTest1: all three pairs have different ends → 3.\\nTest2: total pairs = 6, equal‑end pairs are (1,3) and (2,4) → 6‑2 = 4.\\nTest3: all elements distinct, total pairs = 5·4/2 = 10."
      }
    ]
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "label": "minimum boundary",
        "input": "1\n1\n0",
        "expectedOutput": "0",
        "weight": 2
      },
      {
        "id": "tc_2",
        "label": "all equal elements",
        "input": "1\n5\n7 7 7 7 7",
        "expectedOutput": "0",
        "weight": 2
      },
      {
        "id": "tc_3",
        "label": "mixed frequencies",
        "input": "1\n6\n1 2 2 3 1 4",
        "expectedOutput": "13",
        "weight": 2
      },
      {
        "id": "tc_4",
        "label": "large N with distinct values",
        "input": "1\n8\n10 20 30 40 50 60 70 80",
        "expectedOutput": "28",
        "weight": 2
      },
      {
        "id": "tc_5",
        "label": "random case",
        "input": "1\n7\n5 5 1 2 5 1 3",
        "expectedOutput": "16",
        "weight": 2
      }
    ],
    "totalTestCases": 5,
    "totalWeight": 10
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      2,
      2,
      2,
      2,
      2
    ],
    "maxScore": 10
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "For any subarray [l, r] (l < r) we have\n    f(l, r) = Σ_{i=l}^{r-1} (Ai - Ai+1) = Al - Ar\nby telescoping. The subarray is unstable when f(l, r) ≠ (Ar - Al). Since (Ar - Al) = -(Al - Ar), the inequality becomes Al - Ar ≠ -(Al - Ar) ⇔ 2·(Al - Ar) ≠ 0 ⇔ Al ≠ Ar.\nThus a subarray is unstable exactly when its first and last elements are different. All length‑1 subarrays are stable.\nThe number of subarrays with l < r is totalPairs = N·(N‑1)/2. From these we must subtract the pairs where Al = Ar. For each distinct value v occurring cnt[v] times, the number of pairs with equal ends is cnt[v]·(cnt[v]‑1)/2. The answer is:\n    answer = totalPairs - Σ_v cnt[v]·(cnt[v]‑1)/2.\nWe can compute frequencies with a hash map (or by sorting) in O(N) time per test case.\nEdge cases: N = 1 → answer = 0; all elements equal → answer = 0; all elements distinct → answer = totalPairs.",
    "timeComplexity": "O(N) per test case (building the frequency map). The overall complexity is O(Σ N) ≤ 5·10^5.",
    "spaceComplexity": "O(K) where K is the number of distinct values in the array (≤ N).",
    "code": {
      "C": "#include <stdio.h>\n#include <stdlib.h>\n\nint cmp(const void *a, const void *b) {\n    long long x = *(const long long *)a;\n    long long y = *(const long long *)b;\n    return (x > y) - (x < y);\n}\n\nint main() {\n    int T; if (scanf(\"%d\", &T)!=1) return 0;\n    while (T--) {\n        int N; scanf(\"%d\", &N);\n        long long *arr = (long long *)malloc(N * sizeof(long long));\n        for (int i = 0; i < N; ++i) scanf(\"%lld\", &arr[i]);\n        qsort(arr, N, sizeof(long long), cmp);\n        long long total = (long long)N * (N - 1) / 2;\n        long long sub = 0;\n        long long cnt = 1;\n        for (int i = 1; i <= N; ++i) {\n            if (i < N && arr[i] == arr[i-1]) {\n                ++cnt;\n            } else {\n                sub += cnt * (cnt - 1) / 2;\n                cnt = 1;\n            }\n        }\n        printf(\"%lld\\n\", total - sub);\n        free(arr);\n    }\n    return 0;\n}\n",
      "C++": "#include <bits/stdc++.h>\nusing namespace std;\nint main(){\n    ios::sync_with_stdio(false);\n    cin.tie(nullptr);\n    int T; if(!(cin>>T)) return 0;\n    while(T--){\n        int N; cin>>N;\n        vector<long long> a(N);\n        for(int i=0;i<N;++i) cin>>a[i];\n        unordered_map<long long,long long> freq;\n        freq.reserve(N*2);\n        for(long long x: a) ++freq[x];\n        long long total = 1LL*N*(N-1)/2;\n        long long same = 0;\n        for(auto &p: freq){\n            long long c = p.second;\n            same += c*(c-1)/2;\n        }\n        cout << total - same << '\\n';\n    }\n    return 0;\n}\n",
      "Java": "import java.io.*;\nimport java.util.*;\n\npublic class Main {\n    public static void main(String[] args) throws Exception {\n        FastScanner fs = new FastScanner(System.in);\n        int T = fs.nextInt();\n        StringBuilder sb = new StringBuilder();\n        while (T-- > 0) {\n            int N = fs.nextInt();\n            long[] arr = new long[N];\n            for (int i = 0; i < N; i++) arr[i] = fs.nextLong();\n            HashMap<Long, Long> freq = new HashMap<>(N * 2);\n            for (long v : arr) freq.put(v, freq.getOrDefault(v, 0L) + 1);\n            long total = 1L * N * (N - 1) / 2;\n            long same = 0;\n            for (long c : freq.values()) same += c * (c - 1) / 2;\n            sb.append(total - same).append('\\n');\n        }\n        System.out.print(sb.toString());\n    }\n\n    // Fast scanner for large input\n    static class FastScanner {\n        private final BufferedInputStream in;\n        private final byte[] buffer = new byte[1 << 16];\n        private int ptr = 0, len = 0;\n        FastScanner(InputStream is) { in = new BufferedInputStream(is); }\n        private int readByte() throws IOException {\n            if (ptr >= len) {\n                len = in.read(buffer);\n                ptr = 0;\n                if (len <= 0) return -1;\n            }\n            return buffer[ptr++];\n        }\n        long nextLong() throws IOException { return Long.parseLong(next()); }\n        int nextInt() throws IOException { return Integer.parseInt(next()); }\n        String next() throws IOException {\n            StringBuilder sb = new StringBuilder();\n            int c;\n            while ((c = readByte()) != -1 && c <= ' ') {}\n            while (c != -1 && c > ' ') {\n                sb.append((char) c);\n                c = readByte();\n            }\n            return sb.toString();\n        }\n    }\n}\n",
      "Python3": "import sys\nfrom collections import Counter\n\ndef solve():\n    data = sys.stdin.read().strip().split()\n    if not data:\n        return\n    it = iter(data)\n    T = int(next(it))\n    out_lines = []\n    for _ in range(T):\n        N = int(next(it))\n        arr = [int(next(it)) for _ in range(N)]\n        freq = Counter(arr)\n        total = N * (N - 1) // 2\n        same = sum(c * (c - 1) // 2 for c in freq.values())\n        out_lines.append(str(total - same))\n    sys.stdout.write('\\n'.join(out_lines))\n\nif __name__ == \"__main__\":\n    solve()\n",
      "JavaScript": "const fs = require('fs');\nconst input = fs.readFileSync(0, 'utf-8').trim().split(/\\s+/);\nlet idx = 0;\nconst T = Number(input[idx++]);\nlet outputs = [];\nfor (let t = 0; t < T; ++t) {\n  const N = Number(input[idx++]);\n  const freq = new Map();\n  for (let i = 0; i < N; ++i) {\n    const val = Number(input[idx++]);\n    freq.set(val, (freq.get(val) || 0) + 1);\n  }\n  const total = N * (N - 1) / 2;\n  let same = 0;\n  for (const c of freq.values()) {\n    same += c * (c - 1) / 2;\n  }\n  outputs.push(String(total - same));\n}\nconsole.log(outputs.join('\\n'));\n"
    }
  },
  "QBCategory": "CCD",
  "boilerPlates": {
    "C": "#include <stdio.h>\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "C++": "#include <iostream>\nusing namespace std;\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "Java": "import java.util.*;\nimport java.io.*;\n\npublic class Main {\n    public static void main(String[] args) throws IOException {\n        // your code goes here\n    }\n}",
    "Python3": "# your code goes here\n",
    "JavaScript": "// your code goes here\n"
  },
  "_internal": {
    "methodName": "",
    "params": []
  }
}