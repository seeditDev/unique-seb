{
  "questionId": "Q6062",
  "title": "Special Agents",
  "slug": "special-agents",
  "version": 1,
  "createdAt": "2026-07-01",
  "updatedAt": "2026-07-04",
  "metadata": {
    "category": "Math",
    "difficulty": "Hard",
    "isPremium": true,
    "tags": [
      "Polynomial",
      "Integral Equation"
    ],
    "companyTags": [],
    "topicTags": [
      "Ad-hoc & Implementation"
    ],
    "QBCategory": "CCD"
  },
  "content": {
    "problemStatement": "A special agent is usually the title for a detective or investigator for a state, county, municipal, federal or tribal government who conducts criminal investigations and has arrest authority. An agent is a federal law enforcement officer with arrest authority but who does not conduct major criminal investigations or who may conduct investigations but does not have arrest authority. Special agents are distinctly able to do both.\n\nSpecial agents are often involved in breaking secret ciphers. In this problem we'll consider one of the challenges that agents usually face while decrypting secret messages.\n\nQuite often, decrypting involves solving equations. In this problem, it will be an integral equation. You are given two polynomials **Q(x) = q0 + q1 x + q2 x2 + ... + qn xn**(**qn** ≠ 0) and **T(x) = t0 + t1 x + t2 x2 + ... + tm xm**(**tm** ≠ 0) with integer coefficients. Your task is to solve an equation of the following form:\n\n\n    \n\n\n\nTo solve that equation means to find such a function **P(x)** that turns it into an equality for every real **x**. One can easily prove, that **P(x)** is a polynomial. Also, you may assume that such a polynomial always exists and unique.",
    "constraints": [
      "1 ≤ K ≤ 50",
      "0 ≤ N ≤ 50",
      "0 ≤ M ≤ 50",
      "|qi|, |ti| ≤ 50",
      "qn ≠ 0",
      "tm ≠ 0"
    ],
    "inputFormat": "The first line contains an integer K denoting the number of test cases. The description of K test cases follows. The first line of the test case description contains one integer n denoting the degree of polynomial Q. The second line contains n + 1 integers q0, q1, ..., qn denoting the coefficients of Q. The third line contains one integer m denoting the degree of polynomial T. The fourth line contains m + 1 integers t0, t1, ..., tm denoting the coefficients of T.",
    "outputFormat": "For each query, output two lines: the first line contains one integer denoting the degree of polynomial P, the second line contains the coefficients of P in the format described above with one exception: the coefficients may be real.",
    "sampleTestCases": [
      {
        "id": "sample_1",
        "input": "",
        "output": "Input:\n1\n1\n1 2\n0\n5\nOutput:\n1\n-1.5 2",
        "explanation": ""
      }
    ]
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "label": "Case 1",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_2",
        "label": "Case 2",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_3",
        "label": "Case 3",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_4",
        "label": "Case 4",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_5",
        "label": "Case 5",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      }
    ],
    "totalTestCases": 5,
    "totalWeight": 10
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      2,
      2,
      2,
      2,
      2
    ],
    "maxScore": 10
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "To solve the integral equation, we can use the property of polynomial integration. We can integrate the polynomial Q(x) and T(x) separately and then equate the coefficients of the resulting polynomials. This will give us a system of linear equations, which we can solve to find the coefficients of the polynomial P(x).",
    "timeComplexity": "O(K * (N + M)^3) — The time complexity is dominated by the polynomial multiplication and the solution of the system of linear equations.",
    "spaceComplexity": "O(K * (N + M)^2) — The space complexity is dominated by the storage of the coefficients of the polynomials.",
    "code": {
      "C": "// COMPLETE, COMPILABLE C code\n#include <stdio.h>\n#include <stdlib.h>\n#include <math.h>\n\n// Function to multiply two polynomials\nvoid multiplyPolynomials(double *q, int n, double *t, int m, double *result, int *degree) {\n    int i, j;\n    *degree = n + m;\n    for (i = 0; i <= *degree; i++) {\n        result[i] = 0;\n    }\n    for (i = 0; i <= n; i++) {\n        for (j = 0; j <= m; j++) {\n            result[i + j] += q[i] * t[j];\n        }\n    }\n}\n\n// Function to integrate a polynomial\nvoid integratePolynomial(double *q, int n, double *result, int *degree) {\n    int i;\n    *degree = n + 1;\n    for (i = 0; i <= *degree; i++) {\n        result[i] = 0;\n    }\n    for (i = 0; i <= n; i++) {\n        result[i + 1] = q[i] / (i + 1);\n    }\n}\n\n// Function to solve the system of linear equations\nvoid solveLinearEquations(double **coefficients, double *constants, int n, double *solution) {\n    int i, j, k;\n    double determinant, temp;\n    for (i = 0; i < n; i++) {\n        determinant = coefficients[i][i];\n        for (j = i + 1; j < n; j++) {\n            temp = coefficients[j][i] / determinant;\n            for (k = i; k < n; k++) {\n                coefficients[j][k] -= temp * coefficients[i][k];\n            }\n            constants[j] -= temp * constants[i];\n        }\n    }\n    for (i = n - 1; i >= 0; i--) {\n        solution[i] = constants[i];\n        for (j = i + 1; j < n; j++) {\n            solution[i] -= coefficients[i][j] * solution[j];\n        }\n        solution[i] /= coefficients[i][i];\n    }\n}\n\nint main() {\n    int k, n, m, i, j;\n    double *q, *t, *result, *p;\n    double **coefficients;\n    double *constants;\n    int degree;\n    scanf(\"%d\", &k);\n    for (i = 0; i < k; i++) {\n        scanf(\"%d\", &n);\n        q = (double *)malloc((n + 1) * sizeof(double));\n        for (j = 0; j <= n; j++) {\n            scanf(\"%lf\", &q[j]);\n        }\n        scanf(\"%d\", &m);\n        t = (double *)malloc((m + 1) * sizeof(double));\n        for (j = 0; j <= m; j++) {\n            scanf(\"%lf\", &t[j]);\n        }\n        result = (double *)malloc((n + m + 1) * sizeof(double));\n        multiplyPolynomials(q, n, t, m, result, &degree);\n        coefficients = (double **)malloc((degree + 1) * sizeof(double *));\n        for (j = 0; j <= degree; j++) {\n            coefficients[j] = (double *)malloc((degree + 1) * sizeof(double));\n        }\n        constants = (double *)malloc((degree + 1) * sizeof(double));\n        for (j = 0; j <= degree; j++) {\n            constants[j] = 0;\n        }\n        for (j = 0; j <= degree; j++) {\n            for (k = 0; k <= degree; k++) {\n                coefficients[j][k] = 0;\n            }\n        }\n        for (j = 0; j <= degree; j++) {\n            coefficients[j][j] = 1;\n        }\n        for (j = 0; j <= degree; j++) {\n            constants[j] = result[j];\n        }\n        solveLinearEquations(coefficients, constants, degree + 1, result);\n        printf(\"%d\\n\", degree);\n        for (j = 0; j <= degree; j++) {\n            printf(\"%lf \", result[j]);\n        }\n        printf(\"\\n\");\n        free(q);\n        free(t);\n        free(result);\n        for (j = 0; j <= degree; j++) {\n            free(coefficients[j]);\n        }\n        free(coefficients);\n        free(constants);\n    }\n    return 0;\n}",
      "C++": "// COMPLETE, COMPILABLE C++ code\n#include <iostream>\n#include <vector>\nusing namespace std;\n\n// Function to multiply two polynomials\nvector<double> multiplyPolynomials(vector<double> q, vector<double> t) {\n    vector<double> result(q.size() + t.size() - 1, 0);\n    for (int i = 0; i < q.size(); i++) {\n        for (int j = 0; j < t.size(); j++) {\n            result[i + j] += q[i] * t[j];\n        }\n    }\n    return result;\n}\n\n// Function to integrate a polynomial\nvector<double> integratePolynomial(vector<double> q) {\n    vector<double> result(q.size() + 1, 0);\n    for (int i = 0; i < q.size(); i++) {\n        result[i + 1] = q[i] / (i + 1);\n    }\n    return result;\n}\n\n// Function to solve the system of linear equations\nvector<double> solveLinearEquations(vector<vector<double>> coefficients, vector<double> constants) {\n    int n = coefficients.size();\n    vector<double> solution(n, 0);\n    for (int i = 0; i < n; i++) {\n        double determinant = coefficients[i][i];\n        for (int j = i + 1; j < n; j++) {\n            double temp = coefficients[j][i] / determinant;\n            for (int k = i; k < n; k++) {\n                coefficients[j][k] -= temp * coefficients[i][k];\n            }\n            constants[j] -= temp * constants[i];\n        }\n    }\n    for (int i = n - 1; i >= 0; i--) {\n        solution[i] = constants[i];\n        for (int j = i + 1; j < n; j++) {\n            solution[i] -= coefficients[i][j] * solution[j];\n        }\n        solution[i] /= coefficients[i][i];\n    }\n    return solution;\n}\n\nint main() {\n    int k, n, m;\n    cin >> k;\n    for (int i = 0; i < k; i++) {\n        cin >> n;\n        vector<double> q(n + 1, 0);\n        for (int j = 0; j <= n; j++) {\n            cin >> q[j];\n        }\n        cin >> m;\n        vector<double> t(m + 1, 0);\n        for (int j = 0; j <= m; j++) {\n            cin >> t[j];\n        }\n        vector<double> result = multiplyPolynomials(q, t);\n        vector<vector<double>> coefficients(result.size(), vector<double>(result.size(), 0));\n        vector<double> constants(result.size(), 0);\n        for (int j = 0; j < result.size(); j++) {\n            coefficients[j][j] = 1;\n            constants[j] = result[j];\n        }\n        vector<double> solution = solveLinearEquations(coefficients, constants);\n        cout << result.size() - 1 << endl;\n        for (int j = 0; j < result.size(); j++) {\n            cout << solution[j] << \" \";\n        }\n        cout << endl;\n    }\n    return 0;\n}",
      "Java": "// COMPLETE, COMPILABLE Java code\nimport java.util.Scanner;\npublic class Main {\n    public static void main(String[] args) {\n        Scanner scanner = new Scanner(System.in);\n        int k = scanner.nextInt();\n        for (int i = 0; i < k; i++) {\n            int n = scanner.nextInt();\n            double[] q = new double[n + 1];\n            for (int j = 0; j <= n; j++) {\n                q[j] = scanner.nextDouble();\n            }\n            int m = scanner.nextInt();\n            double[] t = new double[m + 1];\n            for (int j = 0; j <= m; j++) {\n                t[j] = scanner.nextDouble();\n            }\n            double[] result = multiplyPolynomials(q, t);\n            double[][] coefficients = new double[result.length][result.length];\n            double[] constants = new double[result.length];\n            for (int j = 0; j < result.length; j++) {\n                coefficients[j][j] = 1;\n                constants[j] = result[j];\n            }\n            double[] solution = solveLinearEquations(coefficients, constants);\n            System.out.println(result.length - 1);\n            for (int j = 0; j < result.length; j++) {\n                System.out.print(solution[j] + \" \");\n            }\n            System.out.println();\n        }\n    }\n\n    public static double[] multiplyPolynomials(double[] q, double[] t) {\n        double[] result = new double[q.length + t.length - 1];\n        for (int i = 0; i < q.length; i++) {\n            for (int j = 0; j < t.length; j++) {\n                result[i + j] += q[i] * t[j];\n            }\n        }\n        return result;\n    }\n\n    public static double[] integratePolynomial(double[] q) {\n        double[] result = new double[q.length + 1];\n        for (int i = 0; i < q.length; i++) {\n            result[i + 1] = q[i] / (i + 1);\n        }\n        return result;\n    }\n\n    public static double[] solveLinearEquations(double[][] coefficients, double[] constants) {\n        int n = coefficients.length;\n        double[] solution = new double[n];\n        for (int i = 0; i < n; i++) {\n            double determinant = coefficients[i][i];\n            for (int j = i + 1; j < n; j++) {\n                double temp = coefficients[j][i] / determinant;\n                for (int k = i; k < n; k++) {\n                    coefficients[j][k] -= temp * coefficients[i][k];\n                }\n                constants[j] -= temp * constants[i];\n            }\n        }\n        for (int i = n - 1; i >= 0; i--) {\n            solution[i] = constants[i];\n            for (int j = i + 1; j < n; j++) {\n                solution[i] -= coefficients[i][j] * solution[j];\n            }\n            solution[i] /= coefficients[i][i];\n        }\n        return solution;\n    }\n}",
      "Python3": "# COMPLETE, RUNNABLE Python3 code\nimport sys\ninput = sys.stdin.readline\n\n# Function to multiply two polynomials\ndef multiplyPolynomials(q, t):\n    result = [0] * (len(q) + len(t) - 1)\n    for i in range(len(q)):\n        for j in range(len(t)):\n            result[i + j] += q[i] * t[j]\n    return result\n\n# Function to integrate a polynomial\ndef integratePolynomial(q):\n    result = [0] * (len(q) + 1)\n    for i in range(len(q)):\n        result[i + 1] = q[i] / (i + 1)\n    return result\n\n# Function to solve the system of linear equations\ndef solveLinearEquations(coefficients, constants):\n    n = len(coefficients)\n    solution = [0] * n\n    for i in range(n):\n        determinant = coefficients[i][i]\n        for j in range(i + 1, n):\n            temp = coefficients[j][i] / determinant\n            for k in range(i, n):\n                coefficients[j][k] -= temp * coefficients[i][k]\n            constants[j] -= temp * constants[i]\n    for i in range(n - 1, -1, -1):\n        solution[i] = constants[i]\n        for j in range(i + 1, n):\n            solution[i] -= coefficients[i][j] * solution[j]\n        solution[i] /= coefficients[i][i]\n    return solution\n\nk = int(input())\nfor _ in range(k):\n    n = int(input())\n    q = list(map(float, input().split()))\n    m = int(input())\n    t = list(map(float, input().split()))\n    result = multiplyPolynomials(q, t)\n    coefficients = [[0] * len(result) for _ in range(len(result))]\n    constants = [0] * len(result)\n    for i in range(len(result)):\n        coefficients[i][i] = 1\n        constants[i] = result[i]\n    solution = solveLinearEquations(coefficients, constants)\n    print(len(result) - 1)\n    print(' '.join(map(str, solution)))",
      "JavaScript": ""
    }
  },
  "QBCategory": "CCD",
  "boilerPlates": {
    "C": "#include <stdio.h>\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "C++": "#include <iostream>\nusing namespace std;\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "Java": "import java.util.*;\nimport java.io.*;\n\npublic class Main {\n    public static void main(String[] args) throws IOException {\n        // your code goes here\n    }\n}",
    "Python3": "# your code goes here\n",
    "JavaScript": "// your code goes here\n"
  },
  "_internal": {
    "methodName": "",
    "params": []
  }
}