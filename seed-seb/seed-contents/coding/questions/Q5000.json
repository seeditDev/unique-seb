{
  "questionId": "Q5000",
  "title": "URL Shortener - Final Touch",
  "slug": "url-shortener-final-touch",
  "version": 1,
  "createdAt": "2026-07-01",
  "updatedAt": "2026-07-04",
  "metadata": {
    "category": "Data Structures",
    "difficulty": "Easy",
    "isPremium": true,
    "tags": [
      "Hash Map",
      "String Manipulation",
      "Base Conversion"
    ],
    "companyTags": [],
    "topicTags": [
      "Python Standard Library"
    ],
    "QBCategory": "CCD"
  },
  "content": {
    "problemStatement": "Great! You have successfully built a **URL Shortener** using Python. 🎉\n\nHere are some improvements you can try:\n\n\n**Store URLs Persistently** – Save shortened URLs in a database or a file to retain them after restarting the program.\n**Implement API &amp; Server** – Use Flask or FastAPI to create a web-based URL shortener service.\n**Analytics &amp; Tracking** – Track the number of times a short URL has been accessed.\n**Web Interface** – Build a simple frontend using Flask or Django for a user-friendly experience.\n**Final Updates:**\nEnsure both **shortening** and **retrieval** functions work properly.\nHandle edge cases like duplicate URLs and invalid inputs.\nImprove user experience with clear messages and validations.\nTry implementing these improvements to make your URL Shortener more efficient and user-friendly! 🚀",
    "constraints": [
      "1 ≤ T ≤ 10",
      "1 ≤ Q ≤ 1000",
      "All URLs contain only printable ASCII characters except whitespace and have length ≤ 100.",
      "Short codes consist of characters 0‑9, a‑z, A‑Z and have length ≤ 6."
    ],
    "inputFormat": "The first line contains an integer T (1 ≤ T ≤ 10), the number of test cases. Each test case starts with an integer Q (1 ≤ Q ≤ 1000), the number of queries. The following Q lines each contain a query in one of two formats:\n1. SHORTEN <url>   – shorten the given URL (the URL is a non‑empty string without spaces, length ≤ 100).\n2. GET <code>      – retrieve the original URL for the given short code (the code consists of alphanumeric characters, length ≤ 6).",
    "outputFormat": "For each query, output a single line:\n- For a SHORTEN query, output the generated short code. If the URL has been shortened before, output the previously generated code.\n- For a GET query, output the original URL if the code exists, otherwise output the word \"error\".",
    "sampleTestCases": [
      {
        "id": "sample_1",
        "input": "7\nSHORTEN https://example.com\nSHORTEN https://openai.com\nGET 1\nGET 2\nSHORTEN https://example.com\nGET 1\nGET 2",
        "output": "1\n2\nerror\nerror\n1\nhttps://example.com\nhttps://openai.com",
        "explanation": "The first SHORTEN creates code \"1\" for https://example.com. The second creates \"2\" for https://openai.com. GET 1 and GET 2 are asked before any GET, but codes \"1\" and \"2\" exist, so they return the URLs. The third SHORTEN is a duplicate URL, so it reuses code \"1\". The final GET queries retrieve the stored URLs."
      },
      {
        "id": "sample_2",
        "input": "5\nGET abc\nSHORTEN https://test.org\nGET 1\nSHORTEN https://test.org\nGET 1",
        "output": "error\n1\nhttps://test.org\n1\nhttps://test.org",
        "explanation": "GET abc fails because no such code exists. The first SHORTEN generates code \"1\". GET 1 now returns the stored URL. The second SHORTEN is a duplicate, so it returns the same code \"1\". The final GET again returns the URL."
      },
      {
        "id": "sample_3",
        "input": "4\nSHORTEN a\nSHORTEN b\nSHORTEN c\nGET 3",
        "output": "1\n2\n3\nc",
        "explanation": "Three distinct URLs receive sequential codes 1, 2 and 3. GET 3 retrieves the third URL \"c\"."
      }
    ]
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "label": "Case 1",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_2",
        "label": "Case 2",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_3",
        "label": "Case 3",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_4",
        "label": "Case 4",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_5",
        "label": "Case 5",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      }
    ],
    "totalTestCases": 5,
    "totalWeight": 10
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      2,
      2,
      2,
      2,
      2
    ],
    "maxScore": 10
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "We maintain two hash maps:\n- url2code: maps a full URL to its short code.\n- code2url: maps a short code back to the original URL.\nWhen a SHORTEN query arrives we check url2code. If the URL already exists we reuse its code; otherwise we generate a new code by converting an increasing integer id to base‑62 (characters 0‑9, a‑z, A‑Z). The new mapping is stored in both hash maps.\nFor a GET query we look up the code in code2url; if it is present we output the stored URL, otherwise we output \"error\".\nBoth operations are O(1) average time using hash tables. The total time per test case is O(Q) and the memory consumption is O(number of distinct URLs).",
    "timeComplexity": "O(Q) per test case (each query is O(1) average).",
    "spaceComplexity": "O(N) where N is the number of distinct URLs stored (at most Q).",
    "code": {
      "C": "#include <stdio.h>\n#include <stdlib.h>\n#include <string.h>\n\n#define MAXURL 105\n#define BASE 62\nconst char charset[] = \"0123456789abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ\";\n\nvoid encode(long long num, char *out) {\n    char buf[20];\n    int idx = 0;\n    if (num == 0) {\n        buf[idx++] = '0';\n    } else {\n        while (num > 0) {\n            buf[idx++] = charset[num % BASE];\n            num /= BASE;\n        }\n    }\n    for (int i = 0; i < idx; ++i) out[i] = buf[idx - i - 1];\n    out[idx] = '\\0';\n}\n\ntypedef struct { char key[MAXURL]; char val[10]; int used; } Pair;\n\nunsigned long hash_str(const char *s) {\n    unsigned long h = 5381;\n    while (*s) h = ((h << 5) + h) + (unsigned char)*s++;\n    return h;\n}\n\nint main() {\n    int T; if (scanf(\"%d\", &T) != 1) return 0;\n    while (T--) {\n        int Q; if (scanf(\"%d\", &Q) != 1) return 0;\n        int size = 1; while (size < Q * 4) size <<= 1;\n        Pair *url2code = calloc(size, sizeof(Pair));\n        Pair *code2url = calloc(size, sizeof(Pair));\n        long long nextId = 1;\n        char op[10], arg[MAXURL];\n        for (int i = 0; i < Q; ++i) {\n            if (scanf(\"%s %s\", op, arg) != 2) break;\n            if (strcmp(op, \"SHORTEN\") == 0) {\n                unsigned long h = hash_str(arg) & (size - 1);\n                while (url2code[h].used && strcmp(url2code[h].key, arg) != 0) h = (h + 1) & (size - 1);\n                if (url2code[h].used) {\n                    printf(\"%s\\n\", url2code[h].val);\n                } else {\n                    char code[10]; encode(nextId++, code);\n                    strcpy(url2code[h].key, arg);\n                    strcpy(url2code[h].val, code);\n                    url2code[h].used = 1;\n                    unsigned long hc = hash_str(code) & (size - 1);\n                    while (code2url[hc].used) hc = (hc + 1) & (size - 1);\n                    strcpy(code2url[hc].key, code);\n                    strcpy(code2url[hc].val, arg);\n                    code2url[hc].used = 1;\n                    printf(\"%s\\n\", code);\n                }\n            } else { // GET\n                unsigned long h = hash_str(arg) & (size - 1);\n                while (code2url[h].used && strcmp(code2url[h].key, arg) != 0) h = (h + 1) & (size - 1);\n                if (code2url[h].used) {\n                    printf(\"%s\\n\", code2url[h].val);\n                } else {\n                    printf(\"error\\n\");\n                }\n            }\n        }\n        free(url2code);\n        free(code2url);\n    }\n    return 0;\n}",
      "C++": "#include <bits/stdc++.h>\nusing namespace std;\n\nconst string charset = \"0123456789abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ\";\nstring encode(long long num) {\n    if (num == 0) return \"0\";\n    string s;\n    while (num > 0) {\n        s.push_back(charset[num % 62]);\n        num /= 62;\n    }\n    reverse(s.begin(), s.end());\n    return s;\n}\n\nint main() {\n    ios::sync_with_stdio(false);\n    cin.tie(nullptr);\n    int T; if(!(cin >> T)) return 0;\n    while (T--) {\n        int Q; cin >> Q;\n        unordered_map<string,string> url2code, code2url;\n        long long nextId = 1;\n        for (int i = 0; i < Q; ++i) {\n            string op, arg; cin >> op >> arg;\n            if (op == \"SHORTEN\") {\n                auto it = url2code.find(arg);\n                if (it != url2code.end()) {\n                    cout << it->second << '\\n';\n                } else {\n                    string code = encode(nextId++);\n                    url2code[arg] = code;\n                    code2url[code] = arg;\n                    cout << code << '\\n';\n                }\n            } else { // GET\n                auto it = code2url.find(arg);\n                if (it != code2url.end()) cout << it->second << '\\n';\n                else cout << \"error\" << '\\n';\n            }\n        }\n    }\n    return 0;\n}",
      "Java": "import java.io.*;\nimport java.util.*;\n\npublic class Main {\n    private static final char[] CHARSET = \"0123456789abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ\".toCharArray();\n    private static String encode(long num) {\n        if (num == 0) return \"0\";\n        StringBuilder sb = new StringBuilder();\n        while (num > 0) {\n            sb.append(CHARSET[(int)(num % 62)]);\n            num /= 62;\n        }\n        return sb.reverse().toString();\n    }\n    public static void main(String[] args) throws Exception {\n        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));\n        String line = br.readLine();\n        if (line == null) return;\n        int T = Integer.parseInt(line.trim());\n        StringBuilder out = new StringBuilder();\n        for (int tc = 0; tc < T; ++tc) {\n            int Q = Integer.parseInt(br.readLine().trim());\n            Map<String, String> url2code = new HashMap<>();\n            Map<String, String> code2url = new HashMap<>();\n            long nextId = 1;\n            for (int i = 0; i < Q; ++i) {\n                StringTokenizer st = new StringTokenizer(br.readLine());\n                String op = st.nextToken();\n                String arg = st.nextToken();\n                if (op.equals(\"SHORTEN\")) {\n                    String code = url2code.get(arg);\n                    if (code == null) {\n                        code = encode(nextId++);\n                        url2code.put(arg, code);\n                        code2url.put(code, arg);\n                    }\n                    out.append(code).append('\\n');\n                } else { // GET\n                    String url = code2url.get(arg);\n                    out.append(url == null ? \"error\" : url).append('\\n');\n                }\n            }\n        }\n        System.out.print(out.toString());\n    }\n}",
      "Python3": "#!/usr/bin/env python3\nimport sys\n\ncharset = \"0123456789abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ\"\n\ndef encode(num: int) -> str:\n    if num == 0:\n        return \"0\"\n    s = []\n    while num:\n        s.append(charset[num % 62])\n        num //= 62\n    return ''.join(reversed(s))\n\ndef main():\n    data = sys.stdin.read().splitlines()\n    if not data:\n        return\n    idx = 0\n    T = int(data[idx].strip()); idx += 1\n    out_lines = []\n    for _ in range(T):\n        Q = int(data[idx].strip()); idx += 1\n        url2code = {}\n        code2url = {}\n        nxt = 1\n        for _ in range(Q):\n            op, arg = data[idx].split(' ', 1)\n            idx += 1\n            if op == 'SHORTEN':\n                if arg in url2code:\n                    out_lines.append(url2code[arg])\n                else:\n                    code = encode(nxt)\n                    nxt += 1\n                    url2code[arg] = code\n                    code2url[code] = arg\n                    out_lines.append(code)\n            else:  # GET\n                out_lines.append(code2url.get(arg, 'error'))\n    sys.stdout.write('\\n'.join(out_lines))\n\nif __name__ == \"__main__\":\n    main()\n",
      "JavaScript": "const fs = require('fs');\nconst input = fs.readFileSync(0, 'utf-8').trim().split('\\n');\nif (input.length === 0) process.exit(0);\nlet ptr = 0;\nconst T = parseInt(input[ptr++], 10);\nconst charset = '0123456789abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ';\nfunction encode(num) {\n  if (num === 0) return '0';\n  let s = '';\n  while (num > 0) {\n    s = charset[num % 62] + s;\n    num = Math.floor(num / 62);\n  }\n  return s;\n}\nlet output = [];\nfor (let tc = 0; tc < T; ++tc) {\n  const Q = parseInt(input[ptr++], 10);\n  const url2code = new Map();\n  const code2url = new Map();\n  let nextId = 1;\n  for (let i = 0; i < Q; ++i) {\n    const [op, arg] = input[ptr++].split(' ');\n    if (op === 'SHORTEN') {\n      if (url2code.has(arg)) {\n        output.push(url2code.get(arg));\n      } else {\n        const code = encode(nextId++);\n        url2code.set(arg, code);\n        code2url.set(code, arg);\n        output.push(code);\n      }\n    } else { // GET\n      output.push(code2url.get(arg) || 'error');\n    }\n  }\n}\nconsole.log(output.join('\\n'));\n"
    }
  },
  "QBCategory": "CCD",
  "boilerPlates": {
    "C": "#include <stdio.h>\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "C++": "#include <iostream>\nusing namespace std;\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "Java": "import java.util.*;\nimport java.io.*;\n\npublic class Main {\n    public static void main(String[] args) throws IOException {\n        // your code goes here\n    }\n}",
    "Python3": "# your code goes here\n",
    "JavaScript": "// your code goes here\n"
  },
  "_internal": {
    "methodName": "",
    "params": []
  }
}