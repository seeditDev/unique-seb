{
  "questionId": "Q11807",
  "title": "Course Schedule II",
  "slug": "course-schedule-ii",
  "version": 1,
  "createdAt": "2026-07-05",
  "updatedAt": "2026-07-05",
  "metadata": {
    "category": "Graph",
    "difficulty": "Medium",
    "isPremium": true,
    "tags": [
      "Topological Sorting",
      "BFS",
      "Graph Theory"
    ],
    "companyTags": [],
    "topicTags": [],
    "QBCategory": "CCD"
  },
  "content": {
    "problemStatement": "**Problem Statement I (Course Schedule):** There are a total of n tasks you have to pick, labeled from 0 to n-1. Some tasks may have prerequisites tasks, for example, to pick task 0 you have to first finish tasks 1, which is expressed as a pair: [0, 1]\n\n\n\n\n\nGiven the total number of n tasks and a list of prerequisite pairs of size m. Find the order of tasks you should pick to finish all tasks.\n\n\n\n\n\nNote: There may be multiple correct orders, you need to return one of them. If it is impossible to finish all tasks, return an empty array.\n\n\n\n\n\n**Problem Statement II (Pre-requisite Tasks): **There are",
    "constraints": [
      "1 <= numCourses <= 200",
      "0 <= prerequisites.length <= numCourses * (numCourses - 1) / 2",
      "prerequisites[i].length == 2",
      "0 <= prerequisites[i][0], prerequisites[i][1] < numCourses",
      "prerequisites[i][0] != prerequisites[i][1]"
    ],
    "inputFormat": "The input will contain two parameters: numCourses (the total number of tasks) and prerequisites (a list of pairs where the first element in the pair is the task and the second element is its prerequisite).",
    "outputFormat": "The output should be an array representing one possible order of tasks to finish all tasks. If it is impossible to finish all tasks, return an empty array.",
    "sampleTestCases": []
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "label": "min boundary",
        "input": {
          "numCourses": 1,
          "prerequisites": []
        },
        "expectedOutput": [
          0
        ],
        "weight": 2
      },
      {
        "id": "tc_2",
        "label": "max boundary",
        "input": {
          "numCourses": 200,
          "prerequisites": [
            [
              1,
              0
            ],
            [
              2,
              0
            ],
            [
              3,
              1
            ],
            [
              3,
              2
            ]
          ]
        },
        "expectedOutput": [
          0,
          1,
          2,
          3
        ],
        "weight": 2
      },
      {
        "id": "tc_3",
        "label": "uniform/edge cases",
        "input": {
          "numCourses": 2,
          "prerequisites": [
            [
              1,
              0
            ]
          ]
        },
        "expectedOutput": [
          0,
          1
        ],
        "weight": 2
      },
      {
        "id": "tc_4",
        "label": "tricky case / empty-like",
        "input": {
          "numCourses": 4,
          "prerequisites": [
            [
              1,
              0
            ],
            [
              2,
              0
            ],
            [
              3,
              1
            ],
            [
              3,
              2
            ]
          ]
        },
        "expectedOutput": [
          0,
          1,
          2,
          3
        ],
        "weight": 2
      },
      {
        "id": "tc_5",
        "label": "general mixed case",
        "input": {
          "numCourses": 5,
          "prerequisites": [
            [
              1,
              4
            ],
            [
              2,
              4
            ],
            [
              3,
              1
            ],
            [
              3,
              2
            ]
          ]
        },
        "expectedOutput": [
          4,
          1,
          2,
          3,
          0
        ],
        "weight": 2
      }
    ],
    "totalTestCases": 5,
    "totalWeight": 10
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      2,
      2,
      2,
      2,
      2
    ],
    "maxScore": 10
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "To solve this problem, we can use topological sorting with BFS. First, we build the adjacency list and in-degree array. Then, we initialize a queue with nodes having an in-degree of 0. We process the queue, reducing the in-degree of neighbors and adding them to the queue if their in-degree becomes 0. Finally, we return the processed nodes as the order of tasks.",
    "timeComplexity": "O(n + m)",
    "spaceComplexity": "O(n + m)",
    "code": {
      "C": "#include <stdio.h>\n#include <stdlib.h>\n\n// Structure for a graph node\ntypedef struct Node {\n    int data;\n    struct Node* next;\n} Node;\n\n// Function to create a new node\nNode* createNode(int data) {\n    Node* newNode = (Node*) malloc(sizeof(Node));\n    if (!newNode) {\n        printf(\"Memory error\\n\");\n        return NULL;\n    }\n    newNode->data = data;\n    newNode->next = NULL;\n    return newNode;\n}\n\n// Function to add an edge to the graph\nvoid addEdge(Node** adj, int src, int dest, int numCourses) {\n    Node* newNode = createNode(dest);\n    if (adj[src] == NULL) {\n        adj[src] = newNode;\n    } else {\n        Node* lastNode = adj[src];\n        while (lastNode->next) {\n            lastNode = lastNode->next;\n        }\n        lastNode->next = newNode;\n    }\n}\n\n// Function to perform topological sorting using BFS\nint* findOrder(int numCourses, int** prerequisites, int prerequisitesSize, int* returnSize) {\n    Node** adj = (Node**) malloc(numCourses * sizeof(Node*));\n    int* inDegree = (int*) malloc(numCourses * sizeof(int));\n    int* order = (int*) malloc(numCourses * sizeof(int));\n    *returnSize = 0;\n\n    // Initialize adjacency list and in-degree array\n    for (int i = 0; i < numCourses; i++) {\n        adj[i] = NULL;\n        inDegree[i] = 0;\n    }\n\n    // Build adjacency list and in-degree array\n    for (int i = 0; i < prerequisitesSize; i++) {\n        addEdge(adj, prerequisites[i][1], prerequisites[i][0], numCourses);\n        inDegree[prerequisites[i][0]]++;\n    }\n\n    // Initialize queue with nodes having an in-degree of 0\n    int queue[numCourses], front = 0, rear = -1;\n    for (int i = 0; i < numCourses; i++) {\n        if (inDegree[i] == 0) {\n            queue[++rear] = i;\n        }\n    }\n\n    // Process queue\n    while (front <= rear) {\n        int node = queue[front++];\n        order[(*returnSize)++] = node;\n\n        Node* temp = adj[node];\n        while (temp) {\n            inDegree[temp->data]--;\n            if (inDegree[temp->data] == 0) {\n                queue[++rear] = temp->data;\n            }\n            temp = temp->next;\n        }\n    }\n\n    // Check if all nodes are processed\n    if (*returnSize != numCourses) {\n        *returnSize = 0;\n    }\n\n    return order;\n}\n",
      "C++": "#include <bits/stdc++.h>\nusing namespace std;\n\nclass Solution {\npublic:\n    vector<int> findOrder(int numCourses, vector<vector<int>>& prerequisites) {\n        vector<vector<int>> adj(numCourses);\n        vector<int> inDegree(numCourses, 0);\n        vector<int> order;\n\n        // Build adjacency list and in-degree array\n        for (auto& pre : prerequisites) {\n            adj[pre[1]].push_back(pre[0]);\n            inDegree[pre[0]]++;\n        }\n\n        // Initialize queue with nodes having an in-degree of 0\n        queue<int> q;\n        for (int i = 0; i < numCourses; i++) {\n            if (inDegree[i] == 0) {\n                q.push(i);\n            }\n        }\n\n        // Process queue\n        while (!q.empty()) {\n            int node = q.front();\n            q.pop();\n            order.push_back(node);\n\n            for (int nei : adj[node]) {\n                inDegree[nei]--;\n                if (inDegree[nei] == 0) {\n                    q.push(nei);\n                }\n            }\n        }\n\n        // Check if all nodes are processed\n        if (order.size() != numCourses) {\n            order.clear();\n        }\n\n        return order;\n    }\n};\n",
      "Java": "import java.util.*;\n\nclass Solution {\n    public int[] findOrder(int numCourses, int[][] prerequisites) {\n        List<List<Integer>> adj = new ArrayList<>();\n        int[] inDegree = new int[numCourses];\n        int[] order = new int[numCourses];\n        int index = 0;\n\n        // Initialize adjacency list\n        for (int i = 0; i < numCourses; i++) {\n            adj.add(new ArrayList<>());\n        }\n\n        // Build adjacency list and in-degree array\n        for (int[] pre : prerequisites) {\n            adj.get(pre[1]).add(pre[0]);\n            inDegree[pre[0]]++;\n        }\n\n        // Initialize queue with nodes having an in-degree of 0\n        Queue<Integer> q = new LinkedList<>();\n        for (int i = 0; i < numCourses; i++) {\n            if (inDegree[i] == 0) {\n                q.offer(i);\n            }\n        }\n\n        // Process queue\n        while (!q.isEmpty()) {\n            int node = q.poll();\n            order[index++] = node;\n\n            for (int nei : adj.get(node)) {\n                inDegree[nei]--;\n                if (inDegree[nei] == 0) {\n                    q.offer(nei);\n                }\n            }\n        }\n\n        // Check if all nodes are processed\n        if (index != numCourses) {\n            return new int[0];\n        }\n\n        return order;\n    }\n}\n",
      "Python3": "from collections import deque\n\nclass Solution:\n    def findOrder(self, numCourses, prerequisites):\n        adj = [[] for _ in range(numCourses)]\n        inDegree = [0] * numCourses\n        order = []\n\n        # Build adjacency list and in-degree array\n        for a, b in prerequisites:\n            adj[b].append(a)\n            inDegree[a] += 1\n\n        # Initialize queue with nodes having an in-degree of 0\n        q = deque([i for i in range(numCourses) if inDegree[i] == 0])\n\n        # Process queue\n        while q:\n            node = q.popleft()\n            order.append(node)\n\n            for nei in adj[node]:\n                inDegree[nei] -= 1\n                if inDegree[nei] == 0:\n                    q.append(nei)\n\n        # Check if all nodes are processed\n        if len(order) != numCourses:\n            return []\n\n        return order\n",
      "JavaScript": "class Solution {\n    findOrder(numCourses, prerequisites) {\n        const adj = Array.from({ length: numCourses }, () => []);\n        const inDegree = new Array(numCourses).fill(0);\n        const order = [];\n\n        // Build adjacency list and in-degree array\n        for (let [a, b] of prerequisites) {\n            adj[b].push(a);\n            inDegree[a]++;\n        }\n\n        // Initialize queue with nodes having an in-degree of 0\n        const q = [];\n        for (let i = 0; i < numCourses; i++) {\n            if (inDegree[i] === 0) {\n                q.push(i);\n            }\n        }\n\n        // Process queue\n        while (q.length > 0) {\n            const node = q.shift();\n            order.push(node);\n\n            for (let nei of adj[node]) {\n                inDegree[nei]--;\n                if (inDegree[nei] === 0) {\n                    q.push(nei);\n                }\n            }\n        }\n\n        // Check if all nodes are processed\n        if (order.length !== numCourses) {\n            return [];\n        }\n\n        return order;\n    }\n}\n"
    }
  },
  "QBCategory": "CCD",
  "boilerPlates": {
    "C": "#include <stdio.h>\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "C++": "#include <iostream>\nusing namespace std;\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "Java": "import java.util.*;\nimport java.io.*;\n\npublic class Main {\n    public static void main(String[] args) throws IOException {\n        // your code goes here\n    }\n}",
    "Python3": "# your code goes here\n",
    "JavaScript": "// your code goes here\n"
  },
  "_internal": {
    "methodName": "",
    "params": []
  }
}