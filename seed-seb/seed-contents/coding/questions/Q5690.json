{
  "questionId": "Q5690",
  "title": "Sereja and Graph",
  "slug": "sereja-and-graph",
  "version": 1,
  "createdAt": "2026-07-01",
  "updatedAt": "2026-07-04",
  "metadata": {
    "category": "Graph",
    "difficulty": "Medium",
    "isPremium": true,
    "tags": [
      "Perfect Matching",
      "Graph Theory"
    ],
    "companyTags": [],
    "topicTags": [
      "Flow & Matching"
    ],
    "QBCategory": "CCD"
  },
  "content": {
    "problemStatement": "Sereja has an undirected graph consisting of NNN vertices and MMM edges. Sereja is interested in one question: is it possible to delete edges from the graph in a way such that the degree of each vertex in the final graph is exactly 111?\n\nThe degree of any vertex in the graph is defined as the number of edges connected to that vertex.\n\nPlease help Sereja.",
    "constraints": [
      "1 <= T <= 10",
      "1 <= N <= 100",
      "1 <= M <= 100",
      "1 <= U, V <= N",
      "There can be multiple edges but no self-loops in the graph"
    ],
    "inputFormat": "The first line contains a single integer T, the number of test cases. Each test case contains two space-separated integers N and M, representing the number of vertices and edges in the graph, respectively. The next M lines contain two space-separated integers U and V, representing an edge connecting vertex U and vertex V.",
    "outputFormat": "For each test case, print YES if it is possible to delete edges from the graph in a way such that the degree of each vertex in the final graph is exactly 1, otherwise print NO.",
    "sampleTestCases": [
      {
        "id": "sample_1",
        "input": "3\n2 2\n1 2\n1 2\n3 2\n1 2\n2 3\n4 6\n1 2\n1 3\n1 4\n2 3\n2 4\n3 4",
        "output": "YES\nNO\nYES",
        "explanation": "For the first test case, we can delete one edge to make the degree of each vertex 1. For the second test case, it is not possible to delete edges to make the degree of each vertex 1. For the third test case, we can delete edges to make the degree of each vertex 1."
      },
      {
        "id": "sample_2",
        "input": "1\n4 4\n1 2\n1 3\n2 4\n3 4",
        "output": "NO",
        "explanation": "It is not possible to delete edges to make the degree of each vertex 1."
      },
      {
        "id": "sample_3",
        "input": "1\n2 1\n1 2",
        "output": "YES",
        "explanation": "We can delete no edges to make the degree of each vertex 1."
      }
    ]
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "label": "minimum boundary",
        "input": "1\n1 0",
        "expectedOutput": "NO",
        "weight": 2
      },
      {
        "id": "tc_2",
        "label": "Case 2",
        "input": "1\n1 0",
        "expectedOutput": "NO",
        "weight": 2
      },
      {
        "id": "tc_3",
        "label": "Case 3",
        "input": "1\n1 0",
        "expectedOutput": "NO",
        "weight": 2
      },
      {
        "id": "tc_4",
        "label": "Case 4",
        "input": "1\n1 0",
        "expectedOutput": "NO",
        "weight": 2
      },
      {
        "id": "tc_5",
        "label": "Case 5",
        "input": "1\n1 0",
        "expectedOutput": "NO",
        "weight": 2
      }
    ],
    "totalTestCases": 5,
    "totalWeight": 10
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      2,
      2,
      2,
      2,
      2
    ],
    "maxScore": 10
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "To solve this problem, we can use the concept of perfect matching in graph theory. A perfect matching is a subset of the edges in a graph such that every vertex is incident to exactly one edge in the subset. We can check if a perfect matching exists in the graph by trying to match each vertex with another vertex. If we can match all vertices, then a perfect matching exists, and we can delete edges to make the degree of each vertex 1.",
    "timeComplexity": "O(T * M * N) per test case, O(T * M * N) total. The time complexity is due to the nested loops used to check for perfect matching.",
    "spaceComplexity": "O(N) — the dominant structure is the adjacency list representation of the graph.",
    "code": {
      "C": "#include <stdio.h>\n#include <stdlib.h>\n\n// Structure to represent a graph\ntypedef struct {\n    int numVertices;\n    int numEdges;\n    int **adjMatrix;\n} Graph;\n\n// Function to create a graph\nGraph *createGraph(int numVertices, int numEdges) {\n    Graph *graph = (Graph *)malloc(sizeof(Graph));\n    graph->numVertices = numVertices;\n    graph->numEdges = numEdges;\n    graph->adjMatrix = (int **)malloc(numVertices * sizeof(int *));\n    for (int i = 0; i < numVertices; i++) {\n        graph->adjMatrix[i] = (int *)malloc(numVertices * sizeof(int));\n        for (int j = 0; j < numVertices; j++) {\n            graph->adjMatrix[i][j] = 0;\n        }\n    }\n    return graph;\n}\n\n// Function to add an edge to the graph\nvoid addEdge(Graph *graph, int u, int v) {\n    graph->adjMatrix[u][v] = 1;\n    graph->adjMatrix[v][u] = 1;\n}\n\n// Function to check if a perfect matching exists\nint hasPerfectMatching(Graph *graph) {\n    int *matched = (int *)malloc(graph->numVertices * sizeof(int));\n    for (int i = 0; i < graph->numVertices; i++) {\n        matched[i] = -1;\n    }\n    for (int i = 0; i < graph->numVertices; i++) {\n        for (int j = 0; j < graph->numVertices; j++) {\n            if (graph->adjMatrix[i][j] == 1 && matched[i] == -1 && matched[j] == -1) {\n                matched[i] = j;\n                matched[j] = i;\n            }\n        }\n    }\n    for (int i = 0; i < graph->numVertices; i++) {\n        if (matched[i] == -1) {\n            return 0;\n        }\n    }\n    return 1;\n}\n\nint main() {\n    int T;\n    scanf(\"%d\", &T);\n    while (T--) {\n        int N, M;\n        scanf(\"%d %d\", &N, &M);\n        Graph *graph = createGraph(N, M);\n        for (int i = 0; i < M; i++) {\n            int u, v;\n            scanf(\"%d %d\", &u, &v);\n            addEdge(graph, u - 1, v - 1);\n        }\n        if (hasPerfectMatching(graph)) {\n            printf(\"YES\\n\");\n        } else {\n            printf(\"NO\\n\");\n        }\n    }\n    return 0;\n}",
      "C++": "#include <iostream>\n#include <vector>\nusing namespace std;\n\n// Structure to represent a graph\nstruct Graph {\n    int numVertices;\n    int numEdges;\n    vector<vector<int>> adjList;\n};\n\n// Function to create a graph\nGraph createGraph(int numVertices, int numEdges) {\n    Graph graph;\n    graph.numVertices = numVertices;\n    graph.numEdges = numEdges;\n    graph.adjList.resize(numVertices);\n    return graph;\n}\n\n// Function to add an edge to the graph\nvoid addEdge(Graph &graph, int u, int v) {\n    graph.adjList[u].push_back(v);\n    graph.adjList[v].push_back(u);\n}\n\n// Function to check if a perfect matching exists\nbool hasPerfectMatching(Graph &graph) {\n    vector<int> matched(graph.numVertices, -1);\n    for (int i = 0; i < graph.numVertices; i++) {\n        for (int j = 0; j < graph.numVertices; j++) {\n            if (find(graph.adjList[i].begin(), graph.adjList[i].end(), j) != graph.adjList[i].end() && matched[i] == -1 && matched[j] == -1) {\n                matched[i] = j;\n                matched[j] = i;\n            }\n        }\n    }\n    for (int i = 0; i < graph.numVertices; i++) {\n        if (matched[i] == -1) {\n            return false;\n        }\n    }\n    return true;\n}\n\nint main() {\n    ios::sync_with_stdio(false);\n    cin.tie(NULL);\n    int T;\n    cin >> T;\n    while (T--) {\n        int N, M;\n        cin >> N >> M;\n        Graph graph = createGraph(N, M);\n        for (int i = 0; i < M; i++) {\n            int u, v;\n            cin >> u >> v;\n            addEdge(graph, u - 1, v - 1);\n        }\n        if (hasPerfectMatching(graph)) {\n            cout << \"YES\" << endl;\n        } else {\n            cout << \"NO\" << endl;\n        }\n    }\n    return 0;\n}",
      "Java": "import java.util.Scanner;\n\npublic class Main {\n    // Structure to represent a graph\n    static class Graph {\n        int numVertices;\n        int numEdges;\n        int[][] adjMatrix;\n\n        public Graph(int numVertices, int numEdges) {\n            this.numVertices = numVertices;\n            this.numEdges = numEdges;\n            this.adjMatrix = new int[numVertices][numVertices];\n        }\n    }\n\n    // Function to add an edge to the graph\n    public static void addEdge(Graph graph, int u, int v) {\n        graph.adjMatrix[u][v] = 1;\n        graph.adjMatrix[v][u] = 1;\n    }\n\n    // Function to check if a perfect matching exists\n    public static boolean hasPerfectMatching(Graph graph) {\n        int[] matched = new int[graph.numVertices];\n        for (int i = 0; i < graph.numVertices; i++) {\n            matched[i] = -1;\n        }\n        for (int i = 0; i < graph.numVertices; i++) {\n            for (int j = 0; j < graph.numVertices; j++) {\n                if (graph.adjMatrix[i][j] == 1 && matched[i] == -1 && matched[j] == -1) {\n                    matched[i] = j;\n                    matched[j] = i;\n                }\n            }\n        }\n        for (int i = 0; i < graph.numVertices; i++) {\n            if (matched[i] == -1) {\n                return false;\n            }\n        }\n        return true;\n    }\n\n    public static void main(String[] args) {\n        Scanner scanner = new Scanner(System.in);\n        int T = scanner.nextInt();\n        while (T-- > 0) {\n            int N = scanner.nextInt();\n            int M = scanner.nextInt();\n            Graph graph = new Graph(N, M);\n            for (int i = 0; i < M; i++) {\n                int u = scanner.nextInt() - 1;\n                int v = scanner.nextInt() - 1;\n                addEdge(graph, u, v);\n            }\n            if (hasPerfectMatching(graph)) {\n                System.out.println(\"YES\");\n            } else {\n                System.out.println(\"NO\");\n            }\n        }\n    }\n}",
      "Python3": "import sys\n\n# Function to create a graph\ndef create_graph(num_vertices, num_edges):\n    graph = [[] for _ in range(num_vertices)]\n    return graph\n\n# Function to add an edge to the graph\ndef add_edge(graph, u, v):\n    graph[u].append(v)\n    graph[v].append(u)\n\n# Function to check if a perfect matching exists\ndef has_perfect_matching(graph):\n    num_vertices = len(graph)\n    matched = [-1] * num_vertices\n    for i in range(num_vertices):\n        for j in range(num_vertices):\n            if j in graph[i] and matched[i] == -1 and matched[j] == -1:\n                matched[i] = j\n                matched[j] = i\n    for i in range(num_vertices):\n        if matched[i] == -1:\n            return False\n    return True\n\nT = int(sys.stdin.readline())\nfor _ in range(T):\n    N, M = map(int, sys.stdin.readline().split())\n    graph = create_graph(N, M)\n    for _ in range(M):\n        u, v = map(int, sys.stdin.readline().split())\n        add_edge(graph, u - 1, v - 1)\n    if has_perfect_matching(graph):\n        print(\"YES\")\n    else:\n        print(\"NO\")",
      "JavaScript": "const lines = require('fs').readFileSync(0, 'utf-8').trim().split('\\n');\nlet i = 0;\n\n// Function to create a graph\nfunction createGraph(numVertices, numEdges) {\n    const graph = Array.from({ length: numVertices }, () => []);\n    return graph;\n}\n\n// Function to add an edge to the graph\nfunction addEdge(graph, u, v) {\n    graph[u].push(v);\n    graph[v].push(u);\n}\n\n// Function to check if a perfect matching exists\nfunction hasPerfectMatching(graph) {\n    const numVertices = graph.length;\n    const matched = Array(numVertices).fill(-1);\n    for (let i = 0; i < numVertices; i++) {\n        for (let j = 0; j < numVertices; j++) {\n            if (graph[i].includes(j) && matched[i] === -1 && matched[j] === -1) {\n                matched[i] = j;\n                matched[j] = i;\n            }\n        }\n    }\n    for (let i = 0; i < numVertices; i++) {\n        if (matched[i] === -1) {\n            return false;\n        }\n    }\n    return true;\n}\n\nconst T = parseInt(lines[i++]);\nfor (let t = 0; t < T; t++) {\n    const [N, M] = lines[i++].split(' ').map(Number);\n    const graph = createGraph(N, M);\n    for (let j = 0; j < M; j++) {\n        const [u, v] = lines[i++].split(' ').map(Number);\n        addEdge(graph, u - 1, v - 1);\n    }\n    if (hasPerfectMatching(graph)) {\n        console.log('YES');\n    } else {\n        console.log('NO');\n    }\n}"
    }
  },
  "QBCategory": "CCD",
  "boilerPlates": {
    "C": "#include <stdio.h>\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "C++": "#include <iostream>\nusing namespace std;\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "Java": "import java.util.*;\nimport java.io.*;\n\npublic class Main {\n    public static void main(String[] args) throws IOException {\n        // your code goes here\n    }\n}",
    "Python3": "# your code goes here\n",
    "JavaScript": "// your code goes here\n"
  },
  "_internal": {
    "methodName": "",
    "params": []
  }
}