{
  "questionId": "Q11361",
  "title": "Coin change II",
  "slug": "coin-change-2",
  "version": 1,
  "createdAt": "2026-07-05",
  "updatedAt": "2026-07-05",
  "metadata": {
    "category": "Dynamic Programming",
    "difficulty": "Hard",
    "isPremium": true,
    "tags": [
      "Coin Change",
      "Memoization"
    ],
    "companyTags": [],
    "topicTags": [],
    "QBCategory": "CCD"
  },
  "content": {
    "problemStatement": "We are given an array Arr with N distinct coins and a target. We have an\n    infinite supply of each coin denomination. We need to find the number of ways we sum up the coin values to give us\n    the target.",
    "constraints": [
      "1 <= N <= 100",
      "1 <= T <= 1000",
      "1 <= arr[i] <= 1000"
    ],
    "inputFormat": "The first line contains an integer N, the number of distinct coins. The second line contains N space-separated integers representing the coin denominations. The third line contains an integer T, the target sum.",
    "outputFormat": "The total number of ways to sum up the coin values to give the target.",
    "sampleTestCases": []
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "label": "min boundary",
        "input": "1\n1\n1",
        "expectedOutput": "1",
        "weight": 2
      },
      {
        "id": "tc_2",
        "label": "max boundary",
        "input": "5\n1 2 3 4 5\n10",
        "expectedOutput": "30",
        "weight": 2
      },
      {
        "id": "tc_3",
        "label": "uniform case",
        "input": "3\n1 1 1\n3",
        "expectedOutput": "1",
        "weight": 2
      },
      {
        "id": "tc_4",
        "label": "tricky case",
        "input": "2\n1 3\n4",
        "expectedOutput": "3",
        "weight": 2
      },
      {
        "id": "tc_5",
        "label": "mixed case",
        "input": "4\n1 2 3 4\n10",
        "expectedOutput": "14",
        "weight": 2
      }
    ],
    "totalTestCases": 5,
    "totalWeight": 10
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      2,
      2,
      2,
      2,
      2
    ],
    "maxScore": 10
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "Use dynamic programming with memoization to store the results of subproblems and avoid redundant calculations.",
    "timeComplexity": "O(N * T)",
    "spaceComplexity": "O(N * T)",
    "code": {
      "C": "#include <stdio.h>\n\nlong countWaysToMakeChangeUtil(int* arr, int ind, int T, long** dp) {\n    if (ind == 0) return (T % arr[0] == 0) ? 1 : 0;\n    if (dp[ind][T] != -1) return dp[ind][T];\n\n    long notTaken = countWaysToMakeChangeUtil(arr, ind - 1, T, dp);\n    long taken = 0;\n    if (arr[ind] <= T) taken = countWaysToMakeChangeUtil(arr, ind, T - arr[ind], dp);\n\n    return dp[ind][T] = notTaken + taken;\n}\n\nlong countWaysToMakeChange(int* arr, int n, int T) {\n    long** dp = (long**)malloc(n * sizeof(long*));\n    for (int i = 0; i < n; i++) dp[i] = (long*)malloc((T + 1) * sizeof(long));\n    for (int i = 0; i < n; i++) for (int j = 0; j <= T; j++) dp[i][j] = -1;\n    return countWaysToMakeChangeUtil(arr, n - 1, T, dp);\n}\n\nint main() {\n    int n, T;\n    scanf(\"%d\", &n);\n    int* arr = (int*)malloc(n * sizeof(int));\n    for (int i = 0; i < n; i++) scanf(\"%d\", &arr[i]);\n    scanf(\"%d\", &T);\n\n    printf(\"%ld\\n\", countWaysToMakeChange(arr, n, T));\n    return 0;\n}",
      "C++": "#include <bits/stdc++.h>\nusing namespace std;\n\n// Recursive function with memoization to count ways to make change\nlong countWaysToMakeChangeUtil(vector<int>& arr, int ind, int T, vector<vector<long>>& dp) {\n    // Base case: if we are at the first element\n    if (ind == 0) {\n        return (T % arr[0] == 0); // Only one way if divisible\n    }\n\n    // If already calculated, return stored value\n    if (dp[ind][T] != -1)\n        return dp[ind][T];\n\n    // Do not take the current coin\n    long notTaken = countWaysToMakeChangeUtil(arr, ind - 1, T, dp);\n\n    // Take the current coin (if feasible)\n    long taken = 0;\n    if (arr[ind] <= T)\n        taken = countWaysToMakeChangeUtil(arr, ind, T - arr[ind], dp);\n\n    // Store the result in DP table\n    return dp[ind][T] = notTaken + taken;\n}\n\n// Function to initialize DP table and start recursion\nlong countWaysToMakeChange(vector<int>& arr, int n, int T) {\n    vector<vector<long>> dp(n, vector<long>(T + 1, -1));\n    return countWaysToMakeChangeUtil(arr, n - 1, T, dp);\n}\n\nint main() {\n    vector<int> arr = {1, 2, 3};\n    int target = 4;\n    int n = arr.size();\n\n    cout << \"The total number of ways is \" << countWaysToMakeChange(arr, n, target) << endl;\n    return 0;\n}",
      "Java": "import java.util.*;\n\nclass Solution {\n    // Recursive function with memoization\n    private long countWaysToMakeChangeUtil(int[] arr, int ind, int T, long[][] dp) {\n        if (ind == 0) {\n            return (T % arr[0] == 0) ? 1 : 0;\n        }\n        if (dp[ind][T] != -1) return dp[ind][T];\n\n        long notTaken = countWaysToMakeChangeUtil(arr, ind - 1, T, dp);\n        long taken = 0;\n        if (arr[ind] <= T) {\n            taken = countWaysToMakeChangeUtil(arr, ind, T - arr[ind], dp);\n        }\n        return dp[ind][T] = notTaken + taken;\n    }\n\n    public long countWaysToMakeChange(int[] arr, int n, int T) {\n        long[][] dp = new long[n][T + 1];\n        for (long[] row : dp) Arrays.fill(row, -1);\n        return countWaysToMakeChangeUtil(arr, n - 1, T, dp);\n    }\n\n    public static void main(String[] args) {\n        int[] arr = {1, 2, 3};\n        int target = 4;\n        int n = arr.length;\n\n        Solution sol = new Solution();\n        System.out.println(\"The total number of ways is \" + sol.countWaysToMakeChange(arr, n, target));\n    }\n}",
      "Python3": "class Solution:\n    def countWaysToMakeChangeUtil(self, arr, ind, T, dp):\n        if ind == 0:\n            return 1 if T % arr[0] == 0 else 0\n        if dp[ind][T] != -1:\n            return dp[ind][T]\n\n        notTaken = self.countWaysToMakeChangeUtil(arr, ind - 1, T, dp)\n        taken = 0\n        if arr[ind] <= T:\n            taken = self.countWaysToMakeChangeUtil(arr, ind, T - arr[ind], dp)\n\n        dp[ind][T] = notTaken + taken\n        return dp[ind][T]\n\n    def countWaysToMakeChange(self, arr, n, T):\n        dp = [[-1 for _ in range(T + 1)] for _ in range(n)]\n        return self.countWaysToMakeChangeUtil(arr, n - 1, T, dp)\n\nif __name__ == \"__main__\":\n    arr = [1, 2, 3]\n    target = 4\n    n = len(arr)\n\n    sol = Solution()\n    print(\"The total number of ways is\", sol.countWaysToMakeChange(arr, n, target))",
      "JavaScript": "class Solution {\n    // Recursive function with memoization\n    countWaysToMakeChangeUtil(arr, ind, T, dp) {\n        if (ind === 0) {\n            return (T % arr[0] === 0) ? 1 : 0;\n        }\n        if (dp[ind][T] !== -1) return dp[ind][T];\n\n        let notTaken = this.countWaysToMakeChangeUtil(arr, ind - 1, T, dp);\n        let taken = 0;\n        if (arr[ind] <= T) {\n            taken = this.countWaysToMakeChangeUtil(arr, ind, T - arr[ind], dp);\n        }\n        dp[ind][T] = notTaken + taken;\n        return dp[ind][T];\n    }\n\n    countWaysToMakeChange(arr, n, T) {\n        const dp = Array.from({ length: n }, () => Array(T + 1).fill(-1));\n        return this.countWaysToMakeChangeUtil(arr, n - 1, T, dp);\n    }\n}\n\n// Driver code\nconst arr = [1, 2, 3];\nconst target = 4;\nconst n = arr.length;\n\nconst sol = new Solution();\nconsole.log(\"The total number of ways is\", sol.countWaysToMakeChange(arr, n, target));"
    }
  },
  "QBCategory": "CCD",
  "boilerPlates": {
    "C": "#include <stdio.h>\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "C++": "#include <iostream>\nusing namespace std;\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "Java": "import java.util.*;\nimport java.io.*;\n\npublic class Main {\n    public static void main(String[] args) throws IOException {\n        // your code goes here\n    }\n}",
    "Python3": "# your code goes here\n",
    "JavaScript": "// your code goes here\n"
  },
  "_internal": {
    "methodName": "",
    "params": []
  }
}