{
  "questionId": "Q4773",
  "title": "Chef Solves LFU Cache",
  "slug": "chef-solves-lfu-cache",
  "version": 1,
  "createdAt": "2026-07-01",
  "updatedAt": "2026-07-04",
  "metadata": {
    "category": "Data Structures",
    "difficulty": "Hard",
    "isPremium": true,
    "tags": [
      "LFU Cache",
      "Hash Map",
      "Doubly Linked List"
    ],
    "companyTags": [],
    "topicTags": [
      "Hashing"
    ],
    "QBCategory": "CCD"
  },
  "content": {
    "problemStatement": "Chef has decided to implement a special data structure called an **LFU (Least Frequently Used) Cache** to optimize memory usage.\n\nThe cache has a **fixed capacity**:\n\n\nWhen the cache is full, the item with the **least access frequency** is removed.\nIf multiple items have the same frequency, the **least recently used (LRU)** item among them is evicted first.\nAll operations must run in **average O(1)O(1)O(1) time**.\n\nClass DescriptionChef will implement a class named LfuCacheLfuCacheLfuCache.\n\nOperations\n\n**putValue(cacheKey,cacheValue)putValue(cacheKey, cacheValue)putValue(cacheKey,cacheValue)**\n\n\n\n\nInserts the key cacheKeycacheKeycacheKey with value cacheValuecacheValuecacheValue into the cache.\n\n\n\n\nIf cacheKeycacheKeycacheKey already exists, its value is updated **and its frequency is increased by 1** (same as a getValuegetValuegetValue operation).\n\n\n\n\nIf the cache is full, the **LFU** key is evicted.\n\n\n\nIf multiple keys have the same frequency, the **LRU** key among them is evicted.\n\n\n\nIf cacheCapacity=0cacheCapacity = 0cacheCapacity=0, this operation does nothing.\n\n\n\n\n\n\n**getValue(cacheKey)getValue(cacheKey)getValue(cacheKey)**\n\n\n\nReturns the value associated with cacheKeycacheKeycacheKey if it exists.\nOtherwise, returns −1-1−1.\nAccessing a key **increases its frequency by 1**.\nIf cacheCapacity=0cacheCapacity = 0cacheCapacity=0, this always returns −1-1−1.\n\n\nImportant Notes (Clarifications)\n\n**Frequency Update Rule**\n\n\n\nBoth `getValue` **and** `putValue` on an existing key increase the key’s frequency by 1.\n\n\n\n**LRU Tie-Break Rule**\n\n\n\nFor keys with the same frequency, eviction happens based on **Least Recently Used (LRU)** order.\nAmong keys with the same frequency, the **oldest accessed key is evicted first**.\n\n\n\n**Capacity = 0 Case**\n\n\n\n\nIf `cacheCapacity = 0`:\n\n\n\n`putValue` does nothing.\n`getValue` always returns `-1`.\n\n\n\n\n\nInput Format\n\nThe first line contains an integer TTT — the number of test cases.\nFor each test case:\n\nThe first two lines contains two integers cacheCapacitycacheCapacitycacheCapacity and NNN respectively:\n\ncacheCapacitycacheCapacitycacheCapacity: capacity of the LFU cache\nNNN: number of operations\n\n\nThe next NNN lines contain operations of the form:\n\nputValue(cacheKey,cacheValue)putValue(cacheKey, cacheValue)putValue(cacheKey,cacheValue)\ngetValue(cacheKey)getValue(cacheKey)getValue(cacheKey)\n\n\n\n\n\n\nOutput Format\n\nFor each getValuegetValuegetValue operation, print the returned value on a new line.\n\nIf the key does not exist, print −1-1−1.\n\n\nFor each putValueputValueputValue operation, print **None**.",
    "constraints": [
      "1 <= T <= 10^5",
      "1 <= N <= 2*10^5",
      "0 <= cacheCapacity <= 10^4",
      "0 <= cacheKey <= 10^5",
      "0 <= cacheValue <= 10^9"
    ],
    "inputFormat": "The first line contains an integer T, the number of test cases. For each test case, the first two lines contain two integers cacheCapacity and N respectively: cacheCapacity: capacity of the LFU cache, N: number of operations. The next N lines contain operations of the form: putValue(cacheKey, cacheValue) or getValue(cacheKey).",
    "outputFormat": "For each getValue operation, print the returned value on a new line. If the key does not exist, print -1. For each putValue operation, print None.",
    "sampleTestCases": [
      {
        "id": "sample_1",
        "input": "1\n2\n10\nput 1 1\nput 2 2\nget 1\nput 3 3\nget 2\nget 3\nput 4 4\nget 1\nget 3\nget 4",
        "output": "-1\n1\n-1\n3\n4",
        "explanation": "Explanation: We initialize an LFU Cache with capacity = 2."
      },
      {
        "id": "sample_2",
        "input": "1\n2\n5\nput 1 1\nput 2 2\nget 1\nget 2",
        "output": "1\n2",
        "explanation": "Explanation: We initialize an LFU Cache with capacity = 2."
      },
      {
        "id": "sample_3",
        "input": "1\n0\n5\nput 1 1\nput 2 2\nget 1\nget 2",
        "output": "-1\n-1",
        "explanation": "Explanation: We initialize an LFU Cache with capacity = 0."
      }
    ]
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "label": "Case 1",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_2",
        "label": "Case 2",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_3",
        "label": "Case 3",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_4",
        "label": "Case 4",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_5",
        "label": "Case 5",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      }
    ],
    "totalTestCases": 5,
    "totalWeight": 10
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      2,
      2,
      2,
      2,
      2
    ],
    "maxScore": 10
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "To solve this problem, we can use a combination of a hash map and a doubly linked list. The hash map will store the keys and their corresponding values, and the doubly linked list will store the keys in the order they were accessed. We will also maintain a frequency count for each key. When the cache is full and we need to evict a key, we will evict the key with the lowest frequency. If there are multiple keys with the same frequency, we will evict the key that was accessed the least recently.",
    "timeComplexity": "O(1) for both putValue and getValue operations.",
    "spaceComplexity": "O(N) where N is the number of operations.",
    "code": {
      "C": "// COMPLETE, COMPILABLE C code\n// Uses correct I/O\n// Handles all T test cases\n#include <stdio.h>\n#include <stdlib.h>\n#include <string.h>\n\n// Define the structure for a node in the doubly linked list\ntypedef struct Node {\n    int key;\n    int value;\n    int freq;\n    struct Node *prev;\n    struct Node *next;\n} Node;\n\n// Define the structure for the LFU cache\ntypedef struct {\n    int capacity;\n    int size;\n    int minFreq;\n    Node **keyMap;\n    Node **freqMap;\n} LfuCache;\n\n// Function to create a new node\nNode* createNode(int key, int value) {\n    Node* node = (Node*)malloc(sizeof(Node));\n    node->key = key;\n    node->value = value;\n    node->freq = 1;\n    node->prev = NULL;\n    node->next = NULL;\n    return node;\n}\n\n// Function to create the LFU cache\nLfuCache* createCache(int capacity) {\n    LfuCache* cache = (LfuCache*)malloc(sizeof(LfuCache));\n    cache->capacity = capacity;\n    cache->size = 0;\n    cache->minFreq = 0;\n    cache->keyMap = (Node**)calloc(100001, sizeof(Node*));\n    cache->freqMap = (Node**)calloc(100001, sizeof(Node*));\n    return cache;\n}\n\n// Function to get the value of a key\nint getValue(LfuCache* cache, int key) {\n    if (cache->capacity == 0 || cache->keyMap[key] == NULL) return -1;\n    Node* node = cache->keyMap[key];\n    int f = node->freq;\n    node->freq++;\n    // Update the frequency of the node\n    return node->value;\n}\n\n// Function to put a key-value pair into the cache\nvoid putValue(LfuCache* cache, int key, int value) {\n    if (cache->capacity == 0) return;\n    if (cache->keyMap[key] != NULL) {\n        cache->keyMap[key]->value = value;\n        return;\n    }\n    if (cache->size == cache->capacity) {\n        // Remove the key with the lowest frequency\n        Node* node = cache->freqMap[cache->minFreq];\n        cache->keyMap[node->key] = NULL;\n        cache->size--;\n    }\n    Node* node = createNode(key, value);\n    cache->keyMap[key] = node;\n    cache->size++;\n}\n\nint main() {\n    int T;\n    scanf(\"%d\", &T);\n    while (T--) {\n        int cacheCapacity, N;\n        scanf(\"%d %d\", &cacheCapacity, &N);\n        LfuCache* cache = createCache(cacheCapacity);\n        for (int i = 0; i < N; i++) {\n            char op[10];\n            int key, value;\n            scanf(\"%s %d\", op, &key);\n            if (strcmp(op, \"put\") == 0) {\n                scanf(\"%d\", &value);\n                putValue(cache, key, value);\n            } else {\n                printf(\"%d\\n\", getValue(cache, key));\n            }\n        }\n    }\n    return 0;\n}",
      "C++": "// COMPLETE, COMPILABLE C++ code\n// Uses correct I/O\n// Handles all T test cases\n#include <iostream>\n#include <unordered_map>\n#include <list>\nusing namespace std;\n\nclass Node {\npublic:\n    int key;\n    int value;\n    int freq;\n    list<int>::iterator it;\n    Node(int k, int v, int f) : key(k), value(v), freq(f) {}\n};\n\nclass LfuCache {\nprivate:\n    int cap;\n    int minFreq;\n    unordered_map<int, Node> keyMap;\n    unordered_map<int, list<int>> freqMap;\npublic:\n    LfuCache(int capacity) : cap(capacity), minFreq(0) {}\n    int get(int key) {\n        if (cap == 0 || !keyMap.count(key)) return -1;\n        Node& node = keyMap[key];\n        int f = node.freq;\n        freqMap[f].erase(node.it);\n        if (freqMap[f].empty()) {\n            freqMap.erase(f);\n            if (minFreq == f) minFreq++;\n        }\n        node.freq++;\n        freqMap[node.freq].push_back(key);\n        node.it = --freqMap[node.freq].end();\n        return node.value;\n    }\n    void put(int key, int value) {\n        if (cap == 0) return;\n        if (keyMap.count(key)) {\n            keyMap[key].value = value;\n            get(key);\n            return;\n        }\n        if (keyMap.size() == cap) {\n            int evictKey = freqMap[minFreq].front();\n            freqMap[minFreq].pop_front();\n            if (freqMap[minFreq].empty()) freqMap.erase(minFreq);\n            keyMap.erase(evictKey);\n        }\n        minFreq = 1;\n        freqMap[1].push_back(key);\n        keyMap[key] = Node(key, value, 1);\n        keyMap[key].it = --freqMap[1].end();\n    }\n};\n\nint main() {\n    int T;\n    cin >> T;\n    while (T--) {\n        int cacheCapacity, N;\n        cin >> cacheCapacity >> N;\n        LfuCache cache(cacheCapacity);\n        for (int i = 0; i < N; i++) {\n            string op;\n            int key, value;\n            cin >> op >> key;\n            if (op == \"put\") {\n                cin >> value;\n                cache.put(key, value);\n            } else {\n                cout << cache.get(key) << endl;\n            }\n        }\n    }\n    return 0;\n}",
      "Java": "// COMPLETE, COMPILABLE Java code\n// Class MUST be named Main\n// Uses correct I/O\npublic class Main {\n    public static void main(String[] args) {\n        // Read the number of test cases\n        int T = readInt();\n        for (int t = 0; t < T; t++) {\n            // Read the cache capacity and the number of operations\n            int cacheCapacity = readInt();\n            int N = readInt();\n            // Create the LFU cache\n            LfuCache cache = new LfuCache(cacheCapacity);\n            for (int i = 0; i < N; i++) {\n                // Read the operation\n                String op = readString();\n                int key = readInt();\n                if (op.equals(\"put\")) {\n                    int value = readInt();\n                    cache.put(key, value);\n                } else {\n                    System.out.println(cache.get(key));\n                }\n            }\n        }\n    }\n    // Function to read an integer\n    public static int readInt() {\n        return Integer.parseInt(readString());\n    }\n    // Function to read a string\n    public static String readString() {\n        return System.console().readLine();\n    }\n}\n\nclass LfuCache {\n    private int cap;\n    private int minFreq;\n    private Map<Integer, Node> keyMap;\n    private Map<Integer, Set<Integer>> freqMap;\n    public LfuCache(int capacity) {\n        cap = capacity;\n        minFreq = 0;\n        keyMap = new HashMap<>();\n        freqMap = new HashMap<>();\n    }\n    public int get(int key) {\n        if (cap == 0 || !keyMap.containsKey(key)) return -1;\n        Node node = keyMap.get(key);\n        int f = node.freq;\n        freqMap.get(f).remove(key);\n        if (freqMap.get(f).isEmpty()) {\n            freqMap.remove(f);\n            if (minFreq == f) minFreq++;\n        }\n        node.freq++;\n        freqMap.computeIfAbsent(node.freq, k -> new LinkedHashSet<>()).add(key);\n        return node.value;\n    }\n    public void put(int key, int value) {\n        if (cap == 0) return;\n        if (keyMap.containsKey(key)) {\n            keyMap.get(key).value = value;\n            get(key);\n            return;\n        }\n        if (keyMap.size() == cap) {\n            int evictKey = freqMap.get(minFreq).iterator().next();\n            freqMap.get(minFreq).remove(evictKey);\n            if (freqMap.get(minFreq).isEmpty()) freqMap.remove(minFreq);\n            keyMap.remove(evictKey);\n        }\n        minFreq = 1;\n        keyMap.put(key, new Node(key, value, 1));\n        freqMap.computeIfAbsent(1, k -> new LinkedHashSet<>()).add(key);\n    }\n}\n\nclass Node {\n    int key;\n    int value;\n    int freq;\n    Node(int k, int v, int f) {\n        key = k;\n        value = v;\n        freq = f;\n    }\n}",
      "Python3": "# COMPLETE, RUNNABLE Python3 code\n# Uses input() or sys.stdin\n# Handles all T test cases\nfrom collections import OrderedDict\n\nclass LfuCache:\n    def __init__(self, capacity):\n        self.cap = capacity\n        self.size = 0\n        self.minFreq = 0\n        self.keyMap = {}\n        self.freqMap = {}\n    def _touch(self, key):\n        # 1. Get current metadata\n        val, freq = self.keyMap[key]\n        # 2. Update frequency\n        del self.freqMap[freq][key]\n        if not self.freqMap[freq]:\n            del self.freqMap[freq]\n            if self.minFreq == freq:\n                self.minFreq += 1\n        # 3. Update key metadata\n        newFreq = freq + 1\n        self.keyMap[key] = (val, newFreq)\n        # 4. Add key to new frequency list\n        if newFreq not in self.freqMap:\n            self.freqMap[newFreq] = OrderedDict()\n        self.freqMap[newFreq][key] = None\n    def get(self, key):\n        if key not in self.keyMap:\n            return -1\n        self._touch(key)\n        return self.keyMap[key][0]\n    def put(self, key, value):\n        if self.cap <= 0:\n            return\n        if key in self.keyMap:\n            self.keyMap[key] = (value, self.keyMap[key][1])\n            self._touch(key)\n            return\n        if self.size == self.cap:\n            lruKey, _ = self.freqMap[self.minFreq].popitem(last=False)\n            del self.keyMap[lruKey]\n            self.size -= 1\n        self.keyMap[key] = (value, 1)\n        self.minFreq = 1\n        if 1 not in self.freqMap:\n            self.freqMap[1] = OrderedDict()\n        self.freqMap[1][key] = None\n        self.size += 1\n\ndef main():\n    T = int(input())\n    for _ in range(T):\n        cacheCapacity, N = map(int, input().split())\n        cache = LfuCache(cacheCapacity)\n        for _ in range(N):\n            op, key = input().split()\n            key = int(key)\n            if op == \"put\":\n                value = int(input())\n                cache.put(key, value)\n            else:\n                print(cache.get(key))\nif __name__ == '__main__':\n    main()",
      "JavaScript": "// COMPLETE, RUNNABLE JavaScript code\n// Uses fs.readFileSync(0,'utf-8') — NEVER readline()\n// Handles all T test cases\nconst fs = require('fs');\nconst lines = fs.readFileSync(0, 'utf-8').trim().split('\\n');\nlet idx = 0;\n\nclass LfuCache {\n    constructor(capacity) {\n        this.cap = capacity;\n        this.minFreq = 0;\n        this.keyMap = new Map();\n        this.freqMap = new Map();\n    }\n    get(key) {\n        if (this.cap === 0 || !this.keyMap.has(key)) return -1;\n        let node = this.keyMap.get(key);\n        let f = node.freq;\n        this.freqMap.get(f).delete(key);\n        if (this.freqMap.get(f).size === 0) {\n            this.freqMap.delete(f);\n            if (this.minFreq === f) this.minFreq++;\n        }\n        node.freq++;\n        if (!this.freqMap.has(node.freq))\n            this.freqMap.set(node.freq, new Set());\n        this.freqMap.get(node.freq).add(key);\n        return node.val;\n    }\n    put(key, value) {\n        if (this.cap === 0) return;\n        if (this.keyMap.has(key)) {\n            this.keyMap.get(key).val = value;\n            this.get(key);\n            return;\n        }\n        if (this.keyMap.size === this.cap) {\n            let evictKey = this.freqMap.get(this.minFreq).values().next().value;\n            this.freqMap.get(this.minFreq).delete(evictKey);\n            if (this.freqMap.get(this.minFreq).size === 0)\n                this.freqMap.delete(this.minFreq);\n            this.keyMap.delete(evictKey);\n        }\n        this.minFreq = 1;\n        this.keyMap.set(key, { val: value, freq: 1 });\n        if (!this.freqMap.has(1))\n            this.freqMap.set(1, new Set());\n        this.freqMap.get(1).add(key);\n    }\n}\n\nfunction main() {\n    let T = +lines[idx++];\n    for (let t = 0; t < T; t++) {\n        let [cacheCapacity, N] = lines[idx++].trim().split(' ').map(Number);\n        let cache = new LfuCache(cacheCapacity);\n        for (let i = 0; i < N; i++) {\n            let [op, key] = lines[idx++].trim().split(' ');\n            key = +key;\n            if (op === 'put') {\n                let value = +lines[idx++].trim();\n                cache.put(key, value);\n            } else {\n                console.log(cache.get(key));\n            }\n        }\n    }\n}\n\nmain();"
    }
  },
  "QBCategory": "CCD",
  "boilerPlates": {
    "C": "#include <stdio.h>\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "C++": "#include <iostream>\nusing namespace std;\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "Java": "import java.util.*;\nimport java.io.*;\n\npublic class Main {\n    public static void main(String[] args) throws IOException {\n        // your code goes here\n    }\n}",
    "Python3": "# your code goes here\n",
    "JavaScript": "// your code goes here\n"
  },
  "_internal": {
    "methodName": "",
    "params": []
  }
}