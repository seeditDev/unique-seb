{
  "questionId": "Q4838",
  "title": "InOrder Traversal",
  "slug": "inorder-traversal",
  "version": 1,
  "createdAt": "2026-07-01",
  "updatedAt": "2026-07-04",
  "metadata": {
    "category": "Trees",
    "difficulty": "Easy",
    "isPremium": true,
    "tags": [
      "Inorder Traversal",
      "Binary Tree"
    ],
    "companyTags": [],
    "topicTags": [
      "Trees"
    ],
    "QBCategory": "CCD"
  },
  "content": {
    "problemStatement": "TaskGiven a binary tree, print its nodes in Inorder traversal, by completing the given function.\n\n\nInput Format\n\nThe first line of the input contains a single integer NNN — the number of nodes in the binary tree.\nNext N−1N - 1N−1 lines contain three space separated characters pi,ci,Rip_i, c_i, R_ipi​,ci​,Ri​, describing the tree with edge informations - pip_ipi​ is an integer denoting the parent node of the iiith edge, cic_ici​ is child node, and the letter RiR_iRi​ denotes whether the child is left child or right child, it's value is `L` if cic_ici​ is left child of pip_ipi​, else `R`.\n\n\nOutput Format\nOutput on the single line, NNN space separated integers - its nodes in the inorder traversal of the given tree.",
    "constraints": [
      "1 <= T <= 10",
      "1 <= N <= 10000",
      "1 <= pi, ci <= N",
      "Ri is either 'L' or 'R'",
      "All node numbers are distinct"
    ],
    "inputFormat": "The first line of the input contains a single integer T, the number of test cases. Each test case contains a single integer N, the number of nodes in the binary tree. The next N-1 lines contain three space-separated values pi, ci, Ri, where pi is the parent node, ci is the child node, and Ri is either 'L' (ci is the left child of pi) or 'R' (ci is the right child of pi).",
    "outputFormat": "For each test case, print N space-separated integers - the node values in inorder traversal of the given tree.",
    "sampleTestCases": [
      {
        "id": "sample_1",
        "input": "1\n5\n1 2 L\n1 3 R\n3 4 L\n3 5 R",
        "output": "2 1 4 3 5",
        "explanation": "The tree has root 1. Inorder traversal visits left subtree (2), then 1, then left child of 3 (4), then 3, then right child of 3 (5)."
      },
      {
        "id": "sample_2",
        "input": "1\n3\n1 2 L\n1 3 R",
        "output": "2 1 3",
        "explanation": "Root 1 with left child 2 and right child 3. Inorder: 2, 1, 3."
      },
      {
        "id": "sample_3",
        "input": "1\n4\n1 2 L\n1 3 R\n3 4 L",
        "output": "2 1 4 3",
        "explanation": "Root 1, left child 2, right child 3 which itself has left child 4. Inorder: 2, 1, 4, 3."
      }
    ]
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "label": "minimum boundary",
        "input": "1\n1",
        "expectedOutput": "1",
        "weight": 2
      },
      {
        "id": "tc_2",
        "label": "maximum boundary",
        "input": "1\n10000\n1 2 L\n1 3 R\n3 4 L\n3 5 R\n",
        "expectedOutput": "2 1 4 3 5",
        "weight": 2
      },
      {
        "id": "tc_3",
        "label": "uniform/edge case",
        "input": "1\n5\n1 2 L\n1 3 R\n3 4 L\n3 5 R",
        "expectedOutput": "2 1 4 3 5",
        "weight": 2
      },
      {
        "id": "tc_4",
        "label": "tricky case",
        "input": "1\n5\n1 2 L\n1 3 R\n3 4 L\n3 5 R",
        "expectedOutput": "2 1 4 3 5",
        "weight": 2
      },
      {
        "id": "tc_5",
        "label": "stress/general",
        "input": "5\n5\n1 2 L\n1 3 R\n3 4 L\n3 5 R\n5\n1 2 L\n1 3 R\n3 4 L\n3 5 R\n5\n1 2 L\n1 3 R\n3 4 L\n3 5 R\n5\n1 2 L\n1 3 R\n3 4 L\n3 5 R\n5\n1 2 L\n1 3 R\n3 4 L\n3 5 R",
        "expectedOutput": "2 1 4 3 5\n2 1 4 3 5\n2 1 4 3 5\n2 1 4 3 5\n2 1 4 3 5",
        "weight": 2
      }
    ],
    "totalTestCases": 5,
    "totalWeight": 10
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      2,
      2,
      2,
      2,
      2
    ],
    "maxScore": 10
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "To solve this problem, we first construct the binary tree based on the given edge information. Then, we perform an inorder traversal of the tree to get the node values in the required order. The key insight here is to identify the root of the tree, which is the node that is not a child of any other node.",
    "timeComplexity": "O(T * N) - where T is the number of test cases and N is the number of nodes in the binary tree. The reason is that for each test case, we are constructing the tree and then performing an inorder traversal, both of which take O(N) time.",
    "spaceComplexity": "O(N) - for storing the nodes of the binary tree.",
    "code": {
      "C": "#include <stdio.h>\n#include <stdlib.h>\n#include <stdbool.h>\n\ntypedef struct Node {\n    int val;\n    struct Node* left;\n    struct Node* right;\n} Node;\n\nNode* newNode(int v) {\n    Node* n = (Node*)malloc(sizeof(Node));\n    n->val = v;\n    n->left = n->right = NULL;\n    return n;\n}\n\nvoid inorder(Node* r) {\n    if (!r) return;\n    inorder(r->left);\n    printf(\"%d \", r->val);\n    inorder(r->right);\n}\n\nint main() {\n    int T; if (scanf(\"%d\", &T)!=1) return 0;\n    while (T--) {\n        int N; scanf(\"%d\", &N);\n        Node** nodes = (Node**)malloc((N+1)*sizeof(Node*));\n        bool* isChild = (bool*)calloc(N+1, sizeof(bool));\n        for (int i=1;i<=N;i++) nodes[i]=newNode(i);\n        for (int i=0;i<N-1;i++) {\n            int p,c; char dir; scanf(\"%d %d %c\", &p, &c, &dir);\n            if (dir=='L') nodes[p]->left = nodes[c];\n            else nodes[p]->right = nodes[c];\n            isChild[c]=true;\n        }\n        int root=1; for (int i=1;i<=N;i++) if (!isChild[i]) {root=i; break;}\n        inorder(nodes[root]);\n        printf(\"\\n\");\n        free(isChild);\n        free(nodes);\n    }\n    return 0;\n}",
      "C++": "#include <bits/stdc++.h>\nusing namespace std;\nstruct Node{int val; Node* left; Node* right; Node(int v):val(v),left(nullptr),right(nullptr){}};\nvoid inorder(Node* r){if(!r) return; inorder(r->left); cout<<r->val<<' '; inorder(r->right);} \nint main(){ios::sync_with_stdio(false);cin.tie(nullptr);\n    int T; if(!(cin>>T)) return 0; while(T--){int N;cin>>N; vector<Node*> nodes(N+1,nullptr); vector<bool> child(N+1,false);\n        for(int i=1;i<=N;i++) nodes[i]=new Node(i);\n        for(int i=0;i<N-1;i++){int p,c; char d;cin>>p>>c>>d; if(d=='L') nodes[p]->left=nodes[c]; else nodes[p]->right=nodes[c]; child[c]=true;}\n        int root=1; for(int i=1;i<=N;i++) if(!child[i]){root=i;break;}\n        inorder(nodes[root]); cout<<\"\\n\";\n        // optional cleanup\n        for(int i=1;i<=N;i++) delete nodes[i];\n    }\n    return 0; }\n",
      "Java": "import java.io.*;\nimport java.util.*;\n\npublic class Main {\n    static class Node {\n        int val; Node left, right;\n        Node(int v){val=v;}\n    }\n    static void inorder(Node r, StringBuilder sb){\n        if(r==null) return;\n        inorder(r.left, sb);\n        sb.append(r.val).append(' ');\n        inorder(r.right, sb);\n    }\n    public static void main(String[] args) throws Exception {\n        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));\n        StringTokenizer st;\n        int T = Integer.parseInt(br.readLine().trim());\n        StringBuilder out = new StringBuilder();\n        while(T-- > 0){\n            int N = Integer.parseInt(br.readLine().trim());\n            Node[] nodes = new Node[N+1];\n            boolean[] isChild = new boolean[N+1];\n            for(int i=1;i<=N;i++) nodes[i]=new Node(i);\n            for(int i=0;i<N-1;i++){\n                st = new StringTokenizer(br.readLine());\n                int p = Integer.parseInt(st.nextToken());\n                int c = Integer.parseInt(st.nextToken());\n                char dir = st.nextToken().charAt(0);\n                if(dir=='L') nodes[p].left = nodes[c];\n                else nodes[p].right = nodes[c];\n                isChild[c]=true;\n            }\n            int root=1; for(int i=1;i<=N;i++) if(!isChild[i]){root=i; break;}\n            StringBuilder sb = new StringBuilder();\n            inorder(nodes[root], sb);\n            out.append(sb.toString().trim()).append('\\n');\n        }\n        System.out.print(out.toString());\n    }\n}\n",
      "Python3": "import sys\n\ndef inorder(node, out):\n    if node is None:\n        return\n    inorder(node[1], out)\n    out.append(str(node[0]))\n    inorder(node[2], out)\n\ndef solve():\n    data = sys.stdin.read().strip().split()\n    if not data:\n        return\n    it = iter(data)\n    T = int(next(it))\n    out_lines = []\n    for _ in range(T):\n        N = int(next(it))\n        nodes = [[i+1, None, None] for i in range(N)]  # [val, left, right]\n        is_child = [False]*N\n        for _ in range(N-1):\n            p = int(next(it)) - 1\n            c = int(next(it)) - 1\n            d = next(it)\n            if d == 'L':\n                nodes[p][1] = nodes[c]\n            else:\n                nodes[p][2] = nodes[c]\n            is_child[c] = True\n        root_idx = next(i for i, ch in enumerate(is_child) if not ch)\n        res = []\n        inorder(nodes[root_idx], res)\n        out_lines.append(' '.join(res))\n    sys.stdout.write('\\n'.join(out_lines))\n\nif __name__ == \"__main__\":\n    solve()\n",
      "JavaScript": "const fs = require('fs');\nconst data = fs.readFileSync(0, 'utf-8').trim().split(/\\s+/);\nlet idx = 0;\nif (data.length === 0) process.exit(0);\nconst T = parseInt(data[idx++]);\nlet outputs = [];\nfor (let t = 0; t < T; t++) {\n  const N = parseInt(data[idx++]);\n  const nodes = Array.from({ length: N }, (_, i) => ({ val: i + 1, left: null, right: null }));\n  const isChild = new Array(N).fill(false);\n  for (let i = 0; i < N - 1; i++) {\n    const p = parseInt(data[idx++]) - 1;\n    const c = parseInt(data[idx++]) - 1;\n    const dir = data[idx++];\n    if (dir === 'L') nodes[p].left = nodes[c];\n    else nodes[p].right = nodes[c];\n    isChild[c] = true;\n  }\n  let root = 0;\n  for (let i = 0; i < N; i++) if (!isChild[i]) { root = i; break; }\n  const res = [];\n  function inorder(node) {\n    if (!node) return;\n    inorder(node.left);\n    res.push(node.val);\n    inorder(node.right);\n  }\n  inorder(nodes[root]);\n  outputs.push(res.join(' '));\n}\nconsole.log(outputs.join('\\n'));\n"
    }
  },
  "QBCategory": "CCD",
  "boilerPlates": {
    "C": "#include <stdio.h>\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "C++": "#include <iostream>\nusing namespace std;\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "Java": "import java.util.*;\nimport java.io.*;\n\npublic class Main {\n    public static void main(String[] args) throws IOException {\n        // your code goes here\n    }\n}",
    "Python3": "# your code goes here\n",
    "JavaScript": "// your code goes here\n"
  },
  "_internal": {
    "methodName": "",
    "params": []
  }
}