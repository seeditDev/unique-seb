{
  "questionId": "PREACT0161",
  "title": "Implementing Tab Navigation",
  "slug": "preact0161",
  "version": 1,
  "createdAt": "2026-07-13",
  "updatedAt": "2026-07-13",
  "source": {
    "platform": "SEED-IT",
    "courseSlug": "react-js",
    "contestCode": "CREACT034",
    "module": "Project - Interactive Forms with Dynamic Tabs",
    "submodule": "Project - The Job Application Form",
    "contentType": "labs",
    "originalUrl": "https://www.codechef.com/learn/course/react-js/CREACT034/problems/PREACT0161"
  },
  "metadata": {
    "category": "Practice",
    "difficulty": "Beginner",
    "isPremium": false,
    "tags": [
      "javascript"
    ],
    "companyTags": [],
    "topicTags": [
      "Project - Interactive Forms with Dynamic Tabs",
      "Project - The Job Application Form"
    ],
    "QBCategory": "SEED-IT"
  },
  "content": {
    "problemStatement": "Based on our decisions, we'll now make the \"Previous\" and \"Next\" buttons actually switch between the tabs in the form.\n\n**Recap of Decisions:**\n\n*   The state controlling the active tab (`activeTabIndex`) will live in the `App` component.\n*   `App` will define functions (`handleNext`, `handlePrevious`) to update this state.\n*   These functions will be passed down as props (`onNext`, `onPrevious`) to the `Tabs` component.\n*   The `Tabs` component will call these functions when the buttons are clicked and use the `activeTabIndex` prop to determine which tab content to show and when to disable the buttons.\n\n**Your Task:**\n\n1.  **Manage Active Tab State in `App.js`:**\n    *   Import `useState` from `react`.\n    *   Inside the `App` component, create a state variable called `activeTabIndex` and its setter function (e.g., `setActiveTabIndex`) using `useState`. Initialize the state to `0`.\n    *   Define a constant `totalTabs` (set it to `3` for now) within `App` for easy reference.\n\n2.  **Create Navigation Handler Functions in `App.js`:**\n    *   Create an arrow function named `handleNext`. Inside this function:\n        *   Use `setActiveTabIndex` to update the state.\n        *   Calculate the new index by taking the previous index (`prevIndex`) and adding 1.\n        *   Use `Math.min()` to ensure the new index doesn't exceed `totalTabs - 1`.\n    *   Create an arrow function named `handlePrevious`. Inside this function:\n        *   Use `setActiveTabIndex` to update the state.\n        *   Calculate the new index by taking the previous index (`prevIndex`) and subtracting 1.\n        *   Use `Math.max()` to ensure the new index doesn't go below `0`.\n\n3.  **Pass State and Handlers to `Tabs`:**\n    *   Modify the `` component rendering within `App.js`:\n        *   Pass the `handleNext` function as a prop named `onNext`.\n        *   Pass the `handlePrevious` function as a prop named `onPrevious`.\n\n4.  **Update `Tabs.js` to Use Props:**\n    *   Modify the `Tabs` function signature to accept the new props: `{ activeTabIndex, onPrevious, onNext }`.\n    *   Inside the `Tabs` component, define `totalTabs = 3`.\n    *   Calculate boolean constants `isFirstTab` (`activeTabIndex === 0`) and `isLastTab` (`activeTabIndex === totalTabs - 1`).\n    *   Ensure all conditional logic (for header `active` class and `tab-content` display) now uses the `activeTabIndex` prop instead of `currentActiveTab`.\n\n5.  **Connect Buttons in `Tabs.js`:**\n    *   Find the \"Previous\" button in the `tab-navigation` section.\n        *   Add an `onClick` attribute and set it to the `onPrevious` prop.\n        *   Add a `disabled` attribute and set it to the `isFirstTab` constant.\n    *   Find the \"Next\" button.\n        *   Add an `onClick` attribute and set it to the `onNext` prop.\n        *   Add a `disabled` attribute and set it to the `isLastTab` constant.",
    "constraints": [
      "- $1 \\leq num \\leq 10^9$\n- $1 \\leq  k \\leq num.length$ (**num** is treated as its string representation.)"
    ],
    "inputFormat": "1. The first line contains an integer representing the maximum size of the queue.\n2. Subsequent lines contain commands to manipulate the queue:\n - **push **: Enqueue an integer value.\n - **pop**: Dequeue the front element.\n - **display**: Print all elements in the queue.\n - **exit**: Terminate the program.",
    "outputFormat": "- For **push** value: Output `Enqueued: value` if successful; otherwise, output `Queue is full, cannot enqueue value`.\n- For **pop**: Output `Dequeued: value` if successful; otherwise, output `Queue is empty, cannot dequeue`.\n- For **display**: Output `Queue elements: followed by the elements` or `Queue is empty` if there are none.",
    "sampleTestCases": []
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "input": "",
        "expectedOutput": "expected",
        "weight": 100
      }
    ],
    "totalTestCases": 1,
    "totalWeight": 100
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      100
    ],
    "maxScore": 100
  },
  "judging": {
    "timeLimitMs": 1000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "boilerPlates": {
    "C": "#include <stdio.h>\n\nint main() {\n    // Write your C code here\n    return 0;\n}",
    "C++": "#include <iostream>\nusing namespace std;\n\nint main() {\n    // Write your C++ code here\n    return 0;\n}",
    "Java": "import java.util.*;\nimport java.io.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        // Write your Java code here\n    }\n}",
    "Python3": "# Write your Python code here\n",
    "JavaScript": "// Write your JavaScript code here\n",
    "Go": "package main\n\nimport \"fmt\"\n\nfunc main() {\n    // Write your Go code here\n}",
    "Rust": "fn main() {\n    // Write your Rust code here\n}",
    "C#": "using System;\n\nclass Program {\n    static void Main() {\n        // Write your C# code here\n    }\n}",
    "SQL": "-- Write your SQL query here\n"
  }
}