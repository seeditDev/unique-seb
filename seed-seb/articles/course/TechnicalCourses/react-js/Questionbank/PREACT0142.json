{
  "questionId": "PREACT0142",
  "title": "HeadlessUI Portals",
  "slug": "preact0142",
  "version": 1,
  "createdAt": "2026-07-13",
  "updatedAt": "2026-07-13",
  "source": {
    "platform": "SEED-IT",
    "courseSlug": "react-js",
    "contestCode": "CREACT032",
    "module": "Advanced Patterns and Smarter State in React",
    "submodule": "Advanced UI and Safe State Updates in React",
    "contentType": "labs",
    "originalUrl": "https://www.codechef.com/learn/course/react-js/CREACT032/problems/PREACT0142"
  },
  "metadata": {
    "category": "Practice",
    "difficulty": "Beginner",
    "isPremium": false,
    "tags": [
      "javascript"
    ],
    "companyTags": [],
    "topicTags": [
      "Advanced Patterns and Smarter State in React",
      "Advanced UI and Safe State Updates in React"
    ],
    "QBCategory": "SEED-IT"
  },
  "content": {
    "problemStatement": "Imagine your React app is like a big treehouse built inside a single, large tree (``). Every component you create adds a branch or a room *inside* that main treehouse structure.\n\n```html\n\n\n  \n    \n  \n\n```\n\n**The Problem: Getting Stuck in the Treehouse**\n\nNow, let's say you build a `Modal` component. You want this modal to pop up *above everything else* on the screen, like a floating balloon tethered to the ground, not just another room *inside* the treehouse.\n\nYou might use CSS like `position: fixed` to make it float. Usually, this works great.\n\n*But*, sometimes, one of the *parent* components way up the tree (maybe a `Header` or a `Sidebar`) has some specific CSS styles (like `transform`, `filter`, or `will-change`). These styles can unexpectedly create a new boundary, like putting a glass ceiling on one of the treehouse levels.\n\nEven though your modal has `position: fixed`, it gets trapped *under* that glass ceiling! It can't float freely relative to the whole window anymore; it's stuck relative to that parent component with the tricky CSS. This breaks the modal's appearance and behavior.\n\nYou *don't* want to worry about whether some distant ancestor component might accidentally break your modal with an obscure CSS rule. That makes using the `Modal` component fragile and complicated.\n\n**The Solution: React Portals (Teleportation!)**\n\nReact Portals offer a clever solution: they let you *teleport* the *final HTML output* of a component to a *different location* in the DOM, outside the main React app treehouse.\n\nThink of it like this:\n1.  You still *define* your `Modal` component *inside* its parent (like the `Header`) in your React code. This keeps your code organized and logical (the button that opens the modal is right next to the modal component itself in the code).\n2.  But when React actually *renders* the modal to the browser's screen (the DOM), you use a Portal to tell React: \"Hey, don't put the HTML for this modal inside the `` treehouse. Instead, teleport it over to this *other* special spot I made.\"\n\n**How to Use Portals**\n\n1.  **Create a Target Spot in HTML:** First, you need to create that separate spot in your main `index.html` file, *outside* the main ``. It's common to use an ID like `modal-root`.\n\n    ```html\n    \n    \n    \n      \n        My App\n      \n      \n         \n         \n      \n    \n    ```\n\n2.  **Use `createPortal` in Your Component:** Inside the component you want to teleport (e.g., your `Modal` component), you import `createPortal` from `react-dom` and use it in your return statement.\n\n    ```jsx\n    // Modal.js\n    import React from 'react';\n    import { createPortal } from 'react-dom'; // Import from react-dom!\n\n    // Find the portal target DOM node (do this once, ideally)\n    const modalRoot = document.getElementById('modal-root');\n\n    function Modal({ children, title, handleDismiss }) {\n      // Your modal's JSX structure\n      const modalContent = (\n        \n          \n            {/* Prevent clicks inside the dialog from closing it */}\n             e.stopPropagation()}> \n              {title}\n              Close\n              \n              {children}\n            \n          \n        \n      );\n\n      // Use createPortal to teleport the modalContent\n      // It takes two arguments:\n      // 1. The React element(s) to render (your JSX)\n      // 2. The actual DOM node where you want to render them\n      return createPortal(modalContent, modalRoot);\n    }\n\n    export default Modal;\n    ```\n\n**What Happens Now?**\n\n*   **In your React Code:** The `` component still lives logically within its parent (e.g., ``). State, props, and context flow normally within the React tree.\n*   **In the Browser's DOM:** When you inspect the elements, you'll see the modal's HTML (`...`) rendered directly inside ``, completely separate from the main `` structure.\n\n**Now It's Your Turn: Try It in the `IDE`!**\\\nWe’ve already wired up this setup for you in the `IDE`. Go check out the code there—see how the `Modal` is defined and how it's using a portal to render cleanly outside the app root. Try modifying it, break it, fix it again—and watch how it behaves in different situations. This hands-on play will help the concept really click.\n\n\n\n**Why is this Better?**\n\nBecause the modal's HTML is now in ``, it's no longer a descendant of the `` (or any other component inside ``) in the *DOM tree*. This means those tricky CSS properties on ancestor components inside `` can no longer trap it! The modal is free to be positioned relative to the viewport using `position: fixed` without interference.\n\n**Key Takeaways:**\n\n1.  **Problem:** Ancestor CSS (like `transform`) can create stacking contexts that trap `position: fixed` elements like modals.\n2.  **Solution:** React Portals let you render a component's HTML output into a *different part* of the DOM tree.\n3.  **How:**\n    *   Add a target `` (e.g., ``) in your `index.html` outside ``.\n    *   Use `createPortal(jsx, domNode)` from `react-dom` in your component to specify *what* to render and *where* to render it in the DOM.\n4.  **Benefit:** Keeps your React component structure logical while allowing the rendered DOM elements to escape CSS traps, making components like modals more robust.\n\nYou won't use `createPortal` directly every day, especially if you use UI libraries (they often use portals internally for their Modals, Dropdowns, Tooltips, etc.), but knowing *how* and *why* they work is crucial for understanding how these common UI patterns are built reliably in React.",
    "constraints": [
      "- $1 \\leq T \\leq 100$\n- $1 \\leq N \\leq 100$\n- $1 \\leq M \\leq N\\cdot(N-1)/2$\n- $1 \\leq u_i, v_i \\leq N$\n- $u_i \\neq v_i$ for each $1 \\leq i \\leq M$.\n- The sum of $N$ over all test cases won't exceed $100$."
    ],
    "inputFormat": "- The first line of input will contain a single integer $T$, denoting the number of test cases.\n- Each test case consists of multiple lines of input.\n    - The first line of each test case contains two space-separated integers $N$ and $M$ — the number of vertices and edges of the graph, respectively.\n    - The next $M$ lines describe the edges. The $i$-th of these $M$ lines contains two space-separated integers $u_i$ and $v_i$, denoting an edge between $u_i$ and $v_i$.",
    "outputFormat": "For each test case, output on a new line the number of good subgraphs of $G$, modulo $10^9 + 7$.",
    "sampleTestCases": []
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "input": "",
        "expectedOutput": "expected",
        "weight": 100
      }
    ],
    "totalTestCases": 1,
    "totalWeight": 100
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      100
    ],
    "maxScore": 100
  },
  "judging": {
    "timeLimitMs": 1000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "boilerPlates": {
    "C": "#include <stdio.h>\n\nint main() {\n    // Write your C code here\n    return 0;\n}",
    "C++": "#include <iostream>\nusing namespace std;\n\nint main() {\n    // Write your C++ code here\n    return 0;\n}",
    "Java": "import java.util.*;\nimport java.io.*;\n\npublic class Main {\n    public static void main(String[] args) {\n        // Write your Java code here\n    }\n}",
    "Python3": "# Write your Python code here\n",
    "JavaScript": "// Write your JavaScript code here\n",
    "Go": "package main\n\nimport \"fmt\"\n\nfunc main() {\n    // Write your Go code here\n}",
    "Rust": "fn main() {\n    // Write your Rust code here\n}",
    "C#": "using System;\n\nclass Program {\n    static void Main() {\n        // Write your C# code here\n    }\n}",
    "SQL": "-- Write your SQL query here\n"
  }
}