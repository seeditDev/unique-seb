{
  "questionId": "Q10517",
  "title": "Good String",
  "slug": "good-string",
  "version": 1,
  "createdAt": "2026-07-02",
  "updatedAt": "2026-07-02",
  "metadata": {
    "category": "Strings",
    "difficulty": "Medium",
    "isPremium": true,
    "tags": [
      "strings",
      "competitive programming"
    ],
    "companyTags": [],
    "topicTags": [
      "Strings"
    ],
    "QBCategory": "CCD"
  },
  "content": {
    "problemStatement": "A picnic to a famous museum is being planned in a school for class VI. When they reached the spot, the students started quarreling among themselves in the queue. So the teacher came up with an idea of “good string” which is explained below.\n\nGood String is provided as input. All letters in this string are good letters. Good letters need to be used in further computations as explained below.\n\nThe teacher asked all the students to convert their names into good names with the help of good string. While converting, they have to calculate the distance. Based on that, she will arrange the students in a queue.\nFor converting a name into good name, for each letter i in the name, select the nearest letter from the good name. Distance is calculated as the differences between the ASCII values of i and selected good letter. If there are two letters which are equidistant from i, select the letter which is nearest to the previously used good letter. In that case, distance will be the difference of ASCII value of previously used good letter and selected letter. If i is already present in the good string then no need to change it. Initially, previous good letter will be the first letter of good string. Calculate the total distance of the given name.\n\nGiven the name of the student who is confused of implementing this task. Help him to calculate the total distance for his name.\n\nNote: Letters from good string can be reused any number of times.\n\n\nInput Format\n\nFirst line consists of good string.\nSecond line consists of the name of the student who is confused to implement the task.\n\n\nOutput Format\nPrint the total distance for that name.",
    "constraints": [
      "1≤1 \\leq1≤ len(good string) ≤100\\leq 100≤100",
      "1≤1 \\leq1≤ len(name) ≤104\\leq 10^4≤104",
      "-Good string will consist of lower, upper case alphabets, digits and symbols.",
      "-Name will consist of only space, lower and upper case alphabets.",
      "-Characters are case sensitive.",
      "The ASCIIASCIIASCII values for all the characters in the good string and name will be between 323232 to 126126126 (both inclusive)."
    ],
    "inputFormat": "",
    "outputFormat": "",
    "notes": [],
    "sampleTestCases": [
      {
        "id": "sample_1",
        "type": "reg",
        "input": "(@HR*i{kcQl\nVyom",
        "output": "10",
        "explanation": "Explanation:\ni\nPrevious good letter\nCurrent good letter for i\nV\n(\nR\ny\nR\n{\no\n{\nl\nm\nl\nl\nThe total distance will be |ASCII(V)-ASCII(R)| + |ASCII(y)-ASCII({)| + |ASCII(o)-ASCII(l)| + |ASCII(m)-ASCII(l)| = 4+2+3+1 = 10."
      },
      {
        "id": "sample_2",
        "type": "reg",
        "input": "6*K4AQf]gpi\nNainika",
        "output": "33",
        "explanation": "Explanation:\ni\nPrevious good letter\nCurrent good letter for i\nN\n6\nK\na\nK\n]\ni\n–\n–\nn\n]\np\ni\n–\n–\nk\np\ni\na\ni\n]\nInitially, Previous good letter=6. Since K and Q are at the same distance from N, so we select the character which is nearest to previous letter(6) which is K.\ni is already present in the good string. So no need to change anything.\nTherefore, total distance will be |ASCII(6)-ASCII(K)| + |ASCII(a)-ASCII(])| + |ASCII(n)-ASCII(p)| + |ASCII(k)-ASCII(i)| + |ASCII(a)-ASCII(])| = 21+4+2+2+4 = 33."
      }
    ]
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_01",
        "type": "reg",
        "label": "",
        "input": "(@HR*i{kcQl\nVyom",
        "expectedOutput": "10",
        "weight": 4
      },
      {
        "id": "tc_02",
        "type": "reg",
        "label": "",
        "input": "6*K4AQf]gpi\nNainika",
        "expectedOutput": "33",
        "weight": 4
      },
      {
        "id": "tc_03",
        "type": "reg",
        "label": "",
        "input": "(@HR*i{kcQl\nVyom",
        "expectedOutput": "10",
        "weight": 4
      },
      {
        "id": "tc_04",
        "type": "reg",
        "label": "",
        "input": "6*K4AQf]gpi\nNainika",
        "expectedOutput": "33",
        "weight": 4
      },
      {
        "id": "tc_05",
        "type": "reg",
        "label": "",
        "input": "(@HR*i{kcQl\nVyom",
        "expectedOutput": "10",
        "weight": 4
      },
      {
        "id": "tc_06",
        "type": "reg",
        "label": "",
        "input": "6*K4AQf]gpi\nNainika",
        "expectedOutput": "33",
        "weight": 4
      },
      {
        "id": "tc_07",
        "type": "reg",
        "label": "",
        "input": "(@HR*i{kcQl\nVyom",
        "expectedOutput": "10",
        "weight": 4
      },
      {
        "id": "tc_08",
        "type": "reg",
        "label": "",
        "input": "6*K4AQf]gpi\nNainika",
        "expectedOutput": "33",
        "weight": 4
      },
      {
        "id": "tc_09",
        "type": "reg",
        "label": "",
        "input": "(@HR*i{kcQl\nVyom",
        "expectedOutput": "10",
        "weight": 4
      },
      {
        "id": "tc_10",
        "type": "reg",
        "label": "",
        "input": "6*K4AQf]gpi\nNainika",
        "expectedOutput": "33",
        "weight": 4
      },
      {
        "id": "tc_11",
        "type": "reg",
        "label": "",
        "input": "(@HR*i{kcQl\nVyom",
        "expectedOutput": "10",
        "weight": 3
      },
      {
        "id": "tc_12",
        "type": "reg",
        "label": "",
        "input": "6*K4AQf]gpi\nNainika",
        "expectedOutput": "33",
        "weight": 3
      },
      {
        "id": "tc_13",
        "type": "reg",
        "label": "",
        "input": "(@HR*i{kcQl\nVyom",
        "expectedOutput": "10",
        "weight": 3
      },
      {
        "id": "tc_14",
        "type": "reg",
        "label": "",
        "input": "6*K4AQf]gpi\nNainika",
        "expectedOutput": "33",
        "weight": 3
      },
      {
        "id": "tc_15",
        "type": "reg",
        "label": "",
        "input": "(@HR*i{kcQl\nVyom",
        "expectedOutput": "10",
        "weight": 3
      },
      {
        "id": "tc_16",
        "type": "reg",
        "label": "",
        "input": "6*K4AQf]gpi\nNainika",
        "expectedOutput": "33",
        "weight": 3
      },
      {
        "id": "tc_17",
        "type": "reg",
        "label": "",
        "input": "(@HR*i{kcQl\nVyom",
        "expectedOutput": "10",
        "weight": 3
      },
      {
        "id": "tc_18",
        "type": "reg",
        "label": "",
        "input": "6*K4AQf]gpi\nNainika",
        "expectedOutput": "33",
        "weight": 3
      },
      {
        "id": "tc_19",
        "type": "reg",
        "label": "",
        "input": "(@HR*i{kcQl\nVyom",
        "expectedOutput": "10",
        "weight": 3
      },
      {
        "id": "tc_20",
        "type": "reg",
        "label": "",
        "input": "6*K4AQf]gpi\nNainika",
        "expectedOutput": "33",
        "weight": 3
      },
      {
        "id": "tc_21",
        "type": "reg",
        "label": "",
        "input": "(@HR*i{kcQl\nVyom",
        "expectedOutput": "10",
        "weight": 3
      },
      {
        "id": "tc_22",
        "type": "reg",
        "label": "",
        "input": "6*K4AQf]gpi\nNainika",
        "expectedOutput": "33",
        "weight": 3
      },
      {
        "id": "tc_23",
        "type": "reg",
        "label": "",
        "input": "(@HR*i{kcQl\nVyom",
        "expectedOutput": "10",
        "weight": 3
      },
      {
        "id": "tc_24",
        "type": "reg",
        "label": "",
        "input": "6*K4AQf]gpi\nNainika",
        "expectedOutput": "33",
        "weight": 3
      },
      {
        "id": "tc_25",
        "type": "reg",
        "label": "",
        "input": "(@HR*i{kcQl\nVyom",
        "expectedOutput": "10",
        "weight": 3
      },
      {
        "id": "tc_26",
        "type": "reg",
        "label": "",
        "input": "6*K4AQf]gpi\nNainika",
        "expectedOutput": "33",
        "weight": 3
      },
      {
        "id": "tc_27",
        "type": "reg",
        "label": "",
        "input": "(@HR*i{kcQl\nVyom",
        "expectedOutput": "10",
        "weight": 3
      },
      {
        "id": "tc_28",
        "type": "reg",
        "label": "",
        "input": "6*K4AQf]gpi\nNainika",
        "expectedOutput": "33",
        "weight": 3
      },
      {
        "id": "tc_29",
        "type": "reg",
        "label": "",
        "input": "(@HR*i{kcQl\nVyom",
        "expectedOutput": "10",
        "weight": 3
      },
      {
        "id": "tc_30",
        "type": "reg",
        "label": "",
        "input": "6*K4AQf]gpi\nNainika",
        "expectedOutput": "33",
        "weight": 3
      }
    ],
    "totalTestCases": 30,
    "totalWeight": 100
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      4,
      4,
      4,
      4,
      4,
      4,
      4,
      4,
      4,
      4,
      3,
      3,
      3,
      3,
      3,
      3,
      3,
      3,
      3,
      3,
      3,
      3,
      3,
      3,
      3,
      3,
      3,
      3,
      3,
      3
    ],
    "maxScore": 100
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ],
    "notes": []
  },
  "solution": {
    "approach": "",
    "timeComplexity": "",
    "spaceComplexity": "",
    "code": {
      "C": "#include <stdio.h>\n#include <stdlib.h>\n#include <limits.h>\n#include <string.h>\n\nint compare(const void *a, const void *b) {\n    return (*(int *)a - *(int *)b);  // Function to be used in qsort\n}\n\nint main() {\n    char s1[100], s2[10000];  // Input strings s1 and s2\n    scanf(\"%s\", s1);\n    scanf(\"%s\", s2);\n\n    int a[1000], n = strlen(s1);  // Array to store ASCII values of s1 and its length\n    for (int i = 0; i < n; i++) {\n        a[i] = (int)s1[i];  // Convert each character to ASCII value\n    }\n\n    // Sort the array 'a' to allow binary search\n    qsort(a, n, sizeof(int), compare);\n\n    int res = 0;  // Variable to store the result (sum of minimum differences)\n    int prev = (int)s1[0];  // Initialize prev to the ASCII of the first character in s1\n\n    // Iterate through each character in s2\n    for (int i = 0; i < strlen(s2); i++) {\n        int b = (int)s2[i];  // Get ASCII value of the character from s2\n        int l = 0, r = n - 1;  // Initialize binary search range\n        int good = INT_MAX;  // Initialize \"good\" to the maximum possible value\n        int curr = -1;  // Variable to store the current closest match\n\n        // Binary search to find the closest character in s1 to the current character of s2\n        while (l <= r) {\n            int mid = (l + r) / 2;\n\n            if (a[mid] == b) {  // If an exact match is found\n                curr = prev;\n                good = 0;  // No difference since it's an exact match\n                break;\n            } \n            else if (a[mid] < b) {  // If the character at mid is smaller than b\n                if (abs(a[mid] - b) < good) {\n                    good = abs(a[mid] - b);\n                    curr = a[mid];\n                } \n                else if (abs(a[mid] - b) == good) {\n                    if (abs(curr - prev) > abs(a[mid] - prev)) {\n                        curr = a[mid];\n                        good = abs(a[mid] - prev);\n                    } \n                    else {\n                        good = abs(curr - prev);\n                    }\n                }\n                l = mid + 1;\n            } \n            else {  // If the character at mid is larger than b\n                if (abs(a[mid] - b) < good) {\n                    good = abs(a[mid] - b);\n                    curr = a[mid];\n                } \n                else if (abs(a[mid] - b) == good) {\n                    if (abs(curr - prev) > abs(a[mid] - prev)) {\n                        curr = a[mid];\n                        good = abs(a[mid] - prev);\n                    } \n                    else {\n                        good = abs(curr - prev);\n                    }\n                }\n                r = mid - 1;\n            }\n        }\n        prev = curr;  // Update previous closest match\n        res += good;  // Add the closest difference to the result\n    }\n\n    printf(\"%d\", res);  // Output the final result\n}\n",
      "C++": "#include <bits/stdc++.h>\nusing namespace std;\n\nint main() {\n    string s1, s2;\n    \n    // Read two input strings\n    cin >> s1;\n    cin >> s2;\n    \n    int good = 0;  // Variable to track the difference between characters\n    vector<int> a;  // Vector to store ASCII values of characters from s1\n\n    // Convert characters of s1 into their ASCII values and store them in vector 'a'\n    for(auto i : s1) {\n        a.push_back((int)i);\n    }\n\n    // Sort the vector 'a' to prepare for binary search\n    sort(a.begin(), a.end());\n    \n    int res = 0;  // Variable to store the final result, i.e., total \"good\" values\n    int prev = (int)s1[0];  // Store the ASCII value of the first character of s1 as the initial 'previous' value\n\n    // Loop through each character of string s2\n    for(auto i : s2) {\n        int b = (int)i;  // Convert character 'i' from s2 to its ASCII value 'b'\n        \n        int l = 0, r = a.size() - 1;  // Initialize the binary search boundaries\n        int good = INT_MAX;  // Initialize 'good' to a large value to find the minimum difference\n        int curr = -1;  // Initialize 'curr' to track the closest character from s1\n\n        // Binary search for the closest character to 'b' in the sorted vector 'a'\n        while(l <= r) {\n            int mid = (l + r) / 2;  // Find the middle index\n            \n            if(a[mid] == b) {  // If an exact match is found\n                curr = prev;  // Update curr to the value of 'prev'\n                good = 0;  // No difference if the characters match exactly\n                break;  // Exit the loop as we found an exact match\n            }\n            else if(a[mid] < b) {  // If the middle element is less than 'b'\n                // Check if the difference between 'a[mid]' and 'b' is smaller than the current 'good'\n                if(abs(a[mid] - b) < good) {\n                    good = abs(a[mid] - b);  // Update 'good' with the smaller difference\n                    curr = a[mid];  // Update 'curr' with the current closest value\n                }\n                // If the difference is equal but 'a[mid]' is closer to 'prev', update 'curr' and 'good'\n                else if(abs(a[mid] - b) == good) {\n                    if(abs(curr - prev) > abs(a[mid] - prev)) {\n                        curr = a[mid];  // Update 'curr' to the closer value\n                        good = abs(a[mid] - prev);  // Update 'good' to the closer value\n                    }\n                    else {\n                        good = abs(curr - prev);  // Keep 'good' unchanged if the current closest is better\n                    }\n                }\n                l = mid + 1;  // Move to the right half of the array\n            }\n            else {  // If the middle element is greater than 'b'\n                // Same logic as above but in reverse, as we now check the left side of the array\n                if(abs(a[mid] - b) < good) {\n                    good = abs(a[mid] - b);\n                    curr = a[mid];\n                }\n                else if(abs(a[mid] - b) == good) {\n                    if(abs(curr - prev) > abs(a[mid] - prev)) {\n                        curr = a[mid];\n                        good = abs(a[mid] - prev);\n                    }\n                    else {\n                        good = abs(curr - prev);\n                    }\n                }\n                r = mid - 1;  // Move to the left half of the array\n            }\n        }\n\n        prev = curr;  // Update 'prev' to the closest character found in this iteration\n        res += good;  // Add the \"good\" value (difference) to the result\n    }\n\n    // Output the total sum of \"good\" values\n    cout << res;\n}\n",
      "Java": "import java.util.*;  // Importing necessary packages\nimport java.lang.*;  // Importing the java.lang package\nimport java.io.*;    // Importing the java.io package for input-output handling\n\nclass Codechef {\n    public static void main(String[] args) throws java.lang.Exception {\n        // BufferedReader is used for efficient input handling\n        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));\n        \n        // Reading the input strings s1 and s2\n        String s1 = br.readLine();  // First input string\n        String s2 = br.readLine();  // Second input string\n        \n        // Initialize a variable to store the minimum difference between characters\n        int good = 0;\n        \n        // List 'a' will store the ASCII values of characters in s1\n        List<Integer> a = new ArrayList<>();\n        \n        // Convert each character of s1 to its ASCII value and add it to the list 'a'\n        for (char i : s1.toCharArray()) {\n            a.add((int) i);\n        }\n        \n        // Sort the list 'a' to allow binary search later\n        Collections.sort(a);\n        \n        // Initialize the result variable 'res' to store the sum of minimum differences\n        int res = 0;\n        \n        // Initialize 'prev' to the ASCII value of the first character in s1\n        int prev = (int) s1.charAt(0);\n        \n        // Iterate through each character in the string s2\n        for (char i : s2.toCharArray()) {\n            int b = (int) i;  // Convert the current character of s2 to its ASCII value\n            int l = 0;  // Left pointer for binary search\n            int r = a.size() - 1;  // Right pointer for binary search\n            int minGood = Integer.MAX_VALUE;  // Initialize minGood to the maximum possible integer value\n            int curr = -1;  // Variable to store the closest matching character in ASCII\n            \n            // Perform binary search to find the closest ASCII value in list 'a' for character 'b'\n            while (l <= r) {\n                int mid = (l + r) / 2;  // Calculate the middle index\n                \n                // If exact match found, break out of the loop\n                if (a.get(mid) == b) {\n                    curr = prev;  // Set curr to the previous closest match\n                    minGood = 0;  // Since it's an exact match, no difference (minGood = 0)\n                    break;\n                } \n                // If the value at mid is less than b, search in the right half\n                else if (a.get(mid) < b) {\n                    // Update the minimum difference and closest match if a closer value is found\n                    if (Math.abs(a.get(mid) - b) < minGood) {\n                        minGood = Math.abs(a.get(mid) - b);\n                        curr = a.get(mid);\n                    } \n                    // If the difference is equal, choose the one closer to 'prev'\n                    else if (Math.abs(a.get(mid) - b) == minGood) {\n                        if (Math.abs(curr - prev) > Math.abs(a.get(mid) - prev)) {\n                            curr = a.get(mid);\n                            minGood = Math.abs(a.get(mid) - prev);\n                        } else {\n                            minGood = Math.abs(curr - prev);\n                        }\n                    }\n                    l = mid + 1;  // Move the left pointer\n                } \n                // If the value at mid is greater than b, search in the left half\n                else {\n                    if (Math.abs(a.get(mid) - b) < minGood) {\n                        minGood = Math.abs(a.get(mid) - b);\n                        curr = a.get(mid);\n                    } \n                    // If the difference is equal, choose the one closer to 'prev'\n                    else if (Math.abs(a.get(mid) - b) == minGood) {\n                        if (Math.abs(curr - prev) > Math.abs(a.get(mid) - prev)) {\n                            curr = a.get(mid);\n                            minGood = Math.abs(a.get(mid) - prev);\n                        } else {\n                            minGood = Math.abs(curr - prev);\n                        }\n                    }\n                    r = mid - 1;  // Move the right pointer\n                }\n            }\n            prev = curr;  // Update 'prev' to the closest matching character found in this iteration\n            res += minGood;  // Add the minimum difference to the result\n        }\n        \n        // Output the result (total minimum difference between characters of s2 and s1)\n        System.out.println(res);\n    }\n}\n",
      "Python3": "def main():\n    # Reading two input strings\n    s1 = input().strip()  # First string\n    s2 = input().strip()  # Second string\n    \n    # Converting characters in s1 to their ASCII values and sorting them\n    a = [ord(i) for i in s1]  # List of ASCII values of characters in s1\n    a.sort()  # Sort the ASCII values to enable binary search\n    \n    res = 0  # Variable to store the total result (sum of minimum differences)\n    prev = ord(s1[0])  # ASCII value of the first character of s1, used to track the previous character\n    \n    # Iterate through each character in s2\n    for i in s2:\n        b = ord(i)  # Get the ASCII value of the character in s2\n        l, r = 0, len(a) - 1  # Initialize binary search boundaries\n        good = float('inf')  # Set 'good' to a very large number, simulating INT_MAX\n        curr = -1  # To store the closest match found during binary search\n        \n        # Perform binary search to find the closest character to `b` in sorted list `a`\n        while l <= r:\n            mid = (l + r) // 2  # Calculate the middle index\n            \n            if a[mid] == b:  # If an exact match is found\n                curr = prev  # Update curr to the value of 'prev'\n                good = 0  # No difference since it's an exact match\n                break  # Exit the binary search loop\n            \n            # If the middle element is less than `b`, search the right half\n            elif a[mid] < b:\n                # Check if the current character in `a` is closer to `b` than the previous best\n                if abs(a[mid] - b) < good:\n                    good = abs(a[mid] - b)  # Update 'good' to the smaller difference\n                    curr = a[mid]  # Update 'curr' to the current closest value\n                # If the difference is the same, check if this element is closer to 'prev'\n                elif abs(a[mid] - b) == good:\n                    if abs(curr - prev) > abs(a[mid] - prev):\n                        curr = a[mid]  # Update 'curr' to the value closer to 'prev'\n                        good = abs(a[mid] - prev)  # Update 'good' accordingly\n                    else:\n                        good = abs(curr - prev)  # Keep 'good' unchanged\n                l = mid + 1  # Move to the right half of the array\n            # If the middle element is greater than `b`, search the left half\n            else:\n                # Similar logic for the left side of the array\n                if abs(a[mid] - b) < good:\n                    good = abs(a[mid] - b)\n                    curr = a[mid]\n                elif abs(a[mid] - b) == good:\n                    if abs(curr - prev) > abs(a[mid] - prev):\n                        curr = a[mid]\n                        good = abs(a[mid] - prev)\n                    else:\n                        good = abs(curr - prev)\n                r = mid - 1  # Move to the left half of the array\n        \n        # After the binary search, update `prev` to the closest character found\n        prev = curr\n        \n        # Add the minimum difference (good) to the result\n        res += good\n    \n    # Output the total sum of differences (result)\n    print(res)\n\n\nif __name__ == \"__main__\":\n    main()\n",
      "JavaScript": "const readline = require('readline');\n\n// Create an interface to read input from the command line\nconst rl = readline.createInterface({\n    input: process.stdin,\n    output: process.stdout\n});\n\nconst input = [];\n\n// Read input line by line\nrl.on('line', line => {\n    input.push(line);\n});\n\n// After the input is complete, process the data\nrl.on('close', () => {\n    // Read the two input strings s1 and s2\n    const s1 = input[0];\n    const s2 = input[1];\n\n    // Convert characters of s1 to their ASCII values and store them in an array\n    const a = [];\n    for (let i = 0; i < s1.length; i++) {\n        a.push(s1.charCodeAt(i));  // Convert each character to its ASCII value\n    }\n\n    // Sort the array of ASCII values\n    a.sort((x, y) => x - y); // Sort in ascending order\n\n    let res = 0;  // To store the result\n    let prev = s1.charCodeAt(0);  // Initialize previous character's ASCII value\n\n    // Iterate over each character in s2\n    for (let i = 0; i < s2.length; i++) {\n        let b = s2.charCodeAt(i);  // Get the ASCII value of the current character\n        let l = 0, r = a.length - 1;  // Binary search variables\n        let minGood = Number.MAX_VALUE;  // To store the minimum difference (good)\n        let curr = -1;  // To store the closest match\n\n        // Perform binary search to find the closest match for character b in array a\n        while (l <= r) {\n            let mid = Math.floor((l + r) / 2);\n            \n            // If exact match is found\n            if (a[mid] === b) {\n                curr = prev;\n                minGood = 0;  // No difference if characters are identical\n                break;\n            }\n            // If mid value is less than b, search the right half\n            else if (a[mid] < b) {\n                // Check if the current element is closer to b\n                if (Math.abs(a[mid] - b) < minGood) {\n                    minGood = Math.abs(a[mid] - b);\n                    curr = a[mid];\n                }\n                // Handle ties by comparing to the previous character\n                else if (Math.abs(a[mid] - b) === minGood) {\n                    if (Math.abs(curr - prev) > Math.abs(a[mid] - prev)) {\n                        curr = a[mid];\n                        minGood = Math.abs(a[mid] - prev);\n                    } else {\n                        minGood = Math.abs(curr - prev);\n                    }\n                }\n                l = mid + 1;  // Move to the right half\n            } \n            // If mid value is greater than b, search the left half\n            else {\n                if (Math.abs(a[mid] - b) < minGood) {\n                    minGood = Math.abs(a[mid] - b);\n                    curr = a[mid];\n                }\n                else if (Math.abs(a[mid] - b) === minGood) {\n                    if (Math.abs(curr - prev) > Math.abs(a[mid] - prev)) {\n                        curr = a[mid];\n                        minGood = Math.abs(a[mid] - prev);\n                    } else {\n                        minGood = Math.abs(curr - prev);\n                    }\n                }\n                r = mid - 1;  // Move to the left half\n            }\n        }\n        prev = curr;  // Update previous character\n        res += minGood;  // Add the minimum difference to the result\n    }\n\n    // Output the final result\n    console.log(res);\n});\n"
    },
    "notes": [],
    "editorial": {
      "bruteForce": {
        "description": "",
        "complexity": ""
      },
      "better": {
        "description": "",
        "complexity": ""
      },
      "optimal": {
        "description": "",
        "complexity": ""
      }
    }
  },
  "hints": [],
  "followUps": [],
  "learningObjectives": [],
  "similarProblems": [],
  "acceptanceRate": null,
  "estimatedSolveTime": "",
  "recommendedAfter": [],
  "boilerPlates": {
    "C": "#include <stdio.h>\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "C++": "#include <iostream>\nusing namespace std;\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "Java": "import java.util.*;\nimport java.io.*;\n\npublic class Main {\n    public static void main(String[] args) throws IOException {\n        // your code goes here\n    }\n}",
    "Python3": "# your code goes here\n",
    "JavaScript": "// your code goes here\n",
    "solution": {
      "approach": "",
      "timeComplexity": "",
      "spaceComplexity": "",
      "code": {
        "C": "#include <stdio.h>\n#include <stdlib.h>\n#include <limits.h>\n#include <string.h>\n\nint compare(const void *a, const void *b) {\n    return (*(int *)a - *(int *)b);  // Function to be used in qsort\n}\n\nint main() {\n    char s1[100], s2[10000];  // Input strings s1 and s2\n    scanf(\"%s\", s1);\n    scanf(\"%s\", s2);\n\n    int a[1000], n = strlen(s1);  // Array to store ASCII values of s1 and its length\n    for (int i = 0; i < n; i++) {\n        a[i] = (int)s1[i];  // Convert each character to ASCII value\n    }\n\n    // Sort the array 'a' to allow binary search\n    qsort(a, n, sizeof(int), compare);\n\n    int res = 0;  // Variable to store the result (sum of minimum differences)\n    int prev = (int)s1[0];  // Initialize prev to the ASCII of the first character in s1\n\n    // Iterate through each character in s2\n    for (int i = 0; i < strlen(s2); i++) {\n        int b = (int)s2[i];  // Get ASCII value of the character from s2\n        int l = 0, r = n - 1;  // Initialize binary search range\n        int good = INT_MAX;  // Initialize \"good\" to the maximum possible value\n        int curr = -1;  // Variable to store the current closest match\n\n        // Binary search to find the closest character in s1 to the current character of s2\n        while (l <= r) {\n            int mid = (l + r) / 2;\n\n            if (a[mid] == b) {  // If an exact match is found\n                curr = prev;\n                good = 0;  // No difference since it's an exact match\n                break;\n            } \n            else if (a[mid] < b) {  // If the character at mid is smaller than b\n                if (abs(a[mid] - b) < good) {\n                    good = abs(a[mid] - b);\n                    curr = a[mid];\n                } \n                else if (abs(a[mid] - b) == good) {\n                    if (abs(curr - prev) > abs(a[mid] - prev)) {\n                        curr = a[mid];\n                        good = abs(a[mid] - prev);\n                    } \n                    else {\n                        good = abs(curr - prev);\n                    }\n                }\n                l = mid + 1;\n            } \n            else {  // If the character at mid is larger than b\n                if (abs(a[mid] - b) < good) {\n                    good = abs(a[mid] - b);\n                    curr = a[mid];\n                } \n                else if (abs(a[mid] - b) == good) {\n                    if (abs(curr - prev) > abs(a[mid] - prev)) {\n                        curr = a[mid];\n                        good = abs(a[mid] - prev);\n                    } \n                    else {\n                        good = abs(curr - prev);\n                    }\n                }\n                r = mid - 1;\n            }\n        }\n        prev = curr;  // Update previous closest match\n        res += good;  // Add the closest difference to the result\n    }\n\n    printf(\"%d\", res);  // Output the final result\n}\n",
        "C++": "#include <bits/stdc++.h>\nusing namespace std;\n\nint main() {\n    string s1, s2;\n    \n    // Read two input strings\n    cin >> s1;\n    cin >> s2;\n    \n    int good = 0;  // Variable to track the difference between characters\n    vector<int> a;  // Vector to store ASCII values of characters from s1\n\n    // Convert characters of s1 into their ASCII values and store them in vector 'a'\n    for(auto i : s1) {\n        a.push_back((int)i);\n    }\n\n    // Sort the vector 'a' to prepare for binary search\n    sort(a.begin(), a.end());\n    \n    int res = 0;  // Variable to store the final result, i.e., total \"good\" values\n    int prev = (int)s1[0];  // Store the ASCII value of the first character of s1 as the initial 'previous' value\n\n    // Loop through each character of string s2\n    for(auto i : s2) {\n        int b = (int)i;  // Convert character 'i' from s2 to its ASCII value 'b'\n        \n        int l = 0, r = a.size() - 1;  // Initialize the binary search boundaries\n        int good = INT_MAX;  // Initialize 'good' to a large value to find the minimum difference\n        int curr = -1;  // Initialize 'curr' to track the closest character from s1\n\n        // Binary search for the closest character to 'b' in the sorted vector 'a'\n        while(l <= r) {\n            int mid = (l + r) / 2;  // Find the middle index\n            \n            if(a[mid] == b) {  // If an exact match is found\n                curr = prev;  // Update curr to the value of 'prev'\n                good = 0;  // No difference if the characters match exactly\n                break;  // Exit the loop as we found an exact match\n            }\n            else if(a[mid] < b) {  // If the middle element is less than 'b'\n                // Check if the difference between 'a[mid]' and 'b' is smaller than the current 'good'\n                if(abs(a[mid] - b) < good) {\n                    good = abs(a[mid] - b);  // Update 'good' with the smaller difference\n                    curr = a[mid];  // Update 'curr' with the current closest value\n                }\n                // If the difference is equal but 'a[mid]' is closer to 'prev', update 'curr' and 'good'\n                else if(abs(a[mid] - b) == good) {\n                    if(abs(curr - prev) > abs(a[mid] - prev)) {\n                        curr = a[mid];  // Update 'curr' to the closer value\n                        good = abs(a[mid] - prev);  // Update 'good' to the closer value\n                    }\n                    else {\n                        good = abs(curr - prev);  // Keep 'good' unchanged if the current closest is better\n                    }\n                }\n                l = mid + 1;  // Move to the right half of the array\n            }\n            else {  // If the middle element is greater than 'b'\n                // Same logic as above but in reverse, as we now check the left side of the array\n                if(abs(a[mid] - b) < good) {\n                    good = abs(a[mid] - b);\n                    curr = a[mid];\n                }\n                else if(abs(a[mid] - b) == good) {\n                    if(abs(curr - prev) > abs(a[mid] - prev)) {\n                        curr = a[mid];\n                        good = abs(a[mid] - prev);\n                    }\n                    else {\n                        good = abs(curr - prev);\n                    }\n                }\n                r = mid - 1;  // Move to the left half of the array\n            }\n        }\n\n        prev = curr;  // Update 'prev' to the closest character found in this iteration\n        res += good;  // Add the \"good\" value (difference) to the result\n    }\n\n    // Output the total sum of \"good\" values\n    cout << res;\n}\n",
        "Java": "import java.util.*;  // Importing necessary packages\nimport java.lang.*;  // Importing the java.lang package\nimport java.io.*;    // Importing the java.io package for input-output handling\n\nclass Codechef {\n    public static void main(String[] args) throws java.lang.Exception {\n        // BufferedReader is used for efficient input handling\n        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));\n        \n        // Reading the input strings s1 and s2\n        String s1 = br.readLine();  // First input string\n        String s2 = br.readLine();  // Second input string\n        \n        // Initialize a variable to store the minimum difference between characters\n        int good = 0;\n        \n        // List 'a' will store the ASCII values of characters in s1\n        List<Integer> a = new ArrayList<>();\n        \n        // Convert each character of s1 to its ASCII value and add it to the list 'a'\n        for (char i : s1.toCharArray()) {\n            a.add((int) i);\n        }\n        \n        // Sort the list 'a' to allow binary search later\n        Collections.sort(a);\n        \n        // Initialize the result variable 'res' to store the sum of minimum differences\n        int res = 0;\n        \n        // Initialize 'prev' to the ASCII value of the first character in s1\n        int prev = (int) s1.charAt(0);\n        \n        // Iterate through each character in the string s2\n        for (char i : s2.toCharArray()) {\n            int b = (int) i;  // Convert the current character of s2 to its ASCII value\n            int l = 0;  // Left pointer for binary search\n            int r = a.size() - 1;  // Right pointer for binary search\n            int minGood = Integer.MAX_VALUE;  // Initialize minGood to the maximum possible integer value\n            int curr = -1;  // Variable to store the closest matching character in ASCII\n            \n            // Perform binary search to find the closest ASCII value in list 'a' for character 'b'\n            while (l <= r) {\n                int mid = (l + r) / 2;  // Calculate the middle index\n                \n                // If exact match found, break out of the loop\n                if (a.get(mid) == b) {\n                    curr = prev;  // Set curr to the previous closest match\n                    minGood = 0;  // Since it's an exact match, no difference (minGood = 0)\n                    break;\n                } \n                // If the value at mid is less than b, search in the right half\n                else if (a.get(mid) < b) {\n                    // Update the minimum difference and closest match if a closer value is found\n                    if (Math.abs(a.get(mid) - b) < minGood) {\n                        minGood = Math.abs(a.get(mid) - b);\n                        curr = a.get(mid);\n                    } \n                    // If the difference is equal, choose the one closer to 'prev'\n                    else if (Math.abs(a.get(mid) - b) == minGood) {\n                        if (Math.abs(curr - prev) > Math.abs(a.get(mid) - prev)) {\n                            curr = a.get(mid);\n                            minGood = Math.abs(a.get(mid) - prev);\n                        } else {\n                            minGood = Math.abs(curr - prev);\n                        }\n                    }\n                    l = mid + 1;  // Move the left pointer\n                } \n                // If the value at mid is greater than b, search in the left half\n                else {\n                    if (Math.abs(a.get(mid) - b) < minGood) {\n                        minGood = Math.abs(a.get(mid) - b);\n                        curr = a.get(mid);\n                    } \n                    // If the difference is equal, choose the one closer to 'prev'\n                    else if (Math.abs(a.get(mid) - b) == minGood) {\n                        if (Math.abs(curr - prev) > Math.abs(a.get(mid) - prev)) {\n                            curr = a.get(mid);\n                            minGood = Math.abs(a.get(mid) - prev);\n                        } else {\n                            minGood = Math.abs(curr - prev);\n                        }\n                    }\n                    r = mid - 1;  // Move the right pointer\n                }\n            }\n            prev = curr;  // Update 'prev' to the closest matching character found in this iteration\n            res += minGood;  // Add the minimum difference to the result\n        }\n        \n        // Output the result (total minimum difference between characters of s2 and s1)\n        System.out.println(res);\n    }\n}\n",
        "Python3": "def main():\n    # Reading two input strings\n    s1 = input().strip()  # First string\n    s2 = input().strip()  # Second string\n    \n    # Converting characters in s1 to their ASCII values and sorting them\n    a = [ord(i) for i in s1]  # List of ASCII values of characters in s1\n    a.sort()  # Sort the ASCII values to enable binary search\n    \n    res = 0  # Variable to store the total result (sum of minimum differences)\n    prev = ord(s1[0])  # ASCII value of the first character of s1, used to track the previous character\n    \n    # Iterate through each character in s2\n    for i in s2:\n        b = ord(i)  # Get the ASCII value of the character in s2\n        l, r = 0, len(a) - 1  # Initialize binary search boundaries\n        good = float('inf')  # Set 'good' to a very large number, simulating INT_MAX\n        curr = -1  # To store the closest match found during binary search\n        \n        # Perform binary search to find the closest character to `b` in sorted list `a`\n        while l <= r:\n            mid = (l + r) // 2  # Calculate the middle index\n            \n            if a[mid] == b:  # If an exact match is found\n                curr = prev  # Update curr to the value of 'prev'\n                good = 0  # No difference since it's an exact match\n                break  # Exit the binary search loop\n            \n            # If the middle element is less than `b`, search the right half\n            elif a[mid] < b:\n                # Check if the current character in `a` is closer to `b` than the previous best\n                if abs(a[mid] - b) < good:\n                    good = abs(a[mid] - b)  # Update 'good' to the smaller difference\n                    curr = a[mid]  # Update 'curr' to the current closest value\n                # If the difference is the same, check if this element is closer to 'prev'\n                elif abs(a[mid] - b) == good:\n                    if abs(curr - prev) > abs(a[mid] - prev):\n                        curr = a[mid]  # Update 'curr' to the value closer to 'prev'\n                        good = abs(a[mid] - prev)  # Update 'good' accordingly\n                    else:\n                        good = abs(curr - prev)  # Keep 'good' unchanged\n                l = mid + 1  # Move to the right half of the array\n            # If the middle element is greater than `b`, search the left half\n            else:\n                # Similar logic for the left side of the array\n                if abs(a[mid] - b) < good:\n                    good = abs(a[mid] - b)\n                    curr = a[mid]\n                elif abs(a[mid] - b) == good:\n                    if abs(curr - prev) > abs(a[mid] - prev):\n                        curr = a[mid]\n                        good = abs(a[mid] - prev)\n                    else:\n                        good = abs(curr - prev)\n                r = mid - 1  # Move to the left half of the array\n        \n        # After the binary search, update `prev` to the closest character found\n        prev = curr\n        \n        # Add the minimum difference (good) to the result\n        res += good\n    \n    # Output the total sum of differences (result)\n    print(res)\n\n\nif __name__ == \"__main__\":\n    main()\n",
        "JavaScript": "const readline = require('readline');\n\n// Create an interface to read input from the command line\nconst rl = readline.createInterface({\n    input: process.stdin,\n    output: process.stdout\n});\n\nconst input = [];\n\n// Read input line by line\nrl.on('line', line => {\n    input.push(line);\n});\n\n// After the input is complete, process the data\nrl.on('close', () => {\n    // Read the two input strings s1 and s2\n    const s1 = input[0];\n    const s2 = input[1];\n\n    // Convert characters of s1 to their ASCII values and store them in an array\n    const a = [];\n    for (let i = 0; i < s1.length; i++) {\n        a.push(s1.charCodeAt(i));  // Convert each character to its ASCII value\n    }\n\n    // Sort the array of ASCII values\n    a.sort((x, y) => x - y); // Sort in ascending order\n\n    let res = 0;  // To store the result\n    let prev = s1.charCodeAt(0);  // Initialize previous character's ASCII value\n\n    // Iterate over each character in s2\n    for (let i = 0; i < s2.length; i++) {\n        let b = s2.charCodeAt(i);  // Get the ASCII value of the current character\n        let l = 0, r = a.length - 1;  // Binary search variables\n        let minGood = Number.MAX_VALUE;  // To store the minimum difference (good)\n        let curr = -1;  // To store the closest match\n\n        // Perform binary search to find the closest match for character b in array a\n        while (l <= r) {\n            let mid = Math.floor((l + r) / 2);\n            \n            // If exact match is found\n            if (a[mid] === b) {\n                curr = prev;\n                minGood = 0;  // No difference if characters are identical\n                break;\n            }\n            // If mid value is less than b, search the right half\n            else if (a[mid] < b) {\n                // Check if the current element is closer to b\n                if (Math.abs(a[mid] - b) < minGood) {\n                    minGood = Math.abs(a[mid] - b);\n                    curr = a[mid];\n                }\n                // Handle ties by comparing to the previous character\n                else if (Math.abs(a[mid] - b) === minGood) {\n                    if (Math.abs(curr - prev) > Math.abs(a[mid] - prev)) {\n                        curr = a[mid];\n                        minGood = Math.abs(a[mid] - prev);\n                    } else {\n                        minGood = Math.abs(curr - prev);\n                    }\n                }\n                l = mid + 1;  // Move to the right half\n            } \n            // If mid value is greater than b, search the left half\n            else {\n                if (Math.abs(a[mid] - b) < minGood) {\n                    minGood = Math.abs(a[mid] - b);\n                    curr = a[mid];\n                }\n                else if (Math.abs(a[mid] - b) === minGood) {\n                    if (Math.abs(curr - prev) > Math.abs(a[mid] - prev)) {\n                        curr = a[mid];\n                        minGood = Math.abs(a[mid] - prev);\n                    } else {\n                        minGood = Math.abs(curr - prev);\n                    }\n                }\n                r = mid - 1;  // Move to the left half\n            }\n        }\n        prev = curr;  // Update previous character\n        res += minGood;  // Add the minimum difference to the result\n    }\n\n    // Output the final result\n    console.log(res);\n});\n"
      }
    },
    "_internal": {
      "methodName": "",
      "params": [],
      "phase": "ready-for-judge"
    },
    "verified": true
  },
  "_internal": {},
  "verified": false
}
