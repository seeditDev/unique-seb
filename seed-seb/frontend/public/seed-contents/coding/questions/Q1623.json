{
  "questionId": "Q1623",
  "title": "Search in Rotated Sorted Array",
  "slug": "search-in-rotated-sorted-array",
  "version": 1,
  "createdAt": "2026-07-01",
  "updatedAt": "2026-07-04",
  "metadata": {
    "category": "Searching",
    "difficulty": "Hard",
    "isPremium": false,
    "tags": [
      "Binary Search",
      "Searching",
      "Rotated Sorted Array"
    ],
    "companyTags": [],
    "topicTags": [
      "Binary Search"
    ]
  },
  "content": {
    "problemStatement": "You are given a sorted array that has been rotated to the right by some number of steps. Your task is to find the index of a given element in this array.\n\nFor example, if a sorted array [1,2,3,4,5] is rotated by 2 positions, it becomes [4,5,1,2,3]. Despite being rotated, the array maintains some order that can be used to search efficiently.\n\n\nInput Format\n\nFirst line contains TTT - the number of test cases\nFor each test case:\n\nFirst line contains two space-separated integers NNN and KKK - size of array and element to find\nSecond line contains NNN space-separated integers A1,A2,...,ANA_1, A_2, ..., A_NA1\u200b,A2\u200b,...,AN\u200b - the rotated array elements\n\n\n\n\nOutput Format\n\nFor each test case, print a single integer - the index of KKK in the array (0-based indexing), or -1 if KKK is not found",
    "constraints": [
      "1 <= T <= 100",
      "1 <= N <= 5000",
      "0 <= Ai <= 100000",
      "0 <= K <= 100000"
    ],
    "inputFormat": "First line contains T, the number of test cases. For each test case, the first line contains two space-separated integers N and K \u2014 the size of the rotated sorted array and the element to search for. The second line contains N space-separated integers representing the rotated array.",
    "outputFormat": "For each test case, print a single integer on its own line \u2014 the 0-based index of K in the array, or -1 if K does not appear.",
    "sampleTestCases": [
      {
        "id": "sample_1",
        "input": "4\n5 2\n12 15 18 2 4\n4 3\n3 4 1 2\n6 6\n6 7 1 2 3 4\n5 5\n1 2 3 4 5",
        "output": "3\n0\n0\n4",
        "explanation": "Each test case searches for K in the given rotated array. The returned indices are 3, 0, 0 and 4 respectively."
      },
      {
        "id": "sample_2",
        "input": "2\n5 3\n1 2 3 4 5\n5 3\n5 4 3 2 1",
        "output": "2\n-1",
        "explanation": "In the first test case, the array is not rotated and the element 3 is found at index 2. In the second test case, the array is rotated and the element 3 is not found."
      },
      {
        "id": "sample_3",
        "input": "1\\n5 5\\n1 2 3 4 5",
        "output": "4",
        "explanation": "The element 5 is found at index 4 in the given array."
      }
    ]
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "label": "minimum boundary",
        "input": "1\n1 1\n1",
        "expectedOutput": "0",
        "weight": 2
      },
      {
        "id": "tc_2",
        "label": "Case 2",
        "input": "1\n1 1\n1",
        "expectedOutput": "0",
        "weight": 2
      },
      {
        "id": "tc_3",
        "label": "Case 3",
        "input": "1\n1 1\n1",
        "expectedOutput": "0",
        "weight": 2
      },
      {
        "id": "tc_4",
        "label": "Case 4",
        "input": "1\n1 1\n1",
        "expectedOutput": "0",
        "weight": 2
      },
      {
        "id": "tc_5",
        "label": "Case 5",
        "input": "1\n1 1\n1",
        "expectedOutput": "0",
        "weight": 2
      }
    ],
    "totalTestCases": 5,
    "totalWeight": 10
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      2,
      2,
      2,
      2,
      2
    ],
    "maxScore": 10
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "The problem can be solved by first finding the pivot element in the rotated sorted array. If the pivot element is found, we can then decide which half of the array to search for the target element K. If the pivot element is not found, it means the array is not rotated and we can perform a standard binary search.",
    "timeComplexity": "O(log N) per test case, O(T log N) total.",
    "spaceComplexity": "O(1) \u2014 no extra space is used.",
    "code": {
      "C": "#include <stdio.h>\n\nint findPivot(int arr[], int low, int high) {\n    if (high < low) return -1;\n    if (high == low) return low;\n    int mid = (low + high) / 2;\n    if (mid < high && arr[mid] > arr[mid + 1]) return mid;\n    if (mid > low && arr[mid] < arr[mid - 1]) return mid - 1;\n    if (arr[low] >= arr[mid]) return findPivot(arr, low, mid - 1);\n    return findPivot(arr, mid + 1, high);\n}\n\nint binarySearch(int arr[], int low, int high, int key) {\n    while (low <= high) {\n        int mid = low + (high - low) / 2;\n        if (arr[mid] == key) return mid;\n        if (arr[mid] < key) low = mid + 1;\n        else high = mid - 1;\n    }\n    return -1;\n}\n\nint search(int arr[], int n, int key) {\n    int pivot = findPivot(arr, 0, n - 1);\n    if (pivot == -1) return binarySearch(arr, 0, n - 1, key);\n    if (arr[pivot] == key) return pivot;\n    if (arr[0] <= key) return binarySearch(arr, 0, pivot - 1, key);\n    return binarySearch(arr, pivot + 1, n - 1, key);\n}\n\nint main() {\n    int T; scanf(\"%d\", &T);\n    while (T--) {\n        int N, K; scanf(\"%d %d\", &N, &K);\n        int arr[5000];\n        for (int i = 0; i < N; ++i) scanf(\"%d\", &arr[i]);\n        printf(\"%d\\n\", search(arr, N, K));\n    }\n    return 0;\n}",
      "C++": "#include <bits/stdc++.h>\nusing namespace std;\n\nint findPivot(const vector<int>& arr, int low, int high) {\n    if (high < low) return -1;\n    if (high == low) return low;\n    int mid = low + (high - low) / 2;\n    if (mid < high && arr[mid] > arr[mid + 1]) return mid;\n    if (mid > low && arr[mid] < arr[mid - 1]) return mid - 1;\n    if (arr[low] >= arr[mid]) return findPivot(arr, low, mid - 1);\n    return findPivot(arr, mid + 1, high);\n}\n\nint binarySearch(const vector<int>& arr, int low, int high, int key) {\n    while (low <= high) {\n        int mid = low + (high - low) / 2;\n        if (arr[mid] == key) return mid;\n        if (arr[mid] < key) low = mid + 1;\n        else high = mid - 1;\n    }\n    return -1;\n}\n\nint search(const vector<int>& arr, int key) {\n    int n = arr.size();\n    int pivot = findPivot(arr, 0, n - 1);\n    if (pivot == -1) return binarySearch(arr, 0, n - 1, key);\n    if (arr[pivot] == key) return pivot;\n    if (arr[0] <= key) return binarySearch(arr, 0, pivot - 1, key);\n    return binarySearch(arr, pivot + 1, n - 1, key);\n}\n\nint main() {\n    ios::sync_with_stdio(false);\n    cin.tie(nullptr);\n    int T; cin >> T;\n    while (T--) {\n        int N, K; cin >> N >> K;\n        vector<int> arr(N);\n        for (int i = 0; i < N; ++i) cin >> arr[i];\n        cout << search(arr, K) << '\\n';\n    }\n    return 0;\n}",
      "Java": "import java.io.*;\nimport java.util.*;\n\npublic class Main {\n    private static int findPivot(int[] arr, int low, int high) {\n        if (high < low) return -1;\n        if (high == low) return low;\n        int mid = low + (high - low) / 2;\n        if (mid < high && arr[mid] > arr[mid + 1]) return mid;\n        if (mid > low && arr[mid] < arr[mid - 1]) return mid - 1;\n        if (arr[low] >= arr[mid]) return findPivot(arr, low, mid - 1);\n        return findPivot(arr, mid + 1, high);\n    }\n\n    private static int binarySearch(int[] arr, int low, int high, int key) {\n        while (low <= high) {\n            int mid = low + (high - low) / 2;\n            if (arr[mid] == key) return mid;\n            if (arr[mid] < key) low = mid + 1;\n            else high = mid - 1;\n        }\n        return -1;\n    }\n\n    private static int search(int[] arr, int key) {\n        int n = arr.length;\n        int pivot = findPivot(arr, 0, n - 1);\n        if (pivot == -1) return binarySearch(arr, 0, n - 1, key);\n        if (arr[pivot] == key) return pivot;\n        if (arr[0] <= key) return binarySearch(arr, 0, pivot - 1, key);\n        return binarySearch(arr, pivot + 1, n - 1, key);\n    }\n\n    public static void main(String[] args) throws Exception {\n        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));\n        StringBuilder sb = new StringBuilder();\n        int T = Integer.parseInt(br.readLine().trim());\n        while (T-- > 0) {\n            StringTokenizer st = new StringTokenizer(br.readLine());\n            int N = Integer.parseInt(st.nextToken());\n            int K = Integer.parseInt(st.nextToken());\n            int[] arr = new int[N];\n            st = new StringTokenizer(br.readLine());\n            for (int i = 0; i < N; ++i) arr[i] = Integer.parseInt(st.nextToken());\n            sb.append(search(arr, K)).append('\\n');\n        }\n        System.out.print(sb.toString());\n    }\n}",
      "Python3": "import sys\n\ndef find_pivot(arr, low, high):\n    while low <= high:\n        if low == high:\n            return low\n        mid = (low + high) // 2\n        if mid < high and arr[mid] > arr[mid + 1]:\n            return mid\n        if mid > low and arr[mid] < arr[mid - 1]:\n            return mid - 1\n        if arr[low] >= arr[mid]:\n            high = mid - 1\n        else:\n            low = mid + 1\n    return -1\n\ndef binary_search(arr, low, high, key):\n    while low <= high:\n        mid = (low + high) // 2\n        if arr[mid] == key:\n            return mid\n        if arr[mid] < key:\n            low = mid + 1\n        else:\n            high = mid - 1\n    return -1\n\ndef search(arr, key):\n    n = len(arr)\n    pivot = find_pivot(arr, 0, n - 1)\n    if pivot == -1:\n        return binary_search(arr, 0, n - 1, key)\n    if arr[pivot] == key:\n        return pivot\n    if arr[0] <= key:\n        return binary_search(arr, 0, pivot - 1, key)\n    return binary_search(arr, pivot + 1, n - 1, key)\n\ndata = sys.stdin.read().strip().split()\nif not data:\n    sys.exit()\nit = iter(data)\nT = int(next(it))\nout_lines = []\nfor _ in range(T):\n    N = int(next(it)); K = int(next(it))\n    arr = [int(next(it)) for _ in range(N)]\n    out_lines.append(str(search(arr, K)))\nprint('\\n'.join(out_lines))",
      "JavaScript": "const fs = require('fs');\nconst input = fs.readFileSync(0, 'utf-8').trim().split(/\\s+/);\nlet idx = 0;\nfunction findPivot(arr, low, high) {\n    while (low <= high) {\n        if (low === high) return low;\n        const mid = Math.floor((low + high) / 2);\n        if (mid < high && arr[mid] > arr[mid + 1]) return mid;\n        if (mid > low && arr[mid] < arr[mid - 1]) return mid - 1;\n        if (arr[low] >= arr[mid]) high = mid - 1; else low = mid + 1;\n    }\n    return -1;\n}\nfunction binarySearch(arr, low, high, key) {\n    while (low <= high) {\n        const mid = Math.floor((low + high) / 2);\n        if (arr[mid] === key) return mid;\n        if (arr[mid] < key) low = mid + 1; else high = mid - 1;\n    }\n    return -1;\n}\nfunction search(arr, key) {\n    const n = arr.length;\n    const pivot = findPivot(arr, 0, n - 1);\n    if (pivot === -1) return binarySearch(arr, 0, n - 1, key);\n    if (arr[pivot] === key) return pivot;\n    if (arr[0] <= key) return binarySearch(arr, 0, pivot - 1, key);\n    return binarySearch(arr, pivot + 1, n - 1, key);\n}\nconst T = Number(input[idx++]);\nlet outputs = [];\nfor (let t = 0; t < T; ++t) {\n    const N = Number(input[idx++]);\n    const K = Number(input[idx++]);\n    const arr = [];\n    for (let i = 0; i < N; ++i) arr.push(Number(input[idx++]));\n    outputs.push(String(search(arr, K)));\n}\nconsole.log(outputs.join('\\n'));"
    }
  }
}