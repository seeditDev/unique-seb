{
  "questionId": "Q2705",
  "title": "Three-Degree-Bounded Maximum Cost Subtree",
  "slug": "three-degree-bounded-maximum-cost-subtree",
  "version": 1,
  "createdAt": "2026-07-01",
  "updatedAt": "2026-07-04",
  "metadata": {
    "category": "Graphs",
    "difficulty": "Hard",
    "isPremium": false,
    "tags": [
      "Tree",
      "Graphs",
      "Backtracking",
      "Depth-First Search"
    ],
    "companyTags": [],
    "topicTags": [
      "Trees"
    ]
  },
  "content": {
    "problemStatement": "Problem StatementChef has become a rich man and now owns the entire road network in the country, which consists of **N** cities (numbered from **1** to **N**) and **M** bidirectional roads. Each road connects two different cities **i** and **j** and has a cost **C(i,j)** - this is the cost paid by the drivers which use this road (and, thus, it is Chef's profit). The road network has the following properties:\n\n\nit is possible to reach any city from any other city using the existing roads\nany subset **S** of **10** or more cities contains at least two different cities **A** and **B** such that all the paths between **A** and **B** have at least one city **X** (**X\u2260A** and **X\u2260B**) in common (note that **X** does not necessarily belong to **S**) - note that this implies that if the city **X** were to be removed from the network, then no path would exist anymore between the cities **A** and **B**\n\n\nHowever, the anti-monopoly committee decided that Chef is making too much money and that some restrictions should be imposed. Chef will be allowed to keep a part **P** of the road network that he chooses, provided that:\n\n\nthere exists exactly one path from any city in **P** to any other city in **P** using only the roads from **P** (this means that the sets of cities and roads from **P** form a tree)\nevery city from **P** has **at most 3** roads from **P** adjacent to it\n\n\nNote that **P** may consist of any subset of the **N** cities and any subset of roads connecting these cities, as long as the two restrictions are satisfied. However, Chef doesn't want to simply pick any part **P** of the road network which satisfies the restrictions. He would like to choose a part **P** for which the total cost of the roads which belong to **P** is **maximum**. Moreover, he would also like to know how many such parts **P** of maximum total cost exist. Two parts **P1** and **P2** are considered different if their subsets of cities are different or if their subsets of roads are different.",
    "constraints": [
      "1 \u2264 T \u2264 6",
      "1 \u2264 N \u2264 100",
      "N-1 \u2264 M \u2264 min(N\u00d7(N-1)/2, 450)",
      "-10000 \u2264 C(i,j) \u2264 10000"
    ],
    "inputFormat": "The first line contains the integer number T denoting the number of test cases. Each test case contains two integers N and M on the first line, followed by M lines, each containing three integers i, j, and C(i,j), representing a road between cities i and j with cost C(i,j).",
    "outputFormat": "For each test case, print two numbers: CMAX and CNT32. CMAX is the maximum cost of any part P which satisfies the restrictions imposed by the anti-monopoly committee. CNT32 is equal to CNT modulo 232, where CNT is the total number of different parts P which have maximum cost and satisfy the restrictions.",
    "sampleTestCases": [
      {
        "id": "sample_1",
        "input": "6\n6 7\n1 3 1\n3 5 2\n5 1 3\n5 2 1\n2 4 2\n4 6 3\n6 5 1\n4 4\n1 2 0\n2 3 0\n3 4 0\n2 4 0\n4 4\n1 2 -1\n2 3 -2\n3 4 -3\n2 4 -4\n8 7\n1 2 1\n2 3 1\n3 4 1\n4 5 -2\n5 6 1\n6 7 1\n7 8 1\n8 7\n1 2 1\n2 3 1\n3 4 1\n4 5 -3\n5 6 1\n6 7 1\n7 8 1\n8 7\n1 2 1\n1 3 2\n1 4 3\n1 5 4\n1 6 5\n1 7 6\n1 8 7",
        "output": "11 2\n0 17\n0 5\n4 1\n3 3\n18 1",
        "explanation": "The maximum cost of a part P which satisfies the restrictions is 11. There are two such maximum cost parts, both of them containing all the cities and the following roads: (1,5), (2,5), (2,4), (3,5), (4,6) and (1,5), (2,4), (3,5), (4,6), (5,6)."
      }
    ]
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "label": "minimum boundary",
        "input": "1\n1 0",
        "expectedOutput": "0 1",
        "weight": 2
      },
      {
        "id": "tc_2",
        "label": "maximum boundary",
        "input": "1\n100 4950",
        "expectedOutput": "0 1",
        "weight": 2
      },
      {
        "id": "tc_3",
        "label": "uniform/edge case",
        "input": "1\n5 4\n1 2 0\n2 3 0\n3 4 0\n4 5 0",
        "expectedOutput": "0 1",
        "weight": 2
      },
      {
        "id": "tc_4",
        "label": "tricky case",
        "input": "1\n5 4\n1 2 1\n2 3 1\n3 4 1\n4 5 1",
        "expectedOutput": "4 1",
        "weight": 2
      },
      {
        "id": "tc_5",
        "label": "Case 5",
        "input": "1\n1 0",
        "expectedOutput": "0 1",
        "weight": 2
      }
    ],
    "totalTestCases": 5,
    "totalWeight": 10
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      2,
      2,
      2,
      2,
      2
    ],
    "maxScore": 10
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "To solve this problem, we can use a depth-first search (DFS) approach to generate all possible subtrees and then check if each subtree satisfies the given conditions. We can use backtracking to efficiently explore all possible subtrees.",
    "timeComplexity": "O(2^N * M) per test case, O(T * 2^N * M) total.",
    "spaceComplexity": "O(N + M) \u2014 for storing the graph and the current subtree.",
    "code": {
      "C": "#include <stdio.h>\n#include <stdlib.h>\n#include <string.h>\n#define MAX_N 100\n#define MAX_M 450\n#define MOD 232\n\n// Structure to represent a road\ntypedef struct {\n    int city1;\n    int city2;\n    int cost;\n} Road;\n\n// Function to find all possible subtrees\nvoid findSubtrees(int n, int m, Road roads[], int maxCost, int *cnt, int *maxCostFound, int *cntFound) {\n    // Initialize the maximum cost and the count of subtrees\n    *maxCostFound = 0;\n    *cntFound = 0;\n\n    // Iterate over all possible subtrees\n    for (int i = 0; i < (1 << n); i++) {\n        int subtreeCost = 0;\n        int subtreeCnt = 0;\n        int degree = 0;\n\n        // Iterate over all roads\n        for (int j = 0; j < m; j++) {\n            // Check if the road is in the current subtree\n            if ((i & (1 << roads[j].city1)) && (i & (1 << roads[j].city2))) {\n                subtreeCost += roads[j].cost;\n                subtreeCnt++;\n                degree++;\n            }\n        }\n\n        // Check if the subtree satisfies the conditions\n        if (subtreeCnt > 0 && degree <= 3 * n && subtreeCost > *maxCostFound) {\n            *maxCostFound = subtreeCost;\n            *cntFound = 1;\n        } else if (subtreeCost == *maxCostFound && subtreeCnt > 0) {\n            (*cntFound)++;\n        }\n    }\n}\n\nint main() {\n    int t;\n    scanf(\"%d\", &t);\n\n    while (t--) {\n        int n, m;\n        scanf(\"%d %d\", &n, &m);\n\n        Road roads[MAX_M];\n        for (int i = 0; i < m; i++) {\n            scanf(\"%d %d %d\", &roads[i].city1, &roads[i].city2, &roads[i].cost);\n        }\n\n        int maxCost = 0;\n        int cnt = 0;\n        int maxCostFound = 0;\n        int cntFound = 0;\n\n        findSubtrees(n, m, roads, maxCost, &cnt, &maxCostFound, &cntFound);\n\n        printf(\"%d %d\\n\", maxCostFound, cntFound % MOD);\n    }\n\n    return 0;\n}",
      "C++": "#include <iostream>\n#include <vector>\nusing namespace std;\n#define MAX_N 100\n#define MAX_M 450\n#define MOD 232\n\n// Structure to represent a road\nstruct Road {\n    int city1;\n    int city2;\n    int cost;\n};\n\n// Function to find all possible subtrees\nvoid findSubtrees(int n, int m, vector<Road> roads, int maxCost, int *cnt, int *maxCostFound, int *cntFound) {\n    // Initialize the maximum cost and the count of subtrees\n    *maxCostFound = 0;\n    *cntFound = 0;\n\n    // Iterate over all possible subtrees\n    for (int i = 0; i < (1 << n); i++) {\n        int subtreeCost = 0;\n        int subtreeCnt = 0;\n        int degree = 0;\n\n        // Iterate over all roads\n        for (int j = 0; j < m; j++) {\n            // Check if the road is in the current subtree\n            if ((i & (1 << roads[j].city1)) && (i & (1 << roads[j].city2))) {\n                subtreeCost += roads[j].cost;\n                subtreeCnt++;\n                degree++;\n            }\n        }\n\n        // Check if the subtree satisfies the conditions\n        if (subtreeCnt > 0 && degree <= 3 * n && subtreeCost > *maxCostFound) {\n            *maxCostFound = subtreeCost;\n            *cntFound = 1;\n        } else if (subtreeCost == *maxCostFound && subtreeCnt > 0) {\n            (*cntFound)++;\n        }\n    }\n}\n\nint main() {\n    int t;\n    cin >> t;\n\n    while (t--) {\n        int n, m;\n        cin >> n >> m;\n\n        vector<Road> roads(m);\n        for (int i = 0; i < m; i++) {\n            cin >> roads[i].city1 >> roads[i].city2 >> roads[i].cost;\n        }\n\n        int maxCost = 0;\n        int cnt = 0;\n        int maxCostFound = 0;\n        int cntFound = 0;\n\n        findSubtrees(n, m, roads, maxCost, &cnt, &maxCostFound, &cntFound);\n\n        cout << maxCostFound << \" \" << cntFound % MOD << endl;\n    }\n\n    return 0;\n}",
      "Java": "import java.util.Scanner;\npublic class Main {\n    static final int MAX_N = 100;\n    static final int MAX_M = 450;\n    static final int MOD = 232;\n\n    // Structure to represent a road\n    static class Road {\n        int city1;\n        int city2;\n        int cost;\n\n        public Road(int city1, int city2, int cost) {\n            this.city1 = city1;\n            this.city2 = city2;\n            this.cost = cost;\n        }\n    }\n\n    // Function to find all possible subtrees\n    public static void findSubtrees(int n, int m, Road[] roads, int maxCost, int[] cnt, int[] maxCostFound, int[] cntFound) {\n        // Initialize the maximum cost and the count of subtrees\n        maxCostFound[0] = 0;\n        cntFound[0] = 0;\n\n        // Iterate over all possible subtrees\n        for (int i = 0; i < (1 << n); i++) {\n            int subtreeCost = 0;\n            int subtreeCnt = 0;\n            int degree = 0;\n\n            // Iterate over all roads\n            for (int j = 0; j < m; j++) {\n                // Check if the road is in the current subtree\n                if ((i & (1 << roads[j].city1)) != 0 && (i & (1 << roads[j].city2)) != 0) {\n                    subtreeCost += roads[j].cost;\n                    subtreeCnt++;\n                    degree++;\n                }\n            }\n\n            // Check if the subtree satisfies the conditions\n            if (subtreeCnt > 0 && degree <= 3 * n && subtreeCost > maxCostFound[0]) {\n                maxCostFound[0] = subtreeCost;\n                cntFound[0] = 1;\n            } else if (subtreeCost == maxCostFound[0] && subtreeCnt > 0) {\n                cntFound[0]++;\n            }\n        }\n    }\n\n    public static void main(String[] args) {\n        Scanner scanner = new Scanner(System.in);\n        int t = scanner.nextInt();\n\n        while (t-- > 0) {\n            int n = scanner.nextInt();\n            int m = scanner.nextInt();\n\n            Road[] roads = new Road[m];\n            for (int i = 0; i < m; i++) {\n                roads[i] = new Road(scanner.nextInt(), scanner.nextInt(), scanner.nextInt());\n            }\n\n            int maxCost = 0;\n            int[] cnt = new int[1];\n            int[] maxCostFound = new int[1];\n            int[] cntFound = new int[1];\n\n            findSubtrees(n, m, roads, maxCost, cnt, maxCostFound, cntFound);\n\n            System.out.println(maxCostFound[0] + \" \" + cntFound[0] % MOD);\n        }\n    }\n}",
      "Python3": "import sys\n\n# Structure to represent a road\nclass Road:\n    def __init__(self, city1, city2, cost):\n        self.city1 = city1\n        self.city2 = city2\n        self.cost = cost\n\n# Function to find all possible subtrees\ndef find_subtrees(n, m, roads, max_cost, cnt, max_cost_found, cnt_found):\n    # Initialize the maximum cost and the count of subtrees\n    max_cost_found[0] = 0\n    cnt_found[0] = 0\n\n    # Iterate over all possible subtrees\n    for i in range(1 << n):\n        subtree_cost = 0\n        subtree_cnt = 0\n        degree = 0\n\n        # Iterate over all roads\n        for j in range(m):\n            # Check if the road is in the current subtree\n            if (i & (1 << roads[j].city1)) and (i & (1 << roads[j].city2)):\n                subtree_cost += roads[j].cost\n                subtree_cnt += 1\n                degree += 1\n\n        # Check if the subtree satisfies the conditions\n        if subtree_cnt > 0 and degree <= 3 * n and subtree_cost > max_cost_found[0]:\n            max_cost_found[0] = subtree_cost\n            cnt_found[0] = 1\n        elif subtree_cost == max_cost_found[0] and subtree_cnt > 0:\n            cnt_found[0] += 1\n\n# Main function\ndef main():\n    t = int(sys.stdin.readline().strip())\n\n    while t > 0:\n        n, m = map(int, sys.stdin.readline().strip().split())\n\n        roads = []\n        for _ in range(m):\n            city1, city2, cost = map(int, sys.stdin.readline().strip().split())\n            roads.append(Road(city1, city2, cost))\n\n        max_cost = 0\n        cnt = [0]\n        max_cost_found = [0]\n        cnt_found = [0]\n\n        find_subtrees(n, m, roads, max_cost, cnt, max_cost_found, cnt_found)\n\n        print(max_cost_found[0], cnt_found[0] % 232)\n\n        t -= 1\n\nif __name__ == '__main__':\n    main()",
      "JavaScript": "const fs = require('fs');\nconst lines = fs.readFileSync(0, 'utf-8').trim().split('\\n');\nlet idx = 0;\n\n// Structure to represent a road\nclass Road {\n    constructor(city1, city2, cost) {\n        this.city1 = city1;\n        this.city2 = city2;\n        this.cost = cost;\n    }\n}\n\n// Function to find all possible subtrees\nfunction findSubtrees(n, m, roads, maxCost, cnt, maxCostFound, cntFound) {\n    // Initialize the maximum cost and the count of subtrees\n    maxCostFound[0] = 0;\n    cntFound[0] = 0;\n\n    // Iterate over all possible subtrees\n    for (let i = 0; i < (1 << n); i++) {\n        let subtreeCost = 0;\n        let subtreeCnt = 0;\n        let degree = 0;\n\n        // Iterate over all roads\n        for (let j = 0; j < m; j++) {\n            // Check if the road is in the current subtree\n            if ((i & (1 << roads[j].city1)) && (i & (1 << roads[j].city2))) {\n                subtreeCost += roads[j].cost;\n                subtreeCnt++;\n                degree++;\n            }\n        }\n\n        // Check if the subtree satisfies the conditions\n        if (subtreeCnt > 0 && degree <= 3 * n && subtreeCost > maxCostFound[0]) {\n            maxCostFound[0] = subtreeCost;\n            cntFound[0] = 1;\n        } else if (subtreeCost == maxCostFound[0] && subtreeCnt > 0) {\n            cntFound[0]++;\n        }\n    }\n}\n\n// Main function\nfunction main() {\n    let t = +lines[idx++];\n\n    while (t-- > 0) {\n        let [n, m] = lines[idx++].trim().split(' ').map(Number);\n\n        let roads = [];\n        for (let i = 0; i < m; i++) {\n            let [city1, city2, cost] = lines[idx++].trim().split(' ').map(Number);\n            roads.push(new Road(city1, city2, cost));\n        }\n\n        let maxCost = 0;\n        let cnt = [0];\n        let maxCostFound = [0];\n        let cntFound = [0];\n\n        findSubtrees(n, m, roads, maxCost, cnt, maxCostFound, cntFound);\n\n        console.log(maxCostFound[0], cntFound[0] % 232);\n    }\n}\n\nmain();"
    }
  }
}