{
  "questionId": "Q3899",
  "title": "Count Nodes",
  "slug": "count-nodes",
  "version": 1,
  "createdAt": "2026-07-02",
  "updatedAt": "2026-07-04",
  "metadata": {
    "category": "Dynamic Programming",
    "difficulty": "Hard",
    "isPremium": false,
    "tags": [
      "Counting",
      "Segment Tree",
      "Dynamic Programming"
    ],
    "companyTags": [],
    "topicTags": [
      "Dynamic Programming"
    ]
  },
  "content": {
    "problemStatement": "&nbsp;\n\nA segment tree is built to perform queries in the range **1** to **N**. The following pseudo code counts the number of nodes which return a value when the tree is queried from l to r:\n\n\n`\ncnt = 0\n\nquery(node, start, end, l, r):\n\n\u2003\u2003if(start &gt; r or end &lt; l):\n\n\u2003\u2003\u2003\u2003return\n\n\u2003\u2003if(start &gt;= l and end &lt;=r):\n\n\u2003\u2003\u2003\u2003cnt = cnt + 1\n\n\u2003\u2003\u2003\u2003return\n\n\u2003\u2003mid = (start + end)/2\n\n\u2003\u2003query(2*node, start, mid, l, r)\n\n\u2003\u2003query(2*node+1, mid+1, end, l, r)\n\n\n`\n\n\nNow, for a given **N**, find the array **A1, A2 .... Ak** where **Ai** is the number of distinct segments(l,r) when\n cnt = i.\n\n\n\n&nbsp;\n\nInput:**T****N**Output:**k****k****A[k] != 0****k****A**Constraint:1 &lt;= **T** &lt;= 10\n1 &lt;= **N** &lt;= 50000Sample test:",
    "constraints": [
      "1 <= T <= 10",
      "1 <= N <= 50000"
    ],
    "inputFormat": "The first line contains an integer T, the number of test cases. Then, for each test case, there is one line containing an integer N.",
    "outputFormat": "For each test case, output the number of distinct segments k, followed by k lines, each containing the number of distinct segments for a given count i.",
    "sampleTestCases": [
      {
        "id": "sample_1",
        "input": "1\n5",
        "output": "3\n1\n2\n3",
        "explanation": "For N = 5, the distinct segments are (1, 1), (1, 2), (1, 3), (1, 4), (1, 5), (2, 2), (2, 3), (2, 4), (2, 5), (3, 3), (3, 4), (3, 5), (4, 4), (4, 5), (5, 5). The count of nodes for each segment is 1, 2, and 3, respectively."
      }
    ]
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "label": "minimum boundary",
        "input": "1\n1",
        "expectedOutput": "1\n1",
        "weight": 2
      },
      {
        "id": "tc_2",
        "label": "maximum boundary",
        "input": "1\n50000",
        "expectedOutput": "...",
        "weight": 2
      },
      {
        "id": "tc_3",
        "label": "edge case",
        "input": "1\n10",
        "expectedOutput": "...",
        "weight": 2
      },
      {
        "id": "tc_4",
        "label": "Case 4",
        "input": "1\n1",
        "expectedOutput": "1\n1",
        "weight": 2
      },
      {
        "id": "tc_5",
        "label": "Case 5",
        "input": "1\n1",
        "expectedOutput": "1\n1",
        "weight": 2
      }
    ],
    "totalTestCases": 5,
    "totalWeight": 10
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      2,
      2,
      2,
      2,
      2
    ],
    "maxScore": 10
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "To solve this problem, we can use a segment tree to count the number of nodes that return a value for each query. We start by initializing the segment tree with the given range [1, N]. Then, for each query, we recursively traverse the segment tree, counting the number of nodes that return a value. We use a dynamic programming approach to store the count of nodes for each segment and avoid redundant calculations.",
    "timeComplexity": "O(T * N * log N) \u2014 explain: per query is O(N * log N), total is O(T * N * log N)",
    "spaceComplexity": "O(N) \u2014 explain: we need to store the segment tree and the count of nodes for each segment",
    "code": {
      "C": "#include <stdio.h>\n#include <stdlib.h>\n\n// Function to query the segment tree\nvoid query(int node, int start, int end, int l, int r, int* cnt) {\n    if (start > r || end < l) return;\n    if (start >= l && end <= r) {\n        (*cnt)++;\n        return;\n    }\n    int mid = (start + end) / 2;\n    query(2 * node, start, mid, l, r, cnt);\n    query(2 * node + 1, mid + 1, end, l, r, cnt);\n}\n\nint main() {\n    int T;\n    scanf(\"%d\", &T);\n    while (T--) {\n        int N;\n        scanf(\"%d\", &N);\n        int* cnt = (int*) malloc((N + 1) * sizeof(int));\n        for (int i = 1; i <= N; i++) cnt[i] = 0;\n        for (int i = 1; i <= N; i++) {\n            for (int j = i; j <= N; j++) {\n                int temp = 0;\n                query(1, 1, N, i, j, &temp);\n                cnt[temp]++;\n            }\n        }\n        int k = 0;\n        for (int i = 1; i <= N; i++) if (cnt[i] > 0) k++;\n        printf(\"%d\\n\", k);\n        for (int i = 1; i <= N; i++) if (cnt[i] > 0) printf(\"%d\\n\", cnt[i]);\n    }\n    return 0;\n}",
      "C++": "#include <iostream>\nusing namespace std;\n\n// Function to query the segment tree\nvoid query(int node, int start, int end, int l, int r, int& cnt) {\n    if (start > r || end < l) return;\n    if (start >= l && end <= r) {\n        cnt++;\n        return;\n    }\n    int mid = (start + end) / 2;\n    query(2 * node, start, mid, l, r, cnt);\n    query(2 * node + 1, mid + 1, end, l, r, cnt);\n}\n\nint main() {\n    int T;\n    cin >> T;\n    while (T--) {\n        int N;\n        cin >> N;\n        int* cnt = new int[N + 1];\n        for (int i = 1; i <= N; i++) cnt[i] = 0;\n        for (int i = 1; i <= N; i++) {\n            for (int j = i; j <= N; j++) {\n                int temp = 0;\n                query(1, 1, N, i, j, temp);\n                cnt[temp]++;\n            }\n        }\n        int k = 0;\n        for (int i = 1; i <= N; i++) if (cnt[i] > 0) k++;\n        cout << k << endl;\n        for (int i = 1; i <= N; i++) if (cnt[i] > 0) cout << cnt[i] << endl;\n    }\n    return 0;\n}",
      "Java": "import java.util.Scanner;\npublic class Main {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int T = sc.nextInt();\n        while (T-- > 0) {\n            int N = sc.nextInt();\n            int[] cnt = new int[N + 1];\n            for (int i = 1; i <= N; i++) {\n                for (int j = i; j <= N; j++) {\n                    int temp = 0;\n                    query(1, 1, N, i, j, temp, cnt);\n                }\n            }\n            int k = 0;\n            for (int i = 1; i <= N; i++) if (cnt[i] > 0) k++;\n            System.out.println(k);\n            for (int i = 1; i <= N; i++) if (cnt[i] > 0) System.out.println(cnt[i]);\n        }\n    }\n    public static void query(int node, int start, int end, int l, int r, int temp, int[] cnt) {\n        if (start > r || end < l) return;\n        if (start >= l && end <= r) {\n            temp++;\n            cnt[temp]++;\n            return;\n        }\n        int mid = (start + end) / 2;\n        query(2 * node, start, mid, l, r, temp, cnt);\n        query(2 * node + 1, mid + 1, end, l, r, temp, cnt);\n    }\n}",
      "Python3": "def query(node, start, end, l, r, cnt):\n    if start > r or end < l:\n        return\n    if start >= l and end <= r:\n        cnt[0] += 1\n        return\n    mid = (start + end) // 2\n    query(2 * node, start, mid, l, r, cnt)\n    query(2 * node + 1, mid + 1, end, l, r, cnt)\n\nT = int(input())\nfor _ in range(T):\n    N = int(input())\n    cnt = [0] * (N + 1)\n    for i in range(1, N + 1):\n        for j in range(i, N + 1):\n            temp = [0]\n            query(1, 1, N, i, j, temp)\n            cnt[temp[0]] += 1\n    k = sum(1 for i in cnt[1:] if i > 0)\n    print(k)\n    for i in range(1, N + 1):\n        if cnt[i] > 0:\n            print(cnt[i])",
      "JavaScript": "const fs = require('fs');\nconst lines = fs.readFileSync(0, 'utf-8').trim().split('\\n');\nlet idx = 0;\nfunction query(node, start, end, l, r, cnt) {\n    if (start > r || end < l) return;\n    if (start >= l && end <= r) {\n        cnt[0]++;\n        return;\n    }\n    let mid = Math.floor((start + end) / 2);\n    query(2 * node, start, mid, l, r, cnt);\n    query(2 * node + 1, mid + 1, end, l, r, cnt);\n}\nlet T = parseInt(lines[idx++]);\nwhile (T-- > 0) {\n    let N = parseInt(lines[idx++]);\n    let cnt = new Array(N + 1).fill(0);\n    for (let i = 1; i <= N; i++) {\n        for (let j = i; j <= N; j++) {\n            let temp = [0];\n            query(1, 1, N, i, j, temp);\n            cnt[temp[0]]++;\n        }\n    }\n    let k = 0;\n    for (let i = 1; i <= N; i++) if (cnt[i] > 0) k++;\n    console.log(k);\n    for (let i = 1; i <= N; i++) if (cnt[i] > 0) console.log(cnt[i]);\n}"
    }
  }
}