{
  "questionId": "Q1724",
  "title": "Chef Builds String Chain",
  "slug": "chef-builds-string-chain",
  "version": 1,
  "createdAt": "2026-07-01",
  "updatedAt": "2026-07-04",
  "metadata": {
    "category": "Strings",
    "difficulty": "Hard",
    "isPremium": false,
    "tags": [
      "Strings",
      "Sorting",
      "String",
      "Hash Map",
      "Dynamic Programming"
    ],
    "companyTags": [],
    "topicTags": [
      "Dynamic Programming"
    ]
  },
  "content": {
    "problemStatement": "Chef is organizing his collection of words, each consisting of lowercase English letters. He wants to arrange them into a **String Chain** to see how they evolve.\n\nA **String Chain** is a sequence of words where each word is a **predecessor** of the next word.\n\nWord **AAA** is a predecessor of word **BBB** if and only if:\n\n\n\n**\u2223B\u2223=\u2223A\u2223+1|B| = |A| + 1\u2223B\u2223=\u2223A\u2223+1**\n\n\n\n\nYou can insert **exactly one** character anywhere in **AAA** to make it identical to **BBB**\n\n\n\n\nThe relative order of existing characters in **AAA** must remain the same.\n\n\n\n\nValid: \"abc\" \u2192 \"ab**a**c\" (Added 'a' at index 2).\n\n\n\n\nInvalid: \"cba\" \u2192 \"bcad\" (The order of 'c', 'b', and 'a' was scrambled).\n\n\n\nChef wants to find the **maximum possible length** of a chain that can be formed using words from his list.\n\nFunction Declaration\n\n**Function Name:**\n\n\n\n**findLongestStringChainfindLongestStringChainfindLongestStringChain**\n\n\n\n**Parameters:**\n\n\n\n**wordListwordListwordList** (`Array&lt;string&gt;`)\nA list of words available to Chef.\n\n\n\n**Return Value:**\n\n\n\nReturns an `int` representing the **length of the longest possible string chain**.",
    "constraints": [
      "1 <= T <= 100000",
      "1 <= N <= 1000",
      "1 <= sum of N over all test cases <= 100000",
      "1 <= |word| <= 16",
      "All strings consist of lowercase English letters only"
    ],
    "inputFormat": "The first line contains an integer T, the number of test cases. For each test case:\n- The first line contains an integer N, the number of words.\n- The second line contains N space\u2011separated strings consisting of lowercase English letters.",
    "outputFormat": "For each test case output a single integer on its own line \u2013 the maximum length of a string chain that can be formed from the given words.",
    "sampleTestCases": [
      {
        "id": "sample_1",
        "input": "3\n6\na b ba bca bda bdca\n5\nxbc pcxbcf xb cxbc pcxbc\n2\nabcd dbqca",
        "output": "4\n5\n1",
        "explanation": "Test case 1: One longest chain is a \u2192 ba \u2192 bda \u2192 bdca (length 4).\\nTest case 2: Chain xb \u2192 xbc \u2192 cxbc \u2192 pcxbc \u2192 pcxbcf (length 5).\\nTest case 3: No two words satisfy the predecessor rule, so the longest possible chain has length 1."
      },
      {
        "id": "sample_2",
        "input": "1\n5\na ab abc abcd abcde",
        "output": "5",
        "explanation": "Each word can be obtained by inserting one character at the end of the previous word, giving a chain of length 5."
      },
      {
        "id": "sample_3",
        "input": "1\n4\na a a a",
        "output": "1",
        "explanation": "All words are identical; a predecessor must be exactly one character shorter, so no extension is possible beyond a single word."
      }
    ]
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "label": "minimum boundary",
        "input": "1\n1\na",
        "expectedOutput": "1",
        "weight": 2
      },
      {
        "id": "tc_2",
        "label": "maximum boundary",
        "input": "1\n15\na aa aaa aaaa aaaaa aaaaaa aaaaaaa aaaaaaaa aaaaaaaaa aaaaaaaaaa aaaaaaaaaaa aaaaaaaaaaaa aaaaaaaaaaaaa aaaaaaaaaaaaaa aaaaaaaaaaaaaaa",
        "expectedOutput": "15",
        "weight": 2
      },
      {
        "id": "tc_3",
        "label": "edge case with duplicates",
        "input": "1\n4\na ab ab abc",
        "expectedOutput": "3",
        "weight": 2
      },
      {
        "id": "tc_4",
        "label": "general case 1 with two separate chains",
        "input": "1\n7\na ab abc abcd x xy xyz",
        "expectedOutput": "4",
        "weight": 2
      },
      {
        "id": "tc_5",
        "label": "general case 2 with non\u2011trivial insert positions",
        "input": "1\n5\nb ab acb acdb acdeb",
        "expectedOutput": "5",
        "weight": 2
      }
    ],
    "totalTestCases": 5,
    "totalWeight": 10
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      2,
      2,
      2,
      2,
      2
    ],
    "maxScore": 10
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "Sort all words by their length (shortest to longest). For each word w we try to remove each possible character (there are |w| possibilities) to obtain a predecessor p. If p has already been processed we know the longest chain ending at p (dp[p]). The longest chain ending at w is 1 + max(dp[p]) over all valid predecessors, or 1 if none exist. Store dp[w] in a hash map. The answer for a test case is the maximum dp value.\nThe sorting guarantees that when we process w, all possible predecessors (which are exactly one character shorter) have already been processed.",
    "timeComplexity": "O(N * L) per test case, where L is the maximum word length (\u226416). Sorting costs O(N log N) but dominates less than the DP step for the given limits.",
    "spaceComplexity": "O(N) to store the hash map of word \u2192 longest chain length.",
    "code": {
      "C": "#include <stdio.h>\n#include <stdlib.h>\n#include <string.h>\n\nint cmpLen(const void *a, const void *b) {\n    const char *sa = *(const char **)a;\n    const char *sb = *(const char **)b;\n    return (int)strlen(sa) - (int)strlen(sb);\n}\n\nint isPredecessor(const char *shorter, const char *longer) {\n    int i = 0, j = 0, diff = 0;\n    int ls = strlen(shorter), ll = strlen(longer);\n    while (i < ls && j < ll) {\n        if (shorter[i] == longer[j]) { i++; j++; }\n        else { diff++; j++; if (diff > 1) return 0; }\n    }\n    return 1; // at most one extra character remains in longer\n}\n\nint findLongestStringChain(char **words, int n) {\n    if (n == 0) return 0;\n    qsort(words, n, sizeof(char *), cmpLen);\n    int *dp = (int *)malloc(n * sizeof(int));\n    for (int i = 0; i < n; ++i) dp[i] = 1;\n    int best = 1;\n    for (int i = 0; i < n; ++i) {\n        for (int j = 0; j < i; ++j) {\n            if (strlen(words[j]) + 1 != strlen(words[i])) continue;\n            if (isPredecessor(words[j], words[i])) {\n                if (dp[i] < dp[j] + 1) dp[i] = dp[j] + 1;\n            }\n        }\n        if (dp[i] > best) best = dp[i];\n    }\n    free(dp);\n    return best;\n}\n\nint main() {\n    int T; if (scanf(\"%d\", &T) != 1) return 0;\n    while (T--) {\n        int N; scanf(\"%d\", &N);\n        char **words = (char **)malloc(N * sizeof(char *));\n        for (int i = 0; i < N; ++i) {\n            words[i] = (char *)malloc(17 * sizeof(char));\n            scanf(\"%16s\", words[i]);\n        }\n        printf(\"%d\\n\", findLongestStringChain(words, N));\n        for (int i = 0; i < N; ++i) free(words[i]);\n        free(words);\n    }\n    return 0;\n}",
      "C++": "#include <bits/stdc++.h>\nusing namespace std;\n\nint findLongestStringChain(vector<string> &words) {\n    sort(words.begin(), words.end(), [](const string &a, const string &b){ return a.size() < b.size(); });\n    unordered_map<string,int> dp;\n    int ans = 1;\n    for (const string &w : words) {\n        int best = 1;\n        for (size_t i = 0; i < w.size(); ++i) {\n            string prev = w.substr(0,i) + w.substr(i+1);\n            auto it = dp.find(prev);\n            if (it != dp.end()) best = max(best, it->second + 1);\n        }\n        dp[w] = best;\n        ans = max(ans, best);\n    }\n    return ans;\n}\n\nint main(){\n    ios::sync_with_stdio(false);\n    cin.tie(nullptr);\n    int T; cin >> T;\n    while (T--) {\n        int N; cin >> N;\n        vector<string> words(N);\n        for (int i = 0; i < N; ++i) cin >> words[i];\n        cout << findLongestStringChain(words) << '\\n';\n    }\n    return 0;\n}",
      "Java": "import java.io.*;\nimport java.util.*;\n\npublic class Main {\n    private static int findLongestStringChain(String[] words) {\n        Arrays.sort(words, Comparator.comparingInt(String::length));\n        Map<String,Integer> dp = new HashMap<>();\n        int ans = 1;\n        for (String w : words) {\n            int best = 1;\n            for (int i = 0; i < w.length(); ++i) {\n                String prev = w.substring(0,i) + w.substring(i+1);\n                best = Math.max(best, dp.getOrDefault(prev, 0) + 1);\n            }\n            dp.put(w, best);\n            ans = Math.max(ans, best);\n        }\n        return ans;\n    }\n\n    public static void main(String[] args) throws Exception {\n        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));\n        StringBuilder out = new StringBuilder();\n        int T = Integer.parseInt(br.readLine().trim());\n        while (T-- > 0) {\n            int N = Integer.parseInt(br.readLine().trim());\n            String[] parts = br.readLine().trim().split(\" \");\n            out.append(findLongestStringChain(parts)).append('\\n');\n        }\n        System.out.print(out.toString());\n    }\n}",
      "Python3": "import sys\n\ndef findLongestStringChain(words):\n    words.sort(key=len)\n    dp = {}\n    ans = 1\n    for w in words:\n        best = 1\n        for i in range(len(w)):\n            prev = w[:i] + w[i+1:]\n            best = max(best, dp.get(prev, 0) + 1)\n        dp[w] = best\n        ans = max(ans, best)\n    return ans\n\ndata = sys.stdin.read().strip().split()\nif not data:\n    sys.exit()\nidx = 0\nT = int(data[idx]); idx += 1\nout_lines = []\nfor _ in range(T):\n    N = int(data[idx]); idx += 1\n    words = data[idx:idx+N]; idx += N\n    out_lines.append(str(findLongestStringChain(words)))\nprint('\\n'.join(out_lines))",
      "JavaScript": "const fs = require('fs');\nconst input = fs.readFileSync(0, 'utf-8').trim().split(/\\s+/);\nlet pos = 0;\nconst T = Number(input[pos++]);\nlet outputs = [];\nfor (let tc = 0; tc < T; ++tc) {\n  const N = Number(input[pos++]);\n  const words = [];\n  for (let i = 0; i < N; ++i) words.push(input[pos++]);\n  words.sort((a, b) => a.length - b.length);\n  const dp = new Map();\n  let ans = 1;\n  for (const w of words) {\n    let best = 1;\n    for (let i = 0; i < w.length; ++i) {\n      const prev = w.slice(0, i) + w.slice(i + 1);\n      if (dp.has(prev)) best = Math.max(best, dp.get(prev) + 1);\n    }\n    dp.set(w, best);\n    ans = Math.max(ans, best);\n  }\n  outputs.push(String(ans));\n}\nconsole.log(outputs.join('\\n'));"
    }
  }
}