{
  "questionId": "Q2489",
  "title": "Fly-overs",
  "slug": "fly-overs",
  "version": 1,
  "createdAt": "2026-07-01",
  "updatedAt": "2026-07-04",
  "metadata": {
    "category": "Math",
    "difficulty": "Hard",
    "isPremium": false,
    "tags": [
      "Graph Theory",
      "Minimum Spanning Tree",
      "Math"
    ],
    "companyTags": [],
    "topicTags": [
      "Ad-hoc & Implementation"
    ]
  },
  "content": {
    "problemStatement": "The Head Chef has opened **N** new restaurants across the city. But he is exhausted since he has to travel so much when he has to go from one restaurant to another. Further, the city traffic is driving him crazy. So he wishes to construct fly-overs between restaurants. A fly-over is a road at a raised height between any two restaurants. If two restaurants are connected by a fly-over, it takes only **1** second for the Chef to move from one restaurant to the other. When commuting between restaurants, our Head Chef wishes to use one or more fly-overs. He doesn't want to travel by the regular roads at all.\n\nUnfortunately, construction of fly-overs costs money. A construction company has agreed to build **1** fly-over for a payment of **C** rupees. It is up to the Chef to decide which restaurants he wants to connect using fly-overs. He asks his assistant chefs to give him suggestions. Each assistant chef proposes his own model. The Chef is now confused, he has no way of comparing models. Some models have too many fly-overs, while other models have too few.\n\nThe Sous Chef came up with a formula to estimate the **Cost of a model**. Suppose the model has **F** fly-overs. Let's label restaurants by numbers **1**, **2**, ..., **N**. Let **T(U, V)** represents the time taken to go from the restaurant **U** to the restaurant **V** via fly-overs of the current model. Compute **T(X, Y)** for every ordered pair of distinct restaurants **(X, Y)** (**N \u2219 (N \u2013 1) ** pairs in total) and then add them up. Denote obtained sum as **P** and called it the time penalty of the model. Note that the constructing of **F** fly-overs will cost **C** \u2219 **F** rupees. The Sous Chef strongly believes that saving time is equivalent to saving money, so the total cost of the model is simply **P** + **C** \u2219 **F**.\n\nThe Head Chef is somewhat satisfied. As a specialist in math and graph theory he immediately found a formula for the cost of the optimal model for a given **C**. But the construction company has a special offer now. It can construct fly-overs between **M** special pairs of restaurants for free. Now the problem of finding the optimal model becomes non-trivial for The Chef and he turns to you for help.",
    "constraints": [
      "1 <= T <= 1000",
      "1 <= N <= 10^9",
      "0 <= C <= 10^9",
      "0 <= M <= 2",
      "1 <= U, V <= N",
      "U != V",
      "All M unordered pairs {U, V} will be different"
    ],
    "inputFormat": "First line contains T, the number of test cases. Each test case contains three values: N (the number of restaurants), C (the cost of constructing one fly-over), and M (the number of special pairs of restaurants). Each of the next M lines contains two integers U and V, the indexes of the restaurants that can be connected by the fly-over for free.",
    "outputFormat": "For each test case, output a single value, the minimum possible cost of a model, with exactly 9 digits after the decimal point.",
    "sampleTestCases": [
      {
        "id": "sample_1",
        "input": "",
        "output": "Input:\n4\n3 1.500 0\n2 18.92 0\n3 3.141592653 1\n1 2\n1 47 0\n\nOutput:\n10.500000000\n20.920000000\n11.141592653\n0.000000000",
        "explanation": ""
      }
    ]
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "label": "Case 1",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_2",
        "label": "Case 2",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_3",
        "label": "Case 3",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_4",
        "label": "Case 4",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_5",
        "label": "Case 5",
        "input": "",
        "expectedOutput": "",
        "weight": 2
      }
    ],
    "totalTestCases": 5,
    "totalWeight": 10
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      2,
      2,
      2,
      2,
      2
    ],
    "maxScore": 10
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "The problem can be solved by using the concept of Minimum Spanning Tree (MST) in graph theory. We can construct a graph where each restaurant is a node, and the cost of constructing a fly-over between two restaurants is the weight of the edge between them. We can then use Kruskal's algorithm to find the MST of the graph, which will give us the minimum possible cost of a model.",
    "timeComplexity": "O(T * (M + N log N)) per test case, O(T * (M + N log N)) total",
    "spaceComplexity": "O(N + M) \u2014 the dominant structure is the parent array for the disjoint set data structure",
    "code": {
      "C": "// COMPLETE, COMPILABLE C code\n#include <stdio.h>\n#include <stdlib.h>\n#include <string.h>\n#include <math.h>\n#define MAX_N 1000\n#define MAX_M 2\n\n// Structure to represent an edge in the graph\ntypedef struct {\n    int u, v;\n    double weight;\n} Edge;\n\n// Comparison function for sorting edges\nint compareEdges(const void *a, const void *b) {\n    Edge *e1 = (Edge *)a;\n    Edge *e2 = (Edge *)b;\n    return (e1->weight > e2->weight) ? 1 : -1;\n}\n\n// Function to find the minimum spanning tree using Kruskal's algorithm\ndouble findMST(int N, int M, Edge *edges, int *parent) {\n    double totalWeight = 0.0;\n    int numEdges = 0;\n    \n    // Sort the edges in non-decreasing order of their weights\n    qsort(edges, M, sizeof(Edge), compareEdges);\n    \n    // Initialize the parent array for the disjoint set data structure\n    for (int i = 0; i < N; i++) {\n        parent[i] = i;\n    }\n    \n    // Iterate over the sorted edges and add them to the minimum spanning tree if they do not form a cycle\n    for (int i = 0; i < M; i++) {\n        int u = edges[i].u;\n        int v = edges[i].v;\n        \n        // Find the roots of the sets containing u and v\n        int rootU = find(parent, u);\n        int rootV = find(parent, v);\n        \n        // If u and v are not in the same set, add the edge to the minimum spanning tree and merge the sets\n        if (rootU != rootV) {\n            totalWeight += edges[i].weight;\n            numEdges++;\n            parent[rootU] = rootV;\n        }\n    }\n    \n    return totalWeight;\n}\n\n// Function to find the root of a set in the disjoint set data structure\nint find(int *parent, int x) {\n    if (parent[x] != x) {\n        parent[x] = find(parent, parent[x]);\n    }\n    return parent[x];\n}\n\nint main() {\n    int T;\n    scanf(\"%d\", &T);\n    \n    for (int t = 0; t < T; t++) {\n        int N, M;\n        double C;\n        scanf(\"%d %lf %d\", &N, &C, &M);\n        \n        Edge edges[MAX_M];\n        int parent[MAX_N];\n        \n        for (int i = 0; i < M; i++) {\n            int u, v;\n            scanf(\"%d %d\", &u, &v);\n            edges[i].u = u - 1;\n            edges[i].v = v - 1;\n            edges[i].weight = 0.0;\n        }\n        \n        double totalWeight = findMST(N, M, edges, parent);\n        \n        printf(\"%.9f\\n\", totalWeight + (N - 1 - M) * C);\n    }\n    \n    return 0;\n}",
      "C++": "// COMPLETE, COMPILABLE C++ code\n#include <iostream>\n#include <vector>\n#include <algorithm>\nusing namespace std;\n\n// Structure to represent an edge in the graph\nstruct Edge {\n    int u, v;\n    double weight;\n};\n\n// Comparison function for sorting edges\nbool compareEdges(const Edge &e1, const Edge &e2) {\n    return e1.weight < e2.weight;\n}\n\n// Function to find the minimum spanning tree using Kruskal's algorithm\ndouble findMST(int N, int M, vector<Edge> &edges, vector<int> &parent) {\n    double totalWeight = 0.0;\n    int numEdges = 0;\n    \n    // Sort the edges in non-decreasing order of their weights\n    sort(edges.begin(), edges.end(), compareEdges);\n    \n    // Initialize the parent array for the disjoint set data structure\n    for (int i = 0; i < N; i++) {\n        parent[i] = i;\n    }\n    \n    // Iterate over the sorted edges and add them to the minimum spanning tree if they do not form a cycle\n    for (int i = 0; i < M; i++) {\n        int u = edges[i].u;\n        int v = edges[i].v;\n        \n        // Find the roots of the sets containing u and v\n        int rootU = find(parent, u);\n        int rootV = find(parent, v);\n        \n        // If u and v are not in the same set, add the edge to the minimum spanning tree and merge the sets\n        if (rootU != rootV) {\n            totalWeight += edges[i].weight;\n            numEdges++;\n            parent[rootU] = rootV;\n        }\n    }\n    \n    return totalWeight;\n}\n\n// Function to find the root of a set in the disjoint set data structure\nint find(vector<int> &parent, int x) {\n    if (parent[x] != x) {\n        parent[x] = find(parent, parent[x]);\n    }\n    return parent[x];\n}\n\nint main() {\n    int T;\n    cin >> T;\n    \n    for (int t = 0; t < T; t++) {\n        int N, M;\n        double C;\n        cin >> N >> C >> M;\n        \n        vector<Edge> edges(M);\n        vector<int> parent(N);\n        \n        for (int i = 0; i < M; i++) {\n            int u, v;\n            cin >> u >> v;\n            edges[i].u = u - 1;\n            edges[i].v = v - 1;\n            edges[i].weight = 0.0;\n        }\n        \n        double totalWeight = findMST(N, M, edges, parent);\n        \n        cout << fixed;\n        cout.precision(9);\n        cout << totalWeight + (N - 1 - M) * C << endl;\n    }\n    \n    return 0;\n}",
      "Java": "// COMPLETE, COMPILABLE Java code\nimport java.util.Arrays;\nimport java.util.Scanner;\n\npublic class Main {\n    // Structure to represent an edge in the graph\n    static class Edge implements Comparable<Edge> {\n        int u, v;\n        double weight;\n        \n        public Edge(int u, int v, double weight) {\n            this.u = u;\n            this.v = v;\n            this.weight = weight;\n        }\n        \n        @Override\n        public int compareTo(Edge o) {\n            return Double.compare(weight, o.weight);\n        }\n    }\n    \n    // Function to find the minimum spanning tree using Kruskal's algorithm\n    public static double findMST(int N, int M, Edge[] edges, int[] parent) {\n        double totalWeight = 0.0;\n        int numEdges = 0;\n        \n        // Sort the edges in non-decreasing order of their weights\n        Arrays.sort(edges);\n        \n        // Initialize the parent array for the disjoint set data structure\n        for (int i = 0; i < N; i++) {\n            parent[i] = i;\n        }\n        \n        // Iterate over the sorted edges and add them to the minimum spanning tree if they do not form a cycle\n        for (int i = 0; i < M; i++) {\n            int u = edges[i].u;\n            int v = edges[i].v;\n            \n            // Find the roots of the sets containing u and v\n            int rootU = find(parent, u);\n            int rootV = find(parent, v);\n            \n            // If u and v are not in the same set, add the edge to the minimum spanning tree and merge the sets\n            if (rootU != rootV) {\n                totalWeight += edges[i].weight;\n                numEdges++;\n                parent[rootU] = rootV;\n            }\n        }\n        \n        return totalWeight;\n    }\n    \n    // Function to find the root of a set in the disjoint set data structure\n    public static int find(int[] parent, int x) {\n        if (parent[x] != x) {\n            parent[x] = find(parent, parent[x]);\n        }\n        return parent[x];\n    }\n    \n    public static void main(String[] args) {\n        Scanner scanner = new Scanner(System.in);\n        int T = scanner.nextInt();\n        \n        for (int t = 0; t < T; t++) {\n            int N = scanner.nextInt();\n            double C = scanner.nextDouble();\n            int M = scanner.nextInt();\n            \n            Edge[] edges = new Edge[M];\n            int[] parent = new int[N];\n            \n            for (int i = 0; i < M; i++) {\n                int u = scanner.nextInt();\n                int v = scanner.nextInt();\n                edges[i] = new Edge(u - 1, v - 1, 0.0);\n            }\n            \n            double totalWeight = findMST(N, M, edges, parent);\n            \n            System.out.printf(\"%.9f\\n\", totalWeight + (N - 1 - M) * C);\n        }\n        \n        scanner.close();\n    }\n}",
      "Python3": "# COMPLETE, RUNNABLE Python3 code\nimport sys\n\n# Structure to represent an edge in the graph\nclass Edge:\n    def __init__(self, u, v, weight):\n        self.u = u\n        self.v = v\n        self.weight = weight\n    \n    def __lt__(self, other):\n        return self.weight < other.weight\n\n# Function to find the minimum spanning tree using Kruskal's algorithm\ndef findMST(N, M, edges, parent):\n    totalWeight = 0.0\n    numEdges = 0\n    \n    # Sort the edges in non-decreasing order of their weights\n    edges.sort()\n    \n    # Initialize the parent array for the disjoint set data structure\n    for i in range(N):\n        parent[i] = i\n    \n    # Iterate over the sorted edges and add them to the minimum spanning tree if they do not form a cycle\n    for i in range(M):\n        u = edges[i].u\n        v = edges[i].v\n        \n        # Find the roots of the sets containing u and v\n        rootU = find(parent, u)\n        rootV = find(parent, v)\n        \n        # If u and v are not in the same set, add the edge to the minimum spanning tree and merge the sets\n        if rootU != rootV:\n            totalWeight += edges[i].weight\n            numEdges += 1\n            parent[rootU] = rootV\n    \n    return totalWeight\n\n# Function to find the root of a set in the disjoint set data structure\ndef find(parent, x):\n    if parent[x] != x:\n        parent[x] = find(parent, parent[x])\n    return parent[x]\n\n# Read input from standard input\nT = int(sys.stdin.readline().strip())\n\nfor t in range(T):\n    N, C, M = map(float, sys.stdin.readline().strip().split())\n    N = int(N)\n    M = int(M)\n    \n    edges = []\n    parent = [0] * N\n    \n    for i in range(M):\n        u, v = map(int, sys.stdin.readline().strip().split())\n        edges.append(Edge(u - 1, v - 1, 0.0))\n    \n    totalWeight = findMST(N, M, edges, parent)\n    \n    print(\"%.9f\" % (totalWeight + (N - 1 - M) * C))",
      "JavaScript": "// COMPLETE, RUNNABLE JavaScript code\nconst fs = require('fs');\nconst input = fs.readFileSync(0, 'utf-8').trim().split('\\n');\nlet idx = 0;\n\n// Structure to represent an edge in the graph\nclass Edge {\n    constructor(u, v, weight) {\n        this.u = u;\n        this.v = v;\n        this.weight = weight;\n    }\n}\n\n// Function to find the minimum spanning tree using Kruskal's algorithm\nfunction findMST(N, M, edges, parent) {\n    let totalWeight = 0.0;\n    let numEdges = 0;\n    \n    // Sort the edges in non-decreasing order of their weights\n    edges.sort((a, b) => a.weight - b.weight);\n    \n    // Initialize the parent array for the disjoint set data structure\n    for (let i = 0; i < N; i++) {\n        parent[i] = i;\n    }\n    \n    // Iterate over the sorted edges and add them to the minimum spanning tree if they do not form a cycle\n    for (let i = 0; i < M; i++) {\n        let u = edges[i].u;\n        let v = edges[i].v;\n        \n        // Find the roots of the sets containing u and v\n        let rootU = find(parent, u);\n        let rootV = find(parent, v);\n        \n        // If u and v are not in the same set, add the edge to the minimum spanning tree and merge the sets\n        if (rootU !== rootV) {\n            totalWeight += edges[i].weight;\n            numEdges++;\n            parent[rootU] = rootV;\n        }\n    }\n    \n    return totalWeight;\n}\n\n// Function to find the root of a set in the disjoint set data structure\nfunction find(parent, x) {\n    if (parent[x] !== x) {\n        parent[x] = find(parent, parent[x]);\n    }\n    return parent[x];\n}\n\n// Read input from standard input\nlet T = parseInt(input[idx++]);\n\nfor (let t = 0; t < T; t++) {\n    let [N, C, M] = input[idx++].trim().split(' ').map(Number);\n    \n    let edges = [];\n    let parent = new Array(N).fill(0);\n    \n    for (let i = 0; i < M; i++) {\n        let [u, v] = input[idx++].trim().split(' ').map(Number);\n        edges.push(new Edge(u - 1, v - 1, 0.0));\n    }\n    \n    let totalWeight = findMST(N, M, edges, parent);\n    \n    console.log((totalWeight + (N - 1 - M) * C).toFixed(9));\n}"
    }
  }
}