{
  "questionId": "Q5155",
  "title": "Battleship V",
  "slug": "battleship-v",
  "version": 1,
  "createdAt": "2026-07-01",
  "updatedAt": "2026-07-04",
  "metadata": {
    "category": "Number Theory",
    "difficulty": "Medium",
    "isPremium": true,
    "tags": [
      "GCD",
      "Inclusion‑Exclusion",
      "Prime Factorization",
      "Coprime Counting"
    ],
    "companyTags": [],
    "topicTags": [
      "Mathematics"
    ],
    "QBCategory": "CCD"
  },
  "content": {
    "problemStatement": "**This problem was part of the CodeChef April Challenge.  All user submissions for this contest problem are publicly available here.**\n\nIn the game of \"BattleShip V\", you control a cannon which is attacking a large enemy battleship, armed with many guns. Your goal is to destroy as many of the guns as possible.\n\nThe battle field is a 2D Cartesian grid, where your cannon is located at the origin.\n\nThe enemy battleship is a horizontal line segment located from the coordinates (X1 , Y) to (X2, Y). \nThere are exactly (X2 - X1 + 1) guns on the ship, located at the integer points (X1, Y), (X1+1, Y), ..., (X2, Y).\n\n\n\nHowever, the problem is, you cannot always fire at a gun. There are supernatural rocks located at all points of the battlefield whose X and Y coordinates are both integers. In order to fire successfully at an enemy's gun, the line connecting your cannon and that gun must not \ngo through any rocks.\n\n\n\nHow many guns you successfully destroy?",
    "constraints": [
      "1 ≤ t ≤ 100",
      "0 < |Y| ≤ 2100000000",
      "-2100000000 ≤ X1 ≤ X2 ≤ 2100000000"
    ],
    "inputFormat": "The first line contains an integer t, the number of test cases. Each of the next t lines contains three space‑separated integers Y, X1, X2 (0 < |Y| ≤ 2100000000, -2100000000 ≤ X1 ≤ X2 ≤ 2100000000).",
    "outputFormat": "For each test case output a single line containing the number of guns that can be destroyed.",
    "notes": [],
    "sampleTestCases": [
      {
        "id": "sample_1",
        "type": "reg",
        "input": "1\\n2 -2 1",
        "output": "2",
        "explanation": "Guns are at x = -2,-1,0,1 with Y = 2. gcd(|-2|,2)=2 (blocked), gcd(1,2)=1 (hit), gcd(0,2)=2 (blocked), gcd(1,2)=1 (hit). Hence 2 guns can be destroyed."
      }
    ]
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_01",
        "type": "reg",
        "label": "Case 1",
        "input": "",
        "expectedOutput": "",
        "weight": 4
      },
      {
        "id": "tc_02",
        "type": "reg",
        "label": "Case 2",
        "input": "",
        "expectedOutput": "",
        "weight": 4
      },
      {
        "id": "tc_03",
        "type": "reg",
        "label": "Case 3",
        "input": "",
        "expectedOutput": "",
        "weight": 4
      },
      {
        "id": "tc_04",
        "type": "reg",
        "label": "Case 4",
        "input": "",
        "expectedOutput": "",
        "weight": 4
      },
      {
        "id": "tc_05",
        "type": "reg",
        "label": "Case 5",
        "input": "",
        "expectedOutput": "",
        "weight": 4
      },
      {
        "id": "tc_06",
        "type": "reg",
        "label": "Case 1",
        "input": "",
        "expectedOutput": "",
        "weight": 4
      },
      {
        "id": "tc_07",
        "type": "reg",
        "label": "Case 2",
        "input": "",
        "expectedOutput": "",
        "weight": 4
      },
      {
        "id": "tc_08",
        "type": "reg",
        "label": "Case 3",
        "input": "",
        "expectedOutput": "",
        "weight": 4
      },
      {
        "id": "tc_09",
        "type": "reg",
        "label": "Case 4",
        "input": "",
        "expectedOutput": "",
        "weight": 4
      },
      {
        "id": "tc_10",
        "type": "reg",
        "label": "Case 5",
        "input": "",
        "expectedOutput": "",
        "weight": 4
      },
      {
        "id": "tc_11",
        "type": "reg",
        "label": "Case 1",
        "input": "",
        "expectedOutput": "",
        "weight": 3
      },
      {
        "id": "tc_12",
        "type": "reg",
        "label": "Case 2",
        "input": "",
        "expectedOutput": "",
        "weight": 3
      },
      {
        "id": "tc_13",
        "type": "reg",
        "label": "Case 3",
        "input": "",
        "expectedOutput": "",
        "weight": 3
      },
      {
        "id": "tc_14",
        "type": "reg",
        "label": "Case 4",
        "input": "",
        "expectedOutput": "",
        "weight": 3
      },
      {
        "id": "tc_15",
        "type": "reg",
        "label": "Case 5",
        "input": "",
        "expectedOutput": "",
        "weight": 3
      },
      {
        "id": "tc_16",
        "type": "reg",
        "label": "Case 1",
        "input": "",
        "expectedOutput": "",
        "weight": 3
      },
      {
        "id": "tc_17",
        "type": "reg",
        "label": "Case 2",
        "input": "",
        "expectedOutput": "",
        "weight": 3
      },
      {
        "id": "tc_18",
        "type": "reg",
        "label": "Case 3",
        "input": "",
        "expectedOutput": "",
        "weight": 3
      },
      {
        "id": "tc_19",
        "type": "reg",
        "label": "Case 4",
        "input": "",
        "expectedOutput": "",
        "weight": 3
      },
      {
        "id": "tc_20",
        "type": "reg",
        "label": "Case 5",
        "input": "",
        "expectedOutput": "",
        "weight": 3
      },
      {
        "id": "tc_21",
        "type": "reg",
        "label": "Case 1",
        "input": "",
        "expectedOutput": "",
        "weight": 3
      },
      {
        "id": "tc_22",
        "type": "reg",
        "label": "Case 2",
        "input": "",
        "expectedOutput": "",
        "weight": 3
      },
      {
        "id": "tc_23",
        "type": "reg",
        "label": "Case 3",
        "input": "",
        "expectedOutput": "",
        "weight": 3
      },
      {
        "id": "tc_24",
        "type": "reg",
        "label": "Case 4",
        "input": "",
        "expectedOutput": "",
        "weight": 3
      },
      {
        "id": "tc_25",
        "type": "reg",
        "label": "Case 5",
        "input": "",
        "expectedOutput": "",
        "weight": 3
      },
      {
        "id": "tc_26",
        "type": "reg",
        "label": "Case 1",
        "input": "",
        "expectedOutput": "",
        "weight": 3
      },
      {
        "id": "tc_27",
        "type": "reg",
        "label": "Case 2",
        "input": "",
        "expectedOutput": "",
        "weight": 3
      },
      {
        "id": "tc_28",
        "type": "reg",
        "label": "Case 3",
        "input": "",
        "expectedOutput": "",
        "weight": 3
      },
      {
        "id": "tc_29",
        "type": "reg",
        "label": "Case 4",
        "input": "",
        "expectedOutput": "",
        "weight": 3
      },
      {
        "id": "tc_30",
        "type": "reg",
        "label": "Case 5",
        "input": "",
        "expectedOutput": "",
        "weight": 3
      }
    ],
    "totalTestCases": 30,
    "totalWeight": 100
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      4,
      4,
      4,
      4,
      4,
      4,
      4,
      4,
      4,
      4,
      3,
      3,
      3,
      3,
      3,
      3,
      3,
      3,
      3,
      3,
      3,
      3,
      3,
      3,
      3,
      3,
      3,
      3,
      3,
      3
    ],
    "maxScore": 100
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ],
    "notes": []
  },
  "solution": {
    "approach": "For a gun at (x, Y) the line from (0,0) to (x,Y) passes through another integer point iff gcd(|x|,|Y|) > 1. Therefore a gun can be destroyed exactly when gcd(|x|,|Y|)=1. Let y = |Y|. If y = 1 every integer x is coprime, so answer = X2‑X1+1. Otherwise factorise y into its distinct prime factors p1…pk (k ≤ 10 because y ≤ 2·10^9). A number x is NOT coprime with y iff it is divisible by at least one pi. Using the inclusion‑exclusion principle we count how many x in [X1,X2] are divisible by any pi and subtract from the total length of the interval. For each non‑empty subset of the prime factors we compute the product d of the primes in the subset, count how many multiples of d lie in the interval, and add or subtract depending on the subset size parity. The counting of multiples uses the formula floor(R/d)‑floor((L‑1)/d). The overall complexity per test case is O(√y + 2^k), easily fast for the given limits.",
    "timeComplexity": "O(√|Y| + 2^{ω(|Y|)}) per test case, where ω(|Y|) is the number of distinct prime factors (≤10).",
    "spaceComplexity": "O(ω(|Y|)) for storing the distinct primes.",
    "code": {
      "C": "#include <stdio.h>\n#include <stdlib.h>\n#include <math.h>\n\nstatic void factorize(long long n, long long *primes, int *cnt) {\n    *cnt = 0;\n    for (long long p = 2; p * p <= n; ++p) {\n        if (n % p == 0) {\n            primes[(*cnt)++] = p;\n            while (n % p == 0) n /= p;\n        }\n    }\n    if (n > 1) primes[(*cnt)++] = n;\n}\n\nstatic long long countDivisible(long long L, long long R, long long d) {\n    if (d == 0) return 0;\n    // works for negative L,R because C truncates toward zero; the formula is still correct for counting multiples.\n    return R / d - (L - 1) / d;\n}\n\nint main() {\n    int T; if (scanf(\"%d\", &T) != 1) return 0;\n    while (T--) {\n        long long Y, X1, X2; scanf(\"%lld %lld %lld\", &Y, &X1, &X2);\n        long long y = llabs(Y);\n        long long total = X2 - X1 + 1;\n        if (y == 1) { printf(\"%lld\\n\", total); continue; }\n        long long primes[12]; int pcnt; factorize(y, primes, &pcnt);\n        long long bad = 0;\n        int subsets = 1 << pcnt;\n        for (int mask = 1; mask < subsets; ++mask) {\n            long long mult = 1;\n            int bits = 0;\n            for (int i = 0; i < pcnt; ++i) if (mask & (1 << i)) {\n                mult *= primes[i];\n                ++bits;\n            }\n            long long cnt = countDivisible(X1, X2, mult);\n            if (bits & 1) bad += cnt; else bad -= cnt;\n        }\n        printf(\"%lld\\n\", total - bad);\n    }\n    return 0;\n}",
      "C++": "#include <bits/stdc++.h>\nusing namespace std;\n\nvector<long long> factorize(long long n) {\n    vector<long long> primes;\n    for (long long p = 2; p * p <= n; ++p) {\n        if (n % p == 0) {\n            primes.push_back(p);\n            while (n % p == 0) n /= p;\n        }\n    }\n    if (n > 1) primes.push_back(n);\n    return primes;\n}\n\nlong long countDivisible(long long L, long long R, long long d) {\n    return R / d - (L - 1) / d;\n}\n\nint main() {\n    ios::sync_with_stdio(false);\n    cin.tie(nullptr);\n    int T; if (!(cin >> T)) return 0;\n    while (T--) {\n        long long Y, X1, X2; cin >> Y >> X1 >> X2;\n        long long y = llabs(Y);\n        long long total = X2 - X1 + 1;\n        if (y == 1) { cout << total << '\\n'; continue; }\n        vector<long long> primes = factorize(y);\n        int k = (int)primes.size();\n        long long bad = 0;\n        for (int mask = 1; mask < (1 << k); ++mask) {\n            long long mult = 1; int bits = 0;\n            for (int i = 0; i < k; ++i) if (mask & (1 << i)) {\n                mult *= primes[i]; ++bits;\n            }\n            long long cnt = countDivisible(X1, X2, mult);\n            if (bits & 1) bad += cnt; else bad -= cnt;\n        }\n        cout << total - bad << '\\n';\n    }\n    return 0;\n}",
      "Java": "import java.io.*;\nimport java.util.*;\n\npublic class Main {\n    private static List<Long> factorize(long n) {\n        List<Long> primes = new ArrayList<>();\n        for (long p = 2; p * p <= n; ++p) {\n            if (n % p == 0) {\n                primes.add(p);\n                while (n % p == 0) n /= p;\n            }\n        }\n        if (n > 1) primes.add(n);\n        return primes;\n    }\n    private static long countDivisible(long L, long R, long d) {\n        return R / d - (L - 1) / d;\n    }\n    public static void main(String[] args) throws Exception {\n        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));\n        int T = Integer.parseInt(br.readLine().trim());\n        StringBuilder sb = new StringBuilder();\n        while (T-- > 0) {\n            StringTokenizer st = new StringTokenizer(br.readLine());\n            long Y = Long.parseLong(st.nextToken());\n            long X1 = Long.parseLong(st.nextToken());\n            long X2 = Long.parseLong(st.nextToken());\n            long y = Math.abs(Y);\n            long total = X2 - X1 + 1;\n            if (y == 1) {\n                sb.append(total).append('\\n');\n                continue;\n            }\n            List<Long> primes = factorize(y);\n            int k = primes.size();\n            long bad = 0;\n            for (int mask = 1; mask < (1 << k); ++mask) {\n                long mult = 1; int bits = 0;\n                for (int i = 0; i < k; ++i) if ((mask & (1 << i)) != 0) {\n                    mult *= primes.get(i);\n                    bits++;\n                }\n                long cnt = countDivisible(X1, X2, mult);\n                if ((bits & 1) == 1) bad += cnt; else bad -= cnt;\n            }\n            sb.append(total - bad).append('\\n');\n        }\n        System.out.print(sb.toString());\n    }\n}",
      "Python3": "#!/usr/bin/env python3\nimport sys, math\n\ndef factorize(n):\n    primes = []\n    p = 2\n    while p * p <= n:\n        if n % p == 0:\n            primes.append(p)\n            while n % p == 0:\n                n //= p\n        p += 1 if p == 2 else 2\n    if n > 1:\n        primes.append(n)\n    return primes\n\ndef count_divisible(L, R, d):\n    return R // d - (L - 1) // d\n\ndef solve():\n    data = sys.stdin.read().strip().split()\n    if not data:\n        return\n    it = iter(data)\n    t = int(next(it))\n    out = []\n    for _ in range(t):\n        Y = int(next(it)); X1 = int(next(it)); X2 = int(next(it))\n        y = abs(Y)\n        total = X2 - X1 + 1\n        if y == 1:\n            out.append(str(total))\n            continue\n        primes = factorize(y)\n        k = len(primes)\n        bad = 0\n        for mask in range(1, 1 << k):\n            mult = 1\n            bits = 0\n            for i in range(k):\n                if mask & (1 << i):\n                    mult *= primes[i]\n                    bits += 1\n            cnt = count_divisible(X1, X2, mult)\n            if bits & 1:\n                bad += cnt\n            else:\n                bad -= cnt\n        out.append(str(total - bad))\n    sys.stdout.write('\\n'.join(out))\n\nif __name__ == \"__main__\":\n    solve()\n",
      "JavaScript": "const fs = require('fs');\nconst input = fs.readFileSync(0, 'utf-8').trim().split(/\\s+/);\nlet idx = 0;\nconst T = Number(input[idx++]);\nlet results = [];\nfunction factorize(n) {\n  const primes = [];\n  let p = 2;\n  while (p * p <= n) {\n    if (n % p === 0) {\n      primes.push(p);\n      while (n % p === 0) n = Math.floor(n / p);\n    }\n    p = p === 2 ? 3 : p + 2;\n  }\n  if (n > 1) primes.push(n);\n  return primes;\n}\nfunction countDivisible(L, R, d) {\n  return Math.floor(R / d) - Math.floor((L - 1) / d);\n}\nfor (let tc = 0; tc < T; ++tc) {\n  const Y = Number(input[idx++]);\n  const X1 = Number(input[idx++]);\n  const X2 = Number(input[idx++]);\n  const y = Math.abs(Y);\n  const total = X2 - X1 + 1;\n  if (y === 1) { results.push(String(total)); continue; }\n  const primes = factorize(y);\n  const k = primes.length;\n  let bad = 0;\n  const subsets = 1 << k;\n  for (let mask = 1; mask < subsets; ++mask) {\n    let mult = 1, bits = 0;\n    for (let i = 0; i < k; ++i) if (mask & (1 << i)) {\n      mult *= primes[i];\n      ++bits;\n    }\n    const cnt = countDivisible(X1, X2, mult);\n    if (bits & 1) bad += cnt; else bad -= cnt;\n  }\n  results.push(String(total - bad));\n}\nconsole.log(results.join('\\n'));\n"
    },
    "notes": [],
    "editorial": {
      "bruteForce": {
        "description": "",
        "complexity": ""
      },
      "better": {
        "description": "",
        "complexity": ""
      },
      "optimal": {
        "description": "",
        "complexity": ""
      }
    }
  },
  "hints": [],
  "followUps": [],
  "learningObjectives": [],
  "similarProblems": [],
  "acceptanceRate": null,
  "estimatedSolveTime": "",
  "recommendedAfter": [],
  "boilerPlates": {
    "C": "#include <stdio.h>\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "C++": "#include <iostream>\nusing namespace std;\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "Java": "import java.util.*;\nimport java.io.*;\n\npublic class Main {\n    public static void main(String[] args) throws IOException {\n        // your code goes here\n    }\n}",
    "Python3": "# your code goes here\n",
    "JavaScript": "// your code goes here\n",
    "solution": {
      "approach": "For a gun at (x, Y) the line from (0,0) to (x,Y) passes through another integer point iff gcd(|x|,|Y|) > 1. Therefore a gun can be destroyed exactly when gcd(|x|,|Y|)=1. Let y = |Y|. If y = 1 every integer x is coprime, so answer = X2‑X1+1. Otherwise factorise y into its distinct prime factors p1…pk (k ≤ 10 because y ≤ 2·10^9). A number x is NOT coprime with y iff it is divisible by at least one pi. Using the inclusion‑exclusion principle we count how many x in [X1,X2] are divisible by any pi and subtract from the total length of the interval. For each non‑empty subset of the prime factors we compute the product d of the primes in the subset, count how many multiples of d lie in the interval, and add or subtract depending on the subset size parity. The counting of multiples uses the formula floor(R/d)‑floor((L‑1)/d). The overall complexity per test case is O(√y + 2^k), easily fast for the given limits.",
      "timeComplexity": "O(√|Y| + 2^{ω(|Y|)}) per test case, where ω(|Y|) is the number of distinct prime factors (≤10).",
      "spaceComplexity": "O(ω(|Y|)) for storing the distinct primes.",
      "code": {
        "C": "#include <stdio.h>\n#include <stdlib.h>\n#include <math.h>\n\nstatic void factorize(long long n, long long *primes, int *cnt) {\n    *cnt = 0;\n    for (long long p = 2; p * p <= n; ++p) {\n        if (n % p == 0) {\n            primes[(*cnt)++] = p;\n            while (n % p == 0) n /= p;\n        }\n    }\n    if (n > 1) primes[(*cnt)++] = n;\n}\n\nstatic long long countDivisible(long long L, long long R, long long d) {\n    if (d == 0) return 0;\n    // works for negative L,R because C truncates toward zero; the formula is still correct for counting multiples.\n    return R / d - (L - 1) / d;\n}\n\nint main() {\n    int T; if (scanf(\"%d\", &T) != 1) return 0;\n    while (T--) {\n        long long Y, X1, X2; scanf(\"%lld %lld %lld\", &Y, &X1, &X2);\n        long long y = llabs(Y);\n        long long total = X2 - X1 + 1;\n        if (y == 1) { printf(\"%lld\\n\", total); continue; }\n        long long primes[12]; int pcnt; factorize(y, primes, &pcnt);\n        long long bad = 0;\n        int subsets = 1 << pcnt;\n        for (int mask = 1; mask < subsets; ++mask) {\n            long long mult = 1;\n            int bits = 0;\n            for (int i = 0; i < pcnt; ++i) if (mask & (1 << i)) {\n                mult *= primes[i];\n                ++bits;\n            }\n            long long cnt = countDivisible(X1, X2, mult);\n            if (bits & 1) bad += cnt; else bad -= cnt;\n        }\n        printf(\"%lld\\n\", total - bad);\n    }\n    return 0;\n}",
        "C++": "#include <bits/stdc++.h>\nusing namespace std;\n\nvector<long long> factorize(long long n) {\n    vector<long long> primes;\n    for (long long p = 2; p * p <= n; ++p) {\n        if (n % p == 0) {\n            primes.push_back(p);\n            while (n % p == 0) n /= p;\n        }\n    }\n    if (n > 1) primes.push_back(n);\n    return primes;\n}\n\nlong long countDivisible(long long L, long long R, long long d) {\n    return R / d - (L - 1) / d;\n}\n\nint main() {\n    ios::sync_with_stdio(false);\n    cin.tie(nullptr);\n    int T; if (!(cin >> T)) return 0;\n    while (T--) {\n        long long Y, X1, X2; cin >> Y >> X1 >> X2;\n        long long y = llabs(Y);\n        long long total = X2 - X1 + 1;\n        if (y == 1) { cout << total << '\\n'; continue; }\n        vector<long long> primes = factorize(y);\n        int k = (int)primes.size();\n        long long bad = 0;\n        for (int mask = 1; mask < (1 << k); ++mask) {\n            long long mult = 1; int bits = 0;\n            for (int i = 0; i < k; ++i) if (mask & (1 << i)) {\n                mult *= primes[i]; ++bits;\n            }\n            long long cnt = countDivisible(X1, X2, mult);\n            if (bits & 1) bad += cnt; else bad -= cnt;\n        }\n        cout << total - bad << '\\n';\n    }\n    return 0;\n}",
        "Java": "import java.io.*;\nimport java.util.*;\n\npublic class Main {\n    private static List<Long> factorize(long n) {\n        List<Long> primes = new ArrayList<>();\n        for (long p = 2; p * p <= n; ++p) {\n            if (n % p == 0) {\n                primes.add(p);\n                while (n % p == 0) n /= p;\n            }\n        }\n        if (n > 1) primes.add(n);\n        return primes;\n    }\n    private static long countDivisible(long L, long R, long d) {\n        return R / d - (L - 1) / d;\n    }\n    public static void main(String[] args) throws Exception {\n        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));\n        int T = Integer.parseInt(br.readLine().trim());\n        StringBuilder sb = new StringBuilder();\n        while (T-- > 0) {\n            StringTokenizer st = new StringTokenizer(br.readLine());\n            long Y = Long.parseLong(st.nextToken());\n            long X1 = Long.parseLong(st.nextToken());\n            long X2 = Long.parseLong(st.nextToken());\n            long y = Math.abs(Y);\n            long total = X2 - X1 + 1;\n            if (y == 1) {\n                sb.append(total).append('\\n');\n                continue;\n            }\n            List<Long> primes = factorize(y);\n            int k = primes.size();\n            long bad = 0;\n            for (int mask = 1; mask < (1 << k); ++mask) {\n                long mult = 1; int bits = 0;\n                for (int i = 0; i < k; ++i) if ((mask & (1 << i)) != 0) {\n                    mult *= primes.get(i);\n                    bits++;\n                }\n                long cnt = countDivisible(X1, X2, mult);\n                if ((bits & 1) == 1) bad += cnt; else bad -= cnt;\n            }\n            sb.append(total - bad).append('\\n');\n        }\n        System.out.print(sb.toString());\n    }\n}",
        "Python3": "#!/usr/bin/env python3\nimport sys, math\n\ndef factorize(n):\n    primes = []\n    p = 2\n    while p * p <= n:\n        if n % p == 0:\n            primes.append(p)\n            while n % p == 0:\n                n //= p\n        p += 1 if p == 2 else 2\n    if n > 1:\n        primes.append(n)\n    return primes\n\ndef count_divisible(L, R, d):\n    return R // d - (L - 1) // d\n\ndef solve():\n    data = sys.stdin.read().strip().split()\n    if not data:\n        return\n    it = iter(data)\n    t = int(next(it))\n    out = []\n    for _ in range(t):\n        Y = int(next(it)); X1 = int(next(it)); X2 = int(next(it))\n        y = abs(Y)\n        total = X2 - X1 + 1\n        if y == 1:\n            out.append(str(total))\n            continue\n        primes = factorize(y)\n        k = len(primes)\n        bad = 0\n        for mask in range(1, 1 << k):\n            mult = 1\n            bits = 0\n            for i in range(k):\n                if mask & (1 << i):\n                    mult *= primes[i]\n                    bits += 1\n            cnt = count_divisible(X1, X2, mult)\n            if bits & 1:\n                bad += cnt\n            else:\n                bad -= cnt\n        out.append(str(total - bad))\n    sys.stdout.write('\\n'.join(out))\n\nif __name__ == \"__main__\":\n    solve()\n",
        "JavaScript": "const fs = require('fs');\nconst input = fs.readFileSync(0, 'utf-8').trim().split(/\\s+/);\nlet idx = 0;\nconst T = Number(input[idx++]);\nlet results = [];\nfunction factorize(n) {\n  const primes = [];\n  let p = 2;\n  while (p * p <= n) {\n    if (n % p === 0) {\n      primes.push(p);\n      while (n % p === 0) n = Math.floor(n / p);\n    }\n    p = p === 2 ? 3 : p + 2;\n  }\n  if (n > 1) primes.push(n);\n  return primes;\n}\nfunction countDivisible(L, R, d) {\n  return Math.floor(R / d) - Math.floor((L - 1) / d);\n}\nfor (let tc = 0; tc < T; ++tc) {\n  const Y = Number(input[idx++]);\n  const X1 = Number(input[idx++]);\n  const X2 = Number(input[idx++]);\n  const y = Math.abs(Y);\n  const total = X2 - X1 + 1;\n  if (y === 1) { results.push(String(total)); continue; }\n  const primes = factorize(y);\n  const k = primes.length;\n  let bad = 0;\n  const subsets = 1 << k;\n  for (let mask = 1; mask < subsets; ++mask) {\n    let mult = 1, bits = 0;\n    for (let i = 0; i < k; ++i) if (mask & (1 << i)) {\n      mult *= primes[i];\n      ++bits;\n    }\n    const cnt = countDivisible(X1, X2, mult);\n    if (bits & 1) bad += cnt; else bad -= cnt;\n  }\n  results.push(String(total - bad));\n}\nconsole.log(results.join('\\n'));\n"
      }
    },
    "_internal": {
      "methodName": "",
      "params": [],
      "phase": "ready-for-judge"
    },
    "verified": true
  },
  "_internal": {},
  "verified": false
}
