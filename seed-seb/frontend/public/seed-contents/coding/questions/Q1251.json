{
  "questionId": "Q1251",
  "title": "Fractional Knapsack Problem",
  "slug": "fractional-knapsack-problem",
  "version": 1,
  "createdAt": "2026-07-01",
  "updatedAt": "2026-07-05",
  "metadata": {
    "category": "Greedy",
    "difficulty": "Easy",
    "isPremium": false,
    "tags": [
      "Sorting",
      "Fractional Knapsack",
      "Greedy"
    ],
    "companyTags": [],
    "topicTags": [
      "Greedy Algorithms"
    ]
  },
  "content": {
    "problemStatement": "Let's try to solve the Fractional Knapsack Problem.\n\nYou have a knapsack that can take upto WmaxW_{max}Wmax\u200b weight. \n\nGiven NNN items, ithi^{th}ith of them having the weight WiW_iWi\u200b and value ViV_iVi\u200b. You can pick some or all the items completely or fractionally so that their weight does not exceed WmaxW_{max}Wmax\u200b. \n\nYour task is to maximise the total value of the items you will carry in your bag.\n\n**Note -** If you take ab\\frac{a}{b}ba\u200b fraction of an item (b&gt;a)(b &gt; a)(b&gt;a), it's weight will be aWib\\frac{a W_i}{b}baWi\u200b\u200b and it's value will be aVib\\frac{a V_i}{b}baVi\u200b\u200b.\n\nPrint the maximum value with relative error upto 10\u2212210^{-2}10\u22122.\n\n\nInput Format\n\nThe first line contains a two integer NNN and WmaxW_{max}Wmax\u200b, denoting the length of array AAA and the maximum weight you can carry.\nNext NNN lines contains the description of the items.\n\nithi^{th}ith line contains two integers WiW_iWi\u200b and ViV_iVi\u200b, denoting the Weight and Value of the ithi^{th}ith item\n\n\n\n\nOutput Format\nOutput the maximum value you can carry while not exceeding the weight limit of the knapsack.",
    "constraints": [
      "1 <= T <= 10",
      "1 <= N <= 100000",
      "1 <= Wmax <= 10^9",
      "1 <= Wi, Vi <= 10^9"
    ],
    "inputFormat": "First line contains an integer T, the number of test cases. For each test case, the first line contains two integers N and Wmax \u2014 the number of items and the maximum weight the knapsack can carry. Then N lines follow, each containing two integers Wi and Vi \u2014 the weight and value of the i\u2011th item.",
    "outputFormat": "For each test case output a single line with the maximum total value that can be carried, printed with exactly two digits after the decimal point.",
    "sampleTestCases": [
      {
        "id": "sample_1",
        "input": "1\n5 10\n3 2\n5 3\n2 1\n6 5\n1 1",
        "output": "8.00",
        "explanation": "Take whole items (6,5), (3,2) and (1,1). Total weight = 10, total value = 5+2+1 = 8.00."
      },
      {
        "id": "sample_2",
        "input": "1\n3 10\n1 1\n2 2\n3 3",
        "output": "6.00",
        "explanation": "All items fit completely, giving value 1+2+3 = 6.00."
      },
      {
        "id": "sample_3",
        "input": "1\\n1 10\\n10 10",
        "output": "10.00",
        "explanation": "The single item fits exactly, value = 10.00."
      }
    ]
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "label": "minimum boundary",
        "input": "1\n1 1\n1 1",
        "expectedOutput": "1.00",
        "weight": 2
      },
      {
        "id": "tc_2",
        "label": "Case 2",
        "input": "1\n1 1\n1 1",
        "expectedOutput": "1.00",
        "weight": 2
      },
      {
        "id": "tc_3",
        "label": "Case 3",
        "input": "1\n1 1\n1 1",
        "expectedOutput": "1.00",
        "weight": 2
      },
      {
        "id": "tc_4",
        "label": "Case 4",
        "input": "1\n1 1\n1 1",
        "expectedOutput": "1.00",
        "weight": 2
      },
      {
        "id": "tc_5",
        "label": "Case 5",
        "input": "1\n1 1\n1 1",
        "expectedOutput": "1.00",
        "weight": 2
      }
    ],
    "totalTestCases": 5,
    "totalWeight": 10
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      2,
      2,
      2,
      2,
      2
    ],
    "maxScore": 10
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "The fractional knapsack problem is solved greedily. Compute the value\u2011to\u2011weight ratio for each item. Sort items in decreasing order of this ratio. Then iterate over the sorted list, taking each whole item while the remaining capacity allows; when an item does not fit completely, take the exact fraction that fills the knapsack and stop. This yields the optimal total value because any other selection would replace a taken fraction with a lower\u2011ratio item, decreasing the total value.",
    "timeComplexity": "O(N log N) per test case for sorting; the subsequent scan is O(N).",
    "spaceComplexity": "O(N) to store the items and their ratios.",
    "code": {
      "C": "#include <stdio.h>\n#include <stdlib.h>\n\ntypedef struct { long long w; long long v; double r; } Item;\n\nint cmp(const void *a, const void *b) {\n    const Item *ia = (const Item *)a;\n    const Item *ib = (const Item *)b;\n    if (ib->r > ia->r) return 1;\n    if (ib->r < ia->r) return -1;\n    return 0;\n}\n\nint main() {\n    int T;\n    if (scanf(\"%d\", &T) != 1) return 0;\n    while (T--) {\n        int N;\n        long long Wmax;\n        scanf(\"%d %lld\", &N, &Wmax);\n        Item *items = (Item *)malloc(N * sizeof(Item));\n        for (int i = 0; i < N; ++i) {\n            scanf(\"%lld %lld\", &items[i].w, &items[i].v);\n            items[i].r = (double)items[i].v / (double)items[i].w;\n        }\n        qsort(items, N, sizeof(Item), cmp);\n        double total = 0.0;\n        long long remaining = Wmax;\n        for (int i = 0; i < N; ++i) {\n            if (items[i].w <= remaining) {\n                total += items[i].v;\n                remaining -= items[i].w;\n            } else {\n                total += items[i].v * ((double)remaining / (double)items[i].w);\n                break;\n            }\n        }\n        printf(\"%.2f\\n\", total);\n        free(items);\n    }\n    return 0;\n}",
      "C++": "#include <bits/stdc++.h>\nusing namespace std;\nstruct Item { long long w, v; double r; };\nint main() {\n    ios::sync_with_stdio(false);\n    cin.tie(nullptr);\n    int T; if(!(cin >> T)) return 0;\n    while (T--) {\n        int N; long long Wmax; cin >> N >> Wmax;\n        vector<Item> items(N);\n        for (int i = 0; i < N; ++i) {\n            cin >> items[i].w >> items[i].v;\n            items[i].r = (double)items[i].v / (double)items[i].w;\n        }\n        sort(items.begin(), items.end(), [](const Item& a, const Item& b){ return a.r > b.r; });\n        double total = 0.0;\n        long long remaining = Wmax;\n        for (const auto& it : items) {\n            if (it.w <= remaining) {\n                total += it.v;\n                remaining -= it.w;\n            } else {\n                total += it.v * ((double)remaining / (double)it.w);\n                break;\n            }\n        }\n        cout.setf(ios::fixed);\n        cout << setprecision(2) << total << '\\n';\n    }\n    return 0;\n}",
      "Java": "import java.io.*;\nimport java.util.*;\n\npublic class Main {\n    static class Item {\n        long w, v;\n        double r;\n        Item(long w, long v) { this.w = w; this.v = v; this.r = (double) v / (double) w; }\n    }\n    public static void main(String[] args) throws Exception {\n        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));\n        StringTokenizer st = new StringTokenizer(br.readLine());\n        int T = Integer.parseInt(st.nextToken());\n        StringBuilder sb = new StringBuilder();\n        while (T-- > 0) {\n            st = new StringTokenizer(br.readLine());\n            int N = Integer.parseInt(st.nextToken());\n            long Wmax = Long.parseLong(st.nextToken());\n            Item[] items = new Item[N];\n            for (int i = 0; i < N; ++i) {\n                st = new StringTokenizer(br.readLine());\n                long w = Long.parseLong(st.nextToken());\n                long v = Long.parseLong(st.nextToken());\n                items[i] = new Item(w, v);\n            }\n            Arrays.sort(items, (a, b) -> Double.compare(b.r, a.r));\n            double total = 0.0;\n            long remaining = Wmax;\n            for (Item it : items) {\n                if (it.w <= remaining) {\n                    total += it.v;\n                    remaining -= it.w;\n                } else {\n                    total += it.v * ((double) remaining / (double) it.w);\n                    break;\n                }\n            }\n            sb.append(String.format(java.util.Locale.US, \"%.2f\", total)).append('\\n');\n        }\n        System.out.print(sb.toString());\n    }\n}",
      "Python3": "import sys\n\ndef solve():\n    data = sys.stdin.buffer.read().split()\n    if not data:\n        return\n    it = iter(data)\n    T = int(next(it))\n    out_lines = []\n    for _ in range(T):\n        N = int(next(it))\n        Wmax = int(next(it))\n        items = []\n        for _ in range(N):\n            w = int(next(it))\n            v = int(next(it))\n            items.append((w, v, v / w))\n        items.sort(key=lambda x: x[2], reverse=True)\n        total = 0.0\n        remaining = Wmax\n        for w, v, r in items:\n            if w <= remaining:\n                total += v\n                remaining -= w\n            else:\n                total += v * (remaining / w)\n                break\n        out_lines.append(f\"{total:.2f}\")\n    sys.stdout.write(\"\\n\".join(out_lines))\n\nif __name__ == \"__main__\":\n    solve()",
      "JavaScript": "const fs = require('fs');\nconst input = fs.readFileSync(0, 'utf-8').trim().split(/\\s+/);\nlet idx = 0;\nconst T = Number(input[idx++]);\nlet results = [];\nfor (let t = 0; t < T; ++t) {\n  const N = Number(input[idx++]);\n  let Wmax = Number(input[idx++]);\n  let items = [];\n  for (let i = 0; i < N; ++i) {\n    const w = Number(input[idx++]);\n    const v = Number(input[idx++]);\n    items.push({w, v, r: v / w});\n  }\n  items.sort((a, b) => b.r - a.r);\n  let total = 0;\n  for (const it of items) {\n    if (it.w <= Wmax) {\n      total += it.v;\n      Wmax -= it.w;\n    } else {\n      total += it.v * (Wmax / it.w);\n      break;\n    }\n  }\n  results.push(total.toFixed(2));\n}\nconsole.log(results.join('\\n'));"
    }
  }
}