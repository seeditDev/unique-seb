{
  "questionId": "Q11905",
  "title": "KMP Algorithm or LPS array",
  "slug": "implement-strstr",
  "version": 1,
  "createdAt": "2026-07-05",
  "updatedAt": "2026-07-05",
  "metadata": {
    "category": "String",
    "difficulty": "Hard",
    "isPremium": true,
    "tags": [
      "KMP Algorithm",
      "Pattern Searching"
    ],
    "companyTags": [],
    "topicTags": [],
    "QBCategory": "CCD"
  },
  "content": {
    "problemStatement": "Given two strings, one is a text string and the other is a pattern string. Find and print the indices of all the occurrences of the pattern string within the text string. Use 0-based indexing for the indices, and ensure that the indices are in ascending order. If the pattern does not occur in the text, return an empty list.\n\n    Implement this solution using the Knuth-Morris-Pratt (KMP) algorithm for efficient pattern matching.",
    "constraints": [
      "1 <= length of text string <= 10^5",
      "1 <= length of pattern string <= 10^5",
      "Both strings consist of lowercase English letters."
    ],
    "inputFormat": "Two strings, one is a text string and the other is a pattern string.",
    "outputFormat": "A list of indices where the pattern string is found in the text string.",
    "notes": [],
    "sampleTestCases": []
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_01",
        "type": "reg",
        "label": "min boundary",
        "input": {
          "text": "a",
          "pattern": "a"
        },
        "expectedOutput": "[0]",
        "weight": 4
      },
      {
        "id": "tc_02",
        "type": "reg",
        "label": "max boundary",
        "input": {
          "text": "abcdefghijklmnopqrstuvwxyz",
          "pattern": "z"
        },
        "expectedOutput": "[25]",
        "weight": 4
      },
      {
        "id": "tc_03",
        "type": "reg",
        "label": "uniform case",
        "input": {
          "text": "aaaaaa",
          "pattern": "a"
        },
        "expectedOutput": "[0, 1, 2, 3, 4, 5]",
        "weight": 4
      },
      {
        "id": "tc_04",
        "type": "reg",
        "label": "tricky case",
        "input": {
          "text": "ababababab",
          "pattern": "aba"
        },
        "expectedOutput": "[0, 2, 4, 6, 8]",
        "weight": 4
      },
      {
        "id": "tc_05",
        "type": "reg",
        "label": "mixed case",
        "input": {
          "text": "ababcababcabc",
          "pattern": "abc"
        },
        "expectedOutput": "[2, 7, 12]",
        "weight": 4
      },
      {
        "id": "tc_06",
        "type": "reg",
        "label": "min boundary",
        "input": {
          "text": "a",
          "pattern": "a"
        },
        "expectedOutput": "[0]",
        "weight": 4
      },
      {
        "id": "tc_07",
        "type": "reg",
        "label": "max boundary",
        "input": {
          "text": "abcdefghijklmnopqrstuvwxyz",
          "pattern": "z"
        },
        "expectedOutput": "[25]",
        "weight": 4
      },
      {
        "id": "tc_08",
        "type": "reg",
        "label": "uniform case",
        "input": {
          "text": "aaaaaa",
          "pattern": "a"
        },
        "expectedOutput": "[0, 1, 2, 3, 4, 5]",
        "weight": 4
      },
      {
        "id": "tc_09",
        "type": "reg",
        "label": "tricky case",
        "input": {
          "text": "ababababab",
          "pattern": "aba"
        },
        "expectedOutput": "[0, 2, 4, 6, 8]",
        "weight": 4
      },
      {
        "id": "tc_10",
        "type": "reg",
        "label": "mixed case",
        "input": {
          "text": "ababcababcabc",
          "pattern": "abc"
        },
        "expectedOutput": "[2, 7, 12]",
        "weight": 4
      },
      {
        "id": "tc_11",
        "type": "reg",
        "label": "min boundary",
        "input": {
          "text": "a",
          "pattern": "a"
        },
        "expectedOutput": "[0]",
        "weight": 3
      },
      {
        "id": "tc_12",
        "type": "reg",
        "label": "max boundary",
        "input": {
          "text": "abcdefghijklmnopqrstuvwxyz",
          "pattern": "z"
        },
        "expectedOutput": "[25]",
        "weight": 3
      },
      {
        "id": "tc_13",
        "type": "reg",
        "label": "uniform case",
        "input": {
          "text": "aaaaaa",
          "pattern": "a"
        },
        "expectedOutput": "[0, 1, 2, 3, 4, 5]",
        "weight": 3
      },
      {
        "id": "tc_14",
        "type": "reg",
        "label": "tricky case",
        "input": {
          "text": "ababababab",
          "pattern": "aba"
        },
        "expectedOutput": "[0, 2, 4, 6, 8]",
        "weight": 3
      },
      {
        "id": "tc_15",
        "type": "reg",
        "label": "mixed case",
        "input": {
          "text": "ababcababcabc",
          "pattern": "abc"
        },
        "expectedOutput": "[2, 7, 12]",
        "weight": 3
      },
      {
        "id": "tc_16",
        "type": "reg",
        "label": "min boundary",
        "input": {
          "text": "a",
          "pattern": "a"
        },
        "expectedOutput": "[0]",
        "weight": 3
      },
      {
        "id": "tc_17",
        "type": "reg",
        "label": "max boundary",
        "input": {
          "text": "abcdefghijklmnopqrstuvwxyz",
          "pattern": "z"
        },
        "expectedOutput": "[25]",
        "weight": 3
      },
      {
        "id": "tc_18",
        "type": "reg",
        "label": "uniform case",
        "input": {
          "text": "aaaaaa",
          "pattern": "a"
        },
        "expectedOutput": "[0, 1, 2, 3, 4, 5]",
        "weight": 3
      },
      {
        "id": "tc_19",
        "type": "reg",
        "label": "tricky case",
        "input": {
          "text": "ababababab",
          "pattern": "aba"
        },
        "expectedOutput": "[0, 2, 4, 6, 8]",
        "weight": 3
      },
      {
        "id": "tc_20",
        "type": "reg",
        "label": "mixed case",
        "input": {
          "text": "ababcababcabc",
          "pattern": "abc"
        },
        "expectedOutput": "[2, 7, 12]",
        "weight": 3
      },
      {
        "id": "tc_21",
        "type": "reg",
        "label": "min boundary",
        "input": {
          "text": "a",
          "pattern": "a"
        },
        "expectedOutput": "[0]",
        "weight": 3
      },
      {
        "id": "tc_22",
        "type": "reg",
        "label": "max boundary",
        "input": {
          "text": "abcdefghijklmnopqrstuvwxyz",
          "pattern": "z"
        },
        "expectedOutput": "[25]",
        "weight": 3
      },
      {
        "id": "tc_23",
        "type": "reg",
        "label": "uniform case",
        "input": {
          "text": "aaaaaa",
          "pattern": "a"
        },
        "expectedOutput": "[0, 1, 2, 3, 4, 5]",
        "weight": 3
      },
      {
        "id": "tc_24",
        "type": "reg",
        "label": "tricky case",
        "input": {
          "text": "ababababab",
          "pattern": "aba"
        },
        "expectedOutput": "[0, 2, 4, 6, 8]",
        "weight": 3
      },
      {
        "id": "tc_25",
        "type": "reg",
        "label": "mixed case",
        "input": {
          "text": "ababcababcabc",
          "pattern": "abc"
        },
        "expectedOutput": "[2, 7, 12]",
        "weight": 3
      },
      {
        "id": "tc_26",
        "type": "reg",
        "label": "min boundary",
        "input": {
          "text": "a",
          "pattern": "a"
        },
        "expectedOutput": "[0]",
        "weight": 3
      },
      {
        "id": "tc_27",
        "type": "reg",
        "label": "max boundary",
        "input": {
          "text": "abcdefghijklmnopqrstuvwxyz",
          "pattern": "z"
        },
        "expectedOutput": "[25]",
        "weight": 3
      },
      {
        "id": "tc_28",
        "type": "reg",
        "label": "uniform case",
        "input": {
          "text": "aaaaaa",
          "pattern": "a"
        },
        "expectedOutput": "[0, 1, 2, 3, 4, 5]",
        "weight": 3
      },
      {
        "id": "tc_29",
        "type": "reg",
        "label": "tricky case",
        "input": {
          "text": "ababababab",
          "pattern": "aba"
        },
        "expectedOutput": "[0, 2, 4, 6, 8]",
        "weight": 3
      },
      {
        "id": "tc_30",
        "type": "reg",
        "label": "mixed case",
        "input": {
          "text": "ababcababcabc",
          "pattern": "abc"
        },
        "expectedOutput": "[2, 7, 12]",
        "weight": 3
      }
    ],
    "totalTestCases": 30,
    "totalWeight": 100
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      4,
      4,
      4,
      4,
      4,
      4,
      4,
      4,
      4,
      4,
      3,
      3,
      3,
      3,
      3,
      3,
      3,
      3,
      3,
      3,
      3,
      3,
      3,
      3,
      3,
      3,
      3,
      3,
      3,
      3
    ],
    "maxScore": 100
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ],
    "notes": []
  },
  "solution": {
    "approach": "Preprocess the pattern to build the LPS array, then use this array to efficiently search for the pattern in the text.",
    "timeComplexity": "O(n + m)",
    "spaceComplexity": "O(m)",
    "code": {
      "C": "#include <stdio.h>\n#include <string.h>\n\nint computeLPS(char *pattern, int M, int *lps) {\n    int len = 0;\n    int i = 1;\n    while (i < M) {\n        if (pattern[i] == pattern[len]) {\n            len++;\n            lps[i] = len;\n            i++;\n        } else {\n            if (len != 0) {\n                len = lps[len - 1];\n            } else {\n                lps[i] = 0;\n                i++;\n            }\n        }\n    }\n    return 0;\n}\n\nint KMP(char *text, char *pattern) {\n    int N = strlen(text);\n    int M = strlen(pattern);\n    int lps[M];\n    computeLPS(pattern, M, lps);\n    int i = 0;\n    int j = 0;\n    while (i < N) {\n        if (pattern[j] == text[i]) {\n            i++;\n            j++;\n        }\n        if (j == M) {\n            printf(\"Pattern found at index %d\\n\", i - j);\n            j = lps[j - 1];\n        } else if (i < N && pattern[j] != text[i]) {\n            if (j != 0) {\n                j = lps[j - 1];\n            } else {\n                i++;\n            }\n        }\n    }\n    return 0;\n}\n\nint main() {\n    char text[] = \"ababcababcabc\";\n    char pattern[] = \"abc\";\n    KMP(text, pattern);\n    return 0;\n}",
      "C++": "#include <bits/stdc++.h>\nusing namespace std;\n\n// Class that contains the KMP algorithm and LPS array construction\nclass Solution {\npublic:\n    // Function to compute the LPS (Longest Prefix Suffix) array\n    vector<int> computeLPS(string pattern) {\n        // Create LPS array initialized with 0\n        vector<int> lps(pattern.length(), 0);\n\n        // Length of the previous longest prefix which is also a suffix\n        int len = 0;\n\n        // Start from the second character of the pattern\n        int i = 1;\n\n        // Loop to fill the LPS array\n        while (i < pattern.length()) {\n            // If current character matches the character at len\n            if (pattern[i] == pattern[len]) {\n                // Increase length of current matching prefix\n                len++;\n                // Assign it to lps[i]\n                lps[i] = len;\n                // Move to next index\n                i++;\n            } else {\n                // If mismatch and len is not 0, try shorter previous prefix\n                if (len != 0) {\n                    len = lps[len - 1];\n                } else {\n                    // If no prefix match found, set lps[i] to 0\n                    lps[i] = 0;\n                    // Move to next index\n                    i++;\n                }\n            }\n        }\n        // Return the complete LPS array\n        return lps;\n    }\n\n    // Function to search pattern in text using KMP\n    vector<int> KMP(string text, string pattern) {\n        // Preprocess the pattern and compute LPS array\n        vector<int> lps = computeLPS(pattern);\n        // Vector to store the starting indices of all matches\n        vector<int> result;\n        // Pointers for text and pattern\n        int i = 0;\n        int j = 0;\n\n        // Loop through the text\n        while (i < text.length()) {\n            // If characters match, move both pointers\n            if (text[i] == pattern[j]) {\n                i++;\n                j++;\n            }\n            // If the whole pattern is matched\n            if (j == pattern.length()) {\n                // Store the index of match\n                result.push_back(i - j);\n\n                // Use LPS to continue searching\n                j = lps[j - 1];\n            }\n            // If mismatch occurs after some matches\n            else if (i < text.length() && text[i] != pattern[j]) {\n                // Use LPS to avoid unnecessary comparisons\n                if (j != 0) {\n                    j = lps[j - 1];\n                } else {\n\n                    // Move forward in text if j is at start\n                    i++;\n                }\n            }\n        }\n        // Return all found indices\n        return result;\n    }\n};\n\n// Main function to test the KMP search\nint main() {\n    // Define the text string\n    string text = \"ababcababcabc\";\n    // Define the pattern to search\n    string pattern = \"abc\";\n\n    // Create an object of the Solution class\n    Solution sol;\n\n    // Call the KMP function to get all match indices\n    vector<int> matches = sol.KMP(text, pattern);\n\n    // Output the results\n    cout << \"Pattern found at indices: \";\n    for (int idx : matches) {\n        cout << idx << \" \";\n    }\n    cout << endl;\n\n    return 0;\n}",
      "Java": "import java.util.*;\n\nclass Solution {\n\n    // Function to compute the LPS (Longest Prefix which is also Suffix) array\n    public int[] computeLPS(String pattern) {\n\n        // Create LPS array\n        int[] lps = new int[pattern.length()];\n\n        // Length of the previous longest prefix suffix\n        int len = 0;\n\n        // Start from index 1 (as lps[0] is always 0)\n        int i = 1;\n\n        // Loop to fill lps[i] for i = 1 to pattern.length() - 1\n        while (i < pattern.length()) {\n\n            // If current character matches with the character at current length\n            if (pattern.charAt(i) == pattern.charAt(len)) {\n                len++;\n                lps[i] = len;\n                i++;\n            } else {\n\n                // If mismatch occurs after at least one match\n                if (len != 0) {\n                    len = lps[len - 1];\n                } else {\n\n                    // No prefix match, set lps[i] to 0\n                    lps[i] = 0;\n                    i++;\n                }\n            }\n        }\n\n        // Return the final LPS array\n        return lps;\n    }\n\n    // Function to perform KMP pattern searching\n    public List<Integer> KMP(String text, String pattern) {\n\n        // Preprocess the pattern to get LPS array\n        int[] lps = computeLPS(pattern);\n\n        // Result list to store indices where pattern is found\n        List<Integer> result = new ArrayList<>();\n\n        // Indices for text and pattern\n        int i = 0, j = 0;\n\n        // Traverse the entire text\n        while (i < text.length()) {\n\n            // If characters match, increment both pointers\n            if (text.charAt(i) == pattern.charAt(j)) {\n                i++;\n                j++;\n            }\n\n            // If full pattern matched\n            if (j == pattern.length()) {\n                result.add(i - j);\n                j = lps[j - 1];\n            }\n\n            // If mismatch occurs after some matches\n            else if (i < text.length() && text.charAt(i) != pattern.charAt(j)) {\n\n                // Use LPS to skip unnecessary comparisons\n                if (j != 0) {\n                    j = lps[j - 1];\n                } else {\n                    i++;\n                }\n            }\n        }\n\n        // Return the list of matching indices\n        return result;\n    }\n}\n\npublic class Main {\n    public static void main(String[] args) {\n\n        // Create object of Solution class\n        Solution sol = new Solution();\n\n        // Define text and pattern\n        String text = \"ababcababcabc\";\n        String pattern = \"abc\";\n\n        // Call the KMP function\n        List<Integer> matches = sol.KMP(text, pattern);\n\n        // Print the result\n        System.out.println(\"Pattern found at indices: \" + matches);\n    }\n}",
      "Python3": "class Solution:\n\n    # Function to compute the LPS array\n    def computeLPS(self, pattern):\n\n        # Create lps array\n        lps = [0] * len(pattern)\n\n        # Length of the previous longest prefix suffix\n        length = 0\n\n        # Start from second character\n        i = 1\n\n        # Build the lps array\n        while i < len(pattern):\n\n            # If characters match\n            if pattern[i] == pattern[length]:\n                length += 1\n                lps[i] = length\n                i += 1\n            else:\n\n                # If mismatch and length > 0\n                if length != 0:\n                    length = lps[length - 1]\n                else:\n\n                    # Set lps[i] to 0 and move on\n                    lps[i] = 0\n                    i += 1\n\n        # Return computed lps\n        return lps\n\n    # KMP search function\n    def KMP(self, text, pattern):\n\n        # Compute the LPS array\n        lps = self.computeLPS(pattern)\n\n        # List to store match indices\n        result = []\n\n        # Indices for text and pattern\n        i = j = 0\n\n        # Scan through the text\n        while i < len(text):\n\n            # If characters match\n            if text[i] == pattern[j]:\n                i += 1\n                j += 1\n\n            # Full match found\n            if j == len(pattern):\n                result.append(i - j)\n                j = lps[j - 1]\n\n            # Mismatch after some matches\n            elif i < len(text) and text[i] != pattern[j]:\n                if j != 0:\n                    j = lps[j - 1]\n                else:\n                    i += 1\n\n        # Return the result\n        return result\n\n\n# Main test\nsol = Solution()\ntext = \"ababcababcabc\"\npattern = \"abc\"\nprint(\"Pattern found at indices:\", sol.KMP(text, pattern))",
      "JavaScript": "class Solution {\n\n    // Function to compute the LPS array\n    computeLPS(pattern) {\n\n        // Create LPS array\n        const lps = new Array(pattern.length).fill(0);\n\n        // Length of previous longest prefix\n        let len = 0;\n\n        // Start from index 1\n        let i = 1;\n\n        // Build the LPS array\n        while (i < pattern.length) {\n\n            // If match found\n            if (pattern[i] === pattern[len]) {\n                len++;\n                lps[i] = len;\n                i++;\n            } else {\n\n                // Use previous lps if mismatch\n                if (len !== 0) {\n                    len = lps[len - 1];\n                } else {\n\n                    // Assign 0 and move ahead\n                    lps[i] = 0;\n                    i++;\n                }\n            }\n        }\n\n        // Return LPS array\n        return lps;\n    }\n\n    // KMP pattern search function\n    KMP(text, pattern) {\n\n        // Preprocess pattern\n        const lps = this.computeLPS(pattern);\n\n        // List to store match indices\n        const result = [];\n\n        // Pointers for text and pattern\n        let i = 0, j = 0;\n\n        // Traverse the text\n        while (i < text.length) {\n\n            // If characters match\n            if (text[i] === pattern[j]) {\n                i++;\n                j++;\n            }\n\n            // Full match found\n            if (j === pattern.length) {\n                result.push(i - j);\n                j = lps[j - 1];\n            }\n\n            // Mismatch after some matches\n            else if (i < text.length && text[i] !== pattern[j]) {\n                if (j !== 0) {\n                    j = lps[j - 1];\n                } else {\n                    i++;\n                }\n            }\n        }\n\n        // Return matched indices\n        return result;\n    }\n}\n\n// Main test\nconst sol = new Solution();\nconst text = \"ababcababcabc\";\nconst pattern = \"abc\";\nconsole.log(\"Pattern found at indices:\", sol.KMP(text, pattern));"
    },
    "notes": [],
    "editorial": {
      "bruteForce": {
        "description": "",
        "complexity": ""
      },
      "better": {
        "description": "",
        "complexity": ""
      },
      "optimal": {
        "description": "",
        "complexity": ""
      }
    }
  },
  "hints": [],
  "followUps": [],
  "learningObjectives": [],
  "similarProblems": [],
  "acceptanceRate": null,
  "estimatedSolveTime": "",
  "recommendedAfter": [],
  "boilerPlates": {
    "C": "#include <stdio.h>\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "C++": "#include <iostream>\nusing namespace std;\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "Java": "import java.util.*;\nimport java.io.*;\n\npublic class Main {\n    public static void main(String[] args) throws IOException {\n        // your code goes here\n    }\n}",
    "Python3": "# your code goes here\n",
    "JavaScript": "// your code goes here\n",
    "solution": {
      "approach": "Preprocess the pattern to build the LPS array, then use this array to efficiently search for the pattern in the text.",
      "timeComplexity": "O(n + m)",
      "spaceComplexity": "O(m)",
      "code": {
        "C": "#include <stdio.h>\n#include <string.h>\n\nint computeLPS(char *pattern, int M, int *lps) {\n    int len = 0;\n    int i = 1;\n    while (i < M) {\n        if (pattern[i] == pattern[len]) {\n            len++;\n            lps[i] = len;\n            i++;\n        } else {\n            if (len != 0) {\n                len = lps[len - 1];\n            } else {\n                lps[i] = 0;\n                i++;\n            }\n        }\n    }\n    return 0;\n}\n\nint KMP(char *text, char *pattern) {\n    int N = strlen(text);\n    int M = strlen(pattern);\n    int lps[M];\n    computeLPS(pattern, M, lps);\n    int i = 0;\n    int j = 0;\n    while (i < N) {\n        if (pattern[j] == text[i]) {\n            i++;\n            j++;\n        }\n        if (j == M) {\n            printf(\"Pattern found at index %d\\n\", i - j);\n            j = lps[j - 1];\n        } else if (i < N && pattern[j] != text[i]) {\n            if (j != 0) {\n                j = lps[j - 1];\n            } else {\n                i++;\n            }\n        }\n    }\n    return 0;\n}\n\nint main() {\n    char text[] = \"ababcababcabc\";\n    char pattern[] = \"abc\";\n    KMP(text, pattern);\n    return 0;\n}",
        "C++": "#include <bits/stdc++.h>\nusing namespace std;\n\n// Class that contains the KMP algorithm and LPS array construction\nclass Solution {\npublic:\n    // Function to compute the LPS (Longest Prefix Suffix) array\n    vector<int> computeLPS(string pattern) {\n        // Create LPS array initialized with 0\n        vector<int> lps(pattern.length(), 0);\n\n        // Length of the previous longest prefix which is also a suffix\n        int len = 0;\n\n        // Start from the second character of the pattern\n        int i = 1;\n\n        // Loop to fill the LPS array\n        while (i < pattern.length()) {\n            // If current character matches the character at len\n            if (pattern[i] == pattern[len]) {\n                // Increase length of current matching prefix\n                len++;\n                // Assign it to lps[i]\n                lps[i] = len;\n                // Move to next index\n                i++;\n            } else {\n                // If mismatch and len is not 0, try shorter previous prefix\n                if (len != 0) {\n                    len = lps[len - 1];\n                } else {\n                    // If no prefix match found, set lps[i] to 0\n                    lps[i] = 0;\n                    // Move to next index\n                    i++;\n                }\n            }\n        }\n        // Return the complete LPS array\n        return lps;\n    }\n\n    // Function to search pattern in text using KMP\n    vector<int> KMP(string text, string pattern) {\n        // Preprocess the pattern and compute LPS array\n        vector<int> lps = computeLPS(pattern);\n        // Vector to store the starting indices of all matches\n        vector<int> result;\n        // Pointers for text and pattern\n        int i = 0;\n        int j = 0;\n\n        // Loop through the text\n        while (i < text.length()) {\n            // If characters match, move both pointers\n            if (text[i] == pattern[j]) {\n                i++;\n                j++;\n            }\n            // If the whole pattern is matched\n            if (j == pattern.length()) {\n                // Store the index of match\n                result.push_back(i - j);\n\n                // Use LPS to continue searching\n                j = lps[j - 1];\n            }\n            // If mismatch occurs after some matches\n            else if (i < text.length() && text[i] != pattern[j]) {\n                // Use LPS to avoid unnecessary comparisons\n                if (j != 0) {\n                    j = lps[j - 1];\n                } else {\n\n                    // Move forward in text if j is at start\n                    i++;\n                }\n            }\n        }\n        // Return all found indices\n        return result;\n    }\n};\n\n// Main function to test the KMP search\nint main() {\n    // Define the text string\n    string text = \"ababcababcabc\";\n    // Define the pattern to search\n    string pattern = \"abc\";\n\n    // Create an object of the Solution class\n    Solution sol;\n\n    // Call the KMP function to get all match indices\n    vector<int> matches = sol.KMP(text, pattern);\n\n    // Output the results\n    cout << \"Pattern found at indices: \";\n    for (int idx : matches) {\n        cout << idx << \" \";\n    }\n    cout << endl;\n\n    return 0;\n}",
        "Java": "import java.util.*;\n\nclass Solution {\n\n    // Function to compute the LPS (Longest Prefix which is also Suffix) array\n    public int[] computeLPS(String pattern) {\n\n        // Create LPS array\n        int[] lps = new int[pattern.length()];\n\n        // Length of the previous longest prefix suffix\n        int len = 0;\n\n        // Start from index 1 (as lps[0] is always 0)\n        int i = 1;\n\n        // Loop to fill lps[i] for i = 1 to pattern.length() - 1\n        while (i < pattern.length()) {\n\n            // If current character matches with the character at current length\n            if (pattern.charAt(i) == pattern.charAt(len)) {\n                len++;\n                lps[i] = len;\n                i++;\n            } else {\n\n                // If mismatch occurs after at least one match\n                if (len != 0) {\n                    len = lps[len - 1];\n                } else {\n\n                    // No prefix match, set lps[i] to 0\n                    lps[i] = 0;\n                    i++;\n                }\n            }\n        }\n\n        // Return the final LPS array\n        return lps;\n    }\n\n    // Function to perform KMP pattern searching\n    public List<Integer> KMP(String text, String pattern) {\n\n        // Preprocess the pattern to get LPS array\n        int[] lps = computeLPS(pattern);\n\n        // Result list to store indices where pattern is found\n        List<Integer> result = new ArrayList<>();\n\n        // Indices for text and pattern\n        int i = 0, j = 0;\n\n        // Traverse the entire text\n        while (i < text.length()) {\n\n            // If characters match, increment both pointers\n            if (text.charAt(i) == pattern.charAt(j)) {\n                i++;\n                j++;\n            }\n\n            // If full pattern matched\n            if (j == pattern.length()) {\n                result.add(i - j);\n                j = lps[j - 1];\n            }\n\n            // If mismatch occurs after some matches\n            else if (i < text.length() && text.charAt(i) != pattern.charAt(j)) {\n\n                // Use LPS to skip unnecessary comparisons\n                if (j != 0) {\n                    j = lps[j - 1];\n                } else {\n                    i++;\n                }\n            }\n        }\n\n        // Return the list of matching indices\n        return result;\n    }\n}\n\npublic class Main {\n    public static void main(String[] args) {\n\n        // Create object of Solution class\n        Solution sol = new Solution();\n\n        // Define text and pattern\n        String text = \"ababcababcabc\";\n        String pattern = \"abc\";\n\n        // Call the KMP function\n        List<Integer> matches = sol.KMP(text, pattern);\n\n        // Print the result\n        System.out.println(\"Pattern found at indices: \" + matches);\n    }\n}",
        "Python3": "class Solution:\n\n    # Function to compute the LPS array\n    def computeLPS(self, pattern):\n\n        # Create lps array\n        lps = [0] * len(pattern)\n\n        # Length of the previous longest prefix suffix\n        length = 0\n\n        # Start from second character\n        i = 1\n\n        # Build the lps array\n        while i < len(pattern):\n\n            # If characters match\n            if pattern[i] == pattern[length]:\n                length += 1\n                lps[i] = length\n                i += 1\n            else:\n\n                # If mismatch and length > 0\n                if length != 0:\n                    length = lps[length - 1]\n                else:\n\n                    # Set lps[i] to 0 and move on\n                    lps[i] = 0\n                    i += 1\n\n        # Return computed lps\n        return lps\n\n    # KMP search function\n    def KMP(self, text, pattern):\n\n        # Compute the LPS array\n        lps = self.computeLPS(pattern)\n\n        # List to store match indices\n        result = []\n\n        # Indices for text and pattern\n        i = j = 0\n\n        # Scan through the text\n        while i < len(text):\n\n            # If characters match\n            if text[i] == pattern[j]:\n                i += 1\n                j += 1\n\n            # Full match found\n            if j == len(pattern):\n                result.append(i - j)\n                j = lps[j - 1]\n\n            # Mismatch after some matches\n            elif i < len(text) and text[i] != pattern[j]:\n                if j != 0:\n                    j = lps[j - 1]\n                else:\n                    i += 1\n\n        # Return the result\n        return result\n\n\n# Main test\nsol = Solution()\ntext = \"ababcababcabc\"\npattern = \"abc\"\nprint(\"Pattern found at indices:\", sol.KMP(text, pattern))",
        "JavaScript": "class Solution {\n\n    // Function to compute the LPS array\n    computeLPS(pattern) {\n\n        // Create LPS array\n        const lps = new Array(pattern.length).fill(0);\n\n        // Length of previous longest prefix\n        let len = 0;\n\n        // Start from index 1\n        let i = 1;\n\n        // Build the LPS array\n        while (i < pattern.length) {\n\n            // If match found\n            if (pattern[i] === pattern[len]) {\n                len++;\n                lps[i] = len;\n                i++;\n            } else {\n\n                // Use previous lps if mismatch\n                if (len !== 0) {\n                    len = lps[len - 1];\n                } else {\n\n                    // Assign 0 and move ahead\n                    lps[i] = 0;\n                    i++;\n                }\n            }\n        }\n\n        // Return LPS array\n        return lps;\n    }\n\n    // KMP pattern search function\n    KMP(text, pattern) {\n\n        // Preprocess pattern\n        const lps = this.computeLPS(pattern);\n\n        // List to store match indices\n        const result = [];\n\n        // Pointers for text and pattern\n        let i = 0, j = 0;\n\n        // Traverse the text\n        while (i < text.length) {\n\n            // If characters match\n            if (text[i] === pattern[j]) {\n                i++;\n                j++;\n            }\n\n            // Full match found\n            if (j === pattern.length) {\n                result.push(i - j);\n                j = lps[j - 1];\n            }\n\n            // Mismatch after some matches\n            else if (i < text.length && text[i] !== pattern[j]) {\n                if (j !== 0) {\n                    j = lps[j - 1];\n                } else {\n                    i++;\n                }\n            }\n        }\n\n        // Return matched indices\n        return result;\n    }\n}\n\n// Main test\nconst sol = new Solution();\nconst text = \"ababcababcabc\";\nconst pattern = \"abc\";\nconsole.log(\"Pattern found at indices:\", sol.KMP(text, pattern));"
      }
    },
    "_internal": {
      "methodName": "",
      "params": [],
      "phase": "ready-for-judge"
    },
    "verified": true
  },
  "_internal": {},
  "verified": false
}
