{
  "questionId": "Q1294",
  "title": "Mirror Tree",
  "slug": "mirror-tree",
  "version": 1,
  "createdAt": "2026-07-01",
  "updatedAt": "2026-07-04",
  "metadata": {
    "category": "Trees",
    "difficulty": "Medium",
    "isPremium": false,
    "tags": [
      "Binary Tree",
      "Trees",
      "DFS",
      "Hash Map",
      "Symmetry",
      "Recursion"
    ],
    "companyTags": [],
    "topicTags": [
      "Recursion & Backtracking"
    ]
  },
  "content": {
    "problemStatement": "Given a binary tree, check whether it is a structural mirror of itself (i.e., structurally symmetric around its center).\n\nFor example, the following binary tree is structurally symmetric:\n\n\nInput Format\n\nThe first line of the input contains a single integer NNN \u2014 the number of nodes in the binary tree.\nNext N\u22121N - 1N\u22121 lines contain three space separated characters pi,ci,Rip_i, c_i, R_ipi\u200b,ci\u200b,Ri\u200b, describing the tree with edge informations - pip_ipi\u200b is an integer denoting the parent node of the iiith edge, cic_ici\u200b is child node, and the letter RiR_iRi\u200b denotes whether the child is left child or right child, it's value is `L` if cic_ici\u200b is left child of pip_ipi\u200b, else `R`.\n\n\nOutput Format\nOutput on the single line, `YES` if the given binary tree is structurally symmetric to itself, `NO` otherwise.",
    "constraints": [
      "1 \u2264 T \u2264 10",
      "1 \u2264 N \u2264 10000",
      "1 \u2264 pi, ci \u2264 100000",
      "Ri \u2208 {'L','R'}",
      "All pi and ci are distinct across a test case"
    ],
    "inputFormat": "The first line contains an integer T (1 \u2264 T \u2264 10) \u2013 the number of test cases. For each test case:\n- The first line contains an integer N (1 \u2264 N \u2264 10000) \u2013 the number of nodes in the binary tree.\n- The next N\u20111 lines each contain three space\u2011separated values pi ci Ri, where pi is the parent node id, ci is the child node id, and Ri is either 'L' (ci is the left child of pi) or 'R' (ci is the right child of pi). All node ids are distinct.\nThe tree is guaranteed to be a valid binary tree.",
    "outputFormat": "For each test case output a single line containing \"YES\" if the tree is structurally symmetric (a mirror of itself) or \"NO\" otherwise.",
    "sampleTestCases": [
      {
        "id": "sample_1",
        "input": "1\n7\n1 2 L\n1 3 R\n3 4 L\n3 5 R\n2 6 R\n2 7 L",
        "output": "YES",
        "explanation": "The tree is perfectly symmetric: left subtree of node 1 mirrors the right subtree."
      },
      {
        "id": "sample_2",
        "input": "1\n5\n1 2 L\n1 3 R\n2 4 L\n2 5 R",
        "output": "YES",
        "explanation": "Both sub\u2011trees of the root have the same shape, therefore the whole tree is symmetric."
      },
      {
        "id": "sample_3",
        "input": "1\n3\n1 2 L\n1 3 R",
        "output": "YES",
        "explanation": "A root with one left and one right child is symmetric regardless of the values."
      }
    ]
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "label": "minimum boundary",
        "input": "1\n1",
        "expectedOutput": "YES",
        "weight": 2
      },
      {
        "id": "tc_2",
        "label": "asymmetric small tree",
        "input": "1\n4\n1 2 L\n1 3 R\n2 4 L",
        "expectedOutput": "NO",
        "weight": 2
      },
      {
        "id": "tc_3",
        "label": "large symmetric tree",
        "input": "1\n9\n1 2 L\n1 3 R\n2 4 L\n2 5 R\n3 6 L\n3 7 R\n4 8 L\n7 9 R",
        "expectedOutput": "YES",
        "weight": 2
      },
      {
        "id": "tc_4",
        "label": "large asymmetric tree",
        "input": "1\n9\n1 2 L\n1 3 R\n2 4 L\n2 5 R\n3 6 L\n3 7 R\n4 8 L\n5 9 R",
        "expectedOutput": "NO",
        "weight": 2
      },
      {
        "id": "tc_5",
        "label": "multiple test cases",
        "input": "3\n1\n3\n1 2 L\n1 3 R\n5\n1 2 L\n1 3 R\n2 4 L\n2 5 R",
        "expectedOutput": "YES\nYES\nYES",
        "weight": 2
      }
    ],
    "totalTestCases": 5,
    "totalWeight": 10
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      2,
      2,
      2,
      2,
      2
    ],
    "maxScore": 10
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "For each test case we build the tree using a hash map from node id to a node structure containing left and right pointers. While reading edges we also mark every node that appears as a child. After all edges are read, the node that never appeared as a child is the root. To check symmetry we use a recursive function isMirror(left, right) that returns true when both sub\u2011trees are empty, or when the left subtree of the left node mirrors the right subtree of the right node **and** the right subtree of the left node mirrors the left subtree of the right node. The whole tree is symmetric iff isMirror(root.left, root.right) is true. The algorithm runs in O(N) time per test case and uses O(N) additional memory for the nodes and the hash map.",
    "timeComplexity": "O(N) per test case, where N is the number of nodes in the test case.",
    "spaceComplexity": "O(N) per test case for storing the nodes and the auxiliary hash map.",
    "code": {
      "C": "#include <stdio.h>\n#include <stdlib.h>\n#include <string.h>\n\ntypedef struct Node {\n    struct Node *left, *right;\n} Node;\n\nNode* newNode() {\n    Node* n = (Node*)malloc(sizeof(Node));\n    n->left = n->right = NULL;\n    return n;\n}\n\nint isMirror(Node* a, Node* b) {\n    if (!a && !b) return 1;\n    if (!a || !b) return 0;\n    return isMirror(a->left, b->right) && isMirror(a->right, b->left);\n}\n\nint main() {\n    int T; if (scanf(\"%d\", &T)!=1) return 0;\n    while (T--) {\n        int N; scanf(\"%d\", &N);\n        // node ids are \u2264100000, allocate static array\n        static Node* nodes[100001];\n        static int isChild[100001];\n        // reset only needed positions\n        for (int i = 0; i <= 100000; ++i) { nodes[i] = NULL; isChild[i] = 0; }\n        for (int i = 0; i < N-1; ++i) {\n            int p, c; char dir;\n            scanf(\"%d %d %c\", &p, &c, &dir);\n            if (!nodes[p]) nodes[p] = newNode();\n            if (!nodes[c]) nodes[c] = newNode();\n            if (dir == 'L') nodes[p]->left = nodes[c];\n            else nodes[p]->right = nodes[c];\n            isChild[c] = 1;\n        }\n        // find root (the one that never appeared as child)\n        int rootId = -1;\n        for (int i = 0; i <= 100000; ++i) {\n            if (nodes[i] && !isChild[i]) { rootId = i; break; }\n        }\n        Node* root = (rootId==-1)? NULL : nodes[rootId];\n        if (!root || isMirror(root->left, root->right))\n            printf(\"YES\\n\");\n        else\n            printf(\"NO\\n\");\n        // free memory (optional for contest)\n        for (int i = 0; i <= 100000; ++i) if (nodes[i]) free(nodes[i]);\n    }\n    return 0;\n}",
      "C++": "#include <bits/stdc++.h>\nusing namespace std;\n\nstruct Node {\n    Node *left = nullptr, *right = nullptr;\n};\n\nbool isMirror(Node* a, Node* b) {\n    if (!a && !b) return true;\n    if (!a || !b) return false;\n    return isMirror(a->left, b->right) && isMirror(a->right, b->left);\n}\n\nint main() {\n    ios::sync_with_stdio(false);\n    cin.tie(nullptr);\n    int T; if(!(cin>>T)) return 0;\n    while (T--) {\n        int N; cin>>N;\n        unordered_map<int, Node*> mp;\n        unordered_set<int> childSet;\n        for (int i=0;i<N-1;++i) {\n            int p,c; char dir; cin>>p>>c>>dir;\n            if (!mp.count(p)) mp[p]=new Node();\n            if (!mp.count(c)) mp[c]=new Node();\n            if (dir=='L') mp[p]->left = mp[c];\n            else mp[p]->right = mp[c];\n            childSet.insert(c);\n        }\n        int rootId = -1;\n        for (auto &kv: mp) if (!childSet.count(kv.first)) { rootId = kv.first; break; }\n        Node* root = (rootId==-1)? nullptr : mp[rootId];\n        if (!root || isMirror(root->left, root->right)) cout << \"YES\\n\";\n        else cout << \"NO\\n\";\n        // optional cleanup\n        for (auto &kv: mp) delete kv.second;\n    }\n    return 0;\n}",
      "Java": "import java.io.*;\nimport java.util.*;\n\nclass Main {\n    static class Node {\n        Node left, right;\n        Node() { left = right = null; }\n    }\n\n    static boolean isMirror(Node a, Node b) {\n        if (a == null && b == null) return true;\n        if (a == null || b == null) return false;\n        return isMirror(a.left, b.right) && isMirror(a.right, b.left);\n    }\n\n    public static void main(String[] args) throws Exception {\n        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));\n        StringTokenizer st;\n        int T = Integer.parseInt(br.readLine().trim());\n        StringBuilder out = new StringBuilder();\n        while (T-- > 0) {\n            int N = Integer.parseInt(br.readLine().trim());\n            Map<Integer, Node> map = new HashMap<>();\n            Set<Integer> child = new HashSet<>();\n            for (int i = 0; i < N - 1; i++) {\n                st = new StringTokenizer(br.readLine());\n                int p = Integer.parseInt(st.nextToken());\n                int c = Integer.parseInt(st.nextToken());\n                char dir = st.nextToken().charAt(0);\n                map.putIfAbsent(p, new Node());\n                map.putIfAbsent(c, new Node());\n                if (dir == 'L') map.get(p).left = map.get(c);\n                else map.get(p).right = map.get(c);\n                child.add(c);\n            }\n            int rootId = -1;\n            for (int id : map.keySet()) if (!child.contains(id)) { rootId = id; break; }\n            Node root = (rootId == -1) ? null : map.get(rootId);\n            if (root == null || isMirror(root.left, root.right)) out.append(\"YES\\n\");\n            else out.append(\"NO\\n\");\n        }\n        System.out.print(out.toString());\n    }\n}",
      "Python3": "import sys\n\ndef is_mirror(a, b):\n    if not a and not b:\n        return True\n    if not a or not b:\n        return False\n    return is_mirror(a[0], b[1]) and is_mirror(a[1], b[0])\n\n# Node will be stored as a tuple (left, right) where each element is either None or another tuple\n\ndef solve():\n    data = sys.stdin.read().strip().split()\n    if not data:\n        return\n    it = iter(data)\n    T = int(next(it))\n    out_lines = []\n    for _ in range(T):\n        N = int(next(it))\n        nodes = {}\n        child_set = set()\n        for __ in range(N-1):\n            p = int(next(it)); c = int(next(it)); dir = next(it)\n            if p not in nodes:\n                nodes[p] = [None, None]  # [left, right]\n            if c not in nodes:\n                nodes[c] = [None, None]\n            if dir == 'L':\n                nodes[p][0] = nodes[c]\n            else:\n                nodes[p][1] = nodes[c]\n            child_set.add(c)\n        root_id = None\n        for nid in nodes:\n            if nid not in child_set:\n                root_id = nid\n                break\n        root = None if root_id is None else nodes[root_id]\n        if root is None or is_mirror(root[0], root[1]):\n            out_lines.append('YES')\n        else:\n            out_lines.append('NO')\n    sys.stdout.write('\\n'.join(out_lines))\n\nif __name__ == \"__main__\":\n    solve()",
      "JavaScript": "const fs = require('fs');\nconst input = fs.readFileSync(0, 'utf-8').trim().split(/\\s+/);\nlet idx = 0;\nfunction isMirror(a, b) {\n    if (!a && !b) return true;\n    if (!a || !b) return false;\n    return isMirror(a.left, b.right) && isMirror(a.right, b.left);\n}\nclass Node {\n    constructor() { this.left = null; this.right = null; }\n}\nlet T = Number(input[idx++]);\nlet outputs = [];\nfor (let t = 0; t < T; ++t) {\n    let N = Number(input[idx++]);\n    const map = new Map(); // id -> Node\n    const childSet = new Set();\n    for (let i = 0; i < N - 1; ++i) {\n        let p = Number(input[idx++]);\n        let c = Number(input[idx++]);\n        let dir = input[idx++];\n        if (!map.has(p)) map.set(p, new Node());\n        if (!map.has(c)) map.set(c, new Node());\n        const parent = map.get(p);\n        const child = map.get(c);\n        if (dir === 'L') parent.left = child; else parent.right = child;\n        childSet.add(c);\n    }\n    let root = null;\n    for (let [id, node] of map.entries()) {\n        if (!childSet.has(id)) { root = node; break; }\n    }\n    if (!root || isMirror(root.left, root.right)) outputs.push('YES');\n    else outputs.push('NO');\n}\nconsole.log(outputs.join('\\n'));\n"
    }
  }
}