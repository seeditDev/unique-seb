{
  "questionId": "Q5249",
  "title": "Bombing",
  "slug": "bombing",
  "version": 1,
  "createdAt": "2026-07-01",
  "updatedAt": "2026-07-05",
  "metadata": {
    "category": "Data Structures",
    "difficulty": "Medium",
    "isPremium": true,
    "tags": [
      "Fenwick Tree",
      "Coordinate Compression",
      "Range Update Point Query",
      "Dynamic Intervals"
    ],
    "companyTags": [],
    "topicTags": [
      "Data Structures"
    ],
    "QBCategory": "CCD"
  },
  "content": {
    "problemStatement": "There are n+1 (0 &lt; n &lt;= 109, indexed 0..n) houses lying on a straight street. One day, they are bombed by an enemy. CodeChef is designing defence systems to protect the street from bombing. Each defense system can protect all houses in some interval, and a house can be protected by more than one system. A defence system can be also moved at any time. By agents, CodeChef knows the bombing schedule of the enemy. Each time they bomb a house and CodeChef wants to know how many systems are protecting that house.",
    "constraints": [
      "1≤T≤10",
      "0≤n≤10^9",
      "1≤m≤250000 per test case",
      "Sum of m over all test cases ≤250000",
      "All indices used in operations satisfy the bounds described in the statement"
    ],
    "inputFormat": "The first line contains an integer T, the number of test cases. Each test case starts with a line containing two integers n and m (0≤n≤10^9, 1≤m≤250000). Then m lines follow, each describing an operation:\n- P u v : add a new defence system that protects every house from u to v (0≤u≤v≤n). Systems are indexed by the order of appearance, starting from 1.\n- M i d : move the i‑th system by d houses (d may be negative). The system currently covering [u,v] will afterwards cover [u+d,v+d]; it is guaranteed that 0≤u+d≤v+d≤n.\n- B x   : the enemy bombs house x (0≤x≤n). For each B operation you must output how many systems currently protect house x.",
    "outputFormat": "For each bombing operation (B) in the order they appear, output a single line containing the number of defence systems that protect the bombed house.",
    "notes": [],
    "sampleTestCases": [
      {
        "id": "sample_1",
        "type": "reg",
        "input": "7 5\nP 1 4\nP 3 5\nB 3\nM 2 1\nB 3",
        "output": "2\n1",
        "explanation": ""
      }
    ]
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_01",
        "type": "reg",
        "label": "Case 1",
        "input": "",
        "expectedOutput": "",
        "weight": 4
      },
      {
        "id": "tc_02",
        "type": "reg",
        "label": "Case 2",
        "input": "",
        "expectedOutput": "",
        "weight": 4
      },
      {
        "id": "tc_03",
        "type": "reg",
        "label": "Case 3",
        "input": "",
        "expectedOutput": "",
        "weight": 4
      },
      {
        "id": "tc_04",
        "type": "reg",
        "label": "Case 4",
        "input": "",
        "expectedOutput": "",
        "weight": 4
      },
      {
        "id": "tc_05",
        "type": "reg",
        "label": "Case 5",
        "input": "",
        "expectedOutput": "",
        "weight": 4
      },
      {
        "id": "tc_06",
        "type": "reg",
        "label": "Case 1",
        "input": "",
        "expectedOutput": "",
        "weight": 4
      },
      {
        "id": "tc_07",
        "type": "reg",
        "label": "Case 2",
        "input": "",
        "expectedOutput": "",
        "weight": 4
      },
      {
        "id": "tc_08",
        "type": "reg",
        "label": "Case 3",
        "input": "",
        "expectedOutput": "",
        "weight": 4
      },
      {
        "id": "tc_09",
        "type": "reg",
        "label": "Case 4",
        "input": "",
        "expectedOutput": "",
        "weight": 4
      },
      {
        "id": "tc_10",
        "type": "reg",
        "label": "Case 5",
        "input": "",
        "expectedOutput": "",
        "weight": 4
      },
      {
        "id": "tc_11",
        "type": "reg",
        "label": "Case 1",
        "input": "",
        "expectedOutput": "",
        "weight": 3
      },
      {
        "id": "tc_12",
        "type": "reg",
        "label": "Case 2",
        "input": "",
        "expectedOutput": "",
        "weight": 3
      },
      {
        "id": "tc_13",
        "type": "reg",
        "label": "Case 3",
        "input": "",
        "expectedOutput": "",
        "weight": 3
      },
      {
        "id": "tc_14",
        "type": "reg",
        "label": "Case 4",
        "input": "",
        "expectedOutput": "",
        "weight": 3
      },
      {
        "id": "tc_15",
        "type": "reg",
        "label": "Case 5",
        "input": "",
        "expectedOutput": "",
        "weight": 3
      },
      {
        "id": "tc_16",
        "type": "reg",
        "label": "Case 1",
        "input": "",
        "expectedOutput": "",
        "weight": 3
      },
      {
        "id": "tc_17",
        "type": "reg",
        "label": "Case 2",
        "input": "",
        "expectedOutput": "",
        "weight": 3
      },
      {
        "id": "tc_18",
        "type": "reg",
        "label": "Case 3",
        "input": "",
        "expectedOutput": "",
        "weight": 3
      },
      {
        "id": "tc_19",
        "type": "reg",
        "label": "Case 4",
        "input": "",
        "expectedOutput": "",
        "weight": 3
      },
      {
        "id": "tc_20",
        "type": "reg",
        "label": "Case 5",
        "input": "",
        "expectedOutput": "",
        "weight": 3
      },
      {
        "id": "tc_21",
        "type": "reg",
        "label": "Case 1",
        "input": "",
        "expectedOutput": "",
        "weight": 3
      },
      {
        "id": "tc_22",
        "type": "reg",
        "label": "Case 2",
        "input": "",
        "expectedOutput": "",
        "weight": 3
      },
      {
        "id": "tc_23",
        "type": "reg",
        "label": "Case 3",
        "input": "",
        "expectedOutput": "",
        "weight": 3
      },
      {
        "id": "tc_24",
        "type": "reg",
        "label": "Case 4",
        "input": "",
        "expectedOutput": "",
        "weight": 3
      },
      {
        "id": "tc_25",
        "type": "reg",
        "label": "Case 5",
        "input": "",
        "expectedOutput": "",
        "weight": 3
      },
      {
        "id": "tc_26",
        "type": "reg",
        "label": "Case 1",
        "input": "",
        "expectedOutput": "",
        "weight": 3
      },
      {
        "id": "tc_27",
        "type": "reg",
        "label": "Case 2",
        "input": "",
        "expectedOutput": "",
        "weight": 3
      },
      {
        "id": "tc_28",
        "type": "reg",
        "label": "Case 3",
        "input": "",
        "expectedOutput": "",
        "weight": 3
      },
      {
        "id": "tc_29",
        "type": "reg",
        "label": "Case 4",
        "input": "",
        "expectedOutput": "",
        "weight": 3
      },
      {
        "id": "tc_30",
        "type": "reg",
        "label": "Case 5",
        "input": "",
        "expectedOutput": "",
        "weight": 3
      }
    ],
    "totalTestCases": 30,
    "totalWeight": 100
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      4,
      4,
      4,
      4,
      4,
      4,
      4,
      4,
      4,
      4,
      3,
      3,
      3,
      3,
      3,
      3,
      3,
      3,
      3,
      3,
      3,
      3,
      3,
      3,
      3,
      3,
      3,
      3,
      3,
      3
    ],
    "maxScore": 100
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ],
    "notes": []
  },
  "solution": {
    "approach": "We need to support three operations on a set of intervals on a line of length up to 10^9: add an interval, move an existing interval, and query how many intervals contain a point. A classic technique for range‑add / point‑query is a Fenwick tree on a difference array: add +1 at the left end of an interval and -1 at (right+1). The prefix sum at position x equals the number of active intervals covering x. Because the coordinate range is huge but the total number of distinct coordinates that ever appear is O(m), we compress all coordinates that can ever be used (every left end, every right+1 end and every query point). We first read all operations while simulating moves to know every coordinate that will appear, store the operations and build a sorted list of unique coordinates. In a second pass we process the operations using a Fenwick tree of size equal to the number of compressed coordinates: • P u v → add +1 at idx(u) and -1 at idx(v+1). • M i d → let the i‑th system currently be [l,r]; remove it by adding -1 at idx(l) and +1 at idx(r+1); compute the new interval [l+d,r+d]; add it by +1 at idx(l+d) and -1 at idx(r+d+1). • B x → answer = prefix sum up to idx(x). All updates and queries are O(log M) where M≤2·m+number of queries, easily fitting the limits. The total time per test case is O(m log m) and the extra memory is O(m).",
    "timeComplexity": "O(m log m) per test case (m is the number of operations in the test case).",
    "spaceComplexity": "O(m) for storing operations, current intervals and the Fenwick tree.",
    "code": {
      "C": "#include <stdio.h>\n#include <stdlib.h>\n#include <string.h>\n\ntypedef long long ll;\n\n/* Fenwick tree for long long values */\nstruct Fenwick {\n    int n;\n    ll *bit;\n    void init(int n_) {\n        n = n_;\n        bit = (ll*)calloc(n+2, sizeof(ll));\n    }\n    void add(int idx, ll val) {\n        for (int i = idx; i <= n; i += i & -i) bit[i] += val;\n    }\n    ll sum(int idx) {\n        ll res = 0;\n        for (int i = idx; i > 0; i -= i & -i) res += bit[i];\n        return res;\n    }\n};\n\nstruct Op {\n    char type; // 'P', 'M', 'B'\n    int a, b;  // parameters as described in analysis\n};\n\nint main() {\n    int T;\n    if (scanf(\"%d\", &T)!=1) return 0;\n    while (T--) {\n        int n, m;\n        scanf(\"%d %d\", &n, &m);\n        struct Op *ops = (struct Op*)malloc(m * sizeof(struct Op));\n        int maxSystems = 0;\n        int *curL = (int*)calloc(m+2, sizeof(int)); // enough for all possible systems\n        int *curR = (int*)calloc(m+2, sizeof(int));\n        int *tempL = (int*)calloc(m+2, sizeof(int)); // temporary for simulation while reading\n        int *tempR = (int*)calloc(m+2, sizeof(int));\n        int sysCnt = 0;\n        // collect coordinates for compression\n        int coordCap = 3*m + 5;\n        ll *coords = (ll*)malloc(coordCap * sizeof(ll));\n        int coordSize = 0;\n        for (int i = 0; i < m; ++i) {\n            char buf[2];\n            scanf(\" %1s\", buf);\n            ops[i].type = buf[0];\n            if (ops[i].type == 'P') {\n                int u, v;\n                scanf(\"%d %d\", &u, &v);\n                ops[i].a = u; ops[i].b = v;\n                ++sysCnt;\n                tempL[sysCnt] = u;\n                tempR[sysCnt] = v;\n                // add coordinates u and v+1\n                coords[coordSize++] = u;\n                coords[coordSize++] = (ll)v + 1;\n            } else if (ops[i].type == 'M') {\n                int id, d;\n                scanf(\"%d %d\", &id, &d);\n                ops[i].a = id; ops[i].b = d;\n                int oldL = tempL[id];\n                int oldR = tempR[id];\n                int newL = oldL + d;\n                int newR = oldR + d;\n                tempL[id] = newL;\n                tempR[id] = newR;\n                // coordinates for new interval\n                coords[coordSize++] = newL;\n                coords[coordSize++] = (ll)newR + 1;\n            } else if (ops[i].type == 'B') {\n                int x; scanf(\"%d\", &x);\n                ops[i].a = x; ops[i].b = 0;\n                coords[coordSize++] = x;\n            }\n        }\n        // also need coordinates for right+1 of intervals that will be removed later (already added when they were created)\n        // sort and deduplicate coordinates\n        qsort(coords, coordSize, sizeof(ll), (int(*)(const void*,const void*))strcmp);\n        // custom comparator for qsort of ll\n        // Since we cannot pass strcmp for ll, implement a simple lambda via function pointer is not allowed in C89.\n        // We'll write a separate comparator.\n        // Re‑sort with proper comparator.\n        int cmp_ll(const void *a, const void *b) {\n            ll va = *(const ll*)a;\n            ll vb = *(const ll*)b;\n            return (va > vb) - (va < vb);\n        }\n        qsort(coords, coordSize, sizeof(ll), cmp_ll);\n        int uniq = 0;\n        for (int i = 0; i < coordSize; ++i) {\n            if (i == 0 || coords[i] != coords[i-1]) {\n                coords[uniq++] = coords[i];\n            }\n        }\n        // mapping function: binary search\n        int getIdx(ll x) {\n            int l = 0, r = uniq-1;\n            while (l <= r) {\n                int mid = (l+r)/2;\n                if (coords[mid] == x) return mid+1; // Fenwick is 1‑based\n                else if (coords[mid] < x) l = mid+1;\n                else r = mid-1;\n            }\n            return -1; // should never happen\n        }\n        struct Fenwick ft;\n        ft.init(uniq+2);\n        // reset current intervals for real processing\n        memset(curL, 0, (m+2)*sizeof(int));\n        memset(curR, 0, (m+2)*sizeof(int));\n        sysCnt = 0;\n        for (int i = 0; i < m; ++i) {\n            if (ops[i].type == 'P') {\n                int u = ops[i].a;\n                int v = ops[i].b;\n                ++sysCnt;\n                curL[sysCnt] = u;\n                curR[sysCnt] = v;\n                ft.add(getIdx(u), 1);\n                ft.add(getIdx((ll)v+1), -1);\n            } else if (ops[i].type == 'M') {\n                int id = ops[i].a;\n                int d = ops[i].b;\n                int oldL = curL[id];\n                int oldR = curR[id];\n                // remove old interval\n                ft.add(getIdx(oldL), -1);\n                ft.add(getIdx((ll)oldR+1), 1);\n                int newL = oldL + d;\n                int newR = oldR + d;\n                curL[id] = newL;\n                curR[id] = newR;\n                // add new interval\n                ft.add(getIdx(newL), 1);\n                ft.add(getIdx((ll)newR+1), -1);\n            } else { // 'B'\n                int x = ops[i].a;\n                ll ans = ft.sum(getIdx(x));\n                printf(\"%lld\\n\", ans);\n            }\n        }\n        free(ops);\n        free(curL);\n        free(curR);\n        free(tempL);\n        free(tempR);\n        free(coords);\n    }\n    return 0;\n}\n",
      "C++": "",
      "Java": "",
      "Python3": "",
      "JavaScript": ""
    },
    "notes": [],
    "editorial": {
      "bruteForce": {
        "description": "",
        "complexity": ""
      },
      "better": {
        "description": "",
        "complexity": ""
      },
      "optimal": {
        "description": "",
        "complexity": ""
      }
    }
  },
  "hints": [],
  "followUps": [],
  "learningObjectives": [],
  "similarProblems": [],
  "acceptanceRate": null,
  "estimatedSolveTime": "",
  "recommendedAfter": [],
  "boilerPlates": {
    "C": "#include <stdio.h>\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "C++": "#include <iostream>\nusing namespace std;\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "Java": "import java.util.*;\nimport java.io.*;\n\npublic class Main {\n    public static void main(String[] args) throws IOException {\n        // your code goes here\n    }\n}",
    "Python3": "# your code goes here\n",
    "JavaScript": "// your code goes here\n",
    "solution": {
      "approach": "We need to support three operations on a set of intervals on a line of length up to 10^9: add an interval, move an existing interval, and query how many intervals contain a point. A classic technique for range‑add / point‑query is a Fenwick tree on a difference array: add +1 at the left end of an interval and -1 at (right+1). The prefix sum at position x equals the number of active intervals covering x. Because the coordinate range is huge but the total number of distinct coordinates that ever appear is O(m), we compress all coordinates that can ever be used (every left end, every right+1 end and every query point). We first read all operations while simulating moves to know every coordinate that will appear, store the operations and build a sorted list of unique coordinates. In a second pass we process the operations using a Fenwick tree of size equal to the number of compressed coordinates: • P u v → add +1 at idx(u) and -1 at idx(v+1). • M i d → let the i‑th system currently be [l,r]; remove it by adding -1 at idx(l) and +1 at idx(r+1); compute the new interval [l+d,r+d]; add it by +1 at idx(l+d) and -1 at idx(r+d+1). • B x → answer = prefix sum up to idx(x). All updates and queries are O(log M) where M≤2·m+number of queries, easily fitting the limits. The total time per test case is O(m log m) and the extra memory is O(m).",
      "timeComplexity": "O(m log m) per test case (m is the number of operations in the test case).",
      "spaceComplexity": "O(m) for storing operations, current intervals and the Fenwick tree.",
      "code": {
        "C": "#include <stdio.h>\n#include <stdlib.h>\n#include <string.h>\n\ntypedef long long ll;\n\n/* Fenwick tree for long long values */\nstruct Fenwick {\n    int n;\n    ll *bit;\n    void init(int n_) {\n        n = n_;\n        bit = (ll*)calloc(n+2, sizeof(ll));\n    }\n    void add(int idx, ll val) {\n        for (int i = idx; i <= n; i += i & -i) bit[i] += val;\n    }\n    ll sum(int idx) {\n        ll res = 0;\n        for (int i = idx; i > 0; i -= i & -i) res += bit[i];\n        return res;\n    }\n};\n\nstruct Op {\n    char type; // 'P', 'M', 'B'\n    int a, b;  // parameters as described in analysis\n};\n\nint main() {\n    int T;\n    if (scanf(\"%d\", &T)!=1) return 0;\n    while (T--) {\n        int n, m;\n        scanf(\"%d %d\", &n, &m);\n        struct Op *ops = (struct Op*)malloc(m * sizeof(struct Op));\n        int maxSystems = 0;\n        int *curL = (int*)calloc(m+2, sizeof(int)); // enough for all possible systems\n        int *curR = (int*)calloc(m+2, sizeof(int));\n        int *tempL = (int*)calloc(m+2, sizeof(int)); // temporary for simulation while reading\n        int *tempR = (int*)calloc(m+2, sizeof(int));\n        int sysCnt = 0;\n        // collect coordinates for compression\n        int coordCap = 3*m + 5;\n        ll *coords = (ll*)malloc(coordCap * sizeof(ll));\n        int coordSize = 0;\n        for (int i = 0; i < m; ++i) {\n            char buf[2];\n            scanf(\" %1s\", buf);\n            ops[i].type = buf[0];\n            if (ops[i].type == 'P') {\n                int u, v;\n                scanf(\"%d %d\", &u, &v);\n                ops[i].a = u; ops[i].b = v;\n                ++sysCnt;\n                tempL[sysCnt] = u;\n                tempR[sysCnt] = v;\n                // add coordinates u and v+1\n                coords[coordSize++] = u;\n                coords[coordSize++] = (ll)v + 1;\n            } else if (ops[i].type == 'M') {\n                int id, d;\n                scanf(\"%d %d\", &id, &d);\n                ops[i].a = id; ops[i].b = d;\n                int oldL = tempL[id];\n                int oldR = tempR[id];\n                int newL = oldL + d;\n                int newR = oldR + d;\n                tempL[id] = newL;\n                tempR[id] = newR;\n                // coordinates for new interval\n                coords[coordSize++] = newL;\n                coords[coordSize++] = (ll)newR + 1;\n            } else if (ops[i].type == 'B') {\n                int x; scanf(\"%d\", &x);\n                ops[i].a = x; ops[i].b = 0;\n                coords[coordSize++] = x;\n            }\n        }\n        // also need coordinates for right+1 of intervals that will be removed later (already added when they were created)\n        // sort and deduplicate coordinates\n        qsort(coords, coordSize, sizeof(ll), (int(*)(const void*,const void*))strcmp);\n        // custom comparator for qsort of ll\n        // Since we cannot pass strcmp for ll, implement a simple lambda via function pointer is not allowed in C89.\n        // We'll write a separate comparator.\n        // Re‑sort with proper comparator.\n        int cmp_ll(const void *a, const void *b) {\n            ll va = *(const ll*)a;\n            ll vb = *(const ll*)b;\n            return (va > vb) - (va < vb);\n        }\n        qsort(coords, coordSize, sizeof(ll), cmp_ll);\n        int uniq = 0;\n        for (int i = 0; i < coordSize; ++i) {\n            if (i == 0 || coords[i] != coords[i-1]) {\n                coords[uniq++] = coords[i];\n            }\n        }\n        // mapping function: binary search\n        int getIdx(ll x) {\n            int l = 0, r = uniq-1;\n            while (l <= r) {\n                int mid = (l+r)/2;\n                if (coords[mid] == x) return mid+1; // Fenwick is 1‑based\n                else if (coords[mid] < x) l = mid+1;\n                else r = mid-1;\n            }\n            return -1; // should never happen\n        }\n        struct Fenwick ft;\n        ft.init(uniq+2);\n        // reset current intervals for real processing\n        memset(curL, 0, (m+2)*sizeof(int));\n        memset(curR, 0, (m+2)*sizeof(int));\n        sysCnt = 0;\n        for (int i = 0; i < m; ++i) {\n            if (ops[i].type == 'P') {\n                int u = ops[i].a;\n                int v = ops[i].b;\n                ++sysCnt;\n                curL[sysCnt] = u;\n                curR[sysCnt] = v;\n                ft.add(getIdx(u), 1);\n                ft.add(getIdx((ll)v+1), -1);\n            } else if (ops[i].type == 'M') {\n                int id = ops[i].a;\n                int d = ops[i].b;\n                int oldL = curL[id];\n                int oldR = curR[id];\n                // remove old interval\n                ft.add(getIdx(oldL), -1);\n                ft.add(getIdx((ll)oldR+1), 1);\n                int newL = oldL + d;\n                int newR = oldR + d;\n                curL[id] = newL;\n                curR[id] = newR;\n                // add new interval\n                ft.add(getIdx(newL), 1);\n                ft.add(getIdx((ll)newR+1), -1);\n            } else { // 'B'\n                int x = ops[i].a;\n                ll ans = ft.sum(getIdx(x));\n                printf(\"%lld\\n\", ans);\n            }\n        }\n        free(ops);\n        free(curL);\n        free(curR);\n        free(tempL);\n        free(tempR);\n        free(coords);\n    }\n    return 0;\n}\n",
        "C++": "",
        "Java": "",
        "Python3": "",
        "JavaScript": ""
      }
    },
    "_internal": {
      "methodName": "",
      "params": [],
      "phase": "ready-for-judge"
    },
    "verified": true
  },
  "_internal": {},
  "verified": false
}
