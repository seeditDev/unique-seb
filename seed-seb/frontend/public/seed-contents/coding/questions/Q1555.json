{
  "questionId": "Q1555",
  "title": "Longest Palindromic Substring",
  "slug": "longest-palindromic-substring",
  "version": 1,
  "createdAt": "2026-07-01",
  "updatedAt": "2026-07-04",
  "metadata": {
    "category": "Strings",
    "difficulty": "Medium",
    "isPremium": false,
    "tags": [
      "Sliding Window",
      "Strings",
      "Expand Around Center",
      "Two Pointers"
    ],
    "companyTags": [],
    "topicTags": [
      "Strings & Pattern Matching"
    ]
  },
  "content": {
    "problemStatement": "Chef has a string **S** consisting of letters and digits.\n\nHe wants to find the **longest palindromic substring**.\n\nA **palindromic substring** is a contiguous sequence of characters that reads the same forward and backward. Due to case sensitivity 'A' != 'a' in a palindrome.\n\nThe function searches for all possible palindromic substrings in sss and returns the one with the maximum length.\n\nIf there is a tie, priority is given to the substring with the **smallest starting index**.\n\nFunction DeclarationFunction NamefindLongestPalindromefindLongestPalindromefindLongestPalindrome \u2013 This function finds the longest palindromic substring in a given string.\n\nParameters\nsss : A string consisting of English letters (uppercase and lowercase) and digits.\nReturn Value\nReturns a string representing the **longest palindromic substring** in sss.\nIf multiple palindromic substrings of the same maximum length exist, the one that appears **first** in sss is returned.\nConstraints\n1\u2264\u2223s\u2223\u226410001 \\leq |s| \\leq 10001\u2264\u2223s\u2223\u22641000\nString sss consists of:\n\nEnglish letters (uppercase and lowercase)\nDigits (`0`\u2013`9`)\n\n\n\nInput Format\n\nA single line containing the string sss.\n\n\nOutput Format\n\nPrint the longest palindromic substring.\nIf multiple answers exist, print the one that appears first in the string.",
    "constraints": [
      "1 <= T <= 10",
      "1 <= |S| <= 1000",
      "S contains only letters (A\u2011Z, a\u2011z) and digits (0\u20119)",
      "Case\u2011sensitive comparison"
    ],
    "inputFormat": "The first line contains an integer T (1 \u2264 T \u2264 10), the number of test cases. Each of the next T lines contains a non\u2011empty string S consisting of English letters (uppercase and lowercase) and digits. |S| \u2264 1000.",
    "outputFormat": "For each test case, output on a separate line the longest palindromic substring of the given string. If several substrings have the same maximum length, output the one that appears first.",
    "sampleTestCases": [
      {
        "id": "sample_1",
        "input": "1\nracecar12321",
        "output": "racecar",
        "explanation": "The longest palindrome in the string is \"racecar\" (length 7)."
      },
      {
        "id": "sample_2",
        "input": "1\nabaxyzzyxf",
        "output": "xyzzyx",
        "explanation": "The longest palindrome is \"xyzzyx\" (length 6)."
      },
      {
        "id": "sample_3",
        "input": "1\n121323",
        "output": "121",
        "explanation": "Two palindromes of length 3 exist: \"121\" and \"323\". The earlier one \"121\" is chosen."
      }
    ]
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "label": "minimum boundary",
        "input": "1\na",
        "expectedOutput": "a",
        "weight": 2
      },
      {
        "id": "tc_2",
        "label": "maximum boundary (100 characters)",
        "input": "1\naaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa",
        "expectedOutput": "aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa",
        "weight": 2
      },
      {
        "id": "tc_3",
        "label": "tie with earlier occurrence",
        "input": "1\nabcba12321",
        "expectedOutput": "abcba",
        "weight": 2
      },
      {
        "id": "tc_4",
        "label": "full string palindrome",
        "input": "1\na1b2b1a",
        "expectedOutput": "a1b2b1a",
        "weight": 2
      },
      {
        "id": "tc_5",
        "label": "no palindrome longer than 1",
        "input": "1\nabcd",
        "expectedOutput": "a",
        "weight": 2
      }
    ],
    "totalTestCases": 5,
    "totalWeight": 10
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      2,
      2,
      2,
      2,
      2
    ],
    "maxScore": 10
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "For each position i in the string we try to expand a palindrome with i as the centre. Two kinds of centres are considered: (1) odd length \u2013 left = right = i, (2) even length \u2013 left = i, right = i+1. While the characters at left and right are equal we move left one step left and right one step right. After the expansion the substring (left+1, right\u20111) is the maximal palindrome for that centre. We keep the longest palindrome seen so far; if a newly found palindrome is longer we replace the answer. Because we only replace on strictly greater length, the first (smallest start index) palindrome of maximal length is retained automatically. The whole process is O(|S|) per test case.",
    "timeComplexity": "O(T * N) where N is the length of each string (\u2264 1000). Each character is visited a constant number of times during expansions.",
    "spaceComplexity": "O(1) extra space besides the input and output strings.",
    "code": {
      "C": "#include <stdio.h>\n#include <string.h>\n#include <stdlib.h>\n\n// expand around centre [left, right] and return the palindrome as a newly allocated string\nchar* expand(const char *s, int left, int right) {\n    int n = strlen(s);\n    while (left >= 0 && right < n && s[left] == s[right]) {\n        left--;\n        right++;\n    }\n    int len = right - left - 1;\n    char *res = (char*)malloc(len + 1);\n    memcpy(res, s + left + 1, len);\n    res[len] = '\\0';\n    return res;\n}\n\nint main() {\n    int T;\n    if (scanf(\"%d\", &T) != 1) return 0;\n    getchar(); // consume newline after T\n    char buffer[1005];\n    for (int tc = 0; tc < T; ++tc) {\n        if (!fgets(buffer, sizeof(buffer), stdin)) break;\n        buffer[strcspn(buffer, \"\\n\")] = '\\0'; // remove trailing newline\n        int n = strlen(buffer);\n        char *best = (char*)malloc(1);\n        best[0] = '\\0';\n        size_t bestLen = 0;\n        for (int i = 0; i < n; ++i) {\n            char *odd = expand(buffer, i, i);\n            size_t lenOdd = strlen(odd);\n            if (lenOdd > bestLen) {\n                free(best);\n                best = odd;\n                bestLen = lenOdd;\n            } else {\n                free(odd);\n            }\n            char *even = expand(buffer, i, i + 1);\n            size_t lenEven = strlen(even);\n            if (lenEven > bestLen) {\n                free(best);\n                best = even;\n                bestLen = lenEven;\n            } else {\n                free(even);\n            }\n        }\n        printf(\"%s\\n\", best);\n        free(best);\n    }\n    return 0;\n}",
      "C++": "#include <bits/stdc++.h>\nusing namespace std;\n\nstring expand(const string &s, int left, int right) {\n    int n = s.size();\n    while (left >= 0 && right < n && s[left] == s[right]) {\n        --left; ++right;\n    }\n    return s.substr(left + 1, right - left - 1);\n}\n\nint main() {\n    ios::sync_with_stdio(false);\n    cin.tie(nullptr);\n    int T; if(!(cin >> T)) return 0; string line; getline(cin, line); // consume endline\n    while (T--) {\n        string s; getline(cin, s);\n        string best = \"\";\n        for (int i = 0; i < (int)s.size(); ++i) {\n            string odd = expand(s, i, i);\n            if (odd.size() > best.size()) best = odd;\n            string even = expand(s, i, i + 1);\n            if (even.size() > best.size()) best = even;\n        }\n        cout << best << '\\n';\n    }\n    return 0;\n}",
      "Java": "import java.io.*;\n\npublic class Main {\n    private static String expand(String s, int left, int right) {\n        int n = s.length();\n        while (left >= 0 && right < n && s.charAt(left) == s.charAt(right)) {\n            left--;\n            right++;\n        }\n        return s.substring(left + 1, right);\n    }\n\n    public static void main(String[] args) throws Exception {\n        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));\n        int T = Integer.parseInt(br.readLine().trim());\n        StringBuilder out = new StringBuilder();\n        for (int tc = 0; tc < T; ++tc) {\n            String s = br.readLine();\n            String best = \"\";\n            for (int i = 0; i < s.length(); ++i) {\n                String odd = expand(s, i, i);\n                if (odd.length() > best.length()) best = odd;\n                String even = expand(s, i, i + 1);\n                if (even.length() > best.length()) best = even;\n            }\n            out.append(best);\n            if (tc + 1 < T) out.append('\\n');\n        }\n        System.out.print(out.toString());\n    }\n}",
      "Python3": "import sys\n\ndef expand(s, left, right):\n    while left >= 0 and right < len(s) and s[left] == s[right]:\n        left -= 1\n        right += 1\n    return s[left + 1:right]\n\ndef longest_palindrome(s):\n    best = \"\"\n    for i in range(len(s)):\n        odd = expand(s, i, i)\n        if len(odd) > len(best):\n            best = odd\n        even = expand(s, i, i + 1)\n        if len(even) > len(best):\n            best = even\n    return best\n\ndef main():\n    data = sys.stdin.read().splitlines()\n    if not data:\n        return\n    T = int(data[0].strip())\n    out = []\n    for i in range(1, T + 1):\n        s = data[i].rstrip('\\n')\n        out.append(longest_palindrome(s))\n    sys.stdout.write('\\n'.join(out))\n\nif __name__ == \"__main__\":\n    main()",
      "JavaScript": "const fs = require('fs');\nconst lines = fs.readFileSync(0, 'utf-8').trim().split('\\n');\nlet idx = 0;\nconst T = parseInt(lines[idx++], 10);\nlet results = [];\nfunction expand(s, left, right) {\n    while (left >= 0 && right < s.length && s[left] === s[right]) {\n        left--; right++;\n    }\n    return s.slice(left + 1, right);\n}\nfor (let t = 0; t < T; ++t) {\n    const s = lines[idx++] || '';\n    let best = '';\n    for (let i = 0; i < s.length; ++i) {\n        const odd = expand(s, i, i);\n        if (odd.length > best.length) best = odd;\n        const even = expand(s, i, i + 1);\n        if (even.length > best.length) best = even;\n    }\n    results.push(best);\n}\nconsole.log(results.join('\\n'));"
    }
  }
}