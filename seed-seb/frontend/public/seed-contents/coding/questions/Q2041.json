{
  "questionId": "Q2041",
  "title": "Handling Exceptional Inputs",
  "slug": "handling-exceptional-inputs",
  "version": 1,
  "createdAt": "2026-07-01",
  "updatedAt": "2026-07-04",
  "metadata": {
    "category": "Implementation",
    "difficulty": "Easy",
    "isPremium": false,
    "tags": [
      "Nested Try-Catch",
      "Input Validation",
      "Exception Handling",
      "Implementation"
    ],
    "companyTags": [],
    "topicTags": [
      "Exception Handling"
    ]
  },
  "content": {
    "problemStatement": "You are tasked with creating a Python program that takes two variables as input and handles exceptions using nested try-except blocks.\n\n\n\nCreate a Python program that performs the following tasks:\n\n\n\n\nPrompt the user to enter two variables, `num1` and `num2`.\n\n\n\n\nImplement a division operation: `result = num1 / num2;`.\n\n\n\n\n\n\nUse nested try-except blocks to handle the following scenarios:\n\n\n\n\n**Innermost try-except (specific exception handling):** Use an inner try-except block to catch exceptions related to invalid input. If the user enters a non-integer value (e.g., a character), throw an exception of type `IllegalArgumentException` with the message \"Invalid input.\"\n\n\n\n\n**Outermost try-except (general exception handling):** Use an outermost try-except block to catch any unhandled exceptions and display \"An unknown error occurred.\".\n\n\n\n\n\n\nDisplay the result of the division operation if it is successful or handle exceptions gracefully and provide meaningful error messages.\n\n\n\nTaskComplete the code to handles exceptions using nested try-except blocks.\n\n\nInput Format\nFirst line contains two variables. (int, double, etc.)\n\n\n\nOutput Format\nComplete the code to display the error message.",
    "constraints": [
      "1 <= T <= 100",
      "-10^9 <= num1, num2 <= 10^9 (when they are valid integers)"
    ],
    "inputFormat": "The first line contains an integer T, the number of test cases. Each test case consists of two lines: the first line is the first value (num1) and the second line is the second value (num2).",
    "outputFormat": "For each test case output one line:\n- If both inputs are valid integers and division succeeds, print the result of num1 / num2 as a decimal number (Python's default string conversion of a float).\n- If either input is not a valid integer, print \"Invalid input.\".\n- If any other exception occurs (e.g., division by zero), print \"An unknown error occurred.\".",
    "sampleTestCases": [
      {
        "id": "sample_1",
        "input": "1\n5\ntom",
        "output": "Invalid input.",
        "explanation": "The second value \"tom\" is not an integer, so the inner validation fails and the program prints the specific error message."
      },
      {
        "id": "sample_2",
        "input": "1\n10\n2",
        "output": "5",
        "explanation": "Both inputs are valid integers and division succeeds; 10/2 = 5, which is printed without a decimal part."
      },
      {
        "id": "sample_3",
        "input": "1\n8\n0",
        "output": "An unknown error occurred.",
        "explanation": "Inputs are integers, but division by zero triggers an exception not caught by the inner block, so the outer block prints the generic message."
      }
    ]
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "label": "minimum boundary",
        "input": "1\n0\n1",
        "expectedOutput": "0",
        "weight": 2
      },
      {
        "id": "tc_2",
        "label": "maximum boundary",
        "input": "1\n1000000000\n1",
        "expectedOutput": "1000000000",
        "weight": 2
      },
      {
        "id": "tc_3",
        "label": "division by zero",
        "input": "1\n123\n0",
        "expectedOutput": "An unknown error occurred.",
        "weight": 2
      },
      {
        "id": "tc_4",
        "label": "negative division",
        "input": "1\n-10\n2",
        "expectedOutput": "-5",
        "weight": 2
      },
      {
        "id": "tc_5",
        "label": "invalid first operand",
        "input": "1\nabc\n5",
        "expectedOutput": "Invalid input.",
        "weight": 2
      }
    ],
    "totalTestCases": 5,
    "totalWeight": 10
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      2,
      2,
      2,
      2,
      2
    ],
    "maxScore": 10
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "For each test case we need to read the two values as strings. The innermost try block validates that both strings represent integers; if not, it throws an exception that is caught immediately and prints \"Invalid input.\". If the validation succeeds we convert the strings to integers and perform the division. Any exception that is not caught by the inner block (for example division by zero) propagates to the outer try\u2011catch, where we print the generic message \"An unknown error occurred.\". This structure mirrors nested try\u2011except in Python and nested try\u2011catch in C++/Java. The algorithm runs in O(T\u00b7L) time where L is the length of the input strings, and uses O(1) additional memory.",
    "timeComplexity": "O(T\u00b7L) \u2013 each test case processes two strings of length L.",
    "spaceComplexity": "O(1) \u2013 only a few variables are stored per test case.",
    "code": {
      "C": "#include <stdio.h>\n#include <stdlib.h>\n#include <stdbool.h>\n#include <string.h>\n\nbool is_int(const char *s) {\n    if (*s == '-' || *s == '+') s++;\n    if (*s == '\\0') return false;\n    while (*s) {\n        if (*s < '0' || *s > '9') return false;\n        s++;\n    }\n    return true;\n}\n\nint main() {\n    int T;\n    if (scanf(\"%d\", &T) != 1) return 0;\n    char buf1[64], buf2[64];\n    for (int tc = 0; tc < T; ++tc) {\n        if (scanf(\"%63s\", buf1) != 1) break;\n        if (scanf(\"%63s\", buf2) != 1) break;\n        // inner validation (simulating inner try)\n        if (!is_int(buf1) || !is_int(buf2)) {\n            printf(\"Invalid input.\\n\");\n            continue;\n        }\n        long long a = atoll(buf1);\n        long long b = atoll(buf2);\n        // outer block (simulating outer try)\n        if (b == 0) {\n            printf(\"An unknown error occurred.\\n\");\n        } else {\n            double res = (double)a / (double)b;\n            // print without trailing .0 when possible, matching sample output\n            if (res == (long long)res) {\n                printf(\"%lld\\n\", (long long)res);\n            } else {\n                printf(\"%g\\n\", res);\n            }\n        }\n    }\n    return 0;\n}",
      "C++": "#include <bits/stdc++.h>\nusing namespace std;\n\nbool isInt(const string &s) {\n    if (s.empty()) return false;\n    size_t i = 0;\n    if (s[0] == '-' || s[0] == '+') i = 1;\n    if (i == s.size()) return false;\n    for (; i < s.size(); ++i) if (!isdigit(s[i])) return false;\n    return true;\n}\n\nint main() {\n    ios::sync_with_stdio(false);\n    cin.tie(nullptr);\n    int T;\n    if (!(cin >> T)) return 0;\n    string num1, num2;\n    for (int tc = 0; tc < T; ++tc) {\n        cin >> num1 >> num2;\n        try {\n            try {\n                if (!isInt(num1) || !isInt(num2))\n                    throw invalid_argument(\"Invalid input.\");\n                long long a = stoll(num1);\n                long long b = stoll(num2);\n                if (b == 0) throw runtime_error(\"division by zero\");\n                double res = static_cast<double>(a) / static_cast<double>(b);\n                // match sample output: omit .0 when result is integral\n                if (res == (long long)res)\n                    cout << (long long)res << '\\n';\n                else\n                    cout << res << '\\n';\n            } catch (const invalid_argument &e) {\n                cout << e.what() << '\\n';\n            }\n        } catch (...) {\n            cout << \"An unknown error occurred.\" << '\\n';\n        }\n    }\n    return 0;\n}",
      "Java": "import java.io.*;\n\npublic class Main {\n    private static boolean isInt(String s) {\n        if (s == null || s.isEmpty()) return false;\n        int i = 0;\n        if (s.charAt(0) == '-' || s.charAt(0) == '+') i = 1;\n        if (i == s.length()) return false;\n        for (; i < s.length(); i++) {\n            if (!Character.isDigit(s.charAt(i))) return false;\n        }\n        return true;\n    }\n\n    public static void main(String[] args) throws Exception {\n        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));\n        String line = br.readLine();\n        if (line == null) return;\n        int T = Integer.parseInt(line.trim());\n        StringBuilder sb = new StringBuilder();\n        for (int tc = 0; tc < T; tc++) {\n            String num1 = br.readLine();\n            String num2 = br.readLine();\n            try {\n                try {\n                    if (!isInt(num1) || !isInt(num2)) {\n                        throw new IllegalArgumentException(\"Invalid input.\");\n                    }\n                    long a = Long.parseLong(num1.trim());\n                    long b = Long.parseLong(num2.trim());\n                    if (b == 0) throw new ArithmeticException(\"division by zero\");\n                    double res = (double) a / (double) b;\n                    if (res == (long) res) {\n                        sb.append((long) res).append('\\n');\n                    } else {\n                        sb.append(res).append('\\n');\n                    }\n                } catch (IllegalArgumentException e) {\n                    sb.append(e.getMessage()).append('\\n');\n                }\n            } catch (Exception e) {\n                sb.append(\"An unknown error occurred.\").append('\\n');\n            }\n        }\n        System.out.print(sb.toString());\n    }\n}",
      "Python3": "# COMPLETE, RUNNABLE Python3 code\nimport sys\n\ndef is_int(s: str) -> bool:\n    s = s.strip()\n    return s.lstrip('-+').isdigit()\n\ndef main():\n    data = sys.stdin.read().splitlines()\n    if not data:\n        return\n    T = int(data[0].strip())\n    idx = 1\n    out = []\n    for _ in range(T):\n        if idx >= len(data):\n            break\n        num1 = data[idx].strip() if idx < len(data) else ''\n        idx += 1\n        num2 = data[idx].strip() if idx < len(data) else ''\n        idx += 1\n        try:\n            try:\n                if not (is_int(num1) and is_int(num2)):\n                    raise ValueError(\"Invalid input.\")\n                a = int(num1)\n                b = int(num2)\n                result = a / b\n                # match sample output: drop trailing .0 when result is integral\n                if result == int(result):\n                    out.append(str(int(result)))\n                else:\n                    out.append(str(result))\n            except ValueError as ve:\n                out.append(str(ve))\n        except Exception:\n            out.append(\"An unknown error occurred.\")\n    sys.stdout.write(\"\\n\".join(out))\n\nif __name__ == \"__main__\":\n    main()\n",
      "JavaScript": "// COMPLETE, RUNNABLE JavaScript code\nconst fs = require('fs');\nconst lines = fs.readFileSync(0, 'utf-8').trim().split(/\\r?\\n/);\nif (lines.length === 0) process.exit(0);\nlet T = parseInt(lines[0].trim(), 10);\nlet idx = 1;\nlet output = [];\nfunction isInt(str) {\n  if (str === undefined) return false;\n  str = str.trim();\n  if (str.length === 0) return false;\n  if (str[0] === '-' || str[0] === '+') str = str.slice(1);\n  return /^[0-9]+$/.test(str);\n}\nfor (let tc = 0; tc < T; tc++) {\n  const num1 = lines[idx++] ?? '';\n  const num2 = lines[idx++] ?? '';\n  try {\n    try {\n      if (!isInt(num1) || !isInt(num2)) {\n        throw new Error('Invalid input.');\n      }\n      const a = Number(num1);\n      const b = Number(num2);\n      if (b === 0) throw new Error('division by zero');\n      const res = a / b;\n      // drop trailing .0 when integer\n      if (Number.isInteger(res)) {\n        output.push(String(res));\n      } else {\n        output.push(String(res));\n      }\n    } catch (e) {\n      if (e.message === 'Invalid input.') {\n        output.push(e.message);\n      } else {\n        throw e; // re\u2011throw to outer block\n      }\n    }\n  } catch (_) {\n    output.push('An unknown error occurred.');\n  }\n}\nconsole.log(output.join('\\n'));\n"
    }
  }
}