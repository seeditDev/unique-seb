{
  "questionId": "Q1811",
  "title": "Chef Finds Word Ladder",
  "slug": "chef-finds-word-ladder",
  "version": 1,
  "createdAt": "2026-07-01",
  "updatedAt": "2026-07-04",
  "metadata": {
    "category": "Graphs",
    "difficulty": "Hard",
    "isPremium": false,
    "tags": [
      "Graphs",
      "BFS",
      "Word Ladder"
    ],
    "companyTags": [],
    "topicTags": [
      "Graphs"
    ]
  },
  "content": {
    "problemStatement": "Chef loves playing with words and now Chef has a new challenge. Chef has two words: a start word startWordstartWordstartWord and a target word targetWordtargetWordtargetWord. Chef wants to transform the start word into the target word by changing one letter at a time. Each intermediate word in this transformation must be a valid word from Chef's given dictionary wordListwordListwordList. \n\nChef wants to find all the shortest possible sequences of such transformations from the start word to the target word. Help Chef find all these shortest transformation sequences or determine if it is impossible.\n\nFunction DeclarationFunction NamefindWordLaddersfindWordLaddersfindWordLadders - This function finds all shortest transformation sequences from a start word to a target word using a given word list.\n\nParameters\nstartWordstartWordstartWord : The initial word from which transformations begin.\ntargetWordtargetWordtargetWord : The final word to reach through valid transformations.\nwordListwordListwordList : A list of allowed intermediate words for transformations.\nReturn Value\nReturns a array of array, where each inner vector represents a shortest transformation sequence from startWordstartWordstartWord to targetWordtargetWordtargetWord.\nEach sequence includes the startWordstartWordstartWord, intermediate words, and the targetWordtargetWordtargetWord.\nIf no sequence exists, returns an empty array.\nConstraints\n1\u2264T\u22641051 \\leq T \\leq 10^51\u2264T\u2264105\n1\u2264N\u22645001 \\leq N \\leq 5001\u2264N\u2264500\n1\u2264length(startWord)=length(targetWord)\u226451 \\leq \\text{length}(startWord) = \\text{length}(targetWord) \\leq 51\u2264length(startWord)=length(targetWord)\u22645\nAll words in `wordList`, as well as `startWord` and `targetWord`, consist of lowercase English letters.\nstartWord\u2260targetWordstartWord \\neq targetWordstartWord\ue020=targetWord\nAll words in `wordList` are unique.\nThe total number of shortest transformation sequences across all test cases does not exceed 10510^5105.\n\nInput Format\n\nThe first line contains a single integer TTT \u2014 the number of test cases.\nFor each test case:\n\nThe first line contains a stringType startWordstartWordstartWord.\nThe second line contains a stringType targetWordtargetWordtargetWord.\nThe third line contains NNN space-separated stringType words representing wordListwordListwordList.\n\n\n\n\nOutput Format\n\nFor each test case, print all shortest transformation sequences.\nEach sequence should be printed on a separate line with words separated by spaces.\nIf no such sequence exists, print a single line with `-1`.",
    "constraints": [
      "1 <= T <= 10^5",
      "1 <= N <= 500",
      "1 <= length(startWord) = length(targetWord) <= 5",
      "All words consist of lowercase English letters.",
      "startWord != targetWord",
      "All words in wordList are unique.",
      "The total number of shortest sequences over all test cases does not exceed 10^5."
    ],
    "inputFormat": "First line contains T, the number of test cases. For each test case: a line with the string startWord, a line with the string targetWord, and a line with N space-separated strings representing wordList.",
    "outputFormat": "For each test case, print all shortest transformation sequences. Each sequence should be printed on a separate line with words separated by a single space. If no sequence exists, print a single line with -1.",
    "sampleTestCases": [
      {
        "id": "sample_1",
        "input": "2\nhit\ncog\nhot dot dog lot log cog\nhit\ncog\nhot dot dog lot log",
        "output": "hit hot lot log cog\nhit hot dot dog cog",
        "explanation": "For the first test case, we can transform 'hit' into 'cog' by changing one character at a time, using the words in the word list. The two possible paths are 'hit' -> 'hot' -> 'lot' -> 'log' -> 'cog' and 'hit' -> 'hot' -> 'dot' -> 'dog' -> 'cog'. For the second test case, we can't transform 'hit' into 'cog' because 'cog' is not in the word list."
      },
      {
        "id": "sample_2",
        "input": "1\nred\nbed\nbed bad bud red",
        "output": "red bed",
        "explanation": "We can transform 'red' into 'bed' by changing one character at a time, using the words in the word list. The only possible path is 'red' -> 'bed'."
      },
      {
        "id": "sample_3",
        "input": "1\nlead\ngold\nload goad gold lead loaf",
        "output": "lead load goad gold",
        "explanation": "We can transform 'lead' into 'gold' by changing one character at a time, using the words in the word list. The only possible path is 'lead' -> 'load' -> 'goad' -> 'gold'."
      }
    ]
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "label": "minimum boundary",
        "input": "1\na\nb\na b",
        "expectedOutput": "a b",
        "weight": 2
      },
      {
        "id": "tc_2",
        "label": "maximum boundary",
        "input": "1\nabcde\nfghij\nabcde fghij",
        "expectedOutput": "-1",
        "weight": 2
      },
      {
        "id": "tc_3",
        "label": "uniform/edge case",
        "input": "1\naaaa\naaaa\naaaa",
        "expectedOutput": "aaaa",
        "weight": 2
      },
      {
        "id": "tc_4",
        "label": "tricky case",
        "input": "1\nabc\ndef\nabc def",
        "expectedOutput": "-1",
        "weight": 2
      },
      {
        "id": "tc_5",
        "label": "stress/general",
        "input": "2\nhit\ncog\nhot dot dog lot log cog\nhit\ncog\nhot dot dog lot log",
        "expectedOutput": "hit hot lot log cog\nhit hot dot dog cog",
        "weight": 2
      }
    ],
    "totalTestCases": 5,
    "totalWeight": 10
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      2,
      2,
      2,
      2,
      2
    ],
    "maxScore": 10
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "To solve this problem, we use a Breadth-First Search (BFS) algorithm. We start with the startWord and explore all possible transformations by changing one character at a time. We use a queue to keep track of the words to be processed and a set to keep track of the visited words. We also use a dictionary to store the parent of each word to reconstruct the transformation sequences.",
    "timeComplexity": "O(T * N * M * 26) per test case, where M is the length of the words. The total time complexity is O(T * N * M * 26).",
    "spaceComplexity": "O(T * N * M) \u2014 the dominant structure is the queue and the set used in the BFS algorithm.",
    "code": {
      "C": "#include <stdio.h>\n#include <stdlib.h>\n#include <string.h>\n#include <stdbool.h>\n#define MAX_WORD_LEN 6\n#define MAX_WORDS 500\n\nvoid bfs(char *startWord, char *targetWord, char **wordList, int wordListSize) {\n    int N = wordListSize;\n    int M = strlen(startWord);\n    bool visited[N];\n    for (int i = 0; i < N; i++) {\n        visited[i] = false;\n    }\n    int queueSize = 0;\n    char **queue = (char **)malloc(N * sizeof(char *));\n    for (int i = 0; i < N; i++) {\n        queue[i] = (char *)malloc(MAX_WORD_LEN * sizeof(char));\n    }\n    strcpy(queue[queueSize++], startWord);\n    visited[0] = true;\n    int level = 0;\n    while (queueSize > 0) {\n        int levelSize = queueSize;\n        for (int i = 0; i < levelSize; i++) {\n            char *currentWord = queue[i - level];\n            if (strcmp(currentWord, targetWord) == 0) {\n                printf(\"%s\", currentWord);\n                return;\n            }\n            for (int j = 0; j < N; j++) {\n                if (!visited[j]) {\n                    char *word = wordList[j];\n                    int diff = 0;\n                    for (int k = 0; k < M; k++) {\n                        if (currentWord[k] != word[k]) {\n                            diff++;\n                        }\n                    }\n                    if (diff == 1) {\n                        strcpy(queue[queueSize], word);\n                        queueSize++;\n                        visited[j] = true;\n                    }\n                }\n            }\n        }\n        level++;\n    }\n    printf(\"-1\");\n}\n\nint main() {\n    int T;\n    scanf(\"%d\", &T);\n    while (T--) {\n        char startWord[MAX_WORD_LEN], targetWord[MAX_WORD_LEN];\n        scanf(\"%s %s\", startWord, targetWord);\n        int N;\n        scanf(\"%d\", &N);\n        char **wordList = (char **)malloc(N * sizeof(char *));\n        for (int i = 0; i < N; i++) {\n            wordList[i] = (char *)malloc(MAX_WORD_LEN * sizeof(char));\n            scanf(\"%s\", wordList[i]);\n        }\n        bfs(startWord, targetWord, wordList, N);\n        for (int i = 0; i < N; i++) {\n            free(wordList[i]);\n        }\n        free(wordList);\n    }\n    return 0;\n}",
      "C++": "#include <iostream>\n#include <queue>\n#include <unordered_set>\n#include <unordered_map>\n#include <string>\nusing namespace std;\n\nvoid bfs(string startWord, string targetWord, vector<string> &wordList) {\n    int N = wordList.size();\n    int M = startWord.length();\n    unordered_set<string> visited;\n    queue<pair<string, vector<string>>> q;\n    q.push({startWord, {startWord}});\n    visited.insert(startWord);\n    while (!q.empty()) {\n        string currentWord = q.front().first;\n        vector<string> path = q.front().second;\n        q.pop();\n        if (currentWord == targetWord) {\n            for (const auto &word : path) {\n                cout << word << \" \";\n            }\n            cout << endl;\n            return;\n        }\n        for (int i = 0; i < N; i++) {\n            string word = wordList[i];\n            if (visited.find(word) == visited.end()) {\n                int diff = 0;\n                for (int j = 0; j < M; j++) {\n                    if (currentWord[j] != word[j]) {\n                        diff++;\n                    }\n                }\n                if (diff == 1) {\n                    q.push({word, path});\n                    q.back().second.push_back(word);\n                    visited.insert(word);\n                }\n            }\n        }\n    }\n    cout << -1;\n}\n\nint main() {\n    ios::sync_with_stdio(false);\n    cin.tie(NULL);\n    int T;\n    cin >> T;\n    while (T--) {\n        string startWord, targetWord;\n        cin >> startWord >> targetWord;\n        int N;\n        cin >> N;\n        vector<string> wordList(N);\n        for (int i = 0; i < N; i++) {\n            cin >> wordList[i];\n        }\n        bfs(startWord, targetWord, wordList);\n    }\n    return 0;\n}",
      "Java": "import java.io.BufferedReader;\nimport java.io.InputStreamReader;\nimport java.util.Queue;\nimport java.util.LinkedList;\nimport java.util.HashSet;\nimport java.util.HashMap;\nimport java.util.ArrayList;\nimport java.util.List;\n\npublic class Main {\n    public static void bfs(String startWord, String targetWord, String[] wordList) {\n        int N = wordList.length;\n        int M = startWord.length();\n        HashSet<String> visited = new HashSet<>();\n        Queue<String> q = new LinkedList<>();\n        q.add(startWord);\n        visited.add(startWord);\n        while (!q.isEmpty()) {\n            String currentWord = q.poll();\n            if (currentWord.equals(targetWord)) {\n                System.out.println(currentWord);\n                return;\n            }\n            for (int i = 0; i < N; i++) {\n                String word = wordList[i];\n                if (!visited.contains(word)) {\n                    int diff = 0;\n                    for (int j = 0; j < M; j++) {\n                        if (currentWord.charAt(j) != word.charAt(j)) {\n                            diff++;\n                        }\n                    }\n                    if (diff == 1) {\n                        q.add(word);\n                        visited.add(word);\n                    }\n                }\n            }\n        }\n        System.out.println(-1);\n    }\n    \n    public static void main(String[] args) throws Exception {\n        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));\n        int T = Integer.parseInt(br.readLine());\n        while (T-- > 0) {\n            String[] input = br.readLine().split(\" \");\n            String startWord = input[0];\n            String targetWord = input[1];\n            int N = Integer.parseInt(br.readLine());\n            String[] wordList = br.readLine().split(\" \");\n            bfs(startWord, targetWord, wordList);\n        }\n    }\n}",
      "Python3": "import sys\nfrom collections import deque\nfrom typing import List\n\ndef bfs(startWord: str, targetWord: str, wordList: List[str]) -> None:\n    N = len(wordList)\n    M = len(startWord)\n    visited = set()\n    q = deque([(startWord, [startWord])])\n    visited.add(startWord)\n    while q:\n        currentWord, path = q.popleft()\n        if currentWord == targetWord:\n            print(' '.join(path))\n            return\n        for i in range(N):\n            word = wordList[i]\n            if word not in visited:\n                diff = sum(c1 != c2 for c1, c2 in zip(currentWord, word))\n                if diff == 1:\n                    q.append((word, path + [word]))\n                    visited.add(word)\n    print(-1)\n\nif __name__ == '__main__':\n    T = int(sys.stdin.readline().strip())\n    for _ in range(T):\n        startWord, targetWord = sys.stdin.readline().strip().split()\n        N = int(sys.stdin.readline().strip())\n        wordList = sys.stdin.readline().strip().split()\n        bfs(startWord, targetWord, wordList)",
      "JavaScript": "const lines = require('fs').readFileSync(0, 'utf-8').trim().split('\\n');\nlet i = 0;\n\nfunction bfs(startWord, targetWord, wordList) {\n    let N = wordList.length;\n    let M = startWord.length;\n    let visited = new Set();\n    let q = [[startWord, [startWord]]];\n    visited.add(startWord);\n    while (q.length > 0) {\n        let [currentWord, path] = q.shift();\n        if (currentWord === targetWord) {\n            console.log(path.join(' '));\n            return;\n        }\n        for (let i = 0; i < N; i++) {\n            let word = wordList[i];\n            if (!visited.has(word)) {\n                let diff = 0;\n                for (let j = 0; j < M; j++) {\n                    if (currentWord[j] !== word[j]) {\n                        diff++;\n                    }\n                }\n                if (diff === 1) {\n                    q.push([word, [...path, word]]);\n                    visited.add(word);\n                }\n            }\n        }\n    }\n    console.log(-1);\n}\n\nfunction main() {\n    let T = +lines[i++];\n    while (T--) {\n        let [startWord, targetWord] = lines[i++].trim().split(' ');\n        let N = +lines[i++];\n        let wordList = lines[i++].trim().split(' ');\n        bfs(startWord, targetWord, wordList);\n    }\n}\n\nmain();"
    }
  }
}