{
  "questionId": "Q3682",
  "title": "Two Coins",
  "slug": "two-coins",
  "version": 1,
  "createdAt": "2026-07-02",
  "updatedAt": "2026-07-04",
  "metadata": {
    "category": "Graphs",
    "difficulty": "Hard",
    "isPremium": false,
    "tags": [
      "Tree",
      "Graphs",
      "Graph Theory",
      "Dynamic Programming"
    ],
    "companyTags": [],
    "topicTags": [
      "Trees"
    ]
  },
  "content": {
    "problemStatement": "You are given a rooted tree consisting of **N** nodes. The nodes are numbered from 1 to **N**, and node 1 is the root. At each node of the tree, you can put zero or one coin such that the following property is satisfied for the tree:\n\n\n  For each node of the tree, starting from the original configuration, we should be able to get two coins on the node by applying at most two operations of the following kind:\n  \n    Take a coin from a node and move it to an adjacent node.\n  \n  While trying to get to a particular node, if the same coin is moved in two operations, both those operations should either be towards the root, or both of them should be away from the root. That is, you cannot move a coin to its parent in one operation and then take it to another child of the parent in the second operation.\nFind the minimum total number of coins which can be used to get a valid configuration.",
    "constraints": [
      "1 <= T <= 10",
      "1 <= N <= 1000",
      "1 <= u, v <= N"
    ],
    "inputFormat": "The first line of the input contains an integer T, the number of test cases. For each test case, the first line contains an integer N, the number of nodes in the tree. Each of the next N - 1 lines contains two space-separated integers u and v, denoting that there is an edge between node u and node v of the tree.",
    "outputFormat": "For each test case, output a single integer corresponding to the minimum number of coins that can be used. If it's not possible to achieve this, output -1.",
    "sampleTestCases": [
      {
        "id": "sample_1",
        "input": "2\n3\n1 2\n1 3\n5\n1 2\n1 3\n3 4\n3 5",
        "output": "3\n4",
        "explanation": "For the first test case, we can place coins on nodes 1, 2, and 3. For the second test case, we can place coins on nodes 1, 2, 3, and 4."
      }
    ]
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "label": "minimum boundary",
        "input": "1\n1",
        "expectedOutput": "1",
        "weight": 2
      },
      {
        "id": "tc_2",
        "label": "maximum boundary",
        "input": "1\n1000",
        "expectedOutput": "1000",
        "weight": 2
      },
      {
        "id": "tc_3",
        "label": "edge case",
        "input": "1\n2\n1 2",
        "expectedOutput": "2",
        "weight": 2
      },
      {
        "id": "tc_4",
        "label": "general case 1",
        "input": "1\n5\n1 2\n1 3\n2 4\n3 5",
        "expectedOutput": "4",
        "weight": 2
      },
      {
        "id": "tc_5",
        "label": "general case 2",
        "input": "1\n6\n1 2\n1 3\n2 4\n3 5\n4 6",
        "expectedOutput": "5",
        "weight": 2
      }
    ],
    "totalTestCases": 5,
    "totalWeight": 10
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      2,
      2,
      2,
      2,
      2
    ],
    "maxScore": 10
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "To solve this problem, we can use a dynamic programming approach. We will perform a depth-first search (DFS) on the tree and for each node, we will calculate the minimum number of coins required to satisfy the condition. We will consider two cases for each node: one where we place a coin on the node and one where we don't. We will then choose the case that requires the minimum number of coins.",
    "timeComplexity": "O(T * N) - where T is the number of test cases and N is the number of nodes in the tree. The DFS traversal takes O(N) time for each test case.",
    "spaceComplexity": "O(N) - where N is the number of nodes in the tree. We need to store the adjacency list of the tree and the dynamic programming table.",
    "code": {
      "C": "#include <stdio.h>\n#include <stdlib.h>\n#define MAXN 1005\n\nint minCoins[MAXN];\nint adj[MAXN][MAXN];\nint visited[MAXN];\n\nvoid dfs(int node, int parent) {\n    visited[node] = 1;\n    int children = 0;\n    for (int i = 1; i <= N; i++) {\n        if (adj[node][i] == 1 && i != parent) {\n            children++;\n            if (!visited[i]) {\n                dfs(i, node);\n            }\n        }\n    }\n    if (children == 0) {\n        minCoins[node] = 1;\n    } else {\n        int sum = 0;\n        for (int i = 1; i <= N; i++) {\n            if (adj[node][i] == 1 && i != parent) {\n                sum += minCoins[i];\n            }\n        }\n        minCoins[node] = sum + (sum == 0 ? 1 : 0);\n    }\n}\n\nint main() {\n    int T;\n    scanf(\"%d\", &T);\n    while (T--) {\n        int N;\n        scanf(\"%d\", &N);\n        for (int i = 1; i <= N; i++) {\n            for (int j = 1; j <= N; j++) {\n                adj[i][j] = 0;\n            }\n        }\n        for (int i = 1; i < N; i++) {\n            int u, v;\n            scanf(\"%d %d\", &u, &v);\n            adj[u][v] = adj[v][u] = 1;\n        }\n        for (int i = 1; i <= N; i++) {\n            visited[i] = 0;\n            minCoins[i] = 0;\n        }\n        dfs(1, -1);\n        printf(\"%d\\n\", minCoins[1]);\n    }\n    return 0;\n}",
      "C++": "#include <iostream>\n#include <vector>\nusing namespace std;\n#define MAXN 1005\n\nint minCoins[MAXN];\nvector<int> adj[MAXN];\nbool visited[MAXN];\n\nvoid dfs(int node, int parent) {\n    visited[node] = true;\n    int children = 0;\n    for (int i = 0; i < adj[node].size(); i++) {\n        if (adj[node][i] != parent) {\n            children++;\n            if (!visited[adj[node][i]]) {\n                dfs(adj[node][i], node);\n            }\n        }\n    }\n    if (children == 0) {\n        minCoins[node] = 1;\n    } else {\n        int sum = 0;\n        for (int i = 0; i < adj[node].size(); i++) {\n            if (adj[node][i] != parent) {\n                sum += minCoins[adj[node][i]];\n            }\n        }\n        minCoins[node] = sum + (sum == 0 ? 1 : 0);\n    }\n}\n\nint main() {\n    int T;\n    cin >> T;\n    while (T--) {\n        int N;\n        cin >> N;\n        for (int i = 0; i <= N; i++) {\n            adj[i].clear();\n            visited[i] = false;\n            minCoins[i] = 0;\n        }\n        for (int i = 1; i < N; i++) {\n            int u, v;\n            cin >> u >> v;\n            adj[u].push_back(v);\n            adj[v].push_back(u);\n        }\n        dfs(1, -1);\n        cout << minCoins[1] << endl;\n    }\n    return 0;\n}",
      "Java": "import java.util.ArrayList;\nimport java.util.Scanner;\n\npublic class Main {\n    static int[] minCoins;\n    static ArrayList<Integer>[] adj;\n    static boolean[] visited;\n    static int N;\n\n    public static void dfs(int node, int parent) {\n        visited[node] = true;\n        int children = 0;\n        for (int i = 0; i < adj[node].size(); i++) {\n            if (adj[node].get(i) != parent) {\n                children++;\n                if (!visited[adj[node].get(i)]) {\n                    dfs(adj[node].get(i), node);\n                }\n            }\n        }\n        if (children == 0) {\n            minCoins[node] = 1;\n        } else {\n            int sum = 0;\n            for (int i = 0; i < adj[node].size(); i++) {\n                if (adj[node].get(i) != parent) {\n                    sum += minCoins[adj[node].get(i)];\n                }\n            }\n            minCoins[node] = sum + (sum == 0 ? 1 : 0);\n        }\n    }\n\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int T = sc.nextInt();\n        while (T-- > 0) {\n            N = sc.nextInt();\n            minCoins = new int[N + 1];\n            adj = new ArrayList[N + 1];\n            visited = new boolean[N + 1];\n            for (int i = 0; i <= N; i++) {\n                adj[i] = new ArrayList<>();\n                visited[i] = false;\n                minCoins[i] = 0;\n            }\n            for (int i = 1; i < N; i++) {\n                int u = sc.nextInt();\n                int v = sc.nextInt();\n                adj[u].add(v);\n                adj[v].add(u);\n            }\n            dfs(1, -1);\n            System.out.println(minCoins[1]);\n        }\n    }\n}",
      "Python3": "import sys\n\nminCoins = [0] * 1005\nadj = [[] for _ in range(1005)]\nvisited = [False] * 1005\n\ndef dfs(node, parent):\n    global minCoins, adj, visited\n    visited[node] = True\n    children = 0\n    for i in adj[node]:\n        if i != parent:\n            children += 1\n            if not visited[i]:\n                dfs(i, node)\n    if children == 0:\n        minCoins[node] = 1\n    else:\n        sum = 0\n        for i in adj[node]:\n            if i != parent:\n                sum += minCoins[i]\n        minCoins[node] = sum + (1 if sum == 0 else 0)\n\nT = int(input())\nwhile T > 0:\n    N = int(input())\n    for i in range(1, N + 1):\n        adj[i] = []\n        visited[i] = False\n        minCoins[i] = 0\n    for _ in range(N - 1):\n        u, v = map(int, input().split())\n        adj[u].append(v)\n        adj[v].append(u)\n    dfs(1, -1)\n    print(minCoins[1])\n    T -= 1",
      "JavaScript": "const fs = require('fs');\nconst lines = fs.readFileSync(0, 'utf-8').trim().split('\\n');\nlet idx = 0;\n\nlet minCoins;\nlet adj;\nlet visited;\nlet N;\n\nfunction dfs(node, parent) {\n    visited[node] = true;\n    let children = 0;\n    for (let i = 0; i < adj[node].length; i++) {\n        if (adj[node][i] !== parent) {\n            children++;\n            if (!visited[adj[node][i]]) {\n                dfs(adj[node][i], node);\n            }\n        }\n    }\n    if (children === 0) {\n        minCoins[node] = 1;\n    } else {\n        let sum = 0;\n        for (let i = 0; i < adj[node].length; i++) {\n            if (adj[node][i] !== parent) {\n                sum += minCoins[adj[node][i]];\n            }\n        }\n        minCoins[node] = sum + (sum === 0 ? 1 : 0);\n    }\n}\n\nlet T = parseInt(lines[idx++]);\nwhile (T-- > 0) {\n    N = parseInt(lines[idx++]);\n    minCoins = new Array(N + 1).fill(0);\n    adj = new Array(N + 1).fill(0).map(() => []);\n    visited = new Array(N + 1).fill(false);\n    for (let i = 1; i < N; i++) {\n        let [u, v] = lines[idx++].split(' ').map(Number);\n        adj[u].push(v);\n        adj[v].push(u);\n    }\n    dfs(1, -1);\n    console.log(minCoins[1]);\n}"
    }
  }
}