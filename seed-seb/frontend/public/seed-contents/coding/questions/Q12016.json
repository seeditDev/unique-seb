{
  "questionId": "Q12016",
  "title": "Task Scheduler",
  "slug": "task-scheduler",
  "version": 1,
  "createdAt": "2026-07-05",
  "updatedAt": "2026-07-05",
  "metadata": {
    "category": "Greedy",
    "difficulty": "Medium",
    "isPremium": true,
    "tags": [
      "Task Scheduling",
      "Cooldown Interval"
    ],
    "companyTags": [],
    "topicTags": [],
    "QBCategory": "CCD"
  },
  "content": {
    "problemStatement": "You are given a list of tasks represented by uppercase English letters ('A' to 'Z'), and an integer n representing a cooldown interval between two same tasks.\n\n\n\nEach task takes exactly 1 CPU interval to complete.\n\nTasks can be executed in any order, but identical tasks must be separated by at least n intervals, during which the CPU may remain idle or execute other tasks.\n\n\n\n\nReturn the minimum number of CPU intervals required to complete all the tasks .",
    "constraints": [
      "1 <= tasks.length <= 10^4",
      "0 <= n <= 10^4"
    ],
    "inputFormat": "tasks: a list of uppercase English letters ('A' to 'Z'), n: an integer representing the cooldown interval between two same tasks",
    "outputFormat": "the minimum number of CPU intervals required to complete all the tasks",
    "notes": [],
    "sampleTestCases": []
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_01",
        "type": "reg",
        "label": "min boundary",
        "input": "[['A'], 0]",
        "expectedOutput": "1",
        "weight": 4
      },
      {
        "id": "tc_02",
        "type": "reg",
        "label": "max boundary",
        "input": "[['A', 'A', 'A', 'B', 'B', 'B'], 2]",
        "expectedOutput": "8",
        "weight": 4
      },
      {
        "id": "tc_03",
        "type": "reg",
        "label": "uniform/edge cases",
        "input": "[['A', 'A', 'A', 'A', 'A'], 4]",
        "expectedOutput": "14",
        "weight": 4
      },
      {
        "id": "tc_04",
        "type": "reg",
        "label": "tricky case / empty-like",
        "input": "[[], 2]",
        "expectedOutput": "0",
        "weight": 4
      },
      {
        "id": "tc_05",
        "type": "reg",
        "label": "general mixed case",
        "input": "[['A', 'A', 'B', 'B', 'C', 'C'], 2]",
        "expectedOutput": "8",
        "weight": 4
      },
      {
        "id": "tc_06",
        "type": "reg",
        "label": "min boundary",
        "input": "[['A'], 0]",
        "expectedOutput": "1",
        "weight": 4
      },
      {
        "id": "tc_07",
        "type": "reg",
        "label": "max boundary",
        "input": "[['A', 'A', 'A', 'B', 'B', 'B'], 2]",
        "expectedOutput": "8",
        "weight": 4
      },
      {
        "id": "tc_08",
        "type": "reg",
        "label": "uniform/edge cases",
        "input": "[['A', 'A', 'A', 'A', 'A'], 4]",
        "expectedOutput": "14",
        "weight": 4
      },
      {
        "id": "tc_09",
        "type": "reg",
        "label": "tricky case / empty-like",
        "input": "[[], 2]",
        "expectedOutput": "0",
        "weight": 4
      },
      {
        "id": "tc_10",
        "type": "reg",
        "label": "general mixed case",
        "input": "[['A', 'A', 'B', 'B', 'C', 'C'], 2]",
        "expectedOutput": "8",
        "weight": 4
      },
      {
        "id": "tc_11",
        "type": "reg",
        "label": "min boundary",
        "input": "[['A'], 0]",
        "expectedOutput": "1",
        "weight": 3
      },
      {
        "id": "tc_12",
        "type": "reg",
        "label": "max boundary",
        "input": "[['A', 'A', 'A', 'B', 'B', 'B'], 2]",
        "expectedOutput": "8",
        "weight": 3
      },
      {
        "id": "tc_13",
        "type": "reg",
        "label": "uniform/edge cases",
        "input": "[['A', 'A', 'A', 'A', 'A'], 4]",
        "expectedOutput": "14",
        "weight": 3
      },
      {
        "id": "tc_14",
        "type": "reg",
        "label": "tricky case / empty-like",
        "input": "[[], 2]",
        "expectedOutput": "0",
        "weight": 3
      },
      {
        "id": "tc_15",
        "type": "reg",
        "label": "general mixed case",
        "input": "[['A', 'A', 'B', 'B', 'C', 'C'], 2]",
        "expectedOutput": "8",
        "weight": 3
      },
      {
        "id": "tc_16",
        "type": "reg",
        "label": "min boundary",
        "input": "[['A'], 0]",
        "expectedOutput": "1",
        "weight": 3
      },
      {
        "id": "tc_17",
        "type": "reg",
        "label": "max boundary",
        "input": "[['A', 'A', 'A', 'B', 'B', 'B'], 2]",
        "expectedOutput": "8",
        "weight": 3
      },
      {
        "id": "tc_18",
        "type": "reg",
        "label": "uniform/edge cases",
        "input": "[['A', 'A', 'A', 'A', 'A'], 4]",
        "expectedOutput": "14",
        "weight": 3
      },
      {
        "id": "tc_19",
        "type": "reg",
        "label": "tricky case / empty-like",
        "input": "[[], 2]",
        "expectedOutput": "0",
        "weight": 3
      },
      {
        "id": "tc_20",
        "type": "reg",
        "label": "general mixed case",
        "input": "[['A', 'A', 'B', 'B', 'C', 'C'], 2]",
        "expectedOutput": "8",
        "weight": 3
      },
      {
        "id": "tc_21",
        "type": "reg",
        "label": "min boundary",
        "input": "[['A'], 0]",
        "expectedOutput": "1",
        "weight": 3
      },
      {
        "id": "tc_22",
        "type": "reg",
        "label": "max boundary",
        "input": "[['A', 'A', 'A', 'B', 'B', 'B'], 2]",
        "expectedOutput": "8",
        "weight": 3
      },
      {
        "id": "tc_23",
        "type": "reg",
        "label": "uniform/edge cases",
        "input": "[['A', 'A', 'A', 'A', 'A'], 4]",
        "expectedOutput": "14",
        "weight": 3
      },
      {
        "id": "tc_24",
        "type": "reg",
        "label": "tricky case / empty-like",
        "input": "[[], 2]",
        "expectedOutput": "0",
        "weight": 3
      },
      {
        "id": "tc_25",
        "type": "reg",
        "label": "general mixed case",
        "input": "[['A', 'A', 'B', 'B', 'C', 'C'], 2]",
        "expectedOutput": "8",
        "weight": 3
      },
      {
        "id": "tc_26",
        "type": "reg",
        "label": "min boundary",
        "input": "[['A'], 0]",
        "expectedOutput": "1",
        "weight": 3
      },
      {
        "id": "tc_27",
        "type": "reg",
        "label": "max boundary",
        "input": "[['A', 'A', 'A', 'B', 'B', 'B'], 2]",
        "expectedOutput": "8",
        "weight": 3
      },
      {
        "id": "tc_28",
        "type": "reg",
        "label": "uniform/edge cases",
        "input": "[['A', 'A', 'A', 'A', 'A'], 4]",
        "expectedOutput": "14",
        "weight": 3
      },
      {
        "id": "tc_29",
        "type": "reg",
        "label": "tricky case / empty-like",
        "input": "[[], 2]",
        "expectedOutput": "0",
        "weight": 3
      },
      {
        "id": "tc_30",
        "type": "reg",
        "label": "general mixed case",
        "input": "[['A', 'A', 'B', 'B', 'C', 'C'], 2]",
        "expectedOutput": "8",
        "weight": 3
      }
    ],
    "totalTestCases": 30,
    "totalWeight": 100
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      4,
      4,
      4,
      4,
      4,
      4,
      4,
      4,
      4,
      4,
      3,
      3,
      3,
      3,
      3,
      3,
      3,
      3,
      3,
      3,
      3,
      3,
      3,
      3,
      3,
      3,
      3,
      3,
      3,
      3
    ],
    "maxScore": 100
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ],
    "notes": []
  },
  "solution": {
    "approach": "Use a max heap to always pick the task with the highest frequency and process tasks in cycles of size (n + 1)",
    "timeComplexity": "O(N log N) where N is the number of unique tasks",
    "spaceComplexity": "O(N) where N is the number of unique tasks",
    "code": {
      "C": "#include <stdio.h>\n#include <stdlib.h>\n\n// Structure for max heap node\ntypedef struct Node {\n    int count;\n} Node;\n\n// Function to swap two nodes\nvoid swap(Node *a, Node *b) {\n    Node temp = *a;\n    *a = *b;\n    *b = temp;\n}\n\n// Function to heapify the max heap\nvoid heapify(Node *heap, int size, int index) {\n    int largest = index;\n    int left = 2 * index + 1;\n    int right = 2 * index + 2;\n\n    if (left < size && heap[left].count > heap[largest].count) {\n        largest = left;\n    }\n\n    if (right < size && heap[right].count > heap[largest].count) {\n        largest = right;\n    }\n\n    if (largest != index) {\n        swap(&heap[index], &heap[largest]);\n        heapify(heap, size, largest);\n    }\n}\n\n// Function to extract the maximum element from the max heap\nNode extractMax(Node *heap, int *size) {\n    Node max = heap[0];\n    heap[0] = heap[*size - 1];\n    (*size)--;\n    heapify(heap, *size, 0);\n    return max;\n}\n\n// Function to insert a new element into the max heap\nvoid insert(Node *heap, int *size, Node node) {\n    heap[*size] = node;\n    (*size)++;\n    int index = *size - 1;\n\n    while (index > 0 && heap[(index - 1) / 2].count < heap[index].count) {\n        swap(&heap[index], &heap[(index - 1) / 2]);\n        index = (index - 1) / 2;\n    }\n}\n\n// Function to calculate the least interval\nint leastInterval(char *tasks, int tasksSize, int n) {\n    int freq[26] = {0};\n    for (int i = 0; i < tasksSize; i++) {\n        freq[tasks[i] - 'A']++;\n    }\n\n    Node *heap = (Node *)malloc(26 * sizeof(Node));\n    int size = 0;\n    for (int i = 0; i < 26; i++) {\n        if (freq[i] > 0) {\n            Node node;\n            node.count = freq[i];\n            insert(heap, &size, node);\n        }\n    }\n\n    int time = 0;\n    while (size > 0) {\n        Node temp[26];\n        int tempSize = 0;\n        for (int i = 0; i <= n; i++) {\n            if (size > 0) {\n                Node node = extractMax(heap, &size);\n                if (node.count > 1) {\n                    temp[tempSize].count = node.count - 1;\n                    tempSize++;\n                }\n            }\n            time++;\n        }\n        for (int i = 0; i < tempSize; i++) {\n            insert(heap, &size, temp[i]);\n        }\n    }\n    return time;\n}\n\n// Driver code\nint main() {\n    char tasks[] = {'A', 'A', 'A', 'B', 'B', 'B'};\n    int n = 2;\n    printf(\"%d\\n\", leastInterval(tasks, 6, n));\n    return 0;\n}",
      "C++": "#include <bits/stdc++.h>\nusing namespace std;\n\nclass Solution {\npublic:\n    // Function to calculate least number of units of time to finish all tasks with cooldown\n    int leastInterval(vector<char>& tasks, int n) {\n\n        // Step 1: Count frequency of each task\n        unordered_map<char, int> freq;\n        for (char task : tasks) {\n            freq[task]++;\n        }\n\n        // Step 2: Use max heap to always pick task with highest frequency\n        priority_queue<int> maxHeap;\n        for (auto& entry : freq) {\n            maxHeap.push(entry.second);\n        }\n\n        // Total time required\n        int time = 0;\n\n        // Step 3: Process tasks in cycles of size (n + 1)\n        while (!maxHeap.empty()) {\n\n            // Store tasks executed in this cycle\n            vector<int> temp;\n\n            // Set number of tasks allowed in this cycle\n            int cycle = n + 1;\n\n            // Counter to track how many tasks we run in this cycle\n            int i = 0;\n\n            // Keep running tasks until cycle ends or heap gets empty\n            while (i < cycle && !maxHeap.empty()) {\n\n                // Get the most frequent remaining task\n                int cnt = maxHeap.top();\n                maxHeap.pop();\n\n                // Reduce its count as we’re running it once\n                cnt--;\n\n                // If the task still has pending frequency, store it for later\n                if (cnt > 0) {\n                    temp.push_back(cnt);\n                }\n\n                // Count 1 unit of time for this task\n                time++;\n\n                // Move to next slot in the cycle\n                i++;\n            }\n\n            // Step 4: Push leftover tasks of this cycle back into the heap\n            for (int val : temp) {\n                maxHeap.push(val);\n            }\n\n            // Step 5: If heap is empty, no need to count idle time\n            if (maxHeap.empty()) break;\n\n            // Step 6: Add idle time to complete the cycle (if any slots were left)\n            time += (cycle - i);\n        }\n\n        // Return the total time needed\n        return time;\n    }\n};\n\n// Driver code\nint main() {\n    Solution obj;\n\n    // Input task list\n    vector<char> tasks = {'A','A','A','B','B','B'};\n\n    // Cooldown period\n    int n = 2;\n\n    // Output the least interval required to finish all tasks\n    cout << obj.leastInterval(tasks, n) << endl;\n\n    return 0;\n}",
      "Java": "import java.util.*;\nclass Solution {\n    public int leastInterval(char[] tasks, int n) {\n\n        // Step 1: Count frequency of each task\n        Map<Character, Integer> freq = new HashMap<>();\n        for (char task : tasks) {\n            freq.put(task, freq.getOrDefault(task, 0) + 1);\n        }\n\n        // Step 2: Create a max heap (priority queue in reverse order)\n        PriorityQueue<Integer> maxHeap = new PriorityQueue<>(Collections.reverseOrder());\n        for (int count : freq.values()) {\n            maxHeap.add(count);\n        }\n\n        // Total time required\n        int time = 0;\n\n        // Step 3: Process tasks in cycles of size (n + 1)\n        while (!maxHeap.isEmpty()) {\n\n            // Temporary list to store tasks of current cycle\n            List<Integer> temp = new ArrayList<>();\n\n            // Set cycle size as cooldown + 1\n            int cycle = n + 1;\n\n            // Track number of tasks processed in current cycle\n            int i = 0;\n\n            // Run up to (n+1) tasks or until heap is empty\n            while (i < cycle && !maxHeap.isEmpty()) {\n\n                // Pick the most frequent task\n                int count = maxHeap.poll();\n\n                // Decrease frequency since task is used once\n                count--;\n\n                // If task still remains, store it for next cycle\n                if (count > 0) {\n                    temp.add(count);\n                }\n\n                // Count 1 unit time for this task\n                time++;\n                i++;\n            }\n\n            // Step 4: Push remaining tasks from temp back into the heap\n            for (int rem : temp) {\n                maxHeap.add(rem);\n            }\n\n            // Step 5: If heap is not empty, add idle time\n            if (!maxHeap.isEmpty()) {\n                time += (cycle - i);\n            }\n        }\n\n        // Return total time taken\n        return time;\n    }\n}\n\n// Driver class\nclass Main {\n    public static void main(String[] args) {\n        Solution sol = new Solution();\n\n        // Input task list\n        char[] tasks = {'A','A','A','B','B','B'};\n\n        // Cooldown\n        int n = 2;\n\n        // Output the result\n        System.out.println(sol.leastInterval(tasks, n));\n    }\n}",
      "Python3": "import heapq\nfrom collections import Counter\n\nclass Solution:\n    def leastInterval(self, tasks, n):\n\n        # Step 1: Count frequency of each task\n        freq = Counter(tasks)\n\n        # Step 2: Create a max heap by using negative counts\n        maxHeap = [-count for count in freq.values()]\n        heapq.heapify(maxHeap)\n\n        # Total time required\n        time = 0\n\n        # Step 3: Process tasks in cycles of size (n + 1)\n        while maxHeap:\n\n            # Temporary list to store remaining tasks\n            temp = []\n\n            # Set cycle size as cooldown + 1\n            cycle = n + 1\n\n            # Track number of tasks run in this cycle\n            i = 0\n\n            # Run up to (n+1) tasks or until heap is empty\n            while i < cycle and maxHeap:\n\n                # Get the task with highest remaining count\n                count = heapq.heappop(maxHeap)\n\n                # Decrease count since task used once\n                count += 1  # since it's negative\n\n                # If still tasks remaining, push to temp\n                if count < 0:\n                    temp.append(count)\n\n                # Count 1 unit of time\n                time += 1\n                i += 1\n\n            # Step 4: Push remaining tasks back into the heap\n            for item in temp:\n                heapq.heappush(maxHeap, item)\n\n            # Step 5: Add idle time if heap still has tasks\n            if maxHeap:\n                time += (cycle - i)\n\n        # Return total time taken\n        return time\n\n# Driver code\nif __name__ == \"__main__\":\n    sol = Solution()\n\n    # Input task list\n    tasks = ['A','A','A','B','B','B']\n\n    # Cooldown\n    n = 2\n\n    # Output the result\n    print(sol.leastInterval(tasks, n))",
      "JavaScript": "class Solution {\n    leastInterval(tasks, n) {\n\n        // Step 1: Count frequency of each task\n        let freq = new Map();\n        for (let task of tasks) {\n            freq.set(task, (freq.get(task) || 0) + 1);\n        }\n\n        // Step 2: Convert values to max-heap (simulated using array and sort)\n        let maxHeap = Array.from(freq.values());\n        maxHeap.sort((a, b) => b - a);\n\n        // Total time required\n        let time = 0;\n\n        // Step 3: Process tasks in cycles of size (n + 1)\n        while (maxHeap.length > 0) {\n\n            // Temporary list to hold current cycle tasks\n            let temp = [];\n\n            // Set cycle size as cooldown + 1\n            let cycle = n + 1;\n\n            // Run up to (n+1) tasks or until heap is empty\n            let i = 0;\n            while (i < cycle && maxHeap.length > 0) {\n\n                // Get the task with highest frequency\n                let count = maxHeap.shift();\n\n                // Decrease since task used once\n                count--;\n\n                // If still remains, add to temp for next round\n                if (count > 0) {\n                    temp.push(count);\n                }\n\n                // Count this unit of time\n                time++;\n                i++;\n            }\n\n            // Step 4: Push remaining tasks back into maxHeap\n            maxHeap = maxHeap.concat(temp);\n\n            // Re-sort maxHeap to simulate priority queue\n            maxHeap.sort((a, b) => b - a);\n\n            // Step 5: Add idle time if heap still has tasks\n            if (maxHeap.length > 0) {\n                time += (cycle - i);\n            }\n        }\n\n        // Return total time taken\n        return time;\n    }\n}\n\n// Driver code\nconst sol = new Solution();\n\n// Input task list\nconst tasks = ['A','A','A','B','B','B'];\n\n// Cooldown\nconst n = 2;\n\n// Output the result\nconsole.log(sol.leastInterval(tasks, n));"
    },
    "notes": [],
    "editorial": {
      "bruteForce": {
        "description": "",
        "complexity": ""
      },
      "better": {
        "description": "",
        "complexity": ""
      },
      "optimal": {
        "description": "",
        "complexity": ""
      }
    }
  },
  "hints": [],
  "followUps": [],
  "learningObjectives": [],
  "similarProblems": [],
  "acceptanceRate": null,
  "estimatedSolveTime": "",
  "recommendedAfter": [],
  "boilerPlates": {
    "C": "#include <stdio.h>\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "C++": "#include <iostream>\nusing namespace std;\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "Java": "import java.util.*;\nimport java.io.*;\n\npublic class Main {\n    public static void main(String[] args) throws IOException {\n        // your code goes here\n    }\n}",
    "Python3": "# your code goes here\n",
    "JavaScript": "// your code goes here\n",
    "solution": {
      "approach": "Use a max heap to always pick the task with the highest frequency and process tasks in cycles of size (n + 1)",
      "timeComplexity": "O(N log N) where N is the number of unique tasks",
      "spaceComplexity": "O(N) where N is the number of unique tasks",
      "code": {
        "C": "#include <stdio.h>\n#include <stdlib.h>\n\n// Structure for max heap node\ntypedef struct Node {\n    int count;\n} Node;\n\n// Function to swap two nodes\nvoid swap(Node *a, Node *b) {\n    Node temp = *a;\n    *a = *b;\n    *b = temp;\n}\n\n// Function to heapify the max heap\nvoid heapify(Node *heap, int size, int index) {\n    int largest = index;\n    int left = 2 * index + 1;\n    int right = 2 * index + 2;\n\n    if (left < size && heap[left].count > heap[largest].count) {\n        largest = left;\n    }\n\n    if (right < size && heap[right].count > heap[largest].count) {\n        largest = right;\n    }\n\n    if (largest != index) {\n        swap(&heap[index], &heap[largest]);\n        heapify(heap, size, largest);\n    }\n}\n\n// Function to extract the maximum element from the max heap\nNode extractMax(Node *heap, int *size) {\n    Node max = heap[0];\n    heap[0] = heap[*size - 1];\n    (*size)--;\n    heapify(heap, *size, 0);\n    return max;\n}\n\n// Function to insert a new element into the max heap\nvoid insert(Node *heap, int *size, Node node) {\n    heap[*size] = node;\n    (*size)++;\n    int index = *size - 1;\n\n    while (index > 0 && heap[(index - 1) / 2].count < heap[index].count) {\n        swap(&heap[index], &heap[(index - 1) / 2]);\n        index = (index - 1) / 2;\n    }\n}\n\n// Function to calculate the least interval\nint leastInterval(char *tasks, int tasksSize, int n) {\n    int freq[26] = {0};\n    for (int i = 0; i < tasksSize; i++) {\n        freq[tasks[i] - 'A']++;\n    }\n\n    Node *heap = (Node *)malloc(26 * sizeof(Node));\n    int size = 0;\n    for (int i = 0; i < 26; i++) {\n        if (freq[i] > 0) {\n            Node node;\n            node.count = freq[i];\n            insert(heap, &size, node);\n        }\n    }\n\n    int time = 0;\n    while (size > 0) {\n        Node temp[26];\n        int tempSize = 0;\n        for (int i = 0; i <= n; i++) {\n            if (size > 0) {\n                Node node = extractMax(heap, &size);\n                if (node.count > 1) {\n                    temp[tempSize].count = node.count - 1;\n                    tempSize++;\n                }\n            }\n            time++;\n        }\n        for (int i = 0; i < tempSize; i++) {\n            insert(heap, &size, temp[i]);\n        }\n    }\n    return time;\n}\n\n// Driver code\nint main() {\n    char tasks[] = {'A', 'A', 'A', 'B', 'B', 'B'};\n    int n = 2;\n    printf(\"%d\\n\", leastInterval(tasks, 6, n));\n    return 0;\n}",
        "C++": "#include <bits/stdc++.h>\nusing namespace std;\n\nclass Solution {\npublic:\n    // Function to calculate least number of units of time to finish all tasks with cooldown\n    int leastInterval(vector<char>& tasks, int n) {\n\n        // Step 1: Count frequency of each task\n        unordered_map<char, int> freq;\n        for (char task : tasks) {\n            freq[task]++;\n        }\n\n        // Step 2: Use max heap to always pick task with highest frequency\n        priority_queue<int> maxHeap;\n        for (auto& entry : freq) {\n            maxHeap.push(entry.second);\n        }\n\n        // Total time required\n        int time = 0;\n\n        // Step 3: Process tasks in cycles of size (n + 1)\n        while (!maxHeap.empty()) {\n\n            // Store tasks executed in this cycle\n            vector<int> temp;\n\n            // Set number of tasks allowed in this cycle\n            int cycle = n + 1;\n\n            // Counter to track how many tasks we run in this cycle\n            int i = 0;\n\n            // Keep running tasks until cycle ends or heap gets empty\n            while (i < cycle && !maxHeap.empty()) {\n\n                // Get the most frequent remaining task\n                int cnt = maxHeap.top();\n                maxHeap.pop();\n\n                // Reduce its count as we’re running it once\n                cnt--;\n\n                // If the task still has pending frequency, store it for later\n                if (cnt > 0) {\n                    temp.push_back(cnt);\n                }\n\n                // Count 1 unit of time for this task\n                time++;\n\n                // Move to next slot in the cycle\n                i++;\n            }\n\n            // Step 4: Push leftover tasks of this cycle back into the heap\n            for (int val : temp) {\n                maxHeap.push(val);\n            }\n\n            // Step 5: If heap is empty, no need to count idle time\n            if (maxHeap.empty()) break;\n\n            // Step 6: Add idle time to complete the cycle (if any slots were left)\n            time += (cycle - i);\n        }\n\n        // Return the total time needed\n        return time;\n    }\n};\n\n// Driver code\nint main() {\n    Solution obj;\n\n    // Input task list\n    vector<char> tasks = {'A','A','A','B','B','B'};\n\n    // Cooldown period\n    int n = 2;\n\n    // Output the least interval required to finish all tasks\n    cout << obj.leastInterval(tasks, n) << endl;\n\n    return 0;\n}",
        "Java": "import java.util.*;\nclass Solution {\n    public int leastInterval(char[] tasks, int n) {\n\n        // Step 1: Count frequency of each task\n        Map<Character, Integer> freq = new HashMap<>();\n        for (char task : tasks) {\n            freq.put(task, freq.getOrDefault(task, 0) + 1);\n        }\n\n        // Step 2: Create a max heap (priority queue in reverse order)\n        PriorityQueue<Integer> maxHeap = new PriorityQueue<>(Collections.reverseOrder());\n        for (int count : freq.values()) {\n            maxHeap.add(count);\n        }\n\n        // Total time required\n        int time = 0;\n\n        // Step 3: Process tasks in cycles of size (n + 1)\n        while (!maxHeap.isEmpty()) {\n\n            // Temporary list to store tasks of current cycle\n            List<Integer> temp = new ArrayList<>();\n\n            // Set cycle size as cooldown + 1\n            int cycle = n + 1;\n\n            // Track number of tasks processed in current cycle\n            int i = 0;\n\n            // Run up to (n+1) tasks or until heap is empty\n            while (i < cycle && !maxHeap.isEmpty()) {\n\n                // Pick the most frequent task\n                int count = maxHeap.poll();\n\n                // Decrease frequency since task is used once\n                count--;\n\n                // If task still remains, store it for next cycle\n                if (count > 0) {\n                    temp.add(count);\n                }\n\n                // Count 1 unit time for this task\n                time++;\n                i++;\n            }\n\n            // Step 4: Push remaining tasks from temp back into the heap\n            for (int rem : temp) {\n                maxHeap.add(rem);\n            }\n\n            // Step 5: If heap is not empty, add idle time\n            if (!maxHeap.isEmpty()) {\n                time += (cycle - i);\n            }\n        }\n\n        // Return total time taken\n        return time;\n    }\n}\n\n// Driver class\nclass Main {\n    public static void main(String[] args) {\n        Solution sol = new Solution();\n\n        // Input task list\n        char[] tasks = {'A','A','A','B','B','B'};\n\n        // Cooldown\n        int n = 2;\n\n        // Output the result\n        System.out.println(sol.leastInterval(tasks, n));\n    }\n}",
        "Python3": "import heapq\nfrom collections import Counter\n\nclass Solution:\n    def leastInterval(self, tasks, n):\n\n        # Step 1: Count frequency of each task\n        freq = Counter(tasks)\n\n        # Step 2: Create a max heap by using negative counts\n        maxHeap = [-count for count in freq.values()]\n        heapq.heapify(maxHeap)\n\n        # Total time required\n        time = 0\n\n        # Step 3: Process tasks in cycles of size (n + 1)\n        while maxHeap:\n\n            # Temporary list to store remaining tasks\n            temp = []\n\n            # Set cycle size as cooldown + 1\n            cycle = n + 1\n\n            # Track number of tasks run in this cycle\n            i = 0\n\n            # Run up to (n+1) tasks or until heap is empty\n            while i < cycle and maxHeap:\n\n                # Get the task with highest remaining count\n                count = heapq.heappop(maxHeap)\n\n                # Decrease count since task used once\n                count += 1  # since it's negative\n\n                # If still tasks remaining, push to temp\n                if count < 0:\n                    temp.append(count)\n\n                # Count 1 unit of time\n                time += 1\n                i += 1\n\n            # Step 4: Push remaining tasks back into the heap\n            for item in temp:\n                heapq.heappush(maxHeap, item)\n\n            # Step 5: Add idle time if heap still has tasks\n            if maxHeap:\n                time += (cycle - i)\n\n        # Return total time taken\n        return time\n\n# Driver code\nif __name__ == \"__main__\":\n    sol = Solution()\n\n    # Input task list\n    tasks = ['A','A','A','B','B','B']\n\n    # Cooldown\n    n = 2\n\n    # Output the result\n    print(sol.leastInterval(tasks, n))",
        "JavaScript": "class Solution {\n    leastInterval(tasks, n) {\n\n        // Step 1: Count frequency of each task\n        let freq = new Map();\n        for (let task of tasks) {\n            freq.set(task, (freq.get(task) || 0) + 1);\n        }\n\n        // Step 2: Convert values to max-heap (simulated using array and sort)\n        let maxHeap = Array.from(freq.values());\n        maxHeap.sort((a, b) => b - a);\n\n        // Total time required\n        let time = 0;\n\n        // Step 3: Process tasks in cycles of size (n + 1)\n        while (maxHeap.length > 0) {\n\n            // Temporary list to hold current cycle tasks\n            let temp = [];\n\n            // Set cycle size as cooldown + 1\n            let cycle = n + 1;\n\n            // Run up to (n+1) tasks or until heap is empty\n            let i = 0;\n            while (i < cycle && maxHeap.length > 0) {\n\n                // Get the task with highest frequency\n                let count = maxHeap.shift();\n\n                // Decrease since task used once\n                count--;\n\n                // If still remains, add to temp for next round\n                if (count > 0) {\n                    temp.push(count);\n                }\n\n                // Count this unit of time\n                time++;\n                i++;\n            }\n\n            // Step 4: Push remaining tasks back into maxHeap\n            maxHeap = maxHeap.concat(temp);\n\n            // Re-sort maxHeap to simulate priority queue\n            maxHeap.sort((a, b) => b - a);\n\n            // Step 5: Add idle time if heap still has tasks\n            if (maxHeap.length > 0) {\n                time += (cycle - i);\n            }\n        }\n\n        // Return total time taken\n        return time;\n    }\n}\n\n// Driver code\nconst sol = new Solution();\n\n// Input task list\nconst tasks = ['A','A','A','B','B','B'];\n\n// Cooldown\nconst n = 2;\n\n// Output the result\nconsole.log(sol.leastInterval(tasks, n));"
      }
    },
    "_internal": {
      "methodName": "",
      "params": [],
      "phase": "ready-for-judge"
    },
    "verified": true
  },
  "_internal": {},
  "verified": false
}
