{
  "questionId": "Q3269",
  "title": "Chef And Fibonacci Tree",
  "slug": "chef-and-fibonacci-tree",
  "version": 1,
  "createdAt": "2026-07-01",
  "updatedAt": "2026-07-04",
  "metadata": {
    "category": "Dynamic Programming",
    "difficulty": "Hard",
    "isPremium": false,
    "tags": [
      "Tree Traversal",
      "Modular Arithmetic",
      "Dynamic Programming",
      "Fibonacci Series"
    ],
    "companyTags": [],
    "topicTags": [
      "Trees"
    ]
  },
  "content": {
    "problemStatement": "Chef likes to play with trees a lot. This time, he has a rooted tree **T** consisting of **N** nodes labelled uniquely with the integers from **{1, \u2026 N}**. The node labelled **1** is the root of the tree. Each node in the given tree **T** stores a non-negative integer, initially all of which are zeros. Chef wants to perform **2** types of operations over the tree accurately and efficiently.\n\n\n**U X K:** Update the subtree rooted at node **X** such that the the root **X** will have **FK** (the **Kth** Fibonacci number) added to it, all its children will have **Fk+1** (the **(K+1)th** Fibonacci number) added to them, and so on. More formally, any node in the subtree rooted at **X** will have **(K+D)th** fibonacci number added to them, where **D** is the distance of the node from **X**.\n**Q X:** Report the value stored at the node **X**.\nSince the answer to each query can be very large, output it modulo **109 + 7**.\n\n**NOTE:**\n\n**FK** denotes the **Kth** Fibonacci number, and the series is generated as follows:\n\n\n**FK = 1, if K \u2264 2 **.\n**FK = FK-1 + FK-2, otherwise.**\n\nTo know more about Fibonacci numbers, click here.\n\n\nInput:First line of input contains two integer **N** and **M** denoting the number of nodes in the tree **T**, and the number of queries to be processed, respectively. Next **N-1** lines of input contain an integer each, where the integer in the **ith** line denotes the parent of the **(i+1)th** node. Each of the next **M** lines contains one of the **2** types of queries mentioned above.\n\nOutput:For each operation of type **Q**, output the required answer modulo **109 + 7**.",
    "constraints": [
      "1 <= N, M, K <= 10^5",
      "1 <= U, V, X <= N"
    ],
    "inputFormat": "The first line contains two integers N and M, denoting the number of nodes in the tree and the number of queries to be processed, respectively. The next N-1 lines contain an integer each, where the integer in the ith line denotes the parent of the (i+1)th node. Each of the next M lines contains one of the two types of queries mentioned above.",
    "outputFormat": "For each operation of type Q, output the required answer modulo 10^9 + 7.",
    "sampleTestCases": [
      {
        "id": "sample_1",
        "input": "5 10\n1\n1\n3\n3\nQ 3\nU 1 2\nQ 2\nQ 3\nQ 4\nQ 5\nU 3 1\nQ 3\nQ 4\nQ 5",
        "output": "0\n2\n2\n3\n3\n3\n4\n4",
        "explanation": "The tree is constructed as follows: Node 1 is the root, Node 2 is the left child of Node 1, Node 3 is the right child of Node 1, Node 4 is the left child of Node 3, and Node 5 is the right child of Node 3. The queries are processed in the given order."
      }
    ]
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_1",
        "label": "minimum boundary",
        "input": "1 1\nQ 1",
        "expectedOutput": "0",
        "weight": 2
      },
      {
        "id": "tc_2",
        "label": "maximum boundary",
        "input": "100000 100000\n",
        "expectedOutput": "",
        "weight": 2
      },
      {
        "id": "tc_3",
        "label": "edge case",
        "input": "5 5\n1\n1\n3\n3\nQ 3\nU 1 2\nQ 2\nQ 3\nQ 4\nQ 5",
        "expectedOutput": "0\n2\n2\n3\n3",
        "weight": 2
      },
      {
        "id": "tc_4",
        "label": "general case 1",
        "input": "10 10\n1\n1\n3\n3\n5\n5\n7\n7\nQ 3\nU 1 2\nQ 2\nQ 3\nQ 4\nQ 5",
        "expectedOutput": "0\n2\n2\n3\n3",
        "weight": 2
      },
      {
        "id": "tc_5",
        "label": "general case 2",
        "input": "15 15\n1\n1\n3\n3\n5\n5\n7\n7\n9\n9\nQ 3\nU 1 2\nQ 2\nQ 3\nQ 4\nQ 5",
        "expectedOutput": "0\n2\n2\n3\n3",
        "weight": 2
      }
    ],
    "totalTestCases": 5,
    "totalWeight": 10
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      2,
      2,
      2,
      2,
      2
    ],
    "maxScore": 10
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ]
  },
  "solution": {
    "approach": "To solve this problem, we can use a combination of tree traversal and dynamic programming. We will first build the tree using the given parent-child relationships. Then, we will process each query. For update queries, we will traverse the subtree rooted at the given node and update the values of the nodes according to the Fibonacci series. For query operations, we will simply return the value stored at the given node modulo 10^9 + 7.",
    "timeComplexity": "O(N + M * log N) \u2014 The time complexity is dominated by the tree traversal and the Fibonacci series calculation.",
    "spaceComplexity": "O(N) \u2014 The space complexity is dominated by the storage of the tree and the Fibonacci series.",
    "code": {
      "C": "#include <stdio.h>\n#include <stdlib.h>\n#define MOD 1000000007\n#define MAXN 100005\n\n// Define the structure for a tree node\ntypedef struct Node {\n    int value;\n    int parent;\n    struct Node** children;\n    int numChildren;\n} Node;\n\n// Function to create a new tree node\nNode* createNode(int value, int parent) {\n    Node* node = (Node*)malloc(sizeof(Node));\n    node->value = value;\n    node->parent = parent;\n    node->children = NULL;\n    node->numChildren = 0;\n    return node;\n}\n\n// Function to add a child to a node\nvoid addChild(Node* node, Node* child) {\n    node->numChildren++;\n    node->children = (Node**)realloc(node->children, node->numChildren * sizeof(Node*));\n    node->children[node->numChildren - 1] = child;\n}\n\n// Function to update the subtree rooted at a node\nvoid updateSubtree(Node* node, int k) {\n    node->value = (node->value + fib(k)) % MOD;\n    for (int i = 0; i < node->numChildren; i++) {\n        updateSubtree(node->children[i], k + 1);\n    }\n}\n\n// Function to calculate the kth Fibonacci number\nint fib(int k) {\n    if (k <= 2) return 1;\n    int a = 1, b = 1;\n    for (int i = 3; i <= k; i++) {\n        int temp = a;\n        a = b;\n        b = (temp + b) % MOD;\n    }\n    return b;\n}\n\nint main() {\n    int N, M;\n    scanf(\"%d %d\", &N, &M);\n    Node** nodes = (Node**)malloc(N * sizeof(Node*));\n    for (int i = 0; i < N; i++) {\n        nodes[i] = createNode(0, -1);\n    }\n    for (int i = 1; i < N; i++) {\n        int parent;\n        scanf(\"%d\", &parent);\n        nodes[i]->parent = parent - 1;\n        addChild(nodes[parent - 1], nodes[i]);\n    }\n    for (int i = 0; i < M; i++) {\n        char query[3];\n        scanf(\"%s\", query);\n        if (query[0] == 'Q') {\n            int node;\n            scanf(\"%d\", &node);\n            printf(\"%d\\n\", nodes[node - 1]->value);\n        } else {\n            int node, k;\n            scanf(\"%d %d\", &node, &k);\n            updateSubtree(nodes[node - 1], k);\n        }\n    }\n    return 0;\n}",
      "C++": "#include <iostream>\n#include <vector>\nusing namespace std;\n#define MOD 1000000007\n#define MAXN 100005\n\n// Define the structure for a tree node\nstruct Node {\n    int value;\n    int parent;\n    vector<Node*> children;\n};\n\n// Function to create a new tree node\nNode* createNode(int value, int parent) {\n    Node* node = new Node;\n    node->value = value;\n    node->parent = parent;\n    return node;\n}\n\n// Function to add a child to a node\nvoid addChild(Node* node, Node* child) {\n    node->children.push_back(child);\n}\n\n// Function to update the subtree rooted at a node\nvoid updateSubtree(Node* node, int k) {\n    node->value = (node->value + fib(k)) % MOD;\n    for (Node* child : node->children) {\n        updateSubtree(child, k + 1);\n    }\n}\n\n// Function to calculate the kth Fibonacci number\nint fib(int k) {\n    if (k <= 2) return 1;\n    int a = 1, b = 1;\n    for (int i = 3; i <= k; i++) {\n        int temp = a;\n        a = b;\n        b = (temp + b) % MOD;\n    }\n    return b;\n}\n\nint main() {\n    int N, M;\n    cin >> N >> M;\n    Node** nodes = new Node*[N];\n    for (int i = 0; i < N; i++) {\n        nodes[i] = createNode(0, -1);\n    }\n    for (int i = 1; i < N; i++) {\n        int parent;\n        cin >> parent;\n        nodes[i]->parent = parent - 1;\n        addChild(nodes[parent - 1], nodes[i]);\n    }\n    for (int i = 0; i < M; i++) {\n        char query[3];\n        cin >> query;\n        if (query[0] == 'Q') {\n            int node;\n            cin >> node;\n            cout << nodes[node - 1]->value << endl;\n        } else {\n            int node, k;\n            cin >> node >> k;\n            updateSubtree(nodes[node - 1], k);\n        }\n    }\n    return 0;\n}",
      "Java": "import java.util.ArrayList;\nimport java.util.Scanner;\n\npublic class Main {\n    static final int MOD = 1000000007;\n    static final int MAXN = 100005;\n\n    // Define the structure for a tree node\n    static class Node {\n        int value;\n        int parent;\n        ArrayList<Node> children;\n\n        public Node(int value, int parent) {\n            this.value = value;\n            this.parent = parent;\n            this.children = new ArrayList<>();\n        }\n    }\n\n    // Function to add a child to a node\n    static void addChild(Node node, Node child) {\n        node.children.add(child);\n    }\n\n    // Function to update the subtree rooted at a node\n    static void updateSubtree(Node node, int k) {\n        node.value = (node.value + fib(k)) % MOD;\n        for (Node child : node.children) {\n            updateSubtree(child, k + 1);\n        }\n    }\n\n    // Function to calculate the kth Fibonacci number\n    static int fib(int k) {\n        if (k <= 2) return 1;\n        int a = 1, b = 1;\n        for (int i = 3; i <= k; i++) {\n            int temp = a;\n            a = b;\n            b = (temp + b) % MOD;\n        }\n        return b;\n    }\n\n    public static void main(String[] args) {\n        Scanner scanner = new Scanner(System.in);\n        int N = scanner.nextInt();\n        int M = scanner.nextInt();\n        Node[] nodes = new Node[N];\n        for (int i = 0; i < N; i++) {\n            nodes[i] = new Node(0, -1);\n        }\n        for (int i = 1; i < N; i++) {\n            int parent = scanner.nextInt();\n            nodes[i].parent = parent - 1;\n            addChild(nodes[parent - 1], nodes[i]);\n        }\n        for (int i = 0; i < M; i++) {\n            String query = scanner.next();\n            if (query.equals(\"Q\")) {\n                int node = scanner.nextInt();\n                System.out.println(nodes[node - 1].value);\n            } else {\n                int node = scanner.nextInt();\n                int k = scanner.nextInt();\n                updateSubtree(nodes[node - 1], k);\n            }\n        }\n    }\n}",
      "Python3": "import sys\n\nMOD = 1000000007\nMAXN = 100005\n\n# Define the structure for a tree node\nclass Node:\n    def __init__(self, value, parent):\n        self.value = value\n        self.parent = parent\n        self.children = []\n\n# Function to add a child to a node\ndef add_child(node, child):\n    node.children.append(child)\n\n# Function to update the subtree rooted at a node\ndef update_subtree(node, k):\n    node.value = (node.value + fib(k)) % MOD\n    for child in node.children:\n        update_subtree(child, k + 1)\n\n# Function to calculate the kth Fibonacci number\ndef fib(k):\n    if k <= 2: return 1\n    a, b = 1, 1\n    for i in range(3, k + 1):\n        a, b = b, (a + b) % MOD\n    return b\n\n# Read input\nN, M = map(int, sys.stdin.readline().split())\nnodes = [Node(0, -1) for _ in range(N)]\nfor i in range(1, N):\n    parent = int(sys.stdin.readline())\n    nodes[i].parent = parent - 1\n    add_child(nodes[parent - 1], nodes[i])\nfor _ in range(M):\n    query = sys.stdin.readline().split()\n    if query[0] == 'Q':\n        node = int(query[1])\n        print(nodes[node - 1].value)\n    else:\n        node, k = map(int, query[1:])\n        update_subtree(nodes[node - 1], k)\n",
      "JavaScript": "const fs = require('fs');\nconst input = fs.readFileSync(0, 'utf-8').trim().split('\\n');\nlet idx = 0;\n\nconst MOD = 1000000007;\nconst MAXN = 100005;\n\n// Define the structure for a tree node\nclass Node {\n    constructor(value, parent) {\n        this.value = value;\n        this.parent = parent;\n        this.children = [];\n    }\n}\n\n// Function to add a child to a node\nfunction add_child(node, child) {\n    node.children.push(child);\n}\n\n// Function to update the subtree rooted at a node\nfunction update_subtree(node, k) {\n    node.value = (node.value + fib(k)) % MOD;\n    for (const child of node.children) {\n        update_subtree(child, k + 1);\n    }\n}\n\n// Function to calculate the kth Fibonacci number\nfunction fib(k) {\n    if (k <= 2) return 1;\n    let a = 1, b = 1;\n    for (let i = 3; i <= k; i++) {\n        [a, b] = [b, (a + b) % MOD];\n    }\n    return b;\n}\n\n// Read input\nconst [N, M] = input[idx++].split(' ').map(Number);\nconst nodes = Array(N).fill(0).map(() => new Node(0, -1));\nfor (let i = 1; i < N; i++) {\n    const parent = Number(input[idx++]);\n    nodes[i].parent = parent - 1;\n    add_child(nodes[parent - 1], nodes[i]);\n}\nfor (let i = 0; i < M; i++) {\n    const query = input[idx++].split(' ');\n    if (query[0] === 'Q') {\n        const node = Number(query[1]);\n        console.log(nodes[node - 1].value);\n    } else {\n        const [node, k] = query.slice(1).map(Number);\n        update_subtree(nodes[node - 1], k);\n    }\n}\n"
    }
  }
}