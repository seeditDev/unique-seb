{
  "questionId": "Q11732",
  "title": "Capacity to Ship Packages Within D Days",
  "slug": "capacity-to-ship-packages-within-d-days",
  "version": 1,
  "createdAt": "2026-07-05",
  "updatedAt": "2026-07-05",
  "metadata": {
    "category": "Greedy",
    "difficulty": "Medium",
    "isPremium": true,
    "tags": [
      "Binary Search",
      "Shipment"
    ],
    "companyTags": [],
    "topicTags": [],
    "QBCategory": "CCD"
  },
  "content": {
    "problemStatement": "You are the owner of a Shipment company. You use conveyor belts to ship packages from one port to another. The packages must be shipped within 'd' days.\nThe weights of the packages are given in an array 'of weights'. The packages are loaded on the conveyor belts every day in the same order as they appear in the array. The loaded weights must not exceed the maximum weight capacity of the ship.\nFind out the least-weight capacity so that you can ship all the packages within 'd' days .",
    "constraints": [
      "1 <= d <= 10^5",
      "1 <= N <= 5*10^4",
      "1 <= weights[i] <= 5*10^4",
      "1 <= sum of weights <= 10^9"
    ],
    "inputFormat": "N = number of packages, weights = array of package weights, d = number of days",
    "outputFormat": "minimum ship capacity needed to ship all packages within d days",
    "notes": [],
    "sampleTestCases": [
      {
        "id": "sample_1",
        "type": "reg",
        "input": "N = 5, weights = [5, 4, 5, 2, 3, 4, 5, 6], d = 5",
        "output": "9",
        "explanation": "The minimum ship capacity needed to ship all packages within 5 days is 9."
      },
      {
        "id": "sample_2",
        "type": "reg",
        "input": "N = 3, weights = [1, 2, 3, 4, 5], d = 2",
        "output": "9",
        "explanation": "With a capacity of 9, the packages can be shipped in 2 days as [1,2,3,4] and [5]."
      }
    ]
  },
  "testCases": {
    "hidden": [
      {
        "id": "tc_01",
        "type": "reg",
        "label": "min boundary",
        "input": "N = 1, weights = [1], d = 1",
        "expectedOutput": "1",
        "weight": 4
      },
      {
        "id": "tc_02",
        "type": "reg",
        "label": "max boundary",
        "input": "N = 5, weights = [100000, 100000, 100000, 100000, 100000], d = 5",
        "expectedOutput": "100000",
        "weight": 4
      },
      {
        "id": "tc_03",
        "type": "reg",
        "label": "uniform case",
        "input": "N = 5, weights = [5, 5, 5, 5, 5], d = 3",
        "expectedOutput": "10",
        "weight": 4
      },
      {
        "id": "tc_04",
        "type": "reg",
        "label": "tricky case",
        "input": "N = 5, weights = [1, 2, 3, 4, 5], d = 1",
        "expectedOutput": "15",
        "weight": 4
      },
      {
        "id": "tc_05",
        "type": "reg",
        "label": "mixed case",
        "input": "N = 8, weights = [5, 4, 5, 2, 3, 4, 5, 6], d = 5",
        "expectedOutput": "9",
        "weight": 4
      },
      {
        "id": "tc_06",
        "type": "reg",
        "label": "min boundary",
        "input": "N = 1, weights = [1], d = 1",
        "expectedOutput": "1",
        "weight": 4
      },
      {
        "id": "tc_07",
        "type": "reg",
        "label": "max boundary",
        "input": "N = 5, weights = [100000, 100000, 100000, 100000, 100000], d = 5",
        "expectedOutput": "100000",
        "weight": 4
      },
      {
        "id": "tc_08",
        "type": "reg",
        "label": "uniform case",
        "input": "N = 5, weights = [5, 5, 5, 5, 5], d = 3",
        "expectedOutput": "10",
        "weight": 4
      },
      {
        "id": "tc_09",
        "type": "reg",
        "label": "tricky case",
        "input": "N = 5, weights = [1, 2, 3, 4, 5], d = 1",
        "expectedOutput": "15",
        "weight": 4
      },
      {
        "id": "tc_10",
        "type": "reg",
        "label": "mixed case",
        "input": "N = 8, weights = [5, 4, 5, 2, 3, 4, 5, 6], d = 5",
        "expectedOutput": "9",
        "weight": 4
      },
      {
        "id": "tc_11",
        "type": "reg",
        "label": "min boundary",
        "input": "N = 1, weights = [1], d = 1",
        "expectedOutput": "1",
        "weight": 3
      },
      {
        "id": "tc_12",
        "type": "reg",
        "label": "max boundary",
        "input": "N = 5, weights = [100000, 100000, 100000, 100000, 100000], d = 5",
        "expectedOutput": "100000",
        "weight": 3
      },
      {
        "id": "tc_13",
        "type": "reg",
        "label": "uniform case",
        "input": "N = 5, weights = [5, 5, 5, 5, 5], d = 3",
        "expectedOutput": "10",
        "weight": 3
      },
      {
        "id": "tc_14",
        "type": "reg",
        "label": "tricky case",
        "input": "N = 5, weights = [1, 2, 3, 4, 5], d = 1",
        "expectedOutput": "15",
        "weight": 3
      },
      {
        "id": "tc_15",
        "type": "reg",
        "label": "mixed case",
        "input": "N = 8, weights = [5, 4, 5, 2, 3, 4, 5, 6], d = 5",
        "expectedOutput": "9",
        "weight": 3
      },
      {
        "id": "tc_16",
        "type": "reg",
        "label": "min boundary",
        "input": "N = 1, weights = [1], d = 1",
        "expectedOutput": "1",
        "weight": 3
      },
      {
        "id": "tc_17",
        "type": "reg",
        "label": "max boundary",
        "input": "N = 5, weights = [100000, 100000, 100000, 100000, 100000], d = 5",
        "expectedOutput": "100000",
        "weight": 3
      },
      {
        "id": "tc_18",
        "type": "reg",
        "label": "uniform case",
        "input": "N = 5, weights = [5, 5, 5, 5, 5], d = 3",
        "expectedOutput": "10",
        "weight": 3
      },
      {
        "id": "tc_19",
        "type": "reg",
        "label": "tricky case",
        "input": "N = 5, weights = [1, 2, 3, 4, 5], d = 1",
        "expectedOutput": "15",
        "weight": 3
      },
      {
        "id": "tc_20",
        "type": "reg",
        "label": "mixed case",
        "input": "N = 8, weights = [5, 4, 5, 2, 3, 4, 5, 6], d = 5",
        "expectedOutput": "9",
        "weight": 3
      },
      {
        "id": "tc_21",
        "type": "reg",
        "label": "min boundary",
        "input": "N = 1, weights = [1], d = 1",
        "expectedOutput": "1",
        "weight": 3
      },
      {
        "id": "tc_22",
        "type": "reg",
        "label": "max boundary",
        "input": "N = 5, weights = [100000, 100000, 100000, 100000, 100000], d = 5",
        "expectedOutput": "100000",
        "weight": 3
      },
      {
        "id": "tc_23",
        "type": "reg",
        "label": "uniform case",
        "input": "N = 5, weights = [5, 5, 5, 5, 5], d = 3",
        "expectedOutput": "10",
        "weight": 3
      },
      {
        "id": "tc_24",
        "type": "reg",
        "label": "tricky case",
        "input": "N = 5, weights = [1, 2, 3, 4, 5], d = 1",
        "expectedOutput": "15",
        "weight": 3
      },
      {
        "id": "tc_25",
        "type": "reg",
        "label": "mixed case",
        "input": "N = 8, weights = [5, 4, 5, 2, 3, 4, 5, 6], d = 5",
        "expectedOutput": "9",
        "weight": 3
      },
      {
        "id": "tc_26",
        "type": "reg",
        "label": "min boundary",
        "input": "N = 1, weights = [1], d = 1",
        "expectedOutput": "1",
        "weight": 3
      },
      {
        "id": "tc_27",
        "type": "reg",
        "label": "max boundary",
        "input": "N = 5, weights = [100000, 100000, 100000, 100000, 100000], d = 5",
        "expectedOutput": "100000",
        "weight": 3
      },
      {
        "id": "tc_28",
        "type": "reg",
        "label": "uniform case",
        "input": "N = 5, weights = [5, 5, 5, 5, 5], d = 3",
        "expectedOutput": "10",
        "weight": 3
      },
      {
        "id": "tc_29",
        "type": "reg",
        "label": "tricky case",
        "input": "N = 5, weights = [1, 2, 3, 4, 5], d = 1",
        "expectedOutput": "15",
        "weight": 3
      },
      {
        "id": "tc_30",
        "type": "reg",
        "label": "mixed case",
        "input": "N = 8, weights = [5, 4, 5, 2, 3, 4, 5, 6], d = 5",
        "expectedOutput": "9",
        "weight": 3
      }
    ],
    "totalTestCases": 30,
    "totalWeight": 100
  },
  "scoring": {
    "defaultScoringType": "PARTIAL_SCORE",
    "testCaseWeights": [
      4,
      4,
      4,
      4,
      4,
      4,
      4,
      4,
      4,
      4,
      3,
      3,
      3,
      3,
      3,
      3,
      3,
      3,
      3,
      3,
      3,
      3,
      3,
      3,
      3,
      3,
      3,
      3,
      3,
      3
    ],
    "maxScore": 100
  },
  "judging": {
    "timeLimitMs": 2000,
    "memoryLimitMb": 256,
    "supportedLanguages": [
      "C",
      "C++",
      "Java",
      "Python3",
      "JavaScript"
    ],
    "notes": []
  },
  "solution": {
    "approach": "Use binary search to find the minimum capacity that can ship all packages within d days",
    "timeComplexity": "O(N log S)",
    "spaceComplexity": "O(1)",
    "code": {
      "C": "#include <stdio.h>\nint daysNeeded(int* weights, int weightsSize, int capacity) {\n    int days = 1, currentLoad = 0;\n    for (int i = 0; i < weightsSize; i++) {\n        if (currentLoad + weights[i] > capacity) {\n            days++;\n            currentLoad = weights[i];\n        } else {\n            currentLoad += weights[i];\n        }\n    }\n    return days;\n}\n\nint shipWithinDays(int* weights, int weightsSize, int d) {\n    int left = weights[0], right = 0;\n    for (int i = 0; i < weightsSize; i++) {\n        left = (left > weights[i]) ? left : weights[i];\n        right += weights[i];\n    }\n    while (left < right) {\n        int mid = left + (right - left) / 2;\n        if (daysNeeded(weights, weightsSize, mid) <= d) {\n            right = mid;\n        } else {\n            left = mid + 1;\n        }\n    }\n    return left;\n}\n\nint main() {\n    int weights[] = {5, 4, 5, 2, 3, 4, 5, 6};\n    int d = 5;\n    printf(\"%d\\n\", shipWithinDays(weights, 8, d));\n    return 0;\n}",
      "C++": "#include <bits/stdc++.h>\nusing namespace std;\n\nclass Solution {\npublic:\n    // Function to check how many days needed for given capacity\n    int daysNeeded(vector<int>& weights, int capacity) {\n        // Initialize day count to 1\n        int days = 1;\n        // Current load for the day\n        int currentLoad = 0;\n\n        // Iterate over all package weights\n        for (int w : weights) {\n            // If adding weight exceeds capacity\n            if (currentLoad + w > capacity) {\n                // Increase day count and reset load\n                days++;\n                currentLoad = w;\n            } else {\n                // Otherwise, add weight to current load\n                currentLoad += w;\n            }\n        }\n        // Return total days needed\n        return days;\n    }\n\n    // Function to find minimum ship capacity to ship in d days\n    int shipWithinDays(vector<int>& weights, int d) {\n        // Find maximum weight as minimum capacity\n        int left = *max_element(weights.begin(), weights.end());\n        // Find total sum as maximum capacity\n        int right = accumulate(weights.begin(), weights.end(), 0);\n\n        // Iterate from minimum to maximum capacity\n        while (left < right) {\n            int mid = left + (right - left) / 2;\n            // Calculate days needed for current capacity\n            int needed = daysNeeded(weights, mid);\n            // If days needed are less than or equal to d, return capacity\n            if (needed <= d) {\n                right = mid;\n            } else {\n                left = mid + 1;\n            }\n        }\n        // Return minimum capacity\n        return left;\n    }\n};\n\nint main() {\n    // Input weights\n    vector<int> weights = {5,4,5,2,3,4,5,6};\n    // Days to ship\n    int d = 5;\n    // Create Solution instance\n    Solution sol;\n    // Call the function and print result\n    cout << sol.shipWithinDays(weights, d) << \"\\n\";\n    return 0;\n}",
      "Java": "import java.util.*;\n\nclass Solution {\n    // Function to check how many days needed for given capacity\n    int daysNeeded(int[] weights, int capacity) {\n        // Initialize day count to 1\n        int days = 1;\n        // Current load for the day\n        int currentLoad = 0;\n\n        // Iterate over all package weights\n        for (int w : weights) {\n            // If adding weight exceeds capacity\n            if (currentLoad + w > capacity) {\n                // Increase day count and reset load\n                days++;\n                currentLoad = w;\n            } else {\n                // Otherwise, add weight to current load\n                currentLoad += w;\n            }\n        }\n        // Return total days needed\n        return days;\n    }\n\n    // Function to find minimum ship capacity to ship in d days\n    int shipWithinDays(int[] weights, int d) {\n        // Find maximum weight as minimum capacity\n        int left = Arrays.stream(weights).max().getAsInt();\n        // Find total sum as maximum capacity\n        int right = Arrays.stream(weights).sum();\n\n        // Iterate from minimum to maximum capacity\n        while (left < right) {\n            int mid = left + (right - left) / 2;\n            // Calculate days needed for current capacity\n            int needed = daysNeeded(weights, mid);\n            // If days needed are less than or equal to d, return capacity\n            if (needed <= d) {\n                right = mid;\n            } else {\n                left = mid + 1;\n            }\n        }\n        // Return minimum capacity\n        return left;\n    }\n}\n\npublic class Main {\n    public static void main(String[] args) {\n        // Input weights\n        int[] weights = {5,4,5,2,3,4,5,6};\n        // Days to ship\n        int d = 5;\n        // Create Solution instance\n        Solution sol = new Solution();\n        // Call the function and print result\n        System.out.println(sol.shipWithinDays(weights, d));\n    }\n}",
      "Python3": "class Solution:\n    # Function to check how many days needed for given capacity\n    def daysNeeded(self, weights, capacity):\n        # Initialize day count to 1\n        days = 1\n        # Current load for the day\n        currentLoad = 0\n\n        # Iterate over all package weights\n        for w in weights:\n            # If adding weight exceeds capacity\n            if currentLoad + w > capacity:\n                # Increase day count and reset load\n                days += 1\n                currentLoad = w\n            else:\n                # Otherwise, add weight to current load\n                currentLoad += w\n        # Return total days needed\n        return days\n\n    # Function to find minimum ship capacity to ship in d days\n    def shipWithinDays(self, weights, d):\n        # Find maximum weight as minimum capacity\n        left = max(weights)\n        # Find total sum as maximum capacity\n        right = sum(weights)\n\n        # Iterate from minimum to maximum capacity\n        while left < right:\n            mid = left + (right - left) // 2\n            # Calculate days needed for current capacity\n            needed = self.daysNeeded(weights, mid)\n            # If days needed are less than or equal to d, return capacity\n            if needed <= d:\n                right = mid\n            else:\n                left = mid + 1\n        # Return minimum capacity\n        return left\n\n\nif __name__ == \"__main__\":\n    # Input weights\n    weights = [5,4,5,2,3,4,5,6]\n    # Days to ship\n    d = 5\n    # Create Solution instance\n    sol = Solution()\n    # Call the function and print result\n    print(sol.shipWithinDays(weights, d))",
      "JavaScript": "class Solution {\n    // Function to check how many days needed for given capacity\n    daysNeeded(weights, capacity) {\n        // Initialize day count to 1\n        let days = 1;\n        // Current load for the day\n        let currentLoad = 0;\n\n        // Iterate over all package weights\n        for (let w of weights) {\n            // If adding weight exceeds capacity\n            if (currentLoad + w > capacity) {\n                // Increase day count and reset load\n                days++;\n                currentLoad = w;\n            } else {\n                // Otherwise, add weight to current load\n                currentLoad += w;\n            }\n        }\n        // Return total days needed\n        return days;\n    }\n\n    // Function to find minimum ship capacity to ship in d days\n    shipWithinDays(weights, d) {\n        // Find maximum weight as minimum capacity\n        let left = Math.max(...weights);\n        // Find total sum as maximum capacity\n        let right = weights.reduce((a, b) => a + b, 0);\n\n        // Iterate from minimum to maximum capacity\n        while (left < right) {\n            let mid = left + Math.floor((right - left) / 2);\n            // Calculate days needed for current capacity\n            let needed = this.daysNeeded(weights, mid);\n            // If days needed are less than or equal to d, return capacity\n            if (needed <= d) {\n                right = mid;\n            } else {\n                left = mid + 1;\n            }\n        }\n        // Return minimum capacity\n        return left;\n    }\n}\n\n// Driver code\nconst weights = [5,4,5,2,3,4,5,6];\nconst d = 5;\nconst sol = new Solution();\nconsole.log(sol.shipWithinDays(weights, d));"
    },
    "notes": [],
    "editorial": {
      "bruteForce": {
        "description": "",
        "complexity": ""
      },
      "better": {
        "description": "",
        "complexity": ""
      },
      "optimal": {
        "description": "",
        "complexity": ""
      }
    }
  },
  "hints": [],
  "followUps": [],
  "learningObjectives": [],
  "similarProblems": [],
  "acceptanceRate": null,
  "estimatedSolveTime": "",
  "recommendedAfter": [],
  "boilerPlates": {
    "C": "#include <stdio.h>\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "C++": "#include <iostream>\nusing namespace std;\n\nint main() {\n    // your code goes here\n    return 0;\n}",
    "Java": "import java.util.*;\nimport java.io.*;\n\npublic class Main {\n    public static void main(String[] args) throws IOException {\n        // your code goes here\n    }\n}",
    "Python3": "# your code goes here\n",
    "JavaScript": "// your code goes here\n",
    "solution": {
      "approach": "Use binary search to find the minimum capacity that can ship all packages within d days",
      "timeComplexity": "O(N log S)",
      "spaceComplexity": "O(1)",
      "code": {
        "C": "#include <stdio.h>\nint daysNeeded(int* weights, int weightsSize, int capacity) {\n    int days = 1, currentLoad = 0;\n    for (int i = 0; i < weightsSize; i++) {\n        if (currentLoad + weights[i] > capacity) {\n            days++;\n            currentLoad = weights[i];\n        } else {\n            currentLoad += weights[i];\n        }\n    }\n    return days;\n}\n\nint shipWithinDays(int* weights, int weightsSize, int d) {\n    int left = weights[0], right = 0;\n    for (int i = 0; i < weightsSize; i++) {\n        left = (left > weights[i]) ? left : weights[i];\n        right += weights[i];\n    }\n    while (left < right) {\n        int mid = left + (right - left) / 2;\n        if (daysNeeded(weights, weightsSize, mid) <= d) {\n            right = mid;\n        } else {\n            left = mid + 1;\n        }\n    }\n    return left;\n}\n\nint main() {\n    int weights[] = {5, 4, 5, 2, 3, 4, 5, 6};\n    int d = 5;\n    printf(\"%d\\n\", shipWithinDays(weights, 8, d));\n    return 0;\n}",
        "C++": "#include <bits/stdc++.h>\nusing namespace std;\n\nclass Solution {\npublic:\n    // Function to check how many days needed for given capacity\n    int daysNeeded(vector<int>& weights, int capacity) {\n        // Initialize day count to 1\n        int days = 1;\n        // Current load for the day\n        int currentLoad = 0;\n\n        // Iterate over all package weights\n        for (int w : weights) {\n            // If adding weight exceeds capacity\n            if (currentLoad + w > capacity) {\n                // Increase day count and reset load\n                days++;\n                currentLoad = w;\n            } else {\n                // Otherwise, add weight to current load\n                currentLoad += w;\n            }\n        }\n        // Return total days needed\n        return days;\n    }\n\n    // Function to find minimum ship capacity to ship in d days\n    int shipWithinDays(vector<int>& weights, int d) {\n        // Find maximum weight as minimum capacity\n        int left = *max_element(weights.begin(), weights.end());\n        // Find total sum as maximum capacity\n        int right = accumulate(weights.begin(), weights.end(), 0);\n\n        // Iterate from minimum to maximum capacity\n        while (left < right) {\n            int mid = left + (right - left) / 2;\n            // Calculate days needed for current capacity\n            int needed = daysNeeded(weights, mid);\n            // If days needed are less than or equal to d, return capacity\n            if (needed <= d) {\n                right = mid;\n            } else {\n                left = mid + 1;\n            }\n        }\n        // Return minimum capacity\n        return left;\n    }\n};\n\nint main() {\n    // Input weights\n    vector<int> weights = {5,4,5,2,3,4,5,6};\n    // Days to ship\n    int d = 5;\n    // Create Solution instance\n    Solution sol;\n    // Call the function and print result\n    cout << sol.shipWithinDays(weights, d) << \"\\n\";\n    return 0;\n}",
        "Java": "import java.util.*;\n\nclass Solution {\n    // Function to check how many days needed for given capacity\n    int daysNeeded(int[] weights, int capacity) {\n        // Initialize day count to 1\n        int days = 1;\n        // Current load for the day\n        int currentLoad = 0;\n\n        // Iterate over all package weights\n        for (int w : weights) {\n            // If adding weight exceeds capacity\n            if (currentLoad + w > capacity) {\n                // Increase day count and reset load\n                days++;\n                currentLoad = w;\n            } else {\n                // Otherwise, add weight to current load\n                currentLoad += w;\n            }\n        }\n        // Return total days needed\n        return days;\n    }\n\n    // Function to find minimum ship capacity to ship in d days\n    int shipWithinDays(int[] weights, int d) {\n        // Find maximum weight as minimum capacity\n        int left = Arrays.stream(weights).max().getAsInt();\n        // Find total sum as maximum capacity\n        int right = Arrays.stream(weights).sum();\n\n        // Iterate from minimum to maximum capacity\n        while (left < right) {\n            int mid = left + (right - left) / 2;\n            // Calculate days needed for current capacity\n            int needed = daysNeeded(weights, mid);\n            // If days needed are less than or equal to d, return capacity\n            if (needed <= d) {\n                right = mid;\n            } else {\n                left = mid + 1;\n            }\n        }\n        // Return minimum capacity\n        return left;\n    }\n}\n\npublic class Main {\n    public static void main(String[] args) {\n        // Input weights\n        int[] weights = {5,4,5,2,3,4,5,6};\n        // Days to ship\n        int d = 5;\n        // Create Solution instance\n        Solution sol = new Solution();\n        // Call the function and print result\n        System.out.println(sol.shipWithinDays(weights, d));\n    }\n}",
        "Python3": "class Solution:\n    # Function to check how many days needed for given capacity\n    def daysNeeded(self, weights, capacity):\n        # Initialize day count to 1\n        days = 1\n        # Current load for the day\n        currentLoad = 0\n\n        # Iterate over all package weights\n        for w in weights:\n            # If adding weight exceeds capacity\n            if currentLoad + w > capacity:\n                # Increase day count and reset load\n                days += 1\n                currentLoad = w\n            else:\n                # Otherwise, add weight to current load\n                currentLoad += w\n        # Return total days needed\n        return days\n\n    # Function to find minimum ship capacity to ship in d days\n    def shipWithinDays(self, weights, d):\n        # Find maximum weight as minimum capacity\n        left = max(weights)\n        # Find total sum as maximum capacity\n        right = sum(weights)\n\n        # Iterate from minimum to maximum capacity\n        while left < right:\n            mid = left + (right - left) // 2\n            # Calculate days needed for current capacity\n            needed = self.daysNeeded(weights, mid)\n            # If days needed are less than or equal to d, return capacity\n            if needed <= d:\n                right = mid\n            else:\n                left = mid + 1\n        # Return minimum capacity\n        return left\n\n\nif __name__ == \"__main__\":\n    # Input weights\n    weights = [5,4,5,2,3,4,5,6]\n    # Days to ship\n    d = 5\n    # Create Solution instance\n    sol = Solution()\n    # Call the function and print result\n    print(sol.shipWithinDays(weights, d))",
        "JavaScript": "class Solution {\n    // Function to check how many days needed for given capacity\n    daysNeeded(weights, capacity) {\n        // Initialize day count to 1\n        let days = 1;\n        // Current load for the day\n        let currentLoad = 0;\n\n        // Iterate over all package weights\n        for (let w of weights) {\n            // If adding weight exceeds capacity\n            if (currentLoad + w > capacity) {\n                // Increase day count and reset load\n                days++;\n                currentLoad = w;\n            } else {\n                // Otherwise, add weight to current load\n                currentLoad += w;\n            }\n        }\n        // Return total days needed\n        return days;\n    }\n\n    // Function to find minimum ship capacity to ship in d days\n    shipWithinDays(weights, d) {\n        // Find maximum weight as minimum capacity\n        let left = Math.max(...weights);\n        // Find total sum as maximum capacity\n        let right = weights.reduce((a, b) => a + b, 0);\n\n        // Iterate from minimum to maximum capacity\n        while (left < right) {\n            let mid = left + Math.floor((right - left) / 2);\n            // Calculate days needed for current capacity\n            let needed = this.daysNeeded(weights, mid);\n            // If days needed are less than or equal to d, return capacity\n            if (needed <= d) {\n                right = mid;\n            } else {\n                left = mid + 1;\n            }\n        }\n        // Return minimum capacity\n        return left;\n    }\n}\n\n// Driver code\nconst weights = [5,4,5,2,3,4,5,6];\nconst d = 5;\nconst sol = new Solution();\nconsole.log(sol.shipWithinDays(weights, d));"
      }
    },
    "_internal": {
      "methodName": "",
      "params": [],
      "phase": "ready-for-judge"
    },
    "verified": true
  },
  "_internal": {},
  "verified": false
}
